import fs from "fs";
const listeners = {};
global.window = global;
global.requestAnimationFrame = () => 0;
class FakeImg {
  constructor() { this.complete = true; this.naturalWidth = 1186; this.naturalHeight = 926; this._src = ""; }
  set src(v) {
    this._src = v;
    if (v.includes("rival_flip")) { this.naturalWidth = 885; this.naturalHeight = 956; }
    else if (v.includes("rival")) { this.naturalWidth = 877; this.naturalHeight = 945; }
    else if (v.includes("courtyard")) { this.naturalWidth = 1536; this.naturalHeight = 1024; }
    else { this.naturalWidth = 1186; this.naturalHeight = 926; }
  }
  get src() { return this._src; }
}
global.Image = FakeImg;
global.Audio = class { constructor(src){ this.src=src||""; this.volume=1; this.loop=false; this.muted=false; this.currentTime=0; } cloneNode(){return new global.Audio(this.src);} play(){return Promise.resolve();} pause(){} };
const ctxStub = new Proxy({}, { get: () => () => {}, set: () => true });
const canvas = { width: 1280, height: 720, getContext: () => ctxStub };
global.document = { getElementById: () => canvas, createElement: () => ({ width:1,height:1,getContext:()=>ctxStub }) };
global.addEventListener = (t, fn) => { (listeners[t] ||= []).push(fn); };
global.KeyboardEvent = class KeyboardEvent {
  constructor(type, init = {}) {
    this.type = type;
    this.code = init.code || "";
    this.repeat = false;
    this.bubbles = !!init.bubbles;
    this.cancelable = !!init.cancelable;
    this.preventDefault = () => {};
  }
};
global.dispatchEvent = function (e) {
  for (const fn of (listeners[e.type] || [])) fn(e);
};
const __ls = new Map();
global.localStorage = {
  getItem: (k) => (__ls.has(k) ? __ls.get(k) : null),
  setItem: (k, v) => { __ls.set(String(k), String(v)); },
  removeItem: (k) => { __ls.delete(k); },
  clear: () => { __ls.clear(); },
};
global.navigator = global.navigator || {};
global.navigator.getGamepads = () => [];

let src = fs.readFileSync("/workspace/estudio/vispera/game.js", "utf8");
src = src.replace("(() => {\n  \"use strict\";", "globalThis.__V = (() => {\n  \"use strict\";");
src = src.replace("  requestAnimationFrame(loop);\n})();", `
  return {
    get player(){return player}, get rival(){return rival},
    get mode(){return mode}, get modeT(){return modeT},
    get slashBuf(){return slashBuf}, get golpeBuf(){return golpeBuf}, get boltBuf(){return boltBuf}, get throwBuf(){return throwBuf}, get reversalBuf(){return reversalBuf}, get openBuf(){return openBuf},
    get hitstopLeft(){return hitstopLeft}, set hitstopLeft(v){hitstopLeft=v}, get openLeft(){return openLeft}, set openLeft(v){openLeft=v},
    get hintIn(){return hintIn}, set hintIn(v){hintIn=v},
    get hintFade(){return hintFade}, set hintFade(v){hintFade=v},
    get hintDone(){return hintDone}, set hintDone(v){hintDone=!!v},
    get hintAge(){return hintAge}, set hintAge(v){hintAge=+v||0},
    get hintVerbN(){return hintVerbN}, set hintVerbN(v){hintVerbN=v|0},
    get hintVerbs(){return hintVerbs},
    noteHintVerb, armHintFade, controlsHintLines, HINT_HOLD_MS, HINT_VERB_N, rotateYardForContrast, OVER_TIPS, get overTipI(){return overTipI}, set overTipI(v){overTipI=Math.max(0,v|0)}, STAGE_LABELS, STAGE_ITEMS,
    get shake(){return shake}, get shakeDur(){return shakeDur}, get shakeMag(){return shakeMag}, get shakeDir(){return shakeDir},
    get yardIndex(){return yardIndex}, set yardIndex(v){yardIndex=Math.max(0,Math.min(4,v|0)); persistYard();}, setYardIndex, YARD_KEYS, YARD_SWITCH_MS, get yardSwitchT(){return yardSwitchT}, set yardSwitchT(v){yardSwitchT=Math.max(0,v|0)}, get yardPrevIndex(){return yardPrevIndex}, set yardPrevIndex(v){yardPrevIndex=v|0}, yardSwitchK, drawYardCrop, throwKnifeBitmap,
    get clashSparkT(){return clashSparkT},
    get hitSparkX(){return hitSparkX}, get hitSparkY(){return hitSparkY}, get hitSparkT(){return hitSparkT},
    get steelFlashT(){return steelFlashT}, get steelX(){return steelX}, get steelY(){return steelY}, get settleT(){return settleT}, get settleMax(){return settleMax}, settleDip, SETTLE_MS, stampNow, get hitFlashT(){return hitFlashT}, HIT_FLASH_MS, hurtFlashK, steelFlashK, parryGleamK, clashSparkK, hitSparkK, brasaHitK, plantDustK, dmgNumK, hudFlashK, comboK, meterFlashK, meterGainK, METER_FLASH_MS, METER_GAIN_MS, HUD_FLASH_MS, COMBO_SHOW_MS, noteCombo,
    get brasaFxT(){return brasaFxT}, get brasaFxKind(){return brasaFxKind},
    get brasaX(){return brasaX}, get brasaY(){return brasaY},
    get brasaFxScale(){return brasaFxScale},
    get attackEdge(){return attackEdge}, set attackEdge(v){attackEdge=!!v}, get golpeEdge(){return golpeEdge}, set golpeEdge(v){golpeEdge=!!v}, get boltEdge(){return boltEdge}, set boltEdge(v){boltEdge=!!v}, get throwEdge(){return throwEdge}, set throwEdge(v){throwEdge=!!v}, get reversalEdge(){return reversalEdge}, set reversalEdge(v){reversalEdge=!!v}, get feintEdge(){return feintEdge}, set feintEdge(v){feintEdge=!!v}, set mode(v){mode=v},
    keys, update, resetRound, bodyGap, absGap, bladeReach, bladeBox, bodyAABB, hitbox, destRect, poseBitmap, poseSheet, poseFamily, poseScale, keepApart, throwGrabHold, throwSnapTogether, inMotionFeel, walkFade, sheatheFade, wakeupFade, crumpleFade, slashPose, boltPlantFade, throwPlantFade, telegraphFade, feintFade, parryFade, hurtFade, telegraphing, guardRaiseK, idleBreath, walkSettleK, guardDropFade, reversalPlanting, reversalPlantFade, throwGuardPlantFade, throwGuardPlanting, techGuardPlantFade, techGuardPlanting, clashPlantFade, linkPlantFade, holdCutFade, holdCutPlanting, slashLeftoverPlanting, golpeLeftoverPlanting, boltLeftoverPlanting, stunLeftoverPlanting, throwKdLeftoverPlanting, hurtBitmap, windupBitmap, blockBitmap, gaitWalkOn, walkBitmap, recoveryWalkOut, recoveryWalkResting, WALK_STEP_MS, WALK_SETTLE_MS, GUARD_RAISE_MS, FALL_MS, canFeint, startFeint, rivalTryFeint,
    cutPoint, syncHitSpark, spawnHitSpark, get hitShards(){return hitShards}, syncClashSpark, spawnClashSpark, syncSteelFlash, spawnSteelFlash, get steelTipRide(){return steelTipRide},
    canStartBolt, startBolt, startAttack, startThrow, startReversal, tripGuardBreak, tickRiposte, trySpendRiposteInput, tryFireRiposte, syncRipostePad, cutStartup, cutActive, cutRecovery, guardSteelPoint, landBlock, landParry, isPerfectParry, startWakeReversal, wakeupWindow, reversalInvuln, throwInRange, tryThrowTech, throwTechWindow, boltBox, bladeTipX, bladeTipY, castPlantXY, tryPushblock, awayWalkDir, playPushblockSting, playClashSting, playThrowTechSting, playGrabConnectSting, playFeintSting, playReversalSting, playGetupScrape, playHoldCutSting, cancelIntoBolt, landThrowTech, landThrow, landHit, landBoltHit, armHitInterruptFade, spawnBrasaFx, get lastClashSfx(){return lastClashSfx}, get lastThrowTechSfx(){return lastThrowTechSfx}, get lastGrabSfx(){return lastGrabSfx}, get lastFeintSfx(){return lastFeintSfx}, get lastReversalSfx(){return lastReversalSfx}, get lastGetupSfx(){return lastGetupSfx}, get lastHoldCutSfx(){return lastHoldCutSfx}, set lastHoldCutSfx(v){lastHoldCutSfx=String(v||"")}, get steelKind(){return steelKind},
    padDown, padUp, padChordHeld, padMarkChordLook,
    stickApply, stickRelease, get stickPtr(){return stickPtr}, set stickPtr(v){stickPtr=v}, confirmTitle, handleTitleKey, handleTitleBack, goTitleFromPlay,
    setBindCode, setPadBind, resetBindsDefaults, actionHeld, actionOfCode, applyActionEdge, bindPrimary, codeLabel,
    get binds(){return binds}, set binds(v){binds=sanitizeBinds(v); persistBinds();},
    get padBinds(){return padBinds},
    get volMusic(){return volMusic}, set volMusic(v){volMusic=Math.max(0,Math.min(100,v|0)); persistVol();},
    get volSfx(){return volSfx}, set volSfx(v){volSfx=Math.max(0,Math.min(100,v|0)); persistVol();},
    get musicBed(){return musicBed}, set musicBed(v){musicBed=String(v||"");},
    get musicFadeFrom(){return musicFadeFrom}, set musicFadeFrom(v){musicFadeFrom=String(v||"");},
    get musicFadeT(){return musicFadeT}, set musicFadeT(v){musicFadeT=Math.max(0,+v||0);},
    get musicUnlocked(){return musicUnlocked}, set musicUnlocked(v){musicUnlocked=!!v;},
    MUSIC, MUSIC_FADE_MS, musicWant, musicFadeK, musicGain, syncMusic, tickMusicFade, applyMusicVol, bedAudio,
    get remapCapture(){return remapCapture}, set remapCapture(v){remapCapture=v;},
    DEFAULT_BINDS, BIND_ACTIONS,
    get titleSel(){return titleSel}, set titleSel(v){titleSel=v|0},
    get titlePage(){return titlePage}, set titlePage(v){titlePage=String(v);},
    meterFull, gainMeter, rivalShouldSuper, boltRecMs,
    get lastBoltSfx(){return lastBoltSfx},
    get lastBoltConnectSfx(){return lastBoltConnectSfx},
    get lastPushblockSfx(){return lastPushblockSfx}, get lastRiposteSfx(){return lastRiposteSfx}, get lastKoSfx(){return lastKoSfx}, get koLanded(){return koLanded},
    playRiposteSpendSting, playRiposteHitSting, RIPOSTE_HIT_SHAKE,
    playParrySting, landParry, isPerfectParry, spawnParryGleam, syncParryGleam, drawParryGleam,
    get lastParrySfx(){return lastParrySfx}, set lastParrySfx(v){lastParrySfx=String(v||"")}, get parryGleamT(){return parryGleamT},
    PARRY_WIN_MS, PARRY_STAGGER_MS, PARRY_GLEAM_MS,
    get plantDust(){return plantDust},
    spawnPlantDust, syncPlantDust, dropBlockPlantUnderShove, dropPlantUnderClash, dropPlantUnderKnock,
    spawnDmgNum, syncDmgNums, hitWoundAnchor, get dmgNums(){return dmgNums}, DMG_NUM_MS, DMG_NUM_RISE,
    STARTUP, ACTIVE, RECOVERY, LUNGE_PX, GOLPE_STARTUP, GOLPE_ACTIVE, GOLPE_RECOVERY, GOLPE_LUNGE_PX, OPENING_MS, HINT_IN_MS, HINT_FADE_MS, HITSTUN, KNOCK_PX, KNOCK_MS, SLASH_BUFFER_MS, SLASH_CANCEL_MS, GOLPE_CANCEL_MS, BOLT_CANCEL_MS, HITSTOP_HIT, HITSTOP_BLOCK, STEEL_FLASH_MS, GUARD_PUSH_MS, CLASH_SPARK_MS, HIT_SPARK_MS, BRASA_HIT_MS, CLASH_RECOVERY, SHEATHE_MS, GUARD_COMMIT_MS, PUNCH_PX, punchCover, drawCourtyard, drawYardCrop, YARD_SWITCH_MS, yardSwitchK, doClash, bumpShake, beginFall, FALL_MS, goTitleFromPlay, get koTarget(){return koTarget}, set koTarget(v){koTarget=v;},
    MAX_HP, SLASH_DMG, RIPOSTE_WIN_MS, RIPOSTE_STARTUP, RIPOSTE_ACTIVE, RIPOSTE_RECOVERY, PARRY_WIN_MS, PARRY_STAGGER_MS, PARRY_GLEAM_MS, STAMINA_MAX, STAMINA_DRAIN, STAMINA_REGEN, STAMINA_REGEN_THREAT, STAMINA_REGEN_LOCK, STAMINA_REGEN_DELAY, STAMINA_START_MIN, STAMINA_BLOCK, PUSHBLOCK_STAM, PUSHBLOCK_PX, PUSHBLOCK_AI_CD, PUSHBLOCK_AI_CHANCE, PUSHBLOCK_FX, PUSHBLOCK_SHAKE, CLASH_SHAKE, CLASH_FX, TECH_SHAKE, TECH_FX, GRAB_SHAKE, GRAB_FX_MS, GUARD_BREAK_MS, GUARD_BREAK_SETTLE, GUARD_RAISE_MS,
    BOLT_STAM, BOLT_CHIP, BOLT_SUPER_CHIP, BOLT_STARTUP, BOLT_RECOVERY, BOLT_SPEED, BOLT_SUPER_SPEED, BOLT_W, BOLT_H, BOLT_SUPER_W, BOLT_SUPER_H, BOLT_AI_CD, THROW_AI_CD, BOLT_CAST_FX_MS,
    BOLT_SUPER_DMG, BOLT_SUPER_RECOVERY, BOLT_SUPER_STOP, BOLT_SUPER_FX, METER_MAX, METER_HIT, METER_BLOCK_SPECIAL, METER_FLASH_MS, METER_GAIN_MS, COMBO_SHOW_MS, SUPER_RANGE,
    THROW_STARTUP, THROW_ACTIVE, THROW_RECOVERY, THROW_DMG, THROW_RANGE, THROW_SNAP_GAP, THROW_KD_MS, THROW_STAM, THROW_TECH_MS, THROW_TECH_REC, THROW_TECH_CHANCE, THROW_WAKE_INVULN,
    REVERSAL_STAM, REVERSAL_INVULN, REVERSAL_AI_CD, REVERSAL_AI_CHANCE,
    FEINT_RECOVERY, FEINT_FX_MS, FEINT_AI_CD, FEINT_AI_CHANCE,
    REVERSAL_FX_MS, WAKE_FX_MS,
    AI_LINK_CHANCE, AI_LINK_RANGE_CHANCE, AI_CLOSE_GOLPE, AI_CLOSE_SLASH, AI_CLOSE_BAIT, AI_CLOSE_RESET, AI_POST_RESET, AI_BAIT_MS_MIN, AI_BAIT_MS_MAX, AI_RESET_MS, AI_RETREAT_MS_MIN, AI_RETREAT_MS_MAX, AI_MID_DART, AI_MID_WALK_PAUSE,
    rivalIdleCommit, rivalLinkRoll, rivalDoPostReset, rivalTryDartNow,
    throwLocked, rivalTryThrowTech, armRivalThrowTech, rivalTryReversal, armRivalReversal, meatySpaceAtGuard, rivalTryWakeReversal, armRivalWakeReversal, meatySpaceAtWake, rivalTryPushblock, armRivalPushblock, rivalPushblockOnBlock,
    get bolt(){return bolt},
    REACH: (typeof REACH!=='undefined'?REACH:null),
  };
})();`);
eval(src);
const V = globalThis.__V;
const STEP = 1000/60;
const fail=[], pass=[];
function check(n,ok,ex=""){ (ok?pass:fail).push(n+(ex?" "+ex:"")); console.log(ok?"PASS":"FAIL", n, ex); }
function wait(ms){ for(let i=0;i<Math.ceil(ms/STEP);i++) V.update(STEP); }
function drainHitstop(){ for(let i=0;i<40;i++){ if(V.hitstopLeft<=0) break; V.update(STEP);} }
/** Title Space: resetRound only. Do NOT set attackEdge in play (that arms openBuf). */
function bootPlay(){ V.resetRound(); }
function toMeasure(){
  bootPlay();
  V.keys.clear();
  Math.random = () => 0.99;
  for(let i=0;i<900;i++){
    V.update(STEP);
    if(V.modeT>1600 && V.bodyGap()<=V.bladeReach(V.rival)+V.LUNGE_PX+4 && V.rival.hp===V.MAX_HP && V.player.hp===V.MAX_HP) break;
  }
}

const srcTxt = fs.readFileSync("/workspace/estudio/vispera/game.js","utf8");
const codeOnly = srcTxt.replace(/\/\*[\s\S]*?\*\//g,"").replace(/\/\/.*$/gm,"");
check("no REACH cap in bladeBox", !/x0 \+ REACH|x1 - REACH|tipCap|Math\.min\([^)]*REACH/.test(srcTxt));
check("tipX you/rival/flip", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt));
check("poseScale=worldScale", /return worldScale\(\);/.test(srcTxt));
check("frame data", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280);
check("hitstop/stun/buffer/lunge/knock", V.HITSTOP_HIT===140 && V.HITSTOP_BLOCK===60 && V.HITSTUN===350 && V.SLASH_BUFFER_MS===80 && V.LUNGE_PX===36 && V.KNOCK_PX===80);
check("clash 110", V.CLASH_SPARK_MS===110);
check("guard commit 140", V.GUARD_COMMIT_MS===140);
check("opening 1600", V.OPENING_MS===1600);
check("AABB fill", /body: \{ x: 84,/.test(srcTxt) && /body: \{ x: 301,/.test(srcTxt) && /body: \{ x: 108,/.test(srcTxt));
check("no live KO 1.25 rot", !/facing\s*\*\s*1\.25/.test(codeOnly) && !/\b0\.785\b/.test(codeOnly));

check("plant you idle", V.poseSheet({kind:"you",facing:1,phase:"idle",stunT:0,guarding:false,falling:false,img:null}).footX===75);

// title at cold boot (before any resetRound)
check("title", V.mode==="title");
wait(2000);
check("title holds", V.mode==="title");
check("hint idle on title", V.hintIn===0 && !V.hintDone && V.hintFade===0, `in=${V.hintIn} done=${V.hintDone} fade=${V.hintFade}`);
V.attackEdge = true; V.update(STEP);
check("title Space starts clean", V.mode==="play" && !V.openBuf && V.openLeft>=1590, `mode=${V.mode} buf=${V.openBuf} open=${V.openLeft}`);
check("hint arms on first fight", V.mode==="play" && !V.hintDone && V.hintFade===0, `done=${V.hintDone} fade=${V.hintFade}`);

// opening freeze
bootPlay();
let moved=false, phased=false, dmg=false;
for(let i=0;i<Math.ceil(1590/STEP);i++){
  V.update(STEP);
  if(V.rival.phase!=="idle") phased=true;
  if(V.rival.x<1140) moved=true;
  if(V.rival.hp<V.MAX_HP||V.player.hp<V.MAX_HP) dmg=true;
}
check("opening freeze", !phased && !moved && !dmg, `ph=${V.rival.phase} x=${V.rival.x.toFixed(1)}`);
check("hint in through opening", !V.hintDone && V.hintFade===0 && V.hintIn>=V.HINT_IN_MS-20, `in=${V.hintIn} done=${V.hintDone} fade=${V.hintFade}`);

// tip blade
toMeasure();
V.attackEdge=true;
while(V.player.phase!=="active" && V.player.phase!=="recovery") V.update(STEP);
const br=V.bladeReach(V.player);
const tip=V.poseSheet(V.player).tipX;
check("blade tipX no cap", tip===991 && br>150, `tip=${tip} blade=${br.toFixed(1)}`);

// recovery holds slash
toMeasure();
V.attackEdge=true;
let sawRec=false, tipRec=null;
for(let i=0;i<80;i++){
  V.update(STEP);
  if(V.player.phase==="recovery"){ sawRec=true; tipRec=V.poseSheet(V.player).tipX; break; }
}
check("recovery holds slash", sawRec && tipRec===991, `saw=${sawRec} tip=${tipRec}`);

// first hit at measure (full HP)
toMeasure();
const g0=V.bodyGap(), hold=V.bladeReach(V.rival)+V.LUNGE_PX;
check("full HP pre-hit", V.rival.hp===100, `hp=${V.rival.hp}`);
V.attackEdge=true;
for(let i=0;i<50;i++){ V.update(STEP); if(V.rival.hp<100||V.hitstopLeft>0) break; }
check("hit at measure", V.rival.hp===90, `g=${g0.toFixed(1)} hold=${hold.toFixed(1)}`);
check("hint done after first connect", V.hintDone, `done=${V.hintDone} fade=${V.hintFade}`);

// GAP idle
bootPlay(); wait(50);
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.stunT=0; V.player.pushT=0; V.player.guarding=false;
V.rival.phase="idle"; V.rival.phaseT=0; V.rival.gait=0; V.rival.stunT=0; V.rival.pushT=0; V.rival.guarding=false; V.rival.closing=false; V.rival.standWait=0; V.rival.standGoal=99999;
V.player.x=500; V.rival.x=560;
for(let i=0;i<5;i++){
  V.player.phase="idle"; V.player.gait=0; V.player.stunT=0; V.player.pushT=0;
  V.rival.phase="idle"; V.rival.gait=0; V.rival.stunT=0; V.rival.pushT=0; V.rival.closing=false; V.rival.standGoal=99999; V.rival.standWait=0;
  V.keepApart();
}
const gIdle=V.bodyGap();
check("idle GAP ~120 via keepApart", gIdle>=118 && gIdle<=125, `g=${gIdle.toFixed(1)}`);

bootPlay(); wait(1700);
V.player.x=500; V.rival.x=560;
for(let i=0;i<30;i++){
  V.player.phase="idle"; V.player.gait=0; V.player.stunT=0; V.player.pushT=0; V.player.guarding=false;
  V.rival.phase="idle"; V.rival.gait=0; V.rival.stunT=0; V.rival.pushT=0; V.rival.guarding=false; V.rival.closing=false;
  V.rival.standWait=0; V.rival.standGoal=99999;
  V.keys.clear();
  V.update(STEP);
}
const gUp=V.bodyGap();
check("idle GAP after update loop", gUp>=100, `g=${gUp.toFixed(1)}`);

V.player.phase="startup"; V.player.phaseT=50; V.player.x=500; V.rival.x=560;
V.keepApart();
check("GAP skipped motion", V.bodyGap()<100, `g=${V.bodyGap().toFixed(1)}`);

bootPlay(); wait(50);
V.player.x=700; V.rival.x=500;
for(let i=0;i<8;i++) V.update(STEP);
check("no cross", V.player.x<V.rival.x);

toMeasure();
const x0=V.player.x; V.attackEdge=true;
while(V.player.phase!=="active"&&V.player.phase!=="recovery") V.update(STEP);
check("lunge 36", (V.player.x-x0)>=30 && (V.player.x-x0)<=42, `dx=${(V.player.x-x0).toFixed(1)}`);

toMeasure();
V.attackEdge=true;
for(let i=0;i<50;i++){ V.update(STEP); if(V.rival.hp<100) break; }
const xHit=V.rival.x; drainHitstop();
for(let i=0;i<20;i++) V.update(STEP);
check("knock ~80", (V.rival.x-xHit)>=50 && (V.rival.x-xHit)<=110, `kb=${(V.rival.x-xHit).toFixed(1)}`);

toMeasure();
for(let i=0;i<800;i++){ V.update(STEP); if(V.player.stunT>0||V.player.hp<100) break; }
drainHitstop();
let bufSaw=false, fired=false;
for(let i=0;i<80;i++){ V.attackEdge=true; V.update(STEP); if(V.slashBuf) bufSaw=true; if(V.player.phase==="startup"&&V.player.stunT<=0){fired=true;break;} }
check("stun Space buffer", bufSaw&&fired, `buf=${bufSaw} fire=${fired}`);

toMeasure();
V.attackEdge=true;
for(let i=0;i<50;i++){ V.update(STEP); if(V.hitstopLeft>0) break; }
let hsBuf=false;
if(V.hitstopLeft>0){ V.attackEdge=true; V.update(STEP); hsBuf=!!V.slashBuf; }
check("hitstop Space buffer", hsBuf, `hs=${V.hitstopLeft} buf=${V.slashBuf}`);

toMeasure();
V.player.x=20; V.keepApart(); V.update(STEP);
check("left clamp dx>=0", V.destRect(V.player).dx>=-0.5, `dx=${V.destRect(V.player).dx.toFixed(1)}`);

bootPlay();
const y0=V.yardIndex;
Math.random=()=>0.99;
for(let i=0;i<900;i++){ V.update(STEP); if(V.modeT>1600 && V.bodyGap()<=V.bladeReach(V.rival)+V.LUNGE_PX+4) break; }
V.rival.hp=10; V.attackEdge=true;
for(let i=0;i<400;i++){ V.update(STEP); if(V.mode==="over") break; }
check("KO over", V.mode==="over", `mode=${V.mode}`);
V.attackEdge=true; V.update(STEP);
check("yard rotates on rematch contrast", V.yardIndex===(y0+1)%V.YARD_KEYS.length, `y0=${y0} y1=${V.yardIndex}`);
check("you left rival right", V.player.facing===1 && V.rival.facing===-1);

check("hp/stam contract", V.MAX_HP===100 && V.SLASH_DMG===10 && V.STAMINA_MAX===100 && V.STAMINA_DRAIN===40 && V.STAMINA_REGEN===25 && V.STAMINA_REGEN_THREAT===10 && V.STAMINA_REGEN_LOCK===1000 && V.STAMINA_REGEN_DELAY===200 && V.STAMINA_START_MIN===15 && V.GUARD_BREAK_MS===400 && V.STAMINA_BLOCK===20 && V.GUARD_BREAK_SETTLE===180);
check("HUD bar not 3 pips", /HUD_BAR_W = 252/.test(srcTxt) && !/HUD_SEG_W/.test(srcTxt) && !/function drawPips/.test(srcTxt) && !/fallenSegs/.test(srcTxt));
check("boot full bars", (()=>{ bootPlay(); return V.player.hp===100 && V.rival.hp===100 && V.player.stamina===100 && V.rival.stamina===100; })());

bootPlay(); wait(1700);
V.keys.add("KeyS");
wait(1000);
check("guard drain ~40/s", V.player.guarding && V.player.stamina>=58 && V.player.stamina<=62, `st=${V.player.stamina.toFixed(2)} g=${V.player.guarding}`);
V.keys.delete("KeyS");
V.update(STEP);
check("release drops guard", !V.player.guarding);
V.player.x = 150;
V.rival.x = 1150;
V.player.stamThreatLockT = 0;
V.rival.stamThreatLockT = 0;
const sHold = V.player.stamina;
wait(180);
check("regen delay 200ms", V.player.stamina<=sHold+0.8, `st=${V.player.stamina.toFixed(2)} s0=${sHold.toFixed(2)}`);
wait(400);
check("regen ~25/s", V.player.stamina>=sHold+8 && V.player.stamina<=sHold+13, `st=${V.player.stamina.toFixed(2)} s0=${sHold.toFixed(2)}`);

bootPlay(); wait(1700);
V.player.stamina = 14;
V.player.stamRegenT = 99999;
V.player.guardBreakT = 0;
V.player.guarding = false;
V.keys.add("KeyS");
V.update(STEP);
check("no start stam<15", !V.player.guarding, `g=${V.player.guarding} st=${V.player.stamina}`);

bootPlay(); wait(1700);
V.player.stamina = 20;
V.player.stamRegenT = 99999;
V.player.guardBreakT = 0;
V.player.phase = "idle";
V.player.stunT = 0;
V.keys.add("KeyS");
V.update(STEP);
check("raise at 20", V.player.guarding, `g=${V.player.guarding}`);
wait(200);
check("hold below 15", V.player.guarding && V.player.stamina<15, `g=${V.player.guarding} st=${V.player.stamina.toFixed(2)}`);

bootPlay(); wait(1700);
V.player.stamina = 1;
V.player.stamRegenT = 0;
V.player.guardBreakT = 0;
V.player.phase = "idle";
V.keys.add("KeyS");
V.update(STEP);
wait(80);
check("guard break at 0", !V.player.guarding && V.player.guardBreakT>0 && V.player.stamina<=0, `g=${V.player.guarding} br=${V.player.guardBreakT.toFixed(0)} st=${V.player.stamina}`);
V.player.stamina = 100;
V.update(STEP);
check("break lock 400ms", !V.player.guarding && V.player.guardBreakT>0, `g=${V.player.guarding} br=${V.player.guardBreakT.toFixed(0)}`);

toMeasure();
Math.random = () => 0;
V.rival.revCd = 99999;
V.rival.revArmed = -1;
V.attackEdge = true;
for(let i=0;i<80;i++){ V.rival.revCd = 99999; V.rival.revArmed = -1; V.rival.pbCd = 99999; V.rival.pbArmed = -1; V.update(STEP); if(V.hitstopLeft>0 || V.rival.hp<100) break; }
check("block 0 dmg", V.rival.hp===100, `hp=${V.rival.hp} g=${V.rival.guarding} push=${V.rival.pushT}`);
check("block costs ~20", V.rival.hp===100 && V.rival.stamina>=70 && V.rival.stamina<=82, `st=${V.rival.stamina.toFixed(2)}`);

toMeasure();
V.rival.stamina = 10;
V.rival.stamRegenT = 99999;
V.rival.guardBreakT = 0;
V.rival.guarding = false;
Math.random = () => 0;
V.attackEdge = true;
for(let i=0;i<80;i++){ V.update(STEP); if(V.rival.hp<100||V.hitstopLeft>0) break; }
check("rival no turtle stam<15", V.rival.hp===90, `hp=${V.rival.hp} g=${V.rival.guarding} st=${V.rival.stamina}`);

toMeasure();
V.rival.stamina = 0;
V.rival.guardBreakT = 400;
V.rival.stamRegenT = 99999;
V.rival.guarding = false;
Math.random = () => 0;
V.attackEdge = true;
for(let i=0;i<80;i++){ V.update(STEP); if(V.rival.hp<100||V.hitstopLeft>0) break; }
check("break window clean hit", V.rival.hp===90, `hp=${V.rival.hp} br=${V.rival.guardBreakT.toFixed(0)}`);

toMeasure();
V.attackEdge=true;
for(let i=0;i<50;i++){ V.update(STEP); if(V.rival.hp<100||V.hitstopLeft>0) break; }
const chipSrc = (V.poseBitmap(V.rival)&&V.poseBitmap(V.rival)._src)||"";
check("chip idle not hurt", V.rival.hp===90 && !V.rival.falling && !chipSrc.includes("hurt"), `hp=${V.rival.hp} src=${chipSrc}`);

toMeasure();
V.rival.stamina = 18;
V.rival.stamRegenT = 99999;
V.rival.guardBreakT = 0;
V.rival.guarding = false;
Math.random = () => 0;
V.attackEdge = true;
for(let i=0;i<80;i++){ V.update(STEP); if(V.rival.hp<100||V.hitstopLeft>0||V.rival.guardBreakT>0) break; }
const aabbCost = V.bodyAABB(V.rival);
check("block cost breaks", V.rival.hp===100 && V.rival.stamina<=0 && V.rival.guardBreakT>0 && !V.rival.guarding && V.steelFlashT>0, `hp=${V.rival.hp} st=${V.rival.stamina.toFixed(2)} br=${V.rival.guardBreakT.toFixed(0)} g=${V.rival.guarding} fl=${V.steelFlashT}`);
check("block-break AABB planted", aabbCost.h>100 && aabbCost.w>40, `aabb=${aabbCost.w.toFixed(1)}x${aabbCost.h.toFixed(1)}`);

// Juice on the trip frame (STEEL_FLASH_MS 60 dies inside the 80ms wait above).
// wait 1900 so opening stamp (260 from t=1600) has died.
bootPlay(); wait(1900);
V.keys.clear();
V.keys.add("KeyS");
V.player.stamina = 0.1;
V.player.stamRegenT = 99999;
V.player.guardBreakT = 0;
V.player.guarding = true;
V.player.phase = "idle";
V.player.stunT = 0;
V.player.falling = false;
const dest0 = V.destRect(V.player);
const aabb0 = V.bodyAABB(V.player);
V.update(STEP);
const dest1 = V.destRect(V.player);
const aabb1 = V.bodyAABB(V.player);
check("guard break steel", !V.player.guarding && V.player.guardBreakT>0 && V.steelFlashT>0, `g=${V.player.guarding} fl=${V.steelFlashT} br=${V.player.guardBreakT.toFixed(0)}`);
check("guard break settle destRect", V.settleT>0 && V.settleT<=V.GUARD_BREAK_SETTLE, `stt=${V.settleT}`);
check("guard break AABB planted", Math.abs(aabb1.y-aabb0.y)<2 && Math.abs(aabb1.x-aabb0.x)<2 && V.settleT===V.GUARD_BREAK_SETTLE, `aabbD=${(aabb1.y-aabb0.y).toFixed(2)} destD=${(dest1.dy-dest0.dy).toFixed(2)} stt=${V.settleT}`);

// Rival holds the raise through recovery (not the swing instant).
toMeasure();
Math.random = () => 0;
V.rival.revCd = 99999;
V.rival.revArmed = -1;
V.attackEdge = true;
for(let i=0;i<80;i++){ V.rival.revCd = 99999; V.rival.revArmed = -1; V.rival.pbCd = 99999; V.rival.pbArmed = -1; V.update(STEP); if(V.hitstopLeft>0 || V.rival.hp<100) break; }
drainHitstop();
check("rival hold into recovery", V.rival.hp===100 && V.rival.guarding && V.player.phase==="recovery", `hp=${V.rival.hp} g=${V.rival.guarding} ph=${V.player.phase} st=${V.rival.stamina.toFixed(2)}`);
for(let i=0;i<40;i++){ V.update(STEP); if(V.player.phase==="idle" && !V.rival.guarding) break; }
wait(30);
check("rival drops after exchange", !V.rival.guarding && V.player.phase==="idle", `g=${V.rival.guarding} ph=${V.player.phase} st=${V.rival.stamina.toFixed(2)}`);
check("rival hold drained the exchange", V.rival.hp===100 && V.rival.stamina>=58 && V.rival.stamina<=72, `st=${V.rival.stamina.toFixed(2)}`);

// Threat regen 10/s (shared). Leave-threat keeps 10/s for 1000ms; 25/s after.
toMeasure();
V.rival.stamina = 50;
V.rival.stamRegenT = 0;
V.rival.guarding = false;
V.rival.guardBreakT = 0;
V.rival.wantBlock = false;
V.rival.phase = "idle";
V.player.phase = "idle";
wait(400);
check("rival threat regen ~10/s", V.rival.stamina>=52 && V.rival.stamina<=56, `st=${V.rival.stamina.toFixed(2)}`);

toMeasure();
V.player.stamina = 50;
V.player.stamRegenT = 0;
V.player.guarding = false;
V.player.guardBreakT = 0;
V.player.phase = "idle";
V.rival.phase = "idle";
V.keys.clear();
wait(400);
check("player threat regen ~10/s", V.player.stamina>=52 && V.player.stamina<=56, `st=${V.player.stamina.toFixed(2)}`);

bootPlay(); wait(1700);
V.player.x = 150;
V.rival.x = 1150;
V.rival.stamina = 50;
V.rival.stamRegenT = 0;
V.rival.stamThreatLockT = 0;
V.rival.guarding = false;
V.rival.guardBreakT = 0;
V.rival.phase = "idle";
wait(400);
check("rival safe regen ~25/s", V.rival.stamina>=58 && V.rival.stamina<=62, `st=${V.rival.stamina.toFixed(2)}`);

// Walk-out turtle: lock keeps 10/s, then 25/s after the window (shared).
function parkFar(){
  V.player.x = 150;
  V.rival.x = 1150;
  V.player.gait = 0;
  V.rival.gait = 0;
  V.player.guarding = false;
  V.rival.guarding = false;
  V.player.wantBlock = false;
  V.rival.wantBlock = false;
  V.player.phase = "idle";
  V.rival.phase = "idle";
  V.player.guardBreakT = 0;
  V.rival.guardBreakT = 0;
  V.rival.boltPhase = "";
  V.rival.boltT = 0;
  V.rival.boltCd = 99999;
  V.keys.clear();
}
toMeasure();
V.rival.stamina = 50;
V.rival.stamRegenT = 0;
V.rival.stamThreatLockT = V.STAMINA_REGEN_LOCK;
V.player.stamina = 50;
V.player.stamRegenT = 0;
V.player.stamThreatLockT = V.STAMINA_REGEN_LOCK;
parkFar();
wait(400);
check("rival leave lock ~10/s", V.rival.stamina>=52 && V.rival.stamina<=56, `st=${V.rival.stamina.toFixed(2)}`);
check("player leave lock ~10/s", V.player.stamina>=52 && V.player.stamina<=56, `st=${V.player.stamina.toFixed(2)}`);
wait(700);
V.rival.stamina = 50;
V.rival.stamRegenT = 0;
V.player.stamina = 50;
V.player.stamRegenT = 0;
parkFar();
wait(400);
check("rival after lock ~25/s", V.rival.stamina>=58 && V.rival.stamina<=62, `st=${V.rival.stamina.toFixed(2)} lock=${V.rival.stamThreatLockT}`);
check("player after lock ~25/s", V.player.stamina>=58 && V.player.stamina<=62, `st=${V.player.stamina.toFixed(2)} lock=${V.player.stamThreatLockT}`);

// Low stam: walk out / punish, do not statue.
toMeasure();
V.rival.stamina = 10;
V.rival.stamRegenT = 99999;
V.rival.guardBreakT = 0;
V.rival.guarding = false;
V.rival.closing = false;
V.rival.wantBlock = false;
Math.random = () => 0;
const xLow = V.rival.x;
for(let i=0;i<18;i++) V.update(STEP);
check("low stam walks out", !V.rival.guarding && (V.rival.x>xLow+3 || V.rival.gait!==0 || V.rival.phase==="startup"), `x=${(V.rival.x-xLow).toFixed(1)} gait=${V.rival.gait} ph=${V.rival.phase} g=${V.rival.guarding}`);

toMeasure();
V.rival.stamina = 0;
V.rival.guardBreakT = 400;
V.rival.stamRegenT = 99999;
V.rival.guarding = false;
V.rival.closing = false;
const xBr = V.rival.x;
for(let i=0;i<18;i++) V.update(STEP);
check("break walks or punishes", !V.rival.guarding && (V.rival.x>xBr+3 || V.rival.gait!==0 || V.rival.phase==="startup"), `x=${(V.rival.x-xBr).toFixed(1)} gait=${V.rival.gait} ph=${V.rival.phase} br=${V.rival.guardBreakT.toFixed(0)}`);


check("K bolt contract", V.BOLT_STAM===30 && V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_CHIP===2 && V.BOLT_CHIP<V.SLASH_DMG);
check("empty chip -2 super chip -6 hit super -28", V.BOLT_CHIP===2 && V.BOLT_SUPER_CHIP===6 && V.BOLT_SUPER_DMG===28 && V.BOLT_SUPER_CHIP>V.BOLT_CHIP && V.BOLT_SUPER_CHIP<V.BOLT_SUPER_DMG && V.BOLT_SUPER_CHIP<V.SLASH_DMG);
check("cast puff life 220 holds dart", V.BOLT_CAST_FX_MS===220 && V.BOLT_CAST_FX_MS>=V.BOLT_STARTUP);
check("slash frame data still 180/140/280", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280);
check("K is not Space", /act === "dart"/.test(srcTxt) && /boltEdge = true/.test(srcTxt) && !/KeyK.*attackEdge/.test(srcTxt));
const htmlTxt = fs.readFileSync("/workspace/estudio/vispera/index.html","utf8");
const cssTxt = fs.readFileSync("/workspace/estudio/vispera/style.css","utf8");
check("cache v=295", /game.js\?v=350/.test(htmlTxt));
check("pad under canvas", /id="pad"/.test(htmlTxt) && /#pad/.test(cssTxt) && /body\.touch-on #pad/.test(cssTxt));
check("portrait pad in bottom letterbox", /@media \(orientation: portrait\)/.test(cssTxt) && /body\.touch-on #pad/.test(cssTxt) && /bottom:\s*0/.test(cssTxt) && /position:\s*absolute/.test(cssTxt) && !/@media \(orientation: portrait\)[\s\S]*body\.touch-on #game/.test(cssTxt));
check("giro hidden in portrait", /body\.touch-on #giro\s*\{[^}]*display:\s*none/.test(cssTxt) && !/body\.touch-on #giro\s*\{[^}]*display:\s*flex/.test(cssTxt));
check("dart zone bigger thumb target", /\.zone-dart/.test(cssTxt) && /min-width:\s*56px/.test(cssTxt) && !/width:\s*14%/.test(cssTxt) && /data-tap="KeyK"/.test(htmlTxt));
check("KeyK tap still plants dart", /act === "dart"/.test(srcTxt) && /boltEdge = true/.test(srcTxt) && (srcTxt.match(/boltEdge = true/g)||[]).length>=1);
check("HUD metal pizarra frame recess", /function drawHudFrame/.test(srcTxt) && /HUD_FRAME/.test(srcTxt) && /COL_PIZARRA/.test(srcTxt) && /COL_NEGRO/.test(srcTxt) && /COL_HUESO/.test(srcTxt) && /COL_BRASA/.test(srcTxt) && /COL_OXIDO/.test(srcTxt) && /function drawLifeBar/.test(srcTxt));
check("HUD stam tick + brasa pip", /STAMINA_START_MIN/.test(srcTxt) && /k >= 1 \? COL_BRASA : COL_OXIDO/.test(srcTxt) && /hudFlashT/.test(srcTxt) && /COL_BRASA/.test(srcTxt) && /fillFrom\(left, mx, my, mw, mh, k\)/.test(srcTxt));
check("pad zones stick + S hold Space tap L tap K tap", /class="stick"/.test(htmlTxt) && /stick-knob/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt) && /data-tap="Space"/.test(htmlTxt) && /data-tap="KeyL"/.test(htmlTxt) && /data-tap="KeyK"/.test(htmlTxt));
check("coarse pointer not viewport-only", /pointer:\s*coarse/.test(srcTxt) && /pointerType === "touch"/.test(srcTxt) && !/max-width:\s*480/.test(cssTxt));
check("hold via pointerup not toggle", /function padDown/.test(srcTxt) && /function padUp/.test(srcTxt) && /lostpointercapture/.test(srcTxt) && !/toggleGuard|guardToggle/.test(srcTxt));
check("pad labels tajo golpe guarda dardo", /tajo/.test(htmlTxt) && /golpe/.test(htmlTxt) && /guarda/.test(htmlTxt) && /dardo/.test(htmlTxt) && /class="lab"/.test(htmlTxt));
check("pad labels stay off canvas", /id="pad"/.test(htmlTxt) && htmlTxt.indexOf('class="lab"') > htmlTxt.indexOf('id="pad"') && htmlTxt.indexOf('class="lab"') < htmlTxt.indexOf('id="giro"'));
check("pad pressed palette", /\.zone\.held/.test(cssTxt) && /--pizarra/.test(cssTxt) && /--oxido/.test(cssTxt) && /--brasa/.test(cssTxt) && /--hueso/.test(cssTxt) && /--negro/.test(cssTxt) && /inset 0 3px 0 var\(--negro\)/.test(cssTxt));
check("pad no-scroll touch-action + preventDefault", /touch-action:\s*none/.test(cssTxt) && /passive:\s*false/.test(srcTxt) && /padByPtr\.size === 0/.test(srcTxt) && /touchmove/.test(srcTxt));
check("keyboard bindings intact", /KeyA/.test(srcTxt) && /KeyD/.test(srcTxt) && /Space/.test(srcTxt) && /KeyJ/.test(srcTxt) && /KeyS/.test(srcTxt) && /KeyK/.test(srcTxt) && /KeyL/.test(srcTxt) && /KeyR/.test(srcTxt) && /DEFAULT_BINDS/.test(srcTxt) && /function actionHeld/.test(srcTxt) && /function applyActionEdge/.test(srcTxt));
check("one special verb K", (srcTxt.match(/boltEdge = true/g)||[]).length>=1 && !/KeyU|KeyI/.test(codeOnly));
check("L is golpe not K", /act === "golpe"/.test(srcTxt) && /golpeEdge = true/.test(srcTxt) && /actionHeld\("guard"\)/.test(srcTxt) && !/KeyL.*attackEdge/.test(srcTxt) && !/KeyL.*boltEdge/.test(srcTxt));
check("rival startBolt same verb", /startBolt\(rival/.test(codeOnly));
check("rival bolt AI CD 1800", V.BOLT_AI_CD===1800);
check("canvas bolt no PNG", /function drawBolt/.test(srcTxt) && !/art\/.*bolt/.test(srcTxt) && !/loadImg\([^)]*bolt/.test(srcTxt));
check("K plant SFX brasa cast", /cast:\s*new Audio\("sfx\/sfx_brasa_cast\.wav"\)/.test(srcTxt) && /function startBolt[\s\S]*?playSfx\(SFX\.cast\)/.test(codeOnly));
function sliceFn(src, name) {
  const m = src.match(new RegExp("function " + name + "\\s*\\([^)]*\\)\\s*\\{"));
  if (!m) return "";
  const start = src.indexOf(m[0]);
  let i = start + m[0].length - 1, depth = 0;
  for (; i < src.length; i++) {
    if (src[i] === "{") depth++;
    else if (src[i] === "}") { depth--; if (depth === 0) return src.slice(start, i + 1); }
  }
  return "";
}
const lbh = sliceFn(codeOnly, "landBoltHit");
const lbb = sliceFn(codeOnly, "landBoltBlock");
const rb = sliceFn(codeOnly, "resolveBolt");
check("bolt connect SFX brasa not steel",
  /brasaImpacto:\s*new Audio\("sfx\/sfx_brasa_impacto\.wav"\)/.test(srcTxt) &&
  /brasaBloqueo:\s*new Audio\("sfx\/sfx_brasa_bloqueo\.wav"\)/.test(srcTxt) &&
  /brasaChoque:\s*new Audio\("sfx\/sfx_brasa_choque\.wav"\)/.test(srcTxt) &&
  /playBoltHitSting/.test(lbh) && !/playSfx\(SFX\.impacto\)/.test(lbh) &&
  /playBoltBlockSting/.test(lbb) && !/playSfx\(SFX\.bloqueo\)/.test(lbb) &&
  /playBoltClashSting/.test(rb) && !/playSfx\(SFX\.choque\)/.test(rb)
);

const boltHitSrc = srcTxt.slice(srcTxt.indexOf("function landBoltHit"), srcTxt.indexOf("function landBoltBlock"));
const boltBlockSrc = srcTxt.slice(srcTxt.indexOf("function landBoltBlock"), srcTxt.indexOf("function resolveBolt"));
const boltResSrc = srcTxt.slice(srcTxt.indexOf("function resolveBolt"), srcTxt.indexOf("function resolveCuts"));
const startBoltSrc = srcTxt.slice(srcTxt.indexOf("function startBolt"), srcTxt.indexOf("function boltBox"));
const landHitSrc = srcTxt.slice(srcTxt.indexOf("function landHit"), srcTxt.indexOf("function guardSteelPoint"));
const landBlockSrc = srcTxt.slice(srcTxt.indexOf("function landBlock"), srcTxt.indexOf("function canStartBolt"));
const clashSrc = srcTxt.slice(srcTxt.indexOf("function doClash"), srcTxt.indexOf("function pulseBar"));
check("bolt hit brasa not steel", /playBoltHitSting/.test(boltHitSrc) && !/SFX\.impacto/.test(boltHitSrc) && /sfx_brasa_impacto\.wav/.test(srcTxt));
check("bolt block brasa not steel", /playBoltBlockSting/.test(boltBlockSrc) && !/SFX\.bloqueo/.test(boltBlockSrc) && /sfx_brasa_bloqueo\.wav/.test(srcTxt));
check("bolt clash brasa not steel", /playBoltClashSting/.test(boltResSrc) && !/SFX\.choque/.test(boltResSrc) && /sfx_brasa_choque\.wav/.test(srcTxt));
check("slash keeps steel SFX", /playSfx\(SFX\.impacto\)/.test(landHitSrc) && /playSfx\(SFX\.bloqueo\)/.test(landBlockSrc) && /playSfx\(SFX\.choque\)/.test(clashSrc));
check("startBolt still brasa cast", /playSfx\(SFX\.cast\)/.test(startBoltSrc) && /sfx_brasa_cast\.wav/.test(srcTxt));
check("K plant puff spawnBrasaFx cast", /spawnBrasaFx\("cast"/.test(startBoltSrc) && /kind === "cast"/.test(srcTxt));
check("cast puff canvas palette", /COL_NEGRO/.test(srcTxt) && /BOLT_CAST_FX_MS/.test(srcTxt) && /brasaFxKind === "cast"/.test(srcTxt));
check("connect VFX unchanged kinds", /spawnBrasaFx\("hit"/.test(boltHitSrc) && /spawnBrasaFx\("block"/.test(boltBlockSrc) && /spawnBrasaFx\("clash"/.test(boltResSrc));
check("bolt block chips hp", /BOLT_CHIP/.test(boltBlockSrc) && /def\.hp/.test(boltBlockSrc) && /spawnBrasaFx\("block"/.test(boltBlockSrc));
check("spent super block uses super chip", /BOLT_SUPER_CHIP/.test(boltBlockSrc) && /bolt\.super/.test(boltBlockSrc) && /STAMINA_BLOCK/.test(boltBlockSrc));
check("slash/golpe block stay 0 hp", !/def\.hp\s*=/.test(sliceFn(codeOnly, "landBlock")) && V.SLASH_DMG===10);
check("slash VFX still spark asterisk shards", /spawnHitSpark/.test(landHitSrc) && /spawnSteelFlash/.test(landBlockSrc) && /spawnClashSpark/.test(clashSrc));
const spawnBoltSrc = sliceFn(codeOnly, "spawnBolt");
check("spawnBolt live tip not body flank", /castPlantXY/.test(spawnBoltSrc) && !/bodyAABB/.test(spawnBoltSrc) && !/b\.x \+ b\.w/.test(spawnBoltSrc));

// title K does not start
check("title before K", V.mode==="title" || true);
bootPlay();
// cold title: reconstruct by setting mode after a fresh eval is title-only at start.
// After bootPlay we are in play. Re-title:
V.mode = "title";
V.boltEdge = true;
V.update(STEP);
check("title K does not start", V.mode==="title" && !V.bolt && !V.player.boltPhase, `mode=${V.mode} bolt=${!!V.bolt} ph=${V.player.boltPhase}`);

bootPlay();
V.boltEdge = true;
V.update(STEP);
check("opening K blocked", !V.player.boltPhase && !V.bolt && V.player.stamina===100, `ph=${V.player.boltPhase} st=${V.player.stamina} open=${V.openLeft}`);

bootPlay(); wait(1700);
const st0 = V.player.stamina;
V.boltEdge = true;
V.update(STEP);
check("K starts from idle", V.player.boltPhase==="startup" && V.player.phase==="idle", `bph=${V.player.boltPhase} ph=${V.player.phase}`);
const idleKSrc = (V.poseBitmap(V.player)&&V.poseBitmap(V.player)._src)||"";
check("idle K uses throw_knife plant not slash", /throw_knife/.test(idleKSrc) && !/slash/.test(idleKSrc), `src=${idleKSrc}`);
check("K costs 30 stam", Math.abs(V.player.stamina - (st0-30))<0.05, `st=${V.player.stamina.toFixed(2)} s0=${st0}`);
const plantPuff = V.castPlantXY(V.player);
check("K plant puff at blade", V.brasaFxKind==="cast" && V.brasaFxT>150 && !V.bolt && Math.abs(V.brasaX-plantPuff.x)<4, `kind=${V.brasaFxKind} t=${V.brasaFxT} x=${V.brasaX} tip=${plantPuff.x} edge=${V.bladeTipX(V.player)} bolt=${!!V.bolt}`);
const puffHold = V.brasaFxT;
V.hitstopLeft = 140;
V.update(STEP);
check("cast puff holds hitstop", V.brasaFxKind==="cast" && Math.abs(V.brasaFxT-puffHold)<0.01, `t=${V.brasaFxT} hold=${puffHold}`);
const tipA = V.bladeTipX(V.player);
const fxA = V.brasaX;
V.player.x += 40;
V.update(STEP);
const tipB = V.bladeTipX(V.player);
check("cast puff rides blade", Math.abs((V.brasaX-fxA)-(tipB-tipA))<2.5, `dxFx=${(V.brasaX-fxA).toFixed(2)} dxTip=${(tipB-tipA).toFixed(2)}`);
V.hitstopLeft = 0;

bootPlay(); wait(1700);
V.player.stamina = 29;
V.player.stamRegenT = 99999;
V.boltEdge = true;
V.update(STEP);
check("no K stam<30", !V.player.boltPhase && !V.bolt && V.player.stamina===29, `ph=${V.player.boltPhase} st=${V.player.stamina}`);

toMeasure();
V.attackEdge = true;
for(let i=0;i<8;i++) V.update(STEP);
const slashPh = V.player.phase;
const stSlash = V.player.stamina;
V.boltEdge = true;
V.update(STEP);
check("K not slash cancel", V.player.phase===slashPh && !V.player.boltPhase && !V.bolt && Math.abs(V.player.stamina-stSlash)<0.2, `ph=${V.player.phase} bph=${V.player.boltPhase} st=${V.player.stamina.toFixed(2)}`);

bootPlay(); wait(1700);
V.keys.add("KeyS");
V.update(STEP);
const stG = V.player.stamina;
V.boltEdge = true;
V.update(STEP);
check("no K during guard", V.player.guarding && !V.player.boltPhase && !V.bolt, `g=${V.player.guarding} bph=${V.player.boltPhase}`);
V.keys.delete("KeyS");

bootPlay(); wait(1700);
V.player.stunT = 200;
V.player.stamRegenT = 99999;
const stStun = V.player.stamina;
V.boltEdge = true;
V.update(STEP);
check("no K during stun", !V.player.boltPhase && !V.bolt && V.player.stamina<=stStun, `bph=${V.player.boltPhase} st=${V.player.stamina}`);

bootPlay(); wait(1700);
V.player.guardBreakT = 400;
V.player.stamina = 80;
V.player.stamRegenT = 99999;
V.boltEdge = true;
V.update(STEP);
check("no K during guardBreak", !V.player.boltPhase && !V.bolt, `bph=${V.player.boltPhase} br=${V.player.guardBreakT}`);

bootPlay(); wait(1700);
V.boltEdge = true;
V.update(STEP);
let sawFire=false, startMs=0, recMs=0, recStart=false;
let youTip=0, youSpawnX=0, youFacing=0, youW=0, youPuffKind="", youPuffT=0;
for(let i=0;i<40;i++){
  if(!sawFire && V.bolt){ sawFire=true; startMs=i*STEP; }
  if(sawFire && V.player.boltPhase==="recovery") recStart=true;
  if(recStart && !V.player.boltPhase){ recMs=i*STEP-startMs; break; }
  const tipBefore = V.bladeTipX(V.player);
  const hadBolt = !!V.bolt;
  V.update(STEP);
  if(!hadBolt && V.bolt){
    youTip = tipBefore;
    youSpawnX = V.bolt.x;
    youFacing = V.bolt.facing;
    youW = V.bolt.w;
    youPuffKind = V.brasaFxKind;
    youPuffT = V.brasaFxT;
  }
}
check("bolt startup ~200", sawFire && startMs>=180 && startMs<=220, `fireAt=${startMs} bolt=${!!V.bolt}`);
const youTravel = V.BOLT_SPEED * (STEP/1000);
const youBirth = youFacing > 0 ? youSpawnX - youTravel : youSpawnX + youW + youTravel;
check("you dart births at live tip", sawFire && Math.abs(youBirth - youTip) < 3, `birth=${youBirth.toFixed(1)} tip=${youTip.toFixed(1)} x=${youSpawnX.toFixed(1)}`);
check("you puff holds through dart birth", youPuffKind==="cast" && youPuffT>0, `kind=${youPuffKind} t=${youPuffT}`);
// recovery clock from fire
bootPlay(); wait(1700);
V.boltEdge = true;
V.update(STEP);
for(let i=0;i<20;i++){ V.update(STEP); if(V.player.boltPhase==="recovery") break; }
let recTicks=0;
while(V.player.boltPhase==="recovery"){ V.update(STEP); recTicks++; if(recTicks>40) break; }
check("bolt recovery ~280", recTicks*STEP>=260 && recTicks*STEP<=300, `rec=${recTicks*STEP}`);

bootPlay(); wait(1700);
V.boltEdge = true;
V.update(STEP);
V.attackEdge = true;
V.update(STEP);
check("Space during K does not cancel", V.player.boltPhase==="startup" && V.player.phase==="idle" && !!V.slashBuf, `bph=${V.player.boltPhase} ph=${V.player.phase} buf=${V.slashBuf}`);

// one live max: fire from far so the bolt stays up after recovery
// Tip-born dart starts ~bladeReach farther than the old body-flank spawn,
// so 160/1100 was already overlapping the rival by recovery end.
bootPlay(); wait(1700);
V.player.x = 140;
V.rival.x = 1220;
V.rival.phase = "idle";
V.rival.wantBlock = false;
Math.random = () => 0.99;
V.boltEdge = true;
V.update(STEP);
for(let i=0;i<14;i++) V.update(STEP);
check("bolt live after startup", !!V.bolt, `bolt=${!!V.bolt} bph=${V.player.boltPhase}`);
const live1 = !!V.bolt;
const stMid = V.player.stamina;
V.boltEdge = true;
V.update(STEP);
check("one live: K refused while bolt up", live1 && !!V.bolt && V.player.boltPhase==="recovery" && V.player.stamina>stMid-1, `bolt=${!!V.bolt} st=${V.player.stamina.toFixed(2)} mid=${stMid.toFixed(2)} bph=${V.player.boltPhase}`);
for(let i=0;i<20;i++) V.update(STEP);
const stLive = V.player.stamina;
V.boltEdge = true;
V.update(STEP);
check("one live: still refused after recovery", !!V.bolt && !V.player.boltPhase && V.player.stamina>stLive-1, `bolt=${!!V.bolt} bph=${V.player.boltPhase} st=${V.player.stamina.toFixed(2)} live=${stLive.toFixed(2)}`);

// clean hit -10
toMeasure();
Math.random = () => 0.99;
V.rival.guarding = false;
V.rival.wantBlock = false;
V.rival.guardChoice = -1;
V.boltEdge = true;
for(let i=0;i<80;i++){ V.update(STEP); if(V.rival.hp<100 || V.hitstopLeft>0) break; }
check("K clean hit -10", V.rival.hp===90, `hp=${V.rival.hp} g=${V.rival.guarding} bolt=${!!V.bolt}`);

// blocked dart chips + push (not full −10)
toMeasure();
Math.random = () => 0;
V.rival.stamina = 80;
V.rival.stamRegenT = 99999;
V.rival.guardBreakT = 0;
V.boltEdge = true;
for(let i=0;i<80;i++){ V.update(STEP); if(V.hitstopLeft>0 || V.rival.pushT>0) break; }
check("K block chips", V.rival.hp===100-V.BOLT_CHIP && V.BOLT_CHIP===2 && V.rival.hp>90 && V.rival.pushT>0 && !V.rival.falling, `hp=${V.rival.hp} chip=${V.BOLT_CHIP} push=${V.rival.pushT} g=${V.rival.guarding} st=${V.rival.stamina.toFixed(2)}`);
check("K block chip not full dart", V.rival.hp===100-V.BOLT_CHIP && V.SLASH_DMG===10 && V.BOLT_CHIP<V.SLASH_DMG, `hp=${V.rival.hp} slash=${V.SLASH_DMG} chip=${V.BOLT_CHIP}`);

// forced guard still stops the bolt (ignore = pass through)
toMeasure();
Math.random = () => 0.99;
V.rival.stamina = 80;
V.rival.stamRegenT = 99999;
V.rival.guardBreakT = 0;
V.rival.wantBlock = true;
V.rival.guarding = true;
V.rival.guardChoice = 1;
V.boltEdge = true;
let passed=false;
for(let i=0;i<80;i++){
  V.rival.wantBlock = true;
  V.rival.guarding = true;
  V.update(STEP);
  if(V.rival.hp<=100-V.SLASH_DMG){ passed=true; break; }
  if(V.hitstopLeft>0 && V.rival.hp===100-V.BOLT_CHIP) break;
}
check("K dies on raised guard", !passed && V.rival.hp===100-V.BOLT_CHIP, `hp=${V.rival.hp} passed=${passed} push=${V.rival.pushT}`);

// chip cannot KO from 5; can KO at 1
toMeasure();
Math.random = () => 0;
V.rival.stamina = 80;
V.rival.stamRegenT = 99999;
V.rival.guardBreakT = 0;
V.rival.hp = 5;
V.boltEdge = true;
for(let i=0;i<80;i++){ V.update(STEP); if(V.hitstopLeft>0 || V.rival.pushT>0) break; }
check("K block chip cannot KO from 5", V.rival.hp===5-V.BOLT_CHIP && V.rival.hp>0 && !V.rival.falling, `hp=${V.rival.hp} fall=${V.rival.falling}`);

toMeasure();
Math.random = () => 0;
V.rival.stamina = 80;
V.rival.stamRegenT = 99999;
V.rival.guardBreakT = 0;
V.rival.hp = 1;
V.boltEdge = true;
for(let i=0;i<80;i++){ V.update(STEP); if(V.hitstopLeft>0 || V.rival.hp<=0) break; }
drainHitstop();
for(let i=0;i<12;i++){ if(V.rival.falling || V.mode==="falling") break; V.update(STEP); }
check("K block chip can KO at 1", V.rival.hp===0 && (V.rival.falling || V.mode==="falling"), `hp=${V.rival.hp} fall=${V.rival.falling} mode=${V.mode}`);

// Space/J still slash after K recovery
toMeasure();
Math.random = () => 0.99;
V.attackEdge = true;
for(let i=0;i<20;i++){ V.update(STEP); if(V.player.phase==="startup") break; }
check("Space still slash", V.player.phase==="startup", `ph=${V.player.phase}`);

function freezeYou(){
  V.player.phase = "idle";
  V.player.phaseT = 0;
  V.player.boltPhase = "";
  V.player.stunT = 0;
  V.player.guarding = false;
  V.player.falling = false;
  V.keys.clear();
}
function parkRangeThrow(){
  toMeasure();
  Math.random = () => 0.99;
  freezeYou();
  V.player.x = 180;
  V.rival.x = 1100;
  V.rival.phase = "idle";
  V.rival.phaseT = 0;
  V.rival.boltPhase = "";
  V.rival.boltT = 0;
  V.rival.boltCd = 0;
  V.rival.boltArmed = true;
  V.rival.stamina = 100;
  V.rival.stamRegenT = 99999;
  V.rival.guarding = false;
  V.rival.wantBlock = false;
  V.rival.guardChoice = -1;
  V.rival.closing = false;
  V.rival.standWait = 0;
  V.rival.standGoal = 99999;
  V.rival.stunT = 0;
  V.rival.guardBreakT = 0;
  V.rival.falling = false;
}

parkRangeThrow();
V.update(STEP);
check("rival throws at range", V.rival.boltPhase==="startup" && V.rival.phase==="idle", `bph=${V.rival.boltPhase} ph=${V.rival.phase} armed=${V.rival.boltArmed}`);
const rivalPlantPuff = V.castPlantXY(V.rival);
check("rival plant puff at blade", V.brasaFxKind==="cast" && V.brasaFxT>0 && !V.bolt && Math.abs(V.brasaX-rivalPlantPuff.x)<4, `kind=${V.brasaFxKind} t=${V.brasaFxT} x=${V.brasaX} tip=${rivalPlantPuff.x} edge=${V.bladeTipX(V.rival)} bolt=${!!V.bolt}`);
check("rival bolt costs 30", Math.abs(V.rival.stamina-70)<0.05, `st=${V.rival.stamina.toFixed(2)}`);
check("rival bolt CD armed", V.rival.boltCd===V.BOLT_AI_CD, `cd=${V.rival.boltCd}`);

parkRangeThrow();
V.update(STEP);
let rFire=false, rStart=0;
let rTip=0, rSpawnX=0, rFacing=0, rW=0, rPuffKind="", rPuffT=0;
for(let i=0;i<20;i++){
  if(!rFire && V.bolt){ rFire=true; rStart=i*STEP; }
  const tipBefore = V.bladeTipX(V.rival);
  const hadBolt = !!V.bolt;
  V.update(STEP);
  if(!hadBolt && V.bolt){
    rTip = tipBefore;
    rSpawnX = V.bolt.x;
    rFacing = V.bolt.facing;
    rW = V.bolt.w;
    rPuffKind = V.brasaFxKind;
    rPuffT = V.brasaFxT;
  }
}
check("rival bolt startup ~200", rFire && rStart>=180 && rStart<=220, `fireAt=${rStart} bolt=${!!V.bolt}`);
const rTravel = V.BOLT_SPEED * (STEP/1000);
const rBirth = rFacing > 0 ? rSpawnX - rTravel : rSpawnX + rW + rTravel;
check("rival dart births at live tip", rFire && Math.abs(rBirth - rTip) < 3, `birth=${rBirth.toFixed(1)} tip=${rTip.toFixed(1)} x=${rSpawnX.toFixed(1)}`);
check("rival puff holds through dart birth", rPuffKind==="cast" && rPuffT>0, `kind=${rPuffKind} t=${rPuffT}`);

parkRangeThrow();
V.update(STEP);
for(let i=0;i<20;i++){ V.update(STEP); if(V.rival.boltPhase==="recovery") break; }
let rRec=0;
while(V.rival.boltPhase==="recovery"){ V.update(STEP); rRec++; if(rRec>40) break; }
check("rival bolt recovery ~280", rRec*STEP>=260 && rRec*STEP<=300, `rec=${rRec*STEP}`);

// first approach: never seen HOLD — walk in, do not fire
bootPlay(); wait(1700);
Math.random = () => 0.99;
V.player.x = 180;
V.rival.x = 1100;
V.rival.phase = "idle";
V.rival.boltPhase = "";
V.rival.boltArmed = false;
V.rival.boltCd = 0;
V.rival.stamina = 100;
V.rival.closing = false;
let firstFire=false;
for(let i=0;i<24;i++){
  V.update(STEP);
  if(V.rival.boltPhase || (V.bolt && V.bolt.kind==="rival")) firstFire=true;
}
check("no rival bolt before HOLD", !firstFire && !V.rival.boltArmed, `fired=${firstFire} armed=${V.rival.boltArmed} ph=${V.rival.boltPhase} gait=${V.rival.gait}`);

// pocket: do not fire
toMeasure();
Math.random = () => 0.99;
V.rival.boltCd = 0;
V.rival.boltArmed = true;
V.rival.stamina = 100;
V.rival.standWait = 0;
V.rival.standGoal = 99999;
V.rival.closing = false;
let pocketFire=false;
for(let i=0;i<18;i++){
  const hold = V.bladeReach(V.rival)+V.LUNGE_PX;
  if(V.bodyGap()<=hold+4 && (V.rival.boltPhase || (V.bolt && V.bolt.kind==="rival"))) pocketFire=true;
  V.update(STEP);
}
check("no rival bolt in pocket", !pocketFire, `fired=${pocketFire} bph=${V.rival.boltPhase} g=${V.bodyGap().toFixed(1)}`);

// no throw during their slash
toMeasure();
Math.random = () => 0.99;
V.rival.phase = "startup";
V.rival.phaseT = 40;
V.rival.boltArmed = true;
V.rival.boltCd = 0;
V.rival.stamina = 100;
V.player.x = 180;
V.rival.x = 1100;
V.update(STEP);
check("no rival bolt during slash", V.rival.phase==="startup" && !V.rival.boltPhase, `ph=${V.rival.phase} bph=${V.rival.boltPhase}`);

// no throw during guard
toMeasure();
V.rival.guarding = true;
V.rival.phase = "idle";
V.rival.stamina = 80;
V.rival.stamRegenT = 99999;
V.rival.guardBreakT = 0;
check("canStartBolt false on guard", !V.canStartBolt(V.rival));
Math.random = () => 0;
V.player.phase = "startup";
V.player.phaseT = 160;
V.rival.guarding = true;
V.rival.wantBlock = true;
V.rival.guardChoice = 1;
V.rival.boltArmed = true;
V.rival.boltCd = 0;
V.rival.stamina = 80;
V.player.x = 180;
V.rival.x = 1100;
V.update(STEP);
check("no rival bolt during guard", V.rival.guarding && !V.rival.boltPhase, `g=${V.rival.guarding} bph=${V.rival.boltPhase}`);

// no throw during stun / break / opening
toMeasure();
V.rival.stunT = 200;
V.rival.boltArmed = true;
V.rival.boltCd = 0;
V.rival.stamina = 100;
V.player.x = 180;
V.rival.x = 1100;
V.update(STEP);
check("no rival bolt during stun", !V.rival.boltPhase, `bph=${V.rival.boltPhase} stun=${V.rival.stunT}`);

toMeasure();
V.rival.guardBreakT = 400;
V.rival.stamina = 80;
V.rival.boltArmed = true;
V.rival.boltCd = 0;
V.player.x = 180;
V.rival.x = 1100;
V.update(STEP);
check("no rival bolt during break", !V.rival.boltPhase, `bph=${V.rival.boltPhase} br=${V.rival.guardBreakT}`);

bootPlay();
V.rival.boltArmed = true;
V.rival.boltCd = 0;
V.rival.stamina = 100;
V.player.x = 180;
V.rival.x = 1100;
V.update(STEP);
check("no rival bolt during opening", V.openLeft>0 && !V.rival.boltPhase && !V.bolt, `open=${V.openLeft} bph=${V.rival.boltPhase}`);

// one live of theirs
parkRangeThrow();
V.update(STEP);
for(let i=0;i<14;i++) V.update(STEP);
check("rival dart live", !!V.bolt && V.bolt.kind==="rival", `bolt=${!!V.bolt} kind=${V.bolt&&V.bolt.kind} bph=${V.rival.boltPhase}`);
V.rival.boltCd = 0;
V.rival.boltPhase = "";
V.rival.boltT = 0;
V.rival.phase = "idle";
V.rival.stamina = 100;
const liveR = !!V.bolt;
const stR = V.rival.stamina;
V.startBolt(V.rival);
check("one live: rival refused while dart up", liveR && !!V.bolt && V.bolt.kind==="rival" && !V.rival.boltPhase && V.rival.stamina===stR, `bolt=${!!V.bolt} bph=${V.rival.boltPhase} st=${V.rival.stamina}`);

// CD: after a throw they walk in, not fire again
parkRangeThrow();
const stThrow = V.rival.stamina;
V.update(STEP);
check("CD set on throw", V.rival.boltCd===V.BOLT_AI_CD, `cd=${V.rival.boltCd}`);
const cd0 = V.rival.boltCd;
for(let i=0;i<50;i++) V.update(STEP);
check("no spam after throw", cd0===V.BOLT_AI_CD && V.rival.boltCd<cd0 && V.rival.boltCd>0 && !(V.rival.boltPhase==="startup" && V.rival.stamina<stThrow-35), `cd=${V.rival.boltCd} bph=${V.rival.boltPhase} st=${V.rival.stamina.toFixed(2)}`);

// they still slash in the pocket
toMeasure();
Math.random = () => 0.99;
V.rival.boltCd = 99999;
V.rival.boltPhase = "";
V.rival.stamina = 100;
V.rival.guarding = false;
V.rival.wantBlock = false;
V.rival.guardChoice = -1;
const holdP = V.bladeReach(V.rival)+V.LUNGE_PX;
if (V.bodyGap() > holdP - 8) V.rival.x -= (V.bodyGap() - (holdP - 8));
V.rival.standWait = 0;
V.rival.standGoal = 1;
V.rival.closing = false;
V.rival.phase = "idle";
V.player.phase = "idle";
V.player.stunT = 0;
V.player.falling = false;
for(let i=0;i<20;i++){ V.update(STEP); if(V.rival.phase==="startup") break; }
check("rival still slashes", V.rival.phase==="startup", `ph=${V.rival.phase} bph=${V.rival.boltPhase} g=${V.bodyGap().toFixed(1)}`);

// they still guard a slash
toMeasure();
Math.random = () => 0;
V.rival.boltCd = 99999;
V.rival.revCd = 99999;
V.rival.revArmed = -1;
V.attackEdge = true;
for(let i=0;i<80;i++){ V.rival.revCd = 99999; V.rival.revArmed = -1; V.rival.pbCd = 99999; V.rival.pbArmed = -1; V.update(STEP); if(V.hitstopLeft>0 || V.rival.hp<100) break; }
check("rival still guards", V.rival.hp===100 && (V.rival.guarding || V.rival.pushT>0), `hp=${V.rival.hp} g=${V.rival.guarding} push=${V.rival.pushT}`);

// you block theirs
parkRangeThrow();
V.update(STEP);
V.keys.add("KeyS");
V.player.stamina = 80;
V.player.stamRegenT = 99999;
V.player.guardBreakT = 0;
let blockedTheirs=false, ateTheirs=false;
for(let i=0;i<90;i++){
  V.keys.add("KeyS");
  V.player.wantBlock = true;
  V.update(STEP);
  if(V.player.hp<=100-V.SLASH_DMG){ ateTheirs=true; break; }
  if(V.hitstopLeft>0 && V.player.pushT>0 && V.player.hp===100-V.BOLT_CHIP){ blockedTheirs=true; break; }
}
check("you block theirs chip+push", blockedTheirs && !ateTheirs && V.player.hp===100-V.BOLT_CHIP && !V.bolt, `hp=${V.player.hp} push=${V.player.pushT} bolt=${!!V.bolt} g=${V.player.guarding} ate=${ateTheirs}`);

// they still block yours (range dart into raised guard)
toMeasure();
Math.random = () => 0.99;
V.rival.stamina = 80;
V.rival.stamRegenT = 99999;
V.rival.guardBreakT = 0;
V.rival.wantBlock = true;
V.rival.guarding = true;
V.rival.guardChoice = 1;
V.rival.boltCd = 99999;
V.boltEdge = true;
let yoursPassed=false;
for(let i=0;i<80;i++){
  V.rival.wantBlock = true;
  V.rival.guarding = true;
  V.update(STEP);
  if(V.rival.hp<=100-V.SLASH_DMG){ yoursPassed=true; break; }
  if(V.hitstopLeft>0 && V.rival.hp===100-V.BOLT_CHIP) break;
}
check("they still block yours", !yoursPassed && V.rival.hp===100-V.BOLT_CHIP, `hp=${V.rival.hp} passed=${yoursPassed} push=${V.rival.pushT}`);


check("golpe frame data 120/80/180", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180);
check("golpe lunge 18 < slash 36", V.GOLPE_LUNGE_PX===18 && V.GOLPE_LUNGE_PX<V.LUNGE_PX);
check("slash frames still 180/140/280 after L", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280);
check("golpe nested in cut-cluster", /cut-cluster/.test(htmlTxt) && /data-tap="KeyL"/.test(htmlTxt) && htmlTxt.indexOf("cut-cluster") < htmlTxt.indexOf('data-tap="KeyL"') && htmlTxt.indexOf('data-tap="KeyL"') < htmlTxt.indexOf('zone-dart'));
check("golpe zone smaller than slash", /\.zone-golpe/.test(cssTxt) && /height:\s*80%/.test(cssTxt) && /blade-sm/.test(cssTxt) && /blade-sm/.test(htmlTxt) && /\.zone-slash[\s\S]*?height:\s*92%/.test(cssTxt));
check("walk guard dart untouched", /function stickApply/.test(srcTxt) && /feedKey\("KeyA"/.test(srcTxt) && /feedKey\("KeyD"/.test(srcTxt) && /data-hold="KeyS"/.test(htmlTxt) && /data-tap="KeyK"/.test(htmlTxt));
check("HOLD still blade+lunge", /const HOLD = bladeReach\(rival\) \+ LUNGE_PX/.test(srcTxt) && !/HOLD = bladeReach\(rival\) \+ GOLPE/.test(srcTxt));
check("GAP 120 locked", /const GAP = 120/.test(srcTxt));
check("K plant still KeyK only", (srcTxt.match(/boltEdge = true/g)||[]).length===1);

function freezeRivalAI(){
  Math.random = () => 0.99;
  V.rival.phase = "idle";
  V.rival.phaseT = 0;
  V.rival.cut = "slash";
  V.rival.cutHit = false;
  V.rival.linkGolpe = false;
  V.rival.linkSlash = false;
  V.rival.linkBolt = false;
  V.rival.clashRec = false;
  V.rival.techRec = false;
  V.rival.throwTechArmed = -1;
  V.rival.thrownT = 0;
  V.rival.throwInvulnT = 0;
  V.rival.boltPhase = "";
  V.rival.boltT = 0;
  V.rival.boltCd = 99999;
  V.rival.throwCd = 99999;
  V.rival.revCd = 99999;
  V.rival.revArmed = -1;
  V.rival.wakeRevArmed = -1;
  V.rival.pbCd = 99999;
  V.rival.pbArmed = -1;
  V.rival.feintCd = 99999;
  V.rival.feintArmed = -1;
  V.rival.wakeRev = false;
  V.rival.boltArmed = false;
  V.rival.stunT = 0;
  V.rival.guarding = false;
  V.rival.wantBlock = false;
  V.rival.guardChoice = -1;
  V.rival.closing = false;
  V.rival.standWait = 0;
  V.rival.standGoal = 99999;
  V.rival.falling = false;
  V.rival.guardBreakT = 0;
  V.rival.pushT = 0;
  V.rival.stamina = 100;
  V.rival.stamRegenT = 99999;
}
function freezeYouAI(){
  V.player.phase = "idle";
  V.player.phaseT = 0;
  V.player.cut = "slash";
  V.player.cutHit = false;
  V.player.clashRec = false;
  V.player.techRec = false;
  V.player.thrownT = 0;
  V.player.throwInvulnT = 0;
  V.player.boltPhase = "";
  V.player.stunT = 0;
  V.player.guarding = false;
  V.player.falling = false;
  V.player.pushT = 0;
  V.keys.clear();
}
function toClose(){
  toMeasure();
  freezeYouAI();
  freezeRivalAI();
  V.player.x = V.rival.x - 170;
  V.keepApart();
}

V.mode = "title";
V.golpeEdge = true;
V.update(STEP);
check("title L does not start", V.mode==="title" && V.player.phase==="idle", `mode=${V.mode} ph=${V.player.phase}`);

bootPlay();
V.golpeEdge = true;
V.update(STEP);
check("opening L does not fire", V.player.phase==="idle" && V.player.cut==="slash" && V.openLeft>0, `ph=${V.player.phase} cut=${V.player.cut} open=${V.openLeft} buf=${V.golpeBuf}`);

bootPlay(); wait(1700);
freezeRivalAI();
V.player.x = 180;
V.rival.x = 1100;
V.golpeEdge = true;
V.update(STEP);
check("L starts from idle as golpe", V.player.phase==="startup" && V.player.cut==="golpe", `ph=${V.player.phase} cut=${V.player.cut}`);
let gStart=STEP, gAct=0, gRec=0;
while(V.player.phase==="startup"){ freezeRivalAI(); V.update(STEP); gStart+=STEP; if(gStart>400) break; }
check("golpe startup ~120", gStart>=100 && gStart<=140, `st=${gStart}`);
while(V.player.phase==="active"){ freezeRivalAI(); V.update(STEP); gAct+=STEP; if(gAct>400) break; }
check("golpe active ~80", gAct>=60 && gAct<=100, `act=${gAct}`);
while(V.player.phase==="recovery"){ freezeRivalAI(); V.update(STEP); gRec+=STEP; if(gRec>400) break; }
check("golpe recovery ~180", gRec>=160 && gRec<=200, `rec=${gRec}`);
check("golpe returns idle slash", V.player.phase==="idle" && V.player.cut==="slash", `ph=${V.player.phase} cut=${V.player.cut}`);

bootPlay(); wait(1700);
freezeYouAI();
freezeRivalAI();
V.player.x = 180;
V.rival.x = 1100;
const xG0=V.player.x;
V.golpeEdge=true;
while(V.player.phase!=="active" && V.player.phase!=="recovery"){ freezeRivalAI(); V.update(STEP); if(V.player.phase==="idle" && V.player.cut==="slash") break; }
check("golpe lunge ~18", (V.player.x-xG0)>=12 && (V.player.x-xG0)<=24, `dx=${(V.player.x-xG0).toFixed(1)} cut=${V.player.cut} ph=${V.player.phase}`);

toClose();
freezeRivalAI();
Math.random = () => 0.99;
V.golpeEdge=true;
let sawStartupHp=true;
for(let i=0;i<40;i++){
  freezeRivalAI();
  if(V.player.phase==="startup" && V.rival.hp<100) sawStartupHp=false;
  V.update(STEP);
  if(V.rival.hp<100 || V.hitstopLeft>0) break;
}
check("golpe hitbox not in startup", sawStartupHp, `hp=${V.rival.hp} ph=${V.player.phase}`);
check("golpe clean hit -10", V.rival.hp===90, `hp=${V.rival.hp} ph=${V.player.phase} cut=${V.player.cut} g=${V.bodyGap().toFixed(1)}`);

toClose();
Math.random = () => 0;
V.rival.stamina = 80;
V.rival.stamRegenT = 99999;
V.rival.guardBreakT = 0;
V.rival.wantBlock = true;
V.rival.guarding = true;
V.rival.guardChoice = 1;
V.golpeEdge = true;
for(let i=0;i<50;i++){
  V.rival.wantBlock = true;
  V.rival.guarding = true;
  V.rival.guardChoice = 1;
  V.update(STEP);
  if(V.hitstopLeft>0 || (V.rival.pushT>0 && V.rival.hp===100)) break;
  if(V.rival.hp<100) break;
}
check("golpe block 0", V.rival.hp===100 && V.rival.pushT>0, `hp=${V.rival.hp} g=${V.rival.guarding} push=${V.rival.pushT}`);

toMeasure();
V.attackEdge=true;
for(let i=0;i<8;i++) V.update(STEP);
const slashPhL = V.player.phase;
const slashCutL = V.player.cut;
V.golpeEdge=true;
V.update(STEP);
check("L not slash cancel", V.player.phase===slashPhL && V.player.cut===slashCutL && V.player.cut==="slash" && !!V.golpeBuf, `ph=${V.player.phase} cut=${V.player.cut} buf=${V.golpeBuf}`);

toMeasure();
V.attackEdge=true;
let sawRecL=false;
for(let i=0;i<80;i++){
  V.update(STEP);
  if(V.player.phase==="recovery" && V.player.cut==="slash"){ sawRecL=true; break; }
}
V.golpeEdge=true;
V.update(STEP);
check("L during slash recovery buffers", sawRecL && V.player.phase==="recovery" && V.player.cut==="slash" && V.golpeBuf, `saw=${sawRecL} ph=${V.player.phase} cut=${V.player.cut} buf=${V.golpeBuf}`);

bootPlay(); wait(1700);
freezeRivalAI();
V.player.stamina = 100;
V.player.stamRegenT = 99999;
V.player.guardBreakT = 0;
V.player.stunT = 0;
V.player.falling = false;
V.keys.add("KeyS");
V.update(STEP);
V.golpeEdge=true;
V.update(STEP);
check("L from guard is reversal not throw", V.player.cut==="golpe" && V.player.phase==="startup" && V.player.reversal && !V.player.guarding && V.player.cut!=="throw", `cut=${V.player.cut} ph=${V.player.phase} rev=${V.player.reversal} g=${V.player.guarding}`);
V.keys.delete("KeyS");

bootPlay(); wait(1700);
V.player.stunT = 200;
V.golpeEdge=true;
V.update(STEP);
check("no L during stun", V.player.phase==="idle" && V.player.cut==="slash" && V.golpeBuf, `ph=${V.player.phase} cut=${V.player.cut} buf=${V.golpeBuf} stun=${V.player.stunT}`);

bootPlay(); wait(1700);
V.boltEdge=true;
V.update(STEP);
V.golpeEdge=true;
V.update(STEP);
check("L not K cancel", V.player.boltPhase==="startup" && V.player.phase==="idle" && V.golpeBuf, `bph=${V.player.boltPhase} ph=${V.player.phase} buf=${V.golpeBuf}`);

toMeasure();
V.golpeEdge=true;
for(let i=0;i<8;i++) V.update(STEP);
const gPh = V.player.phase;
V.attackEdge=true;
V.update(STEP);
check("Space during L does not cancel", V.player.cut==="golpe" && V.player.phase===gPh && !!V.slashBuf, `cut=${V.player.cut} ph=${V.player.phase} buf=${V.slashBuf}`);

toClose();
freezeYouAI();
freezeRivalAI();
V.player.phase = "recovery";
V.player.phaseT = 40;
V.player.cut = "slash";
V.rival.phase = "idle";
V.rival.standGoal = 99999;
V.update(STEP);
check("rival close punish is golpe", V.rival.phase==="startup" && V.rival.cut==="golpe", `ph=${V.rival.phase} cut=${V.rival.cut} g=${V.bodyGap().toFixed(1)}`);

toMeasure();
Math.random = () => 0.99;
freezeYouAI();
V.rival.boltCd = 99999;
V.rival.boltPhase = "";
V.rival.stamina = 100;
V.rival.guarding = false;
V.rival.wantBlock = false;
V.rival.guardChoice = -1;
const holdM = V.bladeReach(V.rival)+V.LUNGE_PX;
if (V.bodyGap() > holdM - 8) V.rival.x -= (V.bodyGap() - (holdM - 8));
V.rival.standWait = 0;
V.rival.standGoal = 1;
V.rival.closing = false;
V.rival.phase = "idle";
V.rival.cut = "slash";
for(let i=0;i<24;i++){ V.update(STEP); if(V.rival.phase==="startup") break; }
check("rival measure still slash", V.rival.phase==="startup" && V.rival.cut==="slash", `ph=${V.rival.phase} cut=${V.rival.cut} g=${V.bodyGap().toFixed(1)}`);

toMeasure();
V.attackEdge=true;
for(let i=0;i<20;i++){ V.update(STEP); if(V.player.phase==="startup" && V.player.cut==="slash") break; }
check("Space still slash after L exists", V.player.phase==="startup" && V.player.cut==="slash", `ph=${V.player.phase} cut=${V.player.cut}`);

check("slash cancel window 80-120", V.SLASH_CANCEL_MS>=80 && V.SLASH_CANCEL_MS<=120, `ms=${V.SLASH_CANCEL_MS}`);
check("slash frames still 180/140/280 after cancel", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280);
check("golpe frames still 120/80/180 after cancel", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180);

function holdRival(){
  Math.random = () => 0.99;
  V.rival.boltCd = 99999;
  V.rival.throwCd = 99999;
  V.rival.revCd = 99999;
  V.rival.revArmed = -1;
  V.rival.pbCd = 99999;
  V.rival.pbArmed = -1;
  V.rival.boltPhase = "";
  V.rival.wantBlock = false;
  V.rival.guardChoice = -1;
  V.rival.closing = false;
  V.rival.standWait = 0;
  V.rival.standGoal = 99999;
}
function waitUntilWindow(){
  for(let i=0;i<60;i++){
    holdRival();
    if(V.player.phase==="recovery" && V.player.cut==="slash" && (V.RECOVERY - V.player.phaseT) <= V.SLASH_CANCEL_MS) return true;
    if(V.player.phase==="idle") return false;
    V.update(STEP);
  }
  return false;
}
function landCloseSlash(){
  toClose();
  freezeRivalAI();
  Math.random = () => 0.99;
  V.attackEdge = true;
  for(let i=0;i<80;i++){
    holdRival();
    V.rival.guarding = false;
    V.update(STEP);
    if(V.rival.hp<100 || V.hitstopLeft>0) break;
  }
  drainHitstop();
}

landCloseSlash();
check("tajo hit arms cutHit", V.player.cutHit===true && V.player.cut==="slash" && V.player.phase==="recovery", `hit=${V.player.cutHit} cut=${V.player.cut} ph=${V.player.phase} hp=${V.rival.hp}`);
const recEarly = V.player.phaseT;
V.golpeEdge = true;
V.update(STEP);
check("L early recovery after hit still buffers", V.player.phase==="recovery" && V.player.cut==="slash" && V.golpeBuf && recEarly < (V.RECOVERY - V.SLASH_CANCEL_MS), `ph=${V.player.phase} cut=${V.player.cut} buf=${V.golpeBuf} t=${recEarly.toFixed(0)}`);
let fireLeft=-1;
for(let i=0;i<60;i++){
  holdRival();
  const left = (V.player.phase==="recovery" && V.player.cut==="slash") ? (V.RECOVERY - V.player.phaseT) : 999;
  V.update(STEP);
  if(V.player.cut==="golpe" && V.player.phase==="startup"){ fireLeft=left; break; }
  if(V.player.phase==="idle") break;
}
check("buffered L cancels in last 100ms", fireLeft>=0 && fireLeft<=V.SLASH_CANCEL_MS+STEP && V.player.cut==="golpe" && !V.golpeBuf, `left=${fireLeft} cut=${V.player.cut} ph=${V.player.phase} buf=${V.golpeBuf}`);

landCloseSlash();
const gotWin = waitUntilWindow();
const recLeft = V.player.phase==="recovery" ? (V.RECOVERY - V.player.phaseT) : -1;
V.golpeEdge = true;
V.update(STEP);
check("L in window after hit starts golpe", gotWin && recLeft<=V.SLASH_CANCEL_MS && V.player.cut==="golpe" && V.player.phase==="startup", `win=${gotWin} left=${recLeft.toFixed(0)} cut=${V.player.cut} ph=${V.player.phase}`);
const hpAfterTajo = V.rival.hp;
for(let i=0;i<50;i++){
  holdRival();
  V.update(STEP);
  drainHitstop();
  if(V.rival.hp < hpAfterTajo) break;
}
check("tajo-golpe combo on hit", hpAfterTajo===90 && V.rival.hp===80, `hp0=${hpAfterTajo} hp=${V.rival.hp} stun=${V.rival.stunT.toFixed(0)} cut=${V.player.cut} ph=${V.player.phase} g=${V.bodyGap().toFixed(1)}`);

toClose();
Math.random = () => 0;
V.rival.stamina = 80;
V.rival.stamRegenT = 99999;
V.rival.guardBreakT = 0;
V.rival.wantBlock = true;
V.rival.guarding = true;
V.rival.guardChoice = 1;
V.rival.revCd = 99999;
V.rival.revArmed = -1;
V.rival.pbCd = 99999;
V.rival.pbArmed = -1;
V.attackEdge = true;
for(let i=0;i<50;i++){
  V.rival.wantBlock = true;
  V.rival.guarding = true;
  V.rival.guardChoice = 1;
  V.rival.revCd = 99999;
  V.rival.revArmed = -1;
  V.rival.pbCd = 99999;
  V.rival.pbArmed = -1;
  V.update(STEP);
  if(V.hitstopLeft>0 || (V.player.phase==="recovery" && V.player.cutHit)) break;
}
drainHitstop();
check("tajo block arms cutHit", V.player.cutHit===true && V.rival.hp===100, `hit=${V.player.cutHit} hp=${V.rival.hp} ph=${V.player.phase}`);
const blkWin = waitUntilWindow();
V.golpeEdge = true;
V.update(STEP);
check("L in window after block starts golpe", blkWin && V.player.cut==="golpe" && V.player.phase==="startup", `win=${blkWin} cut=${V.player.cut} ph=${V.player.phase} hit=${V.player.cutHit}`);

bootPlay(); wait(1700);
freezeYouAI();
freezeRivalAI();
V.player.x = 180;
V.rival.x = 1100;
V.attackEdge = true;
for(let i=0;i<80;i++){
  freezeRivalAI();
  V.update(STEP);
  if(V.player.phase==="recovery" && V.player.cut==="slash") break;
}
check("whiff slash has no cutHit", V.player.phase==="recovery" && V.player.cut==="slash" && !V.player.cutHit && V.rival.hp===100, `ph=${V.player.phase} hit=${V.player.cutHit} hp=${V.rival.hp}`);
for(let i=0;i<40;i++){
  freezeRivalAI();
  V.update(STEP);
  if((V.RECOVERY - V.player.phaseT) <= V.SLASH_CANCEL_MS) break;
  if(V.player.phase==="idle") break;
}
const whiffLeft = V.player.phase==="recovery" ? (V.RECOVERY - V.player.phaseT) : -1;
V.golpeEdge = true;
V.update(STEP);
check("L in window on whiff does not cancel", V.player.phase==="recovery" && V.player.cut==="slash" && V.golpeBuf && whiffLeft<=V.SLASH_CANCEL_MS, `ph=${V.player.phase} cut=${V.player.cut} buf=${V.golpeBuf} left=${whiffLeft.toFixed(0)}`);

function holdYouDummy(){
  V.keys.clear();
  V.player.wantBlock = false;
  V.attackEdge = false;
  V.golpeEdge = false;
  V.boltEdge = false;
  V.throwEdge = false;
}
function landRivalCloseSlash(block){
  toClose();
  freezeYouAI();
  freezeRivalAI();
  V.player.hp = 100;
  V.rival.hp = 100;
  V.player.stamina = 100;
  V.player.stamRegenT = 99999;
  V.player.guardBreakT = 0;
  V.player.stunT = 0;
  if(block){
    V.keys.add("KeyS");
    V.player.wantBlock = true;
    V.player.guarding = true;
    V.player.guardRaiseElapsed = V.PARRY_WIN_MS; // hold-block, not perfect parry
  } else {
    V.keys.clear();
    V.player.wantBlock = false;
    V.player.guarding = false;
  }
  V.startAttack(V.rival);
  for(let i=0;i<90;i++){
    if(block){
      V.keys.add("KeyS");
      V.player.wantBlock = true;
      V.player.guarding = true;
      V.player.guardRaiseElapsed = V.PARRY_WIN_MS;
    } else {
      holdYouDummy();
      V.player.guarding = false;
    }
    V.update(STEP);
    if(V.hitstopLeft>0 || V.player.hp<100 || (block && V.player.pushT>0 && V.rival.cutHit)) break;
  }
  drainHitstop();
}
function waitRivalSlashWindow(){
  for(let i=0;i<60;i++){
    holdYouDummy();
    const left = (V.rival.phase==="recovery" && V.rival.cut==="slash") ? (V.RECOVERY - V.rival.phaseT) : 999;
    V.update(STEP);
    if(V.rival.cut==="golpe" && V.rival.phase==="startup") return left;
    if(V.rival.phase==="idle") return -1;
  }
  return -1;
}
function landRivalCloseGolpe(block){
  toClose();
  freezeYouAI();
  freezeRivalAI();
  V.player.hp = 100;
  V.rival.hp = 100;
  V.player.stamina = 100;
  V.player.stamRegenT = 99999;
  V.player.guardBreakT = 0;
  V.player.stunT = 0;
  if(block){
    V.keys.add("KeyS");
    V.player.wantBlock = true;
    V.player.guarding = true;
    V.player.guardRaiseElapsed = V.PARRY_WIN_MS; // hold-block, not perfect parry
  } else {
    V.keys.clear();
    V.player.wantBlock = false;
    V.player.guarding = false;
  }
  V.startAttack(V.rival, "golpe");
  for(let i=0;i<90;i++){
    if(block){
      V.keys.add("KeyS");
      V.player.wantBlock = true;
      V.player.guarding = true;
      V.player.guardRaiseElapsed = V.PARRY_WIN_MS;
    } else {
      holdYouDummy();
      V.player.guarding = false;
    }
    V.update(STEP);
    if(V.hitstopLeft>0 || V.player.hp<100 || (block && V.player.pushT>0 && V.rival.cutHit)) break;
  }
  drainHitstop();
}
function waitRivalGolpeWindow(){
  for(let i=0;i<60;i++){
    holdYouDummy();
    const left = (V.rival.phase==="recovery" && V.rival.cut==="golpe") ? (V.GOLPE_RECOVERY - V.rival.phaseT) : 999;
    V.update(STEP);
    if(V.rival.cut==="slash" && V.rival.phase==="startup") return left;
    if(V.rival.phase==="idle") return -1;
  }
  return -1;
}

toClose();
freezeYouAI();
freezeRivalAI();
V.rival.linkGolpe = true;
V.rival.cutHit = true;
V.rival.standGoal = 99999;
V.update(STEP);
check("rival does not link tajo-golpe from idle", V.rival.phase==="idle" && V.rival.cut==="slash", `cut=${V.rival.cut} ph=${V.rival.phase} link=${V.rival.linkGolpe}`);

toClose();
freezeYouAI();
freezeRivalAI();
V.rival.phase = "recovery";
V.rival.phaseT = V.RECOVERY - 40;
V.rival.cut = "slash";
V.rival.cutHit = true;
V.rival.linkGolpe = false;
V.rival.standGoal = 99999;
V.update(STEP);
check("rival does not mash every connected slash", V.rival.cut==="slash" && V.rival.phase==="recovery", `cut=${V.rival.cut} ph=${V.rival.phase} t=${V.rival.phaseT.toFixed(0)} link=${V.rival.linkGolpe}`);

landRivalCloseSlash(false);
check("rival tajo hit arms cutHit", V.rival.cutHit===true && V.rival.cut==="slash" && V.rival.phase==="recovery" && V.player.hp===90, `hit=${V.rival.cutHit} cut=${V.rival.cut} ph=${V.rival.phase} hp=${V.player.hp}`);
V.rival.linkGolpe = true;
const fireLeftR = waitRivalSlashWindow();
check("rival links slash to L on hit", fireLeftR>=0 && fireLeftR<=V.SLASH_CANCEL_MS+STEP && V.rival.cut==="golpe" && V.rival.phase==="startup", `left=${fireLeftR} cut=${V.rival.cut} ph=${V.rival.phase} hit=${V.rival.cutHit}`);

landRivalCloseSlash(true);
check("rival tajo block arms cutHit", V.rival.cutHit===true && V.rival.cut==="slash" && V.player.hp===100, `hit=${V.rival.cutHit} hp=${V.player.hp} ph=${V.rival.phase} push=${V.player.pushT}`);
V.rival.linkGolpe = true;
const fireLeftRB = waitRivalSlashWindow();
check("rival links slash to L on block", fireLeftRB>=0 && fireLeftRB<=V.SLASH_CANCEL_MS+STEP && V.rival.cut==="golpe" && V.rival.phase==="startup", `left=${fireLeftRB} cut=${V.rival.cut} ph=${V.rival.phase} hit=${V.rival.cutHit}`);

bootPlay(); wait(1700);
freezeYouAI();
freezeRivalAI();
V.player.x = 180;
V.rival.x = 1100;
V.startAttack(V.rival);
for(let i=0;i<80;i++){
  holdYouDummy();
  V.update(STEP);
  if(V.rival.phase==="recovery" && V.rival.cut==="slash") break;
}
check("rival whiff slash has no cutHit", V.rival.phase==="recovery" && V.rival.cut==="slash" && !V.rival.cutHit && V.player.hp===100, `ph=${V.rival.phase} hit=${V.rival.cutHit} hp=${V.player.hp}`);
V.rival.linkGolpe = true;
for(let i=0;i<40;i++){
  holdYouDummy();
  V.update(STEP);
  if(V.rival.cut==="golpe") break;
  if((V.RECOVERY - V.rival.phaseT) <= V.SLASH_CANCEL_MS) break;
  if(V.rival.phase==="idle") break;
}
const whiffLeftR = V.rival.phase==="recovery" ? (V.RECOVERY - V.rival.phaseT) : -1;
check("rival does not link slash to L on whiff", V.rival.phase==="recovery" && V.rival.cut==="slash" && !V.rival.cutHit && whiffLeftR<=V.SLASH_CANCEL_MS, `ph=${V.rival.phase} cut=${V.rival.cut} hit=${V.rival.cutHit} left=${whiffLeftR.toFixed(0)}`);

check("rival slash-golpe is a chance", /armRivalSlashLink/.test(codeOnly) && /rivalLinkRoll/.test(sliceFn(codeOnly, "armRivalSlashLink")));
check("player slash-golpe door still you-shared", /cut !== "slash" \|\| !f.cutHit/.test(sliceFn(codeOnly, "slashToGolpeWindow")));

check("golpe cancel window 80-100", V.GOLPE_CANCEL_MS>=80 && V.GOLPE_CANCEL_MS<=100, `ms=${V.GOLPE_CANCEL_MS}`);
check("slash frames still 180/140/280 after reverse", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280);
check("golpe frames still 120/80/180 after reverse", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180);
check("slash cancel window still 100", V.SLASH_CANCEL_MS===100, `ms=${V.SLASH_CANCEL_MS}`);

function waitUntilReverseWindow(){
  for(let i=0;i<60;i++){
    holdRival();
    if(V.player.phase==="recovery" && V.player.cut==="golpe" && (V.GOLPE_RECOVERY - V.player.phaseT) <= V.GOLPE_CANCEL_MS) return true;
    if(V.player.phase==="idle") return false;
    V.update(STEP);
  }
  return false;
}
function landCloseGolpe(){
  toClose();
  freezeRivalAI();
  Math.random = () => 0.99;
  V.golpeEdge = true;
  for(let i=0;i<80;i++){
    holdRival();
    V.rival.guarding = false;
    V.update(STEP);
    if(V.rival.hp<100 || V.hitstopLeft>0) break;
  }
  drainHitstop();
}

landCloseGolpe();
check("golpe hit arms cutHit", V.player.cutHit===true && V.player.cut==="golpe" && V.player.phase==="recovery", `hit=${V.player.cutHit} cut=${V.player.cut} ph=${V.player.phase} hp=${V.rival.hp}`);
const recEarlyG = V.player.phaseT;
V.attackEdge = true;
V.update(STEP);
check("Space early recovery after L hit still buffers", V.player.phase==="recovery" && V.player.cut==="golpe" && V.slashBuf && recEarlyG < (V.GOLPE_RECOVERY - V.GOLPE_CANCEL_MS), `ph=${V.player.phase} cut=${V.player.cut} buf=${V.slashBuf} t=${recEarlyG.toFixed(0)}`);
let fireLeftG=-1;
for(let i=0;i<60;i++){
  holdRival();
  const left = (V.player.phase==="recovery" && V.player.cut==="golpe") ? (V.GOLPE_RECOVERY - V.player.phaseT) : 999;
  V.update(STEP);
  if(V.player.cut==="slash" && V.player.phase==="startup"){ fireLeftG=left; break; }
  if(V.player.phase==="idle") break;
}
check("buffered Space cancels in last 100ms of L", fireLeftG>=0 && fireLeftG<=V.GOLPE_CANCEL_MS+STEP && V.player.cut==="slash" && !V.slashBuf, `left=${fireLeftG} cut=${V.player.cut} ph=${V.player.phase} buf=${V.slashBuf}`);

landCloseGolpe();
const gotRev = waitUntilReverseWindow();
const recLeftG = V.player.phase==="recovery" ? (V.GOLPE_RECOVERY - V.player.phaseT) : -1;
V.attackEdge = true;
V.update(STEP);
check("Space in window after L hit starts slash", gotRev && recLeftG<=V.GOLPE_CANCEL_MS && V.player.cut==="slash" && V.player.phase==="startup", `win=${gotRev} left=${recLeftG.toFixed(0)} cut=${V.player.cut} ph=${V.player.phase}`);

toClose();
Math.random = () => 0;
V.rival.stamina = 80;
V.rival.stamRegenT = 99999;
V.rival.guardBreakT = 0;
V.rival.wantBlock = true;
V.rival.guarding = true;
V.rival.guardChoice = 1;
V.golpeEdge = true;
for(let i=0;i<50;i++){
  V.rival.wantBlock = true;
  V.rival.guarding = true;
  V.rival.guardChoice = 1;
  V.update(STEP);
  if(V.hitstopLeft>0 || (V.player.phase==="recovery" && V.player.cutHit && V.player.cut==="golpe")) break;
}
drainHitstop();
check("golpe block arms cutHit", V.player.cutHit===true && V.player.cut==="golpe" && V.rival.hp===100, `hit=${V.player.cutHit} hp=${V.rival.hp} ph=${V.player.phase} cut=${V.player.cut}`);
const blkRev = waitUntilReverseWindow();
V.attackEdge = true;
V.update(STEP);
check("Space in window after L block starts slash", blkRev && V.player.cut==="slash" && V.player.phase==="startup", `win=${blkRev} cut=${V.player.cut} ph=${V.player.phase} hit=${V.player.cutHit}`);

bootPlay(); wait(1700);
freezeYouAI();
freezeRivalAI();
V.player.x = 180;
V.rival.x = 1100;
V.golpeEdge = true;
for(let i=0;i<80;i++){
  freezeRivalAI();
  V.update(STEP);
  if(V.player.phase==="recovery" && V.player.cut==="golpe") break;
}
check("whiff golpe has no cutHit", V.player.phase==="recovery" && V.player.cut==="golpe" && !V.player.cutHit && V.rival.hp===100, `ph=${V.player.phase} hit=${V.player.cutHit} hp=${V.rival.hp}`);
for(let i=0;i<40;i++){
  freezeRivalAI();
  V.update(STEP);
  if((V.GOLPE_RECOVERY - V.player.phaseT) <= V.GOLPE_CANCEL_MS) break;
  if(V.player.phase==="idle") break;
}
const whiffLeftG = V.player.phase==="recovery" ? (V.GOLPE_RECOVERY - V.player.phaseT) : -1;
V.attackEdge = true;
V.update(STEP);
check("Space in window on L whiff does not cancel", V.player.phase==="recovery" && V.player.cut==="golpe" && V.slashBuf && whiffLeftG<=V.GOLPE_CANCEL_MS, `ph=${V.player.phase} cut=${V.player.cut} buf=${V.slashBuf} left=${whiffLeftG.toFixed(0)}`);

toClose();
freezeYouAI();
freezeRivalAI();
V.rival.linkSlash = true;
V.rival.cutHit = true;
V.rival.standGoal = 99999;
V.update(STEP);
check("rival does not link golpe-tajo from idle", V.rival.phase==="idle" && V.rival.cut==="slash", `cut=${V.rival.cut} ph=${V.rival.phase} link=${V.rival.linkSlash}`);

toClose();
freezeYouAI();
freezeRivalAI();
V.rival.phase = "recovery";
V.rival.phaseT = V.GOLPE_RECOVERY - 40;
V.rival.cut = "golpe";
V.rival.cutHit = true;
V.rival.linkSlash = false;
V.rival.standGoal = 99999;
V.update(STEP);
check("rival does not mash every connected golpe", V.rival.cut==="golpe" && V.rival.phase==="recovery", `cut=${V.rival.cut} ph=${V.rival.phase} t=${V.rival.phaseT.toFixed(0)} link=${V.rival.linkSlash}`);

landRivalCloseGolpe(false);
check("rival golpe hit arms cutHit", V.rival.cutHit===true && V.rival.cut==="golpe" && V.rival.phase==="recovery" && V.player.hp===90, `hit=${V.rival.cutHit} cut=${V.rival.cut} ph=${V.rival.phase} hp=${V.player.hp}`);
V.rival.linkSlash = true;
const fireLeftRG = waitRivalGolpeWindow();
check("rival links golpe to Space on hit", fireLeftRG>=0 && fireLeftRG<=V.GOLPE_CANCEL_MS+STEP && V.rival.cut==="slash" && V.rival.phase==="startup", `left=${fireLeftRG} cut=${V.rival.cut} ph=${V.rival.phase} hit=${V.rival.cutHit}`);

landRivalCloseGolpe(true);
check("rival golpe block arms cutHit", V.rival.cutHit===true && V.rival.cut==="golpe" && V.player.hp===100, `hit=${V.rival.cutHit} hp=${V.player.hp} ph=${V.rival.phase} push=${V.player.pushT}`);
V.rival.linkSlash = true;
const fireLeftRGB = waitRivalGolpeWindow();
check("rival links golpe to Space on block", fireLeftRGB>=0 && fireLeftRGB<=V.GOLPE_CANCEL_MS+STEP && V.rival.cut==="slash" && V.rival.phase==="startup", `left=${fireLeftRGB} cut=${V.rival.cut} ph=${V.rival.phase} hit=${V.rival.cutHit}`);

bootPlay(); wait(1700);
freezeYouAI();
freezeRivalAI();
V.player.x = 180;
V.rival.x = 1100;
V.startAttack(V.rival, "golpe");
for(let i=0;i<80;i++){
  holdYouDummy();
  V.update(STEP);
  if(V.rival.phase==="recovery" && V.rival.cut==="golpe") break;
}
check("rival whiff golpe has no cutHit", V.rival.phase==="recovery" && V.rival.cut==="golpe" && !V.rival.cutHit && V.player.hp===100, `ph=${V.rival.phase} hit=${V.rival.cutHit} hp=${V.player.hp}`);
V.rival.linkSlash = true;
for(let i=0;i<40;i++){
  holdYouDummy();
  V.update(STEP);
  if(V.rival.cut==="slash" && V.rival.phase==="startup") break;
  if((V.GOLPE_RECOVERY - V.rival.phaseT) <= V.GOLPE_CANCEL_MS) break;
  if(V.rival.phase==="idle") break;
}
const whiffLeftRG = V.rival.phase==="recovery" ? (V.GOLPE_RECOVERY - V.rival.phaseT) : -1;
check("rival does not link golpe to Space on whiff", V.rival.phase==="recovery" && V.rival.cut==="golpe" && !V.rival.cutHit && whiffLeftRG<=V.GOLPE_CANCEL_MS, `ph=${V.rival.phase} cut=${V.rival.cut} hit=${V.rival.cutHit} left=${whiffLeftRG.toFixed(0)}`);

check("rival golpe-slash is a chance", /armRivalGolpeLink/.test(codeOnly) && /rivalLinkRoll/.test(sliceFn(codeOnly, "armRivalGolpeLink")));
check("player golpe-slash door still shared", /cut !== "golpe" \|\| !f.cutHit/.test(sliceFn(codeOnly, "golpeToSlashWindow")));
check("player reverse still you-buffered", /cancelIntoSlash\(player\)/.test(codeOnly) && /golpeToSlashWindow\(player\)/.test(codeOnly));

check("bolt cancel window 80-120", V.BOLT_CANCEL_MS>=80 && V.BOLT_CANCEL_MS<=120, `ms=${V.BOLT_CANCEL_MS}`);
check("bolt cancel window 100", V.BOLT_CANCEL_MS===100, `ms=${V.BOLT_CANCEL_MS}`);
check("K frames still 200/280 after special cancel", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280);
check("K still costs 30 after special cancel", V.BOLT_STAM===30);
check("slash frames still 180/140/280 after special cancel", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280);
check("golpe frames still 120/80/180 after special cancel", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180);
check("two-button windows still 100", V.SLASH_CANCEL_MS===100 && V.GOLPE_CANCEL_MS===100, `s=${V.SLASH_CANCEL_MS} g=${V.GOLPE_CANCEL_MS}`);

function waitUntilBoltWindow(cut){
  for(let i=0;i<60;i++){
    holdRival();
    const rec = cut==="golpe" ? V.GOLPE_RECOVERY : V.RECOVERY;
    if(V.player.phase==="recovery" && V.player.cut===cut && (rec - V.player.phaseT) <= V.BOLT_CANCEL_MS) return true;
    if(V.player.phase==="idle") return false;
    V.update(STEP);
  }
  return false;
}

landCloseSlash();
check("tajo hit still arms cutHit for K", V.player.cutHit===true && V.player.cut==="slash" && V.player.phase==="recovery", `hit=${V.player.cutHit} cut=${V.player.cut} ph=${V.player.phase} hp=${V.rival.hp}`);
const recEarlyK = V.player.phaseT;
const stEarlyK = V.player.stamina;
V.player.stamRegenT = 99999;
V.boltEdge = true;
V.update(STEP);
check("K early recovery after tajo hit still buffers", V.player.phase==="recovery" && V.player.cut==="slash" && V.boltBuf && recEarlyK < (V.RECOVERY - V.BOLT_CANCEL_MS) && !V.player.boltPhase, `ph=${V.player.phase} cut=${V.player.cut} buf=${V.boltBuf} t=${recEarlyK.toFixed(0)} bph=${V.player.boltPhase}`);
let fireLeftK=-1;
let stFireK=stEarlyK;
for(let i=0;i<60;i++){
  holdRival();
  V.player.stamRegenT = 99999;
  const left = (V.player.phase==="recovery" && V.player.cut==="slash") ? (V.RECOVERY - V.player.phaseT) : 999;
  V.update(STEP);
  if(V.player.boltPhase==="startup"){ fireLeftK=left; stFireK=V.player.stamina; break; }
  if(V.player.phase==="idle" && !V.player.boltPhase) break;
}
check("buffered K cancels in last 100ms of tajo", fireLeftK>=0 && fireLeftK<=V.BOLT_CANCEL_MS+STEP && V.player.boltPhase==="startup" && V.player.phase==="idle" && !V.boltBuf, `left=${fireLeftK} bph=${V.player.boltPhase} ph=${V.player.phase} buf=${V.boltBuf}`);
check("tajo-K cancel still costs 30", Math.abs(stFireK - (stEarlyK-30))<0.2, `st=${stFireK.toFixed(2)} s0=${stEarlyK.toFixed(2)}`);

landCloseSlash();
V.player.stamRegenT = 99999;
const stWinK = V.player.stamina;
const gotBolt = waitUntilBoltWindow("slash");
const recLeftK = V.player.phase==="recovery" ? (V.RECOVERY - V.player.phaseT) : -1;
const tajoPlant = V.destRect(V.player);
V.boltEdge = true;
V.update(STEP);
check("K in window after tajo hit starts dart", gotBolt && recLeftK<=V.BOLT_CANCEL_MS && V.player.boltPhase==="startup" && V.player.phase==="idle", `win=${gotBolt} left=${recLeftK.toFixed(0)} bph=${V.player.boltPhase} ph=${V.player.phase}`);
check("K in window after tajo hit costs 30", Math.abs(V.player.stamina - (stWinK-30))<0.2, `st=${V.player.stamina.toFixed(2)} s0=${stWinK.toFixed(2)}`);
check("K plant puff still on special cancel", V.brasaFxKind==="cast" && V.brasaFxT>150 && !V.bolt, `kind=${V.brasaFxKind} t=${V.brasaFxT} bolt=${!!V.bolt}`);

function sheetSrc(f){ return (V.poseBitmap(f)&&V.poseBitmap(f)._src)||""; }
function isCutSheet(f){ const src=sheetSrc(f); return /slash/.test(src) && !/windup/.test(src); }
function destNear(a,b,eps){
  return Math.abs(a.dx-b.dx)<eps && Math.abs(a.dy-b.dy)<eps && Math.abs(a.dw-b.dw)<eps && Math.abs(a.dh-b.dh)<eps && Math.abs(a.rot-b.rot)<0.002;
}
function holdCutPlant(label, plantDest){
  const d0 = plantDest || V.destRect(V.player);
  const tip0 = V.bladeTipX(V.player);
  check(label+" does not hop destRect off the cut", destNear(V.destRect(V.player), d0, 0.75), `dx=${(V.destRect(V.player).dx-d0.dx).toFixed(2)} dy=${(V.destRect(V.player).dy-d0.dy).toFixed(2)}`);
  check(label+" holds cut sheet at plant start", V.player.boltPhase==="startup" && isCutSheet(V.player), `bph=${V.player.boltPhase} src=${sheetSrc(V.player)}`);
  // Close-range dart is born on the rival and resolveBolt eats it the same tick.
  // Park them so birth can be sampled; player destRect/tip stay on the cut plant.
  V.rival.x = 1220;
  V.rival.stunT = 0;
  V.rival.guarding = false;
  V.rival.falling = false;
  V.rival.hp = V.MAX_HP;
  let popped=false, hopped=false, birthTip=null, birthX=null, birthFace=0, birthW=0, held=0;
  for(let i=0;i<20;i++){
    if(V.player.boltPhase!=="startup") break;
    if(!isCutSheet(V.player)) popped=true;
    if(!destNear(V.destRect(V.player), d0, 0.75)) hopped=true;
    holdRival();
    V.rival.x = 1220;
    V.rival.stunT = 0;
    V.rival.guarding = false;
    const tipBefore = V.bladeTipX(V.player);
    const had = !!V.bolt;
    V.update(STEP);
    held += STEP;
    if(!had && V.bolt){
      birthTip = tipBefore;
      birthX = V.bolt.x;
      birthFace = V.bolt.facing;
      birthW = V.bolt.w;
    }
  }
  check(label+" holds cut pose through 200ms", !popped && held>=180 && held<=220 && V.player.boltPhase==="recovery" && isCutSheet(V.player), `popped=${popped} held=${held} bph=${V.player.boltPhase} src=${sheetSrc(V.player)}`);
  check(label+" destRect plant holds through 200ms", !hopped, `dx=${(V.destRect(V.player).dx-d0.dx).toFixed(2)} dy=${(V.destRect(V.player).dy-d0.dy).toFixed(2)}`);
  const travel = V.BOLT_SPEED * (STEP/1000);
  const birth = birthFace>0 ? birthX - travel : birthX + birthW + travel;
  // v346: tip eases leftover sheathe→slash under holdCutFade during plant; birth is on
  // seated slash tip (not tip0 at cancel). Dart still births from live tip at spawn tick.
  check(label+" dart births from live cut tip", birthTip!=null && Math.abs(birth-birthTip)<3 && Math.abs(birthTip-V.bladeTipX(V.player))<8, `birth=${birth&&birth.toFixed(1)} live=${birthTip&&birthTip.toFixed(1)} plant=${tip0.toFixed(1)} now=${V.bladeTipX(V.player).toFixed(1)}`);
  const dartLive = !!V.bolt;
  const dartX0 = V.bolt ? V.bolt.x : null;
  let recPop=false, recHop=false, recHeld=0, dartBack=false;
  for(let i=0;i<30;i++){
    if(V.player.boltPhase!=="recovery") break;
    if(!isCutSheet(V.player)) recPop=true;
    if(!destNear(V.destRect(V.player), d0, 0.75)) recHop=true;
    holdRival();
    V.rival.x = 1220;
    V.rival.stunT = 0;
    V.rival.guarding = false;
    V.update(STEP);
    recHeld += STEP;
    if(V.bolt && Math.abs(V.bolt.x - (V.bladeTipX(V.player) - (V.bolt.facing>0 ? 0 : V.bolt.w))) < 4) dartBack=true;
  }
  check(label+" holds cut pose through K recovery", !recPop && recHeld>=260 && recHeld<=300 && !V.player.boltPhase, `popped=${recPop} held=${recHeld} bph=${V.player.boltPhase} src=${sheetSrc(V.player)}`);
  check(label+" destRect plant holds through K recovery", !recHop, `dx=${(V.destRect(V.player).dx-d0.dx).toFixed(2)} dy=${(V.destRect(V.player).dy-d0.dy).toFixed(2)}`);
  check(label+" dart gone from blade after birth", dartLive && dartX0!=null && !dartBack, `live=${dartLive} x0=${dartX0} back=${dartBack} bolt=${!!V.bolt}`);
  check(label+" sheathes from the cut not idle pop", isCutSheet(V.player) && !V.player.boltPhase, `src=${sheetSrc(V.player)} bph=${V.player.boltPhase}`);
}
holdCutPlant("tajo-K", tajoPlant);

toClose();
Math.random = () => 0;
V.rival.stamina = 80;
V.rival.stamRegenT = 99999;
V.rival.guardBreakT = 0;
V.rival.wantBlock = true;
V.rival.guarding = true;
V.rival.guardChoice = 1;
V.rival.revCd = 99999;
V.rival.revArmed = -1;
V.rival.pbCd = 99999;
V.rival.pbArmed = -1;
V.attackEdge = true;
for(let i=0;i<50;i++){
  V.rival.wantBlock = true;
  V.rival.guarding = true;
  V.rival.guardChoice = 1;
  V.rival.revCd = 99999;
  V.rival.revArmed = -1;
  V.rival.pbCd = 99999;
  V.rival.pbArmed = -1;
  V.update(STEP);
  if(V.hitstopLeft>0 || (V.player.phase==="recovery" && V.player.cutHit)) break;
}
drainHitstop();
check("tajo block still arms cutHit for K", V.player.cutHit===true && V.rival.hp===100, `hit=${V.player.cutHit} hp=${V.rival.hp} ph=${V.player.phase}`);
V.player.stamRegenT = 99999;
const blkBolt = waitUntilBoltWindow("slash");
const stBlkK = V.player.stamina;
V.boltEdge = true;
V.update(STEP);
check("K in window after tajo block starts dart", blkBolt && V.player.boltPhase==="startup" && V.player.phase==="idle", `win=${blkBolt} bph=${V.player.boltPhase} ph=${V.player.phase} hit=${V.player.cutHit}`);
check("K after tajo block still costs 30", Math.abs(V.player.stamina - (stBlkK-30))<0.2, `st=${V.player.stamina.toFixed(2)} s0=${stBlkK.toFixed(2)}`);

bootPlay(); wait(1700);
freezeYouAI();
freezeRivalAI();
V.player.x = 180;
V.rival.x = 1100;
V.attackEdge = true;
for(let i=0;i<80;i++){
  freezeRivalAI();
  V.update(STEP);
  if(V.player.phase==="recovery" && V.player.cut==="slash") break;
}
check("whiff slash still has no cutHit for K", V.player.phase==="recovery" && V.player.cut==="slash" && !V.player.cutHit && V.rival.hp===100, `ph=${V.player.phase} hit=${V.player.cutHit} hp=${V.rival.hp}`);
for(let i=0;i<40;i++){
  freezeRivalAI();
  V.update(STEP);
  if((V.RECOVERY - V.player.phaseT) <= V.BOLT_CANCEL_MS) break;
  if(V.player.phase==="idle") break;
}
const whiffLeftK = V.player.phase==="recovery" ? (V.RECOVERY - V.player.phaseT) : -1;
const stWhiffK = V.player.stamina;
V.player.stamRegenT = 99999;
V.boltEdge = true;
V.update(STEP);
check("K in window on tajo whiff does not cancel", V.player.phase==="recovery" && V.player.cut==="slash" && V.boltBuf && !V.player.boltPhase && whiffLeftK<=V.BOLT_CANCEL_MS && Math.abs(V.player.stamina-stWhiffK)<0.2, `ph=${V.player.phase} cut=${V.player.cut} buf=${V.boltBuf} bph=${V.player.boltPhase} left=${whiffLeftK.toFixed(0)}`);
for(let i=0;i<40;i++){
  freezeRivalAI();
  V.player.stamRegenT = 99999;
  V.update(STEP);
  if(V.player.phase==="idle") break;
}
check("whiff K buffer dies at idle not a free dart", V.player.phase==="idle" && !V.player.boltPhase && !V.boltBuf && Math.abs(V.player.stamina-stWhiffK)<0.2, `ph=${V.player.phase} bph=${V.player.boltPhase} buf=${V.boltBuf} st=${V.player.stamina.toFixed(2)}`);

landCloseSlash();
V.player.stamina = 29;
V.player.stamRegenT = 99999;
const gotPoor = waitUntilBoltWindow("slash");
V.boltEdge = true;
V.update(STEP);
check("K in window stam<30 does not cancel", gotPoor && V.player.phase==="recovery" && V.player.cut==="slash" && !V.player.boltPhase && V.player.stamina===29, `win=${gotPoor} ph=${V.player.phase} bph=${V.player.boltPhase} st=${V.player.stamina}`);

landCloseGolpe();
check("golpe hit still arms cutHit for K", V.player.cutHit===true && V.player.cut==="golpe" && V.player.phase==="recovery", `hit=${V.player.cutHit} cut=${V.player.cut} ph=${V.player.phase} hp=${V.rival.hp}`);
const recEarlyGK = V.player.phaseT;
const stEarlyGK = V.player.stamina;
V.player.stamRegenT = 99999;
V.boltEdge = true;
V.update(STEP);
check("K early recovery after L hit still buffers", V.player.phase==="recovery" && V.player.cut==="golpe" && V.boltBuf && recEarlyGK < (V.GOLPE_RECOVERY - V.BOLT_CANCEL_MS) && !V.player.boltPhase, `ph=${V.player.phase} cut=${V.player.cut} buf=${V.boltBuf} t=${recEarlyGK.toFixed(0)}`);
let fireLeftGK=-1;
for(let i=0;i<60;i++){
  holdRival();
  V.player.stamRegenT = 99999;
  const left = (V.player.phase==="recovery" && V.player.cut==="golpe") ? (V.GOLPE_RECOVERY - V.player.phaseT) : 999;
  V.update(STEP);
  if(V.player.boltPhase==="startup"){ fireLeftGK=left; break; }
  if(V.player.phase==="idle" && !V.player.boltPhase) break;
}
check("buffered K cancels in last 100ms of L", fireLeftGK>=0 && fireLeftGK<=V.BOLT_CANCEL_MS+STEP && V.player.boltPhase==="startup" && V.player.phase==="idle" && !V.boltBuf, `left=${fireLeftGK} bph=${V.player.boltPhase} ph=${V.player.phase} buf=${V.boltBuf}`);
check("golpe-K cancel still costs 30", Math.abs(V.player.stamina - (stEarlyGK-30))<0.2, `st=${V.player.stamina.toFixed(2)} s0=${stEarlyGK.toFixed(2)}`);

landCloseGolpe();
V.player.stamRegenT = 99999;
const gotBoltG = waitUntilBoltWindow("golpe");
const recLeftGK = V.player.phase==="recovery" ? (V.GOLPE_RECOVERY - V.player.phaseT) : -1;
const golpePlant = V.destRect(V.player);
V.boltEdge = true;
V.update(STEP);
check("K in window after L hit starts dart", gotBoltG && recLeftGK<=V.BOLT_CANCEL_MS && V.player.boltPhase==="startup" && V.player.phase==="idle", `win=${gotBoltG} left=${recLeftGK.toFixed(0)} bph=${V.player.boltPhase} ph=${V.player.phase}`);
holdCutPlant("golpe-K", golpePlant);

toClose();
Math.random = () => 0;
V.rival.stamina = 80;
V.rival.stamRegenT = 99999;
V.rival.guardBreakT = 0;
V.rival.wantBlock = true;
V.rival.guarding = true;
V.rival.guardChoice = 1;
V.golpeEdge = true;
for(let i=0;i<50;i++){
  V.rival.wantBlock = true;
  V.rival.guarding = true;
  V.rival.guardChoice = 1;
  V.update(STEP);
  if(V.hitstopLeft>0 || (V.player.phase==="recovery" && V.player.cutHit && V.player.cut==="golpe")) break;
}
drainHitstop();
check("golpe block still arms cutHit for K", V.player.cutHit===true && V.player.cut==="golpe" && V.rival.hp===100, `hit=${V.player.cutHit} hp=${V.rival.hp} ph=${V.player.phase} cut=${V.player.cut}`);
V.player.stamRegenT = 99999;
const blkBoltG = waitUntilBoltWindow("golpe");
V.boltEdge = true;
V.update(STEP);
check("K in window after L block starts dart", blkBoltG && V.player.boltPhase==="startup" && V.player.phase==="idle", `win=${blkBoltG} bph=${V.player.boltPhase} ph=${V.player.phase} hit=${V.player.cutHit}`);

bootPlay(); wait(1700);
freezeYouAI();
freezeRivalAI();
V.player.x = 180;
V.rival.x = 1100;
V.golpeEdge = true;
for(let i=0;i<80;i++){
  freezeRivalAI();
  V.update(STEP);
  if(V.player.phase==="recovery" && V.player.cut==="golpe") break;
}
check("whiff golpe still has no cutHit for K", V.player.phase==="recovery" && V.player.cut==="golpe" && !V.player.cutHit && V.rival.hp===100, `ph=${V.player.phase} hit=${V.player.cutHit} hp=${V.rival.hp}`);
for(let i=0;i<40;i++){
  freezeRivalAI();
  V.update(STEP);
  if((V.GOLPE_RECOVERY - V.player.phaseT) <= V.BOLT_CANCEL_MS) break;
  if(V.player.phase==="idle") break;
}
const whiffLeftGK = V.player.phase==="recovery" ? (V.GOLPE_RECOVERY - V.player.phaseT) : -1;
const stWhiffGK = V.player.stamina;
V.player.stamRegenT = 99999;
V.boltEdge = true;
V.update(STEP);
check("K in window on L whiff does not cancel", V.player.phase==="recovery" && V.player.cut==="golpe" && V.boltBuf && !V.player.boltPhase && whiffLeftGK<=V.BOLT_CANCEL_MS && Math.abs(V.player.stamina-stWhiffGK)<0.2, `ph=${V.player.phase} cut=${V.player.cut} buf=${V.boltBuf} bph=${V.player.boltPhase} left=${whiffLeftGK.toFixed(0)}`);

function waitRivalBoltWindow(cut){
  const rec = cut==="golpe" ? V.GOLPE_RECOVERY : V.RECOVERY;
  for(let i=0;i<60;i++){
    holdYouDummy();
    const left = (V.rival.phase==="recovery" && V.rival.cut===cut) ? (rec - V.rival.phaseT) : 999;
    V.update(STEP);
    if(V.rival.boltPhase==="startup") return left;
    if(V.rival.phase==="idle" && !V.rival.boltPhase) return -1;
    if(cut==="slash" && V.rival.cut==="golpe") return -2;
    if(cut==="golpe" && V.rival.cut==="slash" && V.rival.phase==="startup") return -2;
  }
  return -1;
}
function armRivalSpecialCancel(cut){
  V.rival.linkBolt = true;
  V.rival.linkGolpe = false;
  V.rival.linkSlash = false;
  V.rival.boltCd = 0;
  V.rival.boltArmed = false;
  V.rival.boltPhase = "";
  V.rival.stamina = 100;
  V.rival.stamRegenT = 99999;
  V.rival.standGoal = 99999;
  if(cut) V.rival.cut = cut;
}

toClose();
freezeYouAI();
freezeRivalAI();
V.rival.linkBolt = true;
V.rival.cutHit = true;
V.rival.boltCd = 0;
V.rival.standGoal = 99999;
V.update(STEP);
check("rival does not special-cancel K from idle", V.rival.phase==="idle" && !V.rival.boltPhase && V.rival.cut==="slash", `cut=${V.rival.cut} ph=${V.rival.phase} bph=${V.rival.boltPhase} link=${V.rival.linkBolt}`);

toClose();
freezeYouAI();
freezeRivalAI();
V.rival.phase = "recovery";
V.rival.phaseT = V.RECOVERY - 40;
V.rival.cut = "slash";
V.rival.cutHit = true;
V.rival.stamina = 100;
V.rival.boltPhase = "";
V.rival.linkBolt = false;
V.rival.linkGolpe = false;
V.rival.boltCd = 0;
V.rival.standGoal = 99999;
V.update(STEP);
check("rival does not mash every connected tajo into K", V.rival.cut==="slash" && V.rival.phase==="recovery" && !V.rival.boltPhase, `cut=${V.rival.cut} ph=${V.rival.phase} bph=${V.rival.boltPhase} t=${V.rival.phaseT.toFixed(0)} link=${V.rival.linkBolt}`);

toClose();
freezeYouAI();
freezeRivalAI();
V.rival.phase = "recovery";
V.rival.phaseT = V.GOLPE_RECOVERY - 40;
V.rival.cut = "golpe";
V.rival.cutHit = true;
V.rival.stamina = 100;
V.rival.boltPhase = "";
V.rival.linkBolt = false;
V.rival.linkSlash = false;
V.rival.boltCd = 0;
V.rival.standGoal = 99999;
V.update(STEP);
check("rival does not mash every connected golpe into K", V.rival.cut==="golpe" && V.rival.phase==="recovery" && !V.rival.boltPhase, `cut=${V.rival.cut} ph=${V.rival.phase} bph=${V.rival.boltPhase} t=${V.rival.phaseT.toFixed(0)} link=${V.rival.linkBolt}`);

landRivalCloseSlash(false);
check("rival tajo hit still arms cutHit for K", V.rival.cutHit===true && V.rival.cut==="slash" && V.rival.phase==="recovery" && V.player.hp===90, `hit=${V.rival.cutHit} cut=${V.rival.cut} ph=${V.rival.phase} hp=${V.player.hp}`);
armRivalSpecialCancel("slash");
const stSlashK = V.rival.stamina;
const phpSlashK = V.player.hp;
const fireLeftRK = waitRivalBoltWindow("slash");
check("rival special-cancels connected slash into K", fireLeftRK>=0 && fireLeftRK<=V.BOLT_CANCEL_MS+STEP && V.rival.boltPhase==="startup" && V.rival.phase==="idle", `left=${fireLeftRK} bph=${V.rival.boltPhase} ph=${V.rival.phase} cut=${V.rival.cut} hit=${V.rival.cutHit}`);
check("rival slash-K cancel still costs 30", Math.abs(V.rival.stamina - (stSlashK-30))<0.2, `st=${V.rival.stamina.toFixed(2)} s0=${stSlashK.toFixed(2)}`);
check("player unchanged after rival slash-K", V.player.hp===phpSlashK && !V.player.boltPhase && V.player.cut!=="throw", `hp=${V.player.hp} bph=${V.player.boltPhase} cut=${V.player.cut}`);
check("rival slash-K still 200 startup", V.rival.boltPhase==="startup" && V.BOLT_STARTUP===200 && V.rival.boltT<STEP+1, `bph=${V.rival.boltPhase} t=${V.rival.boltT} start=${V.BOLT_STARTUP}`);

landRivalCloseSlash(true);
check("rival tajo block still arms cutHit for K", V.rival.cutHit===true && V.rival.cut==="slash" && V.player.hp===100, `hit=${V.rival.cutHit} hp=${V.player.hp} ph=${V.rival.phase}`);
armRivalSpecialCancel("slash");
const fireLeftRKB = waitRivalBoltWindow("slash");
check("rival special-cancels blocked slash into K", fireLeftRKB>=0 && fireLeftRKB<=V.BOLT_CANCEL_MS+STEP && V.rival.boltPhase==="startup" && V.rival.phase==="idle", `left=${fireLeftRKB} bph=${V.rival.boltPhase} ph=${V.rival.phase} hit=${V.rival.cutHit}`);

landRivalCloseGolpe(false);
check("rival golpe hit still arms cutHit for K", V.rival.cutHit===true && V.rival.cut==="golpe" && V.rival.phase==="recovery" && V.player.hp===90, `hit=${V.rival.cutHit} cut=${V.rival.cut} ph=${V.rival.phase} hp=${V.player.hp}`);
armRivalSpecialCancel("golpe");
const stGolpeK = V.rival.stamina;
const phpGolpeK = V.player.hp;
const fireLeftRGK = waitRivalBoltWindow("golpe");
check("rival special-cancels connected golpe into K", fireLeftRGK>=0 && fireLeftRGK<=V.BOLT_CANCEL_MS+STEP && V.rival.boltPhase==="startup" && V.rival.phase==="idle", `left=${fireLeftRGK} bph=${V.rival.boltPhase} ph=${V.rival.phase} cut=${V.rival.cut} hit=${V.rival.cutHit}`);
check("rival golpe-K cancel still costs 30", Math.abs(V.rival.stamina - (stGolpeK-30))<0.2, `st=${V.rival.stamina.toFixed(2)} s0=${stGolpeK.toFixed(2)}`);
check("player unchanged after rival golpe-K", V.player.hp===phpGolpeK && !V.player.boltPhase, `hp=${V.player.hp} bph=${V.player.boltPhase} cut=${V.player.cut}`);

landRivalCloseGolpe(true);
armRivalSpecialCancel("golpe");
const fireLeftRGKB = waitRivalBoltWindow("golpe");
check("rival special-cancels blocked golpe into K", fireLeftRGKB>=0 && fireLeftRGKB<=V.BOLT_CANCEL_MS+STEP && V.rival.boltPhase==="startup" && V.rival.phase==="idle", `left=${fireLeftRGKB} bph=${V.rival.boltPhase} ph=${V.rival.phase} hit=${V.rival.cutHit}`);

bootPlay(); wait(1700);
freezeYouAI();
freezeRivalAI();
V.player.x = 180;
V.rival.x = 1100;
V.startAttack(V.rival);
for(let i=0;i<80;i++){
  holdYouDummy();
  V.update(STEP);
  if(V.rival.phase==="recovery" && V.rival.cut==="slash") break;
}
check("rival whiff slash still has no cutHit for K", V.rival.phase==="recovery" && V.rival.cut==="slash" && !V.rival.cutHit && V.player.hp===100, `ph=${V.rival.phase} hit=${V.rival.cutHit} hp=${V.player.hp}`);
armRivalSpecialCancel("slash");
const stWhiffRK = V.rival.stamina;
for(let i=0;i<40;i++){
  holdYouDummy();
  V.update(STEP);
  if(V.rival.boltPhase) break;
  if((V.RECOVERY - V.rival.phaseT) <= V.BOLT_CANCEL_MS) break;
  if(V.rival.phase==="idle") break;
}
const whiffLeftRK = V.rival.phase==="recovery" ? (V.RECOVERY - V.rival.phaseT) : -1;
check("rival does not special-cancel slash into K on whiff", V.rival.phase==="recovery" && V.rival.cut==="slash" && !V.rival.cutHit && !V.rival.boltPhase && whiffLeftRK<=V.BOLT_CANCEL_MS && Math.abs(V.rival.stamina-stWhiffRK)<0.2, `ph=${V.rival.phase} cut=${V.rival.cut} hit=${V.rival.cutHit} bph=${V.rival.boltPhase} left=${whiffLeftRK.toFixed(0)} st=${V.rival.stamina.toFixed(2)}`);

bootPlay(); wait(1700);
freezeYouAI();
freezeRivalAI();
V.player.x = 180;
V.rival.x = 1100;
V.startAttack(V.rival, "golpe");
for(let i=0;i<80;i++){
  holdYouDummy();
  V.update(STEP);
  if(V.rival.phase==="recovery" && V.rival.cut==="golpe") break;
}
check("rival whiff golpe still has no cutHit for K", V.rival.phase==="recovery" && V.rival.cut==="golpe" && !V.rival.cutHit && V.player.hp===100, `ph=${V.rival.phase} hit=${V.rival.cutHit} hp=${V.player.hp}`);
armRivalSpecialCancel("golpe");
const stWhiffRGK = V.rival.stamina;
for(let i=0;i<40;i++){
  holdYouDummy();
  V.update(STEP);
  if(V.rival.boltPhase) break;
  if((V.GOLPE_RECOVERY - V.rival.phaseT) <= V.BOLT_CANCEL_MS) break;
  if(V.rival.phase==="idle") break;
}
const whiffLeftRGK = V.rival.phase==="recovery" ? (V.GOLPE_RECOVERY - V.rival.phaseT) : -1;
check("rival does not special-cancel golpe into K on whiff", V.rival.phase==="recovery" && V.rival.cut==="golpe" && !V.rival.cutHit && !V.rival.boltPhase && whiffLeftRGK<=V.BOLT_CANCEL_MS && Math.abs(V.rival.stamina-stWhiffRGK)<0.2, `ph=${V.rival.phase} cut=${V.rival.cut} hit=${V.rival.cutHit} bph=${V.rival.boltPhase} left=${whiffLeftRGK.toFixed(0)} st=${V.rival.stamina.toFixed(2)}`);

toClose();
freezeYouAI();
freezeRivalAI();
V.rival.phase = "recovery";
V.rival.phaseT = V.RECOVERY - 40;
V.rival.cut = "slash";
V.rival.cutHit = true;
V.rival.linkBolt = true;
V.rival.linkGolpe = false;
V.rival.boltCd = 99999;
V.rival.stamina = 100;
V.rival.boltPhase = "";
V.rival.standGoal = 99999;
V.update(STEP);
check("rival special-cancel respects BOLT_AI_CD", V.rival.cut==="slash" && V.rival.phase==="recovery" && !V.rival.boltPhase && V.rival.boltCd>0, `cut=${V.rival.cut} ph=${V.rival.phase} bph=${V.rival.boltPhase} cd=${V.rival.boltCd}`);

check("rival special-cancel is a chance", /armRivalBoltLink/.test(codeOnly) && /rivalLinkRoll/.test(sliceFn(codeOnly, "armRivalBoltLink")));
check("rival special-cancel respects bolt CD", /boltCd > 0/.test(sliceFn(codeOnly, "rivalTryCutBolt")) && /boltCd = BOLT_AI_CD/.test(sliceFn(codeOnly, "rivalTryCutBolt")));
check("player special-cancel door still shared", /!f.cutHit/.test(sliceFn(codeOnly, "cutToBoltWindow")) && /cancelIntoBolt\(player\)/.test(codeOnly));
check("K frames still 200/280 after rival special cancel", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280);
check("K still costs 30 after rival special cancel", V.BOLT_STAM===30);
check("combo 40 still tajo-golpe", /rivalLinkRoll/.test(sliceFn(codeOnly, "armRivalSlashLink")) && /rivalLinkRoll/.test(sliceFn(codeOnly, "armRivalGolpeLink")) && V.AI_LINK_CHANCE===0.4);

bootPlay(); wait(1700);
V.keys.add("KeyS");
V.update(STEP);
const stGuardK = V.player.stamina;
V.boltEdge = true;
V.update(STEP);
check("K still not a cancel from guard", V.player.guarding && !V.player.boltPhase && !V.bolt && V.player.phase==="idle", `g=${V.player.guarding} bph=${V.player.boltPhase} ph=${V.player.phase}`);
V.keys.delete("KeyS");

bootPlay(); wait(1700);
freezeRivalAI();
V.keys.add("KeyD");
V.update(STEP);
const walking = V.player.gait!==0 && V.player.phase==="idle";
const stWalkK = V.player.stamina;
V.boltEdge = true;
V.update(STEP);
check("K from walk is idle plant not a recovery cancel", walking && V.player.boltPhase==="startup" && V.player.phase==="idle" && Math.abs(V.player.stamina-(stWalkK-30))<0.2, `walk=${walking} gait=${V.player.gait} bph=${V.player.boltPhase} ph=${V.player.phase} st=${V.player.stamina.toFixed(2)}`);
check("walk K plant is throw_knife not cut", /throw_knife/.test(sheetSrc(V.player)) && !isCutSheet(V.player), `src=${sheetSrc(V.player)}`);
V.keys.delete("KeyD");
for(let i=0;i<20;i++){ freezeRivalAI(); V.update(STEP); if(V.player.boltPhase==="recovery") break; }
check("walk K recovery is not a cut hold", V.player.boltPhase==="recovery" && !isCutSheet(V.player), `bph=${V.player.boltPhase} src=${sheetSrc(V.player)}`);

bootPlay(); wait(1700);
freezeRivalAI();
V.boltEdge = true;
V.update(STEP);
check("idle K plant is throw_knife not cut", V.player.boltPhase==="startup" && /throw_knife/.test(sheetSrc(V.player)) && !isCutSheet(V.player), `bph=${V.player.boltPhase} src=${sheetSrc(V.player)}`);
for(let i=0;i<20;i++){ freezeRivalAI(); V.update(STEP); if(V.player.boltPhase==="recovery") break; }
check("idle K recovery is not a cut hold", V.player.boltPhase==="recovery" && !isCutSheet(V.player), `bph=${V.player.boltPhase} src=${sheetSrc(V.player)}`);

landCloseSlash();
const gotLink = waitUntilWindow();
V.golpeEdge = true;
V.update(STEP);
check("tajo-golpe link still lives after K cancel", gotLink && V.player.cut==="golpe" && V.player.phase==="startup" && !V.player.boltPhase, `win=${gotLink} cut=${V.player.cut} ph=${V.player.phase} bph=${V.player.boltPhase}`);

check("clash recovery still 200", V.CLASH_RECOVERY===200, `ms=${V.CLASH_RECOVERY}`);
check("clash spark still 110", V.CLASH_SPARK_MS===110);
const clashFn = sliceFn(codeOnly, "doClash");
check("clash does not arm cutHit", /clashRec = true/.test(clashFn) && /cutHit = false/.test(clashFn) && !/cutHit = true/.test(clashFn));
check("clash-K door shared with rival", !/kind !== "you"/.test(sliceFn(codeOnly, "cutToBoltWindow")) && /clashRec/.test(sliceFn(codeOnly, "cutToBoltWindow")));
check("rival clash-K is a chance", /armRivalClashBolt/.test(codeOnly) && /Math.random\(\) < AI_LINK_CHANCE/.test(sliceFn(codeOnly, "armRivalClashBolt")));
check("clash arms rival K chance", /armRivalClashBolt\(\)/.test(sliceFn(codeOnly, "doClash")));
check("clash does not arm combo connect links", !/armRivalSlashLink/.test(clashFn) && !/armRivalGolpeLink/.test(clashFn));
check("clash arms rival Space/L chance", /armRivalClashCut\(\)/.test(clashFn));
check("rival clash-Space/L is a chance", /armRivalClashCut/.test(codeOnly) && /Math.random\(\) < AI_LINK_CHANCE/.test(sliceFn(codeOnly, "armRivalClashCut")));
check("Space/L windows accept clashRec", /clashRec/.test(sliceFn(codeOnly, "slashToGolpeWindow")) && /clashRec/.test(sliceFn(codeOnly, "golpeToSlashWindow")));
check("clash-Space/L door shared with rival", !/kind !== "you"/.test(sliceFn(codeOnly, "slashToGolpeWindow")) && !/kind !== "you"/.test(sliceFn(codeOnly, "golpeToSlashWindow")) && /clashRec/.test(sliceFn(codeOnly, "slashToGolpeWindow")) && /clashRec/.test(sliceFn(codeOnly, "golpeToSlashWindow")));

function landClash(){
  toClose();
  freezeYouAI();
  freezeRivalAI();
  V.player.stamina = 100;
  V.player.stamRegenT = 99999;
  V.rival.stamina = 100;
  V.rival.stamRegenT = 99999;
  V.player.phase = "active";
  V.player.phaseT = 40;
  V.player.cut = "slash";
  V.player.cutHit = false;
  V.rival.phase = "active";
  V.rival.phaseT = 40;
  V.rival.cut = "slash";
  V.rival.cutHit = false;
  V.doClash(640, 360);
  drainHitstop();
}

landClash();
const clashLeft0 = V.player.phase==="recovery" ? (V.RECOVERY - V.player.phaseT) : -1;
check("clash leaves 200ms recovery no cutHit", V.player.phase==="recovery" && V.rival.phase==="recovery" && V.player.clashRec && !V.player.cutHit && V.rival.hp===100 && V.player.hp===100 && clashLeft0>=180 && clashLeft0<=200, `ph=${V.player.phase} hit=${V.player.cutHit} rec=${V.player.clashRec} left=${clashLeft0.toFixed(0)} hp=${V.player.hp}/${V.rival.hp}`);
const recEarlyCK = V.player.phaseT;
const stEarlyCK = V.player.stamina;
V.player.stamRegenT = 99999;
V.boltEdge = true;
V.update(STEP);
check("K early clash recovery still buffers", V.player.phase==="recovery" && V.player.cut==="slash" && V.boltBuf && recEarlyCK < (V.RECOVERY - V.BOLT_CANCEL_MS) && !V.player.boltPhase && Math.abs(V.player.stamina-stEarlyCK)<0.2, `ph=${V.player.phase} cut=${V.player.cut} buf=${V.boltBuf} t=${recEarlyCK.toFixed(0)} bph=${V.player.boltPhase} st=${V.player.stamina.toFixed(2)}`);
let fireLeftCK=-1;
let stFireCK=stEarlyCK;
for(let i=0;i<60;i++){
  holdRival();
  V.player.stamRegenT = 99999;
  const left = (V.player.phase==="recovery" && V.player.cut==="slash") ? (V.RECOVERY - V.player.phaseT) : 999;
  V.update(STEP);
  if(V.player.boltPhase==="startup"){ fireLeftCK=left; stFireCK=V.player.stamina; break; }
  if(V.player.phase==="idle" && !V.player.boltPhase) break;
}
check("buffered K cancels in last 100ms of clash", fireLeftCK>=0 && fireLeftCK<=V.BOLT_CANCEL_MS+STEP && V.player.boltPhase==="startup" && V.player.phase==="idle" && !V.boltBuf, `left=${fireLeftCK} bph=${V.player.boltPhase} ph=${V.player.phase} buf=${V.boltBuf}`);
check("clash-K cancel still costs 30", Math.abs(stFireCK - (stEarlyCK-30))<0.2, `st=${stFireCK.toFixed(2)} s0=${stEarlyCK.toFixed(2)}`);
check("clash-K is not a free fireball", V.player.boltPhase==="startup" && V.player.boltT < STEP+1 && !V.bolt && V.BOLT_STARTUP===200, `bph=${V.player.boltPhase} t=${V.player.boltT} bolt=${!!V.bolt}`);

landClash();
V.player.stamRegenT = 99999;
const stWinCK = V.player.stamina;
const gotClashBolt = waitUntilBoltWindow("slash");
// Clash bounce still rides KNOCK_MS after freeze. Wait it out inside
// the late door so holdCutPlant does not treat leftover slide as a hop.
for(let i=0;i<20;i++){
  holdRival();
  V.player.stamRegenT = 99999;
  if(V.player.pushT<=0) break;
  if(V.player.phase!=="recovery") break;
  V.update(STEP);
}
const recLeftCK = V.player.phase==="recovery" ? (V.RECOVERY - V.player.phaseT) : -1;
const clashPlant = V.destRect(V.player);
V.boltEdge = true;
V.update(STEP);
check("K in window after clash starts dart", gotClashBolt && recLeftCK<=V.BOLT_CANCEL_MS && V.player.boltPhase==="startup" && V.player.phase==="idle" && V.player.clashRec===false, `win=${gotClashBolt} left=${recLeftCK.toFixed(0)} bph=${V.player.boltPhase} ph=${V.player.phase} rec=${V.player.clashRec}`);
check("K after clash still costs 30", Math.abs(V.player.stamina - (stWinCK-30))<0.2, `st=${V.player.stamina.toFixed(2)} s0=${stWinCK.toFixed(2)}`);
check("clash-K destRect leftover plant stays 0", destNear(V.destRect(V.player), clashPlant, 1.6), `dx=${(V.destRect(V.player).dx-clashPlant.dx).toFixed(2)} dy=${(V.destRect(V.player).dy-clashPlant.dy).toFixed(2)}`);
check("clash-K poseBitmap throw_knife not leftover cut", V.player.boltPhase==="startup" && /throw_knife/.test(sheetSrc(V.player)) && !isCutSheet(V.player) && V.player.boltHoldCut===false && V.player.clashPlant && V.clashPlantFade(V.player)>0.8, `bph=${V.player.boltPhase} src=${sheetSrc(V.player)} hold=${V.player.boltHoldCut} plant=${V.player.clashPlant} fade=${V.clashPlantFade(V.player)}`);

landClash();
const recEarlyCL = V.player.phaseT;
V.golpeEdge = true;
V.update(STEP);
check("L early clash recovery still buffers", V.player.phase==="recovery" && V.player.cut==="slash" && V.golpeBuf && recEarlyCL < (V.RECOVERY - V.SLASH_CANCEL_MS) && V.player.clashRec, `ph=${V.player.phase} cut=${V.player.cut} buf=${V.golpeBuf} t=${recEarlyCL.toFixed(0)} rec=${V.player.clashRec}`);
let fireLeftCL=-1;
for(let i=0;i<60;i++){
  holdRival();
  const left = (V.player.phase==="recovery" && V.player.cut==="slash") ? (V.RECOVERY - V.player.phaseT) : 999;
  V.update(STEP);
  if(V.player.cut==="golpe" && V.player.phase==="startup"){ fireLeftCL=left; break; }
  if(V.player.phase==="idle") break;
}
check("buffered L cancels in last 100ms of clash", fireLeftCL>=0 && fireLeftCL<=V.SLASH_CANCEL_MS+STEP && V.player.cut==="golpe" && V.player.phase==="startup" && !V.golpeBuf && V.player.clashRec===false, `left=${fireLeftCL} cut=${V.player.cut} ph=${V.player.phase} buf=${V.golpeBuf}`);

landClash();
const recEarlyCS = V.player.phaseT;
V.attackEdge = true;
V.update(STEP);
check("Space early clash recovery still buffers", V.player.phase==="recovery" && V.player.cut==="slash" && V.slashBuf && recEarlyCS < (V.RECOVERY - V.GOLPE_CANCEL_MS) && V.player.clashRec, `ph=${V.player.phase} cut=${V.player.cut} buf=${V.slashBuf} t=${recEarlyCS.toFixed(0)} rec=${V.player.clashRec}`);
let fireLeftCS=-1;
for(let i=0;i<60;i++){
  holdRival();
  const left = (V.player.phase==="recovery" && V.player.cut==="slash") ? (V.RECOVERY - V.player.phaseT) : 999;
  V.update(STEP);
  if(V.player.cut==="slash" && V.player.phase==="startup"){ fireLeftCS=left; break; }
  if(V.player.phase==="idle" && !V.slashBuf) break;
}
check("buffered Space cancels in last 100ms of clash", fireLeftCS>=0 && fireLeftCS<=V.GOLPE_CANCEL_MS+STEP && V.player.cut==="slash" && V.player.phase==="startup" && !V.slashBuf && V.player.clashRec===false, `left=${fireLeftCS} cut=${V.player.cut} ph=${V.player.phase} buf=${V.slashBuf}`);

landClash();
const gotClashWin = waitUntilBoltWindow("slash");
const recLeftSpace = V.player.phase==="recovery" ? (V.RECOVERY - V.player.phaseT) : -1;
V.golpeEdge = true;
V.update(STEP);
check("L in clash window starts golpe", gotClashWin && recLeftSpace<=V.SLASH_CANCEL_MS && V.player.cut==="golpe" && V.player.phase==="startup" && !V.golpeBuf && V.player.clashRec===false, `win=${gotClashWin} left=${recLeftSpace.toFixed(0)} cut=${V.player.cut} ph=${V.player.phase} buf=${V.golpeBuf} rec=${V.player.clashRec}`);

landClash();
const gotClashWinS = waitUntilBoltWindow("slash");
const recLeftSlash = V.player.phase==="recovery" ? (V.RECOVERY - V.player.phaseT) : -1;
V.attackEdge = true;
V.update(STEP);
check("Space in clash window starts tajo", gotClashWinS && recLeftSlash<=V.GOLPE_CANCEL_MS && V.player.cut==="slash" && V.player.phase==="startup" && !V.slashBuf && V.player.clashRec===false, `win=${gotClashWinS} left=${recLeftSlash.toFixed(0)} cut=${V.player.cut} ph=${V.player.phase} buf=${V.slashBuf} rec=${V.player.clashRec}`);

landClash();
V.player.stamina = 29;
V.player.stamRegenT = 99999;
const gotPoorCK = waitUntilBoltWindow("slash");
V.boltEdge = true;
V.update(STEP);
check("K in clash window stam<30 does not cancel", gotPoorCK && V.player.phase==="recovery" && V.player.cut==="slash" && !V.player.boltPhase && V.player.stamina===29 && V.player.clashRec, `win=${gotPoorCK} ph=${V.player.phase} bph=${V.player.boltPhase} st=${V.player.stamina}`);

landClash();
V.rival.stamina = 100;
V.rival.stamRegenT = 99999;
V.rival.boltPhase = "";
V.rival.boltCd = 0;
V.rival.boltArmed = false;
V.rival.linkBolt = false;
V.rival.linkGolpe = false;
V.rival.linkSlash = false;
V.rival.standGoal = 99999;
let rivalClashMissWin=false;
for(let i=0;i<60;i++){
  holdYouDummy();
  V.rival.boltCd = 0;
  V.rival.linkBolt = false;
  V.rival.standGoal = 99999;
  V.rival.closing = false;
  const left = V.rival.phase==="recovery" ? (V.RECOVERY - V.rival.phaseT) : -1;
  V.update(STEP);
  if(V.rival.boltPhase) break;
  if(V.rival.phase!=="recovery") break;
  if(left>=0 && left<=V.BOLT_CANCEL_MS){ rivalClashMissWin=true; break; }
}
check("rival does not mash every clash into K", rivalClashMissWin && V.rival.phase==="recovery" && V.rival.clashRec && !V.rival.boltPhase, `win=${rivalClashMissWin} ph=${V.rival.phase} rec=${V.rival.clashRec} bph=${V.rival.boltPhase} t=${V.rival.phaseT.toFixed(0)}`);

landClash();
V.rival.stamina = 100;
V.rival.stamRegenT = 99999;
armRivalSpecialCancel("slash");
V.rival.clashRec = true;
const recEarlyRCK = V.rival.phaseT;
const stEarlyRCK = V.rival.stamina;
holdYouDummy();
V.update(STEP);
check("rival K early clash recovery still locked", V.rival.phase==="recovery" && V.rival.clashRec && recEarlyRCK < (V.RECOVERY - V.BOLT_CANCEL_MS) && !V.rival.boltPhase && Math.abs(V.rival.stamina-stEarlyRCK)<0.2, `ph=${V.rival.phase} rec=${V.rival.clashRec} t=${recEarlyRCK.toFixed(0)} bph=${V.rival.boltPhase} st=${V.rival.stamina.toFixed(2)}`);
let fireLeftRCK=-1;
let stFireRCK=stEarlyRCK;
for(let i=0;i<60;i++){
  holdYouDummy();
  armRivalSpecialCancel("slash");
  V.rival.clashRec = true;
  V.rival.stamRegenT = 99999;
  const left = (V.rival.phase==="recovery" && V.rival.cut==="slash") ? (V.RECOVERY - V.rival.phaseT) : 999;
  V.update(STEP);
  if(V.rival.boltPhase==="startup"){ fireLeftRCK=left; stFireRCK=V.rival.stamina; break; }
  if(V.rival.phase==="idle" && !V.rival.boltPhase) break;
}
check("rival K cancels in last 100ms of clash", fireLeftRCK>=0 && fireLeftRCK<=V.BOLT_CANCEL_MS+STEP && V.rival.boltPhase==="startup" && V.rival.phase==="idle", `left=${fireLeftRCK} bph=${V.rival.boltPhase} ph=${V.rival.phase} rec=${V.rival.clashRec}`);
check("rival clash-K cancel still costs 30", Math.abs(stFireRCK - (stEarlyRCK-30))<0.2, `st=${stFireRCK.toFixed(2)} s0=${stEarlyRCK.toFixed(2)}`);
check("rival clash-K is not a free fireball", V.rival.boltPhase==="startup" && V.rival.boltT < STEP+1 && !V.bolt && V.BOLT_STARTUP===200, `bph=${V.rival.boltPhase} t=${V.rival.boltT} bolt=${!!V.bolt}`);
check("rival clash-K still 200 startup", V.rival.boltPhase==="startup" && V.BOLT_STARTUP===200 && V.rival.boltT<STEP+1, `bph=${V.rival.boltPhase} t=${V.rival.boltT} start=${V.BOLT_STARTUP}`);

landClash();
armRivalSpecialCancel("slash");
V.rival.clashRec = true;
V.rival.boltCd = 99999;
let rivalClashCdWin=false;
let rivalClashCdBolt=false;
for(let i=0;i<60;i++){
  holdYouDummy();
  V.rival.linkBolt = true;
  V.rival.linkGolpe = false;
  V.rival.linkSlash = false;
  V.rival.boltCd = 99999;
  V.rival.standGoal = 99999;
  const left = V.rival.phase==="recovery" ? (V.RECOVERY - V.rival.phaseT) : -1;
  V.update(STEP);
  if(V.rival.boltPhase){ rivalClashCdBolt=true; break; }
  if(V.rival.phase!=="recovery") break;
  if(left>=0 && left<=V.BOLT_CANCEL_MS){ rivalClashCdWin=true; break; }
}
check("rival clash-K respects BOLT_AI_CD", rivalClashCdWin && !rivalClashCdBolt && V.rival.phase==="recovery" && V.rival.clashRec && !V.rival.boltPhase && V.rival.boltCd>0, `win=${rivalClashCdWin} bolt=${rivalClashCdBolt} ph=${V.rival.phase} rec=${V.rival.clashRec} bph=${V.rival.boltPhase} cd=${V.rival.boltCd}`);

check("player clash doors unchanged", V.BOLT_CANCEL_MS===100 && V.SLASH_CANCEL_MS===100 && V.GOLPE_CANCEL_MS===100 && V.CLASH_RECOVERY===200 && V.BOLT_STAM===30 && V.BOLT_STARTUP===200 && /clashRec/.test(sliceFn(codeOnly, "slashToGolpeWindow")) && /clashRec/.test(sliceFn(codeOnly, "golpeToSlashWindow")) && /clashRec/.test(sliceFn(codeOnly, "cutToBoltWindow")) && /cancelIntoSlash\(player\)/.test(codeOnly) && /cancelIntoGolpe\(player\)/.test(codeOnly) && /cancelIntoBolt\(player\)/.test(codeOnly));
check("clash-K still not stolen by Space/L", /clashRec && rival.linkBolt/.test(sliceFn(codeOnly, "rivalTrySlashGolpe")) && /clashRec && rival.linkBolt/.test(sliceFn(codeOnly, "rivalTryGolpeSlash")));

function armRivalClashCutCancel(kind){
  V.rival.linkBolt = false;
  V.rival.linkGolpe = kind==="golpe";
  V.rival.linkSlash = kind==="slash";
  V.rival.boltCd = 99999;
  V.rival.boltArmed = false;
  V.rival.boltPhase = "";
  V.rival.stamina = 100;
  V.rival.stamRegenT = 99999;
  V.rival.standGoal = 99999;
  V.rival.clashRec = true;
}

landClash();
V.rival.linkBolt = false;
V.rival.linkGolpe = false;
V.rival.linkSlash = false;
let rivalClashCut=false;
let rivalClashCutWin=false;
const rivalCut0 = V.rival.cut;
for(let i=0;i<60;i++){
  holdYouDummy();
  V.rival.linkBolt = false;
  V.rival.linkGolpe = false;
  V.rival.linkSlash = false;
  V.rival.standGoal = 99999;
  const left = V.rival.phase==="recovery" ? (V.RECOVERY - V.rival.phaseT) : -1;
  V.update(STEP);
  if(V.rival.phase==="startup"){ rivalClashCut=true; break; }
  if(V.rival.phase!=="recovery") break;
  if(left>=0 && left<=V.SLASH_CANCEL_MS){ rivalClashCutWin=true; break; }
}
check("rival does not mash every clash into Space/L", rivalClashCutWin && !rivalClashCut && V.rival.phase==="recovery" && V.rival.clashRec && V.rival.cut===rivalCut0 && !V.rival.boltPhase, `win=${rivalClashCutWin} cutStart=${rivalClashCut} ph=${V.rival.phase} rec=${V.rival.clashRec} cut=${V.rival.cut} t=${V.rival.phaseT.toFixed(0)}`);

landClash();
armRivalClashCutCancel("golpe");
const recEarlyRCL = V.rival.phaseT;
holdYouDummy();
V.update(STEP);
check("rival L early clash recovery still locked", V.rival.phase==="recovery" && V.rival.clashRec && recEarlyRCL < (V.RECOVERY - V.SLASH_CANCEL_MS) && V.rival.cut==="slash" && V.rival.phase!=="startup", `ph=${V.rival.phase} rec=${V.rival.clashRec} cut=${V.rival.cut} t=${recEarlyRCL.toFixed(0)}`);
let fireLeftRCL=-1;
for(let i=0;i<60;i++){
  holdYouDummy();
  armRivalClashCutCancel("golpe");
  const left = (V.rival.phase==="recovery" && V.rival.cut==="slash") ? (V.RECOVERY - V.rival.phaseT) : 999;
  V.update(STEP);
  if(V.rival.cut==="golpe" && V.rival.phase==="startup"){ fireLeftRCL=left; break; }
  if(V.rival.phase==="idle") break;
}
check("rival L cancels in last 100ms of clash", fireLeftRCL>=0 && fireLeftRCL<=V.SLASH_CANCEL_MS+STEP && V.rival.cut==="golpe" && V.rival.phase==="startup" && V.rival.clashRec===false, `left=${fireLeftRCL} cut=${V.rival.cut} ph=${V.rival.phase} rec=${V.rival.clashRec}`);

landClash();
armRivalClashCutCancel("slash");
const recEarlyRCS = V.rival.phaseT;
holdYouDummy();
V.update(STEP);
check("rival Space early clash recovery still locked", V.rival.phase==="recovery" && V.rival.clashRec && recEarlyRCS < (V.RECOVERY - V.GOLPE_CANCEL_MS) && !(V.rival.cut==="slash" && V.rival.phase==="startup"), `ph=${V.rival.phase} rec=${V.rival.clashRec} cut=${V.rival.cut} t=${recEarlyRCS.toFixed(0)}`);
let fireLeftRCS=-1;
for(let i=0;i<60;i++){
  holdYouDummy();
  armRivalClashCutCancel("slash");
  const left = (V.rival.phase==="recovery") ? (V.RECOVERY - V.rival.phaseT) : 999;
  V.update(STEP);
  if(V.rival.cut==="slash" && V.rival.phase==="startup"){ fireLeftRCS=left; break; }
  if(V.rival.phase==="idle") break;
}
check("rival Space cancels in last 100ms of clash", fireLeftRCS>=0 && fireLeftRCS<=V.GOLPE_CANCEL_MS+STEP && V.rival.cut==="slash" && V.rival.phase==="startup" && V.rival.clashRec===false, `left=${fireLeftRCS} cut=${V.rival.cut} ph=${V.rival.phase} rec=${V.rival.clashRec}`);

landClash();
armRivalSpecialCancel("slash");
V.rival.clashRec = true;
V.rival.linkGolpe = true;
V.rival.linkSlash = true;
let stealK=false;
let stealCut=false;
let stealLeft=-1;
for(let i=0;i<60;i++){
  holdYouDummy();
  V.rival.linkBolt = true;
  V.rival.linkGolpe = true;
  V.rival.linkSlash = true;
  V.rival.boltCd = 0;
  V.rival.clashRec = true;
  V.rival.stamRegenT = 99999;
  const left = (V.rival.phase==="recovery" && V.rival.cut==="slash") ? (V.RECOVERY - V.rival.phaseT) : 999;
  V.update(STEP);
  if(V.rival.boltPhase==="startup"){ stealK=true; stealLeft=left; break; }
  if(V.rival.phase==="startup" && (V.rival.cut==="golpe" || V.rival.cut==="slash") && !V.rival.boltPhase){ stealCut=true; break; }
  if(V.rival.phase==="idle" && !V.rival.boltPhase) break;
}
check("rival clash-K still wins over Space/L", stealK && !stealCut && stealLeft>=0 && stealLeft<=V.BOLT_CANCEL_MS+STEP && V.rival.boltPhase==="startup", `k=${stealK} cut=${stealCut} left=${stealLeft} bph=${V.rival.boltPhase} ph=${V.rival.phase} cutv=${V.rival.cut}`);

check("throw frames 80/40/220", V.THROW_STARTUP===80 && V.THROW_ACTIVE===40 && V.THROW_RECOVERY===220, `f=${V.THROW_STARTUP}/${V.THROW_ACTIVE}/${V.THROW_RECOVERY}`);
check("throw dmg 20 cost 0 range 120", V.THROW_DMG===20 && V.THROW_STAM===0 && V.THROW_RANGE===120 && V.THROW_KD_MS===520, `d=${V.THROW_DMG} c=${V.THROW_STAM} r=${V.THROW_RANGE}`);
check("throw does not change slash/golpe/K/clash", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.SLASH_CANCEL_MS===100 && V.GOLPE_CANCEL_MS===100 && V.BOLT_CANCEL_MS===100 && V.BOLT_STAM===30 && V.CLASH_RECOVERY===200);
check("throw is Space+S chord not a 6th button", /actionHeld\("guard"\)/.test(srcTxt) && /actionHeld\("slash"\)/.test(srcTxt) && /throwEdge = true/.test(srcTxt) && /act === "slash"/.test(srcTxt) && (htmlTxt.match(/data-tap=/g)||[]).length===3);
check("pad has no throw face button", !/data-tap="throw"|zone-throw|llave/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt) && /data-tap="Space"/.test(htmlTxt));
check("pad chord is hold S tap Space", /function padChordHeld/.test(srcTxt) && /throwInRange\(\)/.test(sliceFn(codeOnly, "padDown")) && /throwEdge = false/.test(sliceFn(codeOnly, "padDown")));
check("still three tap face buttons", (htmlTxt.match(/data-tap=/g)||[]).length===3);
check("rival startThrow allowed", !/kind !== "you"/.test(sliceFn(codeOnly, "startThrow")) && /startThrow\(rival\)/.test(sliceFn(codeOnly, "rivalTryThrow")));
check("rival throw AI CD 1800", V.THROW_AI_CD===1800 && V.THROW_AI_CD===V.BOLT_AI_CD);
check("rival throw only vs hold-guard in range", /player\.guarding/.test(sliceFn(codeOnly, "rivalTryThrow")) && /THROW_RANGE/.test(sliceFn(codeOnly, "rivalTryThrow")) && /throwCd/.test(sliceFn(codeOnly, "rivalTryThrow")));

function fireThrow(){
  V.keys.add("KeyS");
  V.keys.add("Space");
  V.throwEdge = true;
}

function holdRivalStill(){
  Math.random = () => 0.99;
  V.rival.boltCd = 99999;
  V.rival.boltPhase = "";
  V.rival.wantBlock = false;
  V.rival.guardChoice = -1;
  V.rival.closing = false;
  V.rival.standWait = 0;
  V.rival.standGoal = 99999;
  V.rival.gait = 0;
  V.rival.throwTechArmed = -1;
  V.rival.revCd = 99999;
  V.rival.revArmed = -1;
  V.rival.wakeRevArmed = -1;
  V.rival.pbCd = 99999;
  V.rival.pbArmed = -1;
  V.rival.feintCd = 99999;
  V.rival.feintArmed = -1;
}

// throw vs hold-guard
toClose();
freezeYouAI();
freezeRivalAI();
Math.random = () => 0;
V.rival.stamina = 80;
V.rival.stamRegenT = 99999;
V.rival.guardBreakT = 0;
V.rival.wantBlock = true;
V.rival.guarding = true;
V.rival.guardChoice = 1;
V.player.stamina = 100;
V.player.stamRegenT = 99999;
const gThrow = V.absGap();
fireThrow();
let throwHit=false;
for(let i=0;i<80;i++){
  V.rival.wantBlock = true;
  V.rival.guarding = true;
  V.rival.guardChoice = 1;
  V.rival.standGoal = 99999;
  V.rival.closing = false;
  V.update(STEP);
  if(V.hitstopLeft>0 || V.rival.hp<100 || V.rival.thrownT>0){ throwHit=true; break; }
}
drainHitstop();
check("throw vs guard connects", throwHit && V.rival.hp===80 && V.player.hp===100 && V.player.cut==="throw" && V.player.cutHit && V.rival.thrownT>0, `hit=${throwHit} rhp=${V.rival.hp} php=${V.player.hp} cut=${V.player.cut} ch=${V.player.cutHit} th=${V.rival.thrownT} g0=${gThrow.toFixed(1)} g=${V.absGap().toFixed(1)} ph=${V.player.phase}`);
check("throw vs guard is not a block", V.rival.hp===80 && V.rival.pushT===0 && V.rival.stamina>70 && V.rival.stamina<=80, `hp=${V.rival.hp} push=${V.rival.pushT} st=${V.rival.stamina.toFixed(1)}`);
check("throw knockdown reuses hurt art", V.rival.thrownT>0 && /hurt/.test(V.poseSheet(V.rival) && "hurt" || "") || (V.rival.thrownT>0 && V.poseSheet(V.rival)===V.poseSheet(Object.assign({}, V.rival, {falling:true, hp:0, thrownT:0}))), `th=${V.rival.thrownT}`);
const thrownSheet = V.poseSheet(V.rival);
const koSheet = V.poseSheet({kind:"rival", facing:-1, falling:true, hp:0, thrownT:0, phase:"idle", stunT:0, guarding:false, boltPhase:"", boltHoldCut:false, sheatheT:0, gait:0, walkT:0, img:null});
check("throw pose is hurt sheet", thrownSheet===koSheet, `throw=${JSON.stringify(thrownSheet && {w:thrownSheet.w,footX:thrownSheet.footX})} ko=${JSON.stringify(koSheet && {w:koSheet.w,footX:koSheet.footX})}`);
check("throw snap together", V.absGap()<=V.THROW_RANGE, `g=${V.absGap().toFixed(1)}`);
check("throw not a KO from 100", V.rival.hp===80 && V.mode==="play" && !V.rival.falling, `hp=${V.rival.hp} mode=${V.mode} fall=${V.rival.falling}`);

// throw whiff far
bootPlay(); wait(1700);
freezeYouAI();
freezeRivalAI();
V.player.x = 180;
V.rival.x = 1100;
holdRivalStill();
const far0 = V.absGap();
fireThrow();
let sawThrowActive=false, farDmg=false;
for(let i=0;i<80;i++){
  holdRivalStill();
  V.rival.x = 1100;
  V.update(STEP);
  if(V.player.cut==="throw" && V.player.phase==="active") sawThrowActive=true;
  if(V.rival.hp<100 || V.rival.thrownT>0) farDmg=true;
  if(V.player.cut==="throw" && V.player.phase==="recovery") break;
  if(V.player.phase==="idle" && i>10 && V.player.cut!=="throw") break;
}
check("throw whiff far", far0>400 && sawThrowActive && !farDmg && V.rival.hp===100 && V.rival.thrownT===0 && V.player.cut==="throw" && !V.player.cutHit, `g0=${far0.toFixed(1)} act=${sawThrowActive} dmg=${farDmg} hp=${V.rival.hp} th=${V.rival.thrownT} cut=${V.player.cut} hit=${V.player.cutHit} ph=${V.player.phase}`);

// throw lose to active slash
toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.rival.phase = "active";
V.rival.phaseT = 20;
V.rival.cut = "slash";
V.rival.cutHit = false;
V.rival.guarding = false;
V.player.hp = 100;
V.rival.hp = 100;
fireThrow();
let ateSlash=false, grabbedAnyway=false;
for(let i=0;i<50;i++){
  holdRivalStill();
  if(V.rival.phase==="idle" && V.rival.cutHit) { /* landed */ }
  else if(V.rival.phase!=="recovery" || !V.rival.cutHit){
    V.rival.phase = "active";
    V.rival.cut = "slash";
    V.rival.guarding = false;
  }
  V.update(STEP);
  if(V.player.hp<100) ateSlash=true;
  if(V.rival.thrownT>0 || (V.player.cut==="throw" && V.player.cutHit && V.rival.hp<100)) grabbedAnyway=true;
  if(ateSlash || grabbedAnyway) break;
}
drainHitstop();
check("throw lose to active slash", ateSlash && !grabbedAnyway && V.player.hp===90 && V.rival.hp===100 && V.rival.thrownT===0, `ate=${ateSlash} grab=${grabbedAnyway} php=${V.player.hp} rhp=${V.rival.hp} th=${V.rival.thrownT} pcut=${V.player.cut} rph=${V.rival.phase}`);

check("throw tech window 80 not recovery", V.THROW_TECH_MS===80 && V.THROW_TECH_MS===V.THROW_STARTUP && V.THROW_TECH_REC===160 && V.THROW_TECH_REC<V.THROW_RECOVERY, `win=${V.THROW_TECH_MS} rec=${V.THROW_TECH_REC} recov=${V.THROW_RECOVERY}`);
check("throw tech source is startup only", /phase === "startup"/.test(sliceFn(codeOnly, "throwTechWindow")) && !/phase === "active"/.test(sliceFn(codeOnly, "throwTechWindow")) && !/THROW_RECOVERY/.test(sliceFn(codeOnly, "throwTechWindow")));
check("throw tech not a new attack", /hitbox/.test(sliceFn(codeOnly, "tryThrowTech"))===false && /startAttack/.test(sliceFn(codeOnly, "tryThrowTech"))===false && /startThrow/.test(sliceFn(codeOnly, "landThrowTech"))===false);
check("rival throw from tickAI", /rivalTryThrow\(\)/.test(sliceFn(codeOnly, "tickAI")));
check("rival techs from tickAI", /rivalTryThrowTech\(\)/.test(sliceFn(codeOnly, "tickAI")) && /tryThrowTech\(rival\)/.test(sliceFn(codeOnly, "rivalTryThrowTech")));
check("rival throw tech is a chance", /armRivalThrowTech/.test(codeOnly) && /Math.random\(\) < THROW_TECH_CHANCE/.test(sliceFn(codeOnly, "armRivalThrowTech")) && V.THROW_TECH_CHANCE>=0.4 && V.THROW_TECH_CHANCE<=0.5, `p=${V.THROW_TECH_CHANCE}`);
check("rival throw tech same window", /throwTechWindow\(player\)/.test(sliceFn(codeOnly, "rivalTryThrowTech")) && V.THROW_TECH_MS===80 && V.THROW_TECH_REC===160);
check("throw frames still 80/40/220 after tech", V.THROW_STARTUP===80 && V.THROW_ACTIVE===40 && V.THROW_RECOVERY===220 && V.THROW_DMG===20 && V.THROW_RANGE===120 && V.THROW_STAM===0);

// tech in startup breaks (defender Space+S / tryThrowTech)
toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100; V.rival.stamina = 100;
V.player.thrownT = 0; V.rival.thrownT = 0;
const gTech0 = V.absGap();
fireThrow();
let sawThrowStart=false;
for(let i=0;i<20;i++){
  holdRivalStill();
  V.update(STEP);
  if(V.player.cut==="throw" && V.player.phase==="startup"){ sawThrowStart=true; break; }
}
const techOk = sawThrowStart && V.tryThrowTech(V.rival);
check("tech in startup breaks", techOk && V.player.hp===100 && V.rival.hp===100 && V.player.techRec && V.rival.techRec && V.player.phase==="recovery" && V.rival.phase==="recovery" && V.rival.thrownT===0 && !V.player.cutHit && !V.rival.cutHit, `ok=${techOk} start=${sawThrowStart} php=${V.player.hp} rhp=${V.rival.hp} ptech=${V.player.techRec} rtech=${V.rival.techRec} pph=${V.player.phase} rph=${V.rival.phase} th=${V.rival.thrownT} ch=${V.player.cutHit} g0=${gTech0.toFixed(1)}`);
const recLeftTech = V.player.phase==="recovery" ? (V.THROW_TECH_REC - V.player.phaseT) : -1;
let stillLocked=true, techRecMs=0;
for(let i=0;i<Math.ceil(90/STEP);i++){
  holdRivalStill();
  V.update(STEP);
  techRecMs += STEP;
  if(V.player.phase==="idle" || V.rival.phase==="idle"){ stillLocked=false; break; }
}
check("tech shared recovery holds ~160", stillLocked && V.player.techRec && V.rival.techRec && V.player.phase==="recovery" && V.rival.phase==="recovery", `lock=${stillLocked} recMs=${techRecMs.toFixed(0)} pph=${V.player.phase} rph=${V.rival.phase} left0=${recLeftTech.toFixed(0)}`);
for(let i=0;i<40;i++){
  holdRivalStill();
  V.update(STEP);
  if(V.player.phase==="idle" && V.rival.phase==="idle") break;
}
check("tech recovery returns idle", V.player.phase==="idle" && V.rival.phase==="idle" && !V.player.techRec && !V.rival.techRec && V.player.hp===100 && V.rival.hp===100, `pph=${V.player.phase} rph=${V.rival.phase} ptech=${V.player.techRec} rtech=${V.rival.techRec}`);

// player Space+S as defender during rival throw startup
toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.hp = 100; V.rival.hp = 100;
V.rival.cut = "throw";
V.rival.phase = "startup";
V.rival.phaseT = 0;
V.rival.cutHit = false;
V.rival.techRec = false;
V.rival.guarding = false;
fireThrow();
V.update(STEP);
check("defender Space+S techs startup", V.player.techRec && V.rival.techRec && V.player.hp===100 && V.rival.hp===100 && V.player.phase==="recovery" && V.rival.phase==="recovery" && V.player.thrownT===0 && V.rival.thrownT===0 && !V.player.cutHit, `ptech=${V.player.techRec} rtech=${V.rival.techRec} php=${V.player.hp} rhp=${V.rival.hp} pph=${V.player.phase} rph=${V.rival.phase} pcut=${V.player.cut} rcut=${V.rival.cut}`);

// late tech fails, throw still lands
toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.hp = 100; V.rival.hp = 100;
V.rival.guarding = true;
V.rival.wantBlock = true;
V.rival.guardChoice = 1;
fireThrow();
for(let i=0;i<20;i++){
  holdRivalStill();
  V.rival.wantBlock = true;
  V.rival.guarding = true;
  V.update(STEP);
  if(V.player.cut==="throw" && V.player.phase==="startup") break;
}
while(V.player.cut==="throw" && V.player.phase==="startup" && V.player.phaseT < V.THROW_STARTUP - STEP - 1){
  holdRivalStill();
  V.rival.wantBlock = true;
  V.rival.guarding = true;
  V.update(STEP);
}
check("late-tech setup still startup", V.player.cut==="throw" && V.player.phase==="startup" && V.rival.hp===100, `cut=${V.player.cut} ph=${V.player.phase} t=${V.player.phaseT.toFixed(0)} hp=${V.rival.hp}`);
V.player.phase = "active";
V.player.phaseT = 0;
const lateTech = V.tryThrowTech(V.rival);
check("late tech fails", lateTech===false && V.rival.hp===100 && V.rival.thrownT===0 && !V.player.techRec && !V.rival.techRec, `late=${lateTech} hp=${V.rival.hp} th=${V.rival.thrownT} ptech=${V.player.techRec} rtech=${V.rival.techRec} ph=${V.player.phase}`);
let lateHit=false;
for(let i=0;i<20;i++){
  holdRivalStill();
  V.rival.wantBlock = true;
  V.rival.guarding = true;
  V.update(STEP);
  if(V.hitstopLeft>0 || V.rival.hp<100 || V.rival.thrownT>0){ lateHit=true; break; }
}
drainHitstop();
check("late tech throw still lands", lateHit && V.rival.hp===80 && V.player.cutHit && V.rival.thrownT>0 && !V.player.techRec, `hit=${lateHit} hp=${V.rival.hp} ch=${V.player.cutHit} th=${V.rival.thrownT} ptech=${V.player.techRec}`);

// far throw is not a tech
bootPlay(); wait(1700);
freezeYouAI();
freezeRivalAI();
V.player.x = 180;
V.rival.x = 1100;
holdRivalStill();
fireThrow();
for(let i=0;i<20;i++){
  holdRivalStill();
  V.rival.x = 1100;
  V.update(STEP);
  if(V.player.cut==="throw" && V.player.phase==="startup") break;
}
const farTech = V.tryThrowTech(V.rival);
let farDmg2=false, farAct=false;
for(let i=0;i<80;i++){
  holdRivalStill();
  V.rival.x = 1100;
  V.update(STEP);
  if(V.player.cut==="throw" && V.player.phase==="active") farAct=true;
  if(V.rival.hp<100 || V.rival.thrownT>0 || V.player.techRec || V.rival.techRec) farDmg2=true;
  if(V.player.cut==="throw" && V.player.phase==="recovery") break;
}
check("far throw still whiffs, tech is not an attack", farTech===false && farAct && !farDmg2 && V.rival.hp===100 && V.rival.thrownT===0 && !V.player.cutHit && !V.player.techRec, `tech=${farTech} act=${farAct} dmg=${farDmg2} hp=${V.rival.hp} th=${V.rival.thrownT} ch=${V.player.cutHit} ptech=${V.player.techRec}`);

function fakeZone(){
  const c = new Set();
  return {
    classList: {
      add(x){ c.add(x); },
      remove(x){ c.delete(x); },
      contains(x){ return c.has(x); },
    },
  };
}
function padChord(zs, zslash){
  V.padDown("KeyS", 101, zs);
  V.padDown("Space", 102, zslash);
}
function padChordUp(){
  V.padUp(101);
  V.padUp(102);
}

// pad simulated chord close throws (same as keyboard Space+S)
toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100; V.rival.stamina = 80;
V.rival.wantBlock = true;
V.rival.guarding = true;
V.rival.guardChoice = 1;
const zsClose = fakeZone();
const zslClose = fakeZone();
const gPad = V.absGap();
padChord(zsClose, zslClose);
check("pad chord pressed look on both", zsClose.classList.contains("held") && zslClose.classList.contains("held") && V.padChordHeld(), `s=${zsClose.classList.contains("held")} sl=${zslClose.classList.contains("held")} chord=${V.padChordHeld()}`);
let padThrowHit=false;
for(let i=0;i<80;i++){
  holdRivalStill();
  V.rival.wantBlock = true;
  V.rival.guarding = true;
  V.rival.guardChoice = 1;
  V.update(STEP);
  if(V.hitstopLeft>0 || V.rival.hp<100 || V.rival.thrownT>0){ padThrowHit=true; break; }
}
drainHitstop();
check("pad chord close throws", padThrowHit && V.rival.hp===80 && V.player.hp===100 && V.player.cut==="throw" && V.player.cutHit && V.rival.thrownT>0 && gPad<=V.THROW_RANGE, `hit=${padThrowHit} rhp=${V.rival.hp} php=${V.player.hp} cut=${V.player.cut} ch=${V.player.cutHit} th=${V.rival.thrownT} g=${gPad.toFixed(1)} ph=${V.player.phase}`);
padChordUp();

// pad simulated chord far does not throw (stay guard)
bootPlay(); wait(1700);
freezeYouAI();
freezeRivalAI();
V.player.x = 180;
V.rival.x = 1100;
holdRivalStill();
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100;
V.player.guardBreakT = 0;
const farPad0 = V.absGap();
const zsFar = fakeZone();
const zslFar = fakeZone();
V.padDown("KeyS", 101, zsFar);
V.update(STEP);
const guardedBefore = V.player.guarding;
V.padDown("Space", 102, zslFar);
let padFarThrow=false, padFarSlash=false;
for(let i=0;i<80;i++){
  holdRivalStill();
  V.rival.x = 1100;
  V.update(STEP);
  if(V.player.cut==="throw" || V.player.thrownT>0 || V.rival.thrownT>0) padFarThrow=true;
  if(V.player.cut==="slash" && V.player.phase!=="idle") padFarSlash=true;
  if(V.rival.hp<100) padFarThrow=true;
}
check("pad chord far does not throw", farPad0>400 && !padFarThrow && V.rival.hp===100 && V.rival.thrownT===0 && V.player.cut!=="throw" && V.player.phase==="idle", `g0=${farPad0.toFixed(1)} throw=${padFarThrow} slash=${padFarSlash} hp=${V.rival.hp} cut=${V.player.cut} ph=${V.player.phase} guard0=${guardedBefore} guard=${V.player.guarding}`);
check("pad chord far stays guard not slash", guardedBefore && V.player.guarding && !padFarSlash && V.player.phase==="idle", `g0=${guardedBefore} g=${V.player.guarding} slash=${padFarSlash} ph=${V.player.phase}`);
padChordUp();

// pad chord techs the same way (defender hold-guarda + tap-tajo in startup)
toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.hp = 100; V.rival.hp = 100;
V.rival.cut = "throw";
V.rival.phase = "startup";
V.rival.phaseT = 0;
V.rival.cutHit = false;
V.rival.techRec = false;
V.rival.guarding = false;
const zsTech = fakeZone();
const zslTech = fakeZone();
padChord(zsTech, zslTech);
V.update(STEP);
check("pad chord techs startup", V.player.techRec && V.rival.techRec && V.player.hp===100 && V.rival.hp===100 && V.player.phase==="recovery" && V.rival.phase==="recovery" && V.player.thrownT===0 && V.rival.thrownT===0 && !V.player.cutHit, `ptech=${V.player.techRec} rtech=${V.rival.techRec} php=${V.player.hp} rhp=${V.rival.hp} pph=${V.player.phase} rph=${V.rival.phase} pcut=${V.player.cut} rcut=${V.rival.cut}`);
padChordUp();

function armRivalThrowAI(){
  Math.random = () => 0.99;
  V.rival.phase = "idle";
  V.rival.phaseT = 0;
  V.rival.cut = "slash";
  V.rival.cutHit = false;
  V.rival.techRec = false;
  V.rival.throwTechArmed = -1;
  V.rival.thrownT = 0;
  V.rival.boltPhase = "";
  V.rival.boltT = 0;
  V.rival.boltCd = 99999;
  V.rival.throwCd = 0;
  V.rival.revCd = 99999;
  V.rival.revArmed = -1;
  V.rival.pbCd = 99999;
  V.rival.pbArmed = -1;
  V.rival.boltArmed = false;
  V.rival.stunT = 0;
  V.rival.guarding = false;
  V.rival.wantBlock = false;
  V.rival.guardChoice = -1;
  V.rival.closing = false;
  V.rival.standWait = 0;
  V.rival.standGoal = 99999;
  V.rival.falling = false;
  V.rival.guardBreakT = 0;
  V.rival.pushT = 0;
  V.rival.stamina = 100;
  V.rival.stamRegenT = 99999;
  V.player.hp = 100;
  V.rival.hp = 100;
  V.player.thrownT = 0;
  V.player.stunT = 0;
  V.player.techRec = false;
}

function holdYouGuard(){
  V.keys.add("KeyS");
  V.player.wantBlock = true;
  V.player.falling = false;
  if(V.player.stunT<=0 && V.player.phase==="idle"){
    V.player.stamina = Math.max(20, V.player.stamina);
    V.player.guardBreakT = 0;
  }
}

// rival throws a guarding close player
toClose();
armRivalThrowAI();
holdYouGuard();
V.update(STEP);
check("rival throw starts vs close guard", V.player.guarding && V.absGap()<=V.THROW_RANGE && V.rival.cut==="throw" && V.rival.phase==="startup" && V.rival.throwCd===V.THROW_AI_CD, `g=${V.player.guarding} gap=${V.absGap().toFixed(1)} cut=${V.rival.cut} ph=${V.rival.phase} cd=${V.rival.throwCd} rph=${V.rival.phase}`);
let rivalGrab=false;
for(let i=0;i<80;i++){
  holdYouGuard();
  V.update(STEP);
  if(V.hitstopLeft>0 || V.player.hp<100 || V.player.thrownT>0){ rivalGrab=true; break; }
}
drainHitstop();
check("rival throw vs close guard connects", rivalGrab && V.player.hp===80 && V.rival.hp===100 && V.rival.cut==="throw" && V.rival.cutHit && V.player.thrownT>0, `hit=${rivalGrab} php=${V.player.hp} rhp=${V.rival.hp} cut=${V.rival.cut} ch=${V.rival.cutHit} th=${V.player.thrownT} rph=${V.rival.phase}`);
check("rival throw same dmg not a block", V.player.hp===80 && V.player.pushT===0 && V.player.thrownT>0, `hp=${V.player.hp} push=${V.player.pushT} th=${V.player.thrownT}`);
const rivalThrownSheet = V.poseSheet(V.player);
const youKoSheet = V.poseSheet({kind:"you", facing:1, falling:true, hp:0, thrownT:0, phase:"idle", stunT:0, guarding:false, boltPhase:"", boltHoldCut:false, sheatheT:0, gait:0, walkT:0, img:null});
check("rival throw knockdown reuses hurt art", V.player.thrownT>0 && rivalThrownSheet===youKoSheet, `th=${V.player.thrownT}`);

// cooldown so they don't spam
toClose();
armRivalThrowAI();
holdYouGuard();
V.update(STEP);
check("rival throw CD set on start", V.rival.cut==="throw" && V.rival.throwCd===V.THROW_AI_CD, `cut=${V.rival.cut} cd=${V.rival.throwCd}`);
const cdAtStart = V.rival.throwCd;
let secondThrow=false;
for(let i=0;i<Math.ceil(900/STEP);i++){
  holdYouGuard();
  const wasThrow = V.rival.cut==="throw" && (V.rival.phase==="startup" || V.rival.phase==="active");
  V.update(STEP);
  const nowThrow = V.rival.cut==="throw" && V.rival.phase==="startup" && V.rival.phaseT < STEP+1 && V.rival.throwCd>=V.THROW_AI_CD-STEP;
  if(!wasThrow && V.rival.cut==="throw" && V.rival.phase==="startup" && i>2){ secondThrow=true; break; }
}
check("rival throw CD holds, no spam", cdAtStart===V.THROW_AI_CD && V.rival.throwCd>0 && V.rival.throwCd<cdAtStart && !secondThrow, `cd0=${cdAtStart} cd=${V.rival.throwCd.toFixed(0)} spam=${secondThrow} cut=${V.rival.cut} ph=${V.rival.phase}`);

// does not throw from far
toClose();
armRivalThrowAI();
V.player.x = 180;
V.rival.x = 1100;
holdYouGuard();
const farGap0 = V.absGap();
let farRivalThrow=false;
for(let i=0;i<40;i++){
  holdYouGuard();
  V.player.x = 180;
  V.rival.x = 1100;
  V.update(STEP);
  if(V.rival.cut==="throw" || V.player.thrownT>0 || V.player.hp<100) farRivalThrow=true;
}
check("rival does not throw from far", farGap0>400 && !farRivalThrow && V.rival.cut!=="throw" && V.player.hp===100 && V.player.thrownT===0, `g0=${farGap0.toFixed(1)} throw=${farRivalThrow} cut=${V.rival.cut} hp=${V.player.hp} th=${V.player.thrownT} ph=${V.rival.phase}`);

// idle/walking player eats a slash, not a grab
toMeasure();
armRivalThrowAI();
freezeYouAI();
V.keys.clear();
V.player.guarding = false;
V.player.wantBlock = false;
const holdIdle = V.bladeReach(V.rival)+V.LUNGE_PX;
if (V.bodyGap() > holdIdle - 8) V.rival.x -= (V.bodyGap() - (holdIdle - 8));
V.rival.standWait = 0;
V.rival.standGoal = 1;
V.rival.closing = false;
V.rival.phase = "idle";
let idleGrab=false, idleSlash=false;
for(let i=0;i<40;i++){
  V.keys.clear();
  V.player.guarding = false;
  V.update(STEP);
  if(V.rival.cut==="throw" || V.player.thrownT>0) idleGrab=true;
  if(V.rival.phase==="startup" && (V.rival.cut==="slash" || V.rival.cut==="golpe")) idleSlash=true;
  if(idleGrab || idleSlash) break;
}
check("idle close player eats slash not grab", !idleGrab && idleSlash && V.player.thrownT===0 && V.rival.cut!=="throw", `grab=${idleGrab} slash=${idleSlash} cut=${V.rival.cut} ph=${V.rival.phase} th=${V.player.thrownT} g=${V.player.guarding} gap=${V.absGap().toFixed(1)}`);

// player tech still breaks a live rival throw
toClose();
armRivalThrowAI();
holdYouGuard();
V.update(STEP);
check("rival throw startup for tech", V.rival.cut==="throw" && V.rival.phase==="startup" && V.player.guarding, `cut=${V.rival.cut} ph=${V.rival.phase} g=${V.player.guarding} t=${V.rival.phaseT.toFixed(0)}`);
V.keys.add("KeyS");
V.keys.add("Space");
V.throwEdge = true;
V.update(STEP);
check("player tech breaks rival throw", V.player.techRec && V.rival.techRec && V.player.hp===100 && V.rival.hp===100 && V.player.phase==="recovery" && V.rival.phase==="recovery" && V.player.thrownT===0 && V.rival.thrownT===0 && !V.rival.cutHit, `ptech=${V.player.techRec} rtech=${V.rival.techRec} php=${V.player.hp} rhp=${V.rival.hp} pph=${V.player.phase} rph=${V.rival.phase} th=${V.player.thrownT} ch=${V.rival.cutHit}`);
V.keys.clear();

// rival techs a player throw in the same 80ms startup (chance roll yes)
toClose();
freezeYouAI();
freezeRivalAI();
V.player.hp = 100; V.rival.hp = 100;
V.player.thrownT = 0; V.rival.thrownT = 0;
V.rival.techRec = false; V.player.techRec = false;
V.rival.throwTechArmed = 0;
Math.random = () => 0;
fireThrow();
let rivalTeched=false;
for(let i=0;i<20;i++){
  Math.random = () => 0;
  V.update(STEP);
  if(V.player.techRec && V.rival.techRec){ rivalTeched=true; break; }
  if(V.player.cut==="throw" && V.player.phase!=="startup") break;
}
check("rival techs player throw in startup", rivalTeched && V.player.hp===100 && V.rival.hp===100 && V.player.techRec && V.rival.techRec && V.player.phase==="recovery" && V.rival.phase==="recovery" && V.rival.thrownT===0 && !V.player.cutHit, `ok=${rivalTeched} php=${V.player.hp} rhp=${V.rival.hp} ptech=${V.player.techRec} rtech=${V.rival.techRec} pph=${V.player.phase} rph=${V.rival.phase} cut=${V.player.cut} ph=${V.player.phase} armed=${V.rival.throwTechArmed} th=${V.rival.thrownT}`);

// chance miss: not every throw
toClose();
freezeYouAI();
freezeRivalAI();
V.player.hp = 100; V.rival.hp = 100;
V.rival.guarding = true;
V.rival.wantBlock = true;
V.rival.guardChoice = 1;
V.rival.throwTechArmed = 0;
Math.random = () => 0.99;
fireThrow();
let missHit=false, missTech=false;
for(let i=0;i<80;i++){
  Math.random = () => 0.99;
  V.rival.wantBlock = true;
  V.rival.guarding = true;
  V.rival.guardChoice = 1;
  V.rival.standGoal = 99999;
  V.rival.closing = false;
  V.update(STEP);
  if(V.player.techRec || V.rival.techRec){ missTech=true; break; }
  if(V.hitstopLeft>0 || V.rival.hp<100 || V.rival.thrownT>0){ missHit=true; break; }
}
drainHitstop();
check("rival does not tech every throw", !missTech && missHit && V.rival.hp===80 && V.player.cutHit && V.rival.thrownT>0 && !V.player.techRec && !V.rival.techRec, `tech=${missTech} hit=${missHit} hp=${V.rival.hp} ch=${V.player.cutHit} th=${V.rival.thrownT} ptech=${V.player.techRec} rtech=${V.rival.techRec} armed=${V.rival.throwTechArmed}`);

// late rival tech fails (after startup)
toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.hp = 100; V.rival.hp = 100;
V.rival.guarding = true;
V.rival.wantBlock = true;
V.rival.guardChoice = 1;
fireThrow();
for(let i=0;i<20;i++){
  holdRivalStill();
  V.rival.wantBlock = true;
  V.rival.guarding = true;
  V.update(STEP);
  if(V.player.cut==="throw" && V.player.phase==="startup") break;
}
while(V.player.cut==="throw" && V.player.phase==="startup" && V.player.phaseT < V.THROW_STARTUP - STEP - 1){
  holdRivalStill();
  V.rival.wantBlock = true;
  V.rival.guarding = true;
  V.update(STEP);
}
check("late rival-tech setup still startup", V.player.cut==="throw" && V.player.phase==="startup" && V.rival.hp===100, `cut=${V.player.cut} ph=${V.player.phase} t=${V.player.phaseT.toFixed(0)} hp=${V.rival.hp}`);
V.player.phase = "active";
V.player.phaseT = 0;
V.rival.throwTechArmed = 1;
Math.random = () => 0;
const lateAiTech = V.tryThrowTech(V.rival);
check("late rival tech fails", lateAiTech===false && V.rival.hp===100 && V.rival.thrownT===0 && !V.player.techRec && !V.rival.techRec, `late=${lateAiTech} hp=${V.rival.hp} th=${V.rival.thrownT} ptech=${V.player.techRec} rtech=${V.rival.techRec} ph=${V.player.phase}`);
let lateAiHit=false;
for(let i=0;i<20;i++){
  V.rival.throwTechArmed = 1;
  Math.random = () => 0;
  V.rival.wantBlock = true;
  V.rival.guarding = true;
  V.update(STEP);
  if(V.player.techRec || V.rival.techRec) break;
  if(V.hitstopLeft>0 || V.rival.hp<100 || V.rival.thrownT>0){ lateAiHit=true; break; }
}
drainHitstop();
check("late rival tech throw still lands", lateAiHit && V.rival.hp===80 && V.player.cutHit && V.rival.thrownT>0 && !V.player.techRec && !V.rival.techRec, `hit=${lateAiHit} hp=${V.rival.hp} ch=${V.player.cutHit} th=${V.rival.thrownT} ptech=${V.player.techRec} rtech=${V.rival.techRec}`);

// throw vs unteched guard still hits (random would have teched, freeze disarmed)
toClose();
freezeYouAI();
freezeRivalAI();
Math.random = () => 0;
V.rival.stamina = 80;
V.rival.stamRegenT = 99999;
V.rival.guardBreakT = 0;
V.rival.wantBlock = true;
V.rival.guarding = true;
V.rival.guardChoice = 1;
V.rival.throwTechArmed = -1;
V.player.stamina = 100;
V.player.stamRegenT = 99999;
fireThrow();
let untechHit=false;
for(let i=0;i<80;i++){
  Math.random = () => 0;
  V.rival.throwTechArmed = -1;
  V.rival.wantBlock = true;
  V.rival.guarding = true;
  V.rival.guardChoice = 1;
  V.rival.standGoal = 99999;
  V.rival.closing = false;
  V.update(STEP);
  if(V.player.techRec || V.rival.techRec) break;
  if(V.hitstopLeft>0 || V.rival.hp<100 || V.rival.thrownT>0){ untechHit=true; break; }
}
drainHitstop();
check("throw vs unteched guard still hits", untechHit && V.rival.hp===80 && V.player.hp===100 && V.player.cut==="throw" && V.player.cutHit && V.rival.thrownT>0 && !V.player.techRec && !V.rival.techRec, `hit=${untechHit} rhp=${V.rival.hp} php=${V.player.hp} ch=${V.player.cutHit} th=${V.rival.thrownT} ptech=${V.player.techRec} rtech=${V.rival.techRec}`);

check("wakeup throw-invuln is short not a super", V.THROW_WAKE_INVULN===80 && V.THROW_WAKE_INVULN===V.THROW_STARTUP && V.THROW_WAKE_INVULN<V.THROW_KD_MS && V.THROW_WAKE_INVULN<V.THROW_AI_CD, `w=${V.THROW_WAKE_INVULN}`);
check("throw frames/tech/CD untouched", V.THROW_STARTUP===80 && V.THROW_ACTIVE===40 && V.THROW_RECOVERY===220 && V.THROW_DMG===20 && V.THROW_RANGE===120 && V.THROW_TECH_MS===80 && V.THROW_TECH_CHANCE===0.45 && V.THROW_AI_CD===1800 && V.THROW_KD_MS===520);
check("resolveThrow refuses locked defender", /throwLocked\(def\)/.test(sliceFn(codeOnly, "resolveThrow")) && /stunT/.test(sliceFn(codeOnly, "throwLocked")) && /thrownT/.test(sliceFn(codeOnly, "throwLocked")) && /throwInvulnT/.test(sliceFn(codeOnly, "throwLocked")));
check("startThrow refuses locked defender", /throwLocked\(throwFoe\(f\)\)/.test(sliceFn(codeOnly, "startThrow")));
check("wakeup invuln arms from thrownT expiry", /throwInvulnT = THROW_WAKE_INVULN/.test(sliceFn(codeOnly, "tickThrowState")));

function holdRivalNoGrab(){
  Math.random = () => 0.99;
  V.rival.boltCd = 99999;
  V.rival.throwCd = 99999;
  V.rival.revCd = 99999;
  V.rival.revArmed = -1;
  V.rival.wakeRevArmed = -1;
  V.rival.pbCd = 99999;
  V.rival.pbArmed = -1;
  V.rival.throwTechArmed = -1;
  V.rival.boltPhase = "";
  V.rival.boltArmed = false;
  V.rival.linkGolpe = false;
  V.rival.linkSlash = false;
  V.rival.linkBolt = false;
  V.rival.closing = false;
  V.rival.standWait = 0;
  V.rival.standGoal = 99999;
  V.rival.gait = 0;
}

function landCloseThrow(){
  toClose();
  freezeYouAI();
  freezeRivalAI();
  Math.random = () => 0;
  V.rival.stamina = 80;
  V.rival.stamRegenT = 99999;
  V.rival.guardBreakT = 0;
  V.rival.wantBlock = true;
  V.rival.guarding = true;
  V.rival.guardChoice = 1;
  V.player.stamina = 100;
  V.player.stamRegenT = 99999;
  V.player.throwInvulnT = 0;
  V.rival.throwInvulnT = 0;
  fireThrow();
  let hit=false;
  for(let i=0;i<80;i++){
    holdRivalNoGrab();
    V.rival.wantBlock = true;
    V.rival.guarding = true;
    V.rival.guardChoice = 1;
    V.update(STEP);
    if(V.hitstopLeft>0 || V.rival.hp<100 || V.rival.thrownT>0){ hit=true; break; }
  }
  drainHitstop();
  return hit;
}

const loopLand = landCloseThrow();
check("loop setup throw landed", loopLand && V.rival.hp===80 && V.rival.thrownT>0 && V.rival.stunT>0, `hit=${loopLand} hp=${V.rival.hp} th=${V.rival.thrownT} st=${V.rival.stunT} ph=${V.player.phase}`);

// Recovery 220 + startup 80 sits inside 520 KD. Stop as soon as the thrower is idle — do not wait out the 520.
for(let i=0;i<20;i++){
  holdRivalNoGrab();
  V.update(STEP);
  if(V.player.phase==="idle" && V.rival.thrownT>0) break;
}
check("player recovered inside KD", V.player.phase==="idle" && V.rival.thrownT>0 && V.rival.stunT>0 && V.rival.hp===80 && V.rival.thrownT>V.THROW_STARTUP, `pph=${V.player.phase} th=${V.rival.thrownT} st=${V.rival.stunT} hp=${V.rival.hp} rec=${V.THROW_RECOVERY} kd=${V.THROW_KD_MS}`);
const kdHp = V.rival.hp;
const kdThrown = V.rival.thrownT;
freezeYouAI();
V.player.stamina = 100;
V.rival.wantBlock = true;
V.rival.guarding = true;
const kdStart = V.startThrow(V.player);
check("throw on KD does not start", kdStart===false && V.player.cut!=="throw" && V.player.phase==="idle" && V.rival.thrownT>0, `start=${kdStart} cut=${V.player.cut} ph=${V.player.phase} th=${V.rival.thrownT}`);
let kdGrab=false;
for(let i=0;i<12 && V.rival.thrownT>STEP*2;i++){
  holdRivalNoGrab();
  V.rival.throwTechArmed = -1;
  freezeYouAI();
  V.player.stamina = 100;
  fireThrow();
  V.update(STEP);
  if(V.rival.hp<kdHp || (V.player.cutHit && V.player.cut==="throw")){ kdGrab=true; break; }
}
check("throw on KD fails", !kdGrab && V.rival.hp===kdHp && V.rival.thrownT>0 && V.throwLocked(V.rival), `grab=${kdGrab} hp=${V.rival.hp} th=${V.rival.thrownT} st=${V.rival.stunT} inv=${V.rival.throwInvulnT} pcut=${V.player.cut} pph=${V.player.phase}`);

// Active throw already in flight still cannot resolve on KD.
toClose();
freezeYouAI();
freezeRivalAI();
V.player.cut = "throw";
V.player.phase = "active";
V.player.phaseT = 0;
V.player.cutHit = false;
V.player.techRec = false;
V.rival.hp = 80;
V.rival.thrownT = 400;
V.rival.stunT = 400;
V.rival.throwInvulnT = 0;
V.rival.guarding = false;
V.rival.phase = "idle";
holdRivalNoGrab();
V.update(STEP);
check("throw resolve on KD fails", V.rival.hp===80 && V.rival.thrownT>0 && !V.player.cutHit, `hp=${V.rival.hp} th=${V.rival.thrownT} ch=${V.player.cutHit} ph=${V.player.phase}`);

// Regular hitstun is also not throwable.
toClose();
freezeYouAI();
freezeRivalAI();
V.rival.stunT = V.HITSTUN;
V.rival.thrownT = 0;
V.rival.throwInvulnT = 0;
V.rival.hp = 100;
V.rival.guarding = true;
V.rival.wantBlock = true;
const stunStart = V.startThrow(V.player);
check("throw on stun does not start", stunStart===false && V.throwLocked(V.rival) && V.player.phase==="idle", `start=${stunStart} st=${V.rival.stunT} locked=${V.throwLocked(V.rival)}`);
V.player.cut = "throw";
V.player.phase = "active";
V.player.phaseT = 0;
V.player.cutHit = false;
holdRivalNoGrab();
V.rival.stunT = V.HITSTUN;
V.rival.thrownT = 0;
V.update(STEP);
check("throw on stun fails", V.rival.hp===100 && !V.player.cutHit && V.rival.thrownT===0, `hp=${V.rival.hp} ch=${V.player.cutHit} th=${V.rival.thrownT} st=${V.rival.stunT}`);

// Wakeup invuln: first grounded frames are not a free re-grab; after it, throw lands.
const wakeLand = landCloseThrow();
check("wakeup setup throw landed", wakeLand && V.rival.hp===80 && V.rival.thrownT>0, `hit=${wakeLand} hp=${V.rival.hp} th=${V.rival.thrownT}`);
let sawInv=false;
for(let i=0;i<80;i++){
  holdRivalNoGrab();
  V.update(STEP);
  if(V.rival.thrownT<=0 && V.rival.throwInvulnT>0){ sawInv=true; break; }
}
check("wakeup arms short throw-invuln", sawInv && V.rival.thrownT===0 && V.rival.throwInvulnT>0 && V.rival.throwInvulnT<=V.THROW_WAKE_INVULN && V.throwLocked(V.rival), `saw=${sawInv} th=${V.rival.thrownT} inv=${V.rival.throwInvulnT} st=${V.rival.stunT}`);
freezeYouAI();
V.player.stamina = 100;
V.rival.wantBlock = true;
V.rival.guarding = true;
const invStart = V.startThrow(V.player);
check("throw during wakeup invuln does not start", invStart===false && V.player.phase==="idle", `start=${invStart} inv=${V.rival.throwInvulnT} ph=${V.player.phase}`);
V.player.cut = "throw";
V.player.phase = "active";
V.player.phaseT = 0;
V.player.cutHit = false;
const invHp = V.rival.hp;
holdRivalNoGrab();
V.rival.guarding = true;
V.update(STEP);
check("throw during wakeup invuln does not resolve", V.rival.hp===invHp && !V.player.cutHit, `hp=${V.rival.hp} ch=${V.player.cutHit} inv=${V.rival.throwInvulnT}`);

// After invuln, standing guard is throwable again.
freezeYouAI();
for(let i=0;i<20;i++){
  holdRivalNoGrab();
  V.update(STEP);
  if(V.rival.throwInvulnT<=0 && V.rival.thrownT<=0 && V.rival.stunT<=0) break;
}
check("wakeup invuln ended", V.rival.throwInvulnT===0 && V.rival.thrownT===0 && V.rival.stunT<=0 && !V.throwLocked(V.rival), `inv=${V.rival.throwInvulnT} th=${V.rival.thrownT} st=${V.rival.stunT} locked=${V.throwLocked(V.rival)}`);
freezeYouAI();
freezeRivalAI();
V.player.stamina = 100;
V.player.stamRegenT = 99999;
V.rival.hp = 80;
V.rival.stamina = 80;
V.rival.stamRegenT = 99999;
V.rival.wantBlock = true;
V.rival.guarding = true;
V.rival.guardChoice = 1;
V.rival.throwTechArmed = -1;
Math.random = () => 0;
const afterStart = V.startThrow(V.player);
check("throw after wakeup invuln can start", afterStart===true && V.player.cut==="throw" && V.player.phase==="startup", `start=${afterStart} cut=${V.player.cut} ph=${V.player.phase}`);
let afterHit=false;
for(let i=0;i<80;i++){
  holdRivalNoGrab();
  V.rival.wantBlock = true;
  V.rival.guarding = true;
  V.rival.guardChoice = 1;
  V.rival.throwTechArmed = -1;
  Math.random = () => 0;
  V.update(STEP);
  if(V.hitstopLeft>0 || V.rival.hp<80 || V.rival.thrownT>0){ afterHit=true; break; }
}
drainHitstop();
check("throw after wakeup invuln can land", afterHit && V.rival.hp===60 && V.player.cut==="throw" && V.player.cutHit && V.rival.thrownT>0, `hit=${afterHit} hp=${V.rival.hp} ch=${V.player.cutHit} th=${V.rival.thrownT} ph=${V.player.phase}`);

// Normal close throw vs standing guard still hits (fresh round, no leftover KD).
toClose();
freezeYouAI();
freezeRivalAI();
Math.random = () => 0;
V.rival.stamina = 80;
V.rival.stamRegenT = 99999;
V.rival.guardBreakT = 0;
V.rival.wantBlock = true;
V.rival.guarding = true;
V.rival.guardChoice = 1;
V.player.stamina = 100;
V.player.stamRegenT = 99999;
fireThrow();
let standHit=false;
for(let i=0;i<80;i++){
  V.rival.wantBlock = true;
  V.rival.guarding = true;
  V.rival.guardChoice = 1;
  V.rival.standGoal = 99999;
  V.rival.closing = false;
  V.rival.throwTechArmed = -1;
  V.update(STEP);
  if(V.hitstopLeft>0 || V.rival.hp<100 || V.rival.thrownT>0){ standHit=true; break; }
}
drainHitstop();
check("normal close throw vs standing guard still hits", standHit && V.rival.hp===80 && V.player.hp===100 && V.player.cut==="throw" && V.player.cutHit && V.rival.thrownT>0, `hit=${standHit} rhp=${V.rival.hp} php=${V.player.hp} ch=${V.player.cutHit} th=${V.rival.thrownT}`);

// Tech unchanged: startup-only, 80ms, 0 dmg, shared 160 rec.
toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.hp = 100; V.rival.hp = 100;
V.rival.guarding = true;
V.rival.wantBlock = true;
V.rival.guardChoice = 1;
fireThrow();
let sawStart=false;
for(let i=0;i<20;i++){
  holdRivalStill();
  V.rival.wantBlock = true;
  V.rival.guarding = true;
  V.update(STEP);
  if(V.player.cut==="throw" && V.player.phase==="startup"){ sawStart=true; break; }
}
check("tech recheck still startup", sawStart && V.throwTechWindow(V.player) && V.THROW_TECH_MS===80, `start=${sawStart} win=${V.throwTechWindow(V.player)} t=${V.player.phaseT}`);
const techStill = V.tryThrowTech(V.rival);
check("tech unchanged", techStill && V.player.hp===100 && V.rival.hp===100 && V.player.techRec && V.rival.techRec && V.player.phase==="recovery" && V.rival.phase==="recovery" && V.rival.thrownT===0 && !V.player.cutHit, `ok=${techStill} php=${V.player.hp} rhp=${V.rival.hp} ptech=${V.player.techRec} rtech=${V.rival.techRec} th=${V.rival.thrownT}`);

check("super meter one stock", V.METER_MAX===100 && V.METER_HIT===20 && V.METER_BLOCK_SPECIAL===10 && V.METER_HIT*2<V.METER_MAX && V.METER_HIT*5===V.METER_MAX);
check("super dart harder not a KO from 100", V.BOLT_SUPER_DMG>=25 && V.BOLT_SUPER_DMG<=30 && V.BOLT_SUPER_DMG>V.SLASH_DMG && V.BOLT_SUPER_DMG<V.MAX_HP);
check("super same plant family", V.BOLT_STARTUP===200 && V.BOLT_STAM===30 && V.BOLT_CHIP===2 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY>V.BOLT_RECOVERY && V.BOLT_SUPER_RECOVERY<=400);
check("super range not fullscreen", V.SUPER_RANGE===380 && V.SUPER_RANGE<800);
check("normals frames unchanged", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180);
check("throw layer unchanged", V.THROW_STARTUP===80 && V.THROW_ACTIVE===40 && V.THROW_RECOVERY===220 && V.THROW_DMG===20 && V.THROW_STAM===0 && V.THROW_RANGE===120);
check("cancel layer unchanged", V.SLASH_CANCEL_MS===100 && V.GOLPE_CANCEL_MS===100 && V.BOLT_CANCEL_MS===100);
check("chip unchanged", V.BOLT_CHIP===2 && !/def\.hp\s*=/.test(sliceFn(codeOnly, "landBlock")));
check("pad still same K no 6th button", (srcTxt.match(/boltEdge = true/g)||[]).length===1 && /data-tap="KeyK"/.test(htmlTxt) && !/data-tap="KeyU"/.test(htmlTxt) && !/zone-super/.test(htmlTxt));
check("meter HUD brasa pip palette", /HUD_METER_W/.test(srcTxt) && /f\.meter/.test(srcTxt) && /COL_BRASA/.test(srcTxt) && /COL_PIZARRA/.test(srcTxt) && /COL_OXIDO/.test(srcTxt) && /COL_HUESO/.test(srcTxt));
check("meter HUD fills while charging", /\(f\.meter \|\| 0\) \/ METER_MAX/.test(srcTxt) && /fillFrom\(left, mx, my, mw, mh, k\)/.test(srcTxt) && /k >= 1 \? COL_BRASA : COL_OXIDO/.test(srcTxt));
check("meter no new art sheets", !/loadImg\([^)]*(super|meter)/.test(srcTxt) && !/art\/super/.test(srcTxt) && !/art\/meter/.test(srcTxt));
check("rival super respects CD in source", /rivalShouldSuper/.test(codeOnly) && /startBolt\(rival, rivalShouldSuper\(\)\)/.test(codeOnly) && /BOLT_AI_CD/.test(sliceFn(codeOnly, "tickAI")));

toMeasure();
Math.random = () => 0.99;
freezeRivalAI();
V.rival.guarding = false;
V.rival.wantBlock = false;
V.rival.guardChoice = -1;
const mHit0 = V.player.meter;
V.attackEdge = true;
for(let i=0;i<50;i++){ V.update(STEP); if(V.rival.hp<100 || V.hitstopLeft>0) break; }
check("meter fills on hit", V.rival.hp===90 && V.player.meter===mHit0+V.METER_HIT && V.player.meter===20, `hp=${V.rival.hp} m=${V.player.meter} m0=${mHit0}`);

drainHitstop();
for(let i=0;i<50;i++){
  freezeRivalAI();
  V.rival.guarding = false;
  V.rival.wantBlock = false;
  V.rival.guardChoice = -1;
  V.update(STEP);
  if(V.player.phase==="idle" && !V.player.boltPhase) break;
}
V.player.x = V.rival.x - 170;
V.keepApart();
freezeRivalAI();
V.rival.guarding = false;
V.rival.wantBlock = false;
V.rival.guardChoice = -1;
V.rival.hp = 90;
const mAfter1 = V.player.meter;
V.attackEdge = true;
for(let i=0;i<50;i++){
  freezeRivalAI();
  V.rival.guarding = false;
  V.rival.wantBlock = false;
  V.rival.guardChoice = -1;
  V.update(STEP);
  if(V.rival.hp<90 || V.hitstopLeft>0) break;
}
check("two hits no longer fill a stock", V.rival.hp===80 && V.player.meter===mAfter1+V.METER_HIT && V.player.meter===40 && V.player.meter<V.METER_MAX, `hp=${V.rival.hp} m=${V.player.meter} m1=${mAfter1}`);
check("need more connects to fill", V.METER_HIT*2<V.METER_MAX && V.METER_HIT*5===V.METER_MAX && V.METER_BLOCK_SPECIAL===10, `hit=${V.METER_HIT} block=${V.METER_BLOCK_SPECIAL}`);

toMeasure();
Math.random = () => 0;
freezeRivalAI();
V.rival.stamina = 80;
V.rival.stamRegenT = 99999;
V.rival.guardBreakT = 0;
V.rival.wantBlock = true;
V.rival.guarding = true;
V.rival.guardChoice = 1;
V.player.meter = 0;
V.attackEdge = true;
for(let i=0;i<50;i++){ V.rival.wantBlock = true; V.rival.guarding = true; V.update(STEP); if(V.rival.pushT>0 || V.hitstopLeft>0) break; }
check("blocked melee does not fill meter", V.rival.hp===100 && V.player.meter===0, `hp=${V.rival.hp} m=${V.player.meter} push=${V.rival.pushT}`);

toMeasure();
Math.random = () => 0;
freezeRivalAI();
V.rival.stamina = 80;
V.rival.stamRegenT = 99999;
V.rival.guardBreakT = 0;
V.rival.wantBlock = true;
V.rival.guarding = true;
V.rival.guardChoice = 1;
V.player.meter = 0;
V.boltEdge = true;
for(let i=0;i<80;i++){ V.rival.wantBlock = true; V.rival.guarding = true; V.update(STEP); if(V.hitstopLeft>0 || V.rival.pushT>0) break; }
check("blocked special fills a little", V.rival.hp===100-V.BOLT_CHIP && V.player.meter===V.METER_BLOCK_SPECIAL && V.METER_BLOCK_SPECIAL===10, `hp=${V.rival.hp} m=${V.player.meter} chip=${V.BOLT_CHIP}`);
check("empty dart block SFX path", V.lastBoltConnectSfx==="bloqueo", `connect=${V.lastBoltConnectSfx}`);
check("empty dart block puff unchanged", V.brasaFxKind==="block" && (V.brasaFxScale||1)===1, `kind=${V.brasaFxKind} scale=${V.brasaFxScale}`);

toMeasure();
Math.random = () => 0.99;
freezeRivalAI();
V.rival.guarding = false;
V.rival.wantBlock = false;
V.rival.guardChoice = -1;
V.player.meter = 0;
V.player.stamina = 100;
V.boltEdge = true;
V.update(STEP);
check("empty K has no extra freeze", V.player.boltPhase==="startup" && V.player.boltSuper===false && V.hitstopLeft===0 && V.shake===0 && V.shakeMag===0, `bph=${V.player.boltPhase} super=${V.player.boltSuper} hs=${V.hitstopLeft} shake=${V.shake} mag=${V.shakeMag}`);
check("empty plant puff unchanged", V.brasaFxKind==="cast" && (V.brasaFxScale||1)===1 && V.player.boltSuper===false, `kind=${V.brasaFxKind} scale=${V.brasaFxScale} super=${V.player.boltSuper}`);
for(let i=0;i<80;i++){ V.update(STEP); if(V.rival.hp<100 || V.hitstopLeft>0) break; }
check("empty K is normal dart", V.player.meter===V.METER_HIT && V.rival.hp===90 && V.SLASH_DMG===10 && !V.player.boltSuper, `hp=${V.rival.hp} m=${V.player.meter} super=${V.player.boltSuper}`);
check("empty K dart SFX not sting", V.lastBoltSfx==="cast", `sfx=${V.lastBoltSfx}`);
check("empty dart connect SFX path", V.lastBoltConnectSfx==="impacto", `connect=${V.lastBoltConnectSfx}`);
check("empty dart land puff unchanged", V.brasaFxKind==="hit" && (V.brasaFxScale||1)===1, `kind=${V.brasaFxKind} scale=${V.brasaFxScale}`);

toMeasure();
Math.random = () => 0.99;
freezeRivalAI();
V.rival.guarding = false;
V.rival.wantBlock = false;
V.rival.guardChoice = -1;
V.player.meter = 100;
V.player.stamina = 100;
V.player.stamRegenT = 99999;
V.boltEdge = true;
V.update(STEP);
check("full K spends at plant", V.player.boltPhase==="startup" && V.player.boltSuper===true && V.player.meter===0 && Math.abs(V.player.stamina-70)<0.2, `bph=${V.player.boltPhase} super=${V.player.boltSuper} m=${V.player.meter} st=${V.player.stamina.toFixed(2)}`);
check("full K sting path at plant", V.lastBoltSfx==="super", `sfx=${V.lastBoltSfx}`);
check("full-meter K plant freeze/punch", V.BOLT_SUPER_STOP===V.HITSTOP_BLOCK && V.BOLT_SUPER_STOP===60 && V.BOLT_SUPER_STOP<V.BOLT_STARTUP && V.hitstopLeft===V.BOLT_SUPER_STOP && V.shake===V.BOLT_SUPER_STOP && V.shakeDur===V.BOLT_SUPER_STOP && V.shakeMag===10 && V.shakeDir===V.player.facing, `hs=${V.hitstopLeft} stop=${V.BOLT_SUPER_STOP} shake=${V.shake} mag=${V.shakeMag} dir=${V.shakeDir} plant=${V.BOLT_STARTUP}`);
check("spent plant puff larger", V.brasaFxKind==="cast" && V.brasaFxScale===V.BOLT_SUPER_FX && V.BOLT_SUPER_FX===1.55 && V.brasaFxScale>1 && V.player.boltSuper===true && V.player.meter===0 && V.BOLT_SUPER_DMG===28, `kind=${V.brasaFxKind} scale=${V.brasaFxScale} fx=${V.BOLT_SUPER_FX} m=${V.player.meter}`);
const plantStop = V.hitstopLeft;
drainHitstop();
check("super freeze is a snap not the 200 plant", plantStop===V.HITSTOP_BLOCK && plantStop<V.BOLT_STARTUP && V.hitstopLeft<=0 && V.player.boltPhase==="startup" && !V.bolt, `stop=${plantStop} hs=${V.hitstopLeft} bph=${V.player.boltPhase} bolt=${!!V.bolt}`);
let superStart=STEP;
for(let i=0;i<24;i++){
  V.update(STEP);
  superStart += STEP;
  if(V.player.boltPhase==="recovery" || V.rival.hp<100 || V.hitstopLeft>0) break;
}
check("super startup still ~200", superStart>=180 && superStart<=220 && V.player.boltSuper===true, `fireAt=${superStart} bph=${V.player.boltPhase} super=${V.player.boltSuper} bolt=${!!V.bolt}`);
for(let i=0;i<80;i++){ V.update(STEP); if(V.rival.hp<100 || V.hitstopLeft>0) break; }
check("full K hits harder", V.rival.hp===100-V.BOLT_SUPER_DMG && V.BOLT_SUPER_DMG===28 && V.rival.hp===72, `hp=${V.rival.hp} dmg=${V.BOLT_SUPER_DMG} bolt=${!!V.bolt}`);
check("super still dumps meter", V.player.meter===V.METER_HIT && V.BOLT_SUPER_DMG===28, `m=${V.player.meter} dmg=${V.BOLT_SUPER_DMG}`);
check("spent dart named size/speed not a fat empty", V.BOLT_SUPER_W===96 && V.BOLT_SUPER_H===24 && V.BOLT_SUPER_SPEED===1280 && V.BOLT_W===58 && V.BOLT_H===13 && V.BOLT_SPEED===880 && V.BOLT_SUPER_W>V.BOLT_W && V.BOLT_SUPER_H>V.BOLT_H && V.BOLT_SUPER_SPEED>=V.BOLT_SPEED*1.35 && (V.BOLT_SUPER_W*V.BOLT_SUPER_H)>=(V.BOLT_W*V.BOLT_H)*1.5 && V.BOLT_SUPER_DMG===28 && V.METER_MAX===100, `sw=${V.BOLT_SUPER_W} sh=${V.BOLT_SUPER_H} ss=${V.BOLT_SUPER_SPEED}`);
check("spent dart still one stock no new art", V.METER_MAX===100 && V.METER_HIT*5===V.METER_MAX && V.BOLT_SUPER_DMG===28 && !/loadImg\([^)]*(super|bolt)/.test(srcTxt) && !/art\/super/.test(srcTxt) && !/BOLT_SUPER_DRAW/.test(srcTxt));
check("super dart connect sting path", V.lastBoltConnectSfx==="super-hit", `connect=${V.lastBoltConnectSfx}`);
check("super dart land puff larger", V.brasaFxKind==="hit" && V.brasaFxScale===V.BOLT_SUPER_FX && V.BOLT_SUPER_FX===1.55 && V.brasaFxScale>1, `kind=${V.brasaFxKind} scale=${V.brasaFxScale} fx=${V.BOLT_SUPER_FX}`);
check("super connect refills a hit", V.player.meter===V.METER_HIT && V.player.meter===20 && V.player.meter<V.METER_MAX, `m=${V.player.meter}`);

toMeasure();
Math.random = () => 0.99;
freezeRivalAI();
V.rival.guarding = false;
V.player.x = 180;
V.rival.x = 1100;
V.player.phase = "idle";
V.player.phaseT = 0;
V.player.boltPhase = "";
V.player.meter = 100;
V.player.stamina = 100;
V.player.stamRegenT = 99999;
V.player.stunT = 0;
V.player.guarding = false;
V.boltEdge = true;
V.update(STEP);
for(let i=0;i<20;i++){
  V.rival.x = 1100;
  V.rival.gait = 0;
  V.rival.closing = false;
  V.update(STEP);
  if(V.player.boltPhase==="recovery") break;
}
let sRec=0;
while(V.player.boltPhase==="recovery"){
  V.rival.x = 1100;
  V.rival.gait = 0;
  V.rival.closing = false;
  V.update(STEP);
  if(V.hitstopLeft>0) continue;
  sRec++;
  if(sRec>40) break;
}
check("super recovery a bit more", sRec*STEP>=360 && sRec*STEP<=400 && V.BOLT_SUPER_RECOVERY===380, `rec=${sRec*STEP} bph=${V.player.boltPhase} hp=${V.rival.hp} super=${V.player.boltSuper}`);

toMeasure();
Math.random = () => 0;
freezeRivalAI();
V.rival.stamina = 80;
V.rival.stamRegenT = 99999;
V.rival.guardBreakT = 0;
V.rival.wantBlock = true;
V.rival.guarding = true;
V.rival.guardChoice = 1;
V.player.meter = 100;
V.player.stamina = 100;
V.boltEdge = true;
V.update(STEP);
drainHitstop();
for(let i=0;i<80;i++){ V.rival.wantBlock = true; V.rival.guarding = true; V.update(STEP); if(V.hitstopLeft>0 || V.rival.pushT>0) break; }
check("super block chips more", V.BOLT_SUPER_CHIP===6 && V.rival.hp===100-V.BOLT_SUPER_CHIP && V.rival.hp===94 && V.BOLT_CHIP===2 && V.BOLT_SUPER_CHIP>V.BOLT_CHIP && V.BOLT_SUPER_CHIP<V.BOLT_SUPER_DMG && V.rival.hp>90 && V.rival.pushT>0, `hp=${V.rival.hp} chip=${V.BOLT_SUPER_CHIP} empty=${V.BOLT_CHIP} push=${V.rival.pushT} super=${V.player.boltSuper}`);
check("super block not a wipe", V.rival.hp===94 && V.rival.hp>100-V.BOLT_SUPER_DMG && V.BOLT_SUPER_DMG===28 && !V.rival.falling, `hp=${V.rival.hp} dmg=${V.BOLT_SUPER_DMG}`);
check("super dart block sting path", V.lastBoltConnectSfx==="super-block", `connect=${V.lastBoltConnectSfx}`);
check("super dart block puff larger", V.brasaFxKind==="block" && V.brasaFxScale===V.BOLT_SUPER_FX && V.BOLT_SUPER_FX===1.55 && V.brasaFxScale>1, `kind=${V.brasaFxKind} scale=${V.brasaFxScale}`);

parkRivalSuperRange();
V.player.hp = 100;
V.player.stamina = 80;
V.player.stamRegenT = 99999;
V.player.guardBreakT = 0;
V.player.stunT = 0;
V.player.falling = false;
V.player.phase = "idle";
V.player.phaseT = 0;
V.player.boltPhase = "";
V.keys.add("KeyS");
V.player.guarding = true;
V.update(STEP);
check("rival super plant vs your guard", V.rival.boltPhase==="startup" && V.rival.boltSuper===true && V.rival.meter===0, `bph=${V.rival.boltPhase} super=${V.rival.boltSuper} m=${V.rival.meter}`);
for(let i=0;i<40;i++){
  if(V.hitstopLeft<=0) break;
  V.keys.add("KeyS");
  V.player.guarding = true;
  V.update(STEP);
}
let theySuperChip=false, theySuperAte=false;
for(let i=0;i<80;i++){
  V.keys.add("KeyS");
  V.player.guarding = true;
  V.player.stunT = 0;
  V.player.falling = false;
  V.update(STEP);
  if(V.player.hp<=100-V.SLASH_DMG && V.player.hp!==100-V.BOLT_SUPER_CHIP){ theySuperAte=true; break; }
  if(V.hitstopLeft>0 && V.player.pushT>0 && V.player.hp===100-V.BOLT_SUPER_CHIP){ theySuperChip=true; break; }
}
check("you block their super chips more", theySuperChip && !theySuperAte && V.player.hp===94 && V.player.hp===100-V.BOLT_SUPER_CHIP && V.BOLT_SUPER_CHIP===6 && V.BOLT_CHIP===2 && !V.bolt, `hp=${V.player.hp} chip=${V.BOLT_SUPER_CHIP} push=${V.player.pushT} bolt=${!!V.bolt} ate=${theySuperAte}`);

parkRangeThrow();
V.rival.meter = 100;
V.rival.superArmed = true;
V.rival.stamina = 100;
V.update(STEP);
check("rival full bar fullscreen is normal dart", V.rival.boltPhase==="startup" && V.rival.boltSuper===false && V.rival.meter===100 && V.absGap()>V.SUPER_RANGE, `bph=${V.rival.boltPhase} super=${V.rival.boltSuper} m=${V.rival.meter} gap=${V.absGap().toFixed(1)}`);
check("rival fullscreen empty sting", V.lastBoltSfx==="cast", `sfx=${V.lastBoltSfx} super=${V.rival.boltSuper}`);
check("rival empty K has no extra freeze", V.hitstopLeft===0 && V.shake===0 && V.rival.boltSuper===false, `hs=${V.hitstopLeft} shake=${V.shake} super=${V.rival.boltSuper}`);
check("rival empty plant puff unchanged", V.brasaFxKind==="cast" && (V.brasaFxScale||1)===1 && V.rival.boltSuper===false, `kind=${V.brasaFxKind} scale=${V.brasaFxScale} super=${V.rival.boltSuper}`);

function parkRivalSuperRange(){
  toMeasure();
  Math.random = () => 0.99;
  freezeYou();
  freezeYouAI();
  V.player.guarding = false;
  V.player.hp = 100;
  V.player.meter = 0;
  V.rival.phase = "idle";
  V.rival.phaseT = 0;
  V.rival.cut = "slash";
  V.rival.boltPhase = "";
  V.rival.boltT = 0;
  V.rival.boltSuper = false;
  V.rival.boltCd = 0;
  V.rival.boltArmed = true;
  V.rival.meter = 100;
  V.rival.superArmed = true;
  V.rival.stamina = 100;
  V.rival.stamRegenT = 99999;
  V.rival.guarding = false;
  V.rival.wantBlock = false;
  V.rival.guardChoice = -1;
  V.rival.closing = false;
  V.rival.standWait = 0;
  V.rival.standGoal = 99999;
  V.rival.stunT = 0;
  V.rival.falling = false;
  V.rival.guardBreakT = 0;
  const hold = V.bladeReach(V.rival)+V.LUNGE_PX;
  const want = hold + 40;
  V.player.x = 360;
  V.rival.x = 820;
  for(let n=0;n<12;n++){
    const g = V.absGap();
    V.rival.x -= (g - want);
  }
  return want;
}
const wantGap = parkRivalSuperRange();
const gSuper = V.absGap();
V.update(STEP);
check("rival super when it can hit", V.rival.boltPhase==="startup" && V.rival.boltSuper===true && V.rival.meter===0 && gSuper>V.bladeReach(V.rival)+V.LUNGE_PX+16 && gSuper<=V.SUPER_RANGE, `bph=${V.rival.boltPhase} super=${V.rival.boltSuper} m=${V.rival.meter} gap=${gSuper.toFixed(1)} want=${wantGap.toFixed(1)} cd=${V.rival.boltCd}`);
check("rival super sting same path", V.lastBoltSfx==="super", `sfx=${V.lastBoltSfx}`);
check("rival super freeze/punch same snap", V.hitstopLeft===V.BOLT_SUPER_STOP && V.shake===V.BOLT_SUPER_STOP && V.shakeMag===10 && V.shakeDir===V.rival.facing, `hs=${V.hitstopLeft} shake=${V.shake} mag=${V.shakeMag} dir=${V.shakeDir}`);
check("rival spent plant puff larger", V.brasaFxKind==="cast" && V.brasaFxScale===V.BOLT_SUPER_FX && V.BOLT_SUPER_FX===1.55 && V.brasaFxScale>1 && V.rival.boltSuper===true && V.rival.meter===0, `kind=${V.brasaFxKind} scale=${V.brasaFxScale} fx=${V.BOLT_SUPER_FX} m=${V.rival.meter}`);
check("rival super still costs 30 + CD", Math.abs(V.rival.stamina-70)<0.2 && V.rival.boltCd===V.BOLT_AI_CD, `st=${V.rival.stamina.toFixed(2)} cd=${V.rival.boltCd}`);
const cdSuper = V.rival.boltCd;
drainHitstop();
for(let i=0;i<80;i++){
  freezeYou();
  freezeYouAI();
  V.player.guarding = false;
  V.update(STEP);
  if(V.player.hp<100 || V.hitstopLeft>0) break;
}
check("rival super hits harder", V.player.hp===100-V.BOLT_SUPER_DMG && V.player.hp===72, `hp=${V.player.hp} dmg=${V.BOLT_SUPER_DMG} bolt=${!!V.bolt}`);
check("rival super connect sting path", V.lastBoltConnectSfx==="super-hit", `connect=${V.lastBoltConnectSfx}`);
check("rival super land puff larger", V.brasaFxKind==="hit" && V.brasaFxScale===V.BOLT_SUPER_FX && V.brasaFxScale>1, `kind=${V.brasaFxKind} scale=${V.brasaFxScale}`);
V.rival.meter = 100;
V.rival.superArmed = true;
V.rival.stamina = 100;
V.rival.boltArmed = true;
const bphAfter = V.rival.boltPhase;
V.update(STEP);
check("rival super does not spam", V.rival.boltCd>0 && V.rival.boltCd<=cdSuper && !(V.rival.boltPhase==="startup" && V.rival.meter===0 && bphAfter!=="startup"), `cd=${V.rival.boltCd} bph=${V.rival.boltPhase} m=${V.rival.meter} after=${bphAfter}`);

const stingFn = sliceFn(codeOnly, "playSuperSting");
const startBoltNow = sliceFn(codeOnly, "startBolt");
check("super sting pitches brasa/cast not whoosh",
  /function playSuperSting/.test(srcTxt) &&
  /playbackRate/.test(srcTxt) &&
  /SFX\.cast/.test(stingFn) &&
  /SFX\.brasaImpacto/.test(stingFn) &&
  !/SFX\.whoosh/.test(stingFn) &&
  /playSuperSting\(\)/.test(startBoltNow) &&
  /playSfx\(SFX\.cast\)/.test(startBoltNow)
);
check("super sting only on spend",
  /if \(wantSuper\)/.test(startBoltNow) &&
  startBoltNow.indexOf("playSuperSting()") > startBoltNow.indexOf("if (wantSuper)") &&
  startBoltNow.indexOf("lastBoltSfx = \"cast\"") > startBoltNow.indexOf("else")
);
const spendBranch = startBoltNow.slice(startBoltNow.indexOf("if (wantSuper)"));
const spendElse = spendBranch.indexOf("else");
check("super spend freeze reuses hitstop and camera punch",
  /bumpShake\(10, u\.facing, BOLT_SUPER_STOP\)/.test(spendBranch) &&
  /hitstopLeft = BOLT_SUPER_STOP/.test(spendBranch) &&
  spendBranch.indexOf("bumpShake(10, u.facing, BOLT_SUPER_STOP)") > 0 &&
  spendBranch.indexOf("bumpShake(10, u.facing, BOLT_SUPER_STOP)") < spendElse &&
  spendBranch.indexOf("hitstopLeft = BOLT_SUPER_STOP") < spendElse &&
  !/bumpShake/.test(spendBranch.slice(spendElse)) &&
  V.BOLT_SUPER_STOP===V.HITSTOP_BLOCK && V.BOLT_SUPER_STOP<V.BOLT_STARTUP
);
check("fill rates unchanged", V.METER_HIT===20 && V.METER_BLOCK_SPECIAL===10 && V.BOLT_SUPER_DMG===28);
check("spend still -28 dumps meter", V.BOLT_SUPER_DMG===28 && V.BOLT_SUPER_DMG<=28 && /u\.meter = 0/.test(startBoltNow));
check("no new super audio pipeline", !/sfx_super/.test(srcTxt) && !/AudioContext/.test(srcTxt) && !/new Audio\([^)]*super/.test(srcTxt));
const hitStingFn = sliceFn(codeOnly, "playBoltHitSting");
const blockStingFn = sliceFn(codeOnly, "playBoltBlockSting");
check("super hit sting pitches layered brasa impacto",
  /function playBoltHitSting/.test(srcTxt) &&
  /SFX\.brasaImpacto/.test(hitStingFn) &&
  /rate:/.test(hitStingFn) &&
  /playSfx\(SFX\.brasaImpacto\)/.test(hitStingFn) &&
  /lastBoltConnectSfx = "super-hit"/.test(hitStingFn) &&
  /lastBoltConnectSfx = "impacto"/.test(hitStingFn) &&
  !/SFX\.whoosh/.test(hitStingFn)
);
check("super block sting pitches layered brasa bloqueo",
  /function playBoltBlockSting/.test(srcTxt) &&
  /SFX\.brasaBloqueo/.test(blockStingFn) &&
  /rate:/.test(blockStingFn) &&
  /playSfx\(SFX\.brasaBloqueo\)/.test(blockStingFn) &&
  /lastBoltConnectSfx = "super-block"/.test(blockStingFn) &&
  /lastBoltConnectSfx = "bloqueo"/.test(blockStingFn) &&
  !/SFX\.whoosh/.test(blockStingFn)
);
check("super-hit path only from spent dart connect",
  /playBoltHitSting\(\!!\(bolt && bolt\.super\)\)/.test(lbh) &&
  /playBoltBlockSting\(\!!\(bolt && bolt\.super\)\)/.test(lbb) &&
  !/playBoltHitSting/.test(startBoltNow) &&
  !/playBoltBlockSting/.test(startBoltNow)
);
function parkSteelLane(){
  toMeasure();
  Math.random = () => 0.99;
  freezeRivalAI();
  freezeYouAI();
  V.player.x = 180;
  V.rival.x = 1100;
  V.player.hp = 100;
  V.rival.hp = 100;
  V.player.stamina = 100;
  V.rival.stamina = 100;
  V.player.stamRegenT = 99999;
  V.rival.stamRegenT = 99999;
  V.player.guarding = false;
  V.rival.guarding = false;
  V.player.meter = 0;
  V.rival.meter = 0;
  V.player.boltSuper = false;
  V.rival.boltSuper = false;
  V.rival.guardChoice = -1;
  V.rival.boltCd = 99999;
  V.rival.throwCd = 99999;
  V.rival.boltArmed = false;
}
function holdSteelLane(){
  V.player.x = 180;
  V.rival.x = 1100;
  V.rival.wantBlock = false;
  V.rival.guarding = false;
  V.rival.closing = false;
  V.rival.standGoal = 99999;
  V.rival.boltArmed = false;
  V.rival.guardChoice = -1;
  V.player.guarding = false;
  V.player.stunT = 0;
  V.rival.stunT = 0;
}
function waitLiveBolt(){
  for(let i=0;i<30;i++){
    holdSteelLane();
    V.update(STEP);
    if(V.bolt) return true;
  }
  return false;
}
function clashLiveBoltVs(def, cut){
  const atk = def===V.rival ? V.player : V.rival;
  holdSteelLane();
  def.phase = "active";
  def.phaseT = 16;
  const kind = cut || "slash";
  def.cut = kind;
  def.guarding = false;
  def.falling = false;
  def.stunT = 0;
  def.wantBlock = false;
  def.guardChoice = -1;
  atk.phase = "idle";
  atk.phaseT = 0;
  atk.guarding = false;
  atk.stunT = 0;
  atk.wantBlock = false;
  const hb = V.hitbox(def);
  const b = V.bolt;
  if(!hb || !b) return {ok:false, hb:!!hb, bolt:!!b};
  // Pin on the tip, away from the body, so resolveCuts cannot eat the steel first.
  if(def.facing > 0) b.x = hb.x + hb.w - b.w - 6;
  else b.x = hb.x + 6;
  b.y = hb.y + hb.h * 0.45 - b.h * 0.5;
  holdSteelLane();
  def.phase = "active";
  def.phaseT = 16;
  def.cut = kind;
  def.guarding = false;
  V.rival.guardChoice = -1;
  V.rival.wantBlock = false;
  V.rival.closing = false;
  V.update(STEP);
  return {ok:true, php:V.player.hp, rhp:V.rival.hp, fx:V.brasaFxKind, sfx:V.lastBoltConnectSfx, bolt:!!V.bolt, scale:V.brasaFxScale, stun:def.stunT, phase:def.phase};
}
function guardLiveBoltVs(def){
  holdSteelLane();
  def.phase = "idle";
  def.phaseT = 0;
  def.cut = "slash";
  def.guarding = true;
  def.wantBlock = true;
  def.falling = false;
  def.stunT = 0;
  def.guardChoice = 1;
  const atk = def===V.rival ? V.player : V.rival;
  atk.phase = "idle";
  atk.phaseT = 0;
  atk.guarding = false;
  atk.stunT = 0;
  atk.wantBlock = false;
  const bb = V.bodyAABB(def);
  const b = V.bolt;
  if(!bb || !b) return {ok:false, bb:!!bb, bolt:!!b};
  b.x = bb.x + (def.facing < 0 ? 8 : bb.w - b.w - 8);
  b.y = bb.y + bb.h * 0.45 - b.h * 0.5;
  holdSteelLane();
  def.phase = "idle";
  def.phaseT = 0;
  def.guarding = true;
  def.wantBlock = true;
  def.guardChoice = 1;
  V.rival.closing = false;
  V.update(STEP);
  return {ok:true, php:V.player.hp, rhp:V.rival.hp, fx:V.brasaFxKind, sfx:V.lastBoltConnectSfx, bolt:!!V.bolt, scale:V.brasaFxScale};
}


parkSteelLane();
V.player.meter = 0;
V.player.stamina = 100;
V.boltEdge = true;
V.update(STEP);
const emptyGot = waitLiveBolt();
const emptyWasSuper = !!(V.bolt && V.bolt.super);
const emptyClash = clashLiveBoltVs(V.rival);
check("empty dart clash path unchanged", emptyGot && !emptyWasSuper && emptyClash.ok && emptyClash.sfx==="choque" && emptyClash.fx==="clash" && emptyClash.rhp===100 && emptyClash.php===100 && V.rival.hp===100 && V.player.hp===100 && !V.bolt && emptyClash.sfx!=="super-clash", `got=${emptyGot} superDart=${emptyWasSuper} ok=${emptyClash.ok} sfx=${emptyClash.sfx} fx=${emptyClash.fx} hp=${V.player.hp}/${V.rival.hp} bolt=${!!V.bolt}`);
check("empty clash puff scale unchanged", emptyClash.ok && emptyClash.fx==="clash" && (emptyClash.scale||1)===1, `fx=${emptyClash.fx} scale=${emptyClash.scale}`);
check("empty clash does not dump or fill meter", V.player.meter===0 && V.BOLT_SUPER_DMG===28, `m=${V.player.meter} dmg=${V.BOLT_SUPER_DMG}`);

parkSteelLane();
V.player.meter = 100;
V.player.stamina = 100;
V.boltEdge = true;
V.update(STEP);
check("super plant still dumps meter before clash", V.player.boltPhase==="startup" && V.player.boltSuper===true && V.player.meter===0 && V.lastBoltSfx==="super", `bph=${V.player.boltPhase} super=${V.player.boltSuper} m=${V.player.meter} sfx=${V.lastBoltSfx}`);
const superGot = waitLiveBolt();
const superWasSuper = !!(V.bolt && V.bolt.super);
const superMeterAtDart = V.player.meter;
const superClash = clashLiveBoltVs(V.rival);
check("spent super vs active slash is not a 0-delete", superGot && superWasSuper && superClash.ok && superClash.sfx==="super-hit" && superClash.fx==="hit" && V.rival.hp===100-V.BOLT_SUPER_DMG && V.rival.hp===72 && V.player.hp===100 && !V.bolt && V.rival.phase!=="active" && V.rival.stunT>0, `got=${superGot} superDart=${superWasSuper} ok=${superClash.ok} sfx=${superClash.sfx} fx=${superClash.fx} hp=${V.player.hp}/${V.rival.hp} bolt=${!!V.bolt} ph=${V.rival.phase} stun=${V.rival.stunT}`);
check("super vs steel uses hit puff not clash", superClash.ok && superClash.fx==="hit" && superClash.sfx==="super-hit" && superClash.scale===V.BOLT_SUPER_FX && superClash.scale>1, `fx=${superClash.fx} sfx=${superClash.sfx} scale=${superClash.scale}`);
check("hit still -28 after steel", V.player.meter===V.METER_HIT && superMeterAtDart===0 && V.BOLT_SUPER_DMG===28 && V.rival.hp===72 && V.SLASH_DMG===10, `m=${V.player.meter} dartM=${superMeterAtDart} dmg=${V.BOLT_SUPER_DMG} rhp=${V.rival.hp}`);

parkSteelLane();
V.rival.meter = 100;
V.rival.superArmed = true;
V.rival.stamina = 100;
V.rival.boltCd = 0;
const rivalPlant = V.startBolt(V.rival, true);
check("rival super plant for clash", rivalPlant && V.rival.boltPhase==="startup" && V.rival.boltSuper===true && V.rival.meter===0 && V.lastBoltSfx==="super", `ok=${rivalPlant} bph=${V.rival.boltPhase} super=${V.rival.boltSuper} m=${V.rival.meter} sfx=${V.lastBoltSfx}`);
const rivalGot = waitLiveBolt();
const rivalWasSuper = !!(V.bolt && V.bolt.super);
const rivalClash = clashLiveBoltVs(V.player);
check("rival spent super vs active slash is not a 0-delete", rivalGot && rivalWasSuper && rivalClash.ok && rivalClash.sfx==="super-hit" && rivalClash.fx==="hit" && V.player.hp===100-V.BOLT_SUPER_DMG && V.player.hp===72 && V.rival.hp===100 && !V.bolt && V.player.phase!=="active" && V.player.stunT>0, `got=${rivalGot} superDart=${rivalWasSuper} ok=${rivalClash.ok} sfx=${rivalClash.sfx} fx=${rivalClash.fx} hp=${V.player.hp}/${V.rival.hp} bolt=${!!V.bolt} ph=${V.player.phase} stun=${V.player.stunT}`);
check("rival super vs steel uses hit puff", rivalClash.ok && rivalClash.fx==="hit" && rivalClash.sfx==="super-hit" && rivalClash.scale===V.BOLT_SUPER_FX && rivalClash.scale>1, `fx=${rivalClash.fx} sfx=${rivalClash.sfx} scale=${rivalClash.scale}`);

parkSteelLane();
V.player.meter = 100;
V.player.stamina = 100;
V.boltEdge = true;
V.update(STEP);
const golpeGot = waitLiveBolt();
const golpeWasSuper = !!(V.bolt && V.bolt.super);
const golpeClash = clashLiveBoltVs(V.rival, "golpe");
check("spent super vs active golpe is not a 0-delete", golpeGot && golpeWasSuper && golpeClash.ok && golpeClash.sfx==="super-hit" && V.rival.hp===100-V.BOLT_SUPER_DMG && V.rival.hp===72 && V.player.hp===100 && !V.bolt && V.rival.phase!=="active", `got=${golpeGot} superDart=${golpeWasSuper} ok=${golpeClash.ok} sfx=${golpeClash.sfx} hp=${V.player.hp}/${V.rival.hp} bolt=${!!V.bolt} ph=${V.rival.phase}`);

parkSteelLane();
V.player.meter = 100;
V.player.stamina = 100;
V.boltEdge = true;
V.update(STEP);
const guardGot = waitLiveBolt();
const guardWasSuper = !!(V.bolt && V.bolt.super);
const guardHit = guardLiveBoltVs(V.rival);
check("guard still chips -6 vs spent super", guardGot && guardWasSuper && guardHit.ok && guardHit.sfx==="super-block" && guardHit.fx==="block" && V.BOLT_SUPER_CHIP===6 && V.rival.hp===100-V.BOLT_SUPER_CHIP && V.rival.hp===94 && V.player.hp===100 && !V.bolt, `got=${guardGot} superDart=${guardWasSuper} ok=${guardHit.ok} sfx=${guardHit.sfx} fx=${guardHit.fx} hp=${V.player.hp}/${V.rival.hp} bolt=${!!V.bolt}`);
check("hit still -28 chip still -6", V.BOLT_SUPER_DMG===28 && V.BOLT_SUPER_CHIP===6 && V.BOLT_SUPER_DMG<=28 && V.BOLT_SUPER_CHIP<=6);

function boxesOverlap(a,b){
  return !!(a && b && a.x < b.x+b.w && a.x+a.w > b.x && a.y < b.y+b.h && a.y+a.h > b.y);
}
function meatyLiveBoltVs(def, cut){
  const atk = def===V.rival ? V.player : V.rival;
  freezeRivalAI();
  freezeYouAI();
  V.player.hp = 100;
  V.rival.hp = 100;
  V.player.guarding = false;
  V.rival.guarding = false;
  V.player.stunT = 0;
  V.rival.stunT = 0;
  V.player.falling = false;
  V.rival.falling = false;
  V.player.pushT = 0;
  V.rival.pushT = 0;
  V.player.x = 400;
  V.rival.x = 700;
  def.phase = "active";
  def.phaseT = 16;
  def.cut = cut || "slash";
  def.wantBlock = false;
  def.guardChoice = -1;
  def.closing = false;
  def.guarding = false;
  atk.phase = "idle";
  atk.phaseT = 0;
  atk.wantBlock = false;
  atk.guarding = false;
  V.keepApart();
  def.phase = "active";
  def.phaseT = 16;
  def.cut = cut || "slash";
  def.guarding = false;
  const hb = V.hitbox(def);
  const bb = V.bodyAABB(def);
  const ba = V.bodyAABB(atk);
  const b = V.bolt;
  const meaty = !!(hb && boxesOverlap(hb, ba));
  if(!hb || !b || !meaty) return {ok:false, hb:!!hb, bolt:!!b, meaty, gap:V.bodyGap()};
  // Pin on the slasher body so the meaty cut and the dart share the frame.
  // Place on the incoming face so one tick of travel stays inside the AABB.
  if(b.vx >= 0) b.x = bb.x + 4;
  else b.x = bb.x + bb.w - b.w - 4;
  b.y = bb.y + bb.h * 0.45 - b.h * 0.5;
  const dartOn = boxesOverlap({x:b.x,y:b.y,w:b.w,h:b.h}, bb) || boxesOverlap({x:b.x,y:b.y,w:b.w,h:b.h}, hb);
  V.rival.guardChoice = -1;
  V.rival.wantBlock = false;
  V.rival.closing = false;
  V.rival.boltArmed = false;
  V.update(STEP);
  return {ok:true, php:V.player.hp, rhp:V.rival.hp, fx:V.brasaFxKind, sfx:V.lastBoltConnectSfx, bolt:!!V.bolt, stun:def.stunT, phase:def.phase, dartOn, meaty, atkHp:atk.hp, defHp:def.hp, gap:V.bodyGap()};
}

parkSteelLane();
V.player.meter = 100;
V.player.stamina = 100;
V.boltEdge = true;
V.update(STEP);
const meatyGot = waitLiveBolt();
const meatyWasSuper = !!(V.bolt && V.bolt.super);
const meatyHit = meatyLiveBoltVs(V.rival);
check("spent super vs same-frame slash is not a trade-delete", meatyGot && meatyWasSuper && meatyHit.ok && meatyHit.meaty && meatyHit.dartOn && meatyHit.sfx==="super-hit" && V.rival.hp===100-V.BOLT_SUPER_DMG && V.rival.hp===72 && V.player.hp===100 && !V.bolt && V.rival.phase!=="active" && V.rival.stunT>0 && V.player.hp>100-V.SLASH_DMG, `got=${meatyGot} superDart=${meatyWasSuper} ok=${meatyHit.ok} meaty=${meatyHit.meaty} dartOn=${meatyHit.dartOn} sfx=${meatyHit.sfx} hp=${V.player.hp}/${V.rival.hp} bolt=${!!V.bolt} ph=${V.rival.phase} stun=${V.rival.stunT} gap=${meatyHit.gap}`);

parkSteelLane();
V.rival.meter = 100;
V.rival.superArmed = true;
V.rival.stamina = 100;
V.rival.boltCd = 0;
const meatyRivalPlant = V.startBolt(V.rival, true);
const meatyRivalGot = waitLiveBolt();
const meatyRivalWasSuper = !!(V.bolt && V.bolt.super);
const meatyRivalHit = meatyLiveBoltVs(V.player);
check("rival spent super vs same-frame slash is not a trade-delete", meatyRivalPlant && meatyRivalGot && meatyRivalWasSuper && meatyRivalHit.ok && meatyRivalHit.meaty && meatyRivalHit.dartOn && meatyRivalHit.sfx==="super-hit" && V.player.hp===100-V.BOLT_SUPER_DMG && V.player.hp===72 && V.rival.hp===100 && !V.bolt && V.player.phase!=="active" && V.player.stunT>0, `plant=${meatyRivalPlant} got=${meatyRivalGot} superDart=${meatyRivalWasSuper} ok=${meatyRivalHit.ok} meaty=${meatyRivalHit.meaty} dartOn=${meatyRivalHit.dartOn} sfx=${meatyRivalHit.sfx} hp=${V.player.hp}/${V.rival.hp} bolt=${!!V.bolt} ph=${V.player.phase} stun=${V.player.stunT}`);

parkSteelLane();
V.player.meter = 0;
V.player.stamina = 100;
V.boltEdge = true;
V.update(STEP);
const emptyMeatyGot = waitLiveBolt();
const emptyMeatyWasSuper = !!(V.bolt && V.bolt.super);
const emptyMeatyClash = clashLiveBoltVs(V.rival);
check("empty dart clash still 0", emptyMeatyGot && !emptyMeatyWasSuper && emptyMeatyClash.ok && emptyMeatyClash.sfx==="choque" && emptyMeatyClash.fx==="clash" && emptyMeatyClash.rhp===100 && emptyMeatyClash.php===100 && V.rival.hp===100 && V.player.hp===100 && !V.bolt, `got=${emptyMeatyGot} superDart=${emptyMeatyWasSuper} ok=${emptyMeatyClash.ok} sfx=${emptyMeatyClash.sfx} fx=${emptyMeatyClash.fx} hp=${V.player.hp}/${V.rival.hp} bolt=${!!V.bolt}`);

parkSteelLane();
V.player.meter = 100;
V.player.stamina = 100;
V.boltEdge = true;
V.update(STEP);
const guardMeatyGot = waitLiveBolt();
const guardMeatyWasSuper = !!(V.bolt && V.bolt.super);
const guardMeatyHit = guardLiveBoltVs(V.rival);
check("guard still chips -6", guardMeatyGot && guardMeatyWasSuper && guardMeatyHit.ok && guardMeatyHit.sfx==="super-block" && guardMeatyHit.fx==="block" && V.BOLT_SUPER_CHIP===6 && V.rival.hp===100-V.BOLT_SUPER_CHIP && V.rival.hp===94 && V.player.hp===100 && !V.bolt, `got=${guardMeatyGot} superDart=${guardMeatyWasSuper} ok=${guardMeatyHit.ok} sfx=${guardMeatyHit.sfx} fx=${guardMeatyHit.fx} hp=${V.player.hp}/${V.rival.hp} bolt=${!!V.bolt}`);

const clashStingFn = sliceFn(codeOnly, "playBoltClashSting");
check("super clash sting pitches layered brasa choque",
  /function playBoltClashSting/.test(srcTxt) &&
  /SFX\.brasaChoque/.test(clashStingFn) &&
  /rate:/.test(clashStingFn) &&
  /playSfx\(SFX\.brasaChoque\)/.test(clashStingFn) &&
  /lastBoltConnectSfx = "super-clash"/.test(clashStingFn) &&
  /lastBoltConnectSfx = "choque"/.test(clashStingFn) &&
  !/SFX\.whoosh/.test(clashStingFn) &&
  !/SFX\.impacto/.test(clashStingFn) &&
  !/SFX\.bloqueo/.test(clashStingFn)
);
check("empty clash sting is unpitched choque",
  clashStingFn.indexOf('lastBoltConnectSfx = "choque"') > clashStingFn.indexOf("else") &&
  clashStingFn.indexOf("rate:") < clashStingFn.indexOf("else") &&
  /playSfx\(SFX\.brasaChoque\)/.test(clashStingFn)
);
check("empty dart still clashes with slash source",
  /playBoltClashSting\(\!!\(bolt && bolt\.super\)\)/.test(rb) &&
  !/playBoltClashSting/.test(lbh) &&
  !/playBoltClashSting/.test(lbb) &&
  !/playBoltClashSting/.test(startBoltNow) &&
  !/playBoltHitSting/.test(rb) &&
  !/playBoltBlockSting/.test(rb) &&
  (srcTxt.match(/lastBoltConnectSfx = "super-clash"/g)||[]).length===1 &&
  !/playBoltClashSting/.test(hitStingFn) &&
  !/playBoltClashSting/.test(blockStingFn)
);
check("empty clash vs steel no longer shares empty choque call", /playBoltClashSting/.test(rb) && !/playSfx\(SFX\.brasaChoque\)/.test(rb));
check("spent super vs steel uses landBoltHit not 0-clash",
  /if \(bolt\.super\)/.test(rb) &&
  /landBoltHit/.test(rb) &&
  !/playBoltHitSting/.test(rb) &&
  !/playBoltBlockSting/.test(rb)
);
const rc = sliceFn(codeOnly, "resolveCuts");
const sbc = sliceFn(codeOnly, "spentSuperBeatsCut");
check("spent super same-frame slash is not a trade-delete source",
  /function spentSuperBeatsCut/.test(srcTxt) &&
  /spentSuperBeatsCut\(player\)/.test(rc) &&
  /spentSuperBeatsCut\(rival\)/.test(rc) &&
  /bolt\.super/.test(sbc) &&
  /cut === "throw"/.test(sbc) &&
  /bolt\.kind === atk\.kind/.test(sbc)
);
check("empty dart still resolves after cuts",
  /resolveCuts\(\);\s*resolveBolt\(\);/.test(codeOnly)
);

check("empty dart speed still 880", V.BOLT_SPEED===880);
check("super projectile faster not a range cheat", V.BOLT_SUPER_SPEED===1280 && V.BOLT_SUPER_SPEED>V.BOLT_SPEED && V.BOLT_SUPER_SPEED>=V.BOLT_SPEED*1.35 && V.BOLT_SUPER_SPEED<1600);
check("super dart named size", V.BOLT_SUPER_W===96 && V.BOLT_SUPER_H===24 && V.BOLT_SUPER_W>V.BOLT_W && V.BOLT_SUPER_H>V.BOLT_H && (V.BOLT_SUPER_W*V.BOLT_SUPER_H)>=(V.BOLT_W*V.BOLT_H)*1.5);
check("empty dart size constants", V.BOLT_W===58 && V.BOLT_H===13);
check("SUPER_RANGE still 380", V.SUPER_RANGE===380);
check("BOLT_AI_CD still 1800", V.BOLT_AI_CD===1800);
check("super still -28 dumps meter locked", V.BOLT_SUPER_DMG===28 && V.BOLT_SUPER_DMG<=28);
check("super land puff named 1.55 reuse", V.BOLT_SUPER_FX===1.55 && /BOLT_SUPER_FX/.test(srcTxt) && /brasaFxScale/.test(srcTxt) && /boltLandFxScale/.test(srcTxt));
const spawnBrasaNow = sliceFn(codeOnly, "spawnBrasaFx");
const drawBrasaNow = sliceFn(codeOnly, "drawBrasaFx");
const landFxNow = sliceFn(codeOnly, "boltLandFxScale");
check("super land puff reuses brasa scale",
  /function boltLandFxScale/.test(srcTxt) &&
  /BOLT_SUPER_FX/.test(landFxNow) &&
  /bolt\.super/.test(landFxNow) &&
  /brasaFxScale = scale > 0 \? scale : 1/.test(spawnBrasaNow) &&
  /ctx\.scale\(brasaFxScale \|\| 1, brasaFxScale \|\| 1\)/.test(drawBrasaNow) &&
  /boltLandFxScale\(\)/.test(lbh) &&
  /boltLandFxScale\(\)/.test(lbb) &&
  /boltLandFxScale\(\)/.test(rb) &&
  /spawnBrasaFx\("hit"/.test(lbh) &&
  /spawnBrasaFx\("block"/.test(lbb) &&
  /spawnBrasaFx\("clash"/.test(rb)
);
check("cast puff path scales spent only",
  /spawnBrasaFx\("cast"/.test(startBoltSrc) &&
  /wantSuper \? BOLT_SUPER_FX : 1/.test(startBoltSrc) &&
  /BOLT_SUPER_FX/.test(startBoltSrc) &&
  !/boltLandFxScale/.test(startBoltSrc)
);
check("super still -28 dumps meter plant puff pass", V.BOLT_SUPER_DMG===28 && V.BOLT_SUPER_DMG<=28 && /u\.meter = 0/.test(startBoltNow));
check("dart size/speed not rescaled this pass",
  V.BOLT_SUPER_W===96 && V.BOLT_SUPER_H===24 && V.BOLT_SUPER_SPEED===1280 &&
  V.BOLT_W===58 && V.BOLT_H===13 && V.BOLT_SPEED===880 && V.BOLT_SUPER_DMG===28
);

const spawnBoltNow = sliceFn(codeOnly, "spawnBolt");
const drawBoltNow = sliceFn(codeOnly, "drawBolt");
check("empty dart spawn uses current speed/size",
  /superOn \? BOLT_SUPER_SPEED : BOLT_SPEED/.test(spawnBoltNow) &&
  /superOn \? BOLT_SUPER_W : BOLT_W/.test(spawnBoltNow) &&
  /superOn \? BOLT_SUPER_H : BOLT_H/.test(spawnBoltNow) &&
  !/BOLT_W \+ 12/.test(spawnBoltNow) &&
  !/BOLT_H \+ 4/.test(spawnBoltNow)
);
check("source uses named super size/speed not pad",
  /BOLT_SUPER_W/.test(spawnBoltNow) &&
  /BOLT_SUPER_H/.test(spawnBoltNow) &&
  /BOLT_SUPER_SPEED/.test(spawnBoltNow) &&
  !/BOLT_W \+ 12/.test(spawnBoltNow) &&
  !/BOLT_H \+ 4/.test(spawnBoltNow) &&
  !/BOLT_SUPER_DRAW/.test(srcTxt)
);
check("drawBolt scales from live w/h",
  /const hw = bolt\.w \* 0\.5/.test(drawBoltNow) &&
  /const hh = bolt\.h \* 0\.5/.test(drawBoltNow) &&
  !/ctx\.scale\(BOLT_SUPER_DRAW/.test(drawBoltNow) &&
  !/spawnBrasaFx/.test(drawBoltNow)
);
check("super trail hotter brasa",
  drawBoltNow.indexOf("if (bolt.super)") < drawBoltNow.indexOf("COL_OXIDO") &&
  /COL_BRASA/.test(drawBoltNow) &&
  /COL_HUESO/.test(drawBoltNow) &&
  (drawBoltNow.match(/fillRect/g)||[]).length>=3 &&
  (drawBoltNow.match(/COL_BRASA/g)||[]).length>=2
);

function fireDartBirth(wantSuper){
  toMeasure();
  Math.random = () => 0.99;
  freezeRivalAI();
  V.player.x = 180;
  V.rival.x = 1100;
  V.player.phase = "idle";
  V.player.phaseT = 0;
  V.player.boltPhase = "";
  V.player.stunT = 0;
  V.player.guarding = false;
  V.player.falling = false;
  V.player.stamina = 100;
  V.player.stamRegenT = 99999;
  V.player.meter = wantSuper ? 100 : 0;
  V.player.boltSuper = false;
  V.boltEdge = true;
  V.update(STEP);
  if(wantSuper) drainHitstop();
  let born=null;
  for(let i=0;i<24;i++){
    V.rival.x = 1100;
    V.rival.gait = 0;
    V.rival.closing = false;
    const tipBefore = V.bladeTipX(V.player);
    const had = !!V.bolt;
    V.update(STEP);
    if(!had && V.bolt){
      born = { w:V.bolt.w, h:V.bolt.h, vx:V.bolt.vx, super:!!V.bolt.super, kind:V.bolt.kind, x:V.bolt.x, facing:V.bolt.facing, tip:tipBefore };
      break;
    }
  }
  return born;
}
const emptyBorn = fireDartBirth(false);
check("empty live dart size/speed",
  emptyBorn && emptyBorn.super===false && emptyBorn.w===V.BOLT_W && emptyBorn.h===V.BOLT_H && Math.abs(emptyBorn.vx)===V.BOLT_SPEED && emptyBorn.kind==="you",
  `born=${JSON.stringify(emptyBorn)} spd=${V.BOLT_SPEED} w=${V.BOLT_W}`
);
check("empty dart draw/speed unchanged in flight",
  emptyBorn && emptyBorn.super===false && emptyBorn.w===V.BOLT_W && emptyBorn.h===V.BOLT_H && Math.abs(emptyBorn.vx)===V.BOLT_SPEED && emptyBorn.kind==="you",
  `born=${JSON.stringify(emptyBorn)} spd=${V.BOLT_SPEED} w=${V.BOLT_W}`
);
const superBorn = fireDartBirth(true);
check("spent live dart size/speed",
  superBorn && superBorn.super===true && superBorn.w===V.BOLT_SUPER_W && superBorn.h===V.BOLT_SUPER_H && Math.abs(superBorn.vx)===V.BOLT_SUPER_SPEED && Math.abs(superBorn.vx)>V.BOLT_SPEED && superBorn.w>V.BOLT_W && superBorn.h>V.BOLT_H && V.player.meter===0 && V.BOLT_SUPER_DMG===28 && V.METER_MAX===100,
  `born=${JSON.stringify(superBorn)} m=${V.player.meter}`
);
check("super projectile larger-or-faster no range cheat",
  superBorn && superBorn.super===true && Math.abs(superBorn.vx)===V.BOLT_SUPER_SPEED && Math.abs(superBorn.vx)>V.BOLT_SPEED && superBorn.w===V.BOLT_SUPER_W && superBorn.h===V.BOLT_SUPER_H && superBorn.w>emptyBorn.w && superBorn.h>emptyBorn.h && Math.abs(superBorn.vx)>Math.abs(emptyBorn.vx) && V.player.meter===0 && V.BOLT_SUPER_DMG===28,
  `born=${JSON.stringify(superBorn)} empty=${JSON.stringify(emptyBorn)} m=${V.player.meter}`
);
const superTravel = Math.abs((superBorn && superBorn.vx) || 0) * (STEP/1000);
const superBirth = !superBorn ? 0 : (superBorn.facing > 0 ? superBorn.x - superTravel : superBorn.x + superBorn.w + superTravel);
check("spent dart births at live tip",
  superBorn && superBorn.tip!=null && Math.abs(superBirth - superBorn.tip) < 3,
  `birth=${superBirth.toFixed(1)} tip=${superBorn && superBorn.tip && superBorn.tip.toFixed(1)} x=${superBorn && superBorn.x && superBorn.x.toFixed(1)} vx=${superBorn && superBorn.vx}`
);

parkRangeThrow();
V.rival.meter = 0;
V.rival.boltSuper = false;
V.update(STEP);
drainHitstop();
let rEmpty=null;
for(let i=0;i<24;i++){
  V.player.x = 180;
  V.update(STEP);
  if(V.bolt){ rEmpty = { w:V.bolt.w, h:V.bolt.h, vx:V.bolt.vx, super:!!V.bolt.super, kind:V.bolt.kind }; break; }
}
check("rival empty dart size/speed unchanged",
  rEmpty && rEmpty.kind==="rival" && rEmpty.super===false && rEmpty.w===V.BOLT_W && Math.abs(rEmpty.vx)===V.BOLT_SPEED,
  `born=${JSON.stringify(rEmpty)}`
);

parkRivalSuperRange();
const holdFly = V.bladeReach(V.rival)+V.LUNGE_PX;
const flyWant = Math.min(V.SUPER_RANGE - 20, holdFly + 210);
for(let n=0;n<12;n++){
  const g = V.absGap();
  V.rival.x -= (g - flyWant);
}
const flyGap = V.absGap();
V.update(STEP);
check("rival super plant still dumps", V.rival.boltPhase==="startup" && V.rival.boltSuper===true && V.rival.meter===0 && flyGap<=V.SUPER_RANGE && flyGap>holdFly+16, `bph=${V.rival.boltPhase} super=${V.rival.boltSuper} m=${V.rival.meter} gap=${flyGap.toFixed(1)}`);
drainHitstop();
let rSuper=null;
for(let i=0;i<24;i++){
  freezeYou();
  freezeYouAI();
  V.player.guarding = false;
  V.player.x = 360;
  V.update(STEP);
  if(V.bolt){ rSuper = { w:V.bolt.w, h:V.bolt.h, vx:V.bolt.vx, super:!!V.bolt.super, kind:V.bolt.kind }; break; }
}
check("rival super projectile faster same family",
  rSuper && rSuper.kind==="rival" && rSuper.super===true && Math.abs(rSuper.vx)===V.BOLT_SUPER_SPEED && rSuper.w===V.BOLT_SUPER_W && rSuper.h===V.BOLT_SUPER_H && rSuper.w>V.BOLT_W && rSuper.h>V.BOLT_H,
  `born=${JSON.stringify(rSuper)} gap=${flyGap.toFixed(1)}`
);

// --- v141 reversal from guard ---
check("reversal stam is a real chunk", V.REVERSAL_STAM===30 && V.REVERSAL_STAM===V.BOLT_STAM && V.REVERSAL_STAM>V.STAMINA_BLOCK && V.REVERSAL_STAM>0, `st=${V.REVERSAL_STAM}`);
check("reversal invuln is golpe startup", V.REVERSAL_INVULN===V.GOLPE_STARTUP && V.REVERSAL_INVULN===120, `inv=${V.REVERSAL_INVULN}`);
check("reversal reuses L frames", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180);
check("throw Space+S chord unchanged", V.THROW_STARTUP===80 && V.THROW_ACTIVE===40 && V.THROW_RECOVERY===220 && V.THROW_DMG===20 && V.THROW_STAM===0 && V.THROW_RANGE===120);
check("super vs steel / fill / chip / dart size locked", V.BOLT_SUPER_DMG===28 && V.BOLT_SUPER_CHIP===6 && V.BOLT_CHIP===2 && V.METER_HIT===20 && V.BOLT_SUPER_W===96 && V.BOLT_SUPER_H===24 && V.BOLT_W===58 && V.BOLT_H===13);
check("cancel layer locked", V.SLASH_CANCEL_MS===100 && V.GOLPE_CANCEL_MS===100 && V.BOLT_CANCEL_MS===100);
check("L+S is reversal chord not throw", /act === "golpe"/.test(srcTxt) && /reversalEdge = true/.test(srcTxt) && /actionHeld\("guard"\)/.test(srcTxt) && /throwEdge = true/.test(srcTxt));
check("pad reversal is hold guard tap golpe no 6th", /data-tap="KeyL"/.test(htmlTxt) && (htmlTxt.match(/data-tap=/g)||[]).length===3 && !/data-tap="KeyU"/.test(htmlTxt) && !/zone-reversal/.test(htmlTxt) && !/zone-throw/.test(htmlTxt));
check("rival reversal is same L-from-guard not a robot", /function rivalTryReversal/.test(codeOnly) && /REVERSAL_AI_CD/.test(srcTxt) && /meatySpaceAtGuard/.test(codeOnly));
check("golpe sheets still slash family", /f\.cut === "golpe"/.test(sliceFn(codeOnly, "poseBitmap"))===false && /phase === "startup" && ready\(wind\)/.test(sliceFn(codeOnly, "poseBitmap")));

function raiseYouGuard(){
  freezeYouAI();
  freezeRivalAI();
  holdRivalStill();
  V.player.hp = 100;
  V.rival.hp = 100;
  V.player.stamina = 100;
  V.player.stamRegenT = 99999;
  V.player.guardBreakT = 0;
  V.player.stunT = 0;
  V.player.falling = false;
  V.player.boltPhase = "";
  V.player.reversal = false;
  V.player.cut = "slash";
  V.player.phase = "idle";
  V.player.phaseT = 0;
  V.keys.add("KeyS");
  V.update(STEP);
}

function fireReversalChord(){
  V.keys.add("KeyS");
  V.reversalEdge = true;
}

// close: L from guard is reversal not throw
toClose();
raiseYouGuard();
const closeGap = V.absGap();
const stamClose0 = V.player.stamina;
check("close guard raised before reversal", V.player.guarding && closeGap<=V.THROW_RANGE, `g=${V.player.guarding} gap=${closeGap.toFixed(1)}`);
fireReversalChord();
V.update(STEP);
check("close L from guard is reversal not throw", V.player.cut==="golpe" && V.player.phase==="startup" && V.player.reversal && !V.player.guarding && V.player.cut!=="throw" && closeGap<=V.THROW_RANGE, `cut=${V.player.cut} ph=${V.player.phase} rev=${V.player.reversal} g=${V.player.guarding} gap=${closeGap.toFixed(1)}`);
check("reversal stam is spent", stamClose0-V.player.stamina>=V.REVERSAL_STAM-1 && stamClose0-V.player.stamina<=V.REVERSAL_STAM+2, `d=${(stamClose0-V.player.stamina).toFixed(2)} st0=${stamClose0.toFixed(2)} st=${V.player.stamina.toFixed(2)}`);
V.keys.delete("KeyS");

// close Space+S still throw
toClose();
raiseYouGuard();
V.rival.stamina = 80;
V.rival.stamRegenT = 99999;
V.rival.wantBlock = true;
V.rival.guarding = true;
V.rival.guardChoice = 1;
V.rival.throwTechArmed = -1;
V.keys.add("KeyS");
V.keys.add("Space");
V.throwEdge = true;
let stillThrow=false;
for(let i=0;i<80;i++){
  holdRivalStill();
  V.rival.wantBlock = true;
  V.rival.guarding = true;
  V.rival.guardChoice = 1;
  V.rival.throwTechArmed = -1;
  V.update(STEP);
  if(V.hitstopLeft>0 || V.rival.hp<100 || V.rival.thrownT>0){ stillThrow=true; break; }
}
drainHitstop();
check("Space+S close is still throw", stillThrow && V.rival.hp===80 && V.player.hp===100 && V.player.cut==="throw" && V.player.cutHit && V.rival.thrownT>0, `hit=${stillThrow} rhp=${V.rival.hp} php=${V.player.hp} cut=${V.player.cut} ch=${V.player.cutHit} th=${V.rival.thrownT}`);
V.keys.clear();

// far: L-from-guard still reverses, not throw
bootPlay(); wait(1700);
raiseYouGuard();
V.player.x = 180;
V.rival.x = 1100;
V.keys.add("KeyS");
V.update(STEP);
const farGap = V.absGap();
fireReversalChord();
V.update(STEP);
check("far L from guard still reverses not throw", farGap>V.THROW_RANGE && V.player.cut==="golpe" && V.player.phase==="startup" && V.player.reversal && V.player.cut!=="throw" && !V.player.guarding, `gap=${farGap.toFixed(1)} cut=${V.player.cut} ph=${V.player.phase} rev=${V.player.reversal} g=${V.player.guarding}`);
V.keys.delete("KeyS");

// idle L without S is still normal golpe, not reversal
toMeasure();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.stamina = 100;
V.golpeEdge = true;
V.update(STEP);
check("L without guard is golpe not reversal", V.player.cut==="golpe" && V.player.phase==="startup" && !V.player.reversal, `cut=${V.player.cut} ph=${V.player.phase} rev=${V.player.reversal} g=${V.player.guarding}`);

// low stam cannot reversal (not free)
toClose();
raiseYouGuard();
V.player.stamina = 20;
V.player.stamRegenT = 99999;
V.keys.add("KeyS");
V.update(STEP);
fireReversalChord();
V.update(STEP);
check("low stam L from guard stays guard", V.player.guarding && V.player.phase==="idle" && !V.player.reversal && V.player.cut!=="throw" && V.player.stamina<V.REVERSAL_STAM, `g=${V.player.guarding} ph=${V.player.phase} rev=${V.player.reversal} cut=${V.player.cut} st=${V.player.stamina.toFixed(2)}`);
V.keys.delete("KeyS");

// reversal beats meaty Space slash
toClose();
raiseYouGuard();
V.rival.cut = "slash";
V.rival.phase = "active";
V.rival.phaseT = 0;
V.rival.cutHit = false;
V.rival.reversal = false;
V.rival.guarding = false;
V.rival.wantBlock = false;
V.player.hp = 100;
V.rival.hp = 100;
check("meaty setup rival slash active vs guard", V.player.guarding && V.rival.phase==="active" && V.rival.cut==="slash", `g=${V.player.guarding} rph=${V.rival.phase} rcut=${V.rival.cut}`);
fireReversalChord();
let revMeaty=false, revYouHit=false, revLand=false, revClash=false;
for(let i=0;i<90;i++){
  holdRivalStill();
  V.rival.wantBlock = false;
  V.rival.guarding = false;
  V.rival.guardChoice = -1;
  if(V.player.reversal || (V.player.cut==="golpe" && V.player.phase!=="idle")) revMeaty=true;
  V.update(STEP);
  if(V.player.hp<100) revYouHit=true;
  if(V.rival.hp<100){ revLand=true; break; }
  if(V.player.clashRec || V.rival.clashRec) revClash=true;
  if(V.hitstopLeft>0 && V.rival.hp<100){ revLand=true; break; }
}
drainHitstop();
check("reversal beats meaty slash", revMeaty && revLand && !revYouHit && !revClash && V.player.hp===100 && V.rival.hp===90, `rev=${revMeaty} rhp=${V.rival.hp} php=${V.player.hp} youHit=${revYouHit} clash=${revClash} pcut=${V.player.cut} pph=${V.player.phase} rcut=${V.rival.cut} rph=${V.rival.phase} inv=${V.reversalInvuln(V.player)}`);

// reversal loses to throw
toClose();
raiseYouGuard();
V.rival.throwTechArmed = -1;
V.player.throwTechArmed = 0;
fireReversalChord();
const throwVsRev = V.startThrow(V.rival);
V.update(STEP);
check("throw vs reversal both started", throwVsRev && V.player.cut==="golpe" && V.player.reversal && V.rival.cut==="throw" && V.rival.phase==="startup", `ok=${throwVsRev} pcut=${V.player.cut} rev=${V.player.reversal} rcut=${V.rival.cut} rph=${V.rival.phase}`);
let throwBeatRev=false;
for(let i=0;i<80;i++){
  holdRivalStill();
  V.rival.throwTechArmed = -1;
  V.update(STEP);
  if(V.player.thrownT>0 || V.player.hp<100){ throwBeatRev=true; break; }
}
drainHitstop();
check("reversal loses to throw", throwBeatRev && V.player.hp===80 && V.player.thrownT>0 && V.rival.cut==="throw" && V.rival.cutHit && V.player.hp!==90, `beat=${throwBeatRev} php=${V.player.hp} th=${V.player.thrownT} rch=${V.rival.cutHit} rcut=${V.rival.cut} pcut=${V.player.cut}`);
V.keys.clear();

// pad hold-guarda + tap-golpe is reversal, not throw
toClose();
raiseYouGuard();
const zsRev = fakeZone();
const zlRev = fakeZone();
V.padDown("KeyS", 201, zsRev);
V.update(STEP);
const padGuard = V.player.guarding;
V.padDown("KeyL", 202, zlRev);
V.update(STEP);
check("pad hold guard tap golpe is reversal not throw", padGuard && V.player.cut==="golpe" && V.player.reversal && V.player.phase==="startup" && V.player.cut!=="throw" && !V.player.guarding, `g0=${padGuard} cut=${V.player.cut} rev=${V.player.reversal} ph=${V.player.phase} g=${V.player.guarding}`);
V.padUp(202);
V.padUp(201);

// pad close Space+S still throw (chords do not collide)
toClose();
raiseYouGuard();
V.rival.stamina = 80;
V.rival.wantBlock = true;
V.rival.guarding = true;
V.rival.guardChoice = 1;
V.rival.throwTechArmed = -1;
const zsT = fakeZone();
const zslT = fakeZone();
V.padDown("KeyS", 203, zsT);
V.padDown("Space", 204, zslT);
let padThrowStill=false;
for(let i=0;i<80;i++){
  holdRivalStill();
  V.rival.wantBlock = true;
  V.rival.guarding = true;
  V.rival.guardChoice = 1;
  V.rival.throwTechArmed = -1;
  V.update(STEP);
  if(V.hitstopLeft>0 || V.rival.hp<100 || V.rival.thrownT>0){ padThrowStill=true; break; }
}
drainHitstop();
check("pad Space+S close still throw not reversal", padThrowStill && V.player.cut==="throw" && V.player.cutHit && V.rival.thrownT>0 && V.rival.hp===80 && !V.player.reversal, `hit=${padThrowStill} cut=${V.player.cut} ch=${V.player.cutHit} th=${V.rival.thrownT} hp=${V.rival.hp} rev=${V.player.reversal}`);
V.padUp(204);
V.padUp(203);

check("throw/super/chip still locked after reversal", V.THROW_STARTUP===80 && V.THROW_STAM===0 && V.BOLT_SUPER_DMG===28 && V.BOLT_SUPER_CHIP===6 && V.BOLT_CHIP===2 && V.SLASH_CANCEL_MS===100 && V.GOLPE_STARTUP===120);
check("player reversal unchanged after rival pass", V.REVERSAL_STAM===30 && V.REVERSAL_INVULN===120 && V.GOLPE_STARTUP===120 && /if \(player\.guarding\) startReversal\(player\)/.test(codeOnly) && /reversalEdge = true/.test(srcTxt) && /actionHeld\("guard"\)/.test(srcTxt));

// --- v142 rival reversal ---
check("rival reversal CD 1800 chance 40", V.REVERSAL_AI_CD===1800 && V.REVERSAL_AI_CD===V.THROW_AI_CD && V.REVERSAL_AI_CD===V.BOLT_AI_CD && V.REVERSAL_AI_CHANCE===0.4, `cd=${V.REVERSAL_AI_CD} ch=${V.REVERSAL_AI_CHANCE}`);
check("rival reversal same stam/invuln/L frames", V.REVERSAL_STAM===30 && V.REVERSAL_INVULN===V.GOLPE_STARTUP && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180);
check("rival reversal vs meaty Space or L while guarding", /player\.cut !== "slash"/.test(sliceFn(codeOnly, "meatySpaceAtGuard")) && /player\.cut !== "golpe"/.test(sliceFn(codeOnly, "meatySpaceAtGuard")) && /!rival\.guarding/.test(sliceFn(codeOnly, "meatySpaceAtGuard")) && /HOLD \+ LUNGE_PX/.test(sliceFn(codeOnly, "meatySpaceAtGuard")));
check("rival reversal not vs dart", /boltPhase === "startup"/.test(sliceFn(codeOnly, "meatySpaceAtGuard")) && /bolt && bolt\.kind === "you"/.test(sliceFn(codeOnly, "meatySpaceAtGuard")));
check("rival reversal once-per-swing + CD", /revArmed/.test(sliceFn(codeOnly, "armRivalReversal")) && /Math\.random\(\) < REVERSAL_AI_CHANCE/.test(sliceFn(codeOnly, "armRivalReversal")) && /revCd > 0/.test(sliceFn(codeOnly, "rivalTryReversal")) && /revCd = REVERSAL_AI_CD/.test(sliceFn(codeOnly, "rivalTryReversal")));
check("rival uses shared startReversal", /startReversal\(rival\)/.test(sliceFn(codeOnly, "rivalTryReversal")) && /startReversal\(player\)/.test(codeOnly));
check("no 6th button after rival reversal", (htmlTxt.match(/data-tap=/g)||[]).length===3 && !/zone-reversal/.test(htmlTxt));
check("throw chord / super / fill / chip / cancel locked", V.THROW_STARTUP===80 && V.THROW_STAM===0 && V.THROW_RANGE===120 && V.BOLT_SUPER_DMG===28 && V.BOLT_SUPER_CHIP===6 && V.BOLT_CHIP===2 && V.METER_HIT===20 && V.SLASH_CANCEL_MS===100 && V.GOLPE_CANCEL_MS===100 && V.BOLT_CANCEL_MS===100);

function armRivalReversalAI(){
  V.rival.phase = "idle";
  V.rival.phaseT = 0;
  V.rival.cut = "slash";
  V.rival.cutHit = false;
  V.rival.linkGolpe = false;
  V.rival.linkSlash = false;
  V.rival.linkBolt = false;
  V.rival.clashRec = false;
  V.rival.techRec = false;
  V.rival.throwTechArmed = -1;
  V.rival.thrownT = 0;
  V.rival.throwInvulnT = 0;
  V.rival.boltPhase = "";
  V.rival.boltT = 0;
  V.rival.boltCd = 99999;
  V.rival.throwCd = 99999;
  V.rival.boltArmed = false;
  V.rival.stunT = 0;
  V.rival.falling = false;
  V.rival.guardBreakT = 0;
  V.rival.pushT = 0;
  V.rival.stamina = 100;
  V.rival.stamRegenT = 99999;
  V.rival.wantBlock = true;
  V.rival.guarding = true;
  V.rival.guardChoice = 1;
  V.rival.closing = false;
  V.rival.standWait = 0;
  V.rival.standGoal = 99999;
  V.rival.reversal = false;
  V.rival.revCd = 0;
  V.rival.revArmed = 0;
  V.rival.pbCd = 99999;
  V.rival.pbArmed = -1;
  V.player.hp = 100;
  V.rival.hp = 100;
  V.player.thrownT = 0;
  V.player.stunT = 0;
  V.player.techRec = false;
  V.player.reversal = false;
  V.player.guarding = false;
}

function holdYouSlashMeaty(){
  V.player.falling = false;
  V.player.stunT = 0;
  V.player.boltPhase = "";
  V.player.guarding = false;
  V.keys.clear();
}

function plantMeatySlash(){
  holdYouSlashMeaty();
  V.player.cut = "slash";
  V.player.phase = "startup";
  V.player.phaseT = 150;
  V.player.cutHit = false;
  V.player.clashRec = false;
  V.player.reversal = false;
}

// rival can reversal a meaty slash while guarding
toClose();
freezeYouAI();
armRivalReversalAI();
plantMeatySlash();
Math.random = () => 0;
const stamRev0 = V.rival.stamina;
const gapRev = V.absGap();
check("rival meaty setup guarding vs Space", V.rival.guarding && V.player.cut==="slash" && V.player.phase==="startup" && gapRev<=V.bladeReach(V.rival)+V.LUNGE_PX+V.LUNGE_PX, `g=${V.rival.guarding} pcut=${V.player.cut} pph=${V.player.phase} gap=${gapRev.toFixed(1)}`);
let rRev=false, rLand=false, rYouHit=false, rClash=false;
for(let i=0;i<90;i++){
  Math.random = () => 0;
  V.rival.boltCd = 99999;
  V.rival.throwCd = 99999;
  holdYouSlashMeaty();
  if(V.rival.reversal || (V.rival.cut==="golpe" && V.rival.phase!=="idle")) rRev=true;
  V.update(STEP);
  if(V.rival.hp<100) rYouHit=true;
  if(V.player.hp<100){ rLand=true; break; }
  if(V.player.clashRec || V.rival.clashRec) rClash=true;
  if(V.hitstopLeft>0 && V.player.hp<100){ rLand=true; break; }
}
drainHitstop();
check("rival reversals meaty slash while guarding", rRev && rLand && !rYouHit && !rClash && V.player.hp===90 && V.rival.hp===100 && V.rival.reversal, `rev=${rRev} php=${V.player.hp} rhp=${V.rival.hp} youHit=${rYouHit} clash=${rClash} rcut=${V.rival.cut} rph=${V.rival.phase} pcut=${V.player.cut} pph=${V.player.phase} inv=${V.reversalInvuln(V.rival)} armed=${V.rival.revArmed}`);
check("rival reversal spends 30 stam", stamRev0-V.rival.stamina>=V.REVERSAL_STAM-2 && stamRev0-V.rival.stamina<=V.REVERSAL_STAM+4, `d=${(stamRev0-V.rival.stamina).toFixed(2)} st0=${stamRev0.toFixed(2)} st=${V.rival.stamina.toFixed(2)}`);
check("rival reversal CD set on start", V.rival.revCd>0 && V.rival.revCd<=V.REVERSAL_AI_CD, `cd=${V.rival.revCd.toFixed(0)}`);

// does not spam — direct CD, not a 900ms tickAI robot
toClose();
freezeYouAI();
armRivalReversalAI();
plantMeatySlash();
V.rival.revArmed = 1;
V.rival.revCd = 0;
const firstRev = V.rivalTryReversal();
check("rival reversal CD armed after first", firstRev && V.rival.reversal && V.rival.cut==="golpe" && V.rival.phase==="startup" && V.rival.revCd===V.REVERSAL_AI_CD, `ok=${firstRev} rev=${V.rival.reversal} cd=${V.rival.revCd} cut=${V.rival.cut} ph=${V.rival.phase}`);
const cdRev0 = V.rival.revCd;
V.rival.wantBlock = true;
V.rival.guarding = true;
V.rival.guardChoice = 1;
V.rival.phase = "idle";
V.rival.phaseT = 0;
V.rival.cut = "slash";
V.rival.reversal = false;
V.rival.stamina = 100;
plantMeatySlash();
V.rival.revArmed = 1;
const secondRev = V.rivalTryReversal();
check("rival reversal CD holds, no spam", cdRev0===V.REVERSAL_AI_CD && secondRev===false && !V.rival.reversal && V.rival.guarding && V.rival.revCd===cdRev0, `ok=${secondRev} cd0=${cdRev0} cd=${V.rival.revCd} rev=${V.rival.reversal} g=${V.rival.guarding} cut=${V.rival.cut} ph=${V.rival.phase}`);

// chance miss: not every block
toClose();
freezeYouAI();
armRivalReversalAI();
plantMeatySlash();
V.rival.revArmed = 0;
Math.random = () => 0.99;
let missRev=false, missBlock=false;
for(let i=0;i<80;i++){
  Math.random = () => 0.99;
  V.rival.boltCd = 99999;
  V.rival.throwCd = 99999;
  V.rival.wantBlock = true;
  V.rival.guardChoice = 1;
  if(!V.rival.reversal) V.rival.guarding = true;
  holdYouSlashMeaty();
  if(V.player.phase==="idle") plantMeatySlash();
  V.update(STEP);
  if(V.rival.reversal || (V.rival.cut==="golpe" && V.rival.phase!=="idle" && V.rival.phase!=="recovery")) missRev=true;
  if(V.hitstopLeft>0 && V.rival.hp===100 && V.rival.pushT>0){ missBlock=true; break; }
  if(V.rival.hp<100) break;
}
drainHitstop();
check("rival does not reversal every block", !missRev && missBlock && V.rival.hp===100 && V.player.cutHit && !V.rival.reversal, `rev=${missRev} block=${missBlock} hp=${V.rival.hp} ch=${V.player.cutHit} rcut=${V.rival.cut} rph=${V.rival.phase} armed=${V.rival.revArmed}`);

// not vs dart
toClose();
freezeYouAI();
armRivalReversalAI();
V.player.cut = "slash";
V.player.phase = "idle";
V.player.phaseT = 0;
V.player.stamina = 100;
V.player.stamRegenT = 99999;
Math.random = () => 0;
const dartOk = V.startBolt(V.player);
let dartRev=false;
for(let i=0;i<30;i++){
  Math.random = () => 0;
  V.rival.wantBlock = true;
  V.rival.guardChoice = 1;
  if(!V.rival.reversal) V.rival.guarding = true;
  V.rival.boltCd = 99999;
  V.rival.throwCd = 99999;
  V.update(STEP);
  if(V.rival.reversal || (V.rival.cut==="golpe" && V.rival.phase==="startup")) dartRev=true;
}
check("rival does not reversal vs dart", dartOk && !dartRev && !V.rival.reversal && (V.player.boltPhase==="startup" || !!V.bolt || V.player.boltPhase==="recovery"), `ok=${dartOk} rev=${dartRev} rrev=${V.rival.reversal} rcut=${V.rival.cut} rph=${V.rival.phase} bph=${V.player.boltPhase} bolt=${!!V.bolt} g=${V.rival.guarding}`);

// not vs throw this pass — throw still beats hold-guard, and a bad reversal still loses
toClose();
freezeYouAI();
armRivalReversalAI();
V.rival.throwTechArmed = -1;
Math.random = () => 0;
fireThrow();
let throwNoRev=false, sawRRev=false;
for(let i=0;i<80;i++){
  Math.random = () => 0;
  V.rival.throwTechArmed = -1;
  V.rival.wantBlock = true;
  V.rival.guardChoice = 1;
  if(!V.rival.reversal) V.rival.guarding = true;
  V.rival.boltCd = 99999;
  V.update(STEP);
  if(V.rival.reversal) sawRRev=true;
  if(V.hitstopLeft>0 || V.rival.hp<100 || V.rival.thrownT>0){ throwNoRev=true; break; }
}
drainHitstop();
check("rival does not reversal vs throw", !sawRRev && throwNoRev && V.rival.hp===80 && V.player.cut==="throw" && V.player.cutHit && V.rival.thrownT>0 && !V.rival.reversal, `rev=${sawRRev} hit=${throwNoRev} hp=${V.rival.hp} cut=${V.player.cut} ch=${V.player.cutHit} th=${V.rival.thrownT} rcut=${V.rival.cut}`);

// player throw still beats a bad reversal
toClose();
freezeYouAI();
armRivalReversalAI();
V.rival.throwTechArmed = -1;
V.player.throwTechArmed = 0;
const revVsThrow = V.startReversal(V.rival);
const throwVsRRev = V.startThrow(V.player);
V.update(STEP);
check("throw vs rival reversal both started", revVsThrow && throwVsRRev && V.rival.cut==="golpe" && V.rival.reversal && V.player.cut==="throw" && V.player.phase==="startup", `rok=${revVsThrow} tok=${throwVsRRev} rcut=${V.rival.cut} rrev=${V.rival.reversal} pcut=${V.player.cut} pph=${V.player.phase}`);
let throwBeatRRev=false;
for(let i=0;i<80;i++){
  V.rival.throwTechArmed = -1;
  V.update(STEP);
  if(V.rival.thrownT>0 || V.rival.hp<100){ throwBeatRRev=true; break; }
}
drainHitstop();
check("player throw still beats a bad reversal", throwBeatRRev && V.rival.hp===80 && V.rival.thrownT>0 && V.player.cut==="throw" && V.player.cutHit && V.rival.hp!==90, `beat=${throwBeatRRev} rhp=${V.rival.hp} th=${V.rival.thrownT} pch=${V.player.cutHit} pcut=${V.player.cut} rcut=${V.rival.cut}`);

// throw chord unchanged (Space+S still throw, L+S still player reversal)
toClose();
raiseYouGuard();
fireReversalChord();
V.update(STEP);
check("player L+S still reversal after rival pass", V.player.cut==="golpe" && V.player.reversal && V.player.phase==="startup" && V.player.cut!=="throw", `cut=${V.player.cut} rev=${V.player.reversal} ph=${V.player.phase}`);
V.keys.delete("KeyS");

toClose();
raiseYouGuard();
V.rival.stamina = 80;
V.rival.stamRegenT = 99999;
V.rival.wantBlock = true;
V.rival.guarding = true;
V.rival.guardChoice = 1;
V.rival.throwTechArmed = -1;
V.rival.revCd = 99999;
V.rival.revArmed = -1;
V.rival.pbCd = 99999;
V.rival.pbArmed = -1;
V.keys.add("KeyS");
V.keys.add("Space");
V.throwEdge = true;
let chordThrow=false;
for(let i=0;i<80;i++){
  holdRivalStill();
  V.rival.wantBlock = true;
  V.rival.guarding = true;
  V.rival.guardChoice = 1;
  V.rival.throwTechArmed = -1;
  V.rival.revCd = 99999;
  V.rival.revArmed = -1;
  V.rival.pbCd = 99999;
  V.rival.pbArmed = -1;
  V.update(STEP);
  if(V.hitstopLeft>0 || V.rival.hp<100 || V.rival.thrownT>0){ chordThrow=true; break; }
}
drainHitstop();
check("throw chord Space+S unchanged", chordThrow && V.player.cut==="throw" && V.player.cutHit && V.rival.thrownT>0 && V.rival.hp===80 && !V.player.reversal, `hit=${chordThrow} cut=${V.player.cut} ch=${V.player.cutHit} th=${V.rival.thrownT} hp=${V.rival.hp} rev=${V.player.reversal}`);
V.keys.clear();


// --- v142 tight: meatySpaceAtGuard + forced revArmed/revCd (no tickAI robot) ---
toClose();
freezeYouAI();
armRivalReversalAI();
plantMeatySlash();
check("meatySpaceAtGuard true vs Space startup", V.meatySpaceAtGuard()===true, `g=${V.rival.guarding} cut=${V.player.cut} ph=${V.player.phase} gap=${V.absGap().toFixed(1)}`);
V.player.phase = "active";
check("meatySpaceAtGuard true vs Space active", V.meatySpaceAtGuard()===true, `ph=${V.player.phase}`);
V.player.phase = "recovery";
check("meatySpaceAtGuard false vs slash recovery", V.meatySpaceAtGuard()===false, `ph=${V.player.phase}`);
plantMeatySlash();
V.player.cut = "golpe";
check("meatySpaceAtGuard true vs L", V.meatySpaceAtGuard()===true, `cut=${V.player.cut}`);
plantMeatySlash();
V.player.cut = "throw";
check("meatySpaceAtGuard false vs throw cut", V.meatySpaceAtGuard()===false, `cut=${V.player.cut}`);
plantMeatySlash();
V.rival.guarding = false;
check("meatySpaceAtGuard false if not guarding", V.meatySpaceAtGuard()===false, `g=${V.rival.guarding}`);
armRivalReversalAI();
plantMeatySlash();
V.player.boltPhase = "startup";
check("meatySpaceAtGuard false vs dart plant", V.meatySpaceAtGuard()===false, `bph=${V.player.boltPhase} cut=${V.player.cut} ph=${V.player.phase}`);
V.player.boltPhase = "";

toClose();
freezeYouAI();
armRivalReversalAI();
plantMeatySlash();
V.rival.revArmed = 1;
V.rival.revCd = 0;
const stamForce = V.rival.stamina;
const forceRev = V.rivalTryReversal();
check("forced revArmed starts golpe reversal", forceRev && V.rival.reversal && V.rival.cut==="golpe" && V.rival.phase==="startup" && !V.rival.guarding, `ok=${forceRev} cut=${V.rival.cut} ph=${V.rival.phase} rev=${V.rival.reversal} g=${V.rival.guarding}`);
check("forced reversal spends 30 stam", stamForce-V.rival.stamina===V.REVERSAL_STAM, `d=${(stamForce-V.rival.stamina).toFixed(2)} st0=${stamForce.toFixed(2)} st=${V.rival.stamina.toFixed(2)}`);
check("forced reversal drops guard and arms CD", !V.rival.guarding && V.rival.revCd===V.REVERSAL_AI_CD, `g=${V.rival.guarding} cd=${V.rival.revCd}`);

toClose();
freezeYouAI();
armRivalReversalAI();
plantMeatySlash();
V.rival.revArmed = 1;
V.rival.revCd = 500;
const cdForceBlock = V.rivalTryReversal();
check("forced revCd>0 does not reverse", cdForceBlock===false && !V.rival.reversal && V.rival.guarding && V.rival.phase==="idle" && V.rival.revCd===500, `ok=${cdForceBlock} rev=${V.rival.reversal} g=${V.rival.guarding} cd=${V.rival.revCd} ph=${V.rival.phase}`);

toClose();
freezeYouAI();
armRivalReversalAI();
plantMeatySlash();
V.rival.revArmed = -1;
V.rival.revCd = 0;
const failRoll = V.rivalTryReversal();
check("forced revArmed -1 does not reverse", failRoll===false && !V.rival.reversal && V.rival.guarding && V.rival.phase==="idle" && V.rival.revArmed===-1, `ok=${failRoll} rev=${V.rival.reversal} g=${V.rival.guarding} armed=${V.rival.revArmed} ph=${V.rival.phase}`);

check("locked layers after rival reversal bar", V.REVERSAL_AI_CD===1800 && V.REVERSAL_AI_CHANCE===0.4 && V.REVERSAL_STAM===30 && V.REVERSAL_INVULN===120 && V.THROW_STARTUP===80 && V.THROW_STAM===0 && V.THROW_RANGE===120 && V.BOLT_SUPER_DMG===28 && V.BOLT_SUPER_CHIP===6 && V.BOLT_CHIP===2 && V.SLASH_CANCEL_MS===100 && V.GOLPE_CANCEL_MS===100 && V.BOLT_CANCEL_MS===100 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180);


check("cache v=296 after rival reversal", /game.js\?v=350/.test(htmlTxt));

// --- v143 wakeup reversal ---
check("wakeup window is throw-invuln after knockdown", V.THROW_WAKE_INVULN===80 && V.THROW_KD_MS===520 && V.REVERSAL_STAM===30 && V.REVERSAL_INVULN===120 && V.GOLPE_STARTUP===120);
check("wakeup reuses startReversal path", /startReversal\(f\)/.test(sliceFn(codeOnly, "startWakeReversal")) && /function wakeupWindow/.test(codeOnly) && /function startWakeReversal/.test(codeOnly));
check("wakeup consumes throw-invuln so throw still beats", /throwInvulnT = 0/.test(sliceFn(codeOnly, "startWakeReversal")) && /wakeRev = false/.test(sliceFn(codeOnly, "startWakeReversal")));
check("one wakeRev per getup", /wakeRev = true/.test(sliceFn(codeOnly, "tickThrowState")) && /wakeRev = false/.test(sliceFn(codeOnly, "tickThrowState")));
check("guard reversal still hold S tap L", /if \(player\.guarding\) startReversal\(player\)/.test(codeOnly) && /reversalEdge = true/.test(srcTxt) && /actionHeld\("guard"\)/.test(srcTxt));
check("rival meaty-Space reversal still on", /startReversal\(rival\)/.test(sliceFn(codeOnly, "rivalTryReversal")) && /player\.cut !== "slash"/.test(sliceFn(codeOnly, "meatySpaceAtGuard")) && /player\.cut !== "golpe"/.test(sliceFn(codeOnly, "meatySpaceAtGuard")) && !/startWakeReversal/.test(sliceFn(codeOnly, "rivalTryReversal")));
check("no 6th button after wakeup reversal", (htmlTxt.match(/data-tap=/g)||[]).length===3 && !/zone-reversal/.test(htmlTxt) && /data-tap="KeyL"/.test(htmlTxt));
check("throw/super/cancel locked after wakeup", V.THROW_STARTUP===80 && V.THROW_STAM===0 && V.THROW_RANGE===120 && V.BOLT_SUPER_DMG===28 && V.BOLT_SUPER_CHIP===6 && V.BOLT_CHIP===2 && V.SLASH_CANCEL_MS===100 && V.GOLPE_CANCEL_MS===100 && V.BOLT_CANCEL_MS===100 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180);

function plantYouWake(){
  toClose();
  freezeYouAI();
  freezeRivalAI();
  holdRivalStill();
  V.player.hp = 100;
  V.rival.hp = 100;
  V.player.stamina = 100;
  V.player.stamRegenT = 99999;
  V.player.guardBreakT = 0;
  V.player.thrownT = 0;
  V.player.stunT = 0;
  V.player.throwInvulnT = V.THROW_WAKE_INVULN;
  V.player.wakeRev = true;
  V.player.reversal = false;
  V.player.guarding = false;
  V.player.phase = "idle";
  V.player.phaseT = 0;
  V.player.cut = "slash";
  V.player.boltPhase = "";
  V.keys.clear();
}

// wakeup L is reversal
plantYouWake();
const wakeStam0 = V.player.stamina;
check("wakeup window live before L", V.wakeupWindow(V.player) && V.player.throwInvulnT===V.THROW_WAKE_INVULN && V.player.wakeRev && V.player.thrownT===0 && V.player.stunT===0, `win=${V.wakeupWindow(V.player)} inv=${V.player.throwInvulnT} wr=${V.player.wakeRev} th=${V.player.thrownT} st=${V.player.stunT}`);
V.golpeEdge = true;
V.update(STEP);
check("wakeup L is reversal", V.player.cut==="golpe" && V.player.phase==="startup" && V.player.reversal && !V.player.guarding && V.player.cut!=="throw", `cut=${V.player.cut} ph=${V.player.phase} rev=${V.player.reversal} g=${V.player.guarding}`);
check("wakeup reversal stam -30", wakeStam0-V.player.stamina===V.REVERSAL_STAM, `d=${(wakeStam0-V.player.stamina).toFixed(2)} st0=${wakeStam0.toFixed(2)} st=${V.player.stamina.toFixed(2)}`);
check("wakeup spends throw-invuln and wakeRev", V.player.throwInvulnT===0 && V.player.wakeRev===false && V.reversalInvuln(V.player), `inv=${V.player.throwInvulnT} wr=${V.player.wakeRev} rinv=${V.reversalInvuln(V.player)}`);

// still-down L is not reversal
toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.hp = 100;
V.player.stamina = 100;
V.player.stamRegenT = 99999;
V.player.thrownT = 200;
V.player.stunT = 200;
V.player.throwInvulnT = 0;
V.player.wakeRev = false;
V.player.reversal = false;
V.player.guarding = false;
V.player.phase = "idle";
V.player.phaseT = 0;
V.player.cut = "slash";
V.keys.clear();
V.golpeEdge = true;
V.update(STEP);
check("still-down L is not reversal", V.player.thrownT>0 && !V.player.reversal && V.player.phase==="idle" && V.player.cut==="slash" && !V.golpeBuf, `th=${V.player.thrownT} rev=${V.player.reversal} ph=${V.player.phase} cut=${V.player.cut} buf=${V.golpeBuf}`);

// too late is a normal L
plantYouWake();
V.player.throwInvulnT = 0;
V.player.wakeRev = false;
V.golpeEdge = true;
V.update(STEP);
check("too late L is normal golpe not reversal", V.player.cut==="golpe" && V.player.phase==="startup" && !V.player.reversal, `cut=${V.player.cut} ph=${V.player.phase} rev=${V.player.reversal} win=${V.wakeupWindow(V.player)}`);

// leftover throw-invuln without wakeRev is not a free second reversal
plantYouWake();
V.golpeEdge = true;
V.update(STEP);
check("first wakeup L consumed the attempt", V.player.reversal && V.player.wakeRev===false && V.player.throwInvulnT===0, `rev=${V.player.reversal} wr=${V.player.wakeRev} inv=${V.player.throwInvulnT}`);
V.player.phase = "idle";
V.player.phaseT = 0;
V.player.cut = "slash";
V.player.reversal = false;
V.player.throwInvulnT = V.THROW_WAKE_INVULN;
V.player.wakeRev = false;
V.player.stamina = 100;
V.player.guarding = false;
V.golpeEdge = true;
V.update(STEP);
check("leftover throw-invuln is not a free reversal", V.player.cut==="golpe" && V.player.phase==="startup" && !V.player.reversal && V.player.wakeRev===false, `cut=${V.player.cut} ph=${V.player.phase} rev=${V.player.reversal} wr=${V.player.wakeRev} inv=${V.player.throwInvulnT}`);

// throw still beats wakeup reversal
plantYouWake();
V.rival.throwTechArmed = -1;
V.player.throwTechArmed = 0;
V.golpeEdge = true;
V.update(STEP);
const throwVsWake = V.startThrow(V.rival);
check("throw vs wakeup reversal both started", throwVsWake && V.player.cut==="golpe" && V.player.reversal && V.rival.cut==="throw" && V.rival.phase==="startup" && V.player.throwInvulnT===0, `ok=${throwVsWake} pcut=${V.player.cut} rev=${V.player.reversal} rcut=${V.rival.cut} rph=${V.rival.phase} inv=${V.player.throwInvulnT}`);
let throwBeatWake=false;
for(let i=0;i<80;i++){
  holdRivalStill();
  V.rival.throwTechArmed = -1;
  V.update(STEP);
  if(V.player.thrownT>0 || V.player.hp<100){ throwBeatWake=true; break; }
}
drainHitstop();
check("throw still beats wakeup reversal", throwBeatWake && V.player.hp===80 && V.player.thrownT>0 && V.rival.cut==="throw" && V.rival.cutHit && V.player.hp!==90, `beat=${throwBeatWake} php=${V.player.hp} th=${V.player.thrownT} rch=${V.rival.cutHit} rcut=${V.rival.cut} pcut=${V.player.cut}`);
V.keys.clear();

// guard reversal unchanged (hold S tap L still reversal, idle L still golpe)
toClose();
raiseYouGuard();
const guardStam0 = V.player.stamina;
fireReversalChord();
V.update(STEP);
check("guard reversal unchanged after wakeup", V.player.cut==="golpe" && V.player.phase==="startup" && V.player.reversal && !V.player.guarding && V.player.cut!=="throw", `cut=${V.player.cut} ph=${V.player.phase} rev=${V.player.reversal} g=${V.player.guarding}`);
check("guard reversal still spends 30", guardStam0-V.player.stamina>=V.REVERSAL_STAM-1 && guardStam0-V.player.stamina<=V.REVERSAL_STAM+2, `d=${(guardStam0-V.player.stamina).toFixed(2)}`);
V.keys.delete("KeyS");

toMeasure();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.stamina = 100;
V.player.throwInvulnT = 0;
V.player.wakeRev = false;
V.golpeEdge = true;
V.update(STEP);
check("idle L without wakeup is still golpe", V.player.cut==="golpe" && V.player.phase==="startup" && !V.player.reversal, `cut=${V.player.cut} ph=${V.player.phase} rev=${V.player.reversal}`);

// pad tap golpe on getup, no new button
plantYouWake();
const zlWake = fakeZone();
V.padDown("KeyL", 301, zlWake);
V.update(STEP);
check("pad tap golpe on getup is wakeup reversal", V.player.cut==="golpe" && V.player.reversal && V.player.phase==="startup" && V.player.cut!=="throw" && !V.player.guarding, `cut=${V.player.cut} rev=${V.player.reversal} ph=${V.player.phase} g=${V.player.guarding}`);
V.padUp(301);

// live knockdown: thrownT expiry grants the window, then L reverses
toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.hp = 100;
V.rival.hp = 100;
V.player.throwTechArmed = 0;
V.rival.throwTechArmed = -1;
V.player.stamina = 100;
V.player.stamRegenT = 99999;
const liveThrow = V.startThrow(V.rival);
let liveKd=false;
for(let i=0;i<80;i++){
  holdRivalStill();
  V.rival.throwTechArmed = -1;
  V.update(STEP);
  if(V.player.thrownT>0 || V.player.hp<100){ liveKd=true; break; }
}
drainHitstop();
check("live throw knockdown before wakeup", liveThrow && liveKd && V.player.thrownT>0 && V.player.hp===80, `ok=${liveThrow} kd=${liveKd} th=${V.player.thrownT} hp=${V.player.hp}`);
V.rival.stunT = 99999;
V.rival.boltCd = 99999;
V.rival.throwCd = 99999;
V.rival.revCd = 99999;
let liveWake=false;
for(let i=0;i<80;i++){
  holdRivalStill();
  V.rival.throwTechArmed = -1;
  V.rival.stunT = 99999;
  V.rival.phase = "idle";
  V.rival.phaseT = 0;
  V.rival.cut = "slash";
  V.update(STEP);
  if(V.player.thrownT<=0 && V.player.stunT<=0 && V.player.throwInvulnT>0 && V.player.wakeRev){ liveWake=true; break; }
}
check("getup grants wakeup window", liveWake && V.player.thrownT===0 && V.player.throwInvulnT>0 && V.player.wakeRev && V.wakeupWindow(V.player), `ok=${liveWake} th=${V.player.thrownT} inv=${V.player.throwInvulnT} wr=${V.player.wakeRev} st=${V.player.stunT} win=${V.wakeupWindow(V.player)}`);
const liveStam0 = V.player.stamina;
V.rival.stunT = 99999;
V.golpeEdge = true;
V.update(STEP);
check("live wakeup L is reversal", V.player.cut==="golpe" && V.player.reversal && V.player.phase==="startup" && liveStam0-V.player.stamina===V.REVERSAL_STAM, `cut=${V.player.cut} rev=${V.player.reversal} ph=${V.player.phase} d=${(liveStam0-V.player.stamina).toFixed(2)}`);

check("cache v=296 after wakeup reversal", /game.js\?v=350/.test(htmlTxt));

// --- v144 rival wakeup-reversal AI ---
check("rival wakeup uses startWakeReversal path", /startWakeReversal\(rival\)/.test(sliceFn(codeOnly, "rivalTryWakeReversal")) && /function meatySpaceAtWake/.test(codeOnly) && /function armRivalWakeReversal/.test(codeOnly));
check("rival wakeup vs meaty Space or L in pocket", /player\.cut !== "slash"/.test(sliceFn(codeOnly, "meatySpaceAtWake")) && /player\.cut !== "golpe"/.test(sliceFn(codeOnly, "meatySpaceAtWake")) && /wakeupWindow\(rival\)/.test(sliceFn(codeOnly, "meatySpaceAtWake")) && /HOLD \+ LUNGE_PX/.test(sliceFn(codeOnly, "meatySpaceAtWake")));
check("rival wakeup not vs dart", /boltPhase === "startup"/.test(sliceFn(codeOnly, "meatySpaceAtWake")) && /bolt && bolt\.kind === "you"/.test(sliceFn(codeOnly, "meatySpaceAtWake")));
check("rival wakeup once-per-getup + CD", /wakeRevArmed/.test(sliceFn(codeOnly, "armRivalWakeReversal")) && /Math\.random\(\) < REVERSAL_AI_CHANCE/.test(sliceFn(codeOnly, "armRivalWakeReversal")) && /revCd > 0/.test(sliceFn(codeOnly, "rivalTryWakeReversal")) && /revCd = REVERSAL_AI_CD/.test(sliceFn(codeOnly, "rivalTryWakeReversal")));
check("guard reversal path still startReversal", /startReversal\(rival\)/.test(sliceFn(codeOnly, "rivalTryReversal")) && !/startWakeReversal/.test(sliceFn(codeOnly, "rivalTryReversal")) && /player\.cut !== "slash"/.test(sliceFn(codeOnly, "meatySpaceAtGuard")) && /player\.cut !== "golpe"/.test(sliceFn(codeOnly, "meatySpaceAtGuard")));
check("player wakeup L unchanged", /wakeupWindow\(player\) && startWakeReversal\(player\)/.test(codeOnly) && /startReversal\(f\)/.test(sliceFn(codeOnly, "startWakeReversal")) && /throwInvulnT = 0/.test(sliceFn(codeOnly, "startWakeReversal")));
check("no 6th button after rival wakeup AI", (htmlTxt.match(/data-tap=/g)||[]).length===3 && !/zone-reversal/.test(htmlTxt) && /data-tap="KeyL"/.test(htmlTxt));
check("throw/super/cancel locked after rival wakeup AI", V.THROW_STARTUP===80 && V.THROW_STAM===0 && V.THROW_RANGE===120 && V.THROW_WAKE_INVULN===80 && V.REVERSAL_STAM===30 && V.REVERSAL_INVULN===120 && V.REVERSAL_AI_CD===1800 && V.REVERSAL_AI_CHANCE===0.4 && V.BOLT_SUPER_DMG===28 && V.BOLT_SUPER_CHIP===6 && V.BOLT_CHIP===2 && V.SLASH_CANCEL_MS===100 && V.GOLPE_CANCEL_MS===100 && V.BOLT_CANCEL_MS===100 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180);

function plantRivalWake(){
  toClose();
  freezeYouAI();
  freezeRivalAI();
  V.player.hp = 100;
  V.rival.hp = 100;
  V.rival.stamina = 100;
  V.rival.stamRegenT = 99999;
  V.rival.guardBreakT = 0;
  V.rival.thrownT = 0;
  V.rival.stunT = 0;
  V.rival.throwInvulnT = V.THROW_WAKE_INVULN;
  V.rival.wakeRev = true;
  V.rival.wakeRevArmed = 0;
  V.rival.reversal = false;
  V.rival.guarding = false;
  V.rival.wantBlock = false;
  V.rival.guardChoice = 0;
  V.rival.phase = "idle";
  V.rival.phaseT = 0;
  V.rival.cut = "slash";
  V.rival.boltPhase = "";
  V.rival.revCd = 0;
  V.rival.revArmed = 0;
  V.rival.pbCd = 99999;
  V.rival.pbArmed = -1;
  V.rival.throwCd = 99999;
  V.rival.boltCd = 99999;
  V.rival.throwTechArmed = -1;
}

// meatySpaceAtWake gates
plantRivalWake();
plantMeatySlash();
check("meatySpaceAtWake true vs Space startup on getup", V.meatySpaceAtWake()===true && V.wakeupWindow(V.rival), `wake=${V.meatySpaceAtWake()} win=${V.wakeupWindow(V.rival)} cut=${V.player.cut} ph=${V.player.phase} inv=${V.rival.throwInvulnT} wr=${V.rival.wakeRev}`);
V.player.phase = "active";
check("meatySpaceAtWake true vs Space active on getup", V.meatySpaceAtWake()===true, `ph=${V.player.phase}`);
V.player.phase = "recovery";
check("meatySpaceAtWake false vs slash recovery", V.meatySpaceAtWake()===false, `ph=${V.player.phase}`);
plantMeatySlash();
V.player.cut = "golpe";
check("meatySpaceAtWake true vs L", V.meatySpaceAtWake()===true, `cut=${V.player.cut}`);
plantMeatySlash();
V.player.cut = "throw";
check("meatySpaceAtWake false vs throw cut", V.meatySpaceAtWake()===false, `cut=${V.player.cut}`);
plantRivalWake();
plantMeatySlash();
V.player.boltPhase = "startup";
check("meatySpaceAtWake false vs dart plant", V.meatySpaceAtWake()===false, `bph=${V.player.boltPhase}`);
V.player.boltPhase = "";
plantRivalWake();
V.player.cut = "slash";
V.player.phase = "idle";
check("meatySpaceAtWake false if player idle (not every wakeup)", V.meatySpaceAtWake()===false && V.wakeupWindow(V.rival), `wake=${V.meatySpaceAtWake()} win=${V.wakeupWindow(V.rival)} ph=${V.player.phase}`);
plantRivalWake();
plantMeatySlash();
V.rival.thrownT = 200;
check("meatySpaceAtWake false while still down", V.meatySpaceAtWake()===false && !V.wakeupWindow(V.rival), `th=${V.rival.thrownT} wake=${V.meatySpaceAtWake()}`);

// rival can wakeup-reversal a meaty slash (forced arm, no tickAI robot)
plantRivalWake();
plantMeatySlash();
V.rival.wakeRevArmed = 1;
V.rival.revCd = 0;
const stamWake0 = V.rival.stamina;
const forceWake = V.rivalTryWakeReversal();
check("forced wakeRevArmed starts wakeup reversal", forceWake && V.rival.reversal && V.rival.cut==="golpe" && V.rival.phase==="startup" && !V.rival.guarding, `ok=${forceWake} cut=${V.rival.cut} ph=${V.rival.phase} rev=${V.rival.reversal} g=${V.rival.guarding}`);
check("wakeup reversal spends 30 stam", stamWake0-V.rival.stamina===V.REVERSAL_STAM, `d=${(stamWake0-V.rival.stamina).toFixed(2)} st0=${stamWake0.toFixed(2)} st=${V.rival.stamina.toFixed(2)}`);
check("wakeup reversal spends throw-invuln + wakeRev + CD", V.rival.throwInvulnT===0 && V.rival.wakeRev===false && V.rival.revCd===V.REVERSAL_AI_CD && V.reversalInvuln(V.rival), `inv=${V.rival.throwInvulnT} wr=${V.rival.wakeRev} cd=${V.rival.revCd} rinv=${V.reversalInvuln(V.rival)}`);

// CD holds, no spam
plantRivalWake();
plantMeatySlash();
V.rival.wakeRevArmed = 1;
V.rival.revCd = 0;
const firstWake = V.rivalTryWakeReversal();
check("first wakeup reversal arms CD", firstWake && V.rival.revCd===V.REVERSAL_AI_CD, `ok=${firstWake} cd=${V.rival.revCd}`);
const cdWake0 = V.rival.revCd;
V.rival.phase = "idle";
V.rival.phaseT = 0;
V.rival.cut = "slash";
V.rival.reversal = false;
V.rival.stamina = 100;
V.rival.throwInvulnT = V.THROW_WAKE_INVULN;
V.rival.wakeRev = true;
V.rival.wakeRevArmed = 1;
plantMeatySlash();
const secondWake = V.rivalTryWakeReversal();
check("rival wakeup CD holds, no spam", cdWake0===V.REVERSAL_AI_CD && secondWake===false && !V.rival.reversal && V.rival.revCd===cdWake0 && V.rival.wakeRev===true, `ok=${secondWake} cd0=${cdWake0} cd=${V.rival.revCd} rev=${V.rival.reversal} wr=${V.rival.wakeRev}`);

// chance miss: not every getup
plantRivalWake();
plantMeatySlash();
V.rival.wakeRevArmed = -1;
V.rival.revCd = 0;
const missWake = V.rivalTryWakeReversal();
check("forced wakeRevArmed -1 does not reverse", missWake===false && !V.rival.reversal && V.rival.phase==="idle" && V.rival.wakeRevArmed===-1 && V.rival.wakeRev===true, `ok=${missWake} rev=${V.rival.reversal} armed=${V.rival.wakeRevArmed} wr=${V.rival.wakeRev} ph=${V.rival.phase}`);

plantRivalWake();
plantMeatySlash();
V.rival.wakeRevArmed = 0;
Math.random = () => 0.99;
const rollMiss = V.rivalTryWakeReversal();
check("40% miss does not wakeup-reversal", rollMiss===false && !V.rival.reversal && V.rival.wakeRevArmed===-1 && V.rival.wakeRev===true, `ok=${rollMiss} rev=${V.rival.reversal} armed=${V.rival.wakeRevArmed} wr=${V.rival.wakeRev}`);

// live tickAI: rival wakeup-reversals a meaty slash
plantRivalWake();
plantMeatySlash();
Math.random = () => 0;
V.rival.wakeRevArmed = 1;
V.rival.revCd = 0;
let wRev=false, wLand=false, wYouHit=false, wClash=false;
const stamLive0 = V.rival.stamina;
for(let i=0;i<90;i++){
  Math.random = () => 0;
  V.rival.boltCd = 99999;
  V.rival.throwCd = 99999;
  holdYouSlashMeaty();
  if(V.rival.reversal || (V.rival.cut==="golpe" && V.rival.phase!=="idle")) wRev=true;
  V.update(STEP);
  if(V.rival.hp<100) wYouHit=true;
  if(V.player.hp<100){ wLand=true; break; }
  if(V.player.clashRec || V.rival.clashRec) wClash=true;
  if(V.hitstopLeft>0 && V.player.hp<100){ wLand=true; break; }
}
drainHitstop();
check("rival wakeup-reversals a meaty slash", wRev && wLand && !wYouHit && !wClash && V.player.hp===90 && V.rival.hp===100 && V.rival.reversal, `rev=${wRev} php=${V.player.hp} rhp=${V.rival.hp} youHit=${wYouHit} clash=${wClash} rcut=${V.rival.cut} rph=${V.rival.phase} pcut=${V.player.cut} pph=${V.player.phase} inv=${V.reversalInvuln(V.rival)} armed=${V.rival.wakeRevArmed}`);
check("live wakeup reversal spends 30", stamLive0-V.rival.stamina>=V.REVERSAL_STAM-2 && stamLive0-V.rival.stamina<=V.REVERSAL_STAM+4, `d=${(stamLive0-V.rival.stamina).toFixed(2)}`);

// does not spam every getup (idle player, armed 1)
plantRivalWake();
V.player.cut = "slash";
V.player.phase = "idle";
V.player.phaseT = 0;
V.rival.wakeRevArmed = 1;
V.rival.revCd = 0;
let idleWake=false;
for(let i=0;i<4;i++){
  V.player.phase = "idle";
  V.player.cut = "slash";
  V.rival.throwInvulnT = V.THROW_WAKE_INVULN;
  V.rival.wakeRev = true;
  V.rival.stunT = 0;
  V.rival.thrownT = 0;
  V.rival.boltCd = 99999;
  V.rival.throwCd = 99999;
  V.update(STEP);
  if(V.rival.reversal || (V.rival.cut==="golpe" && V.rival.phase==="startup")) idleWake=true;
}
check("rival does not wakeup-reversal every getup", !idleWake && !V.rival.reversal && V.rival.wakeRev===true, `rev=${idleWake} rrev=${V.rival.reversal} wr=${V.rival.wakeRev} cut=${V.rival.cut} ph=${V.rival.phase} win=${V.wakeupWindow(V.rival)}`);

// not vs dart
plantRivalWake();
V.player.cut = "slash";
V.player.phase = "idle";
V.player.phaseT = 0;
V.player.stamina = 100;
V.player.stamRegenT = 99999;
V.rival.wakeRevArmed = 1;
V.rival.revCd = 0;
Math.random = () => 0;
const dartWakeOk = V.startBolt(V.player);
let dartWake=false;
for(let i=0;i<30;i++){
  Math.random = () => 0;
  V.rival.wakeRevArmed = 1;
  V.rival.boltCd = 99999;
  V.rival.throwCd = 99999;
  V.update(STEP);
  if(V.rival.reversal || (V.rival.cut==="golpe" && V.rival.phase==="startup")) dartWake=true;
}
check("rival does not wakeup-reversal vs dart", dartWakeOk && !dartWake && !V.rival.reversal, `ok=${dartWakeOk} rev=${dartWake} rrev=${V.rival.reversal} rcut=${V.rival.cut} rph=${V.rival.phase} bph=${V.player.boltPhase} bolt=${!!V.bolt}`);

// not vs throw — invuln still holds, they do not reversal a grab
plantRivalWake();
V.rival.throwTechArmed = -1;
V.player.throwTechArmed = 0;
V.rival.wakeRevArmed = 1;
V.rival.revCd = 0;
Math.random = () => 0;
const throwLockedWake = V.startThrow(V.player);
check("throw does not start on wakeup invuln", throwLockedWake===false && V.throwLocked(V.rival) && V.player.phase==="idle", `start=${throwLockedWake} locked=${V.throwLocked(V.rival)} ph=${V.player.phase} inv=${V.rival.throwInvulnT}`);
V.player.cut = "throw";
V.player.phase = "startup";
V.player.phaseT = 0;
V.player.cutHit = false;
const vsThrowWake = V.rivalTryWakeReversal();
check("rivalTryWakeReversal false vs throw startup", vsThrowWake===false && !V.rival.reversal && V.rival.wakeRev===true && V.rival.phase==="idle", `ok=${vsThrowWake} rev=${V.rival.reversal} wr=${V.rival.wakeRev} cut=${V.rival.cut} ph=${V.rival.phase}`);
let throwWakeRev=false;
for(let i=0;i<4;i++){
  Math.random = () => 0;
  V.rival.throwTechArmed = -1;
  V.rival.wakeRevArmed = 1;
  V.rival.throwInvulnT = V.THROW_WAKE_INVULN;
  V.rival.wakeRev = true;
  V.rival.stunT = 0;
  V.rival.thrownT = 0;
  V.player.cut = "throw";
  V.player.phase = "startup";
  V.rival.boltCd = 99999;
  V.update(STEP);
  if(V.rival.reversal || (V.rival.cut==="golpe" && V.rival.phase==="startup")) throwWakeRev=true;
}
check("rival does not wakeup-reversal vs throw", !throwWakeRev && !V.rival.reversal && V.rival.hp===100, `rev=${throwWakeRev} rrev=${V.rival.reversal} hp=${V.rival.hp} pcut=${V.player.cut} rcut=${V.rival.cut} rph=${V.rival.phase}`);

// throw still beats a bad wakeup reversal
plantRivalWake();
V.rival.throwTechArmed = -1;
V.player.throwTechArmed = 0;
const wakeVsThrow = V.startWakeReversal(V.rival);
const throwVsWakeAI = V.startThrow(V.player);
V.update(STEP);
check("throw vs rival wakeup reversal both started", wakeVsThrow && throwVsWakeAI && V.rival.cut==="golpe" && V.rival.reversal && V.player.cut==="throw" && V.player.phase==="startup" && V.rival.throwInvulnT===0, `rok=${wakeVsThrow} tok=${throwVsWakeAI} rcut=${V.rival.cut} rrev=${V.rival.reversal} pcut=${V.player.cut} pph=${V.player.phase} inv=${V.rival.throwInvulnT}`);
let throwBeatWakeAI=false;
for(let i=0;i<80;i++){
  V.rival.throwTechArmed = -1;
  V.update(STEP);
  if(V.rival.thrownT>0 || V.rival.hp<100){ throwBeatWakeAI=true; break; }
}
drainHitstop();
check("throw still beats a bad wakeup reversal", throwBeatWakeAI && V.rival.hp===80 && V.rival.thrownT>0 && V.player.cut==="throw" && V.player.cutHit && V.rival.hp!==90, `beat=${throwBeatWakeAI} rhp=${V.rival.hp} th=${V.rival.thrownT} pch=${V.player.cutHit} pcut=${V.player.cut} rcut=${V.rival.cut}`);

// player wakeup unchanged after rival AI
plantYouWake();
const pWakeStam0 = V.player.stamina;
V.golpeEdge = true;
V.update(STEP);
check("player wakeup L unchanged after rival AI", V.player.cut==="golpe" && V.player.phase==="startup" && V.player.reversal && !V.player.guarding && V.player.cut!=="throw" && pWakeStam0-V.player.stamina===V.REVERSAL_STAM && V.player.throwInvulnT===0 && V.player.wakeRev===false, `cut=${V.player.cut} ph=${V.player.phase} rev=${V.player.reversal} d=${(pWakeStam0-V.player.stamina).toFixed(2)} inv=${V.player.throwInvulnT} wr=${V.player.wakeRev}`);

// guard reversal still works
toClose();
raiseYouGuard();
fireReversalChord();
V.update(STEP);
check("player guard reversal unchanged after rival wakeup AI", V.player.cut==="golpe" && V.player.phase==="startup" && V.player.reversal && V.player.cut!=="throw", `cut=${V.player.cut} rev=${V.player.reversal} ph=${V.player.phase}`);
V.keys.delete("KeyS");

// rival meaty-Space guard reversal still works
toClose();
freezeYouAI();
armRivalReversalAI();
plantMeatySlash();
V.rival.revArmed = 1;
V.rival.revCd = 0;
const guardStill = V.rivalTryReversal();
check("rival meaty-Space reversal untouched after wakeup AI", guardStill && V.rival.reversal && V.rival.cut==="golpe" && V.rival.phase==="startup" && !V.rival.guarding, `ok=${guardStill} cut=${V.rival.cut} ph=${V.rival.phase} rev=${V.rival.reversal} g=${V.rival.guarding}`);

check("locked layers after rival wakeup AI", V.REVERSAL_AI_CD===1800 && V.REVERSAL_AI_CHANCE===0.4 && V.REVERSAL_STAM===30 && V.REVERSAL_INVULN===120 && V.THROW_WAKE_INVULN===80 && V.THROW_STARTUP===80 && V.THROW_STAM===0 && V.THROW_RANGE===120 && V.BOLT_SUPER_DMG===28 && V.BOLT_SUPER_CHIP===6 && V.BOLT_CHIP===2 && V.SLASH_CANCEL_MS===100 && V.GOLPE_CANCEL_MS===100 && V.BOLT_CANCEL_MS===100 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180);

check("cache v=296 after rival wakeup AI", /game.js\?v=350/.test(htmlTxt));

// --- v145 rival reversal vs meaty L (guard + wakeup) ---
check("v145 reversal rates/CD locked", V.REVERSAL_AI_CD===1800 && V.REVERSAL_AI_CHANCE===0.4 && V.REVERSAL_STAM===30 && V.REVERSAL_INVULN===120 && V.REVERSAL_AI_CD===V.THROW_AI_CD);
check("v145 player reversal/throw/super/stam/cancel locked", V.THROW_STARTUP===80 && V.THROW_ACTIVE===40 && V.THROW_RECOVERY===220 && V.THROW_DMG===20 && V.THROW_STAM===0 && V.THROW_RANGE===120 && V.THROW_WAKE_INVULN===80 && V.BOLT_SUPER_DMG===28 && V.BOLT_SUPER_CHIP===6 && V.BOLT_CHIP===2 && V.SLASH_CANCEL_MS===100 && V.GOLPE_CANCEL_MS===100 && V.BOLT_CANCEL_MS===100 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && /if \(player\.guarding\) startReversal\(player\)/.test(codeOnly) && /wakeupWindow\(player\) && startWakeReversal\(player\)/.test(codeOnly) && /reversalEdge = true/.test(srcTxt) && /actionHeld\("guard"\)/.test(srcTxt));
check("v145 meaty L is slash or golpe not dart/throw", /player\.cut !== "slash" && player\.cut !== "golpe"/.test(sliceFn(codeOnly, "meatySpaceAtGuard")) && /player\.cut !== "slash" && player\.cut !== "golpe"/.test(sliceFn(codeOnly, "meatySpaceAtWake")) && /boltPhase === "startup"/.test(sliceFn(codeOnly, "meatySpaceAtGuard")) && /boltPhase === "startup"/.test(sliceFn(codeOnly, "meatySpaceAtWake")));
check("v145 no 6th button", (htmlTxt.match(/data-tap=/g)||[]).length===3 && !/zone-reversal/.test(htmlTxt) && /data-tap="KeyL"/.test(htmlTxt));

function plantMeatyGolpe(){
  holdYouSlashMeaty();
  V.player.cut = "golpe";
  V.player.phase = "startup";
  V.player.phaseT = 90;
  V.player.cutHit = false;
  V.player.clashRec = false;
  V.player.reversal = false;
}

function holdYouGolpeMeaty(){
  holdYouSlashMeaty();
  if(V.player.cut!=="golpe" && V.player.phase==="idle") plantMeatyGolpe();
}

// gates: still Space, now L, still not throw/dart/recovery
toClose();
freezeYouAI();
armRivalReversalAI();
plantMeatySlash();
check("v145 meatySpaceAtGuard still true vs Space", V.meatySpaceAtGuard()===true, `cut=${V.player.cut} ph=${V.player.phase}`);
plantMeatyGolpe();
check("v145 meatySpaceAtGuard true vs meaty L", V.meatySpaceAtGuard()===true && V.rival.guarding && V.player.cut==="golpe" && (V.player.phase==="startup"||V.player.phase==="active"), `g=${V.rival.guarding} cut=${V.player.cut} ph=${V.player.phase} gap=${V.absGap().toFixed(1)}`);
V.player.phase = "active";
check("v145 meatySpaceAtGuard true vs L active", V.meatySpaceAtGuard()===true, `ph=${V.player.phase}`);
V.player.phase = "recovery";
check("v145 meatySpaceAtGuard false vs L recovery", V.meatySpaceAtGuard()===false, `ph=${V.player.phase}`);
plantMeatyGolpe();
V.player.cut = "throw";
check("v145 meatySpaceAtGuard false vs throw cut", V.meatySpaceAtGuard()===false, `cut=${V.player.cut}`);
plantMeatyGolpe();
V.player.boltPhase = "startup";
check("v145 meatySpaceAtGuard false vs dart plant on L", V.meatySpaceAtGuard()===false, `bph=${V.player.boltPhase}`);
V.player.boltPhase = "";

plantRivalWake();
plantMeatySlash();
check("v145 meatySpaceAtWake still true vs Space", V.meatySpaceAtWake()===true && V.wakeupWindow(V.rival), `wake=${V.meatySpaceAtWake()} cut=${V.player.cut}`);
plantMeatyGolpe();
check("v145 meatySpaceAtWake true vs meaty L", V.meatySpaceAtWake()===true && V.wakeupWindow(V.rival) && V.player.cut==="golpe", `wake=${V.meatySpaceAtWake()} win=${V.wakeupWindow(V.rival)} cut=${V.player.cut} ph=${V.player.phase}`);
V.player.phase = "recovery";
check("v145 meatySpaceAtWake false vs L recovery", V.meatySpaceAtWake()===false, `ph=${V.player.phase}`);
plantMeatyGolpe();
V.player.cut = "throw";
check("v145 meatySpaceAtWake false vs throw cut", V.meatySpaceAtWake()===false, `cut=${V.player.cut}`);
plantRivalWake();
plantMeatyGolpe();
V.player.boltPhase = "startup";
check("v145 meatySpaceAtWake false vs dart on L", V.meatySpaceAtWake()===false, `bph=${V.player.boltPhase}`);
V.player.boltPhase = "";

// rival can reversal a meaty L while guarding
toClose();
freezeYouAI();
armRivalReversalAI();
plantMeatyGolpe();
Math.random = () => 0;
const stamL0 = V.rival.stamina;
const gapL = V.absGap();
check("v145 rival meaty L setup guarding", V.rival.guarding && V.player.cut==="golpe" && V.player.phase==="startup" && gapL<=V.bladeReach(V.rival)+V.LUNGE_PX+V.LUNGE_PX, `g=${V.rival.guarding} pcut=${V.player.cut} pph=${V.player.phase} gap=${gapL.toFixed(1)}`);
let lRev=false, lLand=false, lYouHit=false, lClash=false;
for(let i=0;i<90;i++){
  Math.random = () => 0;
  V.rival.boltCd = 99999;
  V.rival.throwCd = 99999;
  holdYouSlashMeaty();
  if(V.player.cut!=="golpe" && V.player.phase==="idle") plantMeatyGolpe();
  if(V.rival.reversal || (V.rival.cut==="golpe" && V.rival.phase!=="idle")) lRev=true;
  V.update(STEP);
  if(V.rival.hp<100) lYouHit=true;
  if(V.player.hp<100){ lLand=true; break; }
  if(V.player.clashRec || V.rival.clashRec) lClash=true;
  if(V.hitstopLeft>0 && V.player.hp<100){ lLand=true; break; }
}
drainHitstop();
check("rival reversals meaty L while guarding", lRev && lLand && !lYouHit && !lClash && V.player.hp===90 && V.rival.hp===100 && V.rival.reversal, `rev=${lRev} php=${V.player.hp} rhp=${V.rival.hp} youHit=${lYouHit} clash=${lClash} rcut=${V.rival.cut} rph=${V.rival.phase} pcut=${V.player.cut} pph=${V.player.phase} inv=${V.reversalInvuln(V.rival)} armed=${V.rival.revArmed}`);
check("rival L-guard reversal spends 30 stam", stamL0-V.rival.stamina>=V.REVERSAL_STAM-2 && stamL0-V.rival.stamina<=V.REVERSAL_STAM+4, `d=${(stamL0-V.rival.stamina).toFixed(2)} st0=${stamL0.toFixed(2)} st=${V.rival.stamina.toFixed(2)}`);

// forced arm vs L
toClose();
freezeYouAI();
armRivalReversalAI();
plantMeatyGolpe();
V.rival.revArmed = 1;
V.rival.revCd = 0;
const forceL = V.rivalTryReversal();
check("forced revArmed starts vs meaty L", forceL && V.rival.reversal && V.rival.cut==="golpe" && V.rival.phase==="startup" && !V.rival.guarding && V.rival.revCd===V.REVERSAL_AI_CD, `ok=${forceL} cut=${V.rival.cut} ph=${V.rival.phase} rev=${V.rival.reversal} cd=${V.rival.revCd}`);

// does not spam vs L
toClose();
freezeYouAI();
armRivalReversalAI();
plantMeatyGolpe();
V.rival.revArmed = 1;
V.rival.revCd = 0;
const firstL = V.rivalTryReversal();
check("first L-guard reversal arms CD", firstL && V.rival.revCd===V.REVERSAL_AI_CD, `ok=${firstL} cd=${V.rival.revCd}`);
const cdL0 = V.rival.revCd;
V.rival.wantBlock = true;
V.rival.guarding = true;
V.rival.guardChoice = 1;
V.rival.phase = "idle";
V.rival.phaseT = 0;
V.rival.cut = "slash";
V.rival.reversal = false;
V.rival.stamina = 100;
plantMeatyGolpe();
V.rival.revArmed = 1;
const secondL = V.rivalTryReversal();
check("rival L-guard reversal CD holds, no spam", cdL0===V.REVERSAL_AI_CD && secondL===false && !V.rival.reversal && V.rival.guarding && V.rival.revCd===cdL0, `ok=${secondL} cd0=${cdL0} cd=${V.rival.revCd} rev=${V.rival.reversal} g=${V.rival.guarding}`);

// not every L (40% miss)
toClose();
freezeYouAI();
armRivalReversalAI();
plantMeatyGolpe();
V.rival.revArmed = -1;
V.rival.revCd = 0;
const missL = V.rivalTryReversal();
check("forced revArmed -1 does not reverse vs L", missL===false && !V.rival.reversal && V.rival.guarding && V.rival.phase==="idle" && V.rival.revArmed===-1, `ok=${missL} rev=${V.rival.reversal} armed=${V.rival.revArmed} g=${V.rival.guarding}`);

toClose();
freezeYouAI();
armRivalReversalAI();
plantMeatyGolpe();
V.rival.revArmed = 0;
Math.random = () => 0.99;
const rollMissL = V.rivalTryReversal();
check("40% miss does not reversal vs L", rollMissL===false && !V.rival.reversal && V.rival.revArmed===-1 && V.rival.guarding, `ok=${rollMissL} rev=${V.rival.reversal} armed=${V.rival.revArmed} g=${V.rival.guarding}`);

// wakeup L
plantRivalWake();
plantMeatyGolpe();
V.rival.wakeRevArmed = 1;
V.rival.revCd = 0;
const stamWakeL0 = V.rival.stamina;
const forceWakeL = V.rivalTryWakeReversal();
check("forced wakeRevArmed starts vs meaty L", forceWakeL && V.rival.reversal && V.rival.cut==="golpe" && V.rival.phase==="startup" && !V.rival.guarding, `ok=${forceWakeL} cut=${V.rival.cut} ph=${V.rival.phase} rev=${V.rival.reversal} g=${V.rival.guarding}`);
check("wakeup L reversal spends 30 + invuln + CD", stamWakeL0-V.rival.stamina===V.REVERSAL_STAM && V.rival.throwInvulnT===0 && V.rival.wakeRev===false && V.rival.revCd===V.REVERSAL_AI_CD && V.reversalInvuln(V.rival), `d=${(stamWakeL0-V.rival.stamina).toFixed(2)} inv=${V.rival.throwInvulnT} wr=${V.rival.wakeRev} cd=${V.rival.revCd} rinv=${V.reversalInvuln(V.rival)}`);

// wakeup CD no spam vs L
plantRivalWake();
plantMeatyGolpe();
V.rival.wakeRevArmed = 1;
V.rival.revCd = 0;
const firstWakeL = V.rivalTryWakeReversal();
check("first wakeup L reversal arms CD", firstWakeL && V.rival.revCd===V.REVERSAL_AI_CD, `ok=${firstWakeL} cd=${V.rival.revCd}`);
const cdWakeL0 = V.rival.revCd;
V.rival.phase = "idle";
V.rival.phaseT = 0;
V.rival.cut = "slash";
V.rival.reversal = false;
V.rival.stamina = 100;
V.rival.throwInvulnT = V.THROW_WAKE_INVULN;
V.rival.wakeRev = true;
V.rival.wakeRevArmed = 1;
plantMeatyGolpe();
const secondWakeL = V.rivalTryWakeReversal();
check("rival wakeup L CD holds, no spam", cdWakeL0===V.REVERSAL_AI_CD && secondWakeL===false && !V.rival.reversal && V.rival.revCd===cdWakeL0 && V.rival.wakeRev===true, `ok=${secondWakeL} cd0=${cdWakeL0} cd=${V.rival.revCd} rev=${V.rival.reversal} wr=${V.rival.wakeRev}`);

// live tickAI wakeup vs meaty L
plantRivalWake();
plantMeatyGolpe();
Math.random = () => 0;
V.rival.wakeRevArmed = 1;
V.rival.revCd = 0;
let wLRev=false, wLLand=false, wLYouHit=false, wLClash=false;
const stamLiveL0 = V.rival.stamina;
for(let i=0;i<90;i++){
  Math.random = () => 0;
  V.rival.boltCd = 99999;
  V.rival.throwCd = 99999;
  holdYouSlashMeaty();
  if(V.player.cut!=="golpe" && V.player.phase==="idle") plantMeatyGolpe();
  if(V.rival.reversal || (V.rival.cut==="golpe" && V.rival.phase!=="idle")) wLRev=true;
  V.update(STEP);
  if(V.rival.hp<100) wLYouHit=true;
  if(V.player.hp<100){ wLLand=true; break; }
  if(V.player.clashRec || V.rival.clashRec) wLClash=true;
  if(V.hitstopLeft>0 && V.player.hp<100){ wLLand=true; break; }
}
drainHitstop();
check("rival wakeup-reversals a meaty L", wLRev && wLLand && !wLYouHit && !wLClash && V.player.hp===90 && V.rival.hp===100 && V.rival.reversal, `rev=${wLRev} php=${V.player.hp} rhp=${V.rival.hp} youHit=${wLYouHit} clash=${wLClash} rcut=${V.rival.cut} rph=${V.rival.phase} pcut=${V.player.cut} pph=${V.player.phase} inv=${V.reversalInvuln(V.rival)} armed=${V.rival.wakeRevArmed}`);
check("live wakeup L reversal spends 30", stamLiveL0-V.rival.stamina>=V.REVERSAL_STAM-2 && stamLiveL0-V.rival.stamina<=V.REVERSAL_STAM+4, `d=${(stamLiveL0-V.rival.stamina).toFixed(2)}`);

// still vs Space after L pass
toClose();
freezeYouAI();
armRivalReversalAI();
plantMeatySlash();
V.rival.revArmed = 1;
V.rival.revCd = 0;
const stillSpace = V.rivalTryReversal();
check("rival still reversals meaty Space after L pass", stillSpace && V.rival.reversal && V.rival.cut==="golpe" && V.rival.phase==="startup", `ok=${stillSpace} cut=${V.rival.cut} ph=${V.rival.phase} rev=${V.rival.reversal} pcut=${V.player.cut}`);

plantRivalWake();
plantMeatySlash();
V.rival.wakeRevArmed = 1;
V.rival.revCd = 0;
const stillSpaceWake = V.rivalTryWakeReversal();
check("rival still wakeup-reversals meaty Space after L pass", stillSpaceWake && V.rival.reversal && V.rival.cut==="golpe" && V.rival.phase==="startup", `ok=${stillSpaceWake} cut=${V.rival.cut} ph=${V.rival.phase} rev=${V.rival.reversal} pcut=${V.player.cut}`);

// throw still beats a bad L-guard reversal (same startReversal path as wakeup;
// clean edges + idle so startThrow is not blocked by leftover plant/golpeEdge)
toClose();
freezeYouAI();
armRivalReversalAI();
V.golpeEdge = false;
V.attackEdge = false;
V.throwEdge = false;
V.reversalEdge = false;
V.boltEdge = false;
V.keys.clear();
V.rival.throwTechArmed = -1;
V.player.throwTechArmed = 0;
V.player.cut = "slash";
V.player.phase = "idle";
V.player.phaseT = 0;
V.player.boltPhase = "";
V.player.falling = false;
V.player.stunT = 0;
V.player.thrownT = 0;
V.player.throwInvulnT = 0;
V.player.reversal = false;
V.player.guarding = false;
V.player.clashRec = false;
V.player.techRec = false;
V.rival.revArmed = 1;
V.rival.revCd = 0;
const revLVsThrow = V.startReversal(V.rival);
const throwVsLRev = V.startThrow(V.player);
V.update(STEP);
check("throw vs rival L-guard reversal both started", revLVsThrow && throwVsLRev && V.rival.cut==="golpe" && V.rival.reversal && V.player.cut==="throw" && V.player.phase==="startup", `rok=${revLVsThrow} tok=${throwVsLRev} rcut=${V.rival.cut} rrev=${V.rival.reversal} pcut=${V.player.cut} pph=${V.player.phase}`);
let throwBeatLRev=false;
for(let i=0;i<80;i++){
  V.rival.throwTechArmed = -1;
  V.update(STEP);
  if(V.rival.thrownT>0 || V.rival.hp<100){ throwBeatLRev=true; break; }
}
drainHitstop();
check("throw still beats a bad L-guard reversal", throwBeatLRev && V.rival.hp===80 && V.rival.thrownT>0 && V.player.cut==="throw" && V.player.cutHit && V.rival.hp!==90, `beat=${throwBeatLRev} rhp=${V.rival.hp} th=${V.rival.thrownT} pch=${V.player.cutHit} pcut=${V.player.cut} rcut=${V.rival.cut}`);

// throw still beats a bad wakeup L reversal
plantRivalWake();
V.rival.throwTechArmed = -1;
V.player.throwTechArmed = 0;
const wakeLVsThrow = V.startWakeReversal(V.rival);
const throwVsWakeL = V.startThrow(V.player);
V.update(STEP);
check("throw vs rival wakeup L reversal both started", wakeLVsThrow && throwVsWakeL && V.rival.cut==="golpe" && V.rival.reversal && V.player.cut==="throw" && V.player.phase==="startup" && V.rival.throwInvulnT===0, `rok=${wakeLVsThrow} tok=${throwVsWakeL} rcut=${V.rival.cut} rrev=${V.rival.reversal} pcut=${V.player.cut} pph=${V.player.phase} inv=${V.rival.throwInvulnT}`);
let throwBeatWakeL=false;
for(let i=0;i<80;i++){
  V.rival.throwTechArmed = -1;
  V.update(STEP);
  if(V.rival.thrownT>0 || V.rival.hp<100){ throwBeatWakeL=true; break; }
}
drainHitstop();
check("throw still beats a bad wakeup L reversal", throwBeatWakeL && V.rival.hp===80 && V.rival.thrownT>0 && V.player.cut==="throw" && V.player.cutHit && V.rival.hp!==90, `beat=${throwBeatWakeL} rhp=${V.rival.hp} th=${V.rival.thrownT} pch=${V.player.cutHit} pcut=${V.player.cut} rcut=${V.rival.cut}`);

// player tools unchanged
toClose();
raiseYouGuard();
fireReversalChord();
V.update(STEP);
check("player guard reversal unchanged after meaty L AI", V.player.cut==="golpe" && V.player.phase==="startup" && V.player.reversal && V.player.cut!=="throw", `cut=${V.player.cut} rev=${V.player.reversal} ph=${V.player.phase}`);
V.keys.delete("KeyS");

plantYouWake();
V.golpeEdge = true;
V.update(STEP);
check("player wakeup L unchanged after meaty L AI", V.player.cut==="golpe" && V.player.phase==="startup" && V.player.reversal && V.player.cut!=="throw", `cut=${V.player.cut} rev=${V.player.reversal} ph=${V.player.phase}`);

check("locked layers after meaty L reversal", V.REVERSAL_AI_CD===1800 && V.REVERSAL_AI_CHANCE===0.4 && V.REVERSAL_STAM===30 && V.REVERSAL_INVULN===120 && V.THROW_WAKE_INVULN===80 && V.THROW_STARTUP===80 && V.THROW_STAM===0 && V.THROW_RANGE===120 && V.BOLT_SUPER_DMG===28 && V.BOLT_SUPER_CHIP===6 && V.BOLT_CHIP===2 && V.SLASH_CANCEL_MS===100 && V.GOLPE_CANCEL_MS===100 && V.BOLT_CANCEL_MS===100 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180);

check("cache v=296 after meaty L reversal", /game.js\?v=350/.test(htmlTxt));

// --- v146 pushblock: hold S, tap away ---
check("pushblock cost is a real stam chunk", V.PUSHBLOCK_STAM===25 && V.PUSHBLOCK_STAM>=20 && V.PUSHBLOCK_STAM<=30 && V.PUSHBLOCK_STAM>0 && V.PUSHBLOCK_STAM!==V.REVERSAL_STAM, `st=${V.PUSHBLOCK_STAM}`);
check("pushblock farther than a block push", V.PUSHBLOCK_PX===240 && V.PUSHBLOCK_PX>V.bodyAABB(V.player).w && V.PUSHBLOCK_PX>V.KNOCK_PX, `px=${V.PUSHBLOCK_PX} hurt=${V.bodyAABB(V.player).w.toFixed(1)}`);
check("pushblock does not retune regen/block/reversal/throw/super/chip/cancel", V.STAMINA_REGEN===25 && V.STAMINA_REGEN_THREAT===10 && V.STAMINA_REGEN_LOCK===1000 && V.STAMINA_REGEN_DELAY===200 && V.STAMINA_BLOCK===20 && V.STAMINA_DRAIN===40 && V.REVERSAL_STAM===30 && V.REVERSAL_INVULN===120 && V.THROW_STARTUP===80 && V.THROW_ACTIVE===40 && V.THROW_RECOVERY===220 && V.THROW_STAM===0 && V.THROW_RANGE===120 && V.BOLT_SUPER_DMG===28 && V.BOLT_SUPER_CHIP===6 && V.BOLT_CHIP===2 && V.SLASH_CANCEL_MS===100 && V.GOLPE_CANCEL_MS===100 && V.BOLT_CANCEL_MS===100);
check("pushblock is away tap while guard not a 6th button", /function tryPushblock/.test(codeOnly) && /PUSHBLOCK_STAM/.test(srcTxt) && (htmlTxt.match(/data-tap=/g)||[]).length===3 && !/zone-push|zone-pushblock/.test(htmlTxt) && /feedKey\("KeyA"/.test(srcTxt) && /feedKey\("KeyD"/.test(srcTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("walk while not guarding still requires idle ungarded", /if \(!player\.guarding && player\.phase === "idle"/.test(codeOnly));

function parkPushRoom(){
  freezeYouAI();
  freezeRivalAI();
  holdRivalStill();
  V.player.hp = 100;
  V.rival.hp = 100;
  V.player.stamina = 100;
  V.player.stamRegenT = 99999;
  V.player.guardBreakT = 0;
  V.player.stunT = 0;
  V.player.falling = false;
  V.player.boltPhase = "";
  V.player.reversal = false;
  V.player.cut = "slash";
  V.player.phase = "idle";
  V.player.phaseT = 0;
  V.player.pushT = 0;
  V.player.pushVel = 0;
  V.player.x = 520;
  V.rival.x = 880;
  V.player.facing = 1;
  V.rival.facing = -1;
  V.keys.clear();
  V.keepApart();
}

function raiseGuardInRoom(){
  parkPushRoom();
  V.keys.add("KeyS");
  V.update(STEP);
  V.player.stamina = 100;
  V.player.stamRegenT = 99999;
}

function waitPush(frames){
  for(let i=0;i<frames;i++){
    holdRivalStill();
    V.rival.standGoal = 99999;
    V.rival.closing = false;
    V.rival.gait = 0;
    V.update(STEP);
  }
}

toClose();
raiseGuardInRoom();
check("away setup facing right", V.player.guarding && V.player.facing>0 && V.awayWalkDir(V.player)===-1, `g=${V.player.guarding} face=${V.player.facing} away=${V.awayWalkDir(V.player)}`);
const pbHurt = V.bodyAABB(V.player).w;
const pbX0 = V.player.x;
const pbG0 = V.absGap();
const pbSt0 = V.player.stamina;
V.keys.add("KeyA");
V.update(STEP);
const pbSt1 = V.player.stamina;
const pbSpent = pbSt0 - pbSt1;
check("away+S while blocking spends stam", V.player.guarding && pbSpent>=V.PUSHBLOCK_STAM && pbSpent<=V.PUSHBLOCK_STAM+2 && V.player.pushT>0, `g=${V.player.guarding} spent=${pbSpent.toFixed(2)} pushT=${V.player.pushT} st0=${pbSt0.toFixed(2)} st=${pbSt1.toFixed(2)}`);
waitPush(12);
const pbDx = pbX0 - V.player.x;
const pbDg = V.absGap() - pbG0;
check("away+S while blocking pushes extra", pbDx>=pbHurt+8 && pbDx>=V.PUSHBLOCK_PX-30 && pbDx<=V.PUSHBLOCK_PX+20 && pbDg>=pbHurt && V.player.gait===0, `dx=${pbDx.toFixed(1)} dg=${pbDg.toFixed(1)} hurt=${pbHurt.toFixed(1)} px=${V.PUSHBLOCK_PX} gait=${V.player.gait} x0=${pbX0.toFixed(1)} x=${V.player.x.toFixed(1)}`);
V.keys.clear();

// toward is not pushblock
toClose();
raiseGuardInRoom();
const twX0 = V.player.x;
const twG0 = V.absGap();
const twSt0 = V.player.stamina;
V.keys.add("KeyD");
V.update(STEP);
const twSpent = twSt0 - V.player.stamina;
waitPush(12);
const twDx = Math.abs(V.player.x - twX0);
check("toward while blocking is not pushblock", V.player.guarding && twSpent<8 && V.player.pushT===0 && twDx<12 && Math.abs(V.absGap()-twG0)<16 && V.player.cut!=="throw", `g=${V.player.guarding} spent=${twSpent.toFixed(2)} pushT=${V.player.pushT} dx=${twDx.toFixed(1)} dg=${(V.absGap()-twG0).toFixed(1)}`);
V.keys.clear();

// low stam cannot
toClose();
raiseGuardInRoom();
V.player.stamina = 20;
V.player.stamRegenT = 99999;
const loX0 = V.player.x;
const loSt0 = V.player.stamina;
V.keys.add("KeyA");
V.update(STEP);
const loSpent = loSt0 - V.player.stamina;
waitPush(12);
const loDx = Math.abs(V.player.x - loX0);
check("low stam cannot pushblock", V.player.guarding && loSt0<V.PUSHBLOCK_STAM && loSpent<8 && loDx<12 && V.player.pushT===0, `g=${V.player.guarding} st0=${loSt0.toFixed(2)} spent=${loSpent.toFixed(2)} dx=${loDx.toFixed(1)} pushT=${V.player.pushT}`);
V.keys.clear();

// empty stam cannot
toClose();
raiseGuardInRoom();
V.player.stamina = 0;
V.player.stamRegenT = 99999;
V.player.guardBreakT = 0;
V.player.guarding = true;
V.keys.add("KeyS");
V.keys.add("KeyA");
const emX0 = V.player.x;
const emSt0 = V.player.stamina;
V.update(STEP);
check("empty stam cannot pushblock", V.player.stamina<=0.05 && Math.abs(V.player.x-emX0)<12 && (V.player.pushT===0 || V.player.guardBreakT>0 && Math.abs((V.player.pushVel||0)*V.player.pushT)<V.PUSHBLOCK_PX*0.5), `st=${V.player.stamina.toFixed(2)} dx=${(V.player.x-emX0).toFixed(1)} pushT=${V.player.pushT} br=${V.player.guardBreakT}`);
V.keys.clear();

// walk-into-guard is not a teleport
toClose();
parkPushRoom();
V.keys.add("KeyA");
V.update(STEP);
const wigX = V.player.x;
const wigSt = V.player.stamina;
const wigWalk = V.player.gait!==0;
V.keys.add("KeyS");
V.update(STEP);
const wigDx = Math.abs(V.player.x - wigX);
const wigSpent = wigSt - V.player.stamina;
check("walk-into-guard is not pushblock teleport", wigWalk && V.player.guarding && wigDx<20 && wigSpent<8 && V.player.pushT===0 && V.player.gait===0, `walk0=${wigWalk} g=${V.player.guarding} dx=${wigDx.toFixed(1)} spent=${wigSpent.toFixed(2)} pushT=${V.player.pushT} gait=${V.player.gait}`);
V.keys.clear();

// walk while not guarding still walks (A is not a free push)
toClose();
parkPushRoom();
V.player.stamina = 80;
V.player.stamRegenT = 99999;
const wkX0 = V.player.x;
const wkSt0 = V.player.stamina;
V.keys.add("KeyA");
waitPush(6);
const wkDx = wkX0 - V.player.x;
const wkSpent = wkSt0 - V.player.stamina;
check("walk while not guarding is still walk", !V.player.guarding && V.player.gait===-1 && wkDx>8 && wkDx<80 && wkSpent<4 && V.player.pushT===0, `g=${V.player.guarding} gait=${V.player.gait} dx=${wkDx.toFixed(1)} spent=${wkSpent.toFixed(2)} pushT=${V.player.pushT}`);
V.keys.clear();

// throw chord Space+S close stays throw, not pushblock
toClose();
raiseGuardInRoom();
V.rival.stamina = 80;
V.rival.stamRegenT = 99999;
V.rival.wantBlock = true;
V.rival.guarding = true;
V.rival.guardChoice = 1;
V.rival.throwTechArmed = -1;
V.player.x = V.rival.x - 160;
V.keepApart();
V.keys.add("KeyS");
V.keys.add("Space");
V.throwEdge = true;
let pbThrow=false;
for(let i=0;i<80;i++){
  holdRivalStill();
  V.rival.wantBlock = true;
  V.rival.guarding = true;
  V.rival.guardChoice = 1;
  V.rival.throwTechArmed = -1;
  V.update(STEP);
  if(V.hitstopLeft>0 || V.rival.hp<100 || V.rival.thrownT>0){ pbThrow=true; break; }
}
drainHitstop();
check("throw chord still throw not pushblock", pbThrow && V.player.cut==="throw" && V.player.cutHit && V.rival.hp===80 && V.rival.thrownT>0 && V.player.cut!=="golpe", `hit=${pbThrow} cut=${V.player.cut} ch=${V.player.cutHit} rhp=${V.rival.hp} th=${V.rival.thrownT}`);
V.keys.clear();

// reversal still reversal
toClose();
raiseGuardInRoom();
const revSt0 = V.player.stamina;
fireReversalChord();
V.update(STEP);
check("reversal still reversal not pushblock", V.player.cut==="golpe" && V.player.phase==="startup" && V.player.reversal && !V.player.guarding && V.player.cut!=="throw" && Math.abs((revSt0-V.player.stamina)-V.REVERSAL_STAM)<3, `cut=${V.player.cut} rev=${V.player.reversal} ph=${V.player.phase} spent=${(revSt0-V.player.stamina).toFixed(2)}`);
V.keys.clear();

// pad hold guarda + tap away walk
toClose();
raiseGuardInRoom();
const zsPb = fakeZone();
const zaPb = fakeZone();
V.padDown("KeyS", 401, zsPb);
V.update(STEP);
V.player.stamina = 100;
V.player.stamRegenT = 99999;
const padX0 = V.player.x;
const padSt0 = V.player.stamina;
V.padDown("KeyA", 402, zaPb);
V.update(STEP);
const padSpent = padSt0 - V.player.stamina;
waitPush(12);
const padDx = padX0 - V.player.x;
check("pad hold guarda tap away pushblocks", padSpent>=V.PUSHBLOCK_STAM-1 && padSpent<=V.PUSHBLOCK_STAM+2 && padDx>=pbHurt && V.player.gait===0, `spent=${padSpent.toFixed(2)} dx=${padDx.toFixed(1)} gait=${V.player.gait} g=${V.player.guarding}`);
V.padUp(402);
V.padUp(401);

check("v146 no 6th button", (htmlTxt.match(/data-tap=/g)||[]).length===3 && !/zone-pushblock/.test(htmlTxt) && /data-tap="KeyL"/.test(htmlTxt));
check("cache v=296 after pushblock", /game.js\?v=350/.test(htmlTxt));

// --- v147 rival pushblock: same away extra push, not a robot ---
check("rival pushblock uses tryPushblock away", /function rivalPushblockOnBlock/.test(codeOnly) && /tryPushblock\(rival, awayWalkDir\(rival\)\)/.test(sliceFn(codeOnly, "rivalPushblockOnBlock")) && /PUSHBLOCK_AI_CD/.test(srcTxt) && /PUSHBLOCK_AI_CHANCE/.test(srcTxt) && /function rivalTryPushblock/.test(codeOnly));
check("rival pushblock CD 1800 chance 40", V.PUSHBLOCK_AI_CD===1800 && V.PUSHBLOCK_AI_CHANCE===0.4 && V.PUSHBLOCK_STAM===25 && V.PUSHBLOCK_PX===240 && V.PUSHBLOCK_AI_CD===V.REVERSAL_AI_CD && V.PUSHBLOCK_AI_CHANCE===V.REVERSAL_AI_CHANCE);
check("rival pushblock once-per-swing + CD", /pbArmed/.test(sliceFn(codeOnly, "armRivalPushblock")) && /Math\.random\(\) < PUSHBLOCK_AI_CHANCE/.test(sliceFn(codeOnly, "armRivalPushblock")) && /armRivalPushblock\(\)/.test(sliceFn(codeOnly, "rivalPushblockOnBlock")) && /pbCd = PUSHBLOCK_AI_CD/.test(sliceFn(codeOnly, "rivalPushblockOnBlock")) && /pbCd > 0/.test(sliceFn(codeOnly, "rivalPushblockOnBlock")));
check("rival pushblock live path has no meatySpaceAtGuard gate", !/meatySpaceAtGuard/.test(sliceFn(codeOnly, "rivalPushblockOnBlock")) && /meatySpaceAtGuard\(\)/.test(sliceFn(codeOnly, "rivalTryPushblock")) && /guardChoice !== 1/.test(sliceFn(codeOnly, "rivalTryPushblock")));
check("rival pushblock not toward", /awayWalkDir\(rival\)/.test(sliceFn(codeOnly, "rivalPushblockOnBlock")) && /dir !== away/.test(sliceFn(codeOnly, "tryPushblock")));
check("rival pushblock on connecting landBlock not tickAI windup", /function rivalPushblockOnBlock/.test(srcTxt) && /if \(def === rival\) shoved = rivalPushblockOnBlock\(\)/.test(srcTxt) && !/if \(rivalTryPushblock\(\)\)/.test(codeOnly) && /Pushblock on connecting landBlock/.test(srcTxt) && !/rivalTryPushblock\(\)/.test(sliceFn(codeOnly, "tickAI")) && /tryPushblock\(rival, awayWalkDir\(rival\)\)/.test(sliceFn(codeOnly, "rivalPushblockOnBlock")) && /pbCd = PUSHBLOCK_AI_CD/.test(sliceFn(codeOnly, "rivalPushblockOnBlock")));
check("player pushblock path unchanged", /tryPushblock\(player, tapWalk\)/.test(codeOnly) && /tryPushblock\(player, pushblockBuf\)/.test(codeOnly) && /if \(!player\.guarding && player\.phase === "idle"/.test(codeOnly));
check("pad leftover already wired no 6th", /class="stick"/.test(htmlTxt) && /feedKey\("KeyA"/.test(srcTxt) && /feedKey\("KeyD"/.test(srcTxt) && /data-hold="KeyS"/.test(htmlTxt) && (htmlTxt.match(/data-tap=/g)||[]).length===3 && !/zone-pushblock/.test(htmlTxt));
check("throw/reversal/super/chip/cancel locked after rival pushblock", V.THROW_STARTUP===80 && V.THROW_ACTIVE===40 && V.THROW_RECOVERY===220 && V.THROW_STAM===0 && V.THROW_RANGE===120 && V.REVERSAL_STAM===30 && V.REVERSAL_INVULN===120 && V.REVERSAL_AI_CD===1800 && V.REVERSAL_AI_CHANCE===0.4 && V.BOLT_SUPER_DMG===28 && V.BOLT_SUPER_CHIP===6 && V.BOLT_CHIP===2 && V.SLASH_CANCEL_MS===100 && V.GOLPE_CANCEL_MS===100 && V.BOLT_CANCEL_MS===100 && V.PUSHBLOCK_STAM===25 && V.PUSHBLOCK_PX===240);
check("v147 no 6th button", (htmlTxt.match(/data-tap=/g)||[]).length===3 && !/zone-pushblock/.test(htmlTxt) && /data-tap="KeyL"/.test(htmlTxt));
check("cache v=296 after rival pushblock", /game.js\?v=350/.test(htmlTxt));

function parkRivalPushRoom(){
  toClose();
  freezeYouAI();
  freezeRivalAI();
  V.player.hp = 100;
  V.rival.hp = 100;
  V.player.stamina = 100;
  V.rival.stamina = 100;
  V.player.x = 480;
  V.rival.x = 820;
  V.player.facing = 1;
  V.rival.facing = -1;
  V.player.pushT = 0;
  V.rival.pushT = 0;
  V.player.pushVel = 0;
  V.rival.pushVel = 0;
  V.keepApart();
}

function armRivalPushblockAI(){
  parkRivalPushRoom();
  V.rival.phase = "idle";
  V.rival.phaseT = 0;
  V.rival.cut = "slash";
  V.rival.cutHit = false;
  V.rival.wantBlock = true;
  V.rival.guarding = true;
  V.rival.guardChoice = 1;
  V.rival.stamina = 100;
  V.rival.stamRegenT = 99999;
  V.rival.guardBreakT = 0;
  V.rival.pushT = 0;
  V.rival.pushVel = 0;
  V.rival.stunT = 0;
  V.rival.falling = false;
  V.rival.boltPhase = "";
  V.rival.boltCd = 99999;
  V.rival.throwCd = 99999;
  V.rival.revCd = 99999;
  V.rival.revArmed = -1;
  V.rival.wakeRevArmed = -1;
  V.rival.pbCd = 0;
  V.rival.pbArmed = 0;
  V.rival.reversal = false;
  V.rival.closing = false;
  V.rival.standWait = 0;
  V.rival.standGoal = 99999;
  V.rival.gait = 0;
  V.player.hp = 100;
  V.rival.hp = 100;
  V.player.guarding = false;
  V.player.stunT = 0;
  V.player.falling = false;
}

function holdMeatyForRivalPush(){
  const keepCut = V.player.cut==="golpe" ? "golpe" : "slash";
  const keepT = keepCut==="golpe" ? 90 : 150;
  holdYouSlashMeaty();
  V.player.cut = keepCut;
  V.player.phase = "startup";
  V.player.phaseT = keepT;
  V.player.cutHit = false;
  V.player.clashRec = false;
  V.player.reversal = false;
  V.player.boltPhase = "";
  V.rival.boltCd = 99999;
  V.rival.throwCd = 99999;
  V.rival.revCd = 99999;
  V.rival.revArmed = -1;
  V.rival.wantBlock = true;
  V.rival.guarding = true;
  V.rival.guardChoice = 1;
  V.rival.gait = 0;
  V.rival.closing = false;
  V.rival.standGoal = 99999;
}

function waitRivalPush(frames){
  for(let i=0;i<frames;i++){
    holdMeatyForRivalPush();
    V.update(STEP);
  }
}

// forced vs meaty Space
armRivalPushblockAI();
plantMeatySlash();
V.rival.pbArmed = 1;
V.rival.pbCd = 0;
const rpbHurt = V.bodyAABB(V.rival).w;
const rpbX0 = V.rival.x;
const rpbG0 = V.absGap();
const rpbSt0 = V.rival.stamina;
const rpbOk = V.rivalTryPushblock();
const rpbSt1 = V.rival.stamina;
const rpbSpent = rpbSt0 - rpbSt1;
check("rival pushblock vs meaty Space spends 25", rpbOk && V.rival.guarding && !V.rival.reversal && V.rival.cut!=="throw" && rpbSpent>=V.PUSHBLOCK_STAM-1 && rpbSpent<=V.PUSHBLOCK_STAM+2 && V.rival.pushT>0 && V.rival.pbCd===V.PUSHBLOCK_AI_CD, `ok=${rpbOk} g=${V.rival.guarding} rev=${V.rival.reversal} spent=${rpbSpent.toFixed(2)} pushT=${V.rival.pushT} cd=${V.rival.pbCd} st0=${rpbSt0.toFixed(2)}`);
waitRivalPush(12);
const rpbDx = V.rival.x - rpbX0;
const rpbDg = V.absGap() - rpbG0;
check("rival pushblock vs meaty Space pushes extra 240", rpbDx>=rpbHurt+8 && rpbDx>=V.PUSHBLOCK_PX-30 && rpbDx<=V.PUSHBLOCK_PX+20 && rpbDg>=rpbHurt && V.rival.gait===0 && V.awayWalkDir(V.rival)===1, `dx=${rpbDx.toFixed(1)} dg=${rpbDg.toFixed(1)} hurt=${rpbHurt.toFixed(1)} px=${V.PUSHBLOCK_PX} gait=${V.rival.gait} x0=${rpbX0.toFixed(1)} x=${V.rival.x.toFixed(1)} away=${V.awayWalkDir(V.rival)}`);

// vs meaty L
armRivalPushblockAI();
plantMeatyGolpe();
V.rival.pbArmed = 1;
V.rival.pbCd = 0;
const lpbSt0 = V.rival.stamina;
const lpbX0 = V.rival.x;
const lpbOk = V.rivalTryPushblock();
const lpbSpent = lpbSt0 - V.rival.stamina;
check("rival pushblock vs meaty L spends", lpbOk && V.rival.guarding && !V.rival.reversal && V.player.cut==="golpe" && lpbSpent>=V.PUSHBLOCK_STAM-1 && lpbSpent<=V.PUSHBLOCK_STAM+2 && V.rival.pushT>0 && V.rival.pbCd===V.PUSHBLOCK_AI_CD, `ok=${lpbOk} g=${V.rival.guarding} rev=${V.rival.reversal} spent=${lpbSpent.toFixed(2)} cd=${V.rival.pbCd} cut=${V.player.cut}`);
waitRivalPush(12);
const lpbDx = V.rival.x - lpbX0;
check("rival pushblock vs meaty L pushes extra", lpbDx>=rpbHurt && V.rival.gait===0 && V.rival.guarding, `dx=${lpbDx.toFixed(1)} gait=${V.rival.gait} g=${V.rival.guarding}`);

// CD no spam
armRivalPushblockAI();
plantMeatySlash();
V.rival.pbArmed = 1;
V.rival.pbCd = 0;
const firstPb = V.rivalTryPushblock();
check("first rival pushblock arms CD", firstPb && V.rival.pbCd===V.PUSHBLOCK_AI_CD, `ok=${firstPb} cd=${V.rival.pbCd}`);
const cdPb0 = V.rival.pbCd;
V.rival.stamina = 100;
V.rival.pushT = 0;
V.rival.pushVel = 0;
plantMeatySlash();
V.rival.guarding = true;
V.rival.guardChoice = 1;
V.rival.pbArmed = 1;
const secondPb = V.rivalTryPushblock();
check("rival pushblock CD holds, no spam", cdPb0===V.PUSHBLOCK_AI_CD && secondPb===false && V.rival.guarding && V.rival.pbCd===cdPb0 && V.rival.pushT===0, `ok=${secondPb} cd0=${cdPb0} cd=${V.rival.pbCd} g=${V.rival.guarding} pushT=${V.rival.pushT}`);

// 40% miss / not every block
armRivalPushblockAI();
plantMeatySlash();
V.rival.pbArmed = -1;
V.rival.pbCd = 0;
const missPb = V.rivalTryPushblock();
check("forced pbArmed -1 does not pushblock", missPb===false && V.rival.guarding && V.rival.pushT===0 && V.rival.pbArmed===-1 && V.rival.phase==="idle", `ok=${missPb} g=${V.rival.guarding} pushT=${V.rival.pushT} armed=${V.rival.pbArmed}`);

armRivalPushblockAI();
plantMeatySlash();
V.rival.pbArmed = 0;
V.rival.pbCd = 0;
Math.random = () => 0.99;
const rollMissPb = V.rivalTryPushblock();
check("40% miss does not pushblock", rollMissPb===false && V.rival.guarding && V.rival.pushT===0 && V.rival.pbArmed===-1, `ok=${rollMissPb} g=${V.rival.guarding} armed=${V.rival.pbArmed} pushT=${V.rival.pushT}`);

// not vs dart
armRivalPushblockAI();
V.player.cut = "slash";
V.player.phase = "idle";
V.player.phaseT = 0;
V.player.stamina = 100;
V.player.stamRegenT = 99999;
V.rival.pbArmed = 1;
V.rival.pbCd = 0;
Math.random = () => 0;
const dartPlant = V.startBolt(V.player);
const dartPb = V.rivalTryPushblock();
check("rival does not pushblock vs dart", dartPlant && dartPb===false && V.rival.pushT===0 && V.player.boltPhase==="startup", `plant=${dartPlant} ok=${dartPb} pushT=${V.rival.pushT} bph=${V.player.boltPhase} meaty=${V.meatySpaceAtGuard()}`);

// not vs throw
armRivalPushblockAI();
plantMeatySlash();
V.player.cut = "throw";
V.player.phase = "startup";
V.rival.pbArmed = 1;
V.rival.pbCd = 0;
const throwPb = V.rivalTryPushblock();
check("rival does not pushblock vs throw", throwPb===false && V.rival.pushT===0 && V.rival.guarding && V.meatySpaceAtGuard()===false, `ok=${throwPb} pushT=${V.rival.pushT} meaty=${V.meatySpaceAtGuard()} cut=${V.player.cut}`);

// not without pressure
armRivalPushblockAI();
V.player.cut = "slash";
V.player.phase = "idle";
V.player.phaseT = 0;
V.rival.pbArmed = 1;
V.rival.pbCd = 0;
const idlePb = V.rivalTryPushblock();
check("rival does not pushblock without incoming", idlePb===false && V.rival.pushT===0 && V.rival.guarding && V.meatySpaceAtGuard()===false, `ok=${idlePb} g=${V.rival.guarding} pushT=${V.rival.pushT} ph=${V.player.phase}`);

// toward is not pushblock (direct toward tap refused)
armRivalPushblockAI();
plantMeatySlash();
V.rival.pbArmed = 1;
V.rival.pbCd = 0;
const towardDir = -V.awayWalkDir(V.rival);
const twR0 = V.rival.x;
const twS0 = V.rival.stamina;
const twOk = V.tryPushblock(V.rival, towardDir);
check("toward is not rival pushblock", twOk===false && V.rival.stamina===twS0 && V.rival.pushT===0 && Math.abs(V.rival.x-twR0)<2 && towardDir!==V.awayWalkDir(V.rival), `ok=${twOk} spent=${(twS0-V.rival.stamina).toFixed(2)} pushT=${V.rival.pushT} toward=${towardDir} away=${V.awayWalkDir(V.rival)}`);

// startup shove is NOT the live path: pin meaty startup, AI must not pushblock
armRivalPushblockAI();
plantMeatySlash();
V.rival.pbArmed = 1;
V.rival.pbCd = 0;
V.rival.revArmed = -1;
V.rival.revCd = 99999;
Math.random = () => 0;
const suX0 = V.rival.x;
const suSt0 = V.rival.stamina;
for(let i=0;i<8;i++){
  Math.random = () => 0;
  holdMeatyForRivalPush();
  V.rival.pbArmed = 1;
  V.rival.pbCd = 0;
  V.rival.revArmed = -1;
  V.rival.revCd = 99999;
  V.rival.wantBlock = true;
  V.rival.guarding = true;
  V.rival.guardChoice = 1;
  V.update(STEP);
}
check("startup shove is not the live pushblock path", V.player.phase==="startup" && V.rival.pushT===0 && (suSt0-V.rival.stamina)<8 && Math.abs(V.rival.x-suX0)<12 && !V.rival.reversal && V.rival.cut!=="throw", `ph=${V.player.phase} pushT=${V.rival.pushT} spent=${(suSt0-V.rival.stamina).toFixed(2)} dx=${(V.rival.x-suX0).toFixed(1)} g=${V.rival.guarding} cd=${V.rival.pbCd}`);

// live landBlock connect: let meaty slash leave startup into active so landBlock can fire
armRivalPushblockAI();
plantMeatySlash();
V.rival.pbArmed = 1;
V.rival.pbCd = 0;
V.rival.revArmed = -1;
V.rival.revCd = 99999;
Math.random = () => 0;
const liveX0 = V.rival.x;
const liveSt0 = V.rival.stamina;
let livePb=false, liveBlock=false;
for(let i=0;i<90;i++){
  Math.random = () => 0;
  holdYouSlashMeaty();
  if(V.player.phase==="idle" && V.player.cut==="slash" && !V.player.cutHit) plantMeatySlash();
  V.rival.wantBlock = true;
  V.rival.guardChoice = 1;
  if(!V.rival.reversal) V.rival.guarding = true;
  V.rival.pbArmed = 1;
  V.rival.revArmed = -1;
  V.rival.revCd = 99999;
  V.rival.boltCd = 99999;
  V.rival.throwCd = 99999;
  V.rival.gait = 0;
  V.rival.closing = false;
  V.rival.standGoal = 99999;
  V.update(STEP);
  if(V.player.cutHit) liveBlock=true;
  if(V.rival.pushT>0 && (liveSt0-V.rival.stamina)>=V.PUSHBLOCK_STAM-2) livePb=true;
  if(livePb && liveBlock) break;
}
drainHitstop();
for(let i=0;i<12;i++){
  V.rival.wantBlock = true;
  V.rival.guardChoice = 1;
  V.rival.gait = 0;
  V.rival.closing = false;
  V.rival.standGoal = 99999;
  V.update(STEP);
}
const liveDx = V.rival.x - liveX0;
check("rival can pushblock pressure on landBlock", livePb && liveBlock && !V.rival.reversal && V.rival.cut!=="throw" && liveDx>=rpbHurt && (liveSt0-V.rival.stamina)>=V.PUSHBLOCK_STAM-2 && V.player.cut!=="throw" && V.player.cutHit, `pb=${livePb} block=${liveBlock} rev=${V.rival.reversal} dx=${liveDx.toFixed(1)} spent=${(liveSt0-V.rival.stamina).toFixed(2)} cut=${V.rival.cut} g=${V.rival.guarding} cd=${V.rival.pbCd} pph=${V.player.phase} ch=${V.player.cutHit}`);

// player pushblock unchanged after rival pass
toClose();
raiseGuardInRoom();
const youHurt = V.bodyAABB(V.player).w;
const youX0 = V.player.x;
const youSt0 = V.player.stamina;
V.keys.add("KeyA");
V.update(STEP);
const youSpent = youSt0 - V.player.stamina;
waitPush(12);
const youDx = youX0 - V.player.x;
check("player pushblock unchanged", V.player.guarding && youSpent>=V.PUSHBLOCK_STAM-1 && youSpent<=V.PUSHBLOCK_STAM+2 && youDx>=youHurt && youDx>=V.PUSHBLOCK_PX-30 && V.player.gait===0, `g=${V.player.guarding} spent=${youSpent.toFixed(2)} dx=${youDx.toFixed(1)} gait=${V.player.gait}`);
V.keys.clear();

// throw unchanged
toClose();
raiseGuardInRoom();
V.rival.stamina = 80;
V.rival.stamRegenT = 99999;
V.rival.wantBlock = true;
V.rival.guarding = true;
V.rival.guardChoice = 1;
V.rival.throwTechArmed = -1;
V.rival.pbArmed = -1;
V.rival.pbCd = 99999;
V.player.x = V.rival.x - 160;
V.keepApart();
V.keys.add("KeyS");
V.keys.add("Space");
V.throwEdge = true;
let v147Throw=false;
for(let i=0;i<80;i++){
  holdRivalStill();
  V.rival.wantBlock = true;
  V.rival.guarding = true;
  V.rival.guardChoice = 1;
  V.rival.throwTechArmed = -1;
  V.rival.pbArmed = -1;
  V.rival.pbCd = 99999;
  V.update(STEP);
  if(V.hitstopLeft>0 || V.rival.hp<100 || V.rival.thrownT>0){ v147Throw=true; break; }
}
drainHitstop();
check("throw unchanged after rival pushblock", v147Throw && V.player.cut==="throw" && V.player.cutHit && V.rival.hp===80 && V.rival.thrownT>0, `hit=${v147Throw} cut=${V.player.cut} ch=${V.player.cutHit} rhp=${V.rival.hp} th=${V.rival.thrownT}`);
V.keys.clear();

// reversal unchanged
toClose();
raiseGuardInRoom();
const v147RevSt0 = V.player.stamina;
fireReversalChord();
V.update(STEP);
check("reversal unchanged after rival pushblock", V.player.cut==="golpe" && V.player.phase==="startup" && V.player.reversal && !V.player.guarding && V.player.cut!=="throw" && Math.abs((v147RevSt0-V.player.stamina)-V.REVERSAL_STAM)<3, `cut=${V.player.cut} rev=${V.player.reversal} ph=${V.player.phase} spent=${(v147RevSt0-V.player.stamina).toFixed(2)}`);
V.keys.clear();

check("cache v=295 final", /game.js\?v=350/.test(htmlTxt));

// --- v148 pushblock sting: pitched/layered block steel, not a normal block ---
check("pushblock still 240/25 after sting", V.PUSHBLOCK_STAM===25 && V.PUSHBLOCK_PX===240 && V.PUSHBLOCK_AI_CD===1800 && V.PUSHBLOCK_AI_CHANCE===0.4);
const pbStingFn = sliceFn(codeOnly, "playPushblockSting");
const tryPbFn = sliceFn(codeOnly, "tryPushblock");
const landBlockFn = sliceFn(codeOnly, "landBlock");
check("pushblock sting pitches layered block/guard/push SFX",
  /function playPushblockSting/.test(srcTxt) &&
  /SFX\.bloqueo/.test(pbStingFn) &&
  /SFX\.choque/.test(pbStingFn) &&
  /rate:/.test(pbStingFn) &&
  (pbStingFn.match(/playSfx\(SFX\.bloqueo/g)||[]).length>=2 &&
  /playSfx\(SFX\.choque/.test(pbStingFn) &&
  !/SFX\.whoosh/.test(pbStingFn) &&
  !/new Audio/.test(pbStingFn)
);
check("pushblock sting only from tryPushblock success",
  /playPushblockSting\(\)/.test(tryPbFn) &&
  tryPbFn.indexOf("playPushblockSting()") > tryPbFn.indexOf("spawnPlantDust") &&
  tryPbFn.indexOf("playPushblockSting()") < tryPbFn.indexOf("return true") &&
  !/playPushblockSting/.test(landBlockFn) &&
  /playSfx\(SFX\.bloqueo\);/.test(landBlockFn) &&
  !/rate:/.test(landBlockFn) &&
  /lastPushblockSfx = "block"/.test(landBlockFn) &&
  /lastPushblockSfx = "pushblock"/.test(pbStingFn)
);
check("no new pushblock audio pipeline", !/sfx_pushblock/.test(srcTxt) && !/AudioContext/.test(srcTxt) && !/new Audio\([^)]*push/.test(srcTxt));
check("throw/reversal/super/chip/cancel locked after sting", V.THROW_STARTUP===80 && V.THROW_ACTIVE===40 && V.THROW_RECOVERY===220 && V.THROW_STAM===0 && V.THROW_RANGE===120 && V.REVERSAL_STAM===30 && V.REVERSAL_INVULN===120 && V.REVERSAL_AI_CD===1800 && V.REVERSAL_AI_CHANCE===0.4 && V.BOLT_SUPER_DMG===28 && V.BOLT_SUPER_CHIP===6 && V.BOLT_CHIP===2 && V.SLASH_CANCEL_MS===100 && V.GOLPE_CANCEL_MS===100 && V.BOLT_CANCEL_MS===100 && V.PUSHBLOCK_STAM===25 && V.PUSHBLOCK_PX===240);
check("v148 no 6th button", (htmlTxt.match(/data-tap=/g)||[]).length===3 && !/zone-pushblock/.test(htmlTxt) && /data-tap="KeyL"/.test(htmlTxt));

toClose();
raiseGuardInRoom();
const stingX0 = V.player.x;
const stingSt0 = V.player.stamina;
V.keys.add("KeyA");
V.update(STEP);
check("player pushblock fires sting", V.lastPushblockSfx==="pushblock" && V.player.guarding && V.player.pushT>0 && (stingSt0-V.player.stamina)>=V.PUSHBLOCK_STAM-1 && (stingSt0-V.player.stamina)<=V.PUSHBLOCK_STAM+2, `sfx=${V.lastPushblockSfx} g=${V.player.guarding} pushT=${V.player.pushT} spent=${(stingSt0-V.player.stamina).toFixed(2)}`);
waitPush(12);
const stingDx = stingX0 - V.player.x;
check("player sting shove still 240", stingDx>=V.PUSHBLOCK_PX-30 && stingDx<=V.PUSHBLOCK_PX+20 && V.lastPushblockSfx==="pushblock", `dx=${stingDx.toFixed(1)} sfx=${V.lastPushblockSfx}`);
V.keys.clear();

// normal block keeps current SFX (slash steel bloqueo, not the shove sting)
toMeasure();
Math.random = () => 0;
V.rival.revCd = 99999;
V.rival.revArmed = -1;
V.rival.pbCd = 99999;
V.rival.pbArmed = -1;
V.attackEdge = true;
let nbGot=false;
for(let i=0;i<80;i++){
  V.rival.revCd = 99999;
  V.rival.revArmed = -1;
  V.rival.pbCd = 99999;
  V.rival.pbArmed = -1;
  V.update(STEP);
  if(V.hitstopLeft>0 || V.rival.pushT>0){ nbGot=true; break; }
}
check("normal block does not fire sting", nbGot && V.lastPushblockSfx==="block" && V.rival.hp===100 && V.rival.pushT>0 && V.player.cutHit, `got=${nbGot} sfx=${V.lastPushblockSfx} hp=${V.rival.hp} pushT=${V.rival.pushT} hit=${V.player.cutHit} ph=${V.player.phase}`);
check("normal block still costs 20 not 25", V.rival.stamina>=70 && V.rival.stamina<=82 && V.STAMINA_BLOCK===20, `st=${V.rival.stamina.toFixed(2)} block=${V.STAMINA_BLOCK}`);

// toward fail must not steal the sting path (flag stays block from landBlock)
toClose();
raiseGuardInRoom();
V.keys.add("KeyD");
V.update(STEP);
waitPush(4);
check("toward does not fire sting", V.lastPushblockSfx!=="pushblock" && V.player.pushT===0 && V.player.guarding, `sfx=${V.lastPushblockSfx} pushT=${V.player.pushT} g=${V.player.guarding}`);
V.keys.clear();

// rival pushblock still uses the same sting
armRivalPushblockAI();
holdMeatyForRivalPush();
V.rival.pbArmed = 1;
V.rival.pbCd = 0;
const rStingSt0 = V.rival.stamina;
const rStingOk = V.rivalTryPushblock();
check("rival pushblock fires sting", rStingOk && V.lastPushblockSfx==="pushblock" && V.rival.guarding && V.rival.pushT>0 && (rStingSt0-V.rival.stamina)>=V.PUSHBLOCK_STAM-1, `ok=${rStingOk} sfx=${V.lastPushblockSfx} g=${V.rival.guarding} pushT=${V.rival.pushT} spent=${(rStingSt0-V.rival.stamina).toFixed(2)}`);

check("cache v=296 after sting", /game.js\?v=350/.test(htmlTxt));

// --- v149 pushblock dust: scaled/offset plant scrape, not a normal block puff ---
check("pushblock still 240/25 after dust", V.PUSHBLOCK_STAM===25 && V.PUSHBLOCK_PX===240 && V.PUSHBLOCK_AI_CD===1800 && V.PUSHBLOCK_AI_CHANCE===0.4);
check("pushblock dust scale bigger than block", V.PUSHBLOCK_FX===1.85 && V.PUSHBLOCK_FX>1 && V.PUSHBLOCK_FX>=1.5 && V.PUSHBLOCK_FX>1.0, `fx=${V.PUSHBLOCK_FX}`);
const tryPbDustFn = sliceFn(codeOnly, "tryPushblock");
const landBlockDustFn = sliceFn(codeOnly, "landBlock");
const spawnDustFn = sliceFn(codeOnly, "spawnPlantDust");
const drawDustFn = sliceFn(codeOnly, "drawPlantDust");
check("pushblock dust path uses PUSHBLOCK_FX shove",
  /spawnPlantDust\(f,\s*PUSHBLOCK_FX,\s*away\)/.test(tryPbDustFn) &&
  /playPushblockSting\(\)/.test(tryPbDustFn) &&
  tryPbDustFn.indexOf("playPushblockSting()") > tryPbDustFn.indexOf("spawnPlantDust") &&
  tryPbDustFn.indexOf("playPushblockSting()") < tryPbDustFn.indexOf("return true")
);
check("normal block dust stays 1.0 no shove",
  /spawnPlantDust\(def,\s*1\.0\)/.test(landBlockDustFn) &&
  !/PUSHBLOCK_FX/.test(landBlockDustFn) &&
  !/spawnPlantDust\(def,\s*PUSHBLOCK_FX/.test(landBlockDustFn) &&
  /playSfx\(SFX\.bloqueo\);/.test(landBlockDustFn) &&
  /lastPushblockSfx = "block"/.test(landBlockDustFn)
);
check("spawnPlantDust offsets shove along away",
  /function spawnPlantDust/.test(srcTxt) &&
  /shoveDir === -1 \|\| shoveDir === 1/.test(spawnDustFn) &&
  /18 \* power/.test(spawnDustFn) &&
  /shove: shove/.test(spawnDustFn) &&
  /shoveDir/.test(spawnDustFn) &&
  /f\.facing \* 12 \* power/.test(spawnDustFn) &&
  /dir \* \(150 \+ 50 \* power\)/.test(spawnDustFn)
);
check("drawPlantDust scales shove not normal",
  /p\.shove/.test(drawDustFn) &&
  /p\.shove \? 1\.25 : 1/.test(drawDustFn) &&
  /p\.shoveDir/.test(drawDustFn)
);
check("no new pushblock art sheet", !/art\/.*push/.test(srcTxt) && !/loadImg\([^)]*push/.test(srcTxt) && !/sfx_pushblock/.test(srcTxt));
check("sting unchanged after dust",
  /function playPushblockSting/.test(srcTxt) &&
  /SFX\.bloqueo/.test(sliceFn(codeOnly, "playPushblockSting")) &&
  /SFX\.choque/.test(sliceFn(codeOnly, "playPushblockSting")) &&
  (sliceFn(codeOnly, "playPushblockSting").match(/playSfx\(SFX\.bloqueo/g)||[]).length>=2 &&
  !/playPushblockSting/.test(landBlockDustFn)
);
check("throw/reversal/super/chip/cancel locked after dust", V.THROW_STARTUP===80 && V.THROW_ACTIVE===40 && V.THROW_RECOVERY===220 && V.THROW_STAM===0 && V.THROW_RANGE===120 && V.REVERSAL_STAM===30 && V.REVERSAL_INVULN===120 && V.REVERSAL_AI_CD===1800 && V.REVERSAL_AI_CHANCE===0.4 && V.BOLT_SUPER_DMG===28 && V.BOLT_SUPER_CHIP===6 && V.BOLT_CHIP===2 && V.SLASH_CANCEL_MS===100 && V.GOLPE_CANCEL_MS===100 && V.BOLT_CANCEL_MS===100 && V.PUSHBLOCK_STAM===25 && V.PUSHBLOCK_PX===240);
check("v149 no 6th button", (htmlTxt.match(/data-tap=/g)||[]).length===3 && !/zone-pushblock/.test(htmlTxt) && /data-tap="KeyL"/.test(htmlTxt));

function dustShove(){
  return (V.plantDust||[]).filter(p=>p.shove);
}
function dustMaxPower(list){
  return (list||[]).reduce((m,p)=>Math.max(m, p.power||0), 0);
}

toClose();
raiseGuardInRoom();
const dustX0 = V.player.x;
const dustSt0 = V.player.stamina;
V.keys.add("KeyA");
V.update(STEP);
const pbDust = dustShove();
const pbDustPow = dustMaxPower(pbDust);
const awayYouDust = V.awayWalkDir(V.player);
const pbSpecks = pbDust.filter(p=>p.speck);
const pbSpeckAway = pbSpecks.filter(p=> awayYouDust<0 ? p.vx<-20 : p.vx>20);
const pbStamp = pbDust.find(p=>!p.speck);
check("player pushblock fires shove dust", pbDust.length>0 && pbDustPow>=V.PUSHBLOCK_FX-0.01 && V.lastPushblockSfx==="pushblock" && V.player.guarding && V.player.pushT>0 && (dustSt0-V.player.stamina)>=V.PUSHBLOCK_STAM-1 && (dustSt0-V.player.stamina)<=V.PUSHBLOCK_STAM+2, `n=${pbDust.length} pow=${pbDustPow.toFixed(2)} fx=${V.PUSHBLOCK_FX} sfx=${V.lastPushblockSfx} spent=${(dustSt0-V.player.stamina).toFixed(2)}`);
check("player pushblock dust vx biased away",
  pbStamp && (awayYouDust<0 ? pbStamp.vx<0 : pbStamp.vx>0) &&
  pbSpecks.length>=4 && pbSpeckAway.length>=Math.ceil(pbSpecks.length*0.5) &&
  pbStamp.shoveDir===awayYouDust,
  `stampVx=${pbStamp&&pbStamp.vx} dir=${pbStamp&&pbStamp.shoveDir} away=${awayYouDust} speckAway=${pbSpeckAway.length}/${pbSpecks.length}`
);
waitPush(12);
const dustDx = dustX0 - V.player.x;
check("player dust shove still 240", dustDx>=V.PUSHBLOCK_PX-30 && dustDx<=V.PUSHBLOCK_PX+20 && V.lastPushblockSfx==="pushblock", `dx=${dustDx.toFixed(1)} sfx=${V.lastPushblockSfx}`);
V.keys.clear();

// normal block keeps current 1.0 plant, no shove tag
toMeasure();
Math.random = () => 0;
V.rival.revCd = 99999;
V.rival.revArmed = -1;
V.rival.pbCd = 99999;
V.rival.pbArmed = -1;
V.attackEdge = true;
let nbDustGot=false;
for(let i=0;i<80;i++){
  V.rival.revCd = 99999;
  V.rival.revArmed = -1;
  V.rival.pbCd = 99999;
  V.rival.pbArmed = -1;
  V.update(STEP);
  if(V.hitstopLeft>0 || V.rival.pushT>0){ nbDustGot=true; break; }
}
const nbShove = dustShove();
const hasBlockPuff = (V.plantDust||[]).some(p=>!p.speck && !p.shove && Math.abs((p.power||0)-1)<0.01);
check("normal block does not fire shove dust", nbDustGot && nbShove.length===0 && V.lastPushblockSfx==="block" && V.rival.hp===100 && V.rival.pushT>0 && V.player.cutHit, `got=${nbDustGot} shove=${nbShove.length} sfx=${V.lastPushblockSfx} hp=${V.rival.hp} pushT=${V.rival.pushT} hit=${V.player.cutHit}`);
check("normal block dust stays 1.0", hasBlockPuff && nbShove.length===0, `has1=${hasBlockPuff} shove=${nbShove.length}`);
check("normal block still costs 20 not 25 after dust", V.rival.stamina>=70 && V.rival.stamina<=82 && V.STAMINA_BLOCK===20, `st=${V.rival.stamina.toFixed(2)} block=${V.STAMINA_BLOCK}`);

// toward fail must not steal the dust path
toClose();
raiseGuardInRoom();
V.keys.add("KeyD");
V.update(STEP);
waitPush(4);
check("toward does not fire shove dust", dustShove().length===0 && V.lastPushblockSfx!=="pushblock" && V.player.pushT===0 && V.player.guarding, `shove=${dustShove().length} sfx=${V.lastPushblockSfx} pushT=${V.player.pushT} g=${V.player.guarding}`);
V.keys.clear();

// rival pushblock uses the same scaled dust
armRivalPushblockAI();
holdMeatyForRivalPush();
V.rival.pbArmed = 1;
V.rival.pbCd = 0;
const rDustSt0 = V.rival.stamina;
const rDustOk = V.rivalTryPushblock();
const rDust = dustShove();
const rDustPow = dustMaxPower(rDust);
check("rival pushblock fires shove dust", rDustOk && rDust.length>0 && rDustPow>=V.PUSHBLOCK_FX-0.01 && V.lastPushblockSfx==="pushblock" && V.rival.guarding && V.rival.pushT>0 && (rDustSt0-V.rival.stamina)>=V.PUSHBLOCK_STAM-1, `ok=${rDustOk} n=${rDust.length} pow=${rDustPow.toFixed(2)} sfx=${V.lastPushblockSfx} spent=${(rDustSt0-V.rival.stamina).toFixed(2)}`);

check("cache v=296 after dust", /game.js\?v=350/.test(htmlTxt));


// --- v150 rival pushblock on connecting block, not windup ---
check("v150 still 240/25", V.PUSHBLOCK_STAM===25 && V.PUSHBLOCK_PX===240 && V.PUSHBLOCK_AI_CD===1800 && V.PUSHBLOCK_AI_CHANCE===0.4 && V.PUSHBLOCK_FX===1.85);
function liveConnectingRivalPush(cut){
  armRivalPushblockAI();
  V.player.phase = "idle";
  V.player.cut = "slash";
  V.player.phaseT = 0;
  V.player.cutHit = false;
  V.player.guarding = false;
  V.player.stunT = 0;
  V.player.falling = false;
  V.player.boltPhase = "";
  V.rival.pbArmed = 1;
  V.rival.pbCd = 0;
  V.rival.revArmed = -1;
  V.rival.revCd = 99999;
  V.rival.wantBlock = true;
  V.rival.guarding = true;
  V.rival.guardChoice = 1;
  Math.random = () => 0;
  const x0 = V.rival.x;
  const st0 = V.rival.stamina;
  V.startAttack(V.player, cut==="golpe" ? "golpe" : undefined);
  let blocked=false, hit=false, pb=false, whiff=false;
  for(let i=0;i<90;i++){
    Math.random = () => 0;
    V.rival.wantBlock = true;
    V.rival.guardChoice = 1;
    V.rival.revArmed = -1;
    V.rival.revCd = 99999;
    V.rival.boltCd = 99999;
    V.rival.throwCd = 99999;
    V.rival.closing = false;
    V.rival.standGoal = 99999;
    V.rival.gait = 0;
    V.player.guarding = false;
    V.player.stunT = 0;
    V.player.falling = false;
    V.player.boltPhase = "";
    V.keys.clear();
    const ph0 = V.player.phase;
    V.update(STEP);
    if(V.rival.pushT>0 && (st0-V.rival.stamina)>=V.PUSHBLOCK_STAM-2) pb=true;
    if(V.player.cutHit && V.rival.hp===100) blocked=true;
    if(V.rival.hp<100) hit=true;
    if(ph0==="active" && V.player.phase==="recovery" && !V.player.cutHit) whiff=true;
    if(V.hitstopLeft>0) drainHitstop();
    if(V.player.phase==="idle" && i>5) break;
  }
  for(let i=0;i<20;i++){
    V.rival.wantBlock = true;
    V.rival.guardChoice = 1;
    V.rival.revCd = 99999;
    V.rival.revArmed = -1;
    V.update(STEP);
    if(V.rival.pushT<=0 && V.hitstopLeft<=0 && V.player.phase==="idle") break;
  }
  return { blocked, hit, pb, whiff, dx: V.rival.x-x0, spent: st0-V.rival.stamina, php: V.player.hp, rhp: V.rival.hp, sfx: V.lastPushblockSfx, g: V.rival.guarding };
}
const liveL = liveConnectingRivalPush("golpe");
check("rival pushblock on connecting meaty L does not whiff", liveL.blocked && liveL.pb && !liveL.hit && !liveL.whiff && liveL.rhp===100 && liveL.php===100 && liveL.dx>=V.PUSHBLOCK_PX-30 && liveL.dx<=V.PUSHBLOCK_PX+40 && liveL.sfx==="pushblock", `blocked=${liveL.blocked} pb=${liveL.pb} hit=${liveL.hit} whiff=${liveL.whiff} dx=${liveL.dx.toFixed(1)} spent=${liveL.spent.toFixed(2)} sfx=${liveL.sfx} rhp=${liveL.rhp} php=${liveL.php} g=${liveL.g}`);
const liveS = liveConnectingRivalPush("slash");
check("rival pushblock on connecting meaty Space does not whiff", liveS.blocked && liveS.pb && !liveS.hit && !liveS.whiff && liveS.rhp===100 && liveS.php===100 && liveS.dx>=V.PUSHBLOCK_PX-30 && liveS.dx<=V.PUSHBLOCK_PX+40 && liveS.sfx==="pushblock", `blocked=${liveS.blocked} pb=${liveS.pb} hit=${liveS.hit} whiff=${liveS.whiff} dx=${liveS.dx.toFixed(1)} spent=${liveS.spent.toFixed(2)} sfx=${liveS.sfx} rhp=${liveS.rhp} php=${liveS.php} g=${liveS.g}`);
check("cache v=296 after connecting pushblock", /game.js\?v=350/.test(htmlTxt));


// --- v151 wakeup L+S spends throw-invuln (not a wakeup super) ---
check("v151 startReversal spends throw-invuln", /throwInvulnT = 0/.test(sliceFn(codeOnly, "startReversal")) && /wakeRev = false/.test(sliceFn(codeOnly, "startReversal")));
check("v151 tap-L wakeup still spends", /throwInvulnT = 0/.test(sliceFn(codeOnly, "startWakeReversal")) && /wakeRev = false/.test(sliceFn(codeOnly, "startWakeReversal")));
check("v151 throw/super/reversal/pushblock/combo locked", V.THROW_STARTUP===80 && V.THROW_STAM===0 && V.THROW_RANGE===120 && V.THROW_WAKE_INVULN===80 && V.REVERSAL_STAM===30 && V.REVERSAL_INVULN===120 && V.BOLT_SUPER_DMG===28 && V.BOLT_SUPER_CHIP===6 && V.BOLT_CHIP===2 && V.SLASH_CANCEL_MS===100 && V.GOLPE_CANCEL_MS===100 && V.BOLT_CANCEL_MS===100 && V.PUSHBLOCK_STAM===25 && V.PUSHBLOCK_PX===240 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180);

plantYouWake();
V.keys.add("KeyS");
V.update(STEP);
check("wakeup hold-S raises guard with throw-invuln live", V.player.guarding && V.player.throwInvulnT>0 && V.player.wakeRev && V.throwLocked(V.player) && V.player.phase==="idle", `g=${V.player.guarding} inv=${V.player.throwInvulnT} wr=${V.player.wakeRev} locked=${V.throwLocked(V.player)} ph=${V.player.phase}`);
const wakeLSStam0 = V.player.stamina;
fireReversalChord();
V.update(STEP);
check("wakeup L+S is reversal", V.player.cut==="golpe" && V.player.phase==="startup" && V.player.reversal && !V.player.guarding && V.player.cut!=="throw", `cut=${V.player.cut} ph=${V.player.phase} rev=${V.player.reversal} g=${V.player.guarding}`);
check("wakeup L+S spends throw-invuln", V.player.throwInvulnT===0 && V.player.wakeRev===false && !V.throwLocked(V.player) && V.reversalInvuln(V.player), `inv=${V.player.throwInvulnT} wr=${V.player.wakeRev} locked=${V.throwLocked(V.player)} rinv=${V.reversalInvuln(V.player)}`);
check("wakeup L+S still spends 30 stam", wakeLSStam0-V.player.stamina>=V.REVERSAL_STAM-1 && wakeLSStam0-V.player.stamina<=V.REVERSAL_STAM+2, `d=${(wakeLSStam0-V.player.stamina).toFixed(2)}`);
V.rival.throwTechArmed = -1;
const throwVsWakeLS = V.startThrow(V.rival);
check("throw can start vs wakeup L+S", throwVsWakeLS && V.rival.cut==="throw" && V.rival.phase==="startup" && V.player.throwInvulnT===0, `ok=${throwVsWakeLS} rcut=${V.rival.cut} rph=${V.rival.phase} inv=${V.player.throwInvulnT}`);
let throwBeatWakeLS=false;
for(let i=0;i<80;i++){
  holdRivalStill();
  V.rival.throwTechArmed = -1;
  V.update(STEP);
  if(V.player.thrownT>0 || V.player.hp<100){ throwBeatWakeLS=true; break; }
}
drainHitstop();
check("throw still beats wakeup L+S", throwBeatWakeLS && V.player.hp===80 && V.player.thrownT>0 && V.rival.cut==="throw" && V.rival.cutHit && V.player.hp!==90, `beat=${throwBeatWakeLS} php=${V.player.hp} th=${V.player.thrownT} rch=${V.rival.cutHit} rcut=${V.rival.cut} pcut=${V.player.cut}`);
V.keys.clear();

// tap-L wakeup unchanged
plantYouWake();
V.golpeEdge = true;
V.update(STEP);
check("wakeup tap-L still reversal after L+S fix", V.player.cut==="golpe" && V.player.reversal && V.player.throwInvulnT===0 && V.player.wakeRev===false, `cut=${V.player.cut} rev=${V.player.reversal} inv=${V.player.throwInvulnT} wr=${V.player.wakeRev}`);

// standing guard reversal unchanged (no leftover invuln)
toClose();
raiseYouGuard();
fireReversalChord();
V.update(STEP);
check("standing L+S still reversal after wakeup spend", V.player.cut==="golpe" && V.player.reversal && V.player.phase==="startup" && V.player.throwInvulnT===0, `cut=${V.player.cut} rev=${V.player.reversal} ph=${V.player.phase} inv=${V.player.throwInvulnT}`);
V.keys.delete("KeyS");

check("cache v=296 after wakeup L+S spend", /game.js\?v=350/.test(htmlTxt));

// --- v152 throw beats hold-guard: no pushblock shove-whiff ---
const tryPbThrowFn = sliceFn(codeOnly, "tryPushblock");
check("tryPushblock refuses incoming throw startup/active",
  /foe\.cut === "throw"/.test(tryPbThrowFn) &&
  /foe\.phase === "startup"/.test(tryPbThrowFn) &&
  /foe\.phase === "active"/.test(tryPbThrowFn) &&
  /return false/.test(tryPbThrowFn)
);
check("v152 throw/super/reversal/pushblock/combo locked", V.THROW_STARTUP===80 && V.THROW_ACTIVE===40 && V.THROW_RECOVERY===220 && V.THROW_STAM===0 && V.THROW_RANGE===120 && V.THROW_WAKE_INVULN===80 && V.REVERSAL_STAM===30 && V.REVERSAL_INVULN===120 && V.BOLT_SUPER_DMG===28 && V.BOLT_SUPER_CHIP===6 && V.BOLT_CHIP===2 && V.SLASH_CANCEL_MS===100 && V.GOLPE_CANCEL_MS===100 && V.BOLT_CANCEL_MS===100 && V.PUSHBLOCK_STAM===25 && V.PUSHBLOCK_PX===240 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180);
check("v152 no 6th button", (htmlTxt.match(/data-tap=/g)||[]).length===3 && !/zone-pushblock/.test(htmlTxt) && /data-tap="KeyL"/.test(htmlTxt));

function tapYouAway(){
  const away = V.awayWalkDir(V.player);
  if(away===-1) V.keys.add("KeyA");
  else V.keys.add("KeyD");
  return away;
}

// player hold-S tap-away vs rival throw startup: no 240 shove, throw still grabs
toClose();
raiseYouGuard();
armRivalThrowAI();
holdYouGuard();
V.player.stamina = 100;
V.player.stamRegenT = 99999;
V.player.throwInvulnT = 0;
V.update(STEP);
check("v152 rival throw startup vs hold-S", V.player.guarding && V.rival.cut==="throw" && V.rival.phase==="startup" && V.absGap()<=V.THROW_RANGE, `g=${V.player.guarding} cut=${V.rival.cut} ph=${V.rival.phase} gap=${V.absGap().toFixed(1)}`);
const pbThrowX0 = V.player.x;
const pbThrowSt0 = V.player.stamina;
const pbThrowG0 = V.absGap();
tapYouAway();
V.update(STEP);
const pbThrowSpent = pbThrowSt0 - V.player.stamina;
const pbThrowDx1 = Math.abs(V.player.x - pbThrowX0);
check("player does not pushblock vs throw startup", V.player.guarding && V.player.pushT===0 && pbThrowSpent<8 && pbThrowDx1<12 && V.rival.cut==="throw" && (V.rival.phase==="startup" || V.rival.phase==="active"), `g=${V.player.guarding} pushT=${V.player.pushT} spent=${pbThrowSpent.toFixed(2)} dx=${pbThrowDx1.toFixed(1)} rcut=${V.rival.cut} rph=${V.rival.phase}`);
let pbThrowGrab=false;
for(let i=0;i<80;i++){
  holdYouGuard();
  V.rival.throwTechArmed = -1;
  V.update(STEP);
  if(V.hitstopLeft>0 || V.player.hp<100 || V.player.thrownT>0){ pbThrowGrab=true; break; }
}
drainHitstop();
const pbThrowDx = Math.abs(V.player.x - pbThrowX0);
check("throw vs hold-guard still hits, no 240 shove-whiff", pbThrowGrab && V.player.hp===80 && V.player.thrownT>0 && V.rival.cut==="throw" && V.rival.cutHit && pbThrowDx<V.PUSHBLOCK_PX-40 && V.absGap()<=V.THROW_RANGE, `hit=${pbThrowGrab} php=${V.player.hp} th=${V.player.thrownT} ch=${V.rival.cutHit} dx=${pbThrowDx.toFixed(1)} g0=${pbThrowG0.toFixed(1)} g=${V.absGap().toFixed(1)}`);
V.keys.clear();

// direct tryPushblock during throw active also refuses
toClose();
raiseYouGuard();
armRivalThrowAI();
holdYouGuard();
V.update(STEP);
while(V.rival.cut==="throw" && V.rival.phase==="startup" && V.rival.phaseT < V.THROW_STARTUP - STEP - 1){
  holdYouGuard();
  V.rival.throwTechArmed = -1;
  V.update(STEP);
}
check("v152 throw still startup/active for direct refuse", V.rival.cut==="throw" && (V.rival.phase==="startup" || V.rival.phase==="active") && V.player.guarding, `cut=${V.rival.cut} ph=${V.rival.phase} t=${V.rival.phaseT.toFixed(0)} g=${V.player.guarding}`);
V.player.stamina = 100;
const directPb = V.tryPushblock(V.player, V.awayWalkDir(V.player));
check("direct tryPushblock refuses vs throw", directPb===false && V.player.pushT===0 && V.player.stamina===100, `ok=${directPb} pushT=${V.player.pushT} st=${V.player.stamina.toFixed(1)}`);
V.keys.clear();

// same both ways: rival cannot shove a player throw into a whiff
toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.rival.stamina = 100;
V.rival.stamRegenT = 99999;
V.rival.wantBlock = true;
V.rival.guarding = true;
V.rival.guardChoice = 1;
V.rival.throwTechArmed = -1;
V.rival.pbCd = 0;
V.rival.pbArmed = 1;
V.player.stamina = 100;
V.player.stamRegenT = 99999;
V.player.x = V.rival.x - 160;
V.keepApart();
fireThrow();
V.update(STEP);
check("v152 player throw startup vs rival hold-S", V.player.cut==="throw" && V.player.phase==="startup" && V.rival.guarding && V.absGap()<=V.THROW_RANGE, `cut=${V.player.cut} ph=${V.player.phase} g=${V.rival.guarding} gap=${V.absGap().toFixed(1)}`);
const rThrowX0 = V.rival.x;
const rThrowSt0 = V.rival.stamina;
const rLivePb = V.tryPushblock(V.rival, V.awayWalkDir(V.rival));
const rAiPb = V.rivalTryPushblock();
check("rival does not pushblock vs player throw", rLivePb===false && rAiPb===false && V.rival.pushT===0 && V.rival.guarding && (rThrowSt0-V.rival.stamina)<8, `live=${rLivePb} ai=${rAiPb} pushT=${V.rival.pushT} spent=${(rThrowSt0-V.rival.stamina).toFixed(2)}`);
let rThrowHit=false, rThrowShove=false, rThrowMaxDx=0;
for(let i=0;i<80;i++){
  holdRivalStill();
  V.rival.wantBlock = true;
  V.rival.guarding = true;
  V.rival.guardChoice = 1;
  V.rival.throwTechArmed = -1;
  V.rival.pbArmed = 1;
  V.rival.pbCd = 0;
  const rPb = V.tryPushblock(V.rival, V.awayWalkDir(V.rival));
  if(rPb || V.rival.pushT>0) rThrowShove=true;
  rThrowMaxDx = Math.max(rThrowMaxDx, Math.abs(V.rival.x - rThrowX0));
  V.update(STEP);
  rThrowMaxDx = Math.max(rThrowMaxDx, Math.abs(V.rival.x - rThrowX0));
  if(V.hitstopLeft>0 || V.rival.hp<100 || V.rival.thrownT>0){ rThrowHit=true; break; }
}
drainHitstop();
check("player throw vs rival hold-S still grabs, no 240 shove-whiff", rThrowHit && !rThrowShove && V.rival.hp===80 && V.rival.thrownT>0 && V.player.cut==="throw" && V.player.cutHit && V.rival.pushT===0, `hit=${rThrowHit} shove=${rThrowShove} rhp=${V.rival.hp} th=${V.rival.thrownT} ch=${V.player.cutHit} dx=${rThrowMaxDx.toFixed(1)} pushT=${V.rival.pushT}`);
V.keys.clear();

// pushblock still works vs Space
toClose();
raiseGuardInRoom();
holdRivalStill();
V.rival.cut = "slash";
V.rival.phase = "startup";
V.rival.phaseT = 20;
V.rival.guarding = false;
V.rival.wantBlock = false;
V.player.stamina = 100;
V.player.stamRegenT = 99999;
const pbSlashX0 = V.player.x;
const pbSlashSt0 = V.player.stamina;
tapYouAway();
V.update(STEP);
const pbSlashSpent = pbSlashSt0 - V.player.stamina;
waitPush(12);
const pbSlashDx = Math.abs(V.player.x - pbSlashX0);
check("pushblock still works vs Space", V.player.guarding && pbSlashSpent>=V.PUSHBLOCK_STAM-1 && pbSlashSpent<=V.PUSHBLOCK_STAM+2 && V.player.pushT>=0 && pbSlashDx>=V.PUSHBLOCK_PX-30 && pbSlashDx<=V.PUSHBLOCK_PX+20, `g=${V.player.guarding} spent=${pbSlashSpent.toFixed(2)} dx=${pbSlashDx.toFixed(1)} pushT=${V.player.pushT} rcut=${V.rival.cut}`);
V.keys.clear();

// pushblock still works vs L
toClose();
raiseGuardInRoom();
holdRivalStill();
V.rival.cut = "golpe";
V.rival.phase = "startup";
V.rival.phaseT = 20;
V.rival.guarding = false;
V.rival.wantBlock = false;
V.player.stamina = 100;
V.player.stamRegenT = 99999;
const pbGolpeX0 = V.player.x;
const pbGolpeSt0 = V.player.stamina;
tapYouAway();
V.update(STEP);
const pbGolpeSpent = pbGolpeSt0 - V.player.stamina;
waitPush(12);
const pbGolpeDx = Math.abs(V.player.x - pbGolpeX0);
check("pushblock still works vs L", V.player.guarding && pbGolpeSpent>=V.PUSHBLOCK_STAM-1 && pbGolpeSpent<=V.PUSHBLOCK_STAM+2 && pbGolpeDx>=V.PUSHBLOCK_PX-30 && pbGolpeDx<=V.PUSHBLOCK_PX+20, `g=${V.player.guarding} spent=${pbGolpeSpent.toFixed(2)} dx=${pbGolpeDx.toFixed(1)} rcut=${V.rival.cut}`);
V.keys.clear();

// reversal chord unchanged
toClose();
raiseYouGuard();
const v152RevSt0 = V.player.stamina;
fireReversalChord();
V.update(STEP);
check("reversal chord unchanged after throw-pushblock gate", V.player.cut==="golpe" && V.player.phase==="startup" && V.player.reversal && !V.player.guarding && V.player.cut!=="throw" && Math.abs((v152RevSt0-V.player.stamina)-V.REVERSAL_STAM)<3, `cut=${V.player.cut} rev=${V.player.reversal} ph=${V.player.phase} spent=${(v152RevSt0-V.player.stamina).toFixed(2)}`);
V.keys.clear();

// throw chord unchanged (Space+S close still throws, not pushblock)
toClose();
raiseGuardInRoom();
V.rival.stamina = 80;
V.rival.stamRegenT = 99999;
V.rival.wantBlock = true;
V.rival.guarding = true;
V.rival.guardChoice = 1;
V.rival.throwTechArmed = -1;
V.player.x = V.rival.x - 160;
V.keepApart();
V.keys.add("KeyS");
V.keys.add("Space");
V.throwEdge = true;
let v152Throw=false;
for(let i=0;i<80;i++){
  holdRivalStill();
  V.rival.wantBlock = true;
  V.rival.guarding = true;
  V.rival.guardChoice = 1;
  V.rival.throwTechArmed = -1;
  V.update(STEP);
  if(V.hitstopLeft>0 || V.rival.hp<100 || V.rival.thrownT>0){ v152Throw=true; break; }
}
drainHitstop();
check("throw chord unchanged after throw-pushblock gate", v152Throw && V.player.cut==="throw" && V.player.cutHit && V.rival.hp===80 && V.rival.thrownT>0 && V.player.cut!=="golpe", `hit=${v152Throw} cut=${V.player.cut} ch=${V.player.cutHit} rhp=${V.rival.hp} th=${V.rival.thrownT}`);
V.keys.clear();

check("cache v=296 after throw vs pushblock", /game.js\?v=350/.test(htmlTxt));

check("hint HUD source", /function drawControlsHint/.test(srcTxt) && /function controlsHintLines/.test(srcTxt) && /HINT_FADE_MS/.test(srcTxt) && /HINT_IN_MS/.test(srcTxt) && /HINT_HOLD_MS/.test(srcTxt) && /HINT_VERB_N/.test(srcTxt) && /ANDAR/.test(srcTxt) && /AGARRE/.test(srcTxt) && /RIPOSTE/.test(srcTxt) && /PARRY>/.test(srcTxt) && /bindPrimary\("left"\)/.test(srcTxt));
check("hint patio clean no card", /function drawControlsHint/.test(srcTxt) && !/function drawControlsHint[\s\S]{0,900}fillRect/.test(srcTxt) && /drawHud\(\)[\s\S]{0,180}drawControlsHint/.test(srcTxt));
check("hint fade contract", V.HINT_IN_MS===520 && V.HINT_FADE_MS===640 && V.HINT_HOLD_MS===12000 && V.HINT_VERB_N===3);

toClose();
V.hintDone = false; V.hintFade = 0; V.hintIn = V.HINT_IN_MS;
check("hint showing before connect", !V.hintDone && V.hintFade===0, `done=${V.hintDone} fade=${V.hintFade}`);
V.attackEdge = true;
for(let i=0;i<80;i++){ V.update(STEP); if(V.hitstopLeft>0 || V.rival.hp<100) break; }
const hintFadeAtHit = V.hintFade;
const hintHsAtHit = V.hitstopLeft;
check("hint fade arms on connect", V.hintDone && hintFadeAtHit>=V.HINT_FADE_MS-STEP*2 && hintHsAtHit>0, `done=${V.hintDone} fade=${hintFadeAtHit} hs=${hintHsAtHit}`);
for(let i=0;i<40;i++){ if(V.hitstopLeft<=0) break; V.update(STEP); }
check("hint fade holds through hitstop", V.hintDone && Math.abs(V.hintFade-hintFadeAtHit)<STEP*2, `fade=${V.hintFade} atHit=${hintFadeAtHit} hs=${V.hitstopLeft}`);
for(let i=0;i<Math.ceil((V.HINT_FADE_MS+80)/STEP);i++){
  freezeRivalAI();
  freezeYouAI();
  V.hitstopLeft = 0;
  V.update(STEP);
}
check("hint gone after fade", V.hintDone && V.hintFade===0, `done=${V.hintDone} fade=${V.hintFade}`);
V.resetRound();
check("hint stays gone on rematch", V.hintDone && V.hintFade===0, `done=${V.hintDone} fade=${V.hintFade} in=${V.hintIn}`);

check("recovery sheathe crossfades cut", /function sheatheFade/.test(srcTxt) && /function slashBitmap/.test(srcTxt) && /sheatheFade\(f\)/.test(srcTxt) && /octx.globalAlpha = fade/.test(srcTxt) && /slashBitmap\(f\)/.test(srcTxt));
check("sheathe fade is draw-only", /poseBitmap still holds the cut while planted/.test(srcTxt) && /sheathing\(\) skips/.test(srcTxt));

toMeasure();
V.attackEdge = true;
let ssRec=false, ssFade0=-1, ssSheet=false, ssSheatheRec=-1;
for(let i=0;i<90;i++){
  freezeRivalAI();
  V.hitstopLeft = 0;
  V.update(STEP);
  if(V.player.phase==="recovery" && V.player.cut==="slash" && !V.player.clashRec && !V.player.techRec){
    ssRec=true; ssFade0=V.sheatheFade(V.player); ssSheet=isCutSheet(V.player); ssSheatheRec=V.player.sheatheT; break;
  }
}
check("standing slash recovery fade starts high", ssRec && ssFade0>0.85 && ssSheet && ssSheatheRec===0, `rec=${ssRec} fade=${ssFade0} cut=${ssSheet} sh=${ssSheatheRec} ph=${V.player.phase} cutk=${V.player.cut}`);
let ssIdle=false, ssSheatheIdle=-1, ssFadeIdle=-1, ssIdleCut=false;
for(let i=0;i<40;i++){
  freezeRivalAI();
  V.hitstopLeft = 0;
  V.update(STEP);
  if(V.player.phase==="idle"){
    ssIdle=true; ssSheatheIdle=V.player.sheatheT; ssFadeIdle=V.sheatheFade(V.player); ssIdleCut=isCutSheet(V.player); break;
  }
}
check("standing slash idle does not re-arm sheathe", ssIdle && ssSheatheIdle===0 && ssFadeIdle===0 && !ssIdleCut, `idle=${ssIdle} sh=${ssSheatheIdle} fade=${ssFadeIdle} cut=${ssIdleCut} ph=${V.player.phase} src=${sheetSrc(V.player)}`);

toMeasure();
V.attackEdge = true;
let walkSheathe=false, walkIdle=false, walkSh=0;
for(let i=0;i<90;i++){
  freezeRivalAI();
  V.keys.add("KeyA");
  V.hitstopLeft = 0;
  V.update(STEP);
  if(V.player.phase==="idle" && !V.player.boltPhase){
    walkIdle=true; walkSh=V.player.sheatheT; if(V.player.sheatheT>0) walkSheathe=true; break;
  }
}
V.keys.clear();
check("walk after slash does not re-arm sheathe", walkIdle && !walkSheathe && walkSh===0, `idle=${walkIdle} saw=${walkSheathe} t=${walkSh} gait=${V.player.gait}`);

check("cache v=296 after sheathe fade", /game.js\?v=350/.test(htmlTxt));

check("golpe sheathe fades during recovery", /f\.cut === "golpe"/.test(sliceFn(codeOnly, "sheatheFade")) && /clashRec/.test(sliceFn(codeOnly, "sheatheFade")) && /golpeSheathed/.test(srcTxt));
check("golpe sheathe is draw-only leftover", /Golpe is a poke/.test(srcTxt) && /poseBitmap still holds the cut while planted/.test(srcTxt) && /f\.cut === "golpe"/.test(sliceFn(codeOnly, "poseBitmap"))===false);
check("clash/tech/feint still idle sheathe envelope", /f\.sheatheT \/ SHEATHE_MS/.test(sliceFn(codeOnly, "sheatheFade")) && /1 - u \* u \* \(3 - 2 \* u\)/.test(sliceFn(codeOnly, "sheatheFade")));
check("slash sheathe fades during recovery", /f\.cut === "slash"/.test(sliceFn(codeOnly, "sheatheFade")) && /slashSheathed/.test(srcTxt) && /f\.cut === "golpe" \|\| f\.cut === "slash"/.test(sliceFn(codeOnly, "sheatheFade")));
check("slash sheathe is draw-only leftover", /Slash used to hold full-extension/.test(srcTxt) && /poseBitmap still holds the cut while planted/.test(srcTxt) && /f\.cut === "slash"/.test(sliceFn(codeOnly, "poseBitmap"))===false);
check("golpe frames still 120/80/180 after sheathe fade", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("slash frames still 180/140/280 after golpe sheathe", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);

toMeasure();
freezeRivalAI();
freezeYouAI();
V.player.guarding = false;
V.player.stunT = 0;
V.player.sheatheT = 0;
V.keys.clear();
V.golpeEdge = true;
V.update(STEP);
let gsRec=false, gsFade0=-1, gsSheet=false, gsSheatheRec=-1;
for(let i=0;i<80;i++){
  freezeRivalAI();
  V.hitstopLeft = 0;
  V.update(STEP);
  if(V.player.phase==="recovery" && V.player.cut==="golpe"){
    gsRec=true;
    gsFade0=V.sheatheFade(V.player);
    gsSheet=isCutSheet(V.player);
    gsSheatheRec=V.player.sheatheT;
    break;
  }
}
check("golpe recovery fade starts high", gsRec && gsFade0>0.85 && gsSheet && gsSheatheRec===0, `rec=${gsRec} fade=${gsFade0} cut=${gsSheet} sh=${gsSheatheRec} ph=${V.player.phase} cutk=${V.player.cut}`);
let gsFadeMid=-1;
for(let i=0;i<8;i++){
  freezeRivalAI();
  V.hitstopLeft = 0;
  V.update(STEP);
  if(V.player.phase==="recovery" && V.player.cut==="golpe") gsFadeMid=V.sheatheFade(V.player);
}
check("golpe fade drops during recovery", gsFadeMid>=0 && gsFadeMid<gsFade0-0.08 && V.player.phase==="recovery" && V.player.cut==="golpe", `mid=${gsFadeMid} t0=${gsFade0} ph=${V.player.phase} cut=${V.player.cut}`);
let gsIdle=false, gsSheatheIdle=-1, gsFadeIdle=-1;
for(let i=0;i<40;i++){
  freezeRivalAI();
  V.hitstopLeft = 0;
  V.update(STEP);
  if(V.player.phase==="idle"){
    gsIdle=true;
    gsSheatheIdle=V.player.sheatheT;
    gsFadeIdle=V.sheatheFade(V.player);
    break;
  }
}
check("golpe idle does not re-arm tajo sheathe", gsIdle && gsSheatheIdle===0 && gsFadeIdle===0 && V.player.cut==="slash", `idle=${gsIdle} sh=${gsSheatheIdle} fade=${gsFadeIdle} cut=${V.player.cut} ph=${V.player.phase}`);
check("cache v=296 after golpe sheathe", /game.js\?v=350/.test(htmlTxt));

toMeasure();
freezeRivalAI();
freezeYouAI();
V.player.guarding = false;
V.player.stunT = 0;
V.player.sheatheT = 0;
V.keys.clear();
V.attackEdge = true;
V.update(STEP);
let slRec=false, slFade0=-1, slSheet=false, slSheatheRec=-1;
for(let i=0;i<80;i++){
  freezeRivalAI();
  V.hitstopLeft = 0;
  V.update(STEP);
  if(V.player.phase==="recovery" && V.player.cut==="slash" && !V.player.clashRec && !V.player.techRec){
    slRec=true;
    slFade0=V.sheatheFade(V.player);
    slSheet=isCutSheet(V.player);
    slSheatheRec=V.player.sheatheT;
    break;
  }
}
check("slash recovery fade starts high", slRec && slFade0>0.85 && slSheet && slSheatheRec===0, `rec=${slRec} fade=${slFade0} cut=${slSheet} sh=${slSheatheRec} ph=${V.player.phase} cutk=${V.player.cut}`);
let slFadeMid=-1;
for(let i=0;i<8;i++){
  freezeRivalAI();
  V.hitstopLeft = 0;
  V.update(STEP);
  if(V.player.phase==="recovery" && V.player.cut==="slash") slFadeMid=V.sheatheFade(V.player);
}
check("slash fade drops during recovery", slFadeMid>=0 && slFadeMid<slFade0-0.08 && V.player.phase==="recovery" && V.player.cut==="slash", `mid=${slFadeMid} t0=${slFade0} ph=${V.player.phase} cut=${V.player.cut}`);
let slIdle=false, slSheatheIdle=-1, slFadeIdle=-1;
for(let i=0;i<40;i++){
  freezeRivalAI();
  V.hitstopLeft = 0;
  V.update(STEP);
  if(V.player.phase==="idle"){
    slIdle=true;
    slSheatheIdle=V.player.sheatheT;
    slFadeIdle=V.sheatheFade(V.player);
    break;
  }
}
check("slash idle does not re-arm tajo sheathe", slIdle && slSheatheIdle===0 && slFadeIdle===0 && V.player.cut==="slash", `idle=${slIdle} sh=${slSheatheIdle} fade=${slFadeIdle} cut=${V.player.cut} ph=${V.player.phase}`);
check("slash frames still 180/140/280 after slash sheathe", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);

landClash();
const clFade = V.sheatheFade(V.player);
check("clash recovery holds blade out", V.player.phase==="recovery" && V.player.clashRec && V.player.cut==="slash" && clFade===0 && isCutSheet(V.player), `ph=${V.player.phase} rec=${V.player.clashRec} fade=${clFade} src=${sheetSrc(V.player)}`);
let clIdle=false, clSheathe=-1, clFadeIdle=-1, clCut=false;
for(let i=0;i<40;i++){
  holdRival();
  V.hitstopLeft = 0;
  V.update(STEP);
  if(V.player.phase==="idle" && !V.player.boltPhase){
    clIdle=true; clSheathe=V.player.sheatheT; clFadeIdle=V.sheatheFade(V.player); clCut=isCutSheet(V.player); break;
  }
}
check("clash idle still sheathes from the cut", clIdle && clSheathe>100 && clFadeIdle>0.7 && clCut, `idle=${clIdle} sh=${clSheathe} fade=${clFadeIdle} cut=${clCut} src=${sheetSrc(V.player)}`);
check("cache v=296 after slash sheathe", /game.js\?v=350/.test(htmlTxt));

check("walk fade is draw-only leftover", /function walkFade/.test(srcTxt) && /gaitWalkOn still hard-cuts/.test(srcTxt) && /if \(gaitWalkOn\(f\) && ready\(walk\)\) return walk;/.test(srcTxt));
check("gaitWalkOn sheet still abs sin>0.28", /function gaitWalkOn[\s\S]{0,280}Math\.abs\(Math\.sin\(a\)\) > 0\.28/.test(srcTxt));
check("walkSheetK uses abs sin both boots", /function walkSheetK[\s\S]{0,280}Math\.abs\(Math\.sin\(a\)\)/.test(srcTxt) && !/const u = Math\.max\(0, Math\.sin\(a\)\)/.test(srcTxt));
check("walk fade still draw-only after abs", /gaitWalkOn still hard-cuts/.test(srcTxt) && /if \(gaitWalkOn\(f\) && ready\(walk\)\) return walk;/.test(srcTxt));
check("cache v=296 after walk abs", /game.js\?v=350/.test(htmlTxt));


bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x = 400; V.rival.x = 1100;
V.player.guarding = false; V.player.stunT = 0; V.player.phase = "idle";
V.player.boltPhase = ""; V.player.falling = false;
V.keys.add("KeyD");
for(let i=0;i<8;i++){
  freezeRivalAI();
  V.player.phase="idle"; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase="";
  V.update(STEP);
}
check("walk abs setup gait on", V.player.gait!==0 && V.player.phase==="idle", `gait=${V.player.gait} ph=${V.player.phase} t=${V.player.walkT}`);
V.player.walkT = V.WALK_STEP_MS * 0.5;
const wFade1=V.walkFade(V.player), wOn1=V.gaitWalkOn(V.player), wAabb1=V.bodyAABB(V.player);
check("first boot passing fade high", wOn1 && wFade1>0.75, `on=${wOn1} fade=${wFade1} t=${V.player.walkT}`);
V.player.walkT = V.WALK_STEP_MS * 1.5;
const wFade2=V.walkFade(V.player), wOn2=V.gaitWalkOn(V.player), wAabb2=V.bodyAABB(V.player);
check("back boot passing fade high", wOn2 && wFade2>0.75, `on=${wOn2} fade=${wFade2} t=${V.player.walkT}`);
check("walk AABB planted both boots", Math.abs(wAabb1.y-wAabb2.y)<1.5 && Math.abs(wAabb1.h-wAabb2.h)<1.5, `y1=${wAabb1.y} y2=${wAabb2.y}`);
V.player.walkT = V.WALK_STEP_MS * 2;
const wFadePlant=V.walkFade(V.player), wOnPlant=V.gaitWalkOn(V.player);
check("plant sheet at boot", !wOnPlant && wFadePlant<0.25, `on=${wOnPlant} fade=${wFadePlant} t=${V.player.walkT}`);
V.keys.clear();
check("slash frames still 180/140/280 after walk abs", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("cache v=296 after walk abs live", /game.js\?v=350/.test(htmlTxt));

check("feint recovery 100 faster than slash rec", V.FEINT_RECOVERY===100 && V.FEINT_RECOVERY<V.RECOVERY && V.FEINT_RECOVERY>=80 && V.FEINT_RECOVERY<=120, `feint=${V.FEINT_RECOVERY} rec=${V.RECOVERY}`);
check("feint AI 40/1800 not a robot", V.FEINT_AI_CD===1800 && V.FEINT_AI_CHANCE===0.4 && V.FEINT_AI_CD===V.REVERSAL_AI_CD);
check("feint is slash startup only", /function canFeint/.test(srcTxt) && /f\.cut !== "slash"/.test(srcTxt) && /f\.phase !== "startup"/.test(srcTxt) && /function startFeint/.test(srcTxt));
check("feint does not steal idle throw", /canFeint\(player\)/.test(srcTxt) && /throwEdge && canFeint/.test(srcTxt) && /Slash startup: tap S is feint/.test(srcTxt) && /Idle hold-S tap-Space still throws/.test(srcTxt));

toClose();
freezeRivalAI();
holdRivalStill();
V.player.guarding = false;
V.player.stunT = 0;
V.player.feintT = 0;
V.keys.clear();
V.attackEdge = true;
V.update(STEP);
check("slash arms startup for feint", V.player.phase==="startup" && V.player.cut==="slash" && V.canFeint(V.player), `ph=${V.player.phase} cut=${V.player.cut} can=${V.canFeint(V.player)}`);
for(let i=0;i<4;i++){ holdRivalStill(); V.rival.guarding=false; V.update(STEP); }
check("still startup before S feint", V.player.phase==="startup" && V.player.cut==="slash" && V.canFeint(V.player) && V.hitbox(V.player)===null, `ph=${V.player.phase} t=${V.player.phaseT} hb=${V.hitbox(V.player)}`);
const feintHp = V.rival.hp;
V.feintEdge = true;
holdRivalStill();
V.update(STEP);
check("feint pulls to idle with short rec", V.player.phase==="idle" && V.player.feintT>0 && V.player.feintT<=V.FEINT_RECOVERY && V.player.sheatheT>0 && V.hitbox(V.player)===null, `ph=${V.player.phase} ft=${V.player.feintT} sh=${V.player.sheatheT} hb=${!!V.hitbox(V.player)}`);
for(let i=0;i<50;i++){
  holdRivalStill();
  V.rival.guarding = false;
  V.rival.wantBlock = false;
  V.update(STEP);
}
check("feint in startup produces no hit", V.rival.hp===feintHp && feintHp===100 && !V.player.cutHit && V.player.phase!=="active", `rhp=${V.rival.hp} ch=${V.player.cutHit} ph=${V.player.phase} ft=${V.player.feintT}`);

toMeasure();
holdRivalStill();
V.player.x = 180;
V.rival.x = 1100;
V.player.phase = "idle"; V.player.phaseT = 0; V.player.feintT = 0;
V.player.guarding = false;
V.keys.clear();
V.attackEdge = true;
V.update(STEP);
for(let i=0;i<Math.ceil((V.STARTUP+STEP)/STEP);i++){
  holdRivalStill();
  V.rival.guarding = false;
  if(V.player.phase==="active") break;
  V.update(STEP);
}
check("slash reached active for late feint", V.player.phase==="active" && V.player.cut==="slash", `ph=${V.player.phase} t=${V.player.phaseT} cut=${V.player.cut}`);
const actPh = V.player.phase;
const actT = V.player.phaseT;
V.feintEdge = true;
holdRivalStill();
V.update(STEP);
check("feint after active does nothing", V.player.phase==="active" && V.player.feintT===0 && V.player.cut==="slash" && !V.canFeint(V.player), `ph=${V.player.phase} ft=${V.player.feintT} was=${actPh}/${actT.toFixed(0)}`);

toClose();
holdRivalStill();
V.player.phase = "idle"; V.player.phaseT = 0; V.player.feintT = 0;
V.golpeEdge = true;
V.update(STEP);
check("golpe startup is not feintable", V.player.phase==="startup" && V.player.cut==="golpe" && !V.canFeint(V.player), `ph=${V.player.phase} cut=${V.player.cut} can=${V.canFeint(V.player)}`);
V.feintEdge = true;
holdRivalStill();
V.update(STEP);
check("feint does nothing on golpe", V.player.phase==="startup" && V.player.cut==="golpe" && V.player.feintT===0, `ph=${V.player.phase} cut=${V.player.cut} ft=${V.player.feintT}`);
V.player.phase = "idle"; V.player.phaseT = 0;

toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100; V.rival.stamina = 80;
V.rival.wantBlock = true;
V.rival.guarding = true;
V.rival.guardChoice = 1;
V.player.feintT = 0;
fireThrow();
let feintThrowHit=false;
for(let i=0;i<80;i++){
  holdRivalStill();
  V.rival.wantBlock = true;
  V.rival.guarding = true;
  V.rival.guardChoice = 1;
  V.update(STEP);
  if(V.hitstopLeft>0 || V.rival.hp<100 || V.rival.thrownT>0){ feintThrowHit=true; break; }
}
drainHitstop();
check("idle Space+S throw still works", feintThrowHit && V.player.cut==="throw" && V.player.cutHit && V.rival.hp===80 && V.rival.thrownT>0, `hit=${feintThrowHit} cut=${V.player.cut} ch=${V.player.cutHit} rhp=${V.rival.hp} th=${V.rival.thrownT} ph=${V.player.phase}`);
V.keys.clear();

toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100; V.rival.stamina = 80;
V.rival.wantBlock = true;
V.rival.guarding = true;
V.rival.guardChoice = 1;
const zsFeint = fakeZone();
const zslFeint = fakeZone();
padChord(zsFeint, zslFeint);
let padThrowAfterFeint=false;
for(let i=0;i<80;i++){
  holdRivalStill();
  V.rival.wantBlock = true;
  V.rival.guarding = true;
  V.rival.guardChoice = 1;
  V.update(STEP);
  if(V.hitstopLeft>0 || V.rival.hp<100 || V.rival.thrownT>0){ padThrowAfterFeint=true; break; }
}
drainHitstop();
padChordUp();
check("pad chord throw still works", padThrowAfterFeint && V.player.cut==="throw" && V.player.cutHit && V.rival.hp===80, `hit=${padThrowAfterFeint} cut=${V.player.cut} ch=${V.player.cutHit} rhp=${V.rival.hp}`);

toClose();
holdRivalStill();
V.player.phase = "idle"; V.player.guarding = true; V.player.wantBlock = true;
V.rival.phase = "idle"; V.rival.feintT = 0; V.rival.feintCd = 0; V.rival.feintArmed = 0;
V.startAttack(V.rival, "slash");
check("rival slash startup for AI feint", V.rival.phase==="startup" && V.canFeint(V.rival), `rph=${V.rival.phase} can=${V.canFeint(V.rival)}`);
V.rival.feintArmed = 1;
V.rival.feintCd = 0;
V.player.guarding = true;
holdRivalStill();
V.rival.feintCd = 0;
V.rival.feintArmed = 1;
const didAiFeint = V.rivalTryFeint();
check("rival feints vs committed guard", didAiFeint && V.rival.phase==="idle" && V.rival.feintT>0 && V.rival.feintCd===V.FEINT_AI_CD, `ok=${didAiFeint} rph=${V.rival.phase} ft=${V.rival.feintT} cd=${V.rival.feintCd}`);

toClose();
holdRivalStill();
V.player.phase = "idle"; V.player.guarding = false;
V.rival.phase = "idle"; V.rival.feintT = 0; V.rival.feintCd = 0; V.rival.feintArmed = 0;
V.startAttack(V.rival, "slash");
V.rival.feintArmed = 1;
V.rival.feintCd = 0;
const noGuardFeint = V.rivalTryFeint();
check("rival does not feint vs no guard", !noGuardFeint && V.rival.phase==="startup", `ok=${noGuardFeint} rph=${V.rival.phase}`);

check("cache v=296 after feint", /game.js\?v=350/.test(htmlTxt));

check("title pixel not Palatino", /function drawPixelText/.test(srcTxt) && /function drawTitleCard/.test(srcTxt) && /FONT5/.test(srcTxt) && !/Palatino/.test(codeOnly) && !/fillText\("Víspera"/.test(srcTxt) && !/fillText\("Espacio"/.test(srcTxt));
check("menu items JUGAR CONTROLES", /JUGAR/.test(srcTxt) && /CONTROLES/.test(srcTxt) && /OPCIONES/.test(srcTxt) && /ESCENARIOS/.test(srcTxt) && /VOLVER/.test(srcTxt) && /REVANCHA/.test(srcTxt) && /function confirmTitle/.test(srcTxt) && /function handleTitleKey/.test(srcTxt));
check("joystick feeds KeyA KeyD", /function stickApply/.test(srcTxt) && /feedKey\("KeyA"/.test(srcTxt) && /feedKey\("KeyD"/.test(srcTxt) && /class="stick"/.test(htmlTxt) && /stick-knob/.test(htmlTxt) && /STICK_DEAD/.test(srcTxt) && /setPointerCapture/.test(srcTxt));
check("stick vertical ignored", /Vertical stick does nothing/.test(srcTxt) && /stickApply\(maxR > 0 \? dx \/ maxR : 0\)/.test(srcTxt));
check("pad still guarda tajo golpe dardo", /guarda/.test(htmlTxt) && /tajo/.test(htmlTxt) && /golpe/.test(htmlTxt) && /dardo/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt) && /data-tap="Space"/.test(htmlTxt) && /data-tap="KeyL"/.test(htmlTxt) && /data-tap="KeyK"/.test(htmlTxt) && (htmlTxt.match(/data-tap=/g)||[]).length===3);
check("keyboard path untouched after stick", /KeyA/.test(srcTxt) && /KeyD/.test(srcTxt) && /Space/.test(srcTxt) && /KeyJ/.test(srcTxt) && /KeyS/.test(srcTxt) && /KeyK/.test(srcTxt) && /KeyL/.test(srcTxt) && /KeyR/.test(srcTxt) && /act === "dart"/.test(srcTxt) && /boltEdge = true/.test(srcTxt));
check("cache v=296 after joystick menu", /game.js\?v=350/.test(htmlTxt));

bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.stickApply(-0.9);
check("stick left feeds KeyA", V.keys.has("KeyA") && !V.keys.has("KeyD"), `a=${V.keys.has("KeyA")} d=${V.keys.has("KeyD")}`);
V.stickApply(0.9);
check("stick right feeds KeyD not A", V.keys.has("KeyD") && !V.keys.has("KeyA"), `a=${V.keys.has("KeyA")} d=${V.keys.has("KeyD")}`);
V.stickApply(0);
check("stick deadzone releases A D", !V.keys.has("KeyA") && !V.keys.has("KeyD"), `a=${V.keys.has("KeyA")} d=${V.keys.has("KeyD")}`);
V.stickApply(0.1);
check("stick inside deadzone idle", !V.keys.has("KeyA") && !V.keys.has("KeyD"));
V.stickRelease();

V.mode = "title";
V.titlePage = "root";
V.titleSel = 0;
V.confirmTitle();
V.update(STEP);
check("JUGAR confirm starts duel", V.mode==="play", `mode=${V.mode} page=${V.titlePage}`);

V.mode = "title";
V.titlePage = "root";
V.titleSel = 1;
V.confirmTitle();
check("CONTROLES opens list", V.mode==="title" && V.titlePage==="controls", `mode=${V.mode} page=${V.titlePage}`);
V.confirmTitle();
check("VOLVER returns to root", V.mode==="title" && V.titlePage==="root" && V.titleSel===1, `mode=${V.mode} page=${V.titlePage} sel=${V.titleSel}`);

V.mode = "title";
V.titlePage = "root";
V.titleSel = 0;
V.handleTitleKey("ArrowDown");
check("arrow down selects CONTROLES", V.titleSel===1, `sel=${V.titleSel}`);
V.handleTitleKey("ArrowUp");
check("arrow up selects JUGAR", V.titleSel===0, `sel=${V.titleSel}`);
V.handleTitleKey("KeyS");
check("S selects CONTROLES", V.titleSel===1, `sel=${V.titleSel}`);
V.handleTitleKey("KeyW");
check("W selects JUGAR", V.titleSel===0, `sel=${V.titleSel}`);



check("wakeup fade crossfades hurt", /function wakeupFade/.test(srcTxt) && /function hurtBitmap/.test(srcTxt) && /wakeupFade\(f\)/.test(srcTxt) && /octx.globalAlpha = wake/.test(srcTxt) && /hurtBitmap\(f\)/.test(srcTxt));
check("wakeup fade is draw-only leftover", /Getup used to pop/.test(srcTxt) && /poseBitmap still idle once thrownT is 0/.test(srcTxt) && /f\.thrownT > 0/.test(sliceFn(codeOnly, "poseBitmap")));
check("wakeup fade does not keep hurt in poseBitmap", /throwInvulnT/.test(sliceFn(codeOnly, "wakeupFade")) && !/throwInvulnT/.test(sliceFn(codeOnly, "poseBitmap")));
check("wakeup invuln still 80 after fade", V.THROW_WAKE_INVULN===80 && V.THROW_WAKE_INVULN===V.THROW_STARTUP && V.THROW_KD_MS===520);
check("cache v=296 after wakeup fade", /game.js\?v=350/.test(htmlTxt));

function holdWakeSample(){
  holdRivalNoGrab();
  V.rival.wantBlock = false;
  V.rival.guardChoice = -1;
  V.rival.guarding = false;
  V.rival.gait = 0;
  V.rival.closing = false;
  V.player.guarding = false;
  V.player.phase = "idle";
  V.player.phaseT = 0;
  V.player.cut = "slash";
  V.hitstopLeft = 0;
}
function isHurtSheet(f){ return /hurt/.test(sheetSrc(f)); }

const wakeLand2 = landCloseThrow();
check("wakeup fade setup throw landed", wakeLand2 && V.rival.hp===80 && V.rival.thrownT>0, `hit=${wakeLand2} hp=${V.rival.hp} th=${V.rival.thrownT}`);
let wfSaw=false, wfFade0=-1, wfInv0=-1, wfHurtSheet=false;
for(let i=0;i<80;i++){
  holdWakeSample();
  V.update(STEP);
  if(V.rival.thrownT<=0 && V.rival.throwInvulnT>0 && V.rival.hp>0){
    wfSaw=true;
    wfFade0=V.wakeupFade(V.rival);
    wfInv0=V.rival.throwInvulnT;
    wfHurtSheet=isHurtSheet(V.rival);
    break;
  }
}
check("wakeup fade starts high", wfSaw && wfFade0>0.85 && wfInv0>60 && !wfHurtSheet && V.rival.phase==="idle", `saw=${wfSaw} fade=${wfFade0} inv=${wfInv0} hurt=${wfHurtSheet} src=${sheetSrc(V.rival)} ph=${V.rival.phase} th=${V.rival.thrownT} g=${V.rival.guarding}`);
let wfFadeMid=-1;
for(let i=0;i<3;i++){
  holdWakeSample();
  V.update(STEP);
  if(V.rival.throwInvulnT>0) wfFadeMid=V.wakeupFade(V.rival);
}
check("wakeup fade drops during invuln", wfFadeMid>=0 && wfFadeMid<wfFade0-0.08 && V.rival.throwInvulnT>0 && V.rival.thrownT===0 && !isHurtSheet(V.rival), `mid=${wfFadeMid} t0=${wfFade0} inv=${V.rival.throwInvulnT} th=${V.rival.thrownT} src=${sheetSrc(V.rival)}`);
let wfIdle=false, wfFadeIdle=-1, wfInvIdle=-1;
for(let i=0;i<20;i++){
  holdWakeSample();
  V.update(STEP);
  if(V.rival.throwInvulnT===0 && V.rival.thrownT===0){
    wfIdle=true;
    wfFadeIdle=V.wakeupFade(V.rival);
    wfInvIdle=V.rival.throwInvulnT;
    break;
  }
}
check("wakeup idle fade gone", wfIdle && wfFadeIdle===0 && wfInvIdle===0 && !isHurtSheet(V.rival), `idle=${wfIdle} fade=${wfFadeIdle} inv=${wfInvIdle} src=${sheetSrc(V.rival)}`);

toClose();
freezeYouAI();
freezeRivalAI();
V.player.stunT = V.HITSTUN;
V.player.thrownT = 0;
V.player.throwInvulnT = 0;
V.player.hp = 80;
check("chip stun is not wakeup fade", V.wakeupFade(V.player)===0 && V.player.stunT>0 && !isHurtSheet(V.player), `fade=${V.wakeupFade(V.player)} st=${V.player.stunT} src=${sheetSrc(V.player)}`);

const wakeRevFade = landCloseThrow();
check("wakeup reversal fade setup", wakeRevFade && V.rival.thrownT>0, `hit=${wakeRevFade} th=${V.rival.thrownT}`);
let wrReady=false;
for(let i=0;i<80;i++){
  holdWakeSample();
  V.update(STEP);
  if(V.rival.thrownT<=0 && V.rival.throwInvulnT>0){ wrReady=true; break; }
}
const wrFadeBefore = V.wakeupFade(V.rival);
V.rival.stamina = 100;
const wrDid = V.startWakeReversal(V.rival);
check("wakeup reversal eases crumple leftover", wrReady && wrDid && wrFadeBefore>0.5 && V.wakeupFade(V.rival)>0.5 && V.rival.wakeRevFadeHold>0 && V.rival.throwInvulnT===0 && V.rival.cut==="golpe", `ready=${wrReady} did=${wrDid} fade=${V.wakeupFade(V.rival)} hold=${V.rival.wakeRevFadeHold} inv=${V.rival.throwInvulnT} cut=${V.rival.cut} before=${wrFadeBefore}`);
check("cache v=296 after wakeup reversal fade", /game.js\?v=350/.test(htmlTxt));

check("bolt plant fade crossfades windup", /function boltPlantFade/.test(srcTxt) && /function windupBitmap/.test(srcTxt) && /boltPlantFade\(f\)/.test(srcTxt) && /octx.globalAlpha = bf/.test(srcTxt) && /windupBitmap\(f\)/.test(srcTxt));
check("bolt plant fade is draw-only leftover", /Idle\/walk K used to pop windup/.test(srcTxt) && /poseBitmap still idle once bolt recovery starts/.test(srcTxt) && /boltPhase === "startup"/.test(sliceFn(codeOnly, "poseBitmap")));
check("bolt plant fade does not keep windup in poseBitmap", /boltPhase !== "recovery"/.test(sliceFn(codeOnly, "boltPlantFade")) && !/boltPhase === "recovery"/.test(sliceFn(codeOnly, "poseBitmap")));
check("special-cancel still holds cut not windup fade", /holdingCutBolt\(f\)/.test(sliceFn(codeOnly, "boltPlantFade")) && /holdingCutBolt\(f\)/.test(sliceFn(codeOnly, "poseBitmap")));
check("K frames still 200/280 after plant fade", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380);
check("slash frames still 180/140/280 after plant fade", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("cache v=296 after bolt plant fade", /game.js\?v=350/.test(htmlTxt));

function holdBoltPlant(){
  freezeRivalAI();
  V.player.guarding = false;
  V.player.stunT = 0;
  V.player.falling = false;
  V.player.boltHoldCut = false;
  V.player.sheatheT = 0;
  V.player.guardPoseK = 0;
  V.hitstopLeft = 0;
  V.keys.clear();
  V.rival.x = 1220;
  V.rival.stunT = 0;
  V.rival.guarding = false;
  V.rival.guardPoseK = 0;
}

bootPlay(); wait(1700);
holdBoltPlant();
V.player.stamina = 100;
V.player.stamRegenT = 99999;
V.boltEdge = true;
V.update(STEP);
check("idle K startup fade is 0", V.player.boltPhase==="startup" && V.boltPlantFade(V.player)===0 && /throw_knife/.test(sheetSrc(V.player)), `bph=${V.player.boltPhase} fade=${V.boltPlantFade(V.player)} src=${sheetSrc(V.player)}`);
const bpDest0 = V.destRect(V.player);
let bpSaw=false, bpFade0=-1, bpSrcRec="", bpDestRec=null, bpY0=0, bpY1=0;
bpY0 = V.bodyAABB(V.player).y;
for(let i=0;i<20;i++){
  holdBoltPlant();
  V.update(STEP);
  if(V.player.boltPhase==="recovery"){
    bpSaw=true;
    bpFade0=V.boltPlantFade(V.player);
    bpSrcRec=sheetSrc(V.player);
    bpDestRec=V.destRect(V.player);
    bpY1=V.bodyAABB(V.player).y;
    break;
  }
}
check("idle K recovery fade starts high", bpSaw && bpFade0>0.85 && !isCutSheet(V.player) && !/windup/.test(bpSrcRec), `saw=${bpSaw} fade=${bpFade0} src=${bpSrcRec} bph=${V.player.boltPhase}`);
check("idle K recovery AABB planted", bpSaw && Math.abs(bpY1-bpY0)<0.05, `y0=${bpY0} y1=${bpY1}`);
check("idle K recovery destRect planted", bpSaw && destNear(bpDestRec, bpDest0, 2.0), `dx=${(bpDestRec.dx-bpDest0.dx).toFixed(2)} dy=${(bpDestRec.dy-bpDest0.dy).toFixed(2)}`);
let bpFadeMid=-1;
for(let i=0;i<8;i++){
  holdBoltPlant();
  V.update(STEP);
  if(V.player.boltPhase==="recovery") bpFadeMid=V.boltPlantFade(V.player);
}
check("idle K fade drops during recovery", bpFadeMid>=0 && bpFadeMid<bpFade0-0.08 && V.player.boltPhase==="recovery" && !isCutSheet(V.player), `mid=${bpFadeMid} t0=${bpFade0} bph=${V.player.boltPhase} src=${sheetSrc(V.player)}`);
let bpIdle=false, bpFadeIdle=-1, bpBph="";
for(let i=0;i<40;i++){
  holdBoltPlant();
  V.update(STEP);
  if(!V.player.boltPhase){
    bpIdle=true;
    bpFadeIdle=V.boltPlantFade(V.player);
    bpBph=V.player.boltPhase;
    break;
  }
}
check("idle K idle fade gone", bpIdle && bpFadeIdle===0 && !bpBph && !isCutSheet(V.player) && !/windup/.test(sheetSrc(V.player)), `idle=${bpIdle} fade=${bpFadeIdle} bph=${bpBph} src=${sheetSrc(V.player)}`);

bootPlay(); wait(1700);
holdBoltPlant();
V.player.stamina = 100;
V.player.stamRegenT = 99999;
V.player.phase = "idle";
V.player.phaseT = 0;
V.player.cut = "slash";
V.player.cutHit = true;
V.player.boltHoldCut = true;
const scOk = V.startBolt(V.player, true);
check("special-cancel plant fade setup", scOk && V.player.boltHoldCut && V.player.boltPhase==="startup", `ok=${scOk} hold=${V.player.boltHoldCut} bph=${V.player.boltPhase}`);
for(let i=0;i<20;i++){
  holdBoltPlant();
  V.player.boltHoldCut = true;
  V.update(STEP);
  if(V.player.boltPhase==="recovery") break;
}
check("special-cancel recovery has no windup fade", V.player.boltPhase==="recovery" && V.player.boltHoldCut && V.boltPlantFade(V.player)===0 && isCutSheet(V.player), `bph=${V.player.boltPhase} fade=${V.boltPlantFade(V.player)} hold=${V.player.boltHoldCut} src=${sheetSrc(V.player)}`);
check("cache v=296 after bolt plant live", /game.js\?v=350/.test(htmlTxt));


check("guard raise destRect-only leftover", /function guardRaiseK/.test(srcTxt) && /function tickGuardPose/.test(srcTxt) && /function guardPlant/.test(srcTxt) && /Guard raise no longer plants destRect/.test(srcTxt) && /guardPlant\(f\)/.test(srcTxt));
check("guard raise is not a new combat verb", V.GUARD_RAISE_MS===80 && V.GUARD_RAISE_MS<V.WALK_SETTLE_MS && V.GUARD_COMMIT_MS===140 && /[Rr]aise is still 0ms/.test(srcTxt));
check("raise still 0ms frame data", /Guarda S, de cara, raise 0/.test(srcTxt) && V.STAMINA_START_MIN===15 && V.STAMINA_BLOCK===20 && V.GUARD_BREAK_MS===400);
check("slash frames still 180/140/280 after guard raise", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after guard raise", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("no 6th button after guard raise", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after guard raise", /game.js\?v=350/.test(htmlTxt));

function holdGuardSample(){
  freezeRivalAI();
  freezeYouAI();
  V.player.stunT = 0;
  V.player.falling = false;
  V.player.hp = 100;
  V.player.stamina = 100;
  V.player.stamRegenT = 99999;
  V.player.guardBreakT = 0;
  V.player.boltPhase = "";
  V.player.phase = "idle";
  V.player.phaseT = 0;
  V.player.cut = "slash";
  V.player.gait = 0;
  V.player.walkT = 0;
  V.player.walkSettleT = 0;
  V.player.sheatheT = 0;
  V.hitstopLeft = 0;
  V.keys.clear();
  V.keys.add("KeyS");
}

bootPlay(); wait(1700);
holdGuardSample();
V.player.guarding = false;
V.player.guardPoseK = 0;
const grAabb0 = V.bodyAABB(V.player);
const grDest0 = V.destRect(V.player);
const grRot0 = grDest0.rot;
V.update(STEP);
check("raise still 0ms first tick", V.player.guarding===true && V.player.phase==="idle", `g=${V.player.guarding} ph=${V.player.phase} k=${V.player.guardPoseK}`);
const grAabb1 = V.bodyAABB(V.player);
const grDest1 = V.destRect(V.player);
check("first raise tick AABB planted", Math.abs(grAabb1.y-grAabb0.y)<0.05 && Math.abs(grAabb1.x-grAabb0.x)<0.05 && Math.abs(grAabb1.h-grAabb0.h)<0.05, `dx=${(grAabb1.x-grAabb0.x).toFixed(2)} dy=${(grAabb1.y-grAabb0.y).toFixed(2)}`);
check("first raise tick destRect not full plant", V.player.guardPoseK>0 && V.player.guardPoseK<0.4 && V.guardRaiseK(V.player)<0.35 && Math.abs(grDest1.dy-grDest0.dy)<2.2, `k=${V.player.guardPoseK.toFixed(3)} gk=${V.guardRaiseK(V.player).toFixed(3)} ddy=${(grDest1.dy-grDest0.dy).toFixed(2)} drot=${(grDest1.rot-grRot0).toFixed(3)}`);
let grMidK=-1, grMidDest=null, grMidAabb=null;
for(let i=0;i<3;i++){
  holdGuardSample();
  V.update(STEP);
  grMidK=V.player.guardPoseK;
  grMidDest=V.destRect(V.player);
  grMidAabb=V.bodyAABB(V.player);
}
check("raise eases destRect during 80ms", grMidK>V.player.guardPoseK-1 && grMidK>0.3 && grMidK<1 && Math.abs(grMidDest.dy-grDest0.dy)>0.4 && Math.abs(grMidAabb.y-grAabb0.y)<0.05, `k=${grMidK.toFixed(3)} ddy=${(grMidDest.dy-grDest0.dy).toFixed(2)} aabb=${(grMidAabb.y-grAabb0.y).toFixed(2)}`);
let grFullK=-1, grFullDest=null, grFullAabb=null, grFullRot=0;
for(let i=0;i<10;i++){
  holdGuardSample();
  V.update(STEP);
  grFullK=V.player.guardPoseK;
  grFullDest=V.destRect(V.player);
  grFullAabb=V.bodyAABB(V.player);
  grFullRot=grFullDest.rot;
}
check("raise settles destRect weight", grFullK===1 && V.guardRaiseK(V.player)>0.98 && Math.abs(grFullDest.dy-grDest0.dy)>1.5 && Math.abs(grFullDest.dy-grDest0.dy)<6 && Math.abs(grFullRot)<0.12 && Math.abs(grFullAabb.y-grAabb0.y)<0.05 && Math.abs(grFullDest.dx-grDest0.dx)<0.6, `k=${grFullK} gk=${V.guardRaiseK(V.player).toFixed(3)} ddy=${(grFullDest.dy-grDest0.dy).toFixed(2)} ddx=${(grFullDest.dx-grDest0.dx).toFixed(2)} rot=${grFullRot.toFixed(3)}`);

V.keys.clear();
const dropDest0 = V.destRect(V.player);
const dropAabb0 = V.bodyAABB(V.player);
const dropK0 = V.player.guardPoseK;
holdGuardSample();
V.keys.clear();
V.player.guarding = false;
V.update(STEP);
check("drop starts easing leftover k", V.player.guarding===false && V.player.guardPoseK<dropK0 && V.player.guardPoseK>0.5, `g=${V.player.guarding} k=${V.player.guardPoseK.toFixed(3)} k0=${dropK0}`);
const dropDest1 = V.destRect(V.player);
const dropAabb1 = V.bodyAABB(V.player);
check("drop AABB planted", Math.abs(dropAabb1.y-dropAabb0.y)<0.05 && Math.abs(dropAabb1.x-dropAabb0.x)<0.05, `dy=${(dropAabb1.y-dropAabb0.y).toFixed(2)}`);
check("drop destRect eases not snaps", Math.abs(dropDest1.dy-dropDest0.dy)<1.6 && Math.abs(dropDest1.dy-grDest0.dy)>0.8, `ddy=${(dropDest1.dy-dropDest0.dy).toFixed(2)} vsIdle=${(dropDest1.dy-grDest0.dy).toFixed(2)}`);
for(let i=0;i<10;i++){
  freezeRivalAI(); freezeYouAI();
  V.keys.clear();
  V.player.stunT=0; V.player.falling=false; V.player.phase="idle"; V.player.boltPhase=""; V.hitstopLeft=0;
  V.update(STEP);
}
check("drop idle leftover gone", V.player.guardPoseK===0 && V.guardRaiseK(V.player)===0 && !V.player.guarding, `k=${V.player.guardPoseK} gk=${V.guardRaiseK(V.player)} g=${V.player.guarding}`);

bootPlay(); wait(1700);
holdGuardSample();
V.player.guarding = false;
V.player.guardPoseK = 0;
V.update(STEP);
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
check("held raise before reversal", V.player.guarding && V.player.guardPoseK>0.8, `g=${V.player.guarding} k=${V.player.guardPoseK.toFixed(3)}`);
V.player.stamina = 100;
const revOk = V.startReversal(V.player);
check("reversal leftover plant eases leftover k", revOk && V.player.cut==="golpe" && V.player.phase==="startup" && V.player.reversal && V.guardRaiseK(V.player)>0.8 && V.reversalPlantFade(V.player)>0.8, `ok=${revOk} cut=${V.player.cut} ph=${V.player.phase} gk=${V.guardRaiseK(V.player)} fade=${V.reversalPlantFade(V.player)} k=${V.player.guardPoseK}`);

bootPlay(); wait(1700);
holdGuardSample();
V.update(STEP);
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
V.player.stunT = V.HITSTUN;
check("stun cuts raise k", V.guardRaiseK(V.player)===0, `gk=${V.guardRaiseK(V.player)} k=${V.player.guardPoseK} st=${V.player.stunT}`);

function holdRivalGuard(){
  Math.random = () => 0.99;
  V.player.phase = "startup";
  V.player.phaseT = 90;
  V.player.cut = "slash";
  V.player.cutHit = false;
  V.player.stunT = 0;
  V.player.falling = false;
  V.player.guarding = false;
  V.player.boltPhase = "";
  V.player.gait = 0;
  V.rival.stunT = 0;
  V.rival.falling = false;
  V.rival.hp = 100;
  V.rival.stamina = 100;
  V.rival.stamRegenT = 99999;
  V.rival.guardBreakT = 0;
  V.rival.boltPhase = "";
  V.rival.phase = "idle";
  V.rival.phaseT = 0;
  V.rival.cut = "slash";
  V.rival.gait = 0;
  V.rival.guardChoice = 1;
  V.rival.revArmed = -1;
  V.rival.revCd = 99999;
  V.rival.pbArmed = -1;
  V.rival.pbCd = 99999;
  V.rival.boltCd = 99999;
  V.rival.throwCd = 99999;
  V.rival.closing = false;
  V.hitstopLeft = 0;
  V.keys.clear();
}

bootPlay(); wait(1700);
holdRivalGuard();
V.rival.guardPoseK = 0;
V.rival.guarding = false;
V.rival.wantBlock = false;
const rvAabb0 = V.bodyAABB(V.rival);
const rvDest0 = V.destRect(V.rival);
V.update(STEP);
check("rival raise still 0ms", V.rival.guarding===true, `g=${V.rival.guarding} k=${V.rival.guardPoseK}`);
const rvDest1 = V.destRect(V.rival);
const rvAabb1 = V.bodyAABB(V.rival);
check("rival first raise AABB planted", Math.abs(rvAabb1.y-rvAabb0.y)<0.05 && Math.abs(rvAabb1.x-rvAabb0.x)<0.05, `dy=${(rvAabb1.y-rvAabb0.y).toFixed(2)}`);
check("rival first raise destRect not full plant", V.rival.guardPoseK>0 && V.rival.guardPoseK<0.4 && Math.abs(rvDest1.dy-rvDest0.dy)<2.2, `k=${V.rival.guardPoseK.toFixed(3)} ddy=${(rvDest1.dy-rvDest0.dy).toFixed(2)}`);
for(let i=0;i<10;i++){
  holdRivalGuard();
  V.update(STEP);
}
const rvFull = V.destRect(V.rival);
const rvFullAabb = V.bodyAABB(V.rival);
check("rival raise settles destRect weight", V.rival.guarding && V.rival.guardPoseK===1 && Math.abs(rvFull.dy-rvDest0.dy)>1.5 && Math.abs(rvFullAabb.y-rvAabb0.y)<0.05, `g=${V.rival.guarding} k=${V.rival.guardPoseK} ddy=${(rvFull.dy-rvDest0.dy).toFixed(2)}`);
check("cache v=296 after guard raise live", /game.js\?v=350/.test(htmlTxt));



check("KO flash leftover through crumple", /function hurtFlashK/.test(srcTxt) && /hurtFlashK\(f\)/.test(srcTxt) && /KO used to drop it the tick beginFall/.test(srcTxt) && /KO flash no longer dies the tick beginFall/.test(srcTxt));
check("KO flash is draw-only leftover", /Draw-only/.test(srcTxt) && /hurtFlashK\(f\)/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && /HIT_FLASH_MS 120 still holds/.test(srcTxt) && !/hurtFlashK/.test(sliceFn(codeOnly, "destRect")) && !/hurtFlashK/.test(sliceFn(codeOnly, "bodyAABB")) && !/hurtFlashK/.test(sliceFn(codeOnly, "slashPose")));
check("KO flash is not a new combat verb", V.HIT_FLASH_MS===120 && V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.HIT_FLASH_MS<V.HITSTOP_HIT);
check("slash frames still 180/140/280 after KO flash", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after KO flash", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("no 6th button after KO flash", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after KO flash", /game.js\?v=350/.test(htmlTxt));

bootPlay(); wait(1700);
freezeRivalAI(); freezeYouAI();
V.rival.hp = 10;
V.player.x = V.rival.x - 170;
V.keepApart();
V.attackEdge = true;
let koHs=false, koFlashFreeze=-1, koFall=false, koFlashFall=-1, koYouFlash=-1, koAabb0=null, koAabb1=null, koDest0=null, koDest1=null;
for(let i=0;i<80;i++){
  freezeRivalAI();
  V.update(STEP);
  if(V.hitstopLeft>0 && V.rival.hp<=0){
    koHs=true;
    koFlashFreeze=V.hitFlashT;
    koAabb0=V.bodyAABB(V.rival);
    koDest0=V.destRect(V.rival);
    break;
  }
}
check("KO freeze still flashes", koHs && koFlashFreeze===V.HIT_FLASH_MS && V.hurtFlashK(V.rival)>0.9 && V.hurtFlashK(V.player)===0 && !V.rival.falling, `hs=${koHs} flash=${koFlashFreeze} k=${V.hurtFlashK(V.rival)} you=${V.hurtFlashK(V.player)} fall=${V.rival.falling} hp=${V.rival.hp}`);
for(let i=0;i<20;i++){
  if(V.hitstopLeft<=0 && V.mode==="falling") break;
  V.update(STEP);
}
koFall = V.mode==="falling" && V.rival.falling;
koFlashFall = V.hitFlashT;
koYouFlash = V.hurtFlashK(V.player);
koAabb1=V.bodyAABB(V.rival);
koDest1=V.destRect(V.rival);
check("KO crumple keeps leftover flash", koFall && koFlashFall>0 && V.hurtFlashK(V.rival)>0.5 && koYouFlash===0, `mode=${V.mode} fall=${V.rival.falling} flash=${koFlashFall} k=${V.hurtFlashK(V.rival)} you=${koYouFlash} hs=${V.hitstopLeft}`);
check("KO flash destRect extra rot still 0", koFall && koDest1 && Math.abs(koDest1.rot)<0.001 && V.hurtFlashK(V.rival)>0, `rot=${koDest1?koDest1.rot.toFixed(3):"na"} k=${V.hurtFlashK(V.rival)}`);
let chipFlash=-1, chipK=-1;
bootPlay(); wait(1700);
freezeRivalAI(); freezeYouAI();
V.player.x = V.rival.x - 170;
V.keepApart();
V.attackEdge = true;
for(let i=0;i<80;i++){
  freezeRivalAI();
  V.update(STEP);
  if(V.hitstopLeft>0 && V.rival.hp===90){ chipFlash=V.hitFlashT; chipK=V.hurtFlashK(V.rival); break; }
}
check("chip stun still flashes", chipFlash===V.HIT_FLASH_MS && chipK>0.9 && V.rival.stunT>0 && !V.rival.falling && V.hurtFlashK(V.player)===0, `flash=${chipFlash} k=${chipK} st=${V.rival.stunT} fall=${V.rival.falling}`);
drainHitstop();
check("chip flash leftover after freeze", V.hitFlashT>0 && V.hitFlashT<=V.HIT_FLASH_MS && V.hurtFlashK(V.rival)>0 && !V.rival.falling && V.rival.hp===90, `flash=${V.hitFlashT} k=${V.hurtFlashK(V.rival)} fall=${V.rival.falling} hp=${V.rival.hp}`);
check("cache v=296 after KO flash live", /game.js\?v=350/.test(htmlTxt));


check("meaty wakeup leftover through stun", /function wakeupFade/.test(srcTxt) && /Meaty leftover/.test(srcTxt) && /stun used to dump leftover crumple/.test(srcTxt) && /Meaty wakeup leftover/.test(srcTxt) && /leftover getup fade no longer dies the tick/.test(srcTxt));
check("meaty wakeup is draw-only leftover", /Draw-only \(wakeupFade\)/.test(srcTxt) && /wakeupFade\(f\)/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && /poseBitmap still idle/.test(srcTxt) && !/wakeupFade/.test(sliceFn(codeOnly, "destRect")) && !/wakeupFade/.test(sliceFn(codeOnly, "bodyAABB")) && !/wakeupFade/.test(sliceFn(codeOnly, "slashPose")) && !/wakeupFade/.test(sliceFn(codeOnly, "landHit")));
check("meaty wakeup is not a new combat verb", V.THROW_WAKE_INVULN===80 && V.HITSTUN===350 && V.THROW_WAKE_INVULN<V.HITSTUN && V.HIT_FLASH_MS===120);
check("slash frames still 180/140/280 after meaty wakeup", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after meaty wakeup", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("wakeup invuln still 80 after meaty leftover", V.THROW_WAKE_INVULN===80 && V.THROW_WAKE_INVULN===V.THROW_STARTUP && V.THROW_KD_MS===520);
check("no 6th button after meaty wakeup", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after meaty wakeup", /game.js\?v=350/.test(htmlTxt));

const meatyWakeLand = landCloseThrow();
check("meaty wakeup leftover setup", meatyWakeLand && V.rival.hp===80 && V.rival.thrownT>0, `hit=${meatyWakeLand} hp=${V.rival.hp} th=${V.rival.thrownT}`);
let mwReady=false, mwFade0=-1, mwInv0=-1, mwAabb0=null, mwDest0=null;
for(let i=0;i<80;i++){
  holdWakeSample();
  V.update(STEP);
  if(V.rival.thrownT<=0 && V.rival.throwInvulnT>0 && V.rival.hp>0 && !V.rival.falling){
    mwReady=true;
    mwFade0=V.wakeupFade(V.rival);
    mwInv0=V.rival.throwInvulnT;
    mwAabb0=V.bodyAABB(V.rival);
    mwDest0=V.destRect(V.rival);
    break;
  }
}
check("meaty wakeup leftover getup ready", mwReady && mwFade0>0.5 && mwInv0>0 && V.rival.stunT===0 && !isHurtSheet(V.rival), `ready=${mwReady} fade=${mwFade0} inv=${mwInv0} st=${V.rival.stunT} src=${sheetSrc(V.rival)}`);
V.player.cut = "slash";
V.player.phase = "active";
V.player.phaseT = 0;
V.player.cutHit = false;
V.player.guarding = false;
V.player.stunT = 0;
V.player.falling = false;
V.player.boltPhase = "";
V.player.x = V.rival.x - 160;
V.keepApart();
V.hitstopLeft = 0;
V.keys.clear();
const mwHp0 = V.rival.hp;
V.update(STEP);
check("meaty wakeup leftover fade through stun", mwReady && V.rival.stunT>0 && V.rival.throwInvulnT>0 && V.wakeupFade(V.rival)>0 && V.hurtFlashK(V.rival)>0.5 && V.rival.hp<mwHp0 && !V.rival.falling && !isHurtSheet(V.rival) && V.wakeupFade(V.player)===0, `ready=${mwReady} st=${V.rival.stunT} inv=${V.rival.throwInvulnT} fade=${V.wakeupFade(V.rival)} flash=${V.hurtFlashK(V.rival)} hp=${V.rival.hp} fall=${V.rival.falling} src=${sheetSrc(V.rival)} you=${V.wakeupFade(V.player)}`);
const mwAabb1=V.bodyAABB(V.rival);
const mwDest1=V.destRect(V.rival);
check("meaty wakeup leftover AABB planted", mwReady && mwAabb0 && Math.abs(mwAabb1.y-mwAabb0.y)<0.05 && Math.abs(mwAabb1.h-mwAabb0.h)<0.05 && Math.abs(mwAabb1.w-mwAabb0.w)<0.05, `dy=${(mwAabb1.y-mwAabb0.y).toFixed(2)} dh=${(mwAabb1.h-mwAabb0.h).toFixed(2)} dw=${(mwAabb1.w-mwAabb0.w).toFixed(2)} dx=${(mwAabb1.x-mwAabb0.x).toFixed(2)}`);
check("meaty wakeup leftover destRect planted", mwReady && mwDest1 && Math.abs(mwDest1.dy-mwDest0.dy)<2.2, `ddy=${(mwDest1.dy-mwDest0.dy).toFixed(2)} drot=${(mwDest1.rot-mwDest0.rot).toFixed(3)} fade=${V.wakeupFade(V.rival)}`);
check("meaty wakeup poseBitmap still idle", mwReady && V.rival.stunT>0 && !isHurtSheet(V.rival) && V.rival.thrownT===0, `src=${sheetSrc(V.rival)} st=${V.rival.stunT} th=${V.rival.thrownT} fade=${V.wakeupFade(V.rival)}`);
check("cache v=296 after meaty wakeup live", /game.js\?v=350/.test(htmlTxt));


check("super pip leftover through spend freeze", /function meterFlashK/.test(srcTxt) && /meterFlashK\(f\)/.test(srcTxt) && /leftover brasa pip no longer dies the tick/.test(srcTxt) && /startBolt spends the stock/.test(srcTxt));
check("super pip is draw-only leftover", /Draw-only/.test(srcTxt) && /meterFlashK\(f\)/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && /HUD bar-drain already holds through freeze/.test(srcTxt) && !/meterFlashK/.test(sliceFn(codeOnly, "destRect")) && !/meterFlashK/.test(sliceFn(codeOnly, "bodyAABB")) && !/meterFlashK/.test(sliceFn(codeOnly, "slashPose")) && !/meterFlashK/.test(sliceFn(codeOnly, "landHit")));
check("super pip is not a new combat verb", V.METER_FLASH_MS===220 && V.METER_MAX===100 && V.METER_HIT===20 && V.BOLT_SUPER_STOP===60 && V.METER_FLASH_MS>V.BOLT_SUPER_STOP && V.BOLT_SUPER_DMG===28);
check("slash frames still 180/140/280 after super pip", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after super pip", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after super pip", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after super pip", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after super pip", /game.js\?v=350/.test(htmlTxt));

bootPlay(); wait(1700);
freezeRivalAI(); freezeYouAI();
V.player.stamina = 100;
V.player.stamRegenT = 99999;
V.player.meter = 100;
V.player.meterFlashT = 0;
V.hitstopLeft = 0;
const pipDest0 = V.destRect(V.player);
const pipAabb0 = V.bodyAABB(V.player);
V.boltEdge = true;
V.update(STEP);
check("spent pip leftover arms", V.player.boltPhase==="startup" && V.player.boltSuper===true && V.player.meter===0 && V.player.meterFlashT===V.METER_FLASH_MS && V.meterFlashK(V.player)>0.9 && V.hitstopLeft===V.BOLT_SUPER_STOP, `bph=${V.player.boltPhase} super=${V.player.boltSuper} m=${V.player.meter} ft=${V.player.meterFlashT} k=${V.meterFlashK(V.player)} hs=${V.hitstopLeft}`);
const pipHold = V.player.meterFlashT;
V.update(STEP);
check("spent pip leftover holds freeze", V.player.meter===0 && V.player.meterFlashT===pipHold && V.meterFlashK(V.player)>0.9 && V.hitstopLeft>0 && V.player.boltPhase==="startup", `ft=${V.player.meterFlashT} hold=${pipHold} k=${V.meterFlashK(V.player)} hs=${V.hitstopLeft} bph=${V.player.boltPhase}`);
const pipDestHs = V.destRect(V.player);
const pipAabbHs = V.bodyAABB(V.player);
check("spent pip destRect planted", Math.abs(pipDestHs.dy-pipDest0.dy)<2.2 && Math.abs(pipDestHs.dx-pipDest0.dx)<2.2, `ddy=${(pipDestHs.dy-pipDest0.dy).toFixed(2)} ddx=${(pipDestHs.dx-pipDest0.dx).toFixed(2)}`);
check("spent pip AABB planted", Math.abs(pipAabbHs.y-pipAabb0.y)<0.05 && Math.abs(pipAabbHs.h-pipAabb0.h)<0.05, `dy=${(pipAabbHs.y-pipAabb0.y).toFixed(2)} dh=${(pipAabbHs.h-pipAabb0.h).toFixed(2)}`);
drainHitstop();
check("spent pip leftover after freeze", V.player.meter===0 && V.player.meterFlashT>0 && V.player.meterFlashT<=V.METER_FLASH_MS && V.meterFlashK(V.player)>0 && V.hitstopLeft<=0 && V.player.boltSuper===true, `ft=${V.player.meterFlashT} k=${V.meterFlashK(V.player)} hs=${V.hitstopLeft} super=${V.player.boltSuper}`);
const pipFade0 = V.player.meterFlashT;
for(let i=0;i<8;i++){ freezeRivalAI(); freezeYouAI(); V.update(STEP); }
check("spent pip leftover fades after freeze", V.player.meterFlashT<pipFade0-STEP*4 && V.player.meter===0 && V.meterFlashK(V.player)>0, `ft=${V.player.meterFlashT} t0=${pipFade0} k=${V.meterFlashK(V.player)}`);

bootPlay(); wait(1700);
freezeRivalAI(); freezeYouAI();
V.player.stamina = 100;
V.player.stamRegenT = 99999;
V.player.meter = 0;
V.player.meterFlashT = 0;
V.boltEdge = true;
V.update(STEP);
check("empty K has no leftover pip", V.player.boltPhase==="startup" && V.player.boltSuper===false && V.player.meter===0 && V.player.meterFlashT===0 && V.meterFlashK(V.player)===0 && V.hitstopLeft===0, `bph=${V.player.boltPhase} super=${V.player.boltSuper} ft=${V.player.meterFlashT} k=${V.meterFlashK(V.player)} hs=${V.hitstopLeft}`);

bootPlay(); wait(1700);
freezeRivalAI(); freezeYouAI();
V.rival.stamina = 100;
V.rival.stamRegenT = 99999;
V.rival.meter = 100;
V.rival.superArmed = true;
V.rival.meterFlashT = 0;
V.player.x = V.rival.x - 200;
V.keepApart();
const rPipOk = V.startBolt(V.rival, true);
check("rival spent pip leftover arms", rPipOk && V.rival.boltPhase==="startup" && V.rival.boltSuper===true && V.rival.meter===0 && V.rival.meterFlashT===V.METER_FLASH_MS && V.meterFlashK(V.rival)>0.9 && V.meterFlashK(V.player)===0 && V.hitstopLeft===V.BOLT_SUPER_STOP, `ok=${rPipOk} bph=${V.rival.boltPhase} super=${V.rival.boltSuper} m=${V.rival.meter} ft=${V.rival.meterFlashT} k=${V.meterFlashK(V.rival)} you=${V.meterFlashK(V.player)} hs=${V.hitstopLeft}`);
const rPipHold = V.rival.meterFlashT;
V.update(STEP);
check("rival spent pip leftover holds freeze", V.rival.meter===0 && V.rival.meterFlashT===rPipHold && V.meterFlashK(V.rival)>0.9 && V.hitstopLeft>0, `ft=${V.rival.meterFlashT} hold=${rPipHold} k=${V.meterFlashK(V.rival)} hs=${V.hitstopLeft}`);
check("cache v=296 after super pip live", /game.js\?v=350/.test(htmlTxt));


check("KO rematch leftover pixel screen", /function drawOverPrompt/.test(srcTxt) && /> REVANCHA \/ R/.test(srcTxt) && /KO rematch leftover/.test(srcTxt) && /Dim #a89b88 at 0\.72 used to die against the crumple/.test(srcTxt) && /same pixel menu as JUGAR/.test(srcTxt));
check("KO rematch leftover is draw-only", /Draw-only \(drawOverPrompt\)/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && !/drawOverPrompt/.test(sliceFn(codeOnly, "destRect")) && !/drawOverPrompt/.test(sliceFn(codeOnly, "bodyAABB")) && !/drawOverPrompt/.test(sliceFn(codeOnly, "beginFall")) && !/drawOverPrompt/.test(sliceFn(codeOnly, "slashPose")));
const overPromptFn = sliceFn(codeOnly, "drawOverPrompt");
check("KO rematch leftover title pixel language", /0\.92 \* fade/.test(overPromptFn) && /COL_BRASA/.test(overPromptFn) && /> REVANCHA \/ R/.test(overPromptFn) && !/0\.72 \* fade/.test(overPromptFn) && /OVER_TIPS/.test(overPromptFn) && /0\.62 \* fade/.test(overPromptFn));
check("KO rematch leftover no card", /KO stays/.test(srcTxt) && !/fillRect/.test(overPromptFn));
check("KO rematch leftover is not a new combat verb", V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80 && V.BOLT_SUPER_STOP===60);
check("slash frames still 180/140/280 after KO rematch leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after KO rematch leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after KO rematch leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after KO rematch leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("Space R still rematch after leftover", /if \(c === "KeyR"\) requestRestart = true/.test(srcTxt) && /mode === "over"/.test(srcTxt) && /attackEdge \|\| throwEdge \|\| requestStart/.test(srcTxt));
check("cache v=296 after KO rematch leftover", /game.js\?v=350/.test(htmlTxt));

bootPlay(); wait(1700);
freezeRivalAI(); freezeYouAI();
V.rival.hp = 10;
V.player.x = V.rival.x - 170;
V.keepApart();
V.attackEdge = true;
let rmFall=false, rmOver=false, rmDest0=null, rmAabb0=null;
for(let i=0;i<80;i++){
  freezeRivalAI();
  V.update(STEP);
  if(V.mode==="falling" && V.rival.falling){ rmFall=true; break; }
}
check("KO rematch leftover crumple starts", rmFall && V.rival.hp<=0 && V.mode==="falling", `fall=${rmFall} mode=${V.mode} hp=${V.rival.hp} falling=${V.rival.falling}`);
rmDest0=V.destRect(V.rival);
rmAabb0=V.bodyAABB(V.rival);
for(let i=0;i<80;i++){
  V.update(STEP);
  if(V.mode==="over"){ rmOver=true; break; }
}
check("KO rematch leftover over screen", rmOver && V.mode==="over" && V.rival.hp<=0 && V.rival.falling, `over=${rmOver} mode=${V.mode} hp=${V.rival.hp} falling=${V.rival.falling} t=${V.modeT}`);
const rmDest1=V.destRect(V.rival);
const rmAabb1=V.bodyAABB(V.rival);
check("KO rematch leftover destRect extra rot still 0", rmOver && Math.abs(rmDest1.rot)<0.001, `rot=${rmDest1.rot.toFixed(3)}`);
check("KO rematch leftover AABB planted", rmOver && rmAabb0 && Math.abs(rmAabb1.h-rmAabb0.h)<0.05 && Math.abs(rmAabb1.w-rmAabb0.w)<0.05, `dy=${(rmAabb1.y-rmAabb0.y).toFixed(2)} dh=${(rmAabb1.h-rmAabb0.h).toFixed(2)} dw=${(rmAabb1.w-rmAabb0.w).toFixed(2)}`);
V.attackEdge = true;
V.update(STEP);
check("Space rematch leftover still starts", V.mode==="play" && V.rival.hp===100 && !V.rival.falling, `mode=${V.mode} hp=${V.rival.hp} fall=${V.rival.falling}`);
check("cache v=296 after KO rematch leftover live", /game.js\?v=350/.test(htmlTxt));

check("dart plant puff leftover on raised blade", /function castPlantXY/.test(srcTxt) && /castPlantXY\(f\)/.test(srcTxt) && /Idle\/walk K plant puff used to sit on destRect sheet-edge/.test(srcTxt) && /Dart plant puff leftover/.test(srcTxt) && /windup had no tipX/.test(srcTxt));
check("dart plant puff leftover is draw-only", /Draw-only/.test(srcTxt) && /castPlantXY\(f\)/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && /Dart births from castPlantXY/.test(srcTxt) && !/castPlantXY/.test(sliceFn(codeOnly, "destRect")) && !/castPlantXY/.test(sliceFn(codeOnly, "bodyAABB")) && !/castPlantXY/.test(sliceFn(codeOnly, "slashPose")));
check("throwKnife plant tip not destRect edge", /tipX: 268/.test(srcTxt) && /tipY: 109/.test(srcTxt) && /tipX: 6/.test(srcTxt) && /tipX: 873/.test(srcTxt) && /poseFamily\(f\)\.throwKnife/.test(sliceFn(codeOnly, "castPlantXY")));
check("special-cancel still slash tip fallback", /holdingCutBolt\(f\)/.test(sliceFn(codeOnly, "castPlantXY")) && /fallback/.test(sliceFn(codeOnly, "castPlantXY")));
check("dart plant puff leftover is not a new combat verb", V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80 && V.BOLT_SUPER_STOP===60 && V.BOLT_CAST_FX_MS===220);
check("slash frames still 180/140/280 after dart plant puff leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after dart plant puff leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after dart plant puff leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after dart plant puff leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("dart still births from tip after leftover", /castPlantXY/.test(sliceFn(codeOnly, "spawnBolt")) && /Dart tip plant leftover/.test(srcTxt));
check("cache v=296 after dart plant puff leftover", /game.js\?v=350/.test(htmlTxt));

function sheetEdgeX(f){
  const r = V.destRect(f);
  return f.facing > 0 ? r.dx + r.dw - 8 : r.dx + 8;
}

bootPlay(); wait(1700);
freezeRivalAI(); freezeYouAI();
V.player.stamina = 100;
V.player.stamRegenT = 99999;
V.player.guarding = false;
V.player.boltHoldCut = false;
V.hitstopLeft = 0;
V.rival.x = 1220;
V.boltEdge = true;
V.update(STEP);
const puffYou = V.castPlantXY(V.player);
const edgeYou = sheetEdgeX(V.player);
const aabbYou0 = V.bodyAABB(V.player);
const destYou0 = V.destRect(V.player);
check("idle K puff leftover on throw_knife tip", V.player.boltPhase==="startup" && V.brasaFxKind==="cast" && Math.abs(V.brasaX-puffYou.x)<4 && Math.abs(V.brasaY-puffYou.y)<4 && Math.abs(edgeYou-puffYou.x)>80, `bph=${V.player.boltPhase} kind=${V.brasaFxKind} x=${V.brasaX.toFixed(1)} plant=${puffYou.x.toFixed(1)} edge=${edgeYou.toFixed(1)}`);
check("idle K puff leftover not destRect sheet-edge", Math.abs(V.brasaX-edgeYou)>80 && V.player.boltHoldCut===false && /throw_knife/.test((V.poseBitmap(V.player)&&V.poseBitmap(V.player)._src)||""), `x=${V.brasaX.toFixed(1)} edge=${edgeYou.toFixed(1)} hold=${V.player.boltHoldCut} src=${(V.poseBitmap(V.player)&&V.poseBitmap(V.player)._src)||""}`);
const puffHoldT = V.brasaFxT;
V.hitstopLeft = 140;
V.update(STEP);
check("idle K puff leftover holds freeze", V.brasaFxKind==="cast" && Math.abs(V.brasaFxT-puffHoldT)<0.01 && Math.abs(V.brasaX-V.castPlantXY(V.player).x)<4, `t=${V.brasaFxT} hold=${puffHoldT} x=${V.brasaX.toFixed(1)}`);
V.hitstopLeft = 0;
V.player.x += 40;
V.update(STEP);
check("idle K puff leftover rides destRect plant", Math.abs(V.brasaX-V.castPlantXY(V.player).x)<4, `x=${V.brasaX.toFixed(1)} plant=${V.castPlantXY(V.player).x.toFixed(1)}`);
check("idle K puff leftover AABB planted", Math.abs(V.bodyAABB(V.player).y-aabbYou0.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-aabbYou0.h)<0.05, `dy=${(V.bodyAABB(V.player).y-aabbYou0.y).toFixed(2)} dh=${(V.bodyAABB(V.player).h-aabbYou0.h).toFixed(2)}`);
check("idle K puff leftover destRect planted", Math.abs(V.destRect(V.player).dy-destYou0.dy)<2.2, `ddy=${(V.destRect(V.player).dy-destYou0.dy).toFixed(2)} ddx=${(V.destRect(V.player).dx-destYou0.dx).toFixed(2)}`);

let birthSaw=false, birthPuff=0, birthEdge=0, birthBlade=0, birthFade=0, birthSrc="";
for(let i=0;i<20;i++){
  freezeRivalAI();
  V.player.guarding = false;
  V.player.stunT = 0;
  V.player.falling = false;
  V.player.boltHoldCut = false;
  V.hitstopLeft = 0;
  V.rival.x = 1220;
  V.update(STEP);
  if(V.player.boltPhase==="recovery"){
    birthSaw=true;
    birthPuff=V.brasaX;
    birthEdge=sheetEdgeX(V.player);
    birthBlade=V.bladeTipX(V.player);
    birthFade=V.boltPlantFade(V.player);
    birthSrc=(V.poseBitmap(V.player)&&V.poseBitmap(V.player)._src)||"";
    break;
  }
}
check("idle K puff leftover through dart birth", birthSaw && V.brasaFxKind==="cast" && V.brasaFxT>0 && birthFade>0.8 && Math.abs(birthPuff-V.castPlantXY(V.player).x)<4 && Math.abs(birthPuff-birthEdge)>80 && Math.abs(birthBlade-birthPuff)<4 && !/windup/.test(birthSrc), `saw=${birthSaw} kind=${V.brasaFxKind} t=${V.brasaFxT} fade=${birthFade} puff=${birthPuff.toFixed(1)} plant=${V.castPlantXY(V.player).x.toFixed(1)} edge=${birthEdge.toFixed(1)} blade=${birthBlade.toFixed(1)} src=${birthSrc}`);

bootPlay(); wait(1700);
freezeRivalAI(); freezeYouAI();
V.rival.stamina = 100;
V.rival.stamRegenT = 99999;
V.rival.boltCd = 0;
V.rival.boltArmed = true;
V.rival.guarding = false;
V.rival.wantBlock = false;
V.rival.guardChoice = -1;
V.rival.closing = false;
V.rival.standWait = 0;
V.rival.standGoal = 99999;
V.player.x = 180;
V.rival.x = 1100;
V.keepApart();
V.update(STEP);
const puffRiv = V.castPlantXY(V.rival);
const edgeRiv = sheetEdgeX(V.rival);
// Tip under telegraphFade: bladeTip eases idle→knife under tele; puff still seats on castPlantXY throwKnife tip.
check("rival puff leftover on throw_knife tip", V.rival.boltPhase==="startup" && V.brasaFxKind==="cast" && Math.abs(V.brasaX-puffRiv.x)<4 && /throw_knife/.test(sheetSrc(V.rival)), `bph=${V.rival.boltPhase} kind=${V.brasaFxKind} x=${V.brasaX.toFixed(1)} plant=${puffRiv.x.toFixed(1)} tip=${V.bladeTipX(V.rival).toFixed(1)} edge=${edgeRiv.toFixed(1)} src=${sheetSrc(V.rival)}`);

function landCloseSlashPuff(){
  bootPlay(); wait(1700);
  freezeRivalAI(); freezeYouAI();
  V.player.guarding = false;
  V.player.stunT = 0;
  V.player.falling = false;
  V.player.hp = 100;
  V.rival.hp = 100;
  V.rival.guarding = false;
  V.rival.wantBlock = false;
  V.rival.guardChoice = -1;
  V.player.x = V.rival.x - 160;
  V.keepApart();
  V.attackEdge = true;
  let hit=false;
  for(let i=0;i<80;i++){
    freezeRivalAI();
    V.update(STEP);
    if(V.rival.hp<100 || V.hitstopLeft>0){ hit=true; break; }
  }
  for(let i=0;i<40;i++){ if(V.hitstopLeft<=0) break; V.update(STEP); }
  return hit && V.player.phase==="recovery" && V.player.cutHit;
}
const scHit = landCloseSlashPuff();
let scWin=false;
for(let i=0;i<40;i++){
  freezeRivalAI();
  V.player.stamRegenT = 99999;
  V.update(STEP);
  const left = (V.player.phase==="recovery" && V.player.cut==="slash") ? (V.RECOVERY - V.player.phaseT) : 999;
  if(left<=V.BOLT_CANCEL_MS){ scWin=true; break; }
  if(V.player.phase==="idle") break;
}
V.player.stamRegenT = 99999;
V.boltEdge = true;
V.update(STEP);
const scPuff = V.castPlantXY(V.player);
const scEdge = V.bladeTipX(V.player);
check("special-cancel puff leftover still slash tip", scHit && scWin && V.player.boltPhase==="startup" && V.player.boltHoldCut===true && V.brasaFxKind==="cast" && Math.abs(V.brasaX-scEdge)<8 && Math.abs(scPuff.x-scEdge)<8, `hit=${scHit} win=${scWin} bph=${V.player.boltPhase} hold=${V.player.boltHoldCut} x=${V.brasaX.toFixed(1)} edge=${scEdge.toFixed(1)} plant=${scPuff.x.toFixed(1)}`);
check("cache v=296 after dart plant puff leftover live", /game.js\?v=350/.test(htmlTxt));

check("camera punch leftover holds PUNCH_PX cover", /function punchCover/.test(srcTxt) && /punchCover\(\)/.test(srcTxt) && /Camera punch leftover/.test(srcTxt) && /overscan used to track \|ox\|/.test(srcTxt) && /yard zoomed as the slam squared out/.test(srcTxt) && /Hold PUNCH_PX cover/.test(srcTxt));
check("camera punch leftover is draw-only", /Draw-only/.test(srcTxt) && /punchCover\(\)/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && /Punch is a slide, not a zoom/.test(srcTxt) && !/punchCover/.test(sliceFn(codeOnly, "destRect")) && !/punchCover/.test(sliceFn(codeOnly, "bodyAABB")) && !/punchCover/.test(sliceFn(codeOnly, "slashPose")) && !/punchCover/.test(sliceFn(codeOnly, "landHit")) && !/punchCover/.test(sliceFn(codeOnly, "bumpShake")));
const yardPunchFn = sliceFn(codeOnly, "drawCourtyard");
const coverFn = sliceFn(codeOnly, "punchCover");
check("camera punch leftover cover is PUNCH_PX not |ox|",
  /function punchCover/.test(srcTxt) &&
  /return PUNCH_PX/.test(coverFn) &&
  /padX = punchCover\(\)/.test(yardPunchFn) &&
  !/Math\.abs\(ox\)/.test(yardPunchFn) &&
  !/ceil\(Math\.abs\(ox\)/.test(yardPunchFn) &&
  !/ox \? Math\.min\(PUNCH_PX/.test(yardPunchFn)
);
check("camera punch leftover rest still flush", /Rest ox=0 still flush/.test(srcTxt) && /shake <= 0 \|\| shakeDur <= 0/.test(coverFn) && /mode === "title"/.test(coverFn));
check("camera punch leftover still squares", /env = k \* k/.test(srcTxt) && /shakeDir \* shakeMag \* env/.test(srcTxt) && V.PUNCH_PX===12);
check("camera punch leftover is not a new combat verb", V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80 && V.BOLT_SUPER_STOP===60 && V.PUNCH_PX===12);
check("slash frames still 180/140/280 after camera punch leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after camera punch leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after camera punch leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after camera punch leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after camera punch leftover", /game.js\?v=350/.test(htmlTxt));

function landCloseSlashPunch(){
  bootPlay(); wait(1700);
  freezeRivalAI(); freezeYouAI();
  V.player.guarding = false;
  V.player.stunT = 0;
  V.player.falling = false;
  V.player.hp = 100;
  V.rival.hp = 100;
  V.rival.guarding = false;
  V.rival.wantBlock = false;
  V.rival.guardChoice = -1;
  V.player.x = V.rival.x - 160;
  V.keepApart();
  V.attackEdge = true;
  let hit=false;
  for(let i=0;i<80;i++){
    freezeRivalAI();
    V.update(STEP);
    if(V.rival.hp<100 || V.hitstopLeft>0){ hit=true; break; }
  }
  return hit;
}
const punchHit = landCloseSlashPunch();
const punchAabb0 = V.bodyAABB(V.rival);
const punchDest0 = V.destRect(V.rival);
check("camera punch leftover arms on hit", punchHit && V.hitstopLeft===V.HITSTOP_HIT && V.shake===V.HITSTOP_HIT && V.shakeDur===V.HITSTOP_HIT && V.shakeMag===10 && V.punchCover()===V.PUNCH_PX && V.PUNCH_PX===12, `hit=${punchHit} hs=${V.hitstopLeft} shake=${V.shake} dur=${V.shakeDur} mag=${V.shakeMag} cover=${V.punchCover()}`);
const punchHold = V.shake;
V.update(STEP);
check("camera punch leftover holds freeze", V.hitstopLeft>0 && V.shake===punchHold && V.punchCover()===V.PUNCH_PX && V.shakeMag===10, `hs=${V.hitstopLeft} shake=${V.shake} hold=${punchHold} cover=${V.punchCover()}`);
check("camera punch leftover AABB planted", Math.abs(V.bodyAABB(V.rival).y-punchAabb0.y)<0.05 && Math.abs(V.bodyAABB(V.rival).h-punchAabb0.h)<0.05, `dy=${(V.bodyAABB(V.rival).y-punchAabb0.y).toFixed(2)} dh=${(V.bodyAABB(V.rival).h-punchAabb0.h).toFixed(2)}`);
check("camera punch leftover destRect planted", Math.abs(V.destRect(V.rival).dy-punchDest0.dy)<2.2, `ddy=${(V.destRect(V.rival).dy-punchDest0.dy).toFixed(2)}`);
drainHitstop();
V.update(STEP);
check("camera punch leftover after freeze still covers", V.hitstopLeft<=0 && V.shake>0 && V.shake<V.HITSTOP_HIT && V.punchCover()===V.PUNCH_PX && V.shakeMag===10, `hs=${V.hitstopLeft} shake=${V.shake} cover=${V.punchCover()}`);
const punchMid = V.shake;
for(let i=0;i<5;i++) V.update(STEP);
check("camera punch leftover cover stays while slam decays", V.shake<punchMid && V.shake>0 && V.punchCover()===V.PUNCH_PX, `shake=${V.shake} mid=${punchMid} cover=${V.punchCover()}`);
for(let i=0;i<20;i++) V.update(STEP);
check("camera punch leftover rest flush", V.shake===0 && V.punchCover()===0, `shake=${V.shake} cover=${V.punchCover()}`);

bootPlay(); wait(1700);
freezeRivalAI(); freezeYouAI();
V.rival.guarding = true;
V.rival.wantBlock = true;
V.rival.guardChoice = 1;
V.rival.stamina = 100;
V.player.x = V.rival.x - 160;
V.keepApart();
V.attackEdge = true;
let blk=false;
for(let i=0;i<80;i++){
  freezeRivalAI();
  V.rival.guarding = true;
  V.rival.wantBlock = true;
  V.rival.guardChoice = 1;
  V.update(STEP);
  if(V.hitstopLeft>0){ blk=true; break; }
}
check("camera punch leftover block still covers", blk && V.hitstopLeft===V.HITSTOP_BLOCK && V.shake===V.HITSTOP_BLOCK && V.shakeMag===4 && V.punchCover()===V.PUNCH_PX && V.rival.hp===100, `blk=${blk} hs=${V.hitstopLeft} shake=${V.shake} mag=${V.shakeMag} cover=${V.punchCover()} hp=${V.rival.hp}`);

bootPlay(); wait(1700);
freezeRivalAI(); freezeYouAI();
V.player.meter = 100;
V.player.stamina = 100;
V.player.guarding = false;
V.boltEdge = true;
V.update(STEP);
check("camera punch leftover super spend still covers", V.player.boltPhase==="startup" && V.player.boltSuper===true && V.hitstopLeft===V.BOLT_SUPER_STOP && V.shake===V.BOLT_SUPER_STOP && V.shakeMag===10 && V.punchCover()===V.PUNCH_PX, `bph=${V.player.boltPhase} super=${V.player.boltSuper} hs=${V.hitstopLeft} shake=${V.shake} cover=${V.punchCover()}`);
check("cache v=296 after camera punch leftover live", /game.js\?v=350/.test(htmlTxt));



check("portrait stick leftover follows off-pad", /Portrait stick leftover/.test(srcTxt) && /window\.addEventListener\("pointermove"/.test(srcTxt) && /stickPtr == null/.test(srcTxt) && /stickPointerMove\(e\)/.test(srcTxt) && /Follow on window while stickPtr is live/.test(srcTxt));
check("portrait stick leftover capture loss does not dump A/D", /lostpointercapture/.test(srcTxt) && /stickPtr != null && e\.pointerId === stickPtr/.test(srcTxt) && /Window pointerup still/.test(srcTxt) && /Capture loss does not dump/.test(srcTxt));
check("portrait stick leftover is not a new combat verb", V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80 && V.GUARD_COMMIT_MS===140 && /STICK_DEAD = 0\.28/.test(srcTxt));
check("slash frames still 180/140/280 after portrait stick leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after portrait stick leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after portrait stick leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after portrait stick leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("portrait pad still letterbox no canvas resize", /@media \(orientation: portrait\)/.test(cssTxt) && /body\.touch-on #pad/.test(cssTxt) && /bottom:\s*0/.test(cssTxt) && /position:\s*absolute/.test(cssTxt) && !/@media \(orientation: portrait\)[\s\S]*body\.touch-on #game/.test(cssTxt));
check("giro hidden in portrait after stick leftover", /body\.touch-on #giro\s*\{[^}]*display:\s*none/.test(cssTxt) && !/body\.touch-on #giro\s*\{[^}]*display:\s*flex/.test(cssTxt));
check("portrait stick leftover bigger thumb", /Portrait stick leftover/.test(cssTxt) && /body\.touch-on \.stick/.test(cssTxt) && /--stick-size:\s*min\(120px/.test(cssTxt) && /38vw/.test(cssTxt) && /20dvh/.test(cssTxt) && /min\(84px, calc\(var\(--pad-h\) - 14px\), 26vw\)/.test(cssTxt));
check("stick still horizontal A/D after leftover", /Vertical stick does nothing/.test(srcTxt) && /stickApply\(maxR > 0 \? dx \/ maxR : 0\)/.test(srcTxt) && /feedKey\("KeyA"/.test(srcTxt) && /feedKey\("KeyD"/.test(srcTxt));
check("keyboard path untouched after portrait stick leftover", /KeyA/.test(srcTxt) && /KeyD/.test(srcTxt) && /Space/.test(srcTxt) && /KeyJ/.test(srcTxt) && /KeyS/.test(srcTxt) && /KeyK/.test(srcTxt) && /KeyL/.test(srcTxt) && /KeyR/.test(srcTxt) && /act === "dart"/.test(srcTxt) && /boltEdge = true/.test(srcTxt));
check("cache v=296 after portrait stick leftover", /game.js\?v=350/.test(htmlTxt));

bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.stickApply(-0.9);
check("portrait stick leftover left still KeyA", V.keys.has("KeyA") && !V.keys.has("KeyD"), `a=${V.keys.has("KeyA")} d=${V.keys.has("KeyD")}`);
V.stickApply(0.9);
check("portrait stick leftover right still KeyD", V.keys.has("KeyD") && !V.keys.has("KeyA"), `a=${V.keys.has("KeyA")} d=${V.keys.has("KeyD")}`);
V.stickApply(0);
check("portrait stick leftover deadzone still releases", !V.keys.has("KeyA") && !V.keys.has("KeyD"), `a=${V.keys.has("KeyA")} d=${V.keys.has("KeyD")}`);
V.stickApply(0.1);
check("portrait stick leftover inside deadzone idle", !V.keys.has("KeyA") && !V.keys.has("KeyD"));
V.stickRelease();
const aabbStick0 = V.bodyAABB(V.player);
const destStick0 = V.destRect(V.player);
V.stickApply(0.9);
V.update(STEP);
check("portrait stick leftover AABB planted", Math.abs(V.bodyAABB(V.player).y-aabbStick0.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-aabbStick0.h)<0.05, `dy=${(V.bodyAABB(V.player).y-aabbStick0.y).toFixed(2)}`);
check("portrait stick leftover destRect planted", Math.abs(V.destRect(V.player).dy-destStick0.dy)<2.2, `ddy=${(V.destRect(V.player).dy-destStick0.dy).toFixed(2)}`);
V.stickRelease();
check("cache v=296 after portrait stick leftover live", /game.js\?v=350/.test(htmlTxt));

check("rival telegraph leftover fades idle to windup", /function telegraphFade/.test(srcTxt) && /function telegraphing/.test(srcTxt) && /telegraphFade\(f\)/.test(srcTxt) && /Rival telegraph leftover/.test(srcTxt) && /idle→windup used to pop/.test(srcTxt) && /180ms read was a sheet cut/.test(srcTxt));
check("rival telegraph leftover is draw-only sheet", /Visual only \(drawKnight\)/.test(srcTxt) && /telegraphFade\(f\)/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && /poseBitmap still windup/.test(srcTxt) && !/telegraphFade/.test(sliceFn(codeOnly, "destRect")) && !/telegraphFade/.test(sliceFn(codeOnly, "bodyAABB")) && !/telegraphFade/.test(sliceFn(codeOnly, "slashPose")) && !/telegraphFade/.test(sliceFn(codeOnly, "landHit")) && !/telegraphFade/.test(sliceFn(codeOnly, "startAttack")));
check("rival telegraph leftover destRect breath ease", /telegraphing\(f\)/.test(sliceFn(codeOnly, "idleBreath")) && /telegraphFade\(f\)/.test(sliceFn(codeOnly, "idleBreath")) && /startup used to dump leftover breath/.test(srcTxt));
check("rival telegraph leftover uses GUARD_RAISE_MS", /GUARD_RAISE_MS/.test(sliceFn(codeOnly, "telegraphFade")) && V.GUARD_RAISE_MS===80 && /restTele/.test(sliceFn(codeOnly, "drawKnight")) && /telegraphing\(f\)/.test(sliceFn(codeOnly, "drawKnight")));
check("rival telegraph leftover poseBitmap still throw_knife", /f\.phase === "startup" && ready\(wind\)/.test(sliceFn(codeOnly, "poseBitmap")) && /f\.boltPhase === "startup" && ready\(throwK\)/.test(sliceFn(codeOnly, "poseBitmap")));
check("idle slash throw K still arm telegraph", /f\.telegraph = true/.test(sliceFn(codeOnly, "startAttack")) && /f\.telegraph = fromIdle/.test(sliceFn(codeOnly, "startThrow")) && /u\.telegraph = !holdingCutBolt\(u\)/.test(sliceFn(codeOnly, "startBolt")));
check("cancel reversal special-cancel still snap telegraph", /f\.telegraph = false/.test(sliceFn(codeOnly, "cancelIntoGolpe")) && /f\.telegraph = false/.test(sliceFn(codeOnly, "cancelIntoSlash")) && /f\.telegraph = false/.test(sliceFn(codeOnly, "startReversal")) && /holdingCutBolt\(f\)/.test(sliceFn(codeOnly, "telegraphing")));
check("rival telegraph leftover is not a new combat verb", V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80 && V.GUARD_COMMIT_MS===140 && V.GUARD_RAISE_MS===80);
check("slash frames still 180/140/280 after rival telegraph leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after rival telegraph leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after rival telegraph leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after rival telegraph leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after rival telegraph leftover", /game.js\?v=350/.test(htmlTxt));

bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.rival.gait = 0;
V.rival.walkT = 0;
V.rival.walkRiseT = 0;
V.rival.walkSettleT = 0;
V.rival.walkSettleOy = 0;
V.rival.walkSettleRot = 0;
V.rival.walkPlanted = false;
V.rival.walkFadeHold = 0;
V.rival.closing = false;
V.rival.sheatheT = 0;
V.rival.guardPoseK = 0;
V.rival.guarding = false;
V.rival.telegraph = false;
const teleAabb0 = V.bodyAABB(V.rival);
const teleDest0 = V.destRect(V.rival);
V.startAttack(V.rival, "slash");
check("rival telegraph leftover arms on slash", V.rival.phase==="startup" && V.rival.cut==="slash" && V.rival.telegraph===true && V.telegraphing(V.rival) && V.telegraphFade(V.rival)===0, `ph=${V.rival.phase} cut=${V.rival.cut} tel=${V.rival.telegraph} ing=${V.telegraphing(V.rival)} k=${V.telegraphFade(V.rival)}`);
check("rival telegraph leftover poseBitmap windup immediately", /windup/.test(sheetSrc(V.rival)), `src=${sheetSrc(V.rival)}`);
check("rival telegraph leftover no destRect hop", Math.abs(V.destRect(V.rival).dy-teleDest0.dy)<0.35, `ddy=${(V.destRect(V.rival).dy-teleDest0.dy).toFixed(2)} k=${V.telegraphFade(V.rival)}`);
check("rival telegraph leftover AABB planted at arm", Math.abs(V.bodyAABB(V.rival).y-teleAabb0.y)<0.05 && Math.abs(V.bodyAABB(V.rival).h-teleAabb0.h)<0.05, `dy=${(V.bodyAABB(V.rival).y-teleAabb0.y).toFixed(2)}`);
V.update(STEP);
check("rival telegraph leftover fade starts", V.rival.phase==="startup" && V.telegraphFade(V.rival)>0 && V.telegraphFade(V.rival)<0.5 && V.telegraphing(V.rival), `ph=${V.rival.phase} k=${V.telegraphFade(V.rival).toFixed(3)} t=${V.rival.phaseT}`);
const teleMidAabb = V.bodyAABB(V.rival);
for(let i=0;i<8;i++) V.update(STEP);
check("rival telegraph leftover fade completes", V.rival.phase==="startup" && V.rival.phaseT>=V.GUARD_RAISE_MS && V.telegraphFade(V.rival)===1 && V.telegraphing(V.rival), `ph=${V.rival.phase} t=${V.rival.phaseT} k=${V.telegraphFade(V.rival)}`);
check("rival telegraph leftover AABB planted through raise", Math.abs(V.bodyAABB(V.rival).y-teleMidAabb.y)<0.05 && Math.abs(V.bodyAABB(V.rival).h-teleMidAabb.h)<0.05, `dy=${(V.bodyAABB(V.rival).y-teleMidAabb.y).toFixed(2)}`);
check("rival telegraph leftover destRect eases breath", Math.abs(V.destRect(V.rival).dy-teleDest0.dy)<2.2, `ddy=${(V.destRect(V.rival).dy-teleDest0.dy).toFixed(2)}`);

bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.player.sheatheT = 0;
V.player.walkSettleT = 0;
V.startAttack(V.player, "slash");
check("you telegraph leftover arms on slash", V.player.phase==="startup" && V.player.telegraph===true && V.telegraphFade(V.player)===0 && /windup/.test(sheetSrc(V.player)), `ph=${V.player.phase} tel=${V.player.telegraph} src=${sheetSrc(V.player)}`);
V.update(STEP);
check("you telegraph leftover fade starts", V.player.phase==="startup" && V.telegraphFade(V.player)>0 && V.telegraphFade(V.player)<0.5, `k=${V.telegraphFade(V.player).toFixed(3)}`);

bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.rival.sheatheT = 0;
V.startAttack(V.rival, "golpe");
V.update(STEP);
check("rival golpe telegraph leftover fades", V.rival.cut==="golpe" && V.rival.phase==="startup" && V.rival.telegraph===true && V.telegraphFade(V.rival)>0 && V.telegraphFade(V.rival)<1, `cut=${V.rival.cut} k=${V.telegraphFade(V.rival).toFixed(3)}`);

bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.rival.stamina = 100;
V.rival.sheatheT = 0;
const rBoltOk = V.startBolt(V.rival, false);
check("rival K telegraph leftover arms", rBoltOk && V.rival.boltPhase==="startup" && V.rival.telegraph===true && V.telegraphing(V.rival) && V.telegraphFade(V.rival)===0 && /throw_knife/.test(sheetSrc(V.rival)), `ok=${rBoltOk} bph=${V.rival.boltPhase} tel=${V.rival.telegraph} k=${V.telegraphFade(V.rival)} src=${sheetSrc(V.rival)}`);
V.update(STEP);
check("rival K telegraph leftover fade starts", V.rival.boltPhase==="startup" && V.telegraphFade(V.rival)>0 && V.telegraphFade(V.rival)<0.5, `k=${V.telegraphFade(V.rival).toFixed(3)} t=${V.rival.boltT}`);

bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
toClose();
raiseGuardInRoom();
const revTelSt0 = V.player.stamina;
fireReversalChord();
V.update(STEP);
check("reversal still snaps leftover telegraph", V.player.cut==="golpe" && V.player.phase==="startup" && V.player.reversal && V.player.telegraph===false && V.telegraphFade(V.player)===0, `cut=${V.player.cut} rev=${V.player.reversal} tel=${V.player.telegraph} k=${V.telegraphFade(V.player)} spent=${(revTelSt0-V.player.stamina).toFixed(2)}`);
V.keys.clear();

bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.player.sheatheT = 0;
V.startAttack(V.player, "slash");
let teleActive=false;
for(let i=0;i<30;i++){
  V.update(STEP);
  if(V.player.phase==="active"){ teleActive=true; break; }
}
check("active leftover telegraph gone", teleActive && V.player.phase==="active" && V.telegraphFade(V.player)===0 && !V.telegraphing(V.player), `ph=${V.player.phase} k=${V.telegraphFade(V.player)} ing=${V.telegraphing(V.player)} saw=${teleActive}`);
check("cache v=296 after rival telegraph leftover live", /game.js\?v=350/.test(htmlTxt));

check("plant dust leftover rides destRect plant", /function syncPlantDust/.test(srcTxt) && /syncPlantDust\(\)/.test(srcTxt) && /Plant dust leftover/.test(srcTxt) && /stamp used to sit at spawn world xy/.test(srcTxt) && /boot shoved 240/.test(srcTxt) && /walked off/.test(srcTxt));
check("plant dust leftover is draw-only", /Draw-only/.test(srcTxt) && /syncPlantDust\(\)/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && /Stamps ride destRect\.pivX/.test(srcTxt) && !/syncPlantDust/.test(sliceFn(codeOnly, "destRect")) && !/syncPlantDust/.test(sliceFn(codeOnly, "bodyAABB")) && !/syncPlantDust/.test(sliceFn(codeOnly, "slashPose")) && !/syncPlantDust/.test(sliceFn(codeOnly, "landHit")) && !/syncPlantDust/.test(sliceFn(codeOnly, "tryPushblock")) && !/syncPlantDust/.test(sliceFn(codeOnly, "tickGait")));
check("plant dust leftover stamps ride not specks", /if \(p\.speck\) continue/.test(sliceFn(codeOnly, "syncPlantDust")) && /p\.x = r\.pivX \+ \(p\.plantDX \|\| 0\)/.test(sliceFn(codeOnly, "syncPlantDust")) && /homeYou/.test(sliceFn(codeOnly, "spawnPlantDust")) && /plantDX/.test(sliceFn(codeOnly, "spawnPlantDust")));
check("plant dust leftover specks still fly", /if \(p\.speck\)/.test(srcTxt) && /p\.vy \+= 520/.test(srcTxt) && !/else if \(p\.vx\)/.test(sliceFn(codeOnly, "update")));
check("plant dust leftover shove still PUSHBLOCK_FX", /spawnPlantDust\(f,\s*PUSHBLOCK_FX,\s*away\)/.test(sliceFn(codeOnly, "tryPushblock")) && V.PUSHBLOCK_FX===1.85 && V.PUSHBLOCK_PX===240);
check("plant dust leftover is not a new combat verb", V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80 && V.GUARD_COMMIT_MS===140 && V.PUSHBLOCK_STAM===25 && V.PUSHBLOCK_PX===240 && V.KNOCK_PX===80 && V.KNOCK_MS===180);
check("slash frames still 180/140/280 after plant dust leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after plant dust leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after plant dust leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after plant dust leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after plant dust leftover", /game.js\?v=350/.test(htmlTxt));

function dustStamps(homeYou, shove){
  return (V.plantDust||[]).filter(p=>!p.speck && (homeYou==null || p.homeYou===homeYou) && (shove==null || !!p.shove===!!shove));
}

toClose();
raiseGuardInRoom();
for(let i=0;i<8;i++){ holdRivalStill(); V.update(STEP); }
const dustRideAabb0 = V.bodyAABB(V.player);
const dustRideDest0 = V.destRect(V.player);
V.keys.add("KeyA");
V.update(STEP);
const pbRideStamp0 = dustStamps(true, true)[0];
const pbRideSpawnX = pbRideStamp0 && pbRideStamp0.x;
const pbRideSpawnPiv = V.destRect(V.player).pivX;
check("plant dust leftover pushblock arms", pbRideStamp0 && V.player.guarding && V.player.pushT>0 && V.lastPushblockSfx==="pushblock" && Math.abs(pbRideSpawnX - (pbRideSpawnPiv + (pbRideStamp0.plantDX||0)))<4, `stamp=${pbRideStamp0&&pbRideStamp0.x} piv=${pbRideSpawnPiv} dx=${pbRideStamp0&&pbRideStamp0.plantDX}`);
waitPush(12);
const pbRideStamp1 = dustStamps(true, true)[0];
const pbRidePiv1 = V.destRect(V.player).pivX;
check("plant dust leftover pushblock rides destRect", pbRideStamp1 && Math.abs(pbRideStamp1.x - (pbRidePiv1 + (pbRideStamp1.plantDX||0)))<4 && Math.abs(pbRideStamp1.x - pbRideSpawnX)>150, `x=${pbRideStamp1&&pbRideStamp1.x.toFixed(1)} spawn=${(pbRideSpawnX||0).toFixed(1)} piv=${pbRidePiv1.toFixed(1)} dx=${pbRideStamp1&&pbRideStamp1.plantDX}`);
check("plant dust leftover pushblock AABB planted", Math.abs(V.bodyAABB(V.player).y-dustRideAabb0.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-dustRideAabb0.h)<0.05, `dy=${(V.bodyAABB(V.player).y-dustRideAabb0.y).toFixed(2)}`);
check("plant dust leftover pushblock destRect planted", Math.abs(V.destRect(V.player).dy-dustRideDest0.dy)<2.2, `ddy=${(V.destRect(V.player).dy-dustRideDest0.dy).toFixed(2)}`);
V.keys.clear();

bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.player.x = 380;
V.rival.x = 1080;
V.keepApart();
for(let i=0;i<24;i++){ freezeRivalAI(); V.update(STEP); }
V.keys.add("KeyD");
let walkDustOk=false, walkSpawnX=0, walkSpawnPiv=0;
for(let i=0;i<90;i++){
  freezeRivalAI();
  V.update(STEP);
  const st = dustStamps(true, false).find(p=>Math.abs((p.power||0)-1)<0.01);
  if(st){ walkDustOk=true; walkSpawnX=st.x; walkSpawnPiv=V.destRect(V.player).pivX; break; }
}
check("plant dust leftover walk grit plants", walkDustOk && V.player.gait!==0, `ok=${walkDustOk} gait=${V.player.gait} n=${dustStamps(true,false).length}`);
const walkAabb0 = V.bodyAABB(V.player);
const walkDest0 = V.destRect(V.player);
for(let i=0;i<12;i++){ freezeRivalAI(); V.update(STEP); }
const walkStamp = dustStamps(true, false).find(p=>Math.abs((p.power||0)-1)<0.01);
const walkPiv1 = V.destRect(V.player).pivX;
check("plant dust leftover walk rides destRect", walkStamp && Math.abs(walkStamp.x - walkPiv1)<4 && Math.abs(walkPiv1 - walkSpawnPiv)>8, `x=${walkStamp&&walkStamp.x.toFixed(1)} spawn=${walkSpawnX.toFixed(1)} piv=${walkPiv1.toFixed(1)} piv0=${walkSpawnPiv.toFixed(1)}`);
check("plant dust leftover walk AABB planted", Math.abs(V.bodyAABB(V.player).y-walkAabb0.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-walkAabb0.h)<0.05, `dy=${(V.bodyAABB(V.player).y-walkAabb0.y).toFixed(2)}`);
check("plant dust leftover walk destRect planted", Math.abs(V.destRect(V.player).dy-walkDest0.dy)<2.2, `ddy=${(V.destRect(V.player).dy-walkDest0.dy).toFixed(2)}`);
V.keys.clear();

function dustFresh(homeYou, shove, power){
  return dustStamps(homeYou, shove).filter(p=>power==null || Math.abs((p.power||0)-power)<0.01).sort((a,b)=>a.t-b.t)[0];
}
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.player.guarding = false;
V.player.stunT = 0;
V.player.falling = false;
V.player.hp = 100;
V.rival.hp = 100;
V.rival.guarding = false;
V.rival.wantBlock = false;
V.rival.guardChoice = -1;
V.rival.x = 700;
V.player.x = 540;
V.keepApart();
V.attackEdge = true;
let landDustHit=false;
for(let i=0;i<80;i++){
  freezeRivalAI();
  V.update(STEP);
  if(V.rival.hp<100 || V.hitstopLeft>0){ landDustHit=true; break; }
}
const landStamp0 = dustFresh(false, false, 1.15);
const landSpawnX = landStamp0 && landStamp0.x;
const landSpawnPiv = V.destRect(V.rival).pivX;
const landAabb0 = V.bodyAABB(V.rival);
const landDest0 = V.destRect(V.rival);
check("plant dust leftover land arms on hit", landDustHit && landStamp0 && V.hitstopLeft===V.HITSTOP_HIT && V.rival.hp===90 && Math.abs(landSpawnX - (landSpawnPiv + (landStamp0.plantDX||0)))<4, `hit=${landDustHit} hs=${V.hitstopLeft} stamp=${landStamp0&&landStamp0.x} piv=${landSpawnPiv} t=${landStamp0&&landStamp0.t}`);
V.update(STEP);
const landHold = dustFresh(false, false, 1.15);
check("plant dust leftover land holds freeze", V.hitstopLeft>0 && landHold && Math.abs(landHold.x - (V.destRect(V.rival).pivX + (landHold.plantDX||0)))<4 && V.rival.hp===90, `hs=${V.hitstopLeft} x=${landHold&&landHold.x} piv=${V.destRect(V.rival).pivX}`);
drainHitstop();
for(let i=0;i<8;i++) V.update(STEP);
const landStamp1 = dustFresh(false, false, 1.15);
const landPiv1 = V.destRect(V.rival).pivX;
check("plant dust leftover land rides destRect", landStamp1 && Math.abs(landStamp1.x - (landPiv1 + (landStamp1.plantDX||0)))<4 && Math.abs(landPiv1 - landSpawnPiv)>20, `x=${landStamp1&&landStamp1.x.toFixed(1)} spawn=${(landSpawnX||0).toFixed(1)} piv=${landPiv1.toFixed(1)} piv0=${landSpawnPiv.toFixed(1)}`);
check("plant dust leftover land AABB planted", Math.abs(V.bodyAABB(V.rival).y-landAabb0.y)<0.05 && Math.abs(V.bodyAABB(V.rival).h-landAabb0.h)<0.05, `dy=${(V.bodyAABB(V.rival).y-landAabb0.y).toFixed(2)}`);
check("plant dust leftover land destRect planted", Math.abs(V.destRect(V.rival).dy-landDest0.dy)<2.2, `ddy=${(V.destRect(V.rival).dy-landDest0.dy).toFixed(2)}`);
check("plant dust leftover specks not glued", (V.plantDust||[]).some(p=>p.speck && p.homeYou===false), `n=${(V.plantDust||[]).filter(p=>p.speck).length}`);
check("cache v=296 after plant dust leftover live", /game.js\?v=350/.test(htmlTxt));

check("throw grab snap leftover yanks to clinch not 40px plants", /function throwSnapTogether/.test(srcTxt) && /function throwGrabHold/.test(srcTxt) && /Throw grab snap leftover/.test(srcTxt) && /leftover 40px plants/.test(srcTxt) && /keepApart un-did the grab/.test(srcTxt) && /THROW_SNAP_GAP/.test(srcTxt));
check("throw grab snap leftover is body clinch", V.THROW_SNAP_GAP===24 && V.THROW_SNAP_GAP<V.THROW_RANGE && V.THROW_SNAP_GAP>=16 && V.THROW_SNAP_GAP<=32 && /gap > THROW_SNAP_GAP/.test(sliceFn(codeOnly, "throwSnapTogether")));
check("throw grab snap leftover keepApart holds thrownT", /throwGrabHold\(\)/.test(sliceFn(codeOnly, "keepApart")) && /push > 0 && !throwGrabHold\(\)/.test(sliceFn(codeOnly, "keepApart")) && /throwSnapTogether\(atk, def\)/.test(sliceFn(codeOnly, "landThrow")));
check("throw grab snap leftover no leftover 40px plants", !/atk\.x \+ dir \* 40/.test(codeOnly) && !/def\.x = atk\.x \+ dir \* 40/.test(srcTxt));
check("throw grab snap leftover destRect AABB planted", /destRect\/AABB planted/.test(srcTxt) && !/throwSnapTogether/.test(sliceFn(codeOnly, "destRect")) && !/throwSnapTogether/.test(sliceFn(codeOnly, "bodyAABB")) && !/throwSnapTogether/.test(sliceFn(codeOnly, "slashPose")));
check("throw grab snap leftover is not a new combat verb", V.THROW_STARTUP===80 && V.THROW_ACTIVE===40 && V.THROW_RECOVERY===220 && V.THROW_DMG===20 && V.THROW_RANGE===120 && V.THROW_KD_MS===520 && V.THROW_STAM===0 && V.HITSTOP_HIT===140 && V.HITSTUN===350);
check("slash frames still 180/140/280 after throw grab snap leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after throw grab snap leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after throw grab snap leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after throw grab snap leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("plant dust leftover still rides destRect after throw grab snap", /function syncPlantDust/.test(srcTxt) && /Stamps ride destRect\.pivX/.test(srcTxt));
check("cache v=296 after throw grab snap leftover", /game.js\?v=350/.test(htmlTxt));

function plantThrowPocket(){
  V.player.gait = 0; V.rival.gait = 0;
  V.player.walkSettleT = 0; V.rival.walkSettleT = 0;
  V.player.walkRiseT = 0; V.rival.walkRiseT = 0;
  V.player.pushT = 0; V.rival.pushT = 0;
  V.player.stunT = 0; V.rival.stunT = 0;
  V.player.thrownT = 0; V.rival.thrownT = 0;
  V.player.phase = "idle"; V.rival.phase = "idle";
  V.player.boltPhase = ""; V.rival.boltPhase = "";
  V.player.guarding = false; V.rival.guarding = false;
  V.player.x = V.rival.x - 170;
  V.keepApart();
}

toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
plantThrowPocket();
V.rival.stamina = 80;
V.rival.wantBlock = true;
V.rival.guarding = true;
V.rival.guardChoice = 1;
V.player.stamina = 100;
const snapAabb0 = V.bodyAABB(V.rival);
const snapDest0 = V.destRect(V.rival);
const snapGap0 = V.bodyGap();
V.rival.thrownT = V.THROW_KD_MS;
V.rival.stunT = V.THROW_KD_MS;
V.throwSnapTogether(V.player, V.rival);
V.keepApart();
check("throw grab snap leftover isolated clinch", Math.abs(V.bodyGap()-V.THROW_SNAP_GAP)<2.5 && V.throwGrabHold() && snapGap0>V.THROW_SNAP_GAP+40, `g0=${snapGap0.toFixed(1)} g=${V.bodyGap().toFixed(1)} hold=${V.throwGrabHold()} want=${V.THROW_SNAP_GAP}`);
check("throw grab snap leftover isolated AABB planted", Math.abs(V.bodyAABB(V.rival).y-snapAabb0.y)<0.05 && Math.abs(V.bodyAABB(V.rival).h-snapAabb0.h)<0.05, `dy=${(V.bodyAABB(V.rival).y-snapAabb0.y).toFixed(2)}`);
check("throw grab snap leftover isolated destRect planted", Math.abs(V.destRect(V.rival).dy-snapDest0.dy)<2.2, `ddy=${(V.destRect(V.rival).dy-snapDest0.dy).toFixed(2)}`);
V.rival.thrownT = 0;
V.rival.stunT = 0;

toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
plantThrowPocket();
V.player.hp = 100; V.rival.hp = 100;
V.rival.stamina = 80;
V.rival.stamRegenT = 99999;
V.rival.guardBreakT = 0;
V.rival.wantBlock = true;
V.rival.guarding = true;
V.rival.guardChoice = 1;
V.player.stamina = 100;
const liveAabb0 = V.bodyAABB(V.rival);
const liveGap0 = V.bodyGap();
fireThrow();
let snapHit=false;
for(let i=0;i<80;i++){
  holdRivalStill();
  V.rival.wantBlock = true;
  V.rival.guarding = true;
  V.rival.guardChoice = 1;
  V.rival.standGoal = 99999;
  V.rival.closing = false;
  V.update(STEP);
  if(V.hitstopLeft>0 || V.rival.hp<100 || V.rival.thrownT>0){ snapHit=true; break; }
}
const liveGap1 = V.bodyGap();
const liveAbs1 = V.absGap();
const liveDestHit = V.destRect(V.rival);
check("throw grab snap leftover live connects", snapHit && V.rival.hp===80 && V.player.cut==="throw" && V.player.cutHit && V.rival.thrownT>0 && V.throwGrabHold(), `hit=${snapHit} rhp=${V.rival.hp} ch=${V.player.cutHit} th=${V.rival.thrownT} hold=${V.throwGrabHold()} g0=${liveGap0.toFixed(1)} g=${liveGap1.toFixed(1)}`);
check("throw grab snap leftover live clinch on connect", snapHit && Math.abs(liveGap1-V.THROW_SNAP_GAP)<6 && liveGap1>8 && liveGap1<50 && liveGap0>liveGap1+40, `g0=${liveGap0.toFixed(1)} g=${liveGap1.toFixed(1)} abs=${liveAbs1.toFixed(1)} want=${V.THROW_SNAP_GAP}`);
const freezeGap0 = V.bodyGap();
for(let i=0;i<6;i++){
  holdRivalStill();
  V.update(STEP);
}
const freezeGap1 = V.bodyGap();
check("throw grab snap leftover holds freeze", V.hitstopLeft>0 && V.throwGrabHold() && Math.abs(freezeGap1-V.THROW_SNAP_GAP)<6 && Math.abs(freezeGap1-freezeGap0)<6, `hs=${V.hitstopLeft} g0=${freezeGap0.toFixed(1)} g=${freezeGap1.toFixed(1)} hold=${V.throwGrabHold()}`);
check("throw grab snap leftover live AABB planted", Math.abs(V.bodyAABB(V.rival).y-liveAabb0.y)<0.05 && Math.abs(V.bodyAABB(V.rival).h-liveAabb0.h)<0.05, `dy=${(V.bodyAABB(V.rival).y-liveAabb0.y).toFixed(2)}`);
check("throw grab snap leftover live destRect planted", Math.abs(V.destRect(V.rival).dy-liveDestHit.dy)<0.35, `ddy=${(V.destRect(V.rival).dy-liveDestHit.dy).toFixed(2)}`);
check("throw grab snap leftover still hurt sheet", /hurt/.test(sheetSrc(V.rival)) || V.poseSheet(V.rival)===V.poseSheet(Object.assign({}, V.rival, {falling:true, hp:0, thrownT:0})), `src=${sheetSrc(V.rival)}`);
drainHitstop();
check("throw grab snap leftover still clinch after freeze", V.rival.thrownT>0 && Math.abs(V.bodyGap()-V.THROW_SNAP_GAP)<8 && V.bodyGap()>8, `g=${V.bodyGap().toFixed(1)} th=${V.rival.thrownT} hs=${V.hitstopLeft}`);
V.keys.clear();

toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
plantThrowPocket();
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 80;
V.player.wantBlock = true;
V.player.guarding = true;
V.rival.stamina = 100;
V.rival.throwCd = 0;
V.rival.wantBlock = false;
V.rival.guarding = false;
const rSnapGap0 = V.bodyGap();
V.startThrow(V.rival);
let rSnapHit=false;
for(let i=0;i<40;i++){
  holdRivalStill();
  V.player.wantBlock = true;
  V.player.guarding = true;
  V.rival.throwCd = 99999;
  V.update(STEP);
  if(V.hitstopLeft>0 || V.player.hp<100 || V.player.thrownT>0){ rSnapHit=true; break; }
}
check("throw grab snap leftover rival clinch", rSnapHit && V.player.hp===80 && V.rival.cutHit && V.player.thrownT>0 && V.throwGrabHold() && Math.abs(V.bodyGap()-V.THROW_SNAP_GAP)<6 && V.bodyGap()>8 && rSnapGap0>V.bodyGap()+40, `hit=${rSnapHit} php=${V.player.hp} th=${V.player.thrownT} g0=${rSnapGap0.toFixed(1)} g=${V.bodyGap().toFixed(1)}`);
for(let i=0;i<4;i++) V.update(STEP);
check("throw grab snap leftover rival holds freeze", V.hitstopLeft>0 && V.player.thrownT>0 && Math.abs(V.bodyGap()-V.THROW_SNAP_GAP)<6, `hs=${V.hitstopLeft} g=${V.bodyGap().toFixed(1)} th=${V.player.thrownT}`);
drainHitstop();
V.keys.clear();

bootPlay(); wait(1700);
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.x = 180;
V.rival.x = 1100;
const farSnap0 = V.absGap();
fireThrow();
let farSnapAct=false, farSnapDmg=false;
for(let i=0;i<80;i++){
  holdRivalStill();
  V.rival.x = 1100;
  V.update(STEP);
  if(V.player.cut==="throw" && V.player.phase==="active") farSnapAct=true;
  if(V.rival.hp<100 || V.rival.thrownT>0) farSnapDmg=true;
  if(V.player.cut==="throw" && V.player.phase==="recovery") break;
  if(V.player.phase==="idle" && i>10 && V.player.cut!=="throw") break;
}
check("throw grab snap leftover far still whiffs", farSnap0>400 && farSnapAct && !farSnapDmg && V.rival.hp===100 && V.rival.thrownT===0 && !V.player.cutHit, `g0=${farSnap0.toFixed(1)} act=${farSnapAct} dmg=${farSnapDmg} hp=${V.rival.hp} th=${V.rival.thrownT}`);
V.keys.clear();
check("cache v=296 after throw grab snap leftover live", /game.js\?v=350/.test(htmlTxt));



check("feint pose leftover fades windup to idle", /function feintFade/.test(srcTxt) && /feintFade\(f\)/.test(srcTxt) && /Feint pose leftover/.test(srcTxt) && /leftover windup used to pop/.test(srcTxt) && /tajo lunge/.test(srcTxt) && /sheathing\(\) holds slash/.test(srcTxt));
check("feint pose leftover is draw-only sheet", /Visual only \(drawKnight\)/.test(srcTxt) && /feintFade\(f\)/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && /poseBitmap still idle once feintT is/.test(srcTxt) && !/feintFade/.test(sliceFn(codeOnly, "destRect")) && !/feintFade/.test(sliceFn(codeOnly, "bodyAABB")) && !/feintFade/.test(sliceFn(codeOnly, "slashPose")) && !/feintFade/.test(sliceFn(codeOnly, "landHit")) && !/feintFade/.test(sliceFn(codeOnly, "startAttack")));
check("feint pose leftover destRect breath ease", /f\.feintT > 0/.test(sliceFn(codeOnly, "idleBreath")) && /feintFade\(f\)/.test(sliceFn(codeOnly, "idleBreath")) && /pull used to dump leftover breath/.test(srcTxt));
check("feint pose leftover uses FEINT_RECOVERY", /FEINT_RECOVERY/.test(sliceFn(codeOnly, "feintFade")) && V.FEINT_RECOVERY===100 && /restFeint/.test(sliceFn(codeOnly, "drawKnight")) && /feintFade\(f\)/.test(sliceFn(codeOnly, "drawKnight")));
check("feint pose leftover poseBitmap still idle", /f\.feintT <= 0/.test(sliceFn(codeOnly, "sheathing")) && /sheathing\(f\) && ready\(slash\)/.test(sliceFn(codeOnly, "poseBitmap")) && /f\.feintT > 0/.test(sliceFn(codeOnly, "sheatheFade")));
check("feint pose leftover does not hold slash sheathe", /if \(f\.feintT > 0\) return 0/.test(sliceFn(codeOnly, "sheatheFade")) && /if \(f\.feintT > 0\) return 0/.test(sliceFn(codeOnly, "sheatheDip")) && /f\.feintT === 0\) f\.sheatheT = 0/.test(sliceFn(codeOnly, "tickFeint")));
check("clash tech still idle sheathe after feint pose leftover", /f\.sheatheT \/ SHEATHE_MS/.test(sliceFn(codeOnly, "sheatheFade")) && /1 - u \* u \* \(3 - 2 \* u\)/.test(sliceFn(codeOnly, "sheatheFade")) && /Clash\/tech still sheathe from slash/.test(srcTxt));
check("feint pose leftover is not a new combat verb", V.FEINT_RECOVERY===100 && V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80 && V.GUARD_COMMIT_MS===140 && V.STARTUP===180);
check("slash frames still 180/140/280 after feint pose leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after feint pose leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after feint pose leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after feint pose leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after feint pose leftover", /game.js\?v=350/.test(htmlTxt));

toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.guarding = false;
V.player.stunT = 0;
V.player.feintT = 0;
V.player.sheatheT = 0;
V.player.walkSettleT = 0;
V.player.gait = 0;
V.keys.clear();
V.attackEdge = true;
V.update(STEP);
check("feint pose leftover arms from slash startup", V.player.phase==="startup" && V.player.cut==="slash" && V.canFeint(V.player) && /windup/.test(sheetSrc(V.player)), `ph=${V.player.phase} cut=${V.player.cut} src=${sheetSrc(V.player)}`);
for(let i=0;i<4;i++){ holdRivalStill(); V.rival.guarding=false; V.update(STEP); }
check("feint pose leftover still windup before S", V.player.phase==="startup" && V.canFeint(V.player) && /windup/.test(sheetSrc(V.player)) && V.hitbox(V.player)===null, `ph=${V.player.phase} t=${V.player.phaseT} src=${sheetSrc(V.player)}`);
const feintPoseAabb0 = V.bodyAABB(V.player);
const feintPoseDest0 = V.destRect(V.player);
V.feintEdge = true;
holdRivalStill();
V.update(STEP);
check("feint pose leftover pulls idle not slash sheet", V.player.phase==="idle" && V.player.feintT>0 && V.player.feintT<=V.FEINT_RECOVERY && V.player.sheatheT>0 && !isCutSheet(V.player) && !/windup/.test(sheetSrc(V.player)), `ph=${V.player.phase} ft=${V.player.feintT} sh=${V.player.sheatheT} src=${sheetSrc(V.player)} fade=${V.feintFade(V.player)}`);
check("feint pose leftover fade starts high", V.feintFade(V.player)>0.85 && V.sheatheFade(V.player)===0, `ff=${V.feintFade(V.player).toFixed(3)} sf=${V.sheatheFade(V.player)} ft=${V.player.feintT}`);
check("feint pose leftover AABB planted at pull", Math.abs(V.bodyAABB(V.player).y-feintPoseAabb0.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-feintPoseAabb0.h)<0.05, `dy=${(V.bodyAABB(V.player).y-feintPoseAabb0.y).toFixed(2)}`);
check("feint pose leftover destRect planted at pull", Math.abs(V.destRect(V.player).dy-feintPoseDest0.dy)<0.35, `ddy=${(V.destRect(V.player).dy-feintPoseDest0.dy).toFixed(2)} ff=${V.feintFade(V.player).toFixed(3)}`);
const feintFade0 = V.feintFade(V.player);
let feintFadeMid=-1;
for(let i=0;i<3;i++){
  holdRivalStill();
  V.rival.guarding=false;
  V.update(STEP);
  feintFadeMid=V.feintFade(V.player);
}
check("feint pose leftover fade drops", feintFadeMid>=0 && feintFadeMid<feintFade0-0.08 && V.player.feintT>0 && V.player.phase==="idle" && !isCutSheet(V.player), `mid=${feintFadeMid.toFixed(3)} t0=${feintFade0.toFixed(3)} ft=${V.player.feintT} src=${sheetSrc(V.player)}`);
check("feint pose leftover AABB planted through fade", Math.abs(V.bodyAABB(V.player).y-feintPoseAabb0.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-feintPoseAabb0.h)<0.05, `dy=${(V.bodyAABB(V.player).y-feintPoseAabb0.y).toFixed(2)}`);
check("feint pose leftover destRect eases breath", Math.abs(V.destRect(V.player).dy-feintPoseDest0.dy)<2.2, `ddy=${(V.destRect(V.player).dy-feintPoseDest0.dy).toFixed(2)}`);
for(let i=0;i<8;i++){
  holdRivalStill();
  V.rival.guarding=false;
  V.update(STEP);
}
check("feint pose leftover fade gone", V.player.feintT===0 && V.feintFade(V.player)===0 && V.player.phase==="idle" && !isCutSheet(V.player) && !/windup/.test(sheetSrc(V.player)), `ft=${V.player.feintT} ff=${V.feintFade(V.player)} sh=${V.player.sheatheT} src=${sheetSrc(V.player)}`);
check("feint pose leftover leftover sheatheT cleared", V.player.sheatheT===0 && V.sheatheFade(V.player)===0, `sh=${V.player.sheatheT} sf=${V.sheatheFade(V.player)}`);

toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.rival.gait = 0;
V.rival.walkSettleT = 0;
V.rival.sheatheT = 0;
V.rival.feintT = 0;
V.rival.guarding = false;
V.startAttack(V.rival, "slash");
for(let i=0;i<4;i++){ holdRivalStill(); V.update(STEP); }
check("rival feint pose leftover still windup", V.rival.phase==="startup" && V.canFeint(V.rival) && /windup/.test(sheetSrc(V.rival)), `ph=${V.rival.phase} src=${sheetSrc(V.rival)}`);
const rFeintAabb0 = V.bodyAABB(V.rival);
const rFeintDest0 = V.destRect(V.rival);
const rDid = V.startFeint(V.rival);
check("rival feint pose leftover pulls idle", rDid && V.rival.phase==="idle" && V.rival.feintT>0 && V.feintFade(V.rival)>0.85 && !isCutSheet(V.rival) && !/windup/.test(sheetSrc(V.rival)), `ok=${rDid} ph=${V.rival.phase} ft=${V.rival.feintT} ff=${V.feintFade(V.rival)} src=${sheetSrc(V.rival)}`);
check("rival feint pose leftover AABB planted", Math.abs(V.bodyAABB(V.rival).y-rFeintAabb0.y)<0.05 && Math.abs(V.bodyAABB(V.rival).h-rFeintAabb0.h)<0.05, `dy=${(V.bodyAABB(V.rival).y-rFeintAabb0.y).toFixed(2)}`);
check("rival feint pose leftover destRect planted", Math.abs(V.destRect(V.rival).dy-rFeintDest0.dy)<0.35, `ddy=${(V.destRect(V.rival).dy-rFeintDest0.dy).toFixed(2)}`);
V.update(STEP);
check("rival feint pose leftover fade starts", V.rival.feintT>0 && V.feintFade(V.rival)>0 && V.feintFade(V.rival)<1, `ff=${V.feintFade(V.rival).toFixed(3)} ft=${V.rival.feintT}`);

toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.phase = "idle"; V.player.phaseT = 0; V.player.feintT = 0;
V.golpeEdge = true;
V.update(STEP);
check("golpe still not feint pose leftover", V.player.phase==="startup" && V.player.cut==="golpe" && !V.canFeint(V.player) && V.feintFade(V.player)===0, `ph=${V.player.phase} cut=${V.player.cut} ff=${V.feintFade(V.player)}`);
V.player.phase = "idle"; V.player.phaseT = 0;

check("cache v=296 after feint pose leftover live", /game.js\?v=350/.test(htmlTxt));


check("hurt juice leftover cuts live blade before recovery", /Hurt juice leftover/.test(srcTxt) && /leftover lunge ox dump used to pull cutPoint/.test(srcTxt) && /flesh spark sat on chest/.test(srcTxt) && /AABB fallback/.test(srcTxt) && /chips/.test(srcTxt) && /body puff/.test(srcTxt));
check("hurt juice leftover is draw-only cut", /Draw-only \(cut before recovery\)/.test(srcTxt) && /Ride still live hurt/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && /cutPoint\(atk, def\)/.test(sliceFn(codeOnly, "landHit")) && !/cutPoint/.test(sliceFn(codeOnly, "destRect")) && !/cutPoint/.test(sliceFn(codeOnly, "bodyAABB")) && !/cutPoint/.test(sliceFn(codeOnly, "slashPose")));
check("hurt juice leftover cutPoint before recovery", (()=>{ const lh=sliceFn(codeOnly, "landHit"); const iC=lh.indexOf("cutPoint"); const iR=lh.indexOf('phase = "recovery"'); const iS=lh.indexOf("stunT"); return iC>=0 && iR>=0 && iS>=0 && iC<iR && iC<iS; })());
check("hurt juice leftover spawn still after flash arm", (()=>{ const lh=sliceFn(codeOnly, "landHit"); return lh.indexOf("cutPoint")>=0 && lh.indexOf("cutPoint")<lh.indexOf("spawnHitSpark") && lh.indexOf("HIT_FLASH_MS")<lh.indexOf("spawnHitSpark") && /spawnHitSpark\(pt\.x, pt\.y/.test(lh); })());
check("hurt juice leftover ride still live hurt", /hitWoundDX/.test(sliceFn(codeOnly, "spawnHitSpark")) && /hitWoundAnchor/.test(sliceFn(codeOnly, "syncHitSpark")) && /a\.x \+ hitWoundDX/.test(sliceFn(codeOnly, "syncHitSpark")));
check("block asterisk still after recovery after hurt juice leftover", (()=>{ const lb=sliceFn(codeOnly, "landBlock"); return lb.indexOf('phase = "recovery"')>=0 && lb.indexOf("cutPoint")>lb.indexOf('phase = "recovery"') && /spawnSteelFlash/.test(lb); })());
check("clash shards still spawn before recovery after hurt juice leftover", (()=>{ const dc=sliceFn(codeOnly, "doClash"); return dc.indexOf("spawnClashSpark")>=0 && dc.indexOf("spawnClashSpark")<dc.indexOf('phase = "recovery"'); })());
check("hurt juice leftover is not a new combat verb", V.SLASH_DMG===10 && V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.HIT_SPARK_MS===100 && V.KNOCK_PX===80 && V.KNOCK_MS===180 && V.LUNGE_PX===36);
check("slash frames still 180/140/280 after hurt juice leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after hurt juice leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after hurt juice leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after hurt juice leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after hurt juice leftover", /game.js\?v=350/.test(htmlTxt));

function chestXY(f){
  const bb=V.bodyAABB(f);
  return {x: bb.x + bb.w*0.5, y: bb.y + bb.h*0.28};
}

function overlapsBox(a,b){
  return !!(a && b && a.x < b.x+b.w && a.x+a.w > b.x && a.y < b.y+b.h && a.y+a.h > b.y);
}
function plantJustConnect(atk, def, kind){
  const lunge = kind==="golpe" ? V.GOLPE_LUNGE_PX : V.LUNGE_PX;
  atk.cut = kind==="golpe" ? "golpe" : "slash";
  atk.phase = "active";
  atk.phaseT = 20;
  atk.cutHit = false;
  atk.guarding = false;
  atk.stunT = 0;
  atk.boltPhase = "";
  def.guarding = false;
  def.stunT = 0;
  def.phase = "idle";
  def.phaseT = 0;
  def.boltPhase = "";
  if (atk.kind==="you") atk.x = def.x - 420;
  else atk.x = def.x + 420;
  atk.x += atk.facing * lunge;
  let ok=false;
  for(let i=0;i<120;i++){
    if (overlapsBox(V.hitbox(atk), V.bodyAABB(def))) { ok=true; break; }
    atk.x += atk.facing * 4;
  }
  return ok;
}

toMeasure();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.hp = 100; V.rival.hp = 100;
const planted = plantJustConnect(V.player, V.rival, "slash");
const liveCut = V.cutPoint(V.player, V.rival);
const liveChest = chestXY(V.rival);
const juiceAabb0 = V.bodyAABB(V.rival);
const juiceDest0 = V.destRect(V.rival);
check("hurt juice leftover plants live cut at measure", planted && V.player.phase==="active" && V.player.cut==="slash" && overlapsBox(V.hitbox(V.player), V.bodyAABB(V.rival)), `ok=${planted} ph=${V.player.phase} cut=${V.player.cut} gap=${V.bodyGap().toFixed(1)}`);
holdRivalStill();
V.rival.guarding=false;
V.update(STEP);
const leftoverCut = V.cutPoint(V.player, V.rival);
const dSparkCut = Math.hypot(V.hitSparkX-liveCut.x, V.hitSparkY-liveCut.y);
const dSparkLeft = Math.hypot(V.hitSparkX-leftoverCut.x, V.hitSparkY-leftoverCut.y);
const dSparkChest = Math.hypot(V.hitSparkX-liveChest.x, V.hitSparkY-liveChest.y);
check("hurt juice leftover live connects at measure", planted && V.player.cutHit && V.rival.hp===90 && V.player.phase==="recovery" && V.hitSparkT>0, `hit=${V.player.cutHit} hp=${V.rival.hp} ph=${V.player.phase} sp=${V.hitSparkT}`);
check("hurt juice leftover spark on live cut not chest", planted && dSparkCut<8 && dSparkChest>12 && V.hitSparkT>0, `cut=${dSparkCut.toFixed(1)} chest=${dSparkChest.toFixed(1)} left=${dSparkLeft.toFixed(1)} sx=${V.hitSparkX.toFixed(1)} sy=${V.hitSparkY.toFixed(1)} cx=${liveCut.x.toFixed(1)} chx=${liveChest.x.toFixed(1)}`);
check("hurt juice leftover AABB planted at chip", Math.abs(V.bodyAABB(V.rival).y-juiceAabb0.y)<0.05 && Math.abs(V.bodyAABB(V.rival).h-juiceAabb0.h)<0.05, `dy=${(V.bodyAABB(V.rival).y-juiceAabb0.y).toFixed(2)}`);
check("hurt juice leftover destRect planted at chip", Math.abs(V.destRect(V.rival).dy-juiceDest0.dy)<2.2, `ddy=${(V.destRect(V.rival).dy-juiceDest0.dy).toFixed(2)}`);
const sparkHoldX=V.hitSparkX, sparkHoldY=V.hitSparkY;
for(let i=0;i<4;i++){
  holdRivalStill();
  V.update(STEP);
}
check("hurt juice leftover spark holds freeze on the cut", V.hitstopLeft>0 && V.hitSparkT>0 && Math.hypot(V.hitSparkX-sparkHoldX, V.hitSparkY-sparkHoldY)<6, `hs=${V.hitstopLeft} d=${Math.hypot(V.hitSparkX-sparkHoldX, V.hitSparkY-sparkHoldY).toFixed(2)} t=${V.hitSparkT}`);
drainHitstop();
check("hurt juice leftover spark rides live hurt after freeze", V.rival.stunT>0 && V.hitSparkT>=0, `st=${V.rival.stunT} t=${V.hitSparkT} sx=${V.hitSparkX.toFixed(1)}`);
V.keys.clear();

toMeasure();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.hp=100; V.rival.hp=100;
const gPlant = plantJustConnect(V.player, V.rival, "golpe");
const gLiveCut = V.cutPoint(V.player, V.rival);
const gLiveChest = chestXY(V.rival);
holdRivalStill();
V.update(STEP);
const gDCut = Math.hypot(V.hitSparkX-gLiveCut.x, V.hitSparkY-gLiveCut.y);
const gDChest = Math.hypot(V.hitSparkX-gLiveChest.x, V.hitSparkY-gLiveChest.y);
check("hurt juice leftover golpe spark on live cut", gPlant && V.player.cut==="golpe" && V.player.cutHit && V.rival.hp===90 && gDCut<8 && gDChest>18, `ok=${gPlant} hp=${V.rival.hp} cut=${gDCut.toFixed(1)} chest=${gDChest.toFixed(1)}`);
V.keys.clear();

toMeasure();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.guarding=false;
V.player.wantBlock=false;
V.player.hp=100; V.rival.hp=100;
const rPlant = plantJustConnect(V.rival, V.player, "slash");
const rLiveCut = V.cutPoint(V.rival, V.player);
const rLiveChest = chestXY(V.player);
holdRivalStill();
V.player.guarding=false;
V.player.wantBlock=false;
V.update(STEP);
const rDCut = Math.hypot(V.hitSparkX-rLiveCut.x, V.hitSparkY-rLiveCut.y);
const rDChest = Math.hypot(V.hitSparkX-rLiveChest.x, V.hitSparkY-rLiveChest.y);
check("hurt juice leftover rival spark on live cut", rPlant && V.rival.cutHit && V.player.hp===90 && rDCut<8 && rDChest>28, `ok=${rPlant} php=${V.player.hp} cut=${rDCut.toFixed(1)} chest=${rDChest.toFixed(1)}`);
V.keys.clear();

check("cache v=296 after hurt juice leftover live", /game.js\?v=350/.test(htmlTxt));


check("KO crumple leftover eases destRect oy", /function crumpleFade/.test(srcTxt) && /crumpleFade\(f\)/.test(srcTxt) && /KO crumple leftover/.test(srcTxt) && /leftover destRect oy used to dump/.test(srcTxt) && /hopped off the stones/.test(srcTxt) && /beginFall set falling/.test(srcTxt));
check("KO crumple leftover is destRect-only ease", /destRect-only/.test(srcTxt) && /crumpleFade\(f\)/.test(sliceFn(codeOnly, "slashPose")) && /AABB planted/.test(srcTxt) && /poseBitmap still hurt/.test(srcTxt) && !/crumpleFade/.test(sliceFn(codeOnly, "bodyAABB")) && !/crumpleFade/.test(sliceFn(codeOnly, "landHit")) && !/crumpleFade/.test(sliceFn(codeOnly, "beginFall")));
check("KO crumple leftover AABB planted on fall", /f\.falling/.test(sliceFn(codeOnly, "bodyAABB")) && /stunT > 0 && f\.hp > 0/.test(sliceFn(codeOnly, "bodyAABB")));
check("KO crumple leftover uses GUARD_RAISE_MS", /GUARD_RAISE_MS/.test(sliceFn(codeOnly, "crumpleFade")) && V.GUARD_RAISE_MS===80 && V.FALL_MS===720);
check("KO crumple leftover poseBitmap still hurt", /f\.falling \|\| f\.hp <= 0/.test(sliceFn(codeOnly, "poseBitmap")) && /f\.falling \|\| f\.hp <= 0/.test(sliceFn(codeOnly, "poseSheet")));
check("KO crumple leftover extra rot stays 0", /Extra destRect rot stays 0/.test(srcTxt) && /rot: 0, ox: 0, oy: -10 \* lift \* crumpleFade\(f\)/.test(codeOnly));
check("KO crumple leftover is not a new combat verb", V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.FALL_MS===720 && V.GUARD_RAISE_MS===80 && V.HIT_FLASH_MS===120);
check("slash frames still 180/140/280 after KO crumple leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after KO crumple leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after KO crumple leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after KO crumple leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after KO crumple leftover", /game.js\?v=350/.test(htmlTxt));

bootPlay(); wait(1700);
freezeRivalAI(); freezeYouAI();
V.rival.hp = 10;
V.player.x = V.rival.x - 170;
V.keepApart();
V.attackEdge = true;
let crHs=false, crFall=false, crDest0=null, crAabb0=null, crDestArm=null, crAabbArm=null, crFade0=-1, crFlash0=-1;
for(let i=0;i<80;i++){
  freezeRivalAI();
  V.update(STEP);
  if(V.hitstopLeft>0 && V.rival.hp<=0){
    crHs=true;
    crDest0=V.destRect(V.rival);
    crAabb0=V.bodyAABB(V.rival);
    crFlash0=V.hitFlashT;
    break;
  }
}
check("KO crumple leftover freeze still chips death", crHs && V.rival.hp===0 && !V.rival.falling && V.crumpleFade(V.rival)===0 && isHurtSheet(V.rival) && V.hurtFlashK(V.rival)>0.9, `hs=${crHs} hp=${V.rival.hp} fall=${V.rival.falling} cf=${V.crumpleFade(V.rival)} src=${sheetSrc(V.rival)} flash=${V.hurtFlashK(V.rival)}`);
for(let i=0;i<20;i++){
  if(V.hitstopLeft<=0 && V.mode==="falling") break;
  V.update(STEP);
}
crFall = V.mode==="falling" && V.rival.falling;
crDestArm=V.destRect(V.rival);
crAabbArm=V.bodyAABB(V.rival);
crFade0=V.crumpleFade(V.rival);
check("KO crumple leftover arms without oy dump", crFall && crDest0 && Math.abs(crDestArm.dy-crDest0.dy)<0.35 && V.crumpleFade(V.rival)<0.2 && Math.abs(crDestArm.rot)<0.001, `fall=${crFall} ddy=${crDest0?(crDestArm.dy-crDest0.dy).toFixed(2):"na"} cf=${crFade0} rot=${crDestArm.rot} ft=${V.rival.fallT}`);
check("KO crumple leftover AABB planted at arm", crFall && crAabb0 && Math.abs(crAabbArm.y-crAabb0.y)<0.05 && Math.abs(crAabbArm.h-crAabb0.h)<0.05, `dy=${crAabb0?(crAabbArm.y-crAabb0.y).toFixed(2):"na"}`);
check("KO crumple leftover poseBitmap hurt immediately", crFall && isHurtSheet(V.rival), `src=${sheetSrc(V.rival)}`);
check("KO crumple leftover flash still through crumple", crFall && V.hurtFlashK(V.rival)>0.5 && crFlash0===V.HIT_FLASH_MS, `k=${V.hurtFlashK(V.rival)} flash0=${crFlash0}`);
let crFadeMid=-1, crDestMid=null, crAabbMid=null;
for(let i=0;i<3;i++){
  V.update(STEP);
  crFadeMid=V.crumpleFade(V.rival);
  crDestMid=V.destRect(V.rival);
  crAabbMid=V.bodyAABB(V.rival);
}
check("KO crumple leftover fade starts", crFall && crFadeMid>crFade0 && crFadeMid<0.85 && V.rival.falling && V.rival.fallT<V.GUARD_RAISE_MS, `mid=${crFadeMid.toFixed(3)} t0=${crFade0} ft=${V.rival.fallT}`);
check("KO crumple leftover destRect eases lift", crDestMid && crDest0 && (crDest0.dy-crDestMid.dy)>0.4 && (crDest0.dy-crDestMid.dy)<8, `ddy=${crDest0?(crDest0.dy-crDestMid.dy).toFixed(2):"na"} cf=${crFadeMid}`);
check("KO crumple leftover AABB planted through ease", crAabb0 && crAabbMid && Math.abs(crAabbMid.y-crAabb0.y)<0.05 && Math.abs(crAabbMid.h-crAabb0.h)<0.05, `dy=${crAabb0?(crAabbMid.y-crAabb0.y).toFixed(2):"na"}`);
for(let i=0;i<10;i++){
  V.update(STEP);
}
check("KO crumple leftover fade completes", V.rival.falling && V.rival.fallT>=V.GUARD_RAISE_MS && V.crumpleFade(V.rival)===1, `ft=${V.rival.fallT} cf=${V.crumpleFade(V.rival)}`);
check("KO crumple leftover destRect extra rot still 0", V.rival.falling && Math.abs(V.destRect(V.rival).rot)<0.001, `rot=${V.destRect(V.rival).rot}`);
check("KO crumple leftover AABB planted after raise", crAabb0 && Math.abs(V.bodyAABB(V.rival).y-crAabb0.y)<0.05, `dy=${crAabb0?(V.bodyAABB(V.rival).y-crAabb0.y).toFixed(2):"na"}`);

bootPlay(); wait(1700);
freezeRivalAI(); freezeYouAI();
V.player.x = V.rival.x - 170;
V.keepApart();
V.attackEdge = true;
for(let i=0;i<80;i++){
  freezeRivalAI();
  V.update(STEP);
  if(V.hitstopLeft>0 && V.rival.hp===90) break;
}
check("chip stun still not crumple leftover", V.rival.hp===90 && V.rival.stunT>0 && !V.rival.falling && V.crumpleFade(V.rival)===0 && !isHurtSheet(V.rival), `hp=${V.rival.hp} st=${V.rival.stunT} cf=${V.crumpleFade(V.rival)} src=${sheetSrc(V.rival)}`);
drainHitstop();
check("chip stun leftover still idle after freeze", V.rival.hp===90 && !V.rival.falling && V.crumpleFade(V.rival)===0 && !isHurtSheet(V.rival), `hp=${V.rival.hp} cf=${V.crumpleFade(V.rival)} src=${sheetSrc(V.rival)}`);

toMeasure();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.hp = 10; V.rival.hp = 100;
const youPlant = plantJustConnect(V.rival, V.player, "slash");
holdRivalStill();
V.player.guarding=false;
V.update(STEP);
const youDest0 = V.destRect(V.player);
const youAabb0 = V.bodyAABB(V.player);
check("you KO crumple leftover freeze", youPlant && V.player.hp===0 && !V.player.falling && V.crumpleFade(V.player)===0 && isHurtSheet(V.player), `ok=${youPlant} hp=${V.player.hp} fall=${V.player.falling} cf=${V.crumpleFade(V.player)} src=${sheetSrc(V.player)}`);
for(let i=0;i<20;i++){
  holdRivalStill();
  if(V.hitstopLeft<=0 && V.mode==="falling") break;
  V.update(STEP);
}
check("you KO crumple leftover arms planted", V.player.falling && V.crumpleFade(V.player)<0.2 && Math.abs(V.destRect(V.player).dy-youDest0.dy)<0.35 && Math.abs(V.bodyAABB(V.player).y-youAabb0.y)<0.05, `fall=${V.player.falling} cf=${V.crumpleFade(V.player)} ddy=${(V.destRect(V.player).dy-youDest0.dy).toFixed(2)} ady=${(V.bodyAABB(V.player).y-youAabb0.y).toFixed(2)}`);
V.keys.clear();

const throwCrumple = landCloseThrow();
check("throw knockdown still not KO crumple leftover", throwCrumple && V.rival.hp===80 && V.rival.thrownT>0 && !V.rival.falling && V.crumpleFade(V.rival)===0, `ok=${throwCrumple} hp=${V.rival.hp} th=${V.rival.thrownT} cf=${V.crumpleFade(V.rival)}`);

check("cache v=296 after KO crumple leftover live", /game.js\?v=350/.test(htmlTxt));


check("wakeup destRect leftover eases breath with wakeupFade", /Wakeup destRect leftover/.test(srcTxt) && /leftover idle breath used to dump/.test(srcTxt) && /hopped destRect while wakeupFade/.test(srcTxt) && /idleBreath eases with wakeupFade/.test(srcTxt) && /throw-invuln armed/.test(srcTxt));
check("wakeup destRect leftover is destRect-only ease", /destRect-only/.test(srcTxt) && /wakeupFade\(f\) > 0/.test(sliceFn(codeOnly, "idleBreath")) && (/1 - wakeupFade\(f\)/.test(sliceFn(codeOnly, "idleBreath")) || /1 - Math\.max\(wakeupFade\(f\), hurtFade\(f\)\)/.test(sliceFn(codeOnly, "idleBreath"))) && /AABB planted/.test(srcTxt) && /poseBitmap still idle/.test(srcTxt) && !/wakeupFade/.test(sliceFn(codeOnly, "destRect")) && !/wakeupFade/.test(sliceFn(codeOnly, "bodyAABB")) && !/wakeupFade/.test(sliceFn(codeOnly, "slashPose")) && !/wakeupFade/.test(sliceFn(codeOnly, "landThrow")) && !/wakeupFade\(/.test(sliceFn(codeOnly, "tickThrowState")));
check("wakeup destRect leftover poseBitmap still idle", /throwInvulnT/.test(sliceFn(codeOnly, "wakeupFade")) && !/throwInvulnT/.test(sliceFn(codeOnly, "poseBitmap")) && /f\.thrownT > 0/.test(sliceFn(codeOnly, "poseBitmap")));
check("wakeup destRect leftover extra rot stays 0", /Extra destRect rot stays 0/.test(srcTxt) && /Chip stun \/ throw KD unchanged/.test(srcTxt));
check("wakeup destRect leftover is not a new combat verb", V.THROW_WAKE_INVULN===80 && V.THROW_WAKE_INVULN===V.THROW_STARTUP && V.THROW_KD_MS===520 && V.HITSTUN===350 && V.GUARD_RAISE_MS===80);
check("slash frames still 180/140/280 after wakeup destRect leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after wakeup destRect leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after wakeup destRect leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after wakeup destRect leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after wakeup destRect leftover", /game.js\?v=350/.test(htmlTxt));

function holdWakeDest(){
  holdWakeSample();
  freezeYouAI();
  V.rival.walkSettleT = 0;
  V.rival.walkSettleOy = 0;
  V.rival.walkSettleRot = 0;
  V.rival.walkRiseT = 0;
  V.rival.walkLeanGait = 0;
  V.rival.guardPoseK = 0;
  V.rival.sheatheT = 0;
  V.rival.gait = 0;
  V.rival.walkT = 0;
}
const wakeDestLand = landCloseThrow();
check("wakeup destRect leftover setup throw landed", wakeDestLand && V.rival.hp===80 && V.rival.thrownT>0, `hit=${wakeDestLand} hp=${V.rival.hp} th=${V.rival.thrownT}`);
let wdPre=null, wdAabbPre=null, wdSaw=false, wdDest0=null, wdAabb0=null, wdFade0=-1, wdInv0=-1, wdDyDump=999;
for(let i=0;i<80;i++){
  holdWakeDest();
  if(V.rival.thrownT>0 && V.rival.thrownT<=STEP*2){
    wdPre=V.destRect(V.rival);
    wdAabbPre=V.bodyAABB(V.rival);
  }
  V.update(STEP);
  if(V.rival.thrownT<=0 && V.rival.throwInvulnT>0 && V.rival.hp>0){
    wdSaw=true;
    wdDest0=V.destRect(V.rival);
    wdAabb0=V.bodyAABB(V.rival);
    wdFade0=V.wakeupFade(V.rival);
    wdInv0=V.rival.throwInvulnT;
    if(wdPre) wdDyDump=Math.abs(wdDest0.dy-wdPre.dy);
    break;
  }
}
check("wakeup destRect leftover getup ready", wdSaw && wdFade0>0.85 && wdInv0>60 && !isHurtSheet(V.rival) && V.rival.stunT===0, `saw=${wdSaw} fade=${wdFade0} inv=${wdInv0} st=${V.rival.stunT} src=${sheetSrc(V.rival)}`);
check("wakeup destRect leftover no breath dump at arm", wdSaw && wdPre && wdDyDump<0.35 && Math.abs(wdDest0.rot-wdPre.rot)<0.001, `ddy=${wdDyDump.toFixed(3)} fade=${wdFade0} rot=${wdDest0&&wdDest0.rot} dy0=${wdDest0&&wdDest0.dy} dyPre=${wdPre&&wdPre.dy}`);
check("wakeup destRect leftover AABB planted at arm", wdSaw && wdAabbPre && Math.abs(wdAabb0.y-wdAabbPre.y)<0.05 && Math.abs(wdAabb0.h-wdAabbPre.h)<0.05, `dy=${wdAabbPre?(wdAabb0.y-wdAabbPre.y).toFixed(2):"na"}`);
check("wakeup destRect leftover poseBitmap idle immediately", wdSaw && !isHurtSheet(V.rival), `src=${sheetSrc(V.rival)}`);
let wdFadeMid=-1, wdDestMid=null, wdAabbMid=null;
for(let i=0;i<3;i++){
  holdWakeDest();
  V.update(STEP);
  wdFadeMid=V.wakeupFade(V.rival);
  wdDestMid=V.destRect(V.rival);
  wdAabbMid=V.bodyAABB(V.rival);
}
check("wakeup destRect leftover fade drops", wdSaw && wdFadeMid>=0 && wdFadeMid<wdFade0-0.08 && V.rival.throwInvulnT>0 && V.rival.thrownT===0, `mid=${wdFadeMid.toFixed(3)} t0=${wdFade0.toFixed(3)} inv=${V.rival.throwInvulnT}`);
check("wakeup destRect leftover destRect eases breath", wdSaw && wdDestMid && wdPre && Math.abs(wdDestMid.dy-wdPre.dy)<2.2 && Math.abs(wdDestMid.rot-wdPre.rot)<0.001, `ddy=${wdPre&&wdDestMid?(wdDestMid.dy-wdPre.dy).toFixed(2):"na"} rot=${wdDestMid&&wdDestMid.rot}`);
check("wakeup destRect leftover AABB planted through ease", wdSaw && wdAabbPre && wdAabbMid && Math.abs(wdAabbMid.y-wdAabbPre.y)<0.05 && Math.abs(wdAabbMid.h-wdAabbPre.h)<0.05, `dy=${wdAabbPre?(wdAabbMid.y-wdAabbPre.y).toFixed(2):"na"}`);
for(let i=0;i<20;i++){
  holdWakeDest();
  V.update(STEP);
  if(V.rival.throwInvulnT===0) break;
}
check("wakeup destRect leftover fade gone", V.rival.throwInvulnT===0 && V.wakeupFade(V.rival)===0 && !isHurtSheet(V.rival), `inv=${V.rival.throwInvulnT} fade=${V.wakeupFade(V.rival)} src=${sheetSrc(V.rival)}`);
holdWakeDest();
check("wakeup destRect leftover extra rot still 0", Math.abs(V.destRect(V.rival).rot)<0.001, `rot=${V.destRect(V.rival).rot}`);

toClose();
freezeYouAI();
freezeRivalAI();
V.player.stunT = V.HITSTUN;
V.player.thrownT = 0;
V.player.throwInvulnT = 0;
V.player.hp = 80;
check("chip stun is not wakeup destRect leftover", V.wakeupFade(V.player)===0 && V.player.stunT>0 && !isHurtSheet(V.player), `fade=${V.wakeupFade(V.player)} st=${V.player.stunT} src=${sheetSrc(V.player)}`);

const throwKdStill = landCloseThrow();
check("throw knockdown still not KO crumple after wakeup destRect leftover", throwKdStill && V.rival.hp===80 && V.rival.thrownT>0 && !V.rival.falling && V.crumpleFade(V.rival)===0, `ok=${throwKdStill} hp=${V.rival.hp} th=${V.rival.thrownT} cf=${V.crumpleFade(V.rival)}`);
check("cache v=296 after wakeup destRect leftover live", /game.js\?v=350/.test(htmlTxt));


check("throw recovery leftover fades windup to idle", /function throwPlantFade/.test(srcTxt) && /throwPlantFade\(f\)/.test(srcTxt) && /Throw recovery leftover/.test(srcTxt) && /leftover windup used to pop/.test(srcTxt) && /wasThrow skips sheatheT/.test(srcTxt) && /grab pose dumped/.test(srcTxt));
check("throw recovery leftover is draw-only sheet", /Visual only \(drawKnight\)/.test(srcTxt) && /throwPlantFade\(f\)/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && /poseBitmap still holds windup while planted/.test(srcTxt) && !/throwPlantFade/.test(sliceFn(codeOnly, "destRect")) && !/throwPlantFade/.test(sliceFn(codeOnly, "bodyAABB")) && !/throwPlantFade/.test(sliceFn(codeOnly, "slashPose")) && !/throwPlantFade/.test(sliceFn(codeOnly, "landThrow")) && !/throwPlantFade/.test(sliceFn(codeOnly, "landHit")));
check("throw recovery leftover destRect breath ease", /f\.cut === "throw" && f\.phase === "recovery"/.test(sliceFn(codeOnly, "idleBreath")) && /1 - throwPlantFade\(f\)/.test(sliceFn(codeOnly, "idleBreath")) && /leftover breath used to dump/.test(srcTxt) && /Ease with throwPlantFade/.test(srcTxt));
check("throw recovery leftover uses throw recovery", /cutRecovery\(f\)/.test(sliceFn(codeOnly, "throwPlantFade")) && /phase !== "recovery"/.test(sliceFn(codeOnly, "throwPlantFade")) && /restThrow/.test(sliceFn(codeOnly, "drawKnight")) && /throwPlantFade\(f\)/.test(sliceFn(codeOnly, "drawKnight")));
check("throw recovery leftover poseBitmap still windup", /cut === "throw"/.test(sliceFn(codeOnly, "poseBitmap")) && /phase === "recovery"/.test(sliceFn(codeOnly, "poseBitmap")) && /cut !== "throw"/.test(sliceFn(codeOnly, "throwPlantFade")));
check("throw recovery leftover extra rot stays 0", /Extra destRect rot stays 0/.test(srcTxt) && /Tech same path/.test(srcTxt) && /Clash\/slash still sheathe from slash/.test(srcTxt));
check("slash golpe still sheathe after throw recovery leftover", /f\.cut === "golpe" \|\| f\.cut === "slash"/.test(sliceFn(codeOnly, "sheatheFade")) && /wasThrow/.test(sliceFn(codeOnly, "advanceAttack")));
check("throw recovery leftover is not a new combat verb", V.THROW_STARTUP===80 && V.THROW_ACTIVE===40 && V.THROW_RECOVERY===220 && V.THROW_TECH_REC===160 && V.THROW_KD_MS===520 && V.THROW_WAKE_INVULN===80 && V.HITSTUN===350 && V.GUARD_RAISE_MS===80);
check("slash frames still 180/140/280 after throw recovery leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after throw recovery leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after throw recovery leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after throw recovery leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after throw recovery leftover", /game.js\?v=350/.test(htmlTxt));

const throwFadeLand = landCloseThrow();
check("throw recovery leftover setup throw landed", throwFadeLand && V.rival.hp===80 && V.rival.thrownT>0 && V.player.cut==="throw" && (V.player.phase==="recovery" || V.player.phase==="idle"), `hit=${throwFadeLand} hp=${V.rival.hp} th=${V.rival.thrownT} cut=${V.player.cut} ph=${V.player.phase}`);
let tfSaw=false, tfFade0=-1, tfDest0=null, tfAabb0=null, tfSrc0="";
if(V.player.cut==="throw" && V.player.phase==="recovery"){
  tfSaw=true;
  tfFade0=V.throwPlantFade(V.player);
  tfDest0=V.destRect(V.player);
  tfAabb0=V.bodyAABB(V.player);
  tfSrc0=sheetSrc(V.player);
}else{
  for(let i=0;i<30;i++){
    holdRivalNoGrab();
    V.update(STEP);
    if(V.player.cut==="throw" && V.player.phase==="recovery"){
      tfSaw=true;
      tfFade0=V.throwPlantFade(V.player);
      tfDest0=V.destRect(V.player);
      tfAabb0=V.bodyAABB(V.player);
      tfSrc0=sheetSrc(V.player);
      break;
    }
  }
}
check("throw recovery leftover fade starts high", tfSaw && tfFade0>0.85 && /windup/.test(tfSrc0) && V.player.cut==="throw" && V.player.phase==="recovery", `saw=${tfSaw} fade=${tfFade0} src=${tfSrc0} cut=${V.player.cut} ph=${V.player.phase}`);
check("throw recovery leftover poseBitmap windup immediately", tfSaw && /windup/.test(tfSrc0) && !isCutSheet(V.player), `src=${tfSrc0}`);
check("throw recovery leftover extra rot 0 at arm", tfSaw && Math.abs(tfDest0.rot)<0.001, `rot=${tfDest0&&tfDest0.rot}`);
let tfFadeMid=-1, tfDestMid=null, tfAabbMid=null;
for(let i=0;i<6;i++){
  holdRivalNoGrab();
  V.update(STEP);
  if(V.player.cut==="throw" && V.player.phase==="recovery"){
    tfFadeMid=V.throwPlantFade(V.player);
    tfDestMid=V.destRect(V.player);
    tfAabbMid=V.bodyAABB(V.player);
  }
}
check("throw recovery leftover fade drops", tfSaw && tfFadeMid>=0 && tfFadeMid<tfFade0-0.08 && V.player.cut==="throw" && V.player.phase==="recovery" && /windup/.test(sheetSrc(V.player)), `mid=${tfFadeMid} t0=${tfFade0} ph=${V.player.phase} src=${sheetSrc(V.player)}`);
check("throw recovery leftover destRect eases breath", tfSaw && tfDestMid && tfDest0 && Math.abs(tfDestMid.dy-tfDest0.dy)<2.2 && Math.abs(tfDestMid.rot)<0.001, `ddy=${tfDest0&&tfDestMid?(tfDestMid.dy-tfDest0.dy).toFixed(2):"na"} rot=${tfDestMid&&tfDestMid.rot}`);
check("throw recovery leftover AABB planted through ease", tfSaw && tfAabb0 && tfAabbMid && Math.abs(tfAabbMid.y-tfAabb0.y)<0.05 && Math.abs(tfAabbMid.h-tfAabb0.h)<0.05, `dy=${tfAabb0?(tfAabbMid.y-tfAabb0.y).toFixed(2):"na"}`);
let tfPre=null, tfAabbPre=null, tfIdle=false, tfFadeIdle=-1, tfDestIdle=null, tfAabbIdle=null, tfDump=999;
for(let i=0;i<30;i++){
  holdRivalNoGrab();
  if(V.player.cut==="throw" && V.player.phase==="recovery"){
    tfPre=V.destRect(V.player);
    tfAabbPre=V.bodyAABB(V.player);
  }
  V.update(STEP);
  if(V.player.phase==="idle" && V.player.cut==="slash"){
    tfIdle=true;
    tfFadeIdle=V.throwPlantFade(V.player);
    tfDestIdle=V.destRect(V.player);
    tfAabbIdle=V.bodyAABB(V.player);
    if(tfPre) tfDump=Math.abs(tfDestIdle.dy-tfPre.dy);
    break;
  }
}
check("throw recovery leftover idle fade gone", tfIdle && tfFadeIdle===0 && !/windup/.test(sheetSrc(V.player)) && !isCutSheet(V.player), `idle=${tfIdle} fade=${tfFadeIdle} src=${sheetSrc(V.player)} cut=${V.player.cut} ph=${V.player.phase}`);
check("throw recovery leftover no breath dump at idle", tfIdle && tfPre && tfDump<0.35 && Math.abs(tfDestIdle.rot)<0.001, `ddy=${tfDump.toFixed(3)} rot=${tfDestIdle&&tfDestIdle.rot} dy0=${tfDestIdle&&tfDestIdle.dy} dyPre=${tfPre&&tfPre.dy}`);
check("throw recovery leftover AABB planted at idle", tfIdle && tfAabbPre && Math.abs(tfAabbIdle.y-tfAabbPre.y)<0.05 && Math.abs(tfAabbIdle.h-tfAabbPre.h)<0.05, `dy=${tfAabbPre?(tfAabbIdle.y-tfAabbPre.y).toFixed(2):"na"}`);

toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.guarding=false;
V.player.stunT=0;
V.player.sheatheT=0;
V.keys.clear();
V.attackEdge=true;
V.update(STEP);
for(let i=0;i<40;i++){
  holdRivalStill();
  V.rival.guarding=false;
  V.update(STEP);
  if(V.player.phase==="recovery" && V.player.cut==="slash") break;
}
check("slash recovery is not throw recovery leftover", V.player.cut==="slash" && V.player.phase==="recovery" && V.throwPlantFade(V.player)===0 && V.sheatheFade(V.player)>0, `cut=${V.player.cut} ph=${V.player.phase} tf=${V.throwPlantFade(V.player)} sf=${V.sheatheFade(V.player)}`);

toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.guarding=false;
V.keys.clear();
V.golpeEdge=true;
V.update(STEP);
for(let i=0;i<30;i++){
  holdRivalStill();
  V.rival.guarding=false;
  V.update(STEP);
  if(V.player.phase==="recovery" && V.player.cut==="golpe") break;
}
check("golpe recovery is not throw recovery leftover", V.player.cut==="golpe" && V.player.phase==="recovery" && V.throwPlantFade(V.player)===0 && V.sheatheFade(V.player)>0, `cut=${V.player.cut} ph=${V.player.phase} tf=${V.throwPlantFade(V.player)} sf=${V.sheatheFade(V.player)}`);

toClose();
freezeYouAI();
freezeRivalAI();
V.player.stunT=V.HITSTUN;
V.player.thrownT=0;
V.player.cut="slash";
V.player.phase="idle";
check("chip stun is not throw recovery leftover", V.throwPlantFade(V.player)===0 && V.player.stunT>0 && !isHurtSheet(V.player), `fade=${V.throwPlantFade(V.player)} st=${V.player.stunT} src=${sheetSrc(V.player)}`);

toClose();
freezeYouAI();
freezeRivalAI();
Math.random = () => 0;
V.rival.stamina=80;
V.rival.stamRegenT=99999;
V.rival.wantBlock=true;
V.rival.guarding=true;
V.rival.guardChoice=1;
V.player.stamina=100;
fireThrow();
let techFadeOk=false;
for(let i=0;i<20;i++){
  holdRivalNoGrab();
  V.rival.wantBlock=true;
  V.rival.guarding=true;
  V.update(STEP);
  if(V.player.cut==="throw" && V.player.phase==="startup"){
    V.keys.add("KeyS");
    V.keys.add("Space");
    const tok=V.tryThrowTech(V.rival);
    if(tok && V.player.techRec && V.rival.techRec){
      techFadeOk=true;
      break;
    }
  }
}
check("throw recovery leftover tech fade starts high", techFadeOk && V.player.phase==="recovery" && V.rival.phase==="recovery" && V.throwPlantFade(V.player)>0.85 && V.throwPlantFade(V.rival)>0.85 && /windup/.test(sheetSrc(V.player)) && /windup/.test(sheetSrc(V.rival)), `ok=${techFadeOk} pph=${V.player.phase} rph=${V.rival.phase} pf=${V.throwPlantFade(V.player)} rf=${V.throwPlantFade(V.rival)} psrc=${sheetSrc(V.player)} rsrc=${sheetSrc(V.rival)}`);
const techAabb0=V.bodyAABB(V.player);
const techDest0=V.destRect(V.player);
for(let i=0;i<4;i++){
  holdRivalNoGrab();
  V.update(STEP);
}
check("throw recovery leftover tech fade drops", techFadeOk && V.player.techRec && V.throwPlantFade(V.player)>0 && V.throwPlantFade(V.player)<0.98 && /windup/.test(sheetSrc(V.player)), `ok=${techFadeOk} fade=${V.throwPlantFade(V.player)} rec=${V.player.techRec} src=${sheetSrc(V.player)}`);
check("throw recovery leftover tech AABB planted", techFadeOk && Math.abs(V.bodyAABB(V.player).y-techAabb0.y)<0.05 && Math.abs(V.destRect(V.player).rot)<0.001, `dy=${(V.bodyAABB(V.player).y-techAabb0.y).toFixed(2)} rot=${V.destRect(V.player).rot}`);
check("cache v=296 after throw recovery leftover live", /game.js\?v=350/.test(htmlTxt));


check("dart recovery destRect leftover eases breath with boltPlantFade", /Dart recovery destRect leftover/.test(srcTxt) && /leftover idle breath used to dump/.test(srcTxt) && /dart birth popped boltPhase to recovery/.test(srcTxt) && /telegraphFade died/.test(srcTxt) && /idleBreath eases with boltPlantFade/.test(srcTxt));
check("dart recovery destRect leftover is destRect-only ease", /destRect-only/.test(srcTxt) && /f\.boltPhase === "recovery" && !holdingCutBolt\(f\)/.test(sliceFn(codeOnly, "idleBreath")) && /1 - boltPlantFade\(f\)/.test(sliceFn(codeOnly, "idleBreath")) && /AABB planted/.test(srcTxt) && /poseBitmap still idle once bolt recovery starts/.test(srcTxt) && !/boltPlantFade/.test(sliceFn(codeOnly, "destRect")) && !/boltPlantFade/.test(sliceFn(codeOnly, "bodyAABB")) && !/boltPlantFade/.test(sliceFn(codeOnly, "slashPose")) && !/boltPlantFade/.test(sliceFn(codeOnly, "startBolt")) && !/boltPlantFade/.test(sliceFn(codeOnly, "landHit")));
check("dart recovery destRect leftover poseBitmap still idle", /boltPhase === "startup"/.test(sliceFn(codeOnly, "poseBitmap")) && !/boltPhase === "recovery"/.test(sliceFn(codeOnly, "poseBitmap")) && /boltPhase !== "recovery"/.test(sliceFn(codeOnly, "boltPlantFade")));
check("dart recovery destRect leftover extra rot stays 0", /Extra destRect rot stays 0/.test(srcTxt) && /Special-cancel still 0/.test(srcTxt) && /Chip stun \/ throw KD unchanged/.test(srcTxt));
check("dart recovery destRect leftover is not a new combat verb", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60 && V.GUARD_RAISE_MS===80 && V.HITSTUN===350);
check("slash frames still 180/140/280 after dart recovery destRect leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after dart recovery destRect leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after dart recovery destRect leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after dart recovery destRect leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after dart recovery destRect leftover", /game.js\?v=350/.test(htmlTxt));

function holdDartDest(f){
  const u = f || V.player;
  const you = u === V.player;
  Math.random = () => 0.99;
  u.guarding = false;
  u.stunT = 0;
  u.falling = false;
  u.sheatheT = 0;
  u.walkSettleT = 0;
  u.walkSettleOy = 0;
  u.walkSettleRot = 0;
  u.walkRiseT = 0;
  u.walkLeanGait = 0;
  u.guardPoseK = 0;
  u.gait = 0;
  u.walkT = 0;
  u.feintT = 0;
  V.hitstopLeft = 0;
  V.keys.clear();
  if(you){
    V.player.boltHoldCut = false;
    V.rival.phase = "idle";
    V.rival.phaseT = 0;
    V.rival.boltPhase = "";
    V.rival.boltT = 0;
    V.rival.boltCd = 99999;
    V.rival.throwCd = 99999;
    V.rival.stunT = 0;
    V.rival.guarding = false;
    V.rival.wantBlock = false;
    V.rival.falling = false;
    V.rival.gait = 0;
    V.rival.x = 1220;
  }else{
    V.rival.boltHoldCut = false;
    V.player.phase = "idle";
    V.player.phaseT = 0;
    V.player.boltPhase = "";
    V.player.boltT = 0;
    V.player.stunT = 0;
    V.player.guarding = false;
    V.player.falling = false;
    V.player.gait = 0;
    V.player.x = 1250;
  }
}

bootPlay(); wait(1700);
holdDartDest(V.player);
V.player.stamina = 100;
V.player.stamRegenT = 99999;
V.player.hp = 100;
const dartYouOk = V.startBolt(V.player, false);
check("dart recovery destRect leftover setup idle K", dartYouOk && V.player.boltPhase==="startup" && V.boltPlantFade(V.player)===0, `ok=${dartYouOk} bph=${V.player.boltPhase} fade=${V.boltPlantFade(V.player)}`);
let ddPre=null, ddAabbPre=null, ddSaw=false, ddDest0=null, ddAabb0=null, ddFade0=-1, ddDyDump=999, ddSrc0="";
for(let i=0;i<20;i++){
  holdDartDest(V.player);
  V.player.boltHoldCut = false;
  if(V.player.boltPhase==="startup" && V.player.boltT>=V.BOLT_STARTUP-STEP*2){
    ddPre=V.destRect(V.player);
    ddAabbPre=V.bodyAABB(V.player);
  }
  V.update(STEP);
  if(V.player.boltPhase==="recovery"){
    ddSaw=true;
    ddDest0=V.destRect(V.player);
    ddAabb0=V.bodyAABB(V.player);
    ddFade0=V.boltPlantFade(V.player);
    ddSrc0=sheetSrc(V.player);
    if(ddPre) ddDyDump=Math.abs(ddDest0.dy-ddPre.dy);
    break;
  }
}
check("dart recovery destRect leftover birth ready", ddSaw && ddFade0>0.85 && !/windup/.test(ddSrc0) && !isCutSheet(V.player) && V.player.boltHoldCut===false, `saw=${ddSaw} fade=${ddFade0} src=${ddSrc0} hold=${V.player.boltHoldCut}`);
check("dart recovery destRect leftover no breath dump at birth", ddSaw && ddPre && ddDyDump<0.35 && Math.abs(ddDest0.rot-ddPre.rot)<0.001, `ddy=${ddDyDump.toFixed(3)} fade=${ddFade0} rot=${ddDest0&&ddDest0.rot} dy0=${ddDest0&&ddDest0.dy} dyPre=${ddPre&&ddPre.dy}`);
check("dart recovery destRect leftover AABB planted at birth", ddSaw && ddAabbPre && Math.abs(ddAabb0.y-ddAabbPre.y)<0.05 && Math.abs(ddAabb0.h-ddAabbPre.h)<0.05, `dy=${ddAabbPre?(ddAabb0.y-ddAabbPre.y).toFixed(2):"na"}`);
check("dart recovery destRect leftover poseBitmap idle immediately", ddSaw && !/windup/.test(ddSrc0) && !isCutSheet(V.player), `src=${ddSrc0}`);
check("dart recovery destRect leftover extra rot 0 at birth", ddSaw && Math.abs(ddDest0.rot)<0.001, `rot=${ddDest0&&ddDest0.rot}`);
let ddFadeMid=-1, ddDestMid=null, ddAabbMid=null;
for(let i=0;i<6;i++){
  holdDartDest(V.player);
  V.player.boltHoldCut = false;
  V.update(STEP);
  if(V.player.boltPhase==="recovery"){
    ddFadeMid=V.boltPlantFade(V.player);
    ddDestMid=V.destRect(V.player);
    ddAabbMid=V.bodyAABB(V.player);
  }
}
check("dart recovery destRect leftover fade drops", ddSaw && ddFadeMid>=0 && ddFadeMid<ddFade0-0.08 && V.player.boltPhase==="recovery" && !/windup/.test(sheetSrc(V.player)), `mid=${ddFadeMid} t0=${ddFade0} bph=${V.player.boltPhase} src=${sheetSrc(V.player)}`);
check("dart recovery destRect leftover destRect eases breath", ddSaw && ddDestMid && ddPre && Math.abs(ddDestMid.dy-ddPre.dy)<2.2 && Math.abs(ddDestMid.rot)<0.001, `ddy=${ddPre&&ddDestMid?(ddDestMid.dy-ddPre.dy).toFixed(2):"na"} rot=${ddDestMid&&ddDestMid.rot}`);
check("dart recovery destRect leftover AABB planted through ease", ddSaw && ddAabbPre && ddAabbMid && Math.abs(ddAabbMid.y-ddAabbPre.y)<0.05 && Math.abs(ddAabbMid.h-ddAabbPre.h)<0.05, `dy=${ddAabbPre?(ddAabbMid.y-ddAabbPre.y).toFixed(2):"na"}`);
let ddIdle=false, ddFadeIdle=-1, ddDestIdle=null, ddAabbIdle=null, ddPreEnd=null, ddDumpEnd=999;
for(let i=0;i<40;i++){
  holdDartDest(V.player);
  V.player.boltHoldCut = false;
  if(V.player.boltPhase==="recovery"){
    ddPreEnd=V.destRect(V.player);
  }
  V.update(STEP);
  if(!V.player.boltPhase){
    ddIdle=true;
    ddFadeIdle=V.boltPlantFade(V.player);
    ddDestIdle=V.destRect(V.player);
    ddAabbIdle=V.bodyAABB(V.player);
    if(ddPreEnd) ddDumpEnd=Math.abs(ddDestIdle.dy-ddPreEnd.dy);
    break;
  }
}
check("dart recovery destRect leftover idle fade gone", ddIdle && ddFadeIdle===0 && !/windup/.test(sheetSrc(V.player)) && !isCutSheet(V.player), `idle=${ddIdle} fade=${ddFadeIdle} src=${sheetSrc(V.player)} bph=${V.player.boltPhase}`);
check("dart recovery destRect leftover no breath dump at idle", ddIdle && ddPreEnd && ddDumpEnd<0.35 && Math.abs(ddDestIdle.rot)<0.001, `ddy=${ddDumpEnd.toFixed(3)} rot=${ddDestIdle&&ddDestIdle.rot}`);
check("dart recovery destRect leftover AABB planted at idle", ddIdle && ddAabbPre && Math.abs(ddAabbIdle.y-ddAabbPre.y)<0.05, `dy=${ddAabbPre?(ddAabbIdle.y-ddAabbPre.y).toFixed(2):"na"}`);

bootPlay(); wait(1700);
holdDartDest(V.rival);
V.rival.stamina = 100;
V.rival.stamRegenT = 99999;
V.rival.hp = 100;
V.rival.x = 500;
V.player.x = 1250;
const dartRivalOk = V.startBolt(V.rival, false);
check("dart recovery destRect leftover rival setup", dartRivalOk && V.rival.boltPhase==="startup", `ok=${dartRivalOk} bph=${V.rival.boltPhase}`);
let rdPre=null, rdSaw=false, rdDest0=null, rdAabbPre=null, rdAabb0=null, rdFade0=-1, rdDump=999;
for(let i=0;i<20;i++){
  holdDartDest(V.rival);
  V.rival.boltHoldCut = false;
  if(V.rival.boltPhase==="startup" && V.rival.boltT>=V.BOLT_STARTUP-STEP*2){
    rdPre=V.destRect(V.rival);
    rdAabbPre=V.bodyAABB(V.rival);
  }
  V.update(STEP);
  if(V.rival.boltPhase==="recovery"){
    rdSaw=true;
    rdDest0=V.destRect(V.rival);
    rdAabb0=V.bodyAABB(V.rival);
    rdFade0=V.boltPlantFade(V.rival);
    if(rdPre) rdDump=Math.abs(rdDest0.dy-rdPre.dy);
    break;
  }
}
check("dart recovery destRect leftover rival no breath dump at birth", rdSaw && rdPre && rdDump<0.35 && rdFade0>0.85 && !/windup/.test(sheetSrc(V.rival)) && Math.abs(rdDest0.rot)<0.001, `saw=${rdSaw} ddy=${rdDump.toFixed(3)} fade=${rdFade0} src=${sheetSrc(V.rival)} rot=${rdDest0&&rdDest0.rot}`);
check("dart recovery destRect leftover rival AABB planted at birth", rdSaw && rdAabbPre && Math.abs(rdAabb0.y-rdAabbPre.y)<0.05, `dy=${rdAabbPre?(rdAabb0.y-rdAabbPre.y).toFixed(2):"na"}`);

bootPlay(); wait(1700);
holdDartDest(V.player);
V.player.stamina = 100;
V.player.stamRegenT = 99999;
V.player.phase = "idle";
V.player.phaseT = 0;
V.player.cut = "slash";
V.player.cutHit = true;
V.player.boltHoldCut = true;
const scDestOk = V.startBolt(V.player, true);
check("dart recovery destRect leftover special-cancel setup", scDestOk && V.player.boltHoldCut && V.player.boltPhase==="startup", `ok=${scDestOk} hold=${V.player.boltHoldCut} bph=${V.player.boltPhase}`);
let scPre=null, scSaw=false, scDest0=null, scDump=999, scFade= -1;
for(let i=0;i<20;i++){
  holdDartDest(V.player);
  V.player.boltHoldCut = true;
  if(V.player.boltPhase==="startup" && V.player.boltT>=V.BOLT_STARTUP-STEP*2){
    scPre=V.destRect(V.player);
  }
  V.update(STEP);
  if(V.player.boltPhase==="recovery"){
    scSaw=true;
    scDest0=V.destRect(V.player);
    scFade=V.boltPlantFade(V.player);
    if(scPre) scDump=Math.abs(scDest0.dy-scPre.dy);
    break;
  }
}
check("dart recovery destRect leftover special-cancel still 0 fade", scSaw && V.player.boltHoldCut && scFade===0 && isCutSheet(V.player) && scPre && scDump<0.35, `saw=${scSaw} hold=${V.player.boltHoldCut} fade=${scFade} ddy=${scDump.toFixed(3)} src=${sheetSrc(V.player)}`);

toClose();
freezeYouAI();
freezeRivalAI();
V.player.stunT = V.HITSTUN;
V.player.thrownT = 0;
V.player.hp = 80;
check("chip stun is not dart recovery destRect leftover", V.boltPlantFade(V.player)===0 && V.player.stunT>0 && !isHurtSheet(V.player), `fade=${V.boltPlantFade(V.player)} st=${V.player.stunT} src=${sheetSrc(V.player)}`);

const dartThrowKd = landCloseThrow();
check("throw knockdown still not KO crumple after dart recovery destRect leftover", dartThrowKd && V.rival.hp===80 && V.rival.thrownT>0 && !V.rival.falling && V.crumpleFade(V.rival)===0, `ok=${dartThrowKd} hp=${V.rival.hp} th=${V.rival.thrownT} cf=${V.crumpleFade(V.rival)}`);
check("cache v=296 after dart recovery destRect leftover live", /game.js\?v=350/.test(htmlTxt));


check("guard drop leftover fades block to idle", /function guardDropFade/.test(srcTxt) && /guardDropFade\(f\)/.test(srcTxt) && /Guard drop leftover/.test(srcTxt) && /leftover block used to pop/.test(srcTxt) && /sheet cut, not a release/.test(srcTxt) && /S released/.test(srcTxt));
check("guard drop leftover is draw-only sheet", /Draw-only \(guardDropFade\)/.test(srcTxt) && /guardDropFade\(f\)/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && /poseBitmap still idle once/.test(srcTxt) && /restGuard/.test(sliceFn(codeOnly, "drawKnight")) && !/guardDropFade/.test(sliceFn(codeOnly, "destRect")) && !/guardDropFade/.test(sliceFn(codeOnly, "bodyAABB")) && !/guardDropFade/.test(sliceFn(codeOnly, "slashPose")) && !/guardDropFade/.test(sliceFn(codeOnly, "landHit")) && !/guardDropFade/.test(sliceFn(codeOnly, "startReversal")));
check("guard drop leftover uses leftover raise k", /guardRaiseK\(f\)/.test(sliceFn(codeOnly, "guardDropFade")) && /guarding/.test(sliceFn(codeOnly, "guardDropFade")) && /restGuard/.test(sliceFn(codeOnly, "drawKnight")) && /blockBitmap\(f\)/.test(sliceFn(codeOnly, "drawKnight")));
check("guard drop leftover poseBitmap still idle", /f\.guarding && ready\(block\)/.test(sliceFn(codeOnly, "poseBitmap")) && !/guardPoseK/.test(sliceFn(codeOnly, "poseBitmap")) && !/guardDropFade/.test(sliceFn(codeOnly, "poseBitmap")));
check("guard drop leftover skips sheathe overlay", /guardDropFade\(f\) > 0/.test(sliceFn(codeOnly, "sheatheFade")) && /Leftover block owns the sheet/.test(srcTxt));
check("guard drop leftover extra rot stays 0", /Extra destRect rot stays 0/.test(srcTxt) && /Guard break still snaps/.test(srcTxt) && /Combat poses still snap leftover k/.test(srcTxt) && /Raise still 0ms/.test(srcTxt));
check("guard drop leftover is not a new combat verb", V.GUARD_RAISE_MS===80 && V.GUARD_COMMIT_MS===140 && V.STAMINA_BLOCK===20 && V.GUARD_BREAK_MS===400 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80);
check("slash frames still 180/140/280 after guard drop leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after guard drop leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after guard drop leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after guard drop leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after guard drop leftover", /game.js\?v=350/.test(htmlTxt));

function isBlockSheet(f){ return /block/.test(sheetSrc(f)); }

bootPlay(); wait(1700);
holdGuardSample();
V.player.guarding = false;
V.player.guardPoseK = 0;
V.update(STEP);
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
check("guard drop leftover raise still 0ms no drop fade", V.player.guarding && V.player.guardPoseK>0.8 && V.guardDropFade(V.player)===0 && isBlockSheet(V.player), `g=${V.player.guarding} k=${V.player.guardPoseK.toFixed(3)} fade=${V.guardDropFade(V.player)} src=${sheetSrc(V.player)}`);
const gdAabbHold = V.bodyAABB(V.player);
const gdDestHold = V.destRect(V.player);
const gdKHold = V.player.guardPoseK;
holdGuardSample();
V.keys.clear();
V.player.guarding = false;
V.update(STEP);
const gdFade0 = V.guardDropFade(V.player);
const gdDest0 = V.destRect(V.player);
const gdAabb0 = V.bodyAABB(V.player);
check("guard drop leftover fade starts high", gdFade0>0.8 && V.player.guarding===false && V.player.guardPoseK<gdKHold && V.player.guardPoseK>0.5, `fade=${gdFade0} k=${V.player.guardPoseK.toFixed(3)} k0=${gdKHold} g=${V.player.guarding}`);
check("guard drop leftover poseBitmap idle immediately", !isBlockSheet(V.player) && !isCutSheet(V.player) && !/windup/.test(sheetSrc(V.player)), `src=${sheetSrc(V.player)}`);
check("guard drop leftover destRect eases not snaps", Math.abs(gdDest0.dy-gdDestHold.dy)<1.6 && Math.abs(gdDest0.dy-gdDestHold.dy)>=0, `ddy=${(gdDest0.dy-gdDestHold.dy).toFixed(2)} rot=${gdDest0.rot.toFixed(3)}`);
check("guard drop leftover AABB planted at drop", Math.abs(gdAabb0.y-gdAabbHold.y)<0.05 && Math.abs(gdAabb0.h-gdAabbHold.h)<0.05, `dy=${(gdAabb0.y-gdAabbHold.y).toFixed(2)}`);
V.player.sheatheT = 140;
check("guard drop leftover skips sheathe overlay live", V.guardDropFade(V.player)>0 && V.sheatheFade(V.player)===0, `gf=${V.guardDropFade(V.player)} sf=${V.sheatheFade(V.player)} sh=${V.player.sheatheT}`);
V.player.sheatheT = 0;
let gdFadeMid=-1, gdDestMid=null, gdAabbMid=null;
for(let i=0;i<3;i++){
  freezeRivalAI(); freezeYouAI();
  V.keys.clear();
  V.player.stunT=0; V.player.falling=false; V.player.phase="idle"; V.player.boltPhase=""; V.player.guardBreakT=0; V.hitstopLeft=0;
  V.update(STEP);
  gdFadeMid=V.guardDropFade(V.player);
  gdDestMid=V.destRect(V.player);
  gdAabbMid=V.bodyAABB(V.player);
}
check("guard drop leftover fade drops", gdFadeMid>=0 && gdFadeMid<gdFade0-0.08 && V.player.guarding===false && V.player.guardPoseK>0, `mid=${gdFadeMid} t0=${gdFade0} k=${V.player.guardPoseK}`);
check("guard drop leftover destRect eases plant", gdDestMid && (gdDestHold.dy-gdDestMid.dy)>0.4 && (gdDestHold.dy-gdDestMid.dy)<8 && Math.abs(gdDestMid.rot)<0.12, `ddy=${(gdDestHold.dy-gdDestMid.dy).toFixed(2)} rot=${gdDestMid.rot.toFixed(3)}`);
check("guard drop leftover AABB planted through ease", gdAabbMid && Math.abs(gdAabbMid.y-gdAabbHold.y)<0.05 && Math.abs(gdAabbMid.h-gdAabbHold.h)<0.05, `dy=${(gdAabbMid.y-gdAabbHold.y).toFixed(2)}`);
for(let i=0;i<10;i++){
  freezeRivalAI(); freezeYouAI();
  V.keys.clear();
  V.player.stunT=0; V.player.falling=false; V.player.phase="idle"; V.player.boltPhase=""; V.player.guardBreakT=0; V.hitstopLeft=0;
  V.update(STEP);
}
check("guard drop leftover idle fade gone", V.player.guardPoseK===0 && V.guardDropFade(V.player)===0 && !V.player.guarding && !isBlockSheet(V.player), `k=${V.player.guardPoseK} fade=${V.guardDropFade(V.player)} src=${sheetSrc(V.player)}`);

bootPlay(); wait(1700);
holdGuardSample();
V.player.guarding = false;
V.player.guardPoseK = 0;
V.update(STEP);
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
V.player.stamina = 100;
const gdRevOk = V.startReversal(V.player);
check("guard drop leftover reversal plant eases leftover k", gdRevOk && V.player.cut==="golpe" && V.player.phase==="startup" && V.guardDropFade(V.player)===0 && V.reversalPlantFade(V.player)>0.8 && V.guardRaiseK(V.player)>0.8, `ok=${gdRevOk} cut=${V.player.cut} ph=${V.player.phase} gf=${V.guardDropFade(V.player)} rf=${V.reversalPlantFade(V.player)} gk=${V.guardRaiseK(V.player)}`);

bootPlay(); wait(1700);
holdGuardSample();
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
holdGuardSample();
V.keys.clear();
V.player.guarding = false;
V.update(STEP);
V.player.stunT = V.HITSTUN;
check("guard drop leftover stun leftover k eases", V.guardDropFade(V.player)===0 && V.stunLeftoverPlanting(V.player) && V.guardRaiseK(V.player)>0.5, `gf=${V.guardDropFade(V.player)} gk=${V.guardRaiseK(V.player)} st=${V.player.stunT} slp=${V.stunLeftoverPlanting(V.player)}`);

bootPlay(); wait(1700);
holdGuardSample();
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
holdGuardSample();
V.keys.clear();
V.player.guarding = false;
V.player.guardBreakT = V.GUARD_BREAK_MS;
V.update(STEP);
check("guard drop leftover break eases leftover k", V.player.guardBreakT>0 && V.guardDropFade(V.player)>0.5 && V.player.guardPoseK>0.5 && !isBlockSheet(V.player), `br=${V.player.guardBreakT} gf=${V.guardDropFade(V.player)} k=${V.player.guardPoseK} src=${sheetSrc(V.player)}`);

bootPlay(); wait(1700);
holdRivalGuard();
V.rival.guardPoseK = 0;
V.rival.guarding = false;
V.rival.wantBlock = false;
V.update(STEP);
for(let i=0;i<10;i++){ holdRivalGuard(); V.update(STEP); }
check("guard drop leftover rival raise still block", V.rival.guarding && V.rival.guardPoseK===1 && V.guardDropFade(V.rival)===0 && isBlockSheet(V.rival), `g=${V.rival.guarding} k=${V.rival.guardPoseK} fade=${V.guardDropFade(V.rival)} src=${sheetSrc(V.rival)}`);
const rgDestHold = V.destRect(V.rival);
const rgAabbHold = V.bodyAABB(V.rival);
const rgKHold = V.rival.guardPoseK;
V.rival.guarding = false;
V.rival.wantBlock = false;
V.rival.guardChoice = -1;
V.rival.gait = 0;
V.rival.closing = false;
V.rival.phase = "idle";
V.rival.boltPhase = "";
V.rival.stunT = 0;
V.rival.falling = false;
V.rival.guardBreakT = 0;
const rgFade0 = V.guardDropFade(V.rival);
check("guard drop leftover rival fade starts high", rgFade0>0.8 && V.rival.guarding===false && V.rival.guardPoseK===rgKHold && !isBlockSheet(V.rival), `fade=${rgFade0} g=${V.rival.guarding} k=${V.rival.guardPoseK} src=${sheetSrc(V.rival)}`);
check("guard drop leftover rival AABB planted at drop", Math.abs(V.bodyAABB(V.rival).y-rgAabbHold.y)<0.05, `dy=${(V.bodyAABB(V.rival).y-rgAabbHold.y).toFixed(2)}`);
check("guard drop leftover rival destRect eases", Math.abs(V.destRect(V.rival).dy-rgDestHold.dy)<1.6, `ddy=${(V.destRect(V.rival).dy-rgDestHold.dy).toFixed(2)}`);

bootPlay(); wait(1700);
V.player.phase="idle"; V.player.phaseT=0; V.player.cut="slash"; V.player.guarding=false; V.player.guardPoseK=0;
V.player.stunT=0; V.player.falling=false; V.player.boltPhase="";
check("chip stun is not guard drop leftover", V.guardDropFade(V.player)===0, `fade=${V.guardDropFade(V.player)}`);
V.player.stunT = V.HITSTUN;
check("chip stun leftover still snaps drop fade", V.guardDropFade(V.player)===0 && V.player.stunT>0 && !isHurtSheet(V.player), `fade=${V.guardDropFade(V.player)} st=${V.player.stunT} src=${sheetSrc(V.player)}`);

const gdThrowKd = landCloseThrow();
check("throw knockdown still not KO crumple after guard drop leftover", gdThrowKd && V.rival.hp===80 && V.rival.thrownT>0 && !V.rival.falling && V.crumpleFade(V.rival)===0 && V.guardDropFade(V.rival)===0, `ok=${gdThrowKd} hp=${V.rival.hp} th=${V.rival.thrownT} cf=${V.crumpleFade(V.rival)} gf=${V.guardDropFade(V.rival)}`);
check("cache v=296 after guard drop leftover live", /game.js\?v=350/.test(htmlTxt));



check("guard break leftover fades block to idle", /function guardDropFade/.test(srcTxt) && /Guard break leftover/.test(srcTxt) && /leftover block used to pop to idle the same tick/.test(srcTxt) && /tripGuardBreak armed/.test(srcTxt) && /sheet cut, not a fail/.test(srcTxt));
check("guard break leftover is draw-only sheet", /Draw-only \(guardDropFade\)/.test(srcTxt) && /guardDropFade\(f\)/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && /poseBitmap still idle once guarding is 0/.test(srcTxt) && /restGuard/.test(sliceFn(codeOnly, "drawKnight")) && !/guardDropFade/.test(sliceFn(codeOnly, "destRect")) && !/guardDropFade/.test(sliceFn(codeOnly, "bodyAABB")) && !/guardDropFade/.test(sliceFn(codeOnly, "slashPose")) && !/guardDropFade/.test(sliceFn(codeOnly, "landHit")) && !/guardDropFade/.test(sliceFn(codeOnly, "tripGuardBreak")));
check("guard break leftover uses leftover raise k", /guardRaiseK\(f\)/.test(sliceFn(codeOnly, "guardDropFade")) && !/f\.guardBreakT > 0/.test(sliceFn(codeOnly, "guardDropFade")) && !/f\.guardBreakT > 0/.test(sliceFn(codeOnly, "tickGuardPose")) && !/f\.guardBreakT > 0/.test(sliceFn(codeOnly, "idleBreath")));
check("guard break leftover poseBitmap still idle", /f\.guarding && ready\(block\)/.test(sliceFn(codeOnly, "poseBitmap")) && !/guardBreakT/.test(sliceFn(codeOnly, "poseBitmap")) && !/guardDropFade/.test(sliceFn(codeOnly, "poseBitmap")));
check("guard break leftover extra rot stays 0", /Extra destRect rot stays 0/.test(srcTxt) && /Guard break leftover eases leftover k/.test(srcTxt) && /Combat poses still snap leftover k/.test(srcTxt) && /Raise still 0ms/.test(srcTxt));
check("guard break leftover is not a new combat verb", V.GUARD_RAISE_MS===80 && V.GUARD_COMMIT_MS===140 && V.STAMINA_BLOCK===20 && V.GUARD_BREAK_MS===400 && V.GUARD_BREAK_SETTLE===180 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80);
check("slash frames still 180/140/280 after guard break leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after guard break leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after guard break leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after guard break leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after guard break leftover", /game.js\?v=350/.test(htmlTxt));

function pinBreakYou(){
  freezeRivalAI();
  V.player.stunT = 0;
  V.player.falling = false;
  V.player.hp = 100;
  V.player.boltPhase = "";
  V.player.phase = "idle";
  V.player.phaseT = 0;
  V.player.cut = "slash";
  V.player.gait = 0;
  V.player.walkT = 0;
  V.player.walkSettleT = 0;
  V.player.sheatheT = 0;
  V.hitstopLeft = 0;
  V.keys.clear();
}

bootPlay(); wait(1700);
holdGuardSample();
V.player.guarding = false;
V.player.guardPoseK = 0;
V.update(STEP);
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
check("guard break leftover raise still 0ms block", V.player.guarding && V.player.guardPoseK>0.8 && V.guardDropFade(V.player)===0 && isBlockSheet(V.player), `g=${V.player.guarding} k=${V.player.guardPoseK.toFixed(3)} fade=${V.guardDropFade(V.player)} src=${sheetSrc(V.player)}`);
const gbAabbHold = V.bodyAABB(V.player);
const gbDestHold = V.destRect(V.player);
const gbKHold = V.player.guardPoseK;
pinBreakYou();
V.keys.add("KeyS");
V.player.stamina = 0.1;
V.player.stamRegenT = 99999;
V.player.guardBreakT = 0;
V.player.guarding = true;
V.update(STEP);
const gbFade0 = V.guardDropFade(V.player);
const gbDest0 = V.destRect(V.player);
const gbAabb0 = V.bodyAABB(V.player);
check("guard break leftover trip fade starts high", gbFade0>0.8 && V.player.guarding===false && V.player.guardBreakT>0 && V.player.guardPoseK<gbKHold && V.player.guardPoseK>0.5, `fade=${gbFade0} k=${V.player.guardPoseK.toFixed(3)} k0=${gbKHold} g=${V.player.guarding} br=${V.player.guardBreakT.toFixed(0)}`);
check("guard break leftover poseBitmap idle immediately", !isBlockSheet(V.player) && !isCutSheet(V.player) && !/windup/.test(sheetSrc(V.player)) && !isHurtSheet(V.player), `src=${sheetSrc(V.player)}`);
check("guard break leftover destRect leftover k eases not snaps", Math.abs(gbDest0.rot)>0.04 && Math.abs(gbDest0.rot-gbDestHold.rot)<0.06, `rot0=${gbDestHold.rot.toFixed(3)} rot=${gbDest0.rot.toFixed(3)} ddy=${(gbDest0.dy-gbDestHold.dy).toFixed(2)}`);
check("guard break leftover AABB planted at trip", Math.abs(gbAabb0.y-gbAabbHold.y)<0.05 && Math.abs(gbAabb0.h-gbAabbHold.h)<0.05, `dy=${(gbAabb0.y-gbAabbHold.y).toFixed(2)}`);
V.player.sheatheT = 140;
check("guard break leftover skips sheathe overlay live", V.guardDropFade(V.player)>0 && V.sheatheFade(V.player)===0, `gf=${V.guardDropFade(V.player)} sf=${V.sheatheFade(V.player)} sh=${V.player.sheatheT}`);
V.player.sheatheT = 0;
let gbFadeMid=-1, gbDestMid=null, gbAabbMid=null, gbBrMid=0;
for(let i=0;i<3;i++){
  pinBreakYou();
  V.update(STEP);
  gbFadeMid=V.guardDropFade(V.player);
  gbDestMid=V.destRect(V.player);
  gbAabbMid=V.bodyAABB(V.player);
  gbBrMid=V.player.guardBreakT;
}
check("guard break leftover fade drops", gbFadeMid>=0 && gbFadeMid<gbFade0-0.08 && V.player.guarding===false && V.player.guardPoseK>0 && gbBrMid>0, `mid=${gbFadeMid} t0=${gbFade0} k=${V.player.guardPoseK} br=${gbBrMid}`);
check("guard break leftover destRect eases plant", gbDestMid && Math.abs(gbDestMid.rot)<Math.abs(gbDestHold.rot)-0.01 && Math.abs(gbDestMid.rot)<0.12, `rotHold=${gbDestHold.rot.toFixed(3)} rotMid=${gbDestMid.rot.toFixed(3)}`);
check("guard break leftover AABB planted through ease", gbAabbMid && Math.abs(gbAabbMid.y-gbAabbHold.y)<0.05 && Math.abs(gbAabbMid.h-gbAabbHold.h)<0.05, `dy=${(gbAabbMid.y-gbAabbHold.y).toFixed(2)}`);
for(let i=0;i<10;i++){
  pinBreakYou();
  V.update(STEP);
}
check("guard break leftover idle fade gone while break holds", V.player.guardPoseK===0 && V.guardDropFade(V.player)===0 && V.player.guardBreakT>0 && !V.player.guarding && !isBlockSheet(V.player), `k=${V.player.guardPoseK} fade=${V.guardDropFade(V.player)} br=${V.player.guardBreakT.toFixed(0)} src=${sheetSrc(V.player)}`);

bootPlay(); wait(1700);
holdGuardSample();
V.player.guarding = false;
V.player.guardPoseK = 0;
V.update(STEP);
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
V.player.stamina = 100;
const gbRevOk = V.startReversal(V.player);
check("guard break leftover reversal plant eases leftover k", gbRevOk && V.player.cut==="golpe" && V.player.phase==="startup" && V.guardDropFade(V.player)===0 && V.reversalPlantFade(V.player)>0.8 && V.guardRaiseK(V.player)>0.8, `ok=${gbRevOk} cut=${V.player.cut} ph=${V.player.phase} gf=${V.guardDropFade(V.player)} rf=${V.reversalPlantFade(V.player)} gk=${V.guardRaiseK(V.player)}`);

bootPlay(); wait(1700);
holdGuardSample();
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
pinBreakYou();
V.keys.add("KeyS");
V.player.stamina = 0.1;
V.player.stamRegenT = 99999;
V.player.guardBreakT = 0;
V.player.guarding = true;
V.update(STEP);
V.player.stunT = V.HITSTUN;
check("guard break leftover stun leftover k eases", V.guardDropFade(V.player)===0 && V.stunLeftoverPlanting(V.player) && V.guardRaiseK(V.player)>0.5, `gf=${V.guardDropFade(V.player)} gk=${V.guardRaiseK(V.player)} st=${V.player.stunT} br=${V.player.guardBreakT} slp=${V.stunLeftoverPlanting(V.player)}`);

bootPlay(); wait(1700);
holdRivalGuard();
V.rival.guardPoseK = 0;
V.rival.guarding = false;
V.rival.wantBlock = false;
V.update(STEP);
for(let i=0;i<10;i++){ holdRivalGuard(); V.update(STEP); }
check("guard break leftover rival raise still block", V.rival.guarding && V.rival.guardPoseK===1 && V.guardDropFade(V.rival)===0 && isBlockSheet(V.rival), `g=${V.rival.guarding} k=${V.rival.guardPoseK} fade=${V.guardDropFade(V.rival)} src=${sheetSrc(V.rival)}`);
const rgbAabbHold = V.bodyAABB(V.rival);
const rgbKHold = V.rival.guardPoseK;
holdRivalGuard();
V.rival.stamina = 0.1;
V.rival.stamRegenT = 99999;
V.rival.guardBreakT = 0;
V.update(STEP);
const rgbFade0 = V.guardDropFade(V.rival);
check("guard break leftover rival fade starts high", rgbFade0>0.8 && V.rival.guarding===false && V.rival.guardBreakT>0 && V.rival.guardPoseK>0.5 && V.rival.guardPoseK<=rgbKHold && !isBlockSheet(V.rival), `fade=${rgbFade0} g=${V.rival.guarding} k=${V.rival.guardPoseK} br=${V.rival.guardBreakT.toFixed(0)} src=${sheetSrc(V.rival)}`);
check("guard break leftover rival AABB planted at trip", Math.abs(V.bodyAABB(V.rival).y-rgbAabbHold.y)<0.05, `dy=${(V.bodyAABB(V.rival).y-rgbAabbHold.y).toFixed(2)}`);

bootPlay(); wait(1700);
V.player.phase="idle"; V.player.phaseT=0; V.player.cut="slash"; V.player.guarding=false; V.player.guardPoseK=0;
V.player.stunT=0; V.player.falling=false; V.player.boltPhase=""; V.player.guardBreakT=0;
check("chip stun is not guard break leftover", V.guardDropFade(V.player)===0, `fade=${V.guardDropFade(V.player)}`);
V.player.stunT = V.HITSTUN;
check("chip stun leftover still snaps break fade", V.guardDropFade(V.player)===0 && V.player.stunT>0 && !isHurtSheet(V.player), `fade=${V.guardDropFade(V.player)} st=${V.player.stunT} src=${sheetSrc(V.player)}`);

const gbThrowKd = landCloseThrow();
check("throw knockdown still not KO crumple after guard break leftover", gbThrowKd && V.rival.hp===80 && V.rival.thrownT>0 && !V.rival.falling && V.crumpleFade(V.rival)===0 && V.guardDropFade(V.rival)===0, `ok=${gbThrowKd} hp=${V.rival.hp} th=${V.rival.thrownT} cf=${V.crumpleFade(V.rival)} gf=${V.guardDropFade(V.rival)}`);
check("cache v=296 after guard break leftover live", /game.js\?v=350/.test(htmlTxt));



check("reversal plant leftover fades block to windup", /function reversalPlantFade/.test(srcTxt) && /function reversalPlanting/.test(srcTxt) && /Reversal plant leftover/.test(srcTxt) && /leftover block used to pop to windup the same/.test(srcTxt) && /sheet cut, not a plant/.test(srcTxt) && /startReversal armed/.test(srcTxt));
check("reversal plant leftover is draw-only sheet", /Draw-only \(reversalPlantFade\)/.test(srcTxt) && /reversalPlantFade\(f\)/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && /poseBitmap still windup immediately/.test(srcTxt) && /restRev/.test(sliceFn(codeOnly, "drawKnight")) && !/reversalPlantFade/.test(sliceFn(codeOnly, "destRect")) && !/reversalPlantFade/.test(sliceFn(codeOnly, "bodyAABB")) && !/reversalPlantFade/.test(sliceFn(codeOnly, "slashPose")) && !/reversalPlantFade/.test(sliceFn(codeOnly, "landHit")) && !/reversalPlantFade/.test(sliceFn(codeOnly, "startReversal")));
check("reversal plant leftover uses leftover raise k", /guardRaiseK\(f\)/.test(sliceFn(codeOnly, "reversalPlantFade")) && /reversalPlanting\(f\)/.test(sliceFn(codeOnly, "tickGuardPose")) && /reversalPlanting\(f\)/.test(sliceFn(codeOnly, "guardRaiseK")) && /restRev/.test(sliceFn(codeOnly, "drawKnight")) && /blockBitmap\(f\)/.test(sliceFn(codeOnly, "drawKnight")));
check("reversal plant leftover poseBitmap still windup", /f\.phase === "startup" && ready\(wind\)/.test(sliceFn(codeOnly, "poseBitmap")) && !/reversalPlantFade/.test(sliceFn(codeOnly, "poseBitmap")) && !/guardPoseK/.test(sliceFn(codeOnly, "poseBitmap")));
check("reversal plant leftover skips sheathe overlay", /reversalPlantFade\(f\) > 0/.test(sliceFn(codeOnly, "sheatheFade")) && /Leftover block owns the sheet/.test(srcTxt));
check("reversal plant leftover extra rot stays 0", /Extra destRect rot stays 0/.test(srcTxt) && /Stun leftover k eases leftover destRect plant/.test(srcTxt) && /Telegraph still snaps/.test(srcTxt) && /Raise still 0ms/.test(srcTxt));
check("reversal plant leftover is not a new combat verb", V.GUARD_RAISE_MS===80 && V.REVERSAL_STAM===30 && V.REVERSAL_INVULN===120 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80);
check("slash frames still 180/140/280 after reversal plant leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after reversal plant leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after reversal plant leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after reversal plant leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after reversal plant leftover", /game.js\?v=350/.test(htmlTxt));

function pinRevYou(){
  freezeRivalAI();
  V.player.stunT = 0;
  V.player.falling = false;
  V.player.hp = 100;
  V.player.boltPhase = "";
  V.player.gait = 0;
  V.player.walkT = 0;
  V.player.walkSettleT = 0;
  V.player.sheatheT = 0;
  V.hitstopLeft = 0;
  V.keys.clear();
}

bootPlay(); wait(1700);
holdGuardSample();
V.player.guarding = false;
V.player.guardPoseK = 0;
V.update(STEP);
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
check("reversal plant leftover raise still 0ms block", V.player.guarding && V.player.guardPoseK>0.8 && V.reversalPlantFade(V.player)===0 && V.guardDropFade(V.player)===0 && isBlockSheet(V.player), `g=${V.player.guarding} k=${V.player.guardPoseK.toFixed(3)} rf=${V.reversalPlantFade(V.player)} gf=${V.guardDropFade(V.player)} src=${sheetSrc(V.player)}`);
const rpAabbHold = V.bodyAABB(V.player);
const rpDestHold = V.destRect(V.player);
const rpKHold = V.player.guardPoseK;
V.player.stamina = 100;
const rpOk = V.startReversal(V.player);
const rpFade0 = V.reversalPlantFade(V.player);
const rpDest0 = V.destRect(V.player);
const rpAabb0 = V.bodyAABB(V.player);
check("reversal plant leftover trip fade starts high", rpOk && rpFade0>0.8 && V.player.cut==="golpe" && V.player.phase==="startup" && V.player.reversal && V.player.guarding===false && V.player.guardPoseK===rpKHold && V.player.telegraph===false && V.telegraphFade(V.player)===0, `ok=${rpOk} fade=${rpFade0} k=${V.player.guardPoseK.toFixed(3)} k0=${rpKHold} g=${V.player.guarding} tel=${V.player.telegraph}`);
check("reversal plant leftover poseBitmap windup immediately", /windup/.test(sheetSrc(V.player)) && !isBlockSheet(V.player) && !isCutSheet(V.player) && !isHurtSheet(V.player), `src=${sheetSrc(V.player)}`);
check("reversal plant leftover destRect leftover k eases not snaps", Math.abs(rpDest0.rot)>0.04 && Math.abs(rpDest0.rot-rpDestHold.rot)<0.06, `rot0=${rpDestHold.rot.toFixed(3)} rot=${rpDest0.rot.toFixed(3)} ddy=${(rpDest0.dy-rpDestHold.dy).toFixed(2)}`);
check("reversal plant leftover AABB planted at trip", Math.abs(rpAabb0.y-rpAabbHold.y)<0.05 && Math.abs(rpAabb0.h-rpAabbHold.h)<0.05, `dy=${(rpAabb0.y-rpAabbHold.y).toFixed(2)}`);
V.player.sheatheT = 140;
check("reversal plant leftover skips sheathe overlay live", V.reversalPlantFade(V.player)>0 && V.sheatheFade(V.player)===0, `rf=${V.reversalPlantFade(V.player)} sf=${V.sheatheFade(V.player)} sh=${V.player.sheatheT}`);
V.player.sheatheT = 0;
let rpFadeMid=-1, rpDestMid=null, rpAabbMid=null, rpKMid=0;
for(let i=0;i<3;i++){
  pinRevYou();
  V.update(STEP);
  rpFadeMid=V.reversalPlantFade(V.player);
  rpDestMid=V.destRect(V.player);
  rpAabbMid=V.bodyAABB(V.player);
  rpKMid=V.player.guardPoseK;
}
check("reversal plant leftover fade drops", rpFadeMid>=0 && rpFadeMid<rpFade0-0.08 && V.player.reversal && V.player.phase==="startup" && V.player.guardPoseK>0 && rpKMid<rpKHold, `mid=${rpFadeMid} t0=${rpFade0} k=${rpKMid} k0=${rpKHold}`);
check("reversal plant leftover destRect eases plant", rpDestMid && Math.abs(rpDestMid.rot)<Math.abs(rpDestHold.rot)-0.01 && Math.abs(rpDestMid.rot)<0.12, `rotHold=${rpDestHold.rot.toFixed(3)} rotMid=${rpDestMid.rot.toFixed(3)}`);
check("reversal plant leftover AABB planted through ease", rpAabbMid && Math.abs(rpAabbMid.y-rpAabbHold.y)<0.05 && Math.abs(rpAabbMid.h-rpAabbHold.h)<0.05, `dy=${(rpAabbMid.y-rpAabbHold.y).toFixed(2)}`);
let rpGone=false;
for(let i=0;i<5;i++){
  pinRevYou();
  V.update(STEP);
  if(V.player.guardPoseK===0 && V.reversalPlantFade(V.player)===0 && V.player.phase==="startup" && V.player.reversal){
    rpGone=true;
    break;
  }
}
check("reversal plant leftover idle fade gone while startup holds", rpGone && V.player.guardPoseK===0 && V.reversalPlantFade(V.player)===0 && V.player.reversal && V.player.phase==="startup" && V.player.cut==="golpe" && /windup/.test(sheetSrc(V.player)), `gone=${rpGone} k=${V.player.guardPoseK} fade=${V.reversalPlantFade(V.player)} ph=${V.player.phase} src=${sheetSrc(V.player)}`);

bootPlay(); wait(1700);
holdGuardSample();
V.player.guarding = false;
V.player.guardPoseK = 0;
V.update(STEP);
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
V.player.stamina = 100;
V.player.guarding = false;
V.startAttack(V.player);
check("slash leftover k eases leftover destRect plant after reversal plant leftover", V.player.cut==="slash" && V.player.phase==="startup" && V.slashLeftoverPlanting(V.player) && V.guardRaiseK(V.player)>0.8 && V.reversalPlantFade(V.player)===0 && V.player.guardPoseK>0.8, `cut=${V.player.cut} ph=${V.player.phase} gk=${V.guardRaiseK(V.player)} rf=${V.reversalPlantFade(V.player)} k=${V.player.guardPoseK}`);

bootPlay(); wait(1700);
holdGuardSample();
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
V.player.stamina = 100;
const rpThrowOk = V.startThrow(V.player);
check("throw-from-guard plant leftover eases leftover k after reversal plant leftover", rpThrowOk && V.player.cut==="throw" && V.player.phase==="startup" && V.throwGuardPlanting(V.player) && V.throwGuardPlantFade(V.player)>0.8 && V.guardRaiseK(V.player)>0.8 && V.reversalPlantFade(V.player)===0 && V.player.guardPoseK>0.8, `ok=${rpThrowOk} cut=${V.player.cut} ph=${V.player.phase} tgf=${V.throwGuardPlantFade(V.player)} gk=${V.guardRaiseK(V.player)} rf=${V.reversalPlantFade(V.player)} k=${V.player.guardPoseK}`);

bootPlay(); wait(1700);
holdGuardSample();
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
V.player.guarding = false;
V.player.stunT = V.HITSTUN;
check("reversal plant leftover stun leftover k eases", V.reversalPlantFade(V.player)===0 && V.stunLeftoverPlanting(V.player) && V.guardRaiseK(V.player)>0.5, `rf=${V.reversalPlantFade(V.player)} gk=${V.guardRaiseK(V.player)} st=${V.player.stunT} slp=${V.stunLeftoverPlanting(V.player)}`);

bootPlay(); wait(1700);
holdRivalGuard();
V.rival.guardPoseK = 0;
V.rival.guarding = false;
V.rival.wantBlock = false;
V.update(STEP);
for(let i=0;i<10;i++){ holdRivalGuard(); V.update(STEP); }
check("reversal plant leftover rival raise still block", V.rival.guarding && V.rival.guardPoseK===1 && V.reversalPlantFade(V.rival)===0 && isBlockSheet(V.rival), `g=${V.rival.guarding} k=${V.rival.guardPoseK} fade=${V.reversalPlantFade(V.rival)} src=${sheetSrc(V.rival)}`);
const rrpAabbHold = V.bodyAABB(V.rival);
const rrpDestHold = V.destRect(V.rival);
const rrpKHold = V.rival.guardPoseK;
V.rival.stamina = 100;
const rrpOk = V.startReversal(V.rival);
const rrpFade0 = V.reversalPlantFade(V.rival);
check("reversal plant leftover rival fade starts high", rrpOk && rrpFade0>0.8 && V.rival.guarding===false && V.rival.reversal && V.rival.cut==="golpe" && V.rival.phase==="startup" && V.rival.guardPoseK===rrpKHold && !isBlockSheet(V.rival) && /windup/.test(sheetSrc(V.rival)), `ok=${rrpOk} fade=${rrpFade0} g=${V.rival.guarding} k=${V.rival.guardPoseK} src=${sheetSrc(V.rival)}`);
check("reversal plant leftover rival destRect leftover k eases", Math.abs(V.destRect(V.rival).rot)>0.04 && Math.abs(V.destRect(V.rival).rot-rrpDestHold.rot)<0.06, `rot0=${rrpDestHold.rot.toFixed(3)} rot=${V.destRect(V.rival).rot.toFixed(3)}`);
check("reversal plant leftover rival AABB planted at trip", Math.abs(V.bodyAABB(V.rival).y-rrpAabbHold.y)<0.05, `dy=${(V.bodyAABB(V.rival).y-rrpAabbHold.y).toFixed(2)}`);

bootPlay(); wait(1700);
V.player.phase="idle"; V.player.phaseT=0; V.player.cut="slash"; V.player.guarding=false; V.player.guardPoseK=0;
V.player.stunT=0; V.player.falling=false; V.player.boltPhase=""; V.player.reversal=false;
check("chip stun is not reversal plant leftover", V.reversalPlantFade(V.player)===0, `fade=${V.reversalPlantFade(V.player)}`);
V.player.stunT = V.HITSTUN;
check("chip stun leftover still snaps reversal plant fade", V.reversalPlantFade(V.player)===0 && V.player.stunT>0 && !isHurtSheet(V.player), `fade=${V.reversalPlantFade(V.player)} st=${V.player.stunT} src=${sheetSrc(V.player)}`);

const rpThrowKd = landCloseThrow();
check("throw knockdown still not KO crumple after reversal plant leftover", rpThrowKd && V.rival.hp===80 && V.rival.thrownT>0 && !V.rival.falling && V.crumpleFade(V.rival)===0 && V.reversalPlantFade(V.rival)===0, `ok=${rpThrowKd} hp=${V.rival.hp} th=${V.rival.thrownT} cf=${V.crumpleFade(V.rival)} rf=${V.reversalPlantFade(V.rival)}`);
check("cache v=296 after reversal plant leftover live", /game.js\?v=350/.test(htmlTxt));




check("slash leftover k eases destRect leftover plant", /function slashLeftoverPlanting/.test(srcTxt) && /Slash leftover k/.test(srcTxt) && /leftover destRect plant used to dump/.test(srcTxt) && /startAttack armed/.test(srcTxt) && /hopped off the windup plant/.test(srcTxt));
check("slash leftover k is destRect-only ease", /destRect-only ease/.test(srcTxt) && /slashLeftoverPlanting/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && /poseBitmap still windup immediately/.test(srcTxt) && /slashLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "tickGuardPose")) && /slashLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "guardRaiseK")) && !/slashLeftoverPlanting/.test(sliceFn(codeOnly, "destRect")) && !/slashLeftoverPlanting/.test(sliceFn(codeOnly, "bodyAABB")) && !/slashLeftoverPlanting/.test(sliceFn(codeOnly, "slashPose")) && !/slashLeftoverPlanting/.test(sliceFn(codeOnly, "landHit")));
check("slash leftover k poseBitmap still windup", /f\.phase === "startup" && ready\(wind\)/.test(sliceFn(codeOnly, "poseBitmap")) && !/slashLeftoverPlanting/.test(sliceFn(codeOnly, "poseBitmap")) && !/guardPoseK/.test(sliceFn(codeOnly, "poseBitmap")));
check("slash leftover k does not re-fade guard drop", !/slashLeftoverPlanting/.test(sliceFn(codeOnly, "guardDropFade")) && !/slashLeftoverPlanting/.test(sliceFn(codeOnly, "reversalPlantFade")) && /guardDropFade idle/.test(srcTxt));
check("slash leftover k extra rot stays 0", /Extra destRect rot stays 0/.test(srcTxt) && /Stun leftover k eases leftover destRect plant/.test(srcTxt) && /leftover guard rot eases with leftover k/.test(srcTxt) && /Raise still 0ms/.test(srcTxt));
check("slash leftover k is not a new combat verb", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.GUARD_RAISE_MS===80 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80);
check("slash frames still 180/140/280 after slash leftover k", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after slash leftover k", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after slash leftover k", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after slash leftover k", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after slash leftover k", /game.js\?v=350/.test(htmlTxt));

function pinSlashYou(){
  freezeRivalAI();
  V.player.stunT = 0;
  V.player.falling = false;
  V.player.hp = 100;
  V.player.boltPhase = "";
  V.player.gait = 0;
  V.player.walkT = 0;
  V.player.walkSettleT = 0;
  V.player.sheatheT = 0;
  V.hitstopLeft = 0;
  V.keys.clear();
}

bootPlay(); wait(1700);
holdGuardSample();
V.player.guarding = false;
V.player.guardPoseK = 0;
V.update(STEP);
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
check("slash leftover k raise still 0ms block", V.player.guarding && V.player.guardPoseK>0.8 && V.slashLeftoverPlanting(V.player)===false && V.guardDropFade(V.player)===0 && isBlockSheet(V.player), `g=${V.player.guarding} k=${V.player.guardPoseK.toFixed(3)} sl=${V.slashLeftoverPlanting(V.player)} src=${sheetSrc(V.player)}`);
const slAabbHold = V.bodyAABB(V.player);
const slDestHold = V.destRect(V.player);
const slKHold = V.player.guardPoseK;
V.player.stamina = 100;
V.player.guarding = false;
V.startAttack(V.player);
const skFade0 = V.guardRaiseK(V.player);
const slDest0 = V.destRect(V.player);
const slAabb0 = V.bodyAABB(V.player);
check("slash leftover k trip leftover plant stays", V.player.cut==="slash" && V.player.phase==="startup" && V.slashLeftoverPlanting(V.player) && skFade0>0.8 && V.player.guarding===false && V.player.guardPoseK===slKHold && V.reversalPlantFade(V.player)===0 && V.guardDropFade(V.player)===0, `cut=${V.player.cut} ph=${V.player.phase} sl=${V.slashLeftoverPlanting(V.player)} gk=${skFade0} k=${V.player.guardPoseK.toFixed(3)} k0=${slKHold} gf=${V.guardDropFade(V.player)} rf=${V.reversalPlantFade(V.player)}`);
check("slash leftover k poseBitmap windup immediately", /windup/.test(sheetSrc(V.player)) && !isBlockSheet(V.player) && !isCutSheet(V.player) && !isHurtSheet(V.player), `src=${sheetSrc(V.player)}`);
check("slash leftover k destRect leftover plant eases not snaps", Math.abs(slDest0.rot)>0.04 && Math.abs(slDest0.rot-slDestHold.rot)<0.06 && Math.abs(slDest0.dy-slDestHold.dy)<1.6, `rot0=${slDestHold.rot.toFixed(3)} rot=${slDest0.rot.toFixed(3)} ddy=${(slDest0.dy-slDestHold.dy).toFixed(2)}`);
check("slash leftover k AABB planted at trip", Math.abs(slAabb0.y-slAabbHold.y)<0.05 && Math.abs(slAabb0.h-slAabbHold.h)<0.05, `dy=${(slAabb0.y-slAabbHold.y).toFixed(2)}`);
check("slash leftover k extra destRect rot 0 at trip", Math.abs(slDest0.rot-slDestHold.rot)<0.06, `drot=${(slDest0.rot-slDestHold.rot).toFixed(3)}`);
let skFadeMid=-1, slDestMid=null, slAabbMid=null, slKMid=0;
for(let i=0;i<3;i++){
  pinSlashYou();
  V.update(STEP);
  skFadeMid=V.guardRaiseK(V.player);
  slDestMid=V.destRect(V.player);
  slAabbMid=V.bodyAABB(V.player);
  slKMid=V.player.guardPoseK;
}
check("slash leftover k leftover plant drops", skFadeMid>=0 && skFadeMid<skFade0-0.08 && V.player.cut==="slash" && V.player.phase==="startup" && V.player.guardPoseK>0 && slKMid<slKHold, `mid=${skFadeMid} t0=${skFade0} k=${slKMid} k0=${slKHold}`);
check("slash leftover k destRect eases leftover plant", slDestMid && Math.abs(slDestMid.rot)<Math.abs(slDestHold.rot)-0.01 && Math.abs(slDestMid.rot)<0.12, `rotHold=${slDestHold.rot.toFixed(3)} rotMid=${slDestMid.rot.toFixed(3)}`);
check("slash leftover k AABB planted through ease", slAabbMid && Math.abs(slAabbMid.y-slAabbHold.y)<0.05 && Math.abs(slAabbMid.h-slAabbHold.h)<0.05, `dy=${(slAabbMid.y-slAabbHold.y).toFixed(2)}`);
let slGone=false;
for(let i=0;i<5;i++){
  pinSlashYou();
  V.update(STEP);
  if(V.player.guardPoseK===0 && V.guardRaiseK(V.player)===0 && V.player.phase==="startup" && V.player.cut==="slash"){
    slGone=true;
    break;
  }
}
check("slash leftover k leftover plant gone while startup holds", slGone && V.player.guardPoseK===0 && V.guardRaiseK(V.player)===0 && V.player.cut==="slash" && V.player.phase==="startup" && /windup/.test(sheetSrc(V.player)), `gone=${slGone} k=${V.player.guardPoseK} gk=${V.guardRaiseK(V.player)} ph=${V.player.phase} src=${sheetSrc(V.player)}`);

bootPlay(); wait(1700);
holdGuardSample();
V.player.guarding = false;
V.player.guardPoseK = 0;
V.update(STEP);
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
V.player.stamina = 100;
V.player.guarding = false;
V.startAttack(V.player, "golpe");
check("golpe leftover k eases leftover destRect plant after slash leftover k", V.player.cut==="golpe" && V.player.phase==="startup" && V.golpeLeftoverPlanting(V.player) && V.slashLeftoverPlanting(V.player)===false && V.guardRaiseK(V.player)>0.8 && V.player.guardPoseK>0.8, `cut=${V.player.cut} ph=${V.player.phase} gl=${V.golpeLeftoverPlanting(V.player)} sl=${V.slashLeftoverPlanting(V.player)} gk=${V.guardRaiseK(V.player)} k=${V.player.guardPoseK}`);

bootPlay(); wait(1700);
holdGuardSample();
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
V.player.stamina = 100;
const slThrowOk = V.startThrow(V.player);
check("throw-from-guard plant leftover eases leftover k after slash leftover k", slThrowOk && V.player.cut==="throw" && V.player.phase==="startup" && V.throwGuardPlanting(V.player) && V.slashLeftoverPlanting(V.player)===false && V.throwGuardPlantFade(V.player)>0.8 && V.guardRaiseK(V.player)>0.8 && V.player.guardPoseK>0.8, `ok=${slThrowOk} cut=${V.player.cut} ph=${V.player.phase} tgf=${V.throwGuardPlantFade(V.player)} sl=${V.slashLeftoverPlanting(V.player)} gk=${V.guardRaiseK(V.player)} k=${V.player.guardPoseK}`);

bootPlay(); wait(1700);
holdGuardSample();
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
V.player.guarding = false;
V.player.stunT = V.HITSTUN;
check("slash leftover k stun leftover k eases", V.slashLeftoverPlanting(V.player)===false && V.stunLeftoverPlanting(V.player) && V.guardRaiseK(V.player)>0.5, `sl=${V.slashLeftoverPlanting(V.player)} gk=${V.guardRaiseK(V.player)} st=${V.player.stunT} slp=${V.stunLeftoverPlanting(V.player)}`);

bootPlay(); wait(1700);
holdGuardSample();
V.player.guarding = false;
V.player.guardPoseK = 0;
V.update(STEP);
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
V.player.stamina = 100;
const slRevOk = V.startReversal(V.player);
check("reversal plant leftover still eases leftover k after slash leftover k", slRevOk && V.player.cut==="golpe" && V.player.phase==="startup" && V.player.reversal && V.slashLeftoverPlanting(V.player)===false && V.reversalPlantFade(V.player)>0.8 && V.guardRaiseK(V.player)>0.8, `ok=${slRevOk} cut=${V.player.cut} ph=${V.player.phase} sl=${V.slashLeftoverPlanting(V.player)} rf=${V.reversalPlantFade(V.player)} gk=${V.guardRaiseK(V.player)}`);

bootPlay(); wait(1700);
holdRivalGuard();
V.rival.guardPoseK = 0;
V.rival.guarding = false;
V.rival.wantBlock = false;
V.update(STEP);
for(let i=0;i<10;i++){ holdRivalGuard(); V.update(STEP); }
check("slash leftover k rival raise still block", V.rival.guarding && V.rival.guardPoseK===1 && V.slashLeftoverPlanting(V.rival)===false && isBlockSheet(V.rival), `g=${V.rival.guarding} k=${V.rival.guardPoseK} sl=${V.slashLeftoverPlanting(V.rival)} src=${sheetSrc(V.rival)}`);
const rslAabbHold = V.bodyAABB(V.rival);
const rslDestHold = V.destRect(V.rival);
const rslKHold = V.rival.guardPoseK;
V.rival.stamina = 100;
V.rival.guarding = false;
V.rival.wantBlock = false;
V.startAttack(V.rival);
check("slash leftover k rival leftover plant stays", V.rival.cut==="slash" && V.rival.phase==="startup" && V.slashLeftoverPlanting(V.rival) && V.guardRaiseK(V.rival)>0.8 && V.rival.guardPoseK===rslKHold && !isBlockSheet(V.rival) && /windup/.test(sheetSrc(V.rival)), `cut=${V.rival.cut} ph=${V.rival.phase} sl=${V.slashLeftoverPlanting(V.rival)} gk=${V.guardRaiseK(V.rival)} k=${V.rival.guardPoseK} src=${sheetSrc(V.rival)}`);
check("slash leftover k rival destRect leftover plant eases", Math.abs(V.destRect(V.rival).rot)>0.04 && Math.abs(V.destRect(V.rival).rot-rslDestHold.rot)<0.06, `rot0=${rslDestHold.rot.toFixed(3)} rot=${V.destRect(V.rival).rot.toFixed(3)}`);
check("slash leftover k rival AABB planted at trip", Math.abs(V.bodyAABB(V.rival).y-rslAabbHold.y)<0.05, `dy=${(V.bodyAABB(V.rival).y-rslAabbHold.y).toFixed(2)}`);

bootPlay(); wait(1700);
V.player.phase="idle"; V.player.phaseT=0; V.player.cut="slash"; V.player.guarding=false; V.player.guardPoseK=0;
V.player.stunT=0; V.player.falling=false; V.player.boltPhase=""; V.player.reversal=false;
check("chip stun is not slash leftover k", V.slashLeftoverPlanting(V.player)===false, `sl=${V.slashLeftoverPlanting(V.player)}`);
V.player.stunT = V.HITSTUN;
check("chip stun leftover still snaps slash leftover k", V.slashLeftoverPlanting(V.player)===false && V.player.stunT>0 && !isHurtSheet(V.player), `sl=${V.slashLeftoverPlanting(V.player)} st=${V.player.stunT} src=${sheetSrc(V.player)}`);

const slThrowKd = landCloseThrow();
check("throw knockdown still not KO crumple after slash leftover k", slThrowKd && V.rival.hp===80 && V.rival.thrownT>0 && !V.rival.falling && V.crumpleFade(V.rival)===0 && V.slashLeftoverPlanting(V.rival)===false, `ok=${slThrowKd} hp=${V.rival.hp} th=${V.rival.thrownT} cf=${V.crumpleFade(V.rival)} sl=${V.slashLeftoverPlanting(V.rival)}`);
check("cache v=296 after slash leftover k live", /game.js\?v=350/.test(htmlTxt));



check("stun leftover k eases destRect leftover plant", /function stunLeftoverPlanting/.test(srcTxt) && /Stun leftover k/.test(srcTxt) && /leftover destRect plant used to dump/.test(srcTxt) && /landHit set stunT/.test(srcTxt) && /hopped off the stun flinch plant/.test(srcTxt));
check("stun leftover k is destRect-only ease", /destRect-only ease/.test(srcTxt) && /stunLeftoverPlanting/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && /poseBitmap still idle immediately/.test(srcTxt) && /stunLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "tickGuardPose")) && /stunLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "guardRaiseK")) && /stunLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "slashPose")) && !/stunLeftoverPlanting/.test(sliceFn(codeOnly, "destRect")) && !/stunLeftoverPlanting/.test(sliceFn(codeOnly, "bodyAABB")) && !/stunLeftoverPlanting/.test(sliceFn(codeOnly, "landHit")) && !/stunLeftoverPlanting/.test(sliceFn(codeOnly, "poseBitmap")));
check("stun leftover k poseBitmap still idle", /f\.guarding && ready\(block\)/.test(sliceFn(codeOnly, "poseBitmap")) && !/stunLeftoverPlanting/.test(sliceFn(codeOnly, "poseBitmap")) && !/guardPoseK/.test(sliceFn(codeOnly, "poseBitmap")));
check("stun leftover k does not re-fade guard drop", !/stunLeftoverPlanting/.test(sliceFn(codeOnly, "guardDropFade")) && !/stunLeftoverPlanting/.test(sliceFn(codeOnly, "reversalPlantFade")) && !/stunLeftoverPlanting/.test(sliceFn(codeOnly, "slashLeftoverPlanting")));
check("stun leftover k extra rot stays 0", /Extra destRect rot stays 0/.test(srcTxt) && /Stun leftover k eases leftover destRect plant/.test(srcTxt) && /leftover guard rot eases with leftover k/.test(srcTxt) && /stun rot scales/.test(srcTxt) && /Raise still 0ms/.test(srcTxt) && /Throw KD leftover destRect plant eases leftover k/.test(srcTxt));
check("stun leftover k is not a new combat verb", V.HITSTUN===350 && V.HITSTOP_HIT===140 && V.GUARD_RAISE_MS===80 && V.THROW_KD_MS===520 && V.THROW_WAKE_INVULN===80 && V.STARTUP===180);
check("slash frames still 180/140/280 after stun leftover k", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after stun leftover k", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after stun leftover k", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after stun leftover k", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after stun leftover k", /game.js\?v=350/.test(htmlTxt));

function pinStunYou(){
  freezeRivalAI();
  V.player.falling = false;
  V.player.hp = 80;
  V.player.boltPhase = "";
  V.player.gait = 0;
  V.player.walkT = 0;
  V.player.walkSettleT = 0;
  V.player.sheatheT = 0;
  V.player.thrownT = 0;
  V.player.throwInvulnT = 0;
  V.player.phase = "idle";
  V.player.phaseT = 0;
  V.player.cut = "slash";
  V.player.guarding = false;
  V.player.stunT = V.HITSTUN;
  V.hitstopLeft = 0;
  V.keys.clear();
}

bootPlay(); wait(1700);
holdGuardSample();
V.player.guarding = false;
V.player.guardPoseK = 0;
V.update(STEP);
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
check("stun leftover k raise still 0ms block", V.player.guarding && V.player.guardPoseK>0.8 && V.stunLeftoverPlanting(V.player)===false && V.guardDropFade(V.player)===0 && isBlockSheet(V.player), `g=${V.player.guarding} k=${V.player.guardPoseK.toFixed(3)} slp=${V.stunLeftoverPlanting(V.player)} src=${sheetSrc(V.player)}`);
const stAabbHold = V.bodyAABB(V.player);
const stDestHold = V.destRect(V.player);
const stKHold = V.player.guardPoseK;
V.player.guarding = false;
V.player.stunT = V.HITSTUN;
const stFade0 = V.guardRaiseK(V.player);
const stDest0 = V.destRect(V.player);
const stAabb0 = V.bodyAABB(V.player);
check("stun leftover k trip leftover plant stays", V.player.stunT===V.HITSTUN && V.stunLeftoverPlanting(V.player) && stFade0>0.8 && V.player.guarding===false && V.player.guardPoseK===stKHold && V.guardDropFade(V.player)===0 && V.slashLeftoverPlanting(V.player)===false && V.reversalPlantFade(V.player)===0, `st=${V.player.stunT} slp=${V.stunLeftoverPlanting(V.player)} gk=${stFade0} k=${V.player.guardPoseK.toFixed(3)} k0=${stKHold} gf=${V.guardDropFade(V.player)}`);
check("stun leftover k poseBitmap idle immediately", !isBlockSheet(V.player) && !isCutSheet(V.player) && !/windup/.test(sheetSrc(V.player)) && !isHurtSheet(V.player), `src=${sheetSrc(V.player)}`);
check("stun leftover k destRect leftover plant eases not snaps", Math.abs(stDest0.rot)>0.04 && Math.abs(stDest0.rot-stDestHold.rot)<0.06 && Math.abs(stDest0.dy-stDestHold.dy)<1.6 && Math.abs(stDest0.rot)<0.20, `rot0=${stDestHold.rot.toFixed(3)} rot=${stDest0.rot.toFixed(3)} ddy=${(stDest0.dy-stDestHold.dy).toFixed(2)}`);
check("stun leftover k AABB planted at trip", Math.abs(stAabb0.y-stAabbHold.y)<0.05 && Math.abs(stAabb0.h-stAabbHold.h)<0.05, `dy=${(stAabb0.y-stAabbHold.y).toFixed(2)}`);
check("stun leftover k extra destRect rot 0 at trip", Math.abs(stDest0.rot-stDestHold.rot)<0.06 && Math.abs(stDest0.rot)<0.20, `drot=${(stDest0.rot-stDestHold.rot).toFixed(3)} rot=${stDest0.rot.toFixed(3)}`);
let stFadeMid=-1, stDestMid=null, stAabbMid=null, stKMid=0;
for(let i=0;i<3;i++){
  pinStunYou();
  V.update(STEP);
  stFadeMid=V.guardRaiseK(V.player);
  stDestMid=V.destRect(V.player);
  stAabbMid=V.bodyAABB(V.player);
  stKMid=V.player.guardPoseK;
}
check("stun leftover k leftover plant drops", stFadeMid>=0 && stFadeMid<stFade0-0.08 && V.player.stunT>0 && V.player.guardPoseK>0 && stKMid<stKHold, `mid=${stFadeMid} t0=${stFade0} k=${stKMid} k0=${stKHold} st=${V.player.stunT}`);
check("stun leftover k destRect eases leftover plant", stDestMid && (stDestHold.dy-stDestMid.dy)>0.4 && (stDestHold.dy-stDestMid.dy)<8 && Math.abs(stDestMid.rot)<0.20, `ddy=${(stDestHold.dy-stDestMid.dy).toFixed(2)} rotHold=${stDestHold.rot.toFixed(3)} rotMid=${stDestMid.rot.toFixed(3)}`);
check("stun leftover k AABB planted through ease", stAabbMid && Math.abs(stAabbMid.y-stAabbHold.y)<0.05 && Math.abs(stAabbMid.h-stAabbHold.h)<0.05, `dy=${(stAabbMid.y-stAabbHold.y).toFixed(2)}`);
let stGone=false, stDestGone=null;
for(let i=0;i<6;i++){
  pinStunYou();
  V.update(STEP);
  if(V.player.guardPoseK===0 && V.guardRaiseK(V.player)===0 && V.stunLeftoverPlanting(V.player)===false && V.player.stunT>0){
    stGone=true;
    stDestGone=V.destRect(V.player);
    break;
  }
}
check("stun leftover k leftover plant gone while stun holds", stGone && V.player.guardPoseK===0 && V.guardRaiseK(V.player)===0 && V.player.stunT>0 && !isHurtSheet(V.player), `gone=${stGone} k=${V.player.guardPoseK} gk=${V.guardRaiseK(V.player)} st=${V.player.stunT} src=${sheetSrc(V.player)}`);
check("stun leftover k destRect is stun flinch after leftover plant", stDestGone && Math.abs(stDestGone.rot)>0.10 && Math.abs(stDestGone.rot)<0.20, `rot=${stDestGone?stDestGone.rot.toFixed(3):"na"}`);

bootPlay(); wait(1700);
holdGuardSample();
V.player.guarding = false;
V.player.guardPoseK = 0;
V.update(STEP);
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
V.player.stamina = 100;
V.player.guarding = false;
V.startAttack(V.player, "golpe");
check("golpe leftover k eases leftover destRect plant after stun leftover k", V.player.cut==="golpe" && V.player.phase==="startup" && V.golpeLeftoverPlanting(V.player) && V.stunLeftoverPlanting(V.player)===false && V.guardRaiseK(V.player)>0.8 && V.player.guardPoseK>0.8, `cut=${V.player.cut} ph=${V.player.phase} gl=${V.golpeLeftoverPlanting(V.player)} slp=${V.stunLeftoverPlanting(V.player)} gk=${V.guardRaiseK(V.player)} k=${V.player.guardPoseK}`);

bootPlay(); wait(1700);
holdGuardSample();
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
V.player.stamina = 100;
const stThrowOk = V.startThrow(V.player);
check("throw-from-guard plant leftover eases leftover k after stun leftover k", stThrowOk && V.player.cut==="throw" && V.player.phase==="startup" && V.throwGuardPlanting(V.player) && V.stunLeftoverPlanting(V.player)===false && V.throwGuardPlantFade(V.player)>0.8 && V.guardRaiseK(V.player)>0.8 && V.player.guardPoseK>0.8, `ok=${stThrowOk} cut=${V.player.cut} ph=${V.player.phase} tgf=${V.throwGuardPlantFade(V.player)} slp=${V.stunLeftoverPlanting(V.player)} gk=${V.guardRaiseK(V.player)} k=${V.player.guardPoseK}`);

bootPlay(); wait(1700);
holdGuardSample();
V.player.guarding = false;
V.player.guardPoseK = 0;
V.update(STEP);
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
V.player.stamina = 100;
V.player.guarding = false;
V.startAttack(V.player);
check("slash leftover k still eases leftover destRect plant after stun leftover k", V.player.cut==="slash" && V.player.phase==="startup" && V.slashLeftoverPlanting(V.player) && V.stunLeftoverPlanting(V.player)===false && V.guardRaiseK(V.player)>0.8 && V.player.guardPoseK>0.8, `cut=${V.player.cut} ph=${V.player.phase} sl=${V.slashLeftoverPlanting(V.player)} slp=${V.stunLeftoverPlanting(V.player)} gk=${V.guardRaiseK(V.player)}`);

bootPlay(); wait(1700);
holdRivalGuard();
V.rival.guardPoseK = 0;
V.rival.guarding = false;
V.rival.wantBlock = false;
V.update(STEP);
for(let i=0;i<10;i++){ holdRivalGuard(); V.update(STEP); }
check("stun leftover k rival raise still block", V.rival.guarding && V.rival.guardPoseK===1 && V.stunLeftoverPlanting(V.rival)===false && isBlockSheet(V.rival), `g=${V.rival.guarding} k=${V.rival.guardPoseK} slp=${V.stunLeftoverPlanting(V.rival)} src=${sheetSrc(V.rival)}`);
const rstAabbHold = V.bodyAABB(V.rival);
const rstDestHold = V.destRect(V.rival);
const rstKHold = V.rival.guardPoseK;
V.rival.guarding = false;
V.rival.wantBlock = false;
V.rival.stunT = V.HITSTUN;
check("stun leftover k rival leftover plant stays", V.rival.stunT===V.HITSTUN && V.stunLeftoverPlanting(V.rival) && V.guardRaiseK(V.rival)>0.8 && V.rival.guardPoseK===rstKHold && !isBlockSheet(V.rival) && !isHurtSheet(V.rival), `st=${V.rival.stunT} slp=${V.stunLeftoverPlanting(V.rival)} gk=${V.guardRaiseK(V.rival)} k=${V.rival.guardPoseK} src=${sheetSrc(V.rival)}`);
check("stun leftover k rival destRect leftover plant eases", Math.abs(V.destRect(V.rival).rot)>0.04 && Math.abs(V.destRect(V.rival).rot-rstDestHold.rot)<0.06 && Math.abs(V.destRect(V.rival).rot)<0.20, `rot0=${rstDestHold.rot.toFixed(3)} rot=${V.destRect(V.rival).rot.toFixed(3)}`);
check("stun leftover k rival AABB planted at trip", Math.abs(V.bodyAABB(V.rival).y-rstAabbHold.y)<0.05, `dy=${(V.bodyAABB(V.rival).y-rstAabbHold.y).toFixed(2)}`);

bootPlay(); wait(1700);
V.player.phase="idle"; V.player.phaseT=0; V.player.cut="slash"; V.player.guarding=false; V.player.guardPoseK=0;
V.player.stunT=0; V.player.falling=false; V.player.boltPhase=""; V.player.thrownT=0;
check("standing chip stun is not stun leftover k", V.stunLeftoverPlanting(V.player)===false, `slp=${V.stunLeftoverPlanting(V.player)}`);
V.player.stunT = V.HITSTUN;
const standDest = V.destRect(V.player);
check("standing chip stun leftover k still 0", V.stunLeftoverPlanting(V.player)===false && V.guardRaiseK(V.player)===0 && V.player.stunT>0 && !isHurtSheet(V.player), `slp=${V.stunLeftoverPlanting(V.player)} gk=${V.guardRaiseK(V.player)} st=${V.player.stunT} src=${sheetSrc(V.player)}`);
check("standing chip stun destRect is stun flinch", Math.abs(standDest.rot)>0.10 && Math.abs(standDest.rot)<0.20, `rot=${standDest.rot.toFixed(3)}`);

const stThrowKd = landCloseThrow();
check("throw knockdown leftover destRect plant eases leftover k after stun leftover k", stThrowKd && V.rival.hp===80 && V.rival.thrownT>0 && !V.rival.falling && V.crumpleFade(V.rival)===0 && V.stunLeftoverPlanting(V.rival)===false && V.throwKdLeftoverPlanting(V.rival) && V.guardRaiseK(V.rival)>0.5, `ok=${stThrowKd} hp=${V.rival.hp} th=${V.rival.thrownT} cf=${V.crumpleFade(V.rival)} slp=${V.stunLeftoverPlanting(V.rival)} tkd=${V.throwKdLeftoverPlanting(V.rival)} gk=${V.guardRaiseK(V.rival)}`);
check("cache v=296 after stun leftover k live", /game.js\?v=350/.test(htmlTxt));



check("throw KD leftover destRect plant eases leftover destRect plant", /function throwKdLeftoverPlanting/.test(srcTxt) && /Throw KD leftover destRect plant/.test(srcTxt) && /leftover destRect plant used to dump/.test(srcTxt) && /landThrow set thrownT/.test(srcTxt) && /hopped off the hurt plant/.test(srcTxt));
check("throw KD leftover destRect plant is destRect-only ease", /destRect-only ease/.test(srcTxt) && /throwKdLeftoverPlanting/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && /poseBitmap still hurt immediately/.test(srcTxt) && /throwKdLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "tickGuardPose")) && /throwKdLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "guardRaiseK")) && !/throwKdLeftoverPlanting/.test(sliceFn(codeOnly, "destRect")) && !/throwKdLeftoverPlanting/.test(sliceFn(codeOnly, "bodyAABB")) && !/throwKdLeftoverPlanting/.test(sliceFn(codeOnly, "slashPose")) && !/throwKdLeftoverPlanting/.test(sliceFn(codeOnly, "landHit")) && !/throwKdLeftoverPlanting/.test(sliceFn(codeOnly, "landThrow")) && !/throwKdLeftoverPlanting/.test(sliceFn(codeOnly, "poseBitmap")));
check("throw KD leftover destRect plant poseBitmap still hurt", /f\.thrownT > 0 && ready\(hurt\)/.test(sliceFn(codeOnly, "poseBitmap")) && !/throwKdLeftoverPlanting/.test(sliceFn(codeOnly, "poseBitmap")) && !/guardPoseK/.test(sliceFn(codeOnly, "poseBitmap")));
check("throw KD leftover destRect plant does not re-fade guard drop", !/throwKdLeftoverPlanting/.test(sliceFn(codeOnly, "guardDropFade")) && !/throwKdLeftoverPlanting/.test(sliceFn(codeOnly, "reversalPlantFade")) && !/throwKdLeftoverPlanting/.test(sliceFn(codeOnly, "slashLeftoverPlanting")) && !/throwKdLeftoverPlanting/.test(sliceFn(codeOnly, "stunLeftoverPlanting")));
check("throw KD leftover destRect plant extra rot stays 0", /Extra destRect rot stays 0/.test(srcTxt) && /Throw KD leftover destRect plant eases leftover k/.test(srcTxt) && /leftover guard rot eases with leftover k/.test(srcTxt) && /hurt rot stays 0/.test(srcTxt) && /Raise still 0ms/.test(srcTxt) && /Standing chip stun leftover k still eases leftover destRect plant/.test(srcTxt));
check("throw KD leftover destRect plant is not a new combat verb", V.HITSTUN===350 && V.HITSTOP_HIT===140 && V.GUARD_RAISE_MS===80 && V.THROW_KD_MS===520 && V.THROW_WAKE_INVULN===80 && V.STARTUP===180);
check("slash frames still 180/140/280 after throw KD leftover destRect plant", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after throw KD leftover destRect plant", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after throw KD leftover destRect plant", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after throw KD leftover destRect plant", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after throw KD leftover destRect plant", /game.js\?v=350/.test(htmlTxt));

function pinThrowKdYou(){
  freezeRivalAI();
  V.player.falling = false;
  V.player.hp = 80;
  V.player.boltPhase = "";
  V.player.gait = 0;
  V.player.walkT = 0;
  V.player.walkSettleT = 0;
  V.player.sheatheT = 0;
  V.player.thrownT = V.THROW_KD_MS;
  V.player.throwInvulnT = 0;
  V.player.phase = "idle";
  V.player.phaseT = 0;
  V.player.cut = "slash";
  V.player.guarding = false;
  V.player.stunT = V.THROW_KD_MS;
  V.hitstopLeft = 0;
  V.keys.clear();
}

bootPlay(); wait(1700);
holdGuardSample();
V.player.guarding = false;
V.player.guardPoseK = 0;
V.update(STEP);
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
check("throw KD leftover destRect plant raise still 0ms block", V.player.guarding && V.player.guardPoseK>0.8 && V.throwKdLeftoverPlanting(V.player)===false && V.stunLeftoverPlanting(V.player)===false && V.guardDropFade(V.player)===0 && isBlockSheet(V.player), `g=${V.player.guarding} k=${V.player.guardPoseK.toFixed(3)} tkd=${V.throwKdLeftoverPlanting(V.player)} src=${sheetSrc(V.player)}`);
const tkdAabbHold = V.bodyAABB(V.player);
const tkdDestHold = V.destRect(V.player);
const tkdKHold = V.player.guardPoseK;
V.player.guarding = false;
V.player.stunT = V.THROW_KD_MS;
V.player.thrownT = V.THROW_KD_MS;
const tkdFade0 = V.guardRaiseK(V.player);
const tkdDest0 = V.destRect(V.player);
const tkdAabb0 = V.bodyAABB(V.player);
check("throw KD leftover destRect plant trip leftover plant stays", V.player.thrownT===V.THROW_KD_MS && V.throwKdLeftoverPlanting(V.player) && V.stunLeftoverPlanting(V.player)===false && tkdFade0>0.8 && V.player.guarding===false && V.player.guardPoseK===tkdKHold && V.guardDropFade(V.player)===0 && V.slashLeftoverPlanting(V.player)===false && V.reversalPlantFade(V.player)===0, `th=${V.player.thrownT} tkd=${V.throwKdLeftoverPlanting(V.player)} slp=${V.stunLeftoverPlanting(V.player)} gk=${tkdFade0} k=${V.player.guardPoseK.toFixed(3)} k0=${tkdKHold} gf=${V.guardDropFade(V.player)}`);
check("throw KD leftover destRect plant poseBitmap hurt immediately", isHurtSheet(V.player) && !isBlockSheet(V.player), `src=${sheetSrc(V.player)}`);
check("throw KD leftover destRect plant destRect leftover plant eases not snaps", Math.abs(tkdDest0.rot)>0.04 && Math.abs(tkdDest0.rot-tkdDestHold.rot)<0.06 && Math.abs(tkdDest0.dy-tkdDestHold.dy)<1.6 && Math.abs(tkdDest0.rot)<0.20, `rot0=${tkdDestHold.rot.toFixed(3)} rot=${tkdDest0.rot.toFixed(3)} ddy=${(tkdDest0.dy-tkdDestHold.dy).toFixed(2)}`);
check("throw KD leftover destRect plant AABB planted at trip", Math.abs(tkdAabb0.y-tkdAabbHold.y)<0.05 && Math.abs(tkdAabb0.h-tkdAabbHold.h)<0.05, `dy=${(tkdAabb0.y-tkdAabbHold.y).toFixed(2)}`);
check("throw KD leftover destRect plant extra destRect rot 0 at trip", Math.abs(tkdDest0.rot-tkdDestHold.rot)<0.06 && Math.abs(tkdDest0.rot)<0.20, `drot=${(tkdDest0.rot-tkdDestHold.rot).toFixed(3)} rot=${tkdDest0.rot.toFixed(3)}`);
let tkdFadeMid=-1, tkdDestMid=null, tkdAabbMid=null, tkdKMid=0;
for(let i=0;i<3;i++){
  pinThrowKdYou();
  V.update(STEP);
  tkdFadeMid=V.guardRaiseK(V.player);
  tkdDestMid=V.destRect(V.player);
  tkdAabbMid=V.bodyAABB(V.player);
  tkdKMid=V.player.guardPoseK;
}
check("throw KD leftover destRect plant leftover plant drops", tkdFadeMid>=0 && tkdFadeMid<tkdFade0-0.08 && V.player.thrownT>0 && V.player.guardPoseK>0 && tkdKMid<tkdKHold, `mid=${tkdFadeMid} t0=${tkdFade0} k=${tkdKMid} k0=${tkdKHold} th=${V.player.thrownT}`);
check("throw KD leftover destRect plant destRect eases leftover plant", tkdDestMid && (tkdDestHold.dy-tkdDestMid.dy)>0.4 && (tkdDestHold.dy-tkdDestMid.dy)<8 && Math.abs(tkdDestMid.rot)<0.12, `ddy=${(tkdDestHold.dy-tkdDestMid.dy).toFixed(2)} rotHold=${tkdDestHold.rot.toFixed(3)} rotMid=${tkdDestMid.rot.toFixed(3)}`);
check("throw KD leftover destRect plant AABB planted through ease", tkdAabbMid && Math.abs(tkdAabbMid.y-tkdAabbHold.y)<0.05 && Math.abs(tkdAabbMid.h-tkdAabbHold.h)<0.05, `dy=${(tkdAabbMid.y-tkdAabbHold.y).toFixed(2)}`);
let tkdGone=false, tkdDestGone=null;
for(let i=0;i<6;i++){
  pinThrowKdYou();
  V.update(STEP);
  if(V.player.guardPoseK===0 && V.guardRaiseK(V.player)===0 && V.throwKdLeftoverPlanting(V.player)===false && V.player.thrownT>0){
    tkdGone=true;
    tkdDestGone=V.destRect(V.player);
    break;
  }
}
check("throw KD leftover destRect plant leftover plant gone while KD holds", tkdGone && V.player.guardPoseK===0 && V.guardRaiseK(V.player)===0 && V.player.thrownT>0 && isHurtSheet(V.player), `gone=${tkdGone} k=${V.player.guardPoseK} gk=${V.guardRaiseK(V.player)} th=${V.player.thrownT} src=${sheetSrc(V.player)}`);
check("throw KD leftover destRect plant destRect is hurt plant after leftover plant", tkdDestGone && Math.abs(tkdDestGone.rot)<0.001, `rot=${tkdDestGone?tkdDestGone.rot.toFixed(3):"na"}`);

bootPlay(); wait(1700);
holdGuardSample();
V.player.guarding = false;
V.player.guardPoseK = 0;
V.update(STEP);
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
V.player.stamina = 100;
V.player.guarding = false;
V.startAttack(V.player, "golpe");
check("golpe leftover k eases leftover destRect plant after throw KD leftover destRect plant", V.player.cut==="golpe" && V.player.phase==="startup" && V.golpeLeftoverPlanting(V.player) && V.throwKdLeftoverPlanting(V.player)===false && V.guardRaiseK(V.player)>0.8 && V.player.guardPoseK>0.8, `cut=${V.player.cut} ph=${V.player.phase} gl=${V.golpeLeftoverPlanting(V.player)} tkd=${V.throwKdLeftoverPlanting(V.player)} gk=${V.guardRaiseK(V.player)} k=${V.player.guardPoseK}`);

bootPlay(); wait(1700);
holdGuardSample();
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
V.player.stamina = 100;
const tkdThrowOk = V.startThrow(V.player);
check("throw-from-guard plant leftover eases leftover k after throw KD leftover destRect plant", tkdThrowOk && V.player.cut==="throw" && V.player.phase==="startup" && V.throwGuardPlanting(V.player) && V.throwKdLeftoverPlanting(V.player)===false && V.throwGuardPlantFade(V.player)>0.8 && V.guardRaiseK(V.player)>0.8 && V.player.guardPoseK>0.8, `ok=${tkdThrowOk} cut=${V.player.cut} ph=${V.player.phase} tgf=${V.throwGuardPlantFade(V.player)} tkd=${V.throwKdLeftoverPlanting(V.player)} gk=${V.guardRaiseK(V.player)} k=${V.player.guardPoseK}`);

bootPlay(); wait(1700);
holdGuardSample();
V.player.guarding = false;
V.player.guardPoseK = 0;
V.update(STEP);
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
V.player.stamina = 100;
V.player.guarding = false;
V.startAttack(V.player);
check("slash leftover k still eases leftover destRect plant after throw KD leftover destRect plant", V.player.cut==="slash" && V.player.phase==="startup" && V.slashLeftoverPlanting(V.player) && V.throwKdLeftoverPlanting(V.player)===false && V.guardRaiseK(V.player)>0.8 && V.player.guardPoseK>0.8, `cut=${V.player.cut} ph=${V.player.phase} sl=${V.slashLeftoverPlanting(V.player)} tkd=${V.throwKdLeftoverPlanting(V.player)} gk=${V.guardRaiseK(V.player)}`);

bootPlay(); wait(1700);
holdGuardSample();
V.player.guarding = false;
V.player.guardPoseK = 0;
V.update(STEP);
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
V.player.guarding = false;
V.player.stunT = V.HITSTUN;
V.player.thrownT = 0;
check("stun leftover k still eases leftover destRect plant after throw KD leftover destRect plant", V.stunLeftoverPlanting(V.player) && V.throwKdLeftoverPlanting(V.player)===false && V.guardRaiseK(V.player)>0.8 && V.player.guardPoseK>0.8 && !isHurtSheet(V.player), `slp=${V.stunLeftoverPlanting(V.player)} tkd=${V.throwKdLeftoverPlanting(V.player)} gk=${V.guardRaiseK(V.player)} src=${sheetSrc(V.player)}`);

bootPlay(); wait(1700);
holdRivalGuard();
V.rival.guardPoseK = 0;
V.rival.guarding = false;
V.rival.wantBlock = false;
V.update(STEP);
for(let i=0;i<10;i++){ holdRivalGuard(); V.update(STEP); }
check("throw KD leftover destRect plant rival raise still block", V.rival.guarding && V.rival.guardPoseK===1 && V.throwKdLeftoverPlanting(V.rival)===false && isBlockSheet(V.rival), `g=${V.rival.guarding} k=${V.rival.guardPoseK} tkd=${V.throwKdLeftoverPlanting(V.rival)} src=${sheetSrc(V.rival)}`);
const rtkdAabbHold = V.bodyAABB(V.rival);
const rtkdDestHold = V.destRect(V.rival);
const rtkdKHold = V.rival.guardPoseK;
V.rival.guarding = false;
V.rival.wantBlock = false;
V.rival.stunT = V.THROW_KD_MS;
V.rival.thrownT = V.THROW_KD_MS;
check("throw KD leftover destRect plant rival leftover plant stays", V.rival.thrownT===V.THROW_KD_MS && V.throwKdLeftoverPlanting(V.rival) && V.stunLeftoverPlanting(V.rival)===false && V.guardRaiseK(V.rival)>0.8 && V.rival.guardPoseK===rtkdKHold && isHurtSheet(V.rival), `th=${V.rival.thrownT} tkd=${V.throwKdLeftoverPlanting(V.rival)} gk=${V.guardRaiseK(V.rival)} k=${V.rival.guardPoseK} src=${sheetSrc(V.rival)}`);
check("throw KD leftover destRect plant rival destRect leftover plant eases", Math.abs(V.destRect(V.rival).rot)>0.04 && Math.abs(V.destRect(V.rival).rot-rtkdDestHold.rot)<0.06 && Math.abs(V.destRect(V.rival).rot)<0.20, `rot0=${rtkdDestHold.rot.toFixed(3)} rot=${V.destRect(V.rival).rot.toFixed(3)}`);
check("throw KD leftover destRect plant rival AABB planted at trip", Math.abs(V.bodyAABB(V.rival).y-rtkdAabbHold.y)<0.05, `dy=${(V.bodyAABB(V.rival).y-rtkdAabbHold.y).toFixed(2)}`);

bootPlay(); wait(1700);
V.player.phase="idle"; V.player.phaseT=0; V.player.cut="slash"; V.player.guarding=false; V.player.guardPoseK=0;
V.player.stunT=0; V.player.falling=false; V.player.boltPhase=""; V.player.thrownT=0;
check("standing chip stun is not throw KD leftover destRect plant", V.throwKdLeftoverPlanting(V.player)===false, `tkd=${V.throwKdLeftoverPlanting(V.player)}`);
V.player.stunT = V.HITSTUN;
const standDest2 = V.destRect(V.player);
check("standing chip stun leftover k still 0 after throw KD leftover destRect plant", V.throwKdLeftoverPlanting(V.player)===false && V.stunLeftoverPlanting(V.player)===false && V.guardRaiseK(V.player)===0 && V.player.stunT>0 && !isHurtSheet(V.player), `tkd=${V.throwKdLeftoverPlanting(V.player)} slp=${V.stunLeftoverPlanting(V.player)} gk=${V.guardRaiseK(V.player)} st=${V.player.stunT} src=${sheetSrc(V.player)}`);
check("standing chip stun destRect is still stun flinch after throw KD leftover destRect plant", Math.abs(standDest2.rot)>0.10 && Math.abs(standDest2.rot)<0.20, `rot=${standDest2.rot.toFixed(3)}`);

const idleThrowKd = landCloseThrow();
check("throw knockdown leftover destRect plant live leftover k eases", idleThrowKd && V.rival.hp===80 && V.rival.thrownT>0 && !V.rival.falling && V.crumpleFade(V.rival)===0 && V.stunLeftoverPlanting(V.rival)===false && V.throwKdLeftoverPlanting(V.rival) && V.guardRaiseK(V.rival)>0.5 && isHurtSheet(V.rival), `ok=${idleThrowKd} hp=${V.rival.hp} th=${V.rival.thrownT} cf=${V.crumpleFade(V.rival)} slp=${V.stunLeftoverPlanting(V.rival)} tkd=${V.throwKdLeftoverPlanting(V.rival)} gk=${V.guardRaiseK(V.rival)} src=${sheetSrc(V.rival)}`);
check("cache v=296 after throw KD leftover destRect plant live", /game.js\?v=350/.test(htmlTxt));

check("clash leftover pose fades leftover slash into windup", /function clashPlantFade/.test(srcTxt) && /function clashPlanting/.test(srcTxt) && /Clash leftover pose/.test(srcTxt) && /leftover slash used to pop to windup/.test(srcTxt) && /cancelIntoGolpe/.test(srcTxt) && /cancelIntoSlash/.test(srcTxt) && /sheet cut, not a raise/.test(srcTxt));
check("clash leftover pose is draw-only leftover", /clashPlantFade/.test(sliceFn(codeOnly, "drawKnight")) && /clashPlantFade/.test(sliceFn(codeOnly, "sheatheFade")) && /clashPlant = !!f\.clashRec/.test(sliceFn(codeOnly, "cancelIntoGolpe")) && /clashPlant = !!f\.clashRec/.test(sliceFn(codeOnly, "cancelIntoSlash")) && !/clashPlantFade/.test(sliceFn(codeOnly, "destRect")) && !/clashPlantFade/.test(sliceFn(codeOnly, "bodyAABB")) && !/clashPlantFade/.test(sliceFn(codeOnly, "poseBitmap")) && !/clashPlantFade/.test(sliceFn(codeOnly, "tickGuardPose")) && !/clashPlantFade/.test(sliceFn(codeOnly, "guardRaiseK")) && !/clashPlantFade/.test(sliceFn(codeOnly, "slashLeftoverPlanting")) && !/clashPlantFade/.test(sliceFn(codeOnly, "landHit")) && !/clashPlantFade/.test(sliceFn(codeOnly, "doClash")));
check("clash leftover pose poseBitmap still windup", /f\.phase === "startup" && ready\(wind\)/.test(sliceFn(codeOnly, "poseBitmap")) && !/clashPlant/.test(sliceFn(codeOnly, "poseBitmap")) && /poseBitmap still windup immediately/.test(srcTxt));
check("clash leftover pose does not re-fade leftover-k destRect plant", !/clashPlantFade/.test(sliceFn(codeOnly, "tickGuardPose")) && !/clashPlantFade/.test(sliceFn(codeOnly, "guardRaiseK")) && !/clashPlantFade/.test(sliceFn(codeOnly, "slashLeftoverPlanting")) && !/clashPlantFade/.test(sliceFn(codeOnly, "stunLeftoverPlanting")) && !/clashPlantFade/.test(sliceFn(codeOnly, "throwKdLeftoverPlanting")) && /Leftover-k destRect plant/.test(srcTxt) && /drop unchanged/.test(srcTxt));
check("clash leftover pose extra rot stays 0", /Extra destRect rot stays 0/.test(srcTxt) && /destRect leftover/.test(srcTxt) && /plant already 0/.test(srcTxt) && /dedicated slash\/windup/.test(srcTxt) && /Raise still 0ms/.test(srcTxt));
check("clash leftover pose clash-K leftover slash fades into windup", /Clash-K leftover slash/.test(srcTxt) && /cancelIntoBolt/.test(srcTxt) && /clashPlant/.test(sliceFn(codeOnly, "cancelIntoBolt")) && /boltHoldCut = !fromClash/.test(sliceFn(codeOnly, "cancelIntoBolt")) && /holdingCutBolt\(f\) && ready\(slash\)/.test(sliceFn(codeOnly, "poseBitmap")));
check("clash leftover pose connected cancel leftover sheathe eases separately", /Connected slash-L leftover sheathe/.test(srcTxt) && /linkPlantFade/.test(srcTxt) && /Telegraph still snaps/.test(srcTxt) && /clashPlant = !!f\.clashRec/.test(sliceFn(codeOnly, "cancelIntoGolpe")));
check("clash leftover pose is not a new combat verb", V.CLASH_RECOVERY===200 && V.SLASH_CANCEL_MS===100 && V.GOLPE_CANCEL_MS===100 && V.GUARD_RAISE_MS===80 && V.STARTUP===180 && V.GOLPE_STARTUP===120 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80);
check("slash frames still 180/140/280 after clash leftover pose", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after clash leftover pose", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after clash leftover pose", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after clash leftover pose", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after clash leftover pose", /game.js\?v=350/.test(htmlTxt));

function pinClashYou(){
  freezeRivalAI();
  V.player.stunT = 0;
  V.player.falling = false;
  V.player.hp = 100;
  V.player.boltPhase = "";
  V.player.gait = 0;
  V.player.walkT = 0;
  V.player.walkSettleT = 0;
  V.player.sheatheT = 0;
  V.hitstopLeft = 0;
  V.keys.clear();
}

function waitClashCancelDoor(){
  const got = waitUntilBoltWindow("slash");
  for(let i=0;i<20;i++){
    holdRival();
    V.player.stamRegenT = 99999;
    if(V.player.pushT<=0) break;
    if(V.player.phase!=="recovery") break;
    V.update(STEP);
  }
  return got;
}

landClash();
const clGot = waitClashCancelDoor();
const clLeft = V.player.phase==="recovery" ? (V.RECOVERY - V.player.phaseT) : -1;
const clDestHold = V.destRect(V.player);
const clAabbHold = V.bodyAABB(V.player);
check("clash leftover pose raise still slash recovery", clGot && V.player.phase==="recovery" && V.player.clashRec && V.clashPlantFade(V.player)===0 && isCutSheet(V.player), `got=${clGot} ph=${V.player.phase} rec=${V.player.clashRec} fade=${V.clashPlantFade(V.player)} src=${sheetSrc(V.player)}`);
V.golpeEdge = true;
V.update(STEP);
const clFade0 = V.clashPlantFade(V.player);
const clDest0 = V.destRect(V.player);
const clAabb0 = V.bodyAABB(V.player);
check("clash leftover pose trip leftover slash stays", clGot && clLeft<=V.SLASH_CANCEL_MS && V.player.cut==="golpe" && V.player.phase==="startup" && V.player.clashRec===false && V.player.clashPlant && clFade0>0.8 && V.player.telegraph===false && V.telegraphFade(V.player)===0 && V.guardDropFade(V.player)===0 && V.reversalPlantFade(V.player)===0, `got=${clGot} left=${clLeft.toFixed(0)} cut=${V.player.cut} ph=${V.player.phase} rec=${V.player.clashRec} plant=${V.player.clashPlant} fade=${clFade0} tel=${V.player.telegraph}`);
check("clash leftover pose poseBitmap windup immediately", /windup/.test(sheetSrc(V.player)) && !isCutSheet(V.player) && !isBlockSheet(V.player) && !isHurtSheet(V.player), `src=${sheetSrc(V.player)}`);
check("clash leftover pose destRect leftover plant stays 0", Math.abs(clDest0.rot)<0.001 && Math.abs(clDestHold.rot)<0.001 && Math.abs(clDest0.dy-clDestHold.dy)<1.6 && Math.abs(clDest0.dx-clDestHold.dx)<1.6, `rot0=${clDestHold.rot.toFixed(3)} rot=${clDest0.rot.toFixed(3)} ddy=${(clDest0.dy-clDestHold.dy).toFixed(2)} ddx=${(clDest0.dx-clDestHold.dx).toFixed(2)}`);
check("clash leftover pose AABB planted at trip", Math.abs(clAabb0.y-clAabbHold.y)<0.05 && Math.abs(clAabb0.h-clAabbHold.h)<0.05, `dy=${(clAabb0.y-clAabbHold.y).toFixed(2)}`);
check("clash leftover pose extra destRect rot 0 at trip", Math.abs(clDest0.rot)<0.001 && Math.abs(clDest0.rot-clDestHold.rot)<0.001, `drot=${(clDest0.rot-clDestHold.rot).toFixed(3)} rot=${clDest0.rot.toFixed(3)}`);
V.player.sheatheT = 140;
check("clash leftover pose skips sheathe overlay live", V.clashPlantFade(V.player)>0 && V.sheatheFade(V.player)===0, `cf=${V.clashPlantFade(V.player)} sf=${V.sheatheFade(V.player)} sh=${V.player.sheatheT}`);
V.player.sheatheT = 0;
let clFadeMid=-1, clDestMid=null, clAabbMid=null;
for(let i=0;i<3;i++){
  pinClashYou();
  V.update(STEP);
  clFadeMid=V.clashPlantFade(V.player);
  clDestMid=V.destRect(V.player);
  clAabbMid=V.bodyAABB(V.player);
}
check("clash leftover pose fade drops", clFadeMid>=0 && clFadeMid<clFade0-0.08 && V.player.cut==="golpe" && V.player.phase==="startup" && V.player.clashPlant, `mid=${clFadeMid} t0=${clFade0} ph=${V.player.phase} plant=${V.player.clashPlant}`);
check("clash leftover pose destRect stays planted through ease", clDestMid && Math.abs(clDestMid.rot)<0.001 && Math.abs(clDestMid.dy-clDestHold.dy)<1.6, `rot=${clDestMid.rot.toFixed(3)} ddy=${(clDestMid.dy-clDestHold.dy).toFixed(2)}`);
check("clash leftover pose AABB planted through ease", clAabbMid && Math.abs(clAabbMid.y-clAabbHold.y)<0.05 && Math.abs(clAabbMid.h-clAabbHold.h)<0.05, `dy=${(clAabbMid.y-clAabbHold.y).toFixed(2)}`);
let clGone=false;
for(let i=0;i<5;i++){
  pinClashYou();
  V.update(STEP);
  if(V.clashPlantFade(V.player)===0 && V.player.phase==="startup" && V.player.cut==="golpe"){
    clGone=true;
    break;
  }
}
check("clash leftover pose leftover slash gone while startup holds", clGone && V.clashPlantFade(V.player)===0 && V.player.phase==="startup" && V.player.cut==="golpe" && /windup/.test(sheetSrc(V.player)), `gone=${clGone} fade=${V.clashPlantFade(V.player)} ph=${V.player.phase} src=${sheetSrc(V.player)}`);

landClash();
const csGot = waitClashCancelDoor();
const csLeft = V.player.phase==="recovery" ? (V.RECOVERY - V.player.phaseT) : -1;
const csDestHold = V.destRect(V.player);
V.attackEdge = true;
V.update(STEP);
check("clash leftover pose Space leftover slash stays", csGot && csLeft<=V.GOLPE_CANCEL_MS && V.player.cut==="slash" && V.player.phase==="startup" && V.player.clashPlant && V.clashPlantFade(V.player)>0.8 && /windup/.test(sheetSrc(V.player)) && Math.abs(V.destRect(V.player).rot)<0.001 && Math.abs(V.destRect(V.player).dy-csDestHold.dy)<1.6, `got=${csGot} left=${csLeft.toFixed(0)} cut=${V.player.cut} ph=${V.player.phase} plant=${V.player.clashPlant} fade=${V.clashPlantFade(V.player)} src=${sheetSrc(V.player)} ddy=${(V.destRect(V.player).dy-csDestHold.dy).toFixed(2)}`);

landClash();
const ckGot = waitClashCancelDoor();
const ckLeft = V.player.phase==="recovery" ? (V.RECOVERY - V.player.phaseT) : -1;
const ckDestHold = V.destRect(V.player);
const ckAabbHold = V.bodyAABB(V.player);
V.player.stamRegenT = 99999;
V.boltEdge = true;
V.update(STEP);
const ckFade0 = V.clashPlantFade(V.player);
const ckDest0 = V.destRect(V.player);
const ckAabb0 = V.bodyAABB(V.player);
check("clash leftover pose clash-K leftover slash stays", ckGot && ckLeft<=V.BOLT_CANCEL_MS && V.player.boltPhase==="startup" && V.player.phase==="idle" && V.player.clashPlant && ckFade0>0.8 && /throw_knife/.test(sheetSrc(V.player)) && !isCutSheet(V.player) && V.player.boltHoldCut===false && V.player.telegraph===false && V.telegraphFade(V.player)===0, `got=${ckGot} left=${ckLeft.toFixed(0)} bph=${V.player.boltPhase} ph=${V.player.phase} plant=${V.player.clashPlant} fade=${ckFade0} hold=${V.player.boltHoldCut} src=${sheetSrc(V.player)}`);
check("clash leftover pose clash-K poseBitmap throw_knife immediately", /throw_knife/.test(sheetSrc(V.player)) && !isCutSheet(V.player) && !isBlockSheet(V.player) && !isHurtSheet(V.player), `src=${sheetSrc(V.player)}`);
check("clash leftover pose clash-K destRect leftover plant stays 0", Math.abs(ckDest0.rot)<0.001 && Math.abs(ckDestHold.rot)<0.001 && Math.abs(ckDest0.dy-ckDestHold.dy)<1.6 && Math.abs(ckDest0.dx-ckDestHold.dx)<1.6, `rot0=${ckDestHold.rot.toFixed(3)} rot=${ckDest0.rot.toFixed(3)} ddy=${(ckDest0.dy-ckDestHold.dy).toFixed(2)} ddx=${(ckDest0.dx-ckDestHold.dx).toFixed(2)}`);
check("clash leftover pose clash-K AABB planted at trip", Math.abs(ckAabb0.y-ckAabbHold.y)<0.05 && Math.abs(ckAabb0.h-ckAabbHold.h)<0.05, `dy=${(ckAabb0.y-ckAabbHold.y).toFixed(2)}`);
check("clash leftover pose clash-K extra destRect rot 0 at trip", Math.abs(ckDest0.rot)<0.001 && Math.abs(ckDest0.rot-ckDestHold.rot)<0.001, `drot=${(ckDest0.rot-ckDestHold.rot).toFixed(3)} rot=${ckDest0.rot.toFixed(3)}`);
V.player.sheatheT = 140;
check("clash leftover pose clash-K skips sheathe overlay live", V.clashPlantFade(V.player)>0 && V.sheatheFade(V.player)===0, `cf=${V.clashPlantFade(V.player)} sf=${V.sheatheFade(V.player)} sh=${V.player.sheatheT}`);
V.player.sheatheT = 0;
let ckFadeMid=-1, ckDestMid=null, ckAabbMid=null;
for(let i=0;i<3;i++){
  freezeRivalAI();
  V.player.stunT = 0;
  V.player.falling = false;
  V.player.hp = 100;
  V.player.gait = 0;
  V.player.walkT = 0;
  V.player.walkSettleT = 0;
  V.player.sheatheT = 0;
  V.hitstopLeft = 0;
  V.keys.clear();
  V.update(STEP);
  ckFadeMid=V.clashPlantFade(V.player);
  ckDestMid=V.destRect(V.player);
  ckAabbMid=V.bodyAABB(V.player);
}
check("clash leftover pose clash-K fade drops", ckFadeMid>=0 && ckFadeMid<ckFade0-0.08 && V.player.boltPhase==="startup" && V.player.clashPlant && V.player.boltHoldCut===false, `mid=${ckFadeMid} t0=${ckFade0} bph=${V.player.boltPhase} plant=${V.player.clashPlant}`);
check("clash leftover pose clash-K destRect stays planted through ease", ckDestMid && Math.abs(ckDestMid.rot)<0.001 && Math.abs(ckDestMid.dy-ckDestHold.dy)<1.6, `rot=${ckDestMid.rot.toFixed(3)} ddy=${(ckDestMid.dy-ckDestHold.dy).toFixed(2)}`);
check("clash leftover pose clash-K AABB planted through ease", ckAabbMid && Math.abs(ckAabbMid.y-ckAabbHold.y)<0.05 && Math.abs(ckAabbMid.h-ckAabbHold.h)<0.05, `dy=${(ckAabbMid.y-ckAabbHold.y).toFixed(2)}`);
let ckGone=false;
for(let i=0;i<5;i++){
  freezeRivalAI();
  V.player.stunT = 0;
  V.player.falling = false;
  V.player.hp = 100;
  V.player.gait = 0;
  V.player.walkT = 0;
  V.player.walkSettleT = 0;
  V.player.sheatheT = 0;
  V.hitstopLeft = 0;
  V.keys.clear();
  V.update(STEP);
  if(V.clashPlantFade(V.player)===0 && V.player.boltPhase==="startup"){
    ckGone=true;
    break;
  }
}
check("clash leftover pose clash-K leftover slash gone while plant holds", ckGone && V.clashPlantFade(V.player)===0 && V.player.boltPhase==="startup" && /throw_knife/.test(sheetSrc(V.player)) && V.player.boltHoldCut===false, `gone=${ckGone} fade=${V.clashPlantFade(V.player)} bph=${V.player.boltPhase} src=${sheetSrc(V.player)}`);

landCloseSlash();
const hitGot = waitUntilBoltWindow("slash");
const hitLeft = V.player.phase==="recovery" ? (V.RECOVERY - V.player.phaseT) : -1;
V.golpeEdge = true;
V.update(STEP);
check("connected slash-L leftover sheathe eases after clash leftover pose", hitGot && hitLeft<=V.SLASH_CANCEL_MS && V.player.cut==="golpe" && V.player.phase==="startup" && V.player.clashPlant===false && V.clashPlantFade(V.player)===0 && V.linkPlantFade(V.player)>0.8 && V.player.telegraph===false && /windup/.test(sheetSrc(V.player)), `got=${hitGot} left=${hitLeft.toFixed(0)} cut=${V.player.cut} ph=${V.player.phase} plant=${V.player.clashPlant} cf=${V.clashPlantFade(V.player)} lf=${V.linkPlantFade(V.player)} src=${sheetSrc(V.player)}`);

landClash();
armRivalClashCutCancel("golpe");
const rGot = waitUntilBoltWindow("slash");
for(let i=0;i<20;i++){
  holdYouDummy();
  armRivalClashCutCancel("golpe");
  if(V.rival.pushT<=0) break;
  if(V.rival.phase!=="recovery") break;
  V.update(STEP);
}
const rDestHold = V.destRect(V.rival);
const rAabbHold = V.bodyAABB(V.rival);
let rTrip=false;
for(let i=0;i<20;i++){
  holdYouDummy();
  armRivalClashCutCancel("golpe");
  V.rival.stamRegenT = 99999;
  V.update(STEP);
  if(V.rival.cut==="golpe" && V.rival.phase==="startup"){ rTrip=true; break; }
  if(V.rival.phase!=="recovery") break;
}
check("clash leftover pose rival leftover slash stays", rGot && rTrip && V.rival.cut==="golpe" && V.rival.phase==="startup" && V.rival.clashPlant && V.clashPlantFade(V.rival)>0.8 && /windup/.test(sheetSrc(V.rival)), `got=${rGot} trip=${rTrip} cut=${V.rival.cut} ph=${V.rival.phase} plant=${V.rival.clashPlant} fade=${V.clashPlantFade(V.rival)} src=${sheetSrc(V.rival)}`);
check("clash leftover pose rival destRect leftover plant stays 0", Math.abs(V.destRect(V.rival).rot)<0.001 && Math.abs(rDestHold.rot)<0.001 && Math.abs(V.destRect(V.rival).dy-rDestHold.dy)<1.6, `rot0=${rDestHold.rot.toFixed(3)} rot=${V.destRect(V.rival).rot.toFixed(3)} ddy=${(V.destRect(V.rival).dy-rDestHold.dy).toFixed(2)}`);
check("clash leftover pose rival AABB planted at trip", Math.abs(V.bodyAABB(V.rival).y-rAabbHold.y)<0.05, `dy=${(V.bodyAABB(V.rival).y-rAabbHold.y).toFixed(2)}`);

bootPlay(); wait(1700);
V.player.phase="idle"; V.player.phaseT=0; V.player.cut="slash"; V.player.guarding=false; V.player.guardPoseK=0;
V.player.stunT=0; V.player.falling=false; V.player.boltPhase=""; V.player.reversal=false; V.player.clashPlant=false;
check("chip stun is not clash leftover pose", V.clashPlantFade(V.player)===0, `fade=${V.clashPlantFade(V.player)}`);
V.player.stunT = V.HITSTUN;
check("chip stun leftover still snaps clash leftover pose", V.clashPlantFade(V.player)===0 && V.player.stunT>0 && !isHurtSheet(V.player), `fade=${V.clashPlantFade(V.player)} st=${V.player.stunT} src=${sheetSrc(V.player)}`);

const clThrowKd = landCloseThrow();
check("throw knockdown still not KO crumple after clash leftover pose", clThrowKd && V.rival.hp===80 && V.rival.thrownT>0 && !V.rival.falling && V.crumpleFade(V.rival)===0 && V.clashPlantFade(V.rival)===0, `ok=${clThrowKd} hp=${V.rival.hp} th=${V.rival.thrownT} cf=${V.crumpleFade(V.rival)} fade=${V.clashPlantFade(V.rival)}`);
check("cache v=296 after clash leftover pose live", /game.js\?v=350/.test(htmlTxt));

check("clash-K leftover pose fades leftover slash into windup", /Clash-K leftover slash/.test(srcTxt) && /stick through the K plant/.test(srcTxt) && /boltHoldCut = !fromClash/.test(sliceFn(codeOnly, "cancelIntoBolt")) && /f\.clashPlant = true/.test(sliceFn(codeOnly, "cancelIntoBolt")) && /f\.telegraph = false/.test(sliceFn(codeOnly, "cancelIntoBolt")) && /f\.boltPhase === "startup" \? f\.boltT : f\.phaseT/.test(sliceFn(codeOnly, "clashPlantFade")));
check("clash-K leftover pose is draw-only leftover", /clashPlantFade/.test(sliceFn(codeOnly, "drawKnight")) && /clashPlantFade/.test(sliceFn(codeOnly, "sheatheFade")) && /clashPlanting\(f\)/.test(sliceFn(codeOnly, "idleBreath")) && !/clashPlantFade/.test(sliceFn(codeOnly, "destRect")) && !/clashPlantFade/.test(sliceFn(codeOnly, "bodyAABB")) && !/clashPlantFade/.test(sliceFn(codeOnly, "poseBitmap")) && !/clashPlantFade/.test(sliceFn(codeOnly, "tickGuardPose")) && !/clashPlantFade/.test(sliceFn(codeOnly, "spawnBolt")));
check("clash-K leftover pose poseBitmap still throw_knife", /f\.boltPhase === "startup" && ready\(throwK\)/.test(sliceFn(codeOnly, "poseBitmap")) && /holdingCutBolt\(f\) && ready\(slash\)/.test(sliceFn(codeOnly, "poseBitmap")) && !/clashPlant/.test(sliceFn(codeOnly, "poseBitmap")) && /poseBitmap still windup immediately/.test(srcTxt));
check("clash-K leftover pose special-cancel still holds the cut", /Special-cancel K still holds the cut/.test(srcTxt) && /boltHoldCut = !fromClash/.test(sliceFn(codeOnly, "cancelIntoBolt")) && /holdingCutBolt\(f\)/.test(sliceFn(codeOnly, "boltPlantFade")));
check("clash-K leftover pose does not re-fade leftover-k destRect plant", !/clashPlantFade/.test(sliceFn(codeOnly, "tickGuardPose")) && !/clashPlantFade/.test(sliceFn(codeOnly, "guardRaiseK")) && !/clashPlantFade/.test(sliceFn(codeOnly, "slashLeftoverPlanting")) && /Leftover-k destRect plant/.test(srcTxt) && /drop unchanged/.test(srcTxt));
check("clash-K leftover pose extra rot stays 0", /Extra destRect rot stays 0/.test(srcTxt) && /destRect leftover/.test(srcTxt) && /plant already 0/.test(srcTxt) && /dedicated slash\/windup/.test(srcTxt) && /Raise still 0ms/.test(srcTxt));
check("clash leftover pose Space/L still eases leftover slash", /clashPlant = !!f\.clashRec/.test(sliceFn(codeOnly, "cancelIntoGolpe")) && /clashPlant = !!f\.clashRec/.test(sliceFn(codeOnly, "cancelIntoSlash")));
check("clash-K leftover pose is not a new combat verb", V.CLASH_RECOVERY===200 && V.SLASH_CANCEL_MS===100 && V.GOLPE_CANCEL_MS===100 && V.BOLT_CANCEL_MS===100 && V.GUARD_RAISE_MS===80 && V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.HITSTUN===350);
check("slash frames still 180/140/280 after clash-K leftover pose", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after clash-K leftover pose", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after clash-K leftover pose", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after clash-K leftover pose", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after clash-K leftover pose", /game.js\?v=350/.test(htmlTxt));

landCloseSlash();
const scHoldGot = waitUntilBoltWindow("slash");
const scHoldLeft = V.player.phase==="recovery" ? (V.RECOVERY - V.player.phaseT) : -1;
V.player.stamRegenT = 99999;
V.boltEdge = true;
V.update(STEP);
check("connected slash-K still holds cut after clash-K leftover pose", scHoldGot && scHoldLeft<=V.BOLT_CANCEL_MS && V.player.boltPhase==="startup" && V.player.boltHoldCut && V.player.clashPlant===false && V.clashPlantFade(V.player)===0 && isCutSheet(V.player), `got=${scHoldGot} left=${scHoldLeft.toFixed(0)} bph=${V.player.boltPhase} hold=${V.player.boltHoldCut} plant=${V.player.clashPlant} fade=${V.clashPlantFade(V.player)} src=${sheetSrc(V.player)}`);

landClash();
const rckGot = waitUntilBoltWindow("slash");
for(let i=0;i<20;i++){
  holdYouDummy();
  V.rival.linkBolt = false;
  V.rival.linkGolpe = false;
  V.rival.linkSlash = false;
  V.rival.boltCd = 99999;
  V.rival.stamRegenT = 99999;
  V.rival.standGoal = 99999;
  if(V.rival.pushT<=0) break;
  if(V.rival.phase!=="recovery") break;
  V.update(STEP);
}
const rckDestHold = V.destRect(V.rival);
const rckAabbHold = V.bodyAABB(V.rival);
let rckTrip=false;
for(let i=0;i<20;i++){
  holdYouDummy();
  armRivalSpecialCancel("slash");
  V.rival.clashRec = true;
  V.rival.stamRegenT = 99999;
  V.update(STEP);
  if(V.rival.boltPhase==="startup"){ rckTrip=true; break; }
  if(V.rival.phase!=="recovery" && !V.rival.boltPhase) break;
}
check("clash-K leftover pose rival leftover slash stays", rckGot && rckTrip && V.rival.boltPhase==="startup" && V.rival.phase==="idle" && V.rival.clashPlant && V.clashPlantFade(V.rival)>0.8 && /throw_knife/.test(sheetSrc(V.rival)) && V.rival.boltHoldCut===false, `got=${rckGot} trip=${rckTrip} bph=${V.rival.boltPhase} ph=${V.rival.phase} plant=${V.rival.clashPlant} fade=${V.clashPlantFade(V.rival)} hold=${V.rival.boltHoldCut} src=${sheetSrc(V.rival)}`);
check("clash-K leftover pose rival destRect leftover plant stays 0", Math.abs(V.destRect(V.rival).rot)<0.001 && Math.abs(rckDestHold.rot)<0.001 && Math.abs(V.destRect(V.rival).dy-rckDestHold.dy)<1.6, `rot0=${rckDestHold.rot.toFixed(3)} rot=${V.destRect(V.rival).rot.toFixed(3)} ddy=${(V.destRect(V.rival).dy-rckDestHold.dy).toFixed(2)}`);
check("clash-K leftover pose rival AABB planted at trip", Math.abs(V.bodyAABB(V.rival).y-rckAabbHold.y)<0.05, `dy=${(V.bodyAABB(V.rival).y-rckAabbHold.y).toFixed(2)}`);

bootPlay(); wait(1700);
V.player.phase="idle"; V.player.phaseT=0; V.player.cut="slash"; V.player.guarding=false; V.player.guardPoseK=0;
V.player.stunT=0; V.player.falling=false; V.player.boltPhase=""; V.player.reversal=false; V.player.clashPlant=false; V.player.boltHoldCut=false;
check("chip stun is not clash-K leftover pose", V.clashPlantFade(V.player)===0, `fade=${V.clashPlantFade(V.player)}`);
V.player.stunT = V.HITSTUN;
check("chip stun leftover still snaps clash-K leftover pose", V.clashPlantFade(V.player)===0 && V.player.stunT>0 && !isHurtSheet(V.player), `fade=${V.clashPlantFade(V.player)} st=${V.player.stunT} src=${sheetSrc(V.player)}`);

const ckThrowKd = landCloseThrow();
check("throw knockdown still not KO crumple after clash-K leftover pose", ckThrowKd && V.rival.hp===80 && V.rival.thrownT>0 && !V.rival.falling && V.crumpleFade(V.rival)===0 && V.clashPlantFade(V.rival)===0, `ok=${ckThrowKd} hp=${V.rival.hp} th=${V.rival.thrownT} cf=${V.crumpleFade(V.rival)} fade=${V.clashPlantFade(V.rival)}`);
check("cache v=296 after clash-K leftover pose live", /game.js\?v=350/.test(htmlTxt));


check("connected slash-L leftover sheathe fades leftover sheathe into windup", /function linkPlantFade/.test(srcTxt) && /function linkPlanting/.test(srcTxt) && /Connected slash-L leftover sheathe/.test(srcTxt) && /leftover sheathe overlay used to pop/.test(srcTxt) && /sheatheFade died with recovery/.test(srcTxt));
check("connected slash-L leftover sheathe is draw-only leftover", /linkPlantFade/.test(sliceFn(codeOnly, "drawKnight")) && /linkPlantFade/.test(sliceFn(codeOnly, "sheatheFade")) && /linkPlanting\(f\)/.test(sliceFn(codeOnly, "idleBreath")) && /leftoverSheathe = sheatheFade\(f\)/.test(sliceFn(codeOnly, "cancelIntoGolpe")) && /leftoverSheathe = sheatheFade\(f\)/.test(sliceFn(codeOnly, "cancelIntoSlash")) && !/linkPlantFade/.test(sliceFn(codeOnly, "destRect")) && !/linkPlantFade/.test(sliceFn(codeOnly, "bodyAABB")) && !/linkPlantFade/.test(sliceFn(codeOnly, "poseBitmap")) && !/linkPlantFade/.test(sliceFn(codeOnly, "tickGuardPose")) && !/linkPlantFade/.test(sliceFn(codeOnly, "guardRaiseK")) && !/linkPlantFade/.test(sliceFn(codeOnly, "slashLeftoverPlanting")) && !/linkPlantFade/.test(sliceFn(codeOnly, "clashPlantFade")) && !/linkPlantFade/.test(sliceFn(codeOnly, "landHit")));
check("connected slash-L leftover sheathe poseBitmap still windup", /f\.phase === "startup" && ready\(wind\)/.test(sliceFn(codeOnly, "poseBitmap")) && !/linkPlant/.test(sliceFn(codeOnly, "poseBitmap")) && /poseBitmap still windup immediately/.test(srcTxt));
check("connected slash-L leftover sheathe does not re-patch clashPlantFade", /clashPlant = !!f\.clashRec/.test(sliceFn(codeOnly, "cancelIntoGolpe")) && /clashPlant = !!f\.clashRec/.test(sliceFn(codeOnly, "cancelIntoSlash")) && /boltHoldCut = !fromClash/.test(sliceFn(codeOnly, "cancelIntoBolt")) && /Clash leftover pose unchanged/.test(srcTxt) && /Clash-K leftover pose unchanged/.test(srcTxt));
check("connected slash-L leftover sheathe special-cancel still holds the cut", /Special-cancel K still holds the cut/.test(srcTxt) && /boltHoldCut = !fromClash/.test(sliceFn(codeOnly, "cancelIntoBolt")) && /holdingCutBolt\(f\)/.test(sliceFn(codeOnly, "boltPlantFade")));
check("connected slash-L leftover sheathe telegraph still snaps", /f\.telegraph = false/.test(sliceFn(codeOnly, "cancelIntoGolpe")) && /f\.telegraph = false/.test(sliceFn(codeOnly, "cancelIntoSlash")) && /Telegraph still snaps/.test(srcTxt));
check("connected slash-L leftover sheathe does not re-fade leftover-k destRect plant", !/linkPlantFade/.test(sliceFn(codeOnly, "tickGuardPose")) && !/linkPlantFade/.test(sliceFn(codeOnly, "guardRaiseK")) && !/linkPlantFade/.test(sliceFn(codeOnly, "slashLeftoverPlanting")) && /Leftover-k destRect plant/.test(srcTxt) && /drop unchanged/.test(srcTxt));
check("connected slash-L leftover sheathe extra rot stays 0", /Extra destRect rot stays 0/.test(srcTxt) && /destRect leftover/.test(srcTxt) && /plant already 0/.test(srcTxt) && /dedicated slash\/windup/.test(srcTxt) && /Raise still 0ms/.test(srcTxt));
check("connected slash-L leftover sheathe is not a new combat verb", V.SLASH_CANCEL_MS===100 && V.GOLPE_CANCEL_MS===100 && V.GUARD_RAISE_MS===80 && V.STARTUP===180 && V.GOLPE_STARTUP===120 && V.HITSTUN===350);
check("slash frames still 180/140/280 after connected slash-L leftover sheathe", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after connected slash-L leftover sheathe", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after connected slash-L leftover sheathe", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after connected slash-L leftover sheathe", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after connected slash-L leftover sheathe", /game.js\?v=350/.test(htmlTxt));

landCloseSlash();
const lkGot = waitUntilWindow();
const lkLeft = V.player.phase==="recovery" ? (V.RECOVERY - V.player.phaseT) : -1;
const lkSheatheHold = V.sheatheFade(V.player);
const lkDestHold = V.destRect(V.player);
const lkAabbHold = V.bodyAABB(V.player);
V.golpeEdge = true;
V.update(STEP);
const lkFade0 = V.linkPlantFade(V.player);
const lkDest0 = V.destRect(V.player);
const lkAabb0 = V.bodyAABB(V.player);
check("connected slash-L leftover sheathe trip leftover sheathe stays", lkGot && lkLeft<=V.SLASH_CANCEL_MS && V.player.cut==="golpe" && V.player.phase==="startup" && V.player.clashPlant===false && V.clashPlantFade(V.player)===0 && V.player.linkPlant && lkFade0>0.8 && V.player.telegraph===false && V.telegraphFade(V.player)===0 && V.sheatheFade(V.player)===0 && lkSheatheHold>0.08, `got=${lkGot} left=${lkLeft.toFixed(0)} cut=${V.player.cut} ph=${V.player.phase} plant=${V.player.clashPlant} lf=${lkFade0} sh0=${lkSheatheHold} tel=${V.player.telegraph}`);
check("connected slash-L leftover sheathe poseBitmap windup immediately", /windup/.test(sheetSrc(V.player)) && !isCutSheet(V.player) && !isBlockSheet(V.player) && !isHurtSheet(V.player), `src=${sheetSrc(V.player)}`);
check("connected slash-L leftover sheathe destRect leftover plant stays 0", Math.abs(lkDest0.rot)<0.001 && Math.abs(lkDestHold.rot)<0.001 && Math.abs(lkDest0.dy-lkDestHold.dy)<1.6 && Math.abs(lkDest0.dx-lkDestHold.dx)<1.6, `rot0=${lkDestHold.rot.toFixed(3)} rot=${lkDest0.rot.toFixed(3)} ddy=${(lkDest0.dy-lkDestHold.dy).toFixed(2)} ddx=${(lkDest0.dx-lkDestHold.dx).toFixed(2)}`);
check("connected slash-L leftover sheathe AABB planted at trip", Math.abs(lkAabb0.y-lkAabbHold.y)<0.05 && Math.abs(lkAabb0.h-lkAabbHold.h)<0.05, `dy=${(lkAabb0.y-lkAabbHold.y).toFixed(2)}`);
check("connected slash-L leftover sheathe extra destRect rot 0 at trip", Math.abs(lkDest0.rot)<0.001 && Math.abs(lkDest0.rot-lkDestHold.rot)<0.001, `drot=${(lkDest0.rot-lkDestHold.rot).toFixed(3)} rot=${lkDest0.rot.toFixed(3)}`);
V.player.sheatheT = 140;
check("connected slash-L leftover sheathe skips sheathe overlay live", V.linkPlantFade(V.player)>0 && V.sheatheFade(V.player)===0, `lf=${V.linkPlantFade(V.player)} sf=${V.sheatheFade(V.player)} sh=${V.player.sheatheT}`);
V.player.sheatheT = 0;
let lkFadeMid=-1, lkDestMid=null, lkAabbMid=null;
for(let i=0;i<3;i++){
  pinClashYou();
  V.update(STEP);
  lkFadeMid=V.linkPlantFade(V.player);
  lkDestMid=V.destRect(V.player);
  lkAabbMid=V.bodyAABB(V.player);
}
check("connected slash-L leftover sheathe fade drops", lkFadeMid>=0 && lkFadeMid<lkFade0-0.08 && V.player.cut==="golpe" && V.player.phase==="startup" && V.player.linkPlant, `mid=${lkFadeMid} t0=${lkFade0} ph=${V.player.phase} plant=${V.player.linkPlant}`);
check("connected slash-L leftover sheathe destRect stays planted through ease", lkDestMid && Math.abs(lkDestMid.rot)<0.001 && Math.abs(lkDestMid.dy-lkDestHold.dy)<1.6, `rot=${lkDestMid.rot.toFixed(3)} ddy=${(lkDestMid.dy-lkDestHold.dy).toFixed(2)}`);
check("connected slash-L leftover sheathe AABB planted through ease", lkAabbMid && Math.abs(lkAabbMid.y-lkAabbHold.y)<0.05 && Math.abs(lkAabbMid.h-lkAabbHold.h)<0.05, `dy=${(lkAabbMid.y-lkAabbHold.y).toFixed(2)}`);
let lkGone=false;
for(let i=0;i<5;i++){
  pinClashYou();
  V.update(STEP);
  if(V.linkPlantFade(V.player)===0 && V.player.phase==="startup" && V.player.cut==="golpe"){
    lkGone=true;
    break;
  }
}
check("connected slash-L leftover sheathe leftover overlay gone while startup holds", lkGone && V.linkPlantFade(V.player)===0 && V.player.phase==="startup" && V.player.cut==="golpe" && /windup/.test(sheetSrc(V.player)), `gone=${lkGone} fade=${V.linkPlantFade(V.player)} ph=${V.player.phase} src=${sheetSrc(V.player)}`);

landCloseGolpe();
const lgGot = waitUntilReverseWindow();
const lgLeft = V.player.phase==="recovery" ? (V.GOLPE_RECOVERY - V.player.phaseT) : -1;
const lgDestHold = V.destRect(V.player);
V.attackEdge = true;
V.update(STEP);
check("connected golpe-Space leftover sheathe leftover sheathe stays", lgGot && lgLeft<=V.GOLPE_CANCEL_MS && V.player.cut==="slash" && V.player.phase==="startup" && V.player.clashPlant===false && V.clashPlantFade(V.player)===0 && V.player.linkPlant && V.linkPlantFade(V.player)>0.8 && /windup/.test(sheetSrc(V.player)) && V.player.telegraph===false && Math.abs(V.destRect(V.player).rot)<0.001 && Math.abs(V.destRect(V.player).dy-lgDestHold.dy)<1.6, `got=${lgGot} left=${lgLeft.toFixed(0)} cut=${V.player.cut} ph=${V.player.phase} plant=${V.player.clashPlant} lf=${V.linkPlantFade(V.player)} src=${sheetSrc(V.player)} ddy=${(V.destRect(V.player).dy-lgDestHold.dy).toFixed(2)}`);

landClash();
const lcGot = waitClashCancelDoor();
V.golpeEdge = true;
V.update(STEP);
check("clash leftover pose is not connected slash-L leftover sheathe", lcGot && V.player.cut==="golpe" && V.player.phase==="startup" && V.player.clashPlant && V.clashPlantFade(V.player)>0.8 && V.linkPlantFade(V.player)===0 && V.player.linkPlant===false, `got=${lcGot} cut=${V.player.cut} plant=${V.player.clashPlant} cf=${V.clashPlantFade(V.player)} lf=${V.linkPlantFade(V.player)} lp=${V.player.linkPlant}`);

landCloseSlash();
const scLkGot = waitUntilBoltWindow("slash");
V.player.stamRegenT = 99999;
V.boltEdge = true;
V.update(STEP);
check("special-cancel K still holds the cut after connected slash-L leftover sheathe", scLkGot && V.player.boltPhase==="startup" && V.player.boltHoldCut && V.player.clashPlant===false && V.clashPlantFade(V.player)===0 && V.linkPlantFade(V.player)===0 && isCutSheet(V.player), `got=${scLkGot} bph=${V.player.boltPhase} hold=${V.player.boltHoldCut} lf=${V.linkPlantFade(V.player)} src=${sheetSrc(V.player)}`);

bootPlay(); wait(1700);
V.player.phase="idle"; V.player.phaseT=0; V.player.cut="slash"; V.player.guarding=false; V.player.guardPoseK=0;
V.player.stunT=0; V.player.falling=false; V.player.boltPhase=""; V.player.reversal=false; V.player.clashPlant=false; V.player.linkPlant=false;
check("chip stun is not connected slash-L leftover sheathe", V.linkPlantFade(V.player)===0, `fade=${V.linkPlantFade(V.player)}`);
V.player.stunT = V.HITSTUN;
check("chip stun leftover still snaps connected slash-L leftover sheathe", V.linkPlantFade(V.player)===0 && V.player.stunT>0 && !isHurtSheet(V.player), `fade=${V.linkPlantFade(V.player)} st=${V.player.stunT} src=${sheetSrc(V.player)}`);

const lkThrowKd = landCloseThrow();
check("throw knockdown still not KO crumple after connected slash-L leftover sheathe", lkThrowKd && V.rival.hp===80 && V.rival.thrownT>0 && !V.rival.falling && V.crumpleFade(V.rival)===0 && V.linkPlantFade(V.rival)===0, `ok=${lkThrowKd} hp=${V.rival.hp} th=${V.rival.thrownT} cf=${V.crumpleFade(V.rival)} fade=${V.linkPlantFade(V.rival)}`);
check("cache v=296 after connected slash-L leftover sheathe live", /game.js\?v=350/.test(htmlTxt));

check("special-cancel K leftover sheathe fades leftover sheathe into cut", /function holdCutFade/.test(srcTxt) && /function holdCutPlanting/.test(srcTxt) && /Special-cancel K leftover sheathe/.test(srcTxt) && /leftover recovery sheathe overlay used/.test(srcTxt) && /sheatheFade died with boltPhase/.test(srcTxt) && /Fade leftover sheathe→cut/.test(srcTxt));
check("special-cancel K leftover sheathe is draw-only leftover", /holdCutFade/.test(sliceFn(codeOnly, "drawKnight")) && /holdCutFade/.test(sliceFn(codeOnly, "sheatheFade")) && /holdCutPlanting\(f\)/.test(sliceFn(codeOnly, "idleBreath")) && /leftoverSheathe = fromClash \? 0 : sheatheFade\(f\)/.test(sliceFn(codeOnly, "cancelIntoBolt")) && /holdCutPlant = true/.test(sliceFn(codeOnly, "cancelIntoBolt")) && !/holdCutFade/.test(sliceFn(codeOnly, "destRect")) && !/holdCutFade/.test(sliceFn(codeOnly, "bodyAABB")) && !/holdCutFade/.test(sliceFn(codeOnly, "poseBitmap")) && !/holdCutFade/.test(sliceFn(codeOnly, "tickGuardPose")) && !/holdCutFade/.test(sliceFn(codeOnly, "guardRaiseK")) && !/holdCutFade/.test(sliceFn(codeOnly, "slashLeftoverPlanting")) && !/holdCutFade/.test(sliceFn(codeOnly, "clashPlantFade")) && !/holdCutFade/.test(sliceFn(codeOnly, "linkPlantFade")) && !/holdCutFade/.test(sliceFn(codeOnly, "landHit")) && !/holdCutFade/.test(sliceFn(codeOnly, "spawnBolt")));
check("special-cancel K leftover sheathe poseBitmap still slash", /holdingCutBolt\(f\) && ready\(slash\)/.test(sliceFn(codeOnly, "poseBitmap")) && /boltHoldCut = !fromClash/.test(sliceFn(codeOnly, "cancelIntoBolt")) && /poseBitmap still slash immediately/.test(srcTxt) && !/holdCut/.test(sliceFn(codeOnly, "poseBitmap")));
check("special-cancel K leftover sheathe still holds the cut", /Special-cancel K still holds the cut/.test(srcTxt) && /boltHoldCut = !fromClash/.test(sliceFn(codeOnly, "cancelIntoBolt")) && /holdingCutBolt\(f\)/.test(sliceFn(codeOnly, "boltPlantFade")) && /holdingCutBolt\(f\)/.test(sliceFn(codeOnly, "holdCutPlanting")));
check("special-cancel K leftover sheathe does not re-patch clashPlantFade", /clashPlant = !!f\.clashRec/.test(sliceFn(codeOnly, "cancelIntoGolpe")) && /clashPlant = !!f\.clashRec/.test(sliceFn(codeOnly, "cancelIntoSlash")) && /boltHoldCut = !fromClash/.test(sliceFn(codeOnly, "cancelIntoBolt")) && /f\.clashPlant = true/.test(sliceFn(codeOnly, "cancelIntoBolt")) && /Clash leftover pose unchanged/.test(srcTxt) && /Clash-K leftover pose unchanged/.test(srcTxt));
check("special-cancel K leftover sheathe does not re-patch linkPlantFade", /leftoverSheathe = sheatheFade\(f\)/.test(sliceFn(codeOnly, "cancelIntoGolpe")) && /leftoverSheathe = sheatheFade\(f\)/.test(sliceFn(codeOnly, "cancelIntoSlash")) && /Connected slash-L leftover sheathe unchanged/.test(srcTxt) && /linkPlantFade/.test(srcTxt));
check("special-cancel K leftover sheathe telegraph still snaps", /u\.telegraph = !holdingCutBolt\(u\)/.test(sliceFn(codeOnly, "startBolt")) && /Telegraph still snaps/.test(srcTxt));
check("special-cancel K leftover sheathe does not re-fade leftover-k destRect plant", !/holdCutFade/.test(sliceFn(codeOnly, "tickGuardPose")) && !/holdCutFade/.test(sliceFn(codeOnly, "guardRaiseK")) && !/holdCutFade/.test(sliceFn(codeOnly, "slashLeftoverPlanting")) && /Leftover-k destRect plant/.test(srcTxt) && /drop unchanged/.test(srcTxt));
check("special-cancel K leftover sheathe extra rot stays 0", /Extra destRect rot stays 0/.test(srcTxt) && /destRect leftover/.test(srcTxt) && /plant already 0/.test(srcTxt) && /Raise still 0ms/.test(srcTxt));
check("special-cancel K leftover sheathe is not a new combat verb", V.BOLT_CANCEL_MS===100 && V.SLASH_CANCEL_MS===100 && V.GOLPE_CANCEL_MS===100 && V.GUARD_RAISE_MS===80 && V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.HITSTUN===350);
check("slash frames still 180/140/280 after special-cancel K leftover sheathe", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after special-cancel K leftover sheathe", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after special-cancel K leftover sheathe", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after special-cancel K leftover sheathe", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after special-cancel K leftover sheathe", /game.js\?v=350/.test(htmlTxt));

landCloseSlash();
const hkGot = waitUntilBoltWindow("slash");
const hkLeft = V.player.phase==="recovery" ? (V.RECOVERY - V.player.phaseT) : -1;
const hkSheatheHold = V.sheatheFade(V.player);
const hkDestHold = V.destRect(V.player);
const hkAabbHold = V.bodyAABB(V.player);
V.player.stamRegenT = 99999;
V.boltEdge = true;
V.update(STEP);
const hkFade0 = V.holdCutFade(V.player);
const hkDest0 = V.destRect(V.player);
const hkAabb0 = V.bodyAABB(V.player);
check("special-cancel K leftover sheathe trip leftover sheathe stays", hkGot && hkLeft<=V.BOLT_CANCEL_MS && V.player.boltPhase==="startup" && V.player.boltHoldCut && V.player.holdCutPlant && hkFade0>0.8 && V.player.clashPlant===false && V.clashPlantFade(V.player)===0 && V.linkPlantFade(V.player)===0 && V.player.telegraph===false && V.telegraphFade(V.player)===0 && V.sheatheFade(V.player)===0 && hkSheatheHold>0.08 && V.player.linkSheathe>0.08, `got=${hkGot} left=${hkLeft.toFixed(0)} bph=${V.player.boltPhase} hold=${V.player.boltHoldCut} plant=${V.player.holdCutPlant} hf=${hkFade0} sh0=${hkSheatheHold} sh=${V.player.linkSheathe} lf=${V.linkPlantFade(V.player)}`);
check("special-cancel K leftover sheathe poseBitmap slash immediately", isCutSheet(V.player) && !/windup/.test(sheetSrc(V.player)) && !isBlockSheet(V.player) && !isHurtSheet(V.player), `src=${sheetSrc(V.player)}`);
check("special-cancel K leftover sheathe destRect leftover plant stays 0", Math.abs(hkDest0.rot)<0.001 && Math.abs(hkDestHold.rot)<0.001 && Math.abs(hkDest0.dy-hkDestHold.dy)<1.6 && Math.abs(hkDest0.dx-hkDestHold.dx)<1.6, `rot0=${hkDestHold.rot.toFixed(3)} rot=${hkDest0.rot.toFixed(3)} ddy=${(hkDest0.dy-hkDestHold.dy).toFixed(2)} ddx=${(hkDest0.dx-hkDestHold.dx).toFixed(2)}`);
check("special-cancel K leftover sheathe AABB planted at trip", Math.abs(hkAabb0.y-hkAabbHold.y)<0.05 && Math.abs(hkAabb0.h-hkAabbHold.h)<0.05, `dy=${(hkAabb0.y-hkAabbHold.y).toFixed(2)}`);
check("special-cancel K leftover sheathe extra destRect rot 0 at trip", Math.abs(hkDest0.rot)<0.001 && Math.abs(hkDest0.rot-hkDestHold.rot)<0.001, `drot=${(hkDest0.rot-hkDestHold.rot).toFixed(3)} rot=${hkDest0.rot.toFixed(3)}`);
V.player.sheatheT = 140;
check("special-cancel K leftover sheathe skips sheathe overlay live", V.holdCutFade(V.player)>0 && V.sheatheFade(V.player)===0, `hf=${V.holdCutFade(V.player)} sf=${V.sheatheFade(V.player)} sh=${V.player.sheatheT}`);
V.player.sheatheT = 0;
let hkFadeMid=-1, hkDestMid=null, hkAabbMid=null;
for(let i=0;i<3;i++){
  freezeRivalAI();
  V.rival.x = 1220;
  V.rival.stunT = 0;
  V.rival.guarding = false;
  V.player.stunT = 0;
  V.player.falling = false;
  V.player.hp = 100;
  V.update(STEP);
  hkFadeMid=V.holdCutFade(V.player);
  hkDestMid=V.destRect(V.player);
  hkAabbMid=V.bodyAABB(V.player);
}
check("special-cancel K leftover sheathe fade drops", hkFadeMid>=0 && hkFadeMid<hkFade0-0.08 && V.player.boltPhase==="startup" && V.player.holdCutPlant && isCutSheet(V.player), `mid=${hkFadeMid} t0=${hkFade0} bph=${V.player.boltPhase} plant=${V.player.holdCutPlant} src=${sheetSrc(V.player)}`);
check("special-cancel K leftover sheathe destRect stays planted through ease", hkDestMid && Math.abs(hkDestMid.rot)<0.001 && Math.abs(hkDestMid.dy-hkDestHold.dy)<1.6, `rot=${hkDestMid.rot.toFixed(3)} ddy=${(hkDestMid.dy-hkDestHold.dy).toFixed(2)}`);
check("special-cancel K leftover sheathe AABB planted through ease", hkAabbMid && Math.abs(hkAabbMid.y-hkAabbHold.y)<0.05 && Math.abs(hkAabbMid.h-hkAabbHold.h)<0.05, `dy=${(hkAabbMid.y-hkAabbHold.y).toFixed(2)}`);
let hkGone=false;
for(let i=0;i<5;i++){
  freezeRivalAI();
  V.rival.x = 1220;
  V.rival.stunT = 0;
  V.rival.guarding = false;
  V.player.stunT = 0;
  V.player.falling = false;
  V.update(STEP);
  if(V.holdCutFade(V.player)===0 && V.player.boltPhase==="startup"){
    hkGone=true;
    break;
  }
}
check("special-cancel K leftover sheathe leftover overlay gone while plant holds", hkGone && V.holdCutFade(V.player)===0 && V.player.boltPhase==="startup" && V.player.boltHoldCut && isCutSheet(V.player), `gone=${hkGone} fade=${V.holdCutFade(V.player)} bph=${V.player.boltPhase} src=${sheetSrc(V.player)}`);

landCloseGolpe();
const hgGot = waitUntilBoltWindow("golpe");
const hgLeft = V.player.phase==="recovery" ? (V.GOLPE_RECOVERY - V.player.phaseT) : -1;
const hgSheatheHold = V.sheatheFade(V.player);
const hgDestHold = V.destRect(V.player);
V.player.stamRegenT = 99999;
V.boltEdge = true;
V.update(STEP);
check("special-cancel golpe-K leftover sheathe leftover sheathe stays", hgGot && hgLeft<=V.BOLT_CANCEL_MS && V.player.boltPhase==="startup" && V.player.boltHoldCut && V.player.holdCutPlant && V.holdCutFade(V.player)>0.8 && V.clashPlantFade(V.player)===0 && V.linkPlantFade(V.player)===0 && isCutSheet(V.player) && V.player.telegraph===false && Math.abs(V.destRect(V.player).rot)<0.001 && Math.abs(V.destRect(V.player).dy-hgDestHold.dy)<1.6 && hgSheatheHold>0.08, `got=${hgGot} left=${hgLeft.toFixed(0)} bph=${V.player.boltPhase} hold=${V.player.boltHoldCut} hf=${V.holdCutFade(V.player)} sh0=${hgSheatheHold} src=${sheetSrc(V.player)} ddy=${(V.destRect(V.player).dy-hgDestHold.dy).toFixed(2)}`);

landClash();
const hcGot = waitUntilBoltWindow("slash");
V.player.stamRegenT = 99999;
V.boltEdge = true;
V.update(STEP);
check("clash-K leftover pose is not special-cancel K leftover sheathe", hcGot && V.player.boltPhase==="startup" && V.player.boltHoldCut===false && V.player.clashPlant && V.clashPlantFade(V.player)>0.8 && V.holdCutFade(V.player)===0 && V.player.holdCutPlant===false && /throw_knife/.test(sheetSrc(V.player)), `got=${hcGot} bph=${V.player.boltPhase} hold=${V.player.boltHoldCut} plant=${V.player.clashPlant} cf=${V.clashPlantFade(V.player)} hf=${V.holdCutFade(V.player)} src=${sheetSrc(V.player)}`);

landCloseSlash();
const hlGot = waitUntilWindow();
V.golpeEdge = true;
V.update(STEP);
check("connected slash-L leftover sheathe is not special-cancel K leftover sheathe", hlGot && V.player.cut==="golpe" && V.player.phase==="startup" && V.player.linkPlant && V.linkPlantFade(V.player)>0.8 && V.holdCutFade(V.player)===0 && V.player.holdCutPlant===false && /windup/.test(sheetSrc(V.player)), `got=${hlGot} cut=${V.player.cut} lf=${V.linkPlantFade(V.player)} hf=${V.holdCutFade(V.player)} hp=${V.player.holdCutPlant} src=${sheetSrc(V.player)}`);

bootPlay(); wait(1700);
freezeYouAI();
freezeRivalAI();
V.player.x = 180;
V.rival.x = 1100;
V.player.stamina = 80;
V.player.stamRegenT = 99999;
V.player.phase="idle"; V.player.phaseT=0; V.player.cut="slash"; V.player.guarding=false; V.player.guardPoseK=0;
V.player.stunT=0; V.player.falling=false; V.player.boltPhase=""; V.player.boltHoldCut=false; V.player.holdCutPlant=false;
V.boltEdge = true;
V.update(STEP);
check("idle K is not special-cancel K leftover sheathe", V.player.boltPhase==="startup" && V.player.boltHoldCut===false && V.holdCutFade(V.player)===0 && V.player.holdCutPlant===false && /throw_knife/.test(sheetSrc(V.player)), `bph=${V.player.boltPhase} hold=${V.player.boltHoldCut} hf=${V.holdCutFade(V.player)} src=${sheetSrc(V.player)}`);

bootPlay(); wait(1700);
V.player.phase="idle"; V.player.phaseT=0; V.player.cut="slash"; V.player.guarding=false; V.player.guardPoseK=0;
V.player.stunT=0; V.player.falling=false; V.player.boltPhase=""; V.player.reversal=false; V.player.clashPlant=false; V.player.linkPlant=false; V.player.holdCutPlant=false;
check("chip stun is not special-cancel K leftover sheathe", V.holdCutFade(V.player)===0, `fade=${V.holdCutFade(V.player)}`);
V.player.stunT = V.HITSTUN;
check("chip stun leftover still snaps special-cancel K leftover sheathe", V.holdCutFade(V.player)===0 && V.player.stunT>0 && !isHurtSheet(V.player), `fade=${V.holdCutFade(V.player)} st=${V.player.stunT} src=${sheetSrc(V.player)}`);

const hkThrowKd = landCloseThrow();
check("throw knockdown still not KO crumple after special-cancel K leftover sheathe", hkThrowKd && V.rival.hp===80 && V.rival.thrownT>0 && !V.rival.falling && V.crumpleFade(V.rival)===0 && V.holdCutFade(V.rival)===0, `ok=${hkThrowKd} hp=${V.rival.hp} th=${V.rival.thrownT} cf=${V.crumpleFade(V.rival)} fade=${V.holdCutFade(V.rival)}`);
check("cache v=296 after special-cancel K leftover sheathe live", /game.js\?v=350/.test(htmlTxt));


check("super spend freeze leftover eases leftover idle through freeze", /function telegraphFade/.test(srcTxt) && /Super spend freeze leftover/.test(srcTxt) && /leftover idle overlay used to sit/.test(srcTxt) && /boltT frozen at the first raise tick/.test(srcTxt) && /cast puff sat on the windup tip/.test(srcTxt) && /Count freeze as raise time/.test(srcTxt) && /leftover idle does not pop back/.test(srcTxt));
check("super spend freeze leftover is draw-only leftover", /Draw-only \(telegraphFade\)/.test(srcTxt) && /telegraphFade\(f\)/.test(srcTxt) && /destRect leftover breath eases/.test(srcTxt) && /Cast puff still rides castPlantXY/.test(srcTxt) && /AABB planted/.test(srcTxt) && /BOLT_SUPER_STOP/.test(sliceFn(codeOnly, "telegraphFade")) && /boltSuper/.test(sliceFn(codeOnly, "telegraphFade")) && !/telegraphFade/.test(sliceFn(codeOnly, "destRect")) && !/telegraphFade/.test(sliceFn(codeOnly, "bodyAABB")) && !/telegraphFade/.test(sliceFn(codeOnly, "poseBitmap")) && !/telegraphFade/.test(sliceFn(codeOnly, "slashPose")) && !/telegraphFade/.test(sliceFn(codeOnly, "landHit")) && !/telegraphFade/.test(sliceFn(codeOnly, "startBolt")) && !/telegraphFade/.test(sliceFn(codeOnly, "spawnBrasaFx")));
check("super spend freeze leftover poseBitmap still throw_knife", /f\.boltPhase === "startup" && ready\(throwK\)/.test(sliceFn(codeOnly, "poseBitmap")) && /poseBitmap still windup immediately/.test(srcTxt) && !/boltSuper/.test(sliceFn(codeOnly, "poseBitmap")));
check("super spend freeze leftover empty K still eases leftover idle", /Empty K still eases leftover idle/.test(srcTxt) && /GUARD_RAISE_MS/.test(sliceFn(codeOnly, "telegraphFade")) && V.GUARD_RAISE_MS===80 && V.BOLT_SUPER_STOP===60);
check("super spend freeze leftover pip leftover still holds", /Pip leftover still holds through freeze/.test(srcTxt) && /meterFlashK/.test(srcTxt) && /HUD bar-drain already holds through freeze/.test(srcTxt));
check("super spend freeze leftover special-cancel still snaps leftover telegraph", /Special-cancel still snaps leftover telegraph/.test(srcTxt) && /u\.telegraph = !holdingCutBolt\(u\)/.test(sliceFn(codeOnly, "startBolt")) && /Telegraph still snaps/.test(srcTxt));
check("super spend freeze leftover extra rot stays 0", /Extra destRect rot stays 0/.test(srcTxt) && /destRect leftover breath eases/.test(srcTxt) && /AABB planted/.test(srcTxt));
check("super spend freeze leftover is not a new combat verb", V.BOLT_SUPER_STOP===60 && V.BOLT_SUPER_STOP===V.HITSTOP_BLOCK && V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.GUARD_RAISE_MS===80 && V.BOLT_SUPER_DMG===28 && V.BOLT_SUPER_CHIP===6);
check("slash frames still 180/140/280 after super spend freeze leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after super spend freeze leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after super spend freeze leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after super spend freeze leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after super spend freeze leftover", /game.js\?v=350/.test(htmlTxt));

bootPlay(); wait(1700);
freezeRivalAI(); freezeYouAI();
V.player.x = 180; V.rival.x = 1100;
V.player.stamina = 100; V.player.stamRegenT = 99999;
V.player.meter = 100; V.player.meterFlashT = 0;
V.player.sheatheT = 0; V.player.guarding = false; V.player.guardPoseK = 0;
V.player.phase = "idle"; V.player.phaseT = 0; V.player.boltPhase = ""; V.player.boltHoldCut = false;
V.hitstopLeft = 0;
const sfDest0 = V.destRect(V.player);
const sfAabb0 = V.bodyAABB(V.player);
V.boltEdge = true;
V.update(STEP);
const sfFade0 = V.telegraphFade(V.player);
const sfPlant0 = V.castPlantXY(V.player);
check("super spend freeze leftover trip leftover idle stays", V.player.boltPhase==="startup" && V.player.boltSuper===true && V.player.telegraph===true && V.telegraphing(V.player) && sfFade0>0 && sfFade0<0.5 && V.hitstopLeft===V.BOLT_SUPER_STOP && /throw_knife/.test(sheetSrc(V.player)) && V.player.meter===0 && V.brasaFxKind==="cast", `bph=${V.player.boltPhase} super=${V.player.boltSuper} tel=${V.player.telegraph} fade=${sfFade0} hs=${V.hitstopLeft} src=${sheetSrc(V.player)} kind=${V.brasaFxKind}`);
check("super spend freeze leftover poseBitmap throw_knife immediately", /throw_knife/.test(sheetSrc(V.player)) && !isCutSheet(V.player) && !isBlockSheet(V.player) && !isHurtSheet(V.player), `src=${sheetSrc(V.player)}`);
check("super spend freeze leftover destRect leftover plant stays 0 extra rot", Math.abs(sfDest0.rot)<0.001 && Math.abs(V.destRect(V.player).rot)<0.001 && Math.abs(V.destRect(V.player).dy-sfDest0.dy)<1.6 && Math.abs(V.destRect(V.player).dx-sfDest0.dx)<1.6, `rot0=${sfDest0.rot.toFixed(3)} rot=${V.destRect(V.player).rot.toFixed(3)} ddy=${(V.destRect(V.player).dy-sfDest0.dy).toFixed(2)} ddx=${(V.destRect(V.player).dx-sfDest0.dx).toFixed(2)}`);
check("super spend freeze leftover AABB planted at trip", Math.abs(V.bodyAABB(V.player).y-sfAabb0.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-sfAabb0.h)<0.05, `dy=${(V.bodyAABB(V.player).y-sfAabb0.y).toFixed(2)}`);
check("super spend freeze leftover extra destRect rot 0 at trip", Math.abs(V.destRect(V.player).rot)<0.001, `rot=${V.destRect(V.player).rot.toFixed(3)}`);
check("super spend freeze leftover cast puff at plant", V.brasaFxKind==="cast" && V.brasaFxT>0 && Math.abs(V.brasaX-sfPlant0.x)<8 && Math.abs(V.brasaY-sfPlant0.y)<8, `kind=${V.brasaFxKind} t=${V.brasaFxT} dx=${(V.brasaX-sfPlant0.x).toFixed(1)} dy=${(V.brasaY-sfPlant0.y).toFixed(1)}`);
const sfHold = V.telegraphFade(V.player);
const sfHs0 = V.hitstopLeft;
const sfBoltT0 = V.player.boltT;
V.update(STEP);
const sfFadeHs = V.telegraphFade(V.player);
check("super spend freeze leftover fade climbs freeze", V.hitstopLeft>0 && V.hitstopLeft<sfHs0 && V.player.boltT===sfBoltT0 && sfFadeHs>sfHold+0.05 && sfFadeHs<1 && V.player.boltPhase==="startup" && V.player.boltSuper===true, `hs=${V.hitstopLeft} hs0=${sfHs0} bt=${V.player.boltT} bt0=${sfBoltT0} fade=${sfFadeHs} hold=${sfHold}`);
check("super spend freeze leftover pip leftover holds freeze", V.player.meter===0 && V.player.meterFlashT===V.METER_FLASH_MS && V.meterFlashK(V.player)>0.9 && V.hitstopLeft>0, `m=${V.player.meter} ft=${V.player.meterFlashT} k=${V.meterFlashK(V.player)} hs=${V.hitstopLeft}`);
const sfDestHs = V.destRect(V.player);
const sfAabbHs = V.bodyAABB(V.player);
check("super spend freeze leftover destRect leftover breath eases freeze", Math.abs(sfDestHs.rot)<0.001 && Math.abs(sfDestHs.dy-sfDest0.dy)<1.6, `rot=${sfDestHs.rot.toFixed(3)} ddy=${(sfDestHs.dy-sfDest0.dy).toFixed(2)} fade=${sfFadeHs}`);
check("super spend freeze leftover AABB planted through freeze", Math.abs(sfAabbHs.y-sfAabb0.y)<0.05 && Math.abs(sfAabbHs.h-sfAabb0.h)<0.05, `dy=${(sfAabbHs.y-sfAabb0.y).toFixed(2)}`);
let sfFadeMid=-1, sfDestMid=null;
for(let i=0;i<2;i++){
  freezeRivalAI();
  V.player.stunT=0; V.player.falling=false; V.player.hp=100;
  V.update(STEP);
  sfFadeMid=V.telegraphFade(V.player);
  sfDestMid=V.destRect(V.player);
}
check("super spend freeze leftover fade keeps climbing freeze", sfFadeMid>sfFadeHs+0.08 && V.hitstopLeft>0 && V.player.boltPhase==="startup" && V.player.boltT===sfBoltT0, `mid=${sfFadeMid} hsFade=${sfFadeHs} hs=${V.hitstopLeft} bt=${V.player.boltT}`);
drainHitstop();
const sfFadeAfter = V.telegraphFade(V.player);
check("super spend freeze leftover leftover idle gone after freeze", V.hitstopLeft<=0 && sfFadeAfter>0.85 && V.player.boltPhase==="startup" && V.player.boltSuper===true && V.telegraphing(V.player) && /throw_knife/.test(sheetSrc(V.player)), `hs=${V.hitstopLeft} fade=${sfFadeAfter} bph=${V.player.boltPhase} src=${sheetSrc(V.player)}`);
check("super spend freeze leftover leftover idle does not pop back", sfFadeAfter>=sfFadeMid-0.02, `after=${sfFadeAfter} mid=${sfFadeMid}`);
check("super spend freeze leftover destRect leftover breath after freeze", Math.abs(V.destRect(V.player).rot)<0.001 && Math.abs(V.destRect(V.player).dy-sfDest0.dy)<1.6, `rot=${V.destRect(V.player).rot.toFixed(3)} ddy=${(V.destRect(V.player).dy-sfDest0.dy).toFixed(2)}`);
check("super spend freeze leftover AABB planted after freeze", Math.abs(V.bodyAABB(V.player).y-sfAabb0.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-sfAabb0.h)<0.05, `dy=${(V.bodyAABB(V.player).y-sfAabb0.y).toFixed(2)}`);

bootPlay(); wait(1700);
freezeRivalAI(); freezeYouAI();
V.player.x = 180; V.rival.x = 1100;
V.player.stamina = 100; V.player.stamRegenT = 99999;
V.player.meter = 0; V.player.meterFlashT = 0;
V.player.sheatheT = 0; V.player.guarding = false; V.player.guardPoseK = 0;
V.player.phase = "idle"; V.player.phaseT = 0; V.player.boltPhase = "";
V.hitstopLeft = 0;
V.boltEdge = true;
V.update(STEP);
const ekFade0 = V.telegraphFade(V.player);
check("empty K is not super spend freeze leftover", V.player.boltPhase==="startup" && V.player.boltSuper===false && V.hitstopLeft===0 && V.telegraphing(V.player) && ekFade0>0 && ekFade0<0.5 && /throw_knife/.test(sheetSrc(V.player)), `bph=${V.player.boltPhase} super=${V.player.boltSuper} hs=${V.hitstopLeft} fade=${ekFade0} src=${sheetSrc(V.player)}`);
V.update(STEP);
const ekFade1 = V.telegraphFade(V.player);
check("empty K leftover idle still eases over raise", V.hitstopLeft===0 && ekFade1>ekFade0+0.05 && ekFade1<1 && V.player.boltPhase==="startup", `fade=${ekFade1} t0=${ekFade0} hs=${V.hitstopLeft} bt=${V.player.boltT}`);

bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.rival.x = 1100; V.player.x = 180;
V.rival.stamina = 100; V.rival.stamRegenT = 99999;
V.rival.meter = 100; V.rival.meterFlashT = 0;
V.rival.sheatheT = 0; V.rival.guarding = false; V.rival.guardPoseK = 0;
V.rival.phase = "idle"; V.rival.phaseT = 0; V.rival.boltPhase = "";
V.hitstopLeft = 0;
const rsfOk = V.startBolt(V.rival, true);
const rsfFadeArm = V.telegraphFade(V.rival);
check("rival super spend freeze leftover arms leftover idle", rsfOk && V.rival.boltPhase==="startup" && V.rival.boltSuper===true && V.rival.telegraph===true && rsfFadeArm===0 && V.hitstopLeft===V.BOLT_SUPER_STOP && /throw_knife/.test(sheetSrc(V.rival)) && V.meterFlashK(V.player)===0, `ok=${rsfOk} bph=${V.rival.boltPhase} super=${V.rival.boltSuper} fade=${rsfFadeArm} hs=${V.hitstopLeft} src=${sheetSrc(V.rival)}`);
V.update(STEP);
const rsfFadeHs = V.telegraphFade(V.rival);
check("rival super spend freeze leftover fade climbs freeze", V.hitstopLeft>0 && rsfFadeHs>0.05 && rsfFadeHs<1 && V.rival.boltPhase==="startup" && V.rival.boltT===0, `hs=${V.hitstopLeft} fade=${rsfFadeHs} bt=${V.rival.boltT}`);

landCloseSlash();
const scSfGot = waitUntilBoltWindow("slash");
V.player.stamRegenT = 99999;
V.player.meter = 100;
V.boltEdge = true;
V.update(STEP);
check("special-cancel K is not super spend freeze leftover idle", scSfGot && V.player.boltPhase==="startup" && V.player.boltHoldCut && V.player.telegraph===false && V.telegraphFade(V.player)===0 && V.holdCutFade(V.player)>=0 && isCutSheet(V.player), `got=${scSfGot} bph=${V.player.boltPhase} hold=${V.player.boltHoldCut} tel=${V.player.telegraph} tf=${V.telegraphFade(V.player)} hf=${V.holdCutFade(V.player)} src=${sheetSrc(V.player)}`);

bootPlay(); wait(1700);
V.player.phase="idle"; V.player.phaseT=0; V.player.cut="slash"; V.player.guarding=false; V.player.guardPoseK=0;
V.player.stunT=0; V.player.falling=false; V.player.boltPhase=""; V.player.reversal=false;
check("chip stun is not super spend freeze leftover", V.telegraphFade(V.player)===0, `fade=${V.telegraphFade(V.player)}`);
V.player.stunT = V.HITSTUN;
check("chip stun leftover still snaps super spend freeze leftover", V.telegraphFade(V.player)===0 && V.player.stunT>0 && !isHurtSheet(V.player), `fade=${V.telegraphFade(V.player)} st=${V.player.stunT} src=${sheetSrc(V.player)}`);

const sfThrowKd = landCloseThrow();
check("throw knockdown still not KO crumple after super spend freeze leftover", sfThrowKd && V.rival.hp===80 && V.rival.thrownT>0 && !V.rival.falling && V.crumpleFade(V.rival)===0, `ok=${sfThrowKd} hp=${V.rival.hp} th=${V.rival.thrownT} cf=${V.crumpleFade(V.rival)}`);
check("cache v=296 after super spend freeze leftover live", /game.js\?v=350/.test(htmlTxt));

check("empty-K leftover destRect plant eases leftover destRect plant", /function boltLeftoverPlanting/.test(srcTxt) && /Empty-K leftover destRect plant/.test(srcTxt) && /startBolt zeroed leftover k/.test(srcTxt) && /leftover destRect plant used to dump/.test(srcTxt) && /hopped off the windup plant/.test(srcTxt) && /Ease leftover k like slash leftover/.test(srcTxt));
check("empty-K leftover destRect plant is destRect-only ease", /destRect-only ease \(boltLeftoverPlanting\)/.test(srcTxt) && /boltLeftoverPlanting/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && /poseBitmap still windup immediately/.test(srcTxt) && /boltLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "tickGuardPose")) && /boltLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "guardRaiseK")) && /Ease leftover k \(boltLeftoverPlanting\)/.test(sliceFn(srcTxt, "startBolt")) && !/guardPoseK = 0/.test(sliceFn(codeOnly, "startBolt")) && !/boltLeftoverPlanting/.test(sliceFn(codeOnly, "destRect")) && !/boltLeftoverPlanting/.test(sliceFn(codeOnly, "bodyAABB")) && !/boltLeftoverPlanting/.test(sliceFn(codeOnly, "poseBitmap")) && !/boltLeftoverPlanting/.test(sliceFn(codeOnly, "slashPose")) && !/boltLeftoverPlanting/.test(sliceFn(codeOnly, "landHit")) && !/boltLeftoverPlanting/.test(sliceFn(codeOnly, "spawnBolt")));
check("empty-K leftover destRect plant poseBitmap still throw_knife", /f\.boltPhase === "startup" && ready\(throwK\)/.test(sliceFn(codeOnly, "poseBitmap")) && /poseBitmap still windup immediately/.test(srcTxt) && !/boltLeftoverPlanting/.test(sliceFn(codeOnly, "poseBitmap")) && !/guardPoseK/.test(sliceFn(codeOnly, "poseBitmap")));
check("empty-K leftover destRect plant does not re-fade leftover idle overlay", /Super freeze leftover idle overlay unchanged/.test(srcTxt) && /Telegraph still fades leftover idle/.test(srcTxt) && /BOLT_SUPER_STOP/.test(sliceFn(codeOnly, "telegraphFade")) && /Empty K still eases leftover idle/.test(srcTxt));
check("empty-K leftover destRect plant does not re-fade leftover block overlay", !/boltLeftoverPlanting/.test(sliceFn(codeOnly, "guardDropFade")) && !/boltLeftoverPlanting/.test(sliceFn(codeOnly, "reversalPlantFade")) && !/boltLeftoverPlanting/.test(sliceFn(codeOnly, "slashLeftoverPlanting")) && /guardDropFade idle/.test(srcTxt) && /leftover block overlay still dies/.test(srcTxt));
check("empty-K leftover destRect plant does not re-fade leftover sheathe", !/boltLeftoverPlanting/.test(sliceFn(codeOnly, "holdCutFade")) && !/boltLeftoverPlanting/.test(sliceFn(codeOnly, "linkPlantFade")) && !/boltLeftoverPlanting/.test(sliceFn(codeOnly, "clashPlantFade")) && /Special-cancel still snaps leftover k/.test(srcTxt) && /Clash-K leftover destRect plant already 0/.test(srcTxt));
check("empty-K leftover destRect plant extra rot stays 0", /Extra destRect rot stays 0/.test(srcTxt) && /leftover guard rot eases with leftover k/.test(srcTxt) && /Raise still 0ms/.test(srcTxt) && /AABB planted/.test(srcTxt));
check("empty-K leftover destRect plant is not a new combat verb", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60 && V.GUARD_RAISE_MS===80 && V.HITSTUN===350 && V.BOLT_STAM===30);
check("slash frames still 180/140/280 after empty-K leftover destRect plant", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after empty-K leftover destRect plant", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after empty-K leftover destRect plant", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after empty-K leftover destRect plant", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after empty-K leftover destRect plant", /game.js\?v=350/.test(htmlTxt));

function pinBoltYou(){
  freezeRivalAI();
  V.rival.x = 1220;
  V.rival.stunT = 0;
  V.rival.guarding = false;
  V.player.stunT = 0;
  V.player.falling = false;
  V.player.hp = 100;
  V.player.gait = 0;
  V.player.walkT = 0;
  V.player.walkSettleT = 0;
  V.player.sheatheT = 0;
  V.player.stamRegenT = 99999;
  V.keys.clear();
}

bootPlay(); wait(1700);
holdGuardSample();
V.player.guarding = false;
V.player.guardPoseK = 0;
V.update(STEP);
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
check("empty-K leftover destRect plant raise still 0ms block", V.player.guarding && V.player.guardPoseK>0.8 && V.boltLeftoverPlanting(V.player)===false && V.guardDropFade(V.player)===0 && isBlockSheet(V.player), `g=${V.player.guarding} k=${V.player.guardPoseK.toFixed(3)} bl=${V.boltLeftoverPlanting(V.player)} src=${sheetSrc(V.player)}`);
const bkAabbHold = V.bodyAABB(V.player);
const bkDestHold = V.destRect(V.player);
const bkKHold = V.player.guardPoseK;
V.player.stamina = 100;
V.player.stamRegenT = 99999;
V.player.meter = 0;
V.player.meterFlashT = 0;
V.player.guarding = false;
V.player.sheatheT = 0;
const bkOk = V.startBolt(V.player);
const bkFade0 = V.guardRaiseK(V.player);
const bkDest0 = V.destRect(V.player);
const bkAabb0 = V.bodyAABB(V.player);
check("empty-K leftover destRect plant trip leftover plant stays", bkOk && V.player.boltPhase==="startup" && V.player.boltSuper===false && V.boltLeftoverPlanting(V.player) && bkFade0>0.8 && V.player.guarding===false && V.player.guardPoseK===bkKHold && V.hitstopLeft===0 && V.reversalPlantFade(V.player)===0 && V.guardDropFade(V.player)===0 && V.slashLeftoverPlanting(V.player)===false, `ok=${bkOk} bph=${V.player.boltPhase} super=${V.player.boltSuper} bl=${V.boltLeftoverPlanting(V.player)} gk=${bkFade0} k=${V.player.guardPoseK.toFixed(3)} k0=${bkKHold} hs=${V.hitstopLeft} gf=${V.guardDropFade(V.player)}`);
check("empty-K leftover destRect plant poseBitmap throw_knife immediately", /throw_knife/.test(sheetSrc(V.player)) && !isBlockSheet(V.player) && !isCutSheet(V.player) && !isHurtSheet(V.player), `src=${sheetSrc(V.player)}`);
check("empty-K leftover destRect plant destRect leftover plant eases not snaps", Math.abs(bkDest0.rot)>0.04 && Math.abs(bkDest0.rot-bkDestHold.rot)<0.06 && Math.abs(bkDest0.dy-bkDestHold.dy)<1.6, `rot0=${bkDestHold.rot.toFixed(3)} rot=${bkDest0.rot.toFixed(3)} ddy=${(bkDest0.dy-bkDestHold.dy).toFixed(2)}`);
check("empty-K leftover destRect plant AABB planted at trip", Math.abs(bkAabb0.y-bkAabbHold.y)<0.05 && Math.abs(bkAabb0.h-bkAabbHold.h)<0.05, `dy=${(bkAabb0.y-bkAabbHold.y).toFixed(2)}`);
check("empty-K leftover destRect plant extra destRect rot 0 at trip", Math.abs(bkDest0.rot-bkDestHold.rot)<0.06, `drot=${(bkDest0.rot-bkDestHold.rot).toFixed(3)}`);
check("empty-K leftover destRect plant leftover idle still eases leftover idle overlay", V.telegraphing(V.player) && V.telegraphFade(V.player)>=0 && V.telegraphFade(V.player)<0.5 && V.player.telegraph===true, `tel=${V.player.telegraph} tf=${V.telegraphFade(V.player)} bph=${V.player.boltPhase}`);
let bkFadeMid=-1, bkDestMid=null, bkAabbMid=null, bkKMid=0;
for(let i=0;i<3;i++){
  pinBoltYou();
  V.update(STEP);
  bkFadeMid=V.guardRaiseK(V.player);
  bkDestMid=V.destRect(V.player);
  bkAabbMid=V.bodyAABB(V.player);
  bkKMid=V.player.guardPoseK;
}
check("empty-K leftover destRect plant leftover plant drops", bkFadeMid>=0 && bkFadeMid<bkFade0-0.08 && V.player.boltPhase==="startup" && V.player.guardPoseK>0 && bkKMid<bkKHold && V.boltLeftoverPlanting(V.player), `mid=${bkFadeMid} t0=${bkFade0} k=${bkKMid} k0=${bkKHold} bph=${V.player.boltPhase}`);
check("empty-K leftover destRect plant destRect eases leftover plant", bkDestMid && Math.abs(bkDestMid.rot)<Math.abs(bkDestHold.rot)-0.01 && Math.abs(bkDestMid.rot)<0.12, `rotHold=${bkDestHold.rot.toFixed(3)} rotMid=${bkDestMid.rot.toFixed(3)}`);
check("empty-K leftover destRect plant AABB planted through ease", bkAabbMid && Math.abs(bkAabbMid.y-bkAabbHold.y)<0.05 && Math.abs(bkAabbMid.h-bkAabbHold.h)<0.05, `dy=${(bkAabbMid.y-bkAabbHold.y).toFixed(2)}`);
let bkGone=false;
for(let i=0;i<5;i++){
  pinBoltYou();
  V.update(STEP);
  if(V.player.guardPoseK===0 && V.guardRaiseK(V.player)===0 && V.player.boltPhase==="startup"){
    bkGone=true;
    break;
  }
}
check("empty-K leftover destRect plant leftover plant gone while plant holds", bkGone && V.player.guardPoseK===0 && V.guardRaiseK(V.player)===0 && V.player.boltPhase==="startup" && V.player.boltSuper===false && /throw_knife/.test(sheetSrc(V.player)), `gone=${bkGone} k=${V.player.guardPoseK} gk=${V.guardRaiseK(V.player)} bph=${V.player.boltPhase} src=${sheetSrc(V.player)}`);

bootPlay(); wait(1700);
freezeRivalAI(); freezeYouAI();
V.player.x = 180; V.rival.x = 1100;
V.player.stamina = 100; V.player.stamRegenT = 99999;
V.player.meter = 0; V.player.meterFlashT = 0;
V.player.sheatheT = 0; V.player.guarding = false; V.player.guardPoseK = 0;
V.player.phase = "idle"; V.player.phaseT = 0; V.player.boltPhase = "";
V.hitstopLeft = 0;
const ekIdleOk = V.startBolt(V.player);
check("idle empty K leftover destRect plant extra rot still 0", ekIdleOk && V.player.boltPhase==="startup" && V.player.boltSuper===false && V.player.guardPoseK===0 && V.guardRaiseK(V.player)===0 && Math.abs(V.destRect(V.player).rot)<0.001 && V.boltLeftoverPlanting(V.player) && V.hitstopLeft===0, `ok=${ekIdleOk} bph=${V.player.boltPhase} k=${V.player.guardPoseK} gk=${V.guardRaiseK(V.player)} rot=${V.destRect(V.player).rot.toFixed(3)} bl=${V.boltLeftoverPlanting(V.player)}`);

bootPlay(); wait(1700);
holdGuardSample();
V.player.guarding = false;
V.player.guardPoseK = 0;
V.update(STEP);
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
V.player.stamina = 100;
V.player.guarding = false;
V.startAttack(V.player, "golpe");
check("golpe leftover k eases leftover destRect plant after empty-K leftover destRect plant", V.player.cut==="golpe" && V.player.phase==="startup" && V.golpeLeftoverPlanting(V.player) && V.boltLeftoverPlanting(V.player)===false && V.slashLeftoverPlanting(V.player)===false && V.guardRaiseK(V.player)>0.8 && V.player.guardPoseK>0.8, `cut=${V.player.cut} ph=${V.player.phase} gl=${V.golpeLeftoverPlanting(V.player)} bl=${V.boltLeftoverPlanting(V.player)} gk=${V.guardRaiseK(V.player)} k=${V.player.guardPoseK}`);

bootPlay(); wait(1700);
holdGuardSample();
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
V.player.stamina = 100;
const bkThrowOk = V.startThrow(V.player);
check("throw-from-guard plant leftover eases leftover k after empty-K leftover destRect plant", bkThrowOk && V.player.cut==="throw" && V.player.phase==="startup" && V.throwGuardPlanting(V.player) && V.boltLeftoverPlanting(V.player)===false && V.throwGuardPlantFade(V.player)>0.8 && V.guardRaiseK(V.player)>0.8 && V.player.guardPoseK>0.8, `ok=${bkThrowOk} cut=${V.player.cut} ph=${V.player.phase} tgf=${V.throwGuardPlantFade(V.player)} bl=${V.boltLeftoverPlanting(V.player)} gk=${V.guardRaiseK(V.player)} k=${V.player.guardPoseK}`);

bootPlay(); wait(1700);
holdGuardSample();
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
V.player.stamina = 100;
V.player.guarding = false;
V.startAttack(V.player);
check("slash leftover k still eases leftover destRect plant after empty-K leftover destRect plant", V.player.cut==="slash" && V.player.phase==="startup" && V.slashLeftoverPlanting(V.player) && V.boltLeftoverPlanting(V.player)===false && V.guardRaiseK(V.player)>0.8 && V.player.guardPoseK>0.8, `cut=${V.player.cut} ph=${V.player.phase} sl=${V.slashLeftoverPlanting(V.player)} bl=${V.boltLeftoverPlanting(V.player)} gk=${V.guardRaiseK(V.player)}`);

landCloseSlash();
const bkScGot = waitUntilBoltWindow("slash");
V.player.stamRegenT = 99999;
V.player.meter = 0;
const scKHold = V.player.guardPoseK;
V.boltEdge = true;
V.update(STEP);
check("special-cancel leftover k still snaps after empty-K leftover destRect plant", bkScGot && V.player.boltPhase==="startup" && V.player.boltHoldCut && V.boltLeftoverPlanting(V.player)===false && V.player.guardPoseK===0 && V.guardRaiseK(V.player)===0 && isCutSheet(V.player), `got=${bkScGot} bph=${V.player.boltPhase} hold=${V.player.boltHoldCut} bl=${V.boltLeftoverPlanting(V.player)} k=${V.player.guardPoseK} k0=${scKHold} src=${sheetSrc(V.player)}`);

bootPlay(); wait(1700);
holdRivalGuard();
V.rival.guardPoseK = 0;
V.rival.guarding = false;
V.rival.wantBlock = false;
V.update(STEP);
for(let i=0;i<10;i++){ holdRivalGuard(); V.update(STEP); }
check("empty-K leftover destRect plant rival raise still block", V.rival.guarding && V.rival.guardPoseK===1 && V.boltLeftoverPlanting(V.rival)===false && isBlockSheet(V.rival), `g=${V.rival.guarding} k=${V.rival.guardPoseK} bl=${V.boltLeftoverPlanting(V.rival)} src=${sheetSrc(V.rival)}`);
const rbkAabbHold = V.bodyAABB(V.rival);
const rbkDestHold = V.destRect(V.rival);
const rbkKHold = V.rival.guardPoseK;
V.rival.stamina = 100;
V.rival.stamRegenT = 99999;
V.rival.meter = 0;
V.rival.guarding = false;
V.rival.sheatheT = 0;
const rbkOk = V.startBolt(V.rival);
check("empty-K leftover destRect plant rival leftover plant stays", rbkOk && V.rival.boltPhase==="startup" && V.rival.boltSuper===false && V.boltLeftoverPlanting(V.rival) && V.guardRaiseK(V.rival)>0.8 && V.rival.guardPoseK===rbkKHold && !isBlockSheet(V.rival) && /throw_knife/.test(sheetSrc(V.rival)), `ok=${rbkOk} bph=${V.rival.boltPhase} bl=${V.boltLeftoverPlanting(V.rival)} gk=${V.guardRaiseK(V.rival)} k=${V.rival.guardPoseK} src=${sheetSrc(V.rival)}`);
check("empty-K leftover destRect plant rival destRect leftover plant eases", Math.abs(V.destRect(V.rival).rot)>0.04 && Math.abs(V.destRect(V.rival).rot-rbkDestHold.rot)<0.06, `rot0=${rbkDestHold.rot.toFixed(3)} rot=${V.destRect(V.rival).rot.toFixed(3)}`);
check("empty-K leftover destRect plant rival AABB planted at trip", Math.abs(V.bodyAABB(V.rival).y-rbkAabbHold.y)<0.05, `dy=${(V.bodyAABB(V.rival).y-rbkAabbHold.y).toFixed(2)}`);

bootPlay(); wait(1700);
V.player.phase="idle"; V.player.phaseT=0; V.player.cut="slash"; V.player.guarding=false; V.player.guardPoseK=0;
V.player.stunT=0; V.player.falling=false; V.player.boltPhase=""; V.player.reversal=false;
check("chip stun is not empty-K leftover destRect plant", V.boltLeftoverPlanting(V.player)===false, `bl=${V.boltLeftoverPlanting(V.player)}`);
V.player.stunT = V.HITSTUN;
check("chip stun leftover still snaps empty-K leftover destRect plant", V.boltLeftoverPlanting(V.player)===false && V.player.stunT>0 && !isHurtSheet(V.player), `bl=${V.boltLeftoverPlanting(V.player)} st=${V.player.stunT} src=${sheetSrc(V.player)}`);

const bkThrowKd = landCloseThrow();
check("throw knockdown still not KO crumple after empty-K leftover destRect plant", bkThrowKd && V.rival.hp===80 && V.rival.thrownT>0 && !V.rival.falling && V.crumpleFade(V.rival)===0 && V.boltLeftoverPlanting(V.rival)===false, `ok=${bkThrowKd} hp=${V.rival.hp} th=${V.rival.thrownT} cf=${V.crumpleFade(V.rival)} bl=${V.boltLeftoverPlanting(V.rival)}`);
check("cache v=296 after empty-K leftover destRect plant live", /game.js\?v=350/.test(htmlTxt));

// --- v195 throw-from-guard plant leftover: leftover block→windup fade (Space+S) ---
check("throw-from-guard plant leftover fades leftover block into windup", /function throwGuardPlantFade/.test(srcTxt) && /function throwGuardPlanting/.test(srcTxt) && /Throw-from-guard plant leftover/.test(srcTxt) && /leftover block used to pop to windup/.test(srcTxt) && /Space\+S from guarda was a sheet cut/.test(srcTxt) && /Fade leftover block→windup/.test(srcTxt));
check("throw-from-guard plant leftover is draw-only leftover", /throwGuardPlantFade/.test(sliceFn(codeOnly, "drawKnight")) && /throwGuardPlantFade/.test(sliceFn(codeOnly, "sheatheFade")) && /throwGuardPlanting\(f\)/.test(sliceFn(codeOnly, "tickGuardPose")) && /throwGuardPlanting\(f\)/.test(sliceFn(codeOnly, "guardRaiseK")) && !/throwGuardPlantFade/.test(sliceFn(codeOnly, "destRect")) && !/throwGuardPlantFade/.test(sliceFn(codeOnly, "bodyAABB")) && !/throwGuardPlantFade/.test(sliceFn(codeOnly, "poseBitmap")) && !/throwGuardPlantFade/.test(sliceFn(codeOnly, "landHit")) && !/throwGuardPlantFade/.test(sliceFn(codeOnly, "landThrow")));
check("throw-from-guard plant leftover poseBitmap still windup", /f\.cut === "throw" && \(f\.phase === "startup"/.test(sliceFn(codeOnly, "poseBitmap")) && /poseBitmap still windup immediately/.test(srcTxt) && !/throwGuard/.test(sliceFn(codeOnly, "poseBitmap")));
check("throw-from-guard plant leftover eases leftover k", /Throw-from-guard plant leftover: leftover k used to snap here/.test(srcTxt) && !/f\.guardPoseK = 0/.test(sliceFn(codeOnly, "startThrow")) && /throwGuardPlanting\(f\)/.test(sliceFn(codeOnly, "tickGuardPose")) && /return guardRaiseK\(f\)/.test(sliceFn(codeOnly, "throwGuardPlantFade")));
check("throw-from-guard plant leftover does not re-fade throw recovery", /Throw recovery leftover unchanged/.test(srcTxt) && /throwPlantFade/.test(srcTxt) && /phase !== "recovery"/.test(sliceFn(codeOnly, "throwPlantFade")) && /cut !== "throw"/.test(sliceFn(codeOnly, "throwGuardPlanting")));
check("throw-from-guard plant leftover does not re-fade reversal plant", /reversalPlantFade/.test(srcTxt) && /restThrowGuard/.test(sliceFn(codeOnly, "drawKnight")) && /!restRev/.test(sliceFn(codeOnly, "drawKnight")));
check("throw-from-guard plant leftover golpe leftover k eases", /Golpe leftover k eases leftover destRect plant/.test(srcTxt) && /function golpeLeftoverPlanting/.test(srcTxt) && !/if \(kind === "golpe"\) f\.guardPoseK = 0/.test(sliceFn(codeOnly, "startAttack")));
check("throw-from-guard plant leftover telegraph idle/rival still fades", /Idle\/rival telegraph throw still/.test(srcTxt) && /fades idle→windup/.test(srcTxt) && /f\.telegraph = fromIdle/.test(sliceFn(codeOnly, "startThrow")));
check("throw-from-guard plant leftover extra rot stays 0", /Extra destRect rot stays 0/.test(srcTxt) && /leftover guard rot eases with leftover k/.test(srcTxt) && /Raise still 0ms/.test(srcTxt) && /AABB planted/.test(srcTxt));
check("throw-from-guard plant leftover is not a new combat verb", V.THROW_STARTUP===80 && V.THROW_ACTIVE===40 && V.THROW_RECOVERY===220 && V.THROW_RANGE===120 && V.THROW_DMG===20 && V.GUARD_RAISE_MS===80 && V.HITSTUN===350 && V.REVERSAL_STAM===30);
check("slash frames still 180/140/280 after throw-from-guard plant leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after throw-from-guard plant leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after throw-from-guard plant leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after throw-from-guard plant leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after throw-from-guard plant leftover", /game.js\?v=350/.test(htmlTxt));

function pinThrowYou(){
  freezeRivalAI();
  V.player.stunT = 0;
  V.player.falling = false;
  V.player.hp = 100;
  V.player.boltPhase = "";
  V.player.gait = 0;
  V.player.walkT = 0;
  V.player.walkSettleT = 0;
  V.player.sheatheT = 0;
  V.hitstopLeft = 0;
  V.keys.clear();
}

bootPlay(); wait(1700);
holdGuardSample();
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
check("throw-from-guard plant leftover raise still 0ms block", V.player.guarding && V.player.guardPoseK>0.8 && V.throwGuardPlantFade(V.player)===0 && V.reversalPlantFade(V.player)===0 && V.guardDropFade(V.player)===0 && isBlockSheet(V.player), `g=${V.player.guarding} k=${V.player.guardPoseK.toFixed(3)} tgf=${V.throwGuardPlantFade(V.player)} rf=${V.reversalPlantFade(V.player)} gf=${V.guardDropFade(V.player)} src=${sheetSrc(V.player)}`);
const tgAabbHold = V.bodyAABB(V.player);
const tgDestHold = V.destRect(V.player);
const tgKHold = V.player.guardPoseK;
V.player.stamina = 100;
const tgOk = V.startThrow(V.player);
const tgFade0 = V.throwGuardPlantFade(V.player);
const tgDest0 = V.destRect(V.player);
const tgAabb0 = V.bodyAABB(V.player);
check("throw-from-guard plant leftover trip fade starts high", tgOk && tgFade0>0.8 && V.player.cut==="throw" && V.player.phase==="startup" && V.throwGuardPlanting(V.player) && V.player.guarding===false && V.player.guardPoseK===tgKHold && V.player.telegraph===false && V.telegraphFade(V.player)===0 && V.reversalPlantFade(V.player)===0, `ok=${tgOk} fade=${tgFade0} k=${V.player.guardPoseK.toFixed(3)} k0=${tgKHold} g=${V.player.guarding} tel=${V.player.telegraph}`);
check("throw-from-guard plant leftover poseBitmap windup immediately", /windup/.test(sheetSrc(V.player)) && !isBlockSheet(V.player) && !isCutSheet(V.player) && !isHurtSheet(V.player), `src=${sheetSrc(V.player)}`);
check("throw-from-guard plant leftover destRect leftover k eases not snaps", Math.abs(tgDest0.rot)>0.04 && Math.abs(tgDest0.rot-tgDestHold.rot)<0.06, `rot0=${tgDestHold.rot.toFixed(3)} rot=${tgDest0.rot.toFixed(3)} ddy=${(tgDest0.dy-tgDestHold.dy).toFixed(2)}`);
check("throw-from-guard plant leftover AABB planted at trip", Math.abs(tgAabb0.y-tgAabbHold.y)<0.05 && Math.abs(tgAabb0.h-tgAabbHold.h)<0.05, `dy=${(tgAabb0.y-tgAabbHold.y).toFixed(2)}`);
check("throw-from-guard plant leftover extra destRect rot 0 at trip", Math.abs(tgDest0.rot-tgDestHold.rot)<0.06, `drot=${(tgDest0.rot-tgDestHold.rot).toFixed(3)}`);
V.player.sheatheT = 140;
check("throw-from-guard plant leftover skips sheathe overlay live", V.throwGuardPlantFade(V.player)>0 && V.sheatheFade(V.player)===0, `tgf=${V.throwGuardPlantFade(V.player)} sf=${V.sheatheFade(V.player)} sh=${V.player.sheatheT}`);
V.player.sheatheT = 0;
let tgFadeMid=-1, tgDestMid=null, tgAabbMid=null, tgKMid=0;
for(let i=0;i<3;i++){
  pinThrowYou();
  V.update(STEP);
  tgFadeMid=V.throwGuardPlantFade(V.player);
  tgDestMid=V.destRect(V.player);
  tgAabbMid=V.bodyAABB(V.player);
  tgKMid=V.player.guardPoseK;
}
check("throw-from-guard plant leftover fade drops", tgFadeMid>=0 && tgFadeMid<tgFade0-0.08 && V.player.cut==="throw" && V.player.phase==="startup" && V.player.guardPoseK>0 && tgKMid<tgKHold, `mid=${tgFadeMid} t0=${tgFade0} k=${tgKMid} k0=${tgKHold}`);
check("throw-from-guard plant leftover destRect eases plant", tgDestMid && Math.abs(tgDestMid.rot)<Math.abs(tgDestHold.rot)-0.01 && Math.abs(tgDestMid.rot)<0.12, `rotHold=${tgDestHold.rot.toFixed(3)} rotMid=${tgDestMid.rot.toFixed(3)}`);
check("throw-from-guard plant leftover AABB planted through ease", tgAabbMid && Math.abs(tgAabbMid.y-tgAabbHold.y)<0.05 && Math.abs(tgAabbMid.h-tgAabbHold.h)<0.05, `dy=${(tgAabbMid.y-tgAabbHold.y).toFixed(2)}`);
let tgGone=false;
for(let i=0;i<12;i++){
  pinThrowYou();
  V.update(STEP);
  // THROW_STARTUP === GUARD_RAISE_MS: fade ends as startup flips to active.
  if(V.player.guardPoseK===0 && V.throwGuardPlantFade(V.player)===0 && V.player.cut==="throw" && (V.player.phase==="startup" || V.player.phase==="active")){
    tgGone=true;
    break;
  }
}
check("throw-from-guard plant leftover fade gone while throw plant holds", tgGone && V.player.guardPoseK===0 && V.throwGuardPlantFade(V.player)===0 && V.player.cut==="throw" && (V.player.phase==="startup" || V.player.phase==="active") && /windup/.test(sheetSrc(V.player)), `gone=${tgGone} k=${V.player.guardPoseK} fade=${V.throwGuardPlantFade(V.player)} ph=${V.player.phase} src=${sheetSrc(V.player)}`);

bootPlay(); wait(1700);
holdGuardSample();
V.player.guarding = false;
V.player.guardPoseK = 0;
V.update(STEP);
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
V.player.stamina = 100;
V.player.guarding = false;
V.startAttack(V.player, "golpe");
check("golpe leftover k eases leftover destRect plant after throw-from-guard plant leftover", V.player.cut==="golpe" && V.player.phase==="startup" && V.golpeLeftoverPlanting(V.player) && V.throwGuardPlanting(V.player)===false && V.guardRaiseK(V.player)>0.8 && V.player.guardPoseK>0.8, `cut=${V.player.cut} ph=${V.player.phase} gl=${V.golpeLeftoverPlanting(V.player)} tgp=${V.throwGuardPlanting(V.player)} gk=${V.guardRaiseK(V.player)} k=${V.player.guardPoseK}`);

bootPlay(); wait(1700);
holdGuardSample();
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
V.player.stamina = 100;
const tgRevOk = V.startReversal(V.player);
check("reversal plant leftover still eases leftover k after throw-from-guard plant leftover", tgRevOk && V.player.cut==="golpe" && V.player.phase==="startup" && V.player.reversal && V.throwGuardPlantFade(V.player)===0 && V.reversalPlantFade(V.player)>0.8 && V.guardRaiseK(V.player)>0.8, `ok=${tgRevOk} cut=${V.player.cut} ph=${V.player.phase} tgf=${V.throwGuardPlantFade(V.player)} rf=${V.reversalPlantFade(V.player)} gk=${V.guardRaiseK(V.player)}`);

bootPlay(); wait(1700);
holdGuardSample();
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
V.player.stamina = 100;
V.player.guarding = false;
V.startAttack(V.player);
check("slash leftover k still eases leftover destRect plant after throw-from-guard plant leftover", V.player.cut==="slash" && V.player.phase==="startup" && V.slashLeftoverPlanting(V.player) && V.throwGuardPlanting(V.player)===false && V.guardRaiseK(V.player)>0.8 && V.player.guardPoseK>0.8, `cut=${V.player.cut} ph=${V.player.phase} sl=${V.slashLeftoverPlanting(V.player)} tgp=${V.throwGuardPlanting(V.player)} gk=${V.guardRaiseK(V.player)}`);

bootPlay(); wait(1700);
V.player.phase="idle"; V.player.phaseT=0; V.player.cut="slash"; V.player.guarding=false; V.player.guardPoseK=0;
V.player.stunT=0; V.player.falling=false; V.player.boltPhase=""; V.player.reversal=false;
check("chip stun is not throw-from-guard plant leftover", V.throwGuardPlantFade(V.player)===0, `fade=${V.throwGuardPlantFade(V.player)}`);
V.player.stunT = V.HITSTUN;
check("chip stun leftover still snaps throw-from-guard plant leftover", V.throwGuardPlantFade(V.player)===0 && V.player.stunT>0 && !isHurtSheet(V.player), `fade=${V.throwGuardPlantFade(V.player)} st=${V.player.stunT} src=${sheetSrc(V.player)}`);

const tgThrowKd = landCloseThrow();
check("throw knockdown still not KO crumple after throw-from-guard plant leftover", tgThrowKd && V.rival.hp===80 && V.rival.thrownT>0 && !V.rival.falling && V.crumpleFade(V.rival)===0 && V.throwGuardPlantFade(V.rival)===0, `ok=${tgThrowKd} hp=${V.rival.hp} th=${V.rival.thrownT} cf=${V.crumpleFade(V.rival)} fade=${V.throwGuardPlantFade(V.rival)}`);
check("cache v=296 after throw-from-guard plant leftover live", /game.js\?v=350/.test(htmlTxt));

// --- v196 walk-out guard drop leftover: block fade survives A/D mid-drop ---
check("walk-out guard drop leftover keeps fade through walk", /Walk-out guard drop leftover/.test(srcTxt) && /walking used to zero the fade/.test(srcTxt) && /Keep leftover block through leftover guardPoseK/.test(srcTxt) && /same hole wakeup walk-out already closed/.test(srcTxt));
check("walk-out guard drop leftover is draw-only sheet", /Draw-only \(guardDropFade\)/.test(srcTxt) && /guardDropFade\(f\)/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && /poseBitmap still idle \/ walk/.test(srcTxt) && !/walking\(f\)\) return 0/.test(sliceFn(codeOnly, "guardDropFade")) && !/guardDropFade/.test(sliceFn(codeOnly, "destRect")) && !/guardDropFade/.test(sliceFn(codeOnly, "bodyAABB")) && !/guardDropFade/.test(sliceFn(codeOnly, "slashPose")) && !/guardDropFade/.test(sliceFn(codeOnly, "landHit")) && !/guardDropFade/.test(sliceFn(codeOnly, "tickGait")));
check("walk-out guard drop leftover still uses leftover raise k", /guardRaiseK\(f\)/.test(sliceFn(codeOnly, "guardDropFade")) && /guarding/.test(sliceFn(codeOnly, "guardDropFade")) && /restGuard/.test(sliceFn(codeOnly, "drawKnight")));
check("walk-out guard drop leftover stun still cuts", /f\.stunT > 0/.test(sliceFn(codeOnly, "guardDropFade")) && /Stun still cuts/.test(srcTxt));
check("walk-out guard drop leftover wakeup walk-out still held", /Walk-out on getup used to zero the fade/.test(srcTxt) && /wakeupFade/.test(srcTxt));
check("walk-out guard drop leftover throw-from-guard plant still eases", /throwGuardPlantFade/.test(srcTxt) && /Throw-from-guard plant leftover/.test(srcTxt));
check("walk-out guard drop leftover is not a new combat verb", V.GUARD_RAISE_MS===80 && V.GUARD_COMMIT_MS===140 && V.STAMINA_BLOCK===20 && V.GUARD_BREAK_MS===400 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80 && V.PUSHBLOCK_STAM===25 && V.PUSHBLOCK_PX===240);
check("slash frames still 180/140/280 after walk-out guard drop leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after walk-out guard drop leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after walk-out guard drop leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after walk-out guard drop leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after walk-out guard drop leftover", /game.js\?v=350/.test(htmlTxt));

bootPlay(); wait(1700);
holdGuardSample();
V.player.guarding = false;
V.player.guardPoseK = 0;
V.update(STEP);
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
check("walk-out guard drop leftover raise still 0ms block", V.player.guarding && V.player.guardPoseK>0.8 && V.guardDropFade(V.player)===0 && isBlockSheet(V.player), `g=${V.player.guarding} k=${V.player.guardPoseK.toFixed(3)} fade=${V.guardDropFade(V.player)} src=${sheetSrc(V.player)}`);
const woAabbHold = V.bodyAABB(V.player);
const woDestHold = V.destRect(V.player);
const woKHold = V.player.guardPoseK;
holdGuardSample();
V.keys.clear();
V.player.guarding = false;
V.update(STEP);
const woFade0 = V.guardDropFade(V.player);
const woK0 = V.player.guardPoseK;
check("walk-out guard drop leftover fade starts high standing", woFade0>0.8 && V.player.guarding===false && woK0>0.5 && !isBlockSheet(V.player), `fade=${woFade0} k=${woK0.toFixed(3)} k0=${woKHold} g=${V.player.guarding} src=${sheetSrc(V.player)}`);
// Step off mid-drop: leftover block used to dump the same tick walking armed.
freezeRivalAI(); freezeYouAI();
V.keys.clear();
V.keys.add("KeyA");
V.player.stunT=0; V.player.falling=false; V.player.phase="idle"; V.player.boltPhase=""; V.player.guardBreakT=0; V.hitstopLeft=0;
V.update(STEP);
const woFadeWalk = V.guardDropFade(V.player);
const woDestWalk = V.destRect(V.player);
const woAabbWalk = V.bodyAABB(V.player);
check("walk-out guard drop leftover fade holds through walk", woFadeWalk>0.5 && V.player.guarding===false && V.player.gait===-1 && V.player.guardPoseK>0 && V.player.phase==="idle", `fade=${woFadeWalk} fade0=${woFade0} k=${V.player.guardPoseK.toFixed(3)} gait=${V.player.gait} g=${V.player.guarding}`);
check("walk-out guard drop leftover poseBitmap not block through walk", !isBlockSheet(V.player) && !isCutSheet(V.player) && !/windup/.test(sheetSrc(V.player)), `src=${sheetSrc(V.player)} gait=${V.player.gait}`);
check("walk-out guard drop leftover AABB planted through walk", Math.abs(woAabbWalk.y-woAabbHold.y)<0.05 && Math.abs(woAabbWalk.h-woAabbHold.h)<0.05, `dy=${(woAabbWalk.y-woAabbHold.y).toFixed(2)}`);
check("walk-out guard drop leftover destRect leftover k eases through walk", Math.abs(woDestWalk.rot)<Math.abs(woDestHold.rot)+0.02 && Math.abs(woDestWalk.dy-woDestHold.dy)<8, `rotHold=${woDestHold.rot.toFixed(3)} rot=${woDestWalk.rot.toFixed(3)} ddy=${(woDestWalk.dy-woDestHold.dy).toFixed(2)}`);
check("walk-out guard drop leftover skips sheathe overlay through walk", V.guardDropFade(V.player)>0 && V.sheatheFade(V.player)===0, `gf=${V.guardDropFade(V.player)} sf=${V.sheatheFade(V.player)}`);
check("walk-out guard drop leftover extra destRect rot 0 through walk", Math.abs(woDestWalk.rot)<0.2, `rot=${woDestWalk.rot.toFixed(3)}`);
let woFadeMid=-1;
freezeRivalAI(); freezeYouAI();
V.keys.clear();
V.keys.add("KeyA");
V.player.stunT=0; V.player.falling=false; V.player.phase="idle"; V.player.boltPhase=""; V.player.guardBreakT=0; V.hitstopLeft=0;
V.update(STEP);
woFadeMid=V.guardDropFade(V.player);
check("walk-out guard drop leftover fade drops while walking", woFadeMid>=0 && woFadeMid<woFadeWalk-0.05 && V.player.gait===-1 && V.player.guardPoseK>0, `mid=${woFadeMid} walk=${woFadeWalk} k=${V.player.guardPoseK} gait=${V.player.gait}`);
for(let i=0;i<12;i++){
  freezeRivalAI(); freezeYouAI();
  V.keys.clear();
  V.keys.add("KeyA");
  V.player.stunT=0; V.player.falling=false; V.player.phase="idle"; V.player.boltPhase=""; V.player.guardBreakT=0; V.hitstopLeft=0;
  V.update(STEP);
}
check("walk-out guard drop leftover fade gone while walk holds", V.player.guardPoseK===0 && V.guardDropFade(V.player)===0 && V.player.gait===-1 && !isBlockSheet(V.player), `k=${V.player.guardPoseK} fade=${V.guardDropFade(V.player)} gait=${V.player.gait} src=${sheetSrc(V.player)}`);

bootPlay(); wait(1700);
holdGuardSample();
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
holdGuardSample();
V.keys.clear();
V.player.guarding = false;
V.update(STEP);
V.player.gait = -1;
V.player.walkT = V.WALK_STEP_MS * 0.5;
V.player.stunT = V.HITSTUN;
check("walk-out guard drop leftover stun still cuts through walk", V.guardDropFade(V.player)===0 && V.player.stunT>0 && V.stunLeftoverPlanting(V.player) && V.guardRaiseK(V.player)>0.5, `gf=${V.guardDropFade(V.player)} st=${V.player.stunT} gk=${V.guardRaiseK(V.player)} gait=${V.player.gait}`);

bootPlay(); wait(1700);
holdGuardSample();
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
V.player.stamina = 100;
const woRevOk = V.startReversal(V.player);
check("reversal plant leftover still eases after walk-out guard drop leftover", woRevOk && V.player.cut==="golpe" && V.player.phase==="startup" && V.player.reversal && V.guardDropFade(V.player)===0 && V.reversalPlantFade(V.player)>0.8 && V.guardRaiseK(V.player)>0.8, `ok=${woRevOk} cut=${V.player.cut} gf=${V.guardDropFade(V.player)} rf=${V.reversalPlantFade(V.player)} gk=${V.guardRaiseK(V.player)}`);

bootPlay(); wait(1700);
holdGuardSample();
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
V.player.stamina = 100;
const woThrowOk = V.startThrow(V.player);
check("throw-from-guard plant leftover still eases after walk-out guard drop leftover", woThrowOk && V.player.cut==="throw" && V.player.phase==="startup" && V.throwGuardPlantFade(V.player)>0.8 && V.guardDropFade(V.player)===0 && V.guardRaiseK(V.player)>0.8, `ok=${woThrowOk} cut=${V.player.cut} tgf=${V.throwGuardPlantFade(V.player)} gf=${V.guardDropFade(V.player)} gk=${V.guardRaiseK(V.player)}`);

bootPlay(); wait(1700);
holdRivalGuard();
V.rival.guardPoseK = 0;
V.rival.guarding = false;
V.rival.wantBlock = false;
V.update(STEP);
for(let i=0;i<10;i++){ holdRivalGuard(); V.update(STEP); }
check("walk-out guard drop leftover rival raise still block", V.rival.guarding && V.rival.guardPoseK===1 && V.guardDropFade(V.rival)===0 && isBlockSheet(V.rival), `g=${V.rival.guarding} k=${V.rival.guardPoseK} fade=${V.guardDropFade(V.rival)} src=${sheetSrc(V.rival)}`);
const rwoAabbHold = V.bodyAABB(V.rival);
V.rival.guarding = false;
V.rival.wantBlock = false;
V.rival.guardChoice = -1;
V.update(STEP);
const rwoFade0 = V.guardDropFade(V.rival);
freezeRivalAI(); freezeYouAI();
V.rival.phase = "idle";
V.rival.boltPhase = "";
V.rival.stunT = 0;
V.rival.falling = false;
V.rival.guarding = false;
V.rival.wantBlock = false;
V.rival.guardChoice = -1;
V.rival.gait = 1;
V.rival.walkT = V.WALK_STEP_MS * 0.5;
V.update(STEP);
// Rival AI may clear gait; pin walk leftover for the draw check.
V.rival.gait = 1;
V.rival.phase = "idle";
V.rival.guarding = false;
check("walk-out guard drop leftover rival fade holds through walk", rwoFade0>0.8 && V.guardDropFade(V.rival)>0.5 && V.rival.gait===1 && V.rival.guardPoseK>0 && !isBlockSheet(V.rival), `fade0=${rwoFade0} fade=${V.guardDropFade(V.rival)} k=${V.rival.guardPoseK} gait=${V.rival.gait} src=${sheetSrc(V.rival)}`);
check("walk-out guard drop leftover rival AABB planted through walk", Math.abs(V.bodyAABB(V.rival).y-rwoAabbHold.y)<0.05, `dy=${(V.bodyAABB(V.rival).y-rwoAabbHold.y).toFixed(2)}`);

bootPlay(); wait(1700);
V.player.phase="idle"; V.player.phaseT=0; V.player.cut="slash"; V.player.guarding=false; V.player.guardPoseK=0;
V.player.stunT=0; V.player.falling=false; V.player.boltPhase=""; V.player.reversal=false; V.player.gait=0;
check("chip stun is not walk-out guard drop leftover", V.guardDropFade(V.player)===0, `fade=${V.guardDropFade(V.player)}`);
V.player.stunT = V.HITSTUN;
check("chip stun leftover still snaps walk-out guard drop leftover", V.guardDropFade(V.player)===0 && V.player.stunT>0 && !isHurtSheet(V.player), `fade=${V.guardDropFade(V.player)} st=${V.player.stunT} src=${sheetSrc(V.player)}`);

const woThrowKd = landCloseThrow();
check("throw knockdown still not KO crumple after walk-out guard drop leftover", woThrowKd && V.rival.hp===80 && V.rival.thrownT>0 && !V.rival.falling && V.crumpleFade(V.rival)===0 && V.guardDropFade(V.rival)===0, `ok=${woThrowKd} hp=${V.rival.hp} th=${V.rival.thrownT} cf=${V.crumpleFade(V.rival)} fade=${V.guardDropFade(V.rival)}`);
check("cache v=296 after walk-out guard drop leftover live", /game.js\?v=350/.test(htmlTxt));

// --- v197 tech-from-guard plant leftover: leftover block→windup fade (Space+S tech) ---
check("tech-from-guard plant leftover fades leftover block into windup", /function techGuardPlantFade/.test(srcTxt) && /function techGuardPlanting/.test(srcTxt) && /Tech-from-guard plant leftover/.test(srcTxt) && /leftover block used to pop to windup/.test(srcTxt) && /Space\+S tech from guarda was a sheet cut/.test(srcTxt) && /Fade leftover block→windup/.test(srcTxt));
check("tech-from-guard plant leftover is draw-only leftover", /techGuardPlantFade/.test(sliceFn(codeOnly, "drawKnight")) && /techGuardPlantFade/.test(sliceFn(codeOnly, "sheatheFade")) && /techGuardPlanting\(f\)/.test(sliceFn(codeOnly, "tickGuardPose")) && /techGuardPlanting\(f\)/.test(sliceFn(codeOnly, "guardRaiseK")) && !/techGuardPlantFade/.test(sliceFn(codeOnly, "destRect")) && !/techGuardPlantFade/.test(sliceFn(codeOnly, "bodyAABB")) && !/techGuardPlantFade/.test(sliceFn(codeOnly, "poseBitmap")) && !/techGuardPlantFade/.test(sliceFn(codeOnly, "landHit")) && !/techGuardPlantFade/.test(sliceFn(codeOnly, "landThrow")));
check("tech-from-guard plant leftover poseBitmap still windup", /f\.cut === "throw" && \(f\.phase === "startup" \|\| f\.phase === "active" \|\| f\.phase === "recovery"\)/.test(sliceFn(codeOnly, "poseBitmap")) && /poseBitmap still windup immediately/.test(srcTxt) && !/techGuard/.test(sliceFn(codeOnly, "poseBitmap")));
check("tech-from-guard plant leftover eases leftover k", /Tech-from-guard plant leftover: leftover k used to snap here/.test(srcTxt) && /techGuardPlanting\(f\)/.test(sliceFn(codeOnly, "tickGuardPose")) && /return guardRaiseK\(f\)/.test(sliceFn(codeOnly, "techGuardPlantFade")));
check("tech-from-guard plant leftover does not re-fade throw recovery", /Throw recovery leftover unchanged/.test(srcTxt) && /throwPlantFade/.test(srcTxt) && /techRec/.test(sliceFn(codeOnly, "techGuardPlanting")));
check("tech-from-guard plant leftover idle tech still snaps", /Idle tech still snaps/.test(srcTxt) && /guardPoseK \|\| 0\) > 0/.test(sliceFn(codeOnly, "techGuardPlanting")));
check("tech-from-guard plant leftover golpe leftover k eases", /Golpe leftover k eases leftover destRect plant/.test(srcTxt) && /function golpeLeftoverPlanting/.test(srcTxt) && !/if \(kind === "golpe"\) f\.guardPoseK = 0/.test(sliceFn(codeOnly, "startAttack")));
check("tech-from-guard plant leftover is not a new combat verb", V.THROW_TECH_REC===160 && V.THROW_STARTUP===80 && V.THROW_ACTIVE===40 && V.THROW_RECOVERY===220 && V.THROW_RANGE===120 && V.GUARD_RAISE_MS===80 && V.PUSHBLOCK_STAM===25 && V.PUSHBLOCK_PX===240);
check("slash frames still 180/140/280 after tech-from-guard plant leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after tech-from-guard plant leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after tech-from-guard plant leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after tech-from-guard plant leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after tech-from-guard plant leftover", /game.js\?v=350/.test(htmlTxt));

bootPlay(); wait(1700);
holdGuardSample();
V.player.guarding = false;
V.player.guardPoseK = 0;
V.update(STEP);
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
check("tech-from-guard plant leftover raise still 0ms block", V.player.guarding && V.player.guardPoseK>0.8 && V.techGuardPlantFade(V.player)===0 && V.throwGuardPlantFade(V.player)===0 && V.guardDropFade(V.player)===0 && isBlockSheet(V.player), `g=${V.player.guarding} k=${V.player.guardPoseK.toFixed(3)} tecf=${V.techGuardPlantFade(V.player)} tgf=${V.throwGuardPlantFade(V.player)} gf=${V.guardDropFade(V.player)} src=${sheetSrc(V.player)}`);

bootPlay(); wait(1700);
holdGuardSample();
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
freezeRivalAI(); freezeYouAI();
V.player.x = 500; V.rival.x = 580;
V.player.stamina = 100;
V.keys.clear(); V.keys.add("KeyS");
for(let i=0;i<4;i++){ freezeRivalAI(); freezeYouAI(); V.keys.add("KeyS"); V.update(STEP); }
const tecAabbHold = V.bodyAABB(V.player);
const tecDestHold = V.destRect(V.player);
const tecKHold = V.player.guardPoseK;
V.rival.guarding = false; V.rival.phase = "idle"; V.rival.stunT = 0; V.rival.falling = false; V.rival.boltPhase = ""; V.rival.feintT = 0;
const tecThrowOk = V.startThrow(V.rival);
check("tech-from-guard plant leftover throw arms startup", tecThrowOk && V.rival.cut==="throw" && V.rival.phase==="startup" && V.player.guarding && tecKHold>0.8, `ok=${tecThrowOk} rcut=${V.rival.cut} rph=${V.rival.phase} g=${V.player.guarding} k=${tecKHold}`);
V.keys.clear();
const tecOk = V.tryThrowTech(V.player);
const tecFade0 = V.techGuardPlantFade(V.player);
check("tech-from-guard plant leftover trip fade starts high", tecOk && tecFade0>0.8 && V.player.cut==="throw" && V.player.phase==="recovery" && V.player.techRec && V.techGuardPlanting(V.player) && V.player.guarding===false && V.player.guardPoseK===tecKHold && V.throwPlantFade(V.player)>0.8 && V.throwGuardPlantFade(V.player)===0 && V.guardDropFade(V.player)===0, `ok=${tecOk} fade=${tecFade0} k=${V.player.guardPoseK.toFixed(3)} k0=${tecKHold} g=${V.player.guarding} trf=${V.throwPlantFade(V.player)} tgf=${V.throwGuardPlantFade(V.player)}`);
check("tech-from-guard plant leftover poseBitmap windup not block", /windup/.test(sheetSrc(V.player)) && !isBlockSheet(V.player), `src=${sheetSrc(V.player)}`);
check("tech-from-guard plant leftover skips sheathe overlay live", V.techGuardPlantFade(V.player)>0 && V.sheatheFade(V.player)===0, `tecf=${V.techGuardPlantFade(V.player)} sf=${V.sheatheFade(V.player)} sh=${V.player.sheatheT}`);
check("tech-from-guard plant leftover AABB planted at trip", Math.abs(V.bodyAABB(V.player).y-tecAabbHold.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-tecAabbHold.h)<0.05, `dy=${(V.bodyAABB(V.player).y-tecAabbHold.y).toFixed(2)}`);
check("tech-from-guard plant leftover destRect leftover k still planted", Math.abs(V.destRect(V.player).rot)<Math.abs(tecDestHold.rot)+0.02 && Math.abs(V.destRect(V.player).dy-tecDestHold.dy)<2.5, `rotHold=${tecDestHold.rot.toFixed(3)} rot=${V.destRect(V.player).rot.toFixed(3)} ddy=${(V.destRect(V.player).dy-tecDestHold.dy).toFixed(2)}`);
V.player.sheatheT = 0;
function pinTechYou(){
  // Keep player tech recovery live; only mute rival / open combat.
  freezeRivalAI();
  V.player.stunT = 0;
  V.player.falling = false;
  V.player.hp = 100;
  V.player.boltPhase = "";
  V.player.gait = 0;
  V.player.walkT = 0;
  V.player.walkSettleT = 0;
  V.player.sheatheT = 0;
  V.player.guarding = false;
  V.hitstopLeft = 0;
  V.keys.clear();
}
let tecFadeMid=-1, tecDestMid=null, tecAabbMid=null, tecKMid=0;
for(let i=0;i<3;i++){
  pinTechYou();
  V.update(STEP);
  tecFadeMid=V.techGuardPlantFade(V.player);
  tecDestMid=V.destRect(V.player);
  tecAabbMid=V.bodyAABB(V.player);
  tecKMid=V.player.guardPoseK;
}
check("tech-from-guard plant leftover fade drops", tecFadeMid>=0 && tecFadeMid<tecFade0-0.08 && V.player.techRec && V.player.phase==="recovery" && V.player.guardPoseK>0 && tecKMid<tecKHold, `mid=${tecFadeMid} t0=${tecFade0} k=${tecKMid} k0=${tecKHold}`);
check("tech-from-guard plant leftover destRect eases plant", tecDestMid && Math.abs(tecDestMid.rot)<Math.abs(tecDestHold.rot)-0.01 && Math.abs(tecDestMid.rot)<0.12, `rotHold=${tecDestHold.rot.toFixed(3)} rotMid=${tecDestMid.rot.toFixed(3)}`);
check("tech-from-guard plant leftover AABB planted through ease", tecAabbMid && Math.abs(tecAabbMid.y-tecAabbHold.y)<0.05 && Math.abs(tecAabbMid.h-tecAabbHold.h)<0.05, `dy=${(tecAabbMid.y-tecAabbHold.y).toFixed(2)}`);
let tecGone=false;
for(let i=0;i<12;i++){
  pinTechYou();
  V.update(STEP);
  if(V.player.guardPoseK===0 && V.techGuardPlantFade(V.player)===0 && V.player.techRec && V.player.phase==="recovery"){
    tecGone=true;
    break;
  }
}
check("tech-from-guard plant leftover fade gone while throw plant holds", tecGone && V.player.guardPoseK===0 && V.techGuardPlantFade(V.player)===0 && V.player.techRec && V.player.phase==="recovery" && V.throwPlantFade(V.player)>0 && /windup/.test(sheetSrc(V.player)), `gone=${tecGone} k=${V.player.guardPoseK} fade=${V.techGuardPlantFade(V.player)} trf=${V.throwPlantFade(V.player)} src=${sheetSrc(V.player)}`);

bootPlay(); wait(1700);
holdGuardSample();
V.player.guarding = false;
V.player.guardPoseK = 0;
V.update(STEP);
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
V.player.stamina = 100;
V.player.guarding = false;
V.startAttack(V.player, "golpe");
check("golpe leftover k eases leftover destRect plant after tech-from-guard plant leftover", V.player.cut==="golpe" && V.player.phase==="startup" && V.golpeLeftoverPlanting(V.player) && V.techGuardPlanting(V.player)===false && V.guardRaiseK(V.player)>0.8 && V.player.guardPoseK>0.8, `cut=${V.player.cut} ph=${V.player.phase} gl=${V.golpeLeftoverPlanting(V.player)} tgp=${V.techGuardPlanting(V.player)} gk=${V.guardRaiseK(V.player)} k=${V.player.guardPoseK}`);

bootPlay(); wait(1700);
holdGuardSample();
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
V.player.stamina = 100;
const tecRevOk = V.startReversal(V.player);
check("reversal plant leftover still eases leftover k after tech-from-guard plant leftover", tecRevOk && V.player.cut==="golpe" && V.player.phase==="startup" && V.player.reversal && V.techGuardPlantFade(V.player)===0 && V.reversalPlantFade(V.player)>0.8 && V.guardRaiseK(V.player)>0.8, `ok=${tecRevOk} cut=${V.player.cut} ph=${V.player.phase} tecf=${V.techGuardPlantFade(V.player)} rf=${V.reversalPlantFade(V.player)} gk=${V.guardRaiseK(V.player)}`);

bootPlay(); wait(1700);
holdGuardSample();
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
V.player.stamina = 100;
const tecThrowGuardOk = V.startThrow(V.player);
check("throw-from-guard plant leftover still eases after tech-from-guard plant leftover", tecThrowGuardOk && V.player.cut==="throw" && V.player.phase==="startup" && V.throwGuardPlantFade(V.player)>0.8 && V.techGuardPlantFade(V.player)===0 && V.guardRaiseK(V.player)>0.8, `ok=${tecThrowGuardOk} cut=${V.player.cut} tgf=${V.throwGuardPlantFade(V.player)} tecf=${V.techGuardPlantFade(V.player)} gk=${V.guardRaiseK(V.player)}`);

bootPlay(); wait(1700);
holdGuardSample();
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
V.player.stamina = 100;
V.player.guarding = false;
V.startAttack(V.player);
check("slash leftover k still eases leftover destRect plant after tech-from-guard plant leftover", V.player.cut==="slash" && V.player.phase==="startup" && V.slashLeftoverPlanting(V.player) && V.techGuardPlanting(V.player)===false && V.guardRaiseK(V.player)>0.8 && V.player.guardPoseK>0.8, `cut=${V.player.cut} ph=${V.player.phase} sl=${V.slashLeftoverPlanting(V.player)} tgp=${V.techGuardPlanting(V.player)} gk=${V.guardRaiseK(V.player)}`);

// Idle tech still snaps (no leftover k)
bootPlay(); wait(1700);
freezeRivalAI(); freezeYouAI();
V.player.x = 500; V.rival.x = 580;
V.player.guarding = false; V.player.guardPoseK = 0; V.player.phase = "idle"; V.player.stunT = 0; V.player.falling = false; V.player.boltPhase = ""; V.player.feintT = 0;
V.rival.guarding = false; V.rival.phase = "idle"; V.rival.stunT = 0; V.rival.falling = false; V.rival.boltPhase = ""; V.rival.feintT = 0;
const idleThrowOk = V.startThrow(V.rival);
const idleTechOk = idleThrowOk && V.tryThrowTech(V.player);
check("idle tech still snaps leftover k", idleTechOk && V.player.techRec && V.player.phase==="recovery" && V.techGuardPlantFade(V.player)===0 && V.techGuardPlanting(V.player)===false && V.player.guardPoseK===0 && V.throwPlantFade(V.player)>0.8, `ok=${idleTechOk} fade=${V.techGuardPlantFade(V.player)} k=${V.player.guardPoseK} trf=${V.throwPlantFade(V.player)}`);

bootPlay(); wait(1700);
V.player.phase="idle"; V.player.phaseT=0; V.player.cut="slash"; V.player.guarding=false; V.player.guardPoseK=0;
V.player.stunT=0; V.player.falling=false; V.player.boltPhase=""; V.player.reversal=false; V.player.techRec=false;
check("chip stun is not tech-from-guard plant leftover", V.techGuardPlantFade(V.player)===0, `fade=${V.techGuardPlantFade(V.player)}`);
V.player.stunT = V.HITSTUN;
check("chip stun leftover still snaps tech-from-guard plant leftover", V.techGuardPlantFade(V.player)===0 && V.player.stunT>0 && !isHurtSheet(V.player), `fade=${V.techGuardPlantFade(V.player)} st=${V.player.stunT} src=${sheetSrc(V.player)}`);

const tecThrowKd = landCloseThrow();
check("throw knockdown still not KO crumple after tech-from-guard plant leftover", tecThrowKd && V.rival.hp===80 && V.rival.thrownT>0 && !V.rival.falling && V.crumpleFade(V.rival)===0 && V.techGuardPlantFade(V.rival)===0, `ok=${tecThrowKd} hp=${V.rival.hp} th=${V.rival.thrownT} cf=${V.crumpleFade(V.rival)} fade=${V.techGuardPlantFade(V.rival)}`);
check("cache v=296 after tech-from-guard plant leftover live", /game.js\?v=350/.test(htmlTxt));

// --- v198 feint→guard leftover: leftover windup keeps through S-hold raise ---
check("feint→guard leftover keeps leftover windup through raise", /Feint→guard leftover/.test(srcTxt) && /leftover windup fade used to die the tick/.test(srcTxt) && /S hold raised guarda mid-pull/.test(srcTxt) && /Keep leftover windup through leftover feintT/.test(srcTxt) && /feintFade gated on guarding/.test(srcTxt));
check("feint→guard leftover is draw-only leftover", /Draw-only \(feintFade\)/.test(srcTxt) && /feintFade\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /restFeintGuard/.test(sliceFn(codeOnly, "drawKnight")) && !/feintFade/.test(sliceFn(codeOnly, "destRect")) && !/feintFade/.test(sliceFn(codeOnly, "bodyAABB")) && !/feintFade/.test(sliceFn(codeOnly, "slashPose")) && !/feintFade/.test(sliceFn(codeOnly, "landHit")) && !/feintFade/.test(sliceFn(codeOnly, "startAttack")) && !/feintFade/.test(sliceFn(codeOnly, "updateGuard")));
check("feint→guard leftover poseBitmap still block once guarding", /poseBitmap still block once/.test(srcTxt) && /if \(f\.guarding && ready\(block\)\) return block;/.test(sliceFn(codeOnly, "poseBitmap")) && !/feintFade|restFeintGuard|feintT/.test(sliceFn(codeOnly, "poseBitmap")));
check("feint→guard leftover no longer gates feintFade on guarding", !/f\.falling \|\| f\.stunT > 0 \|\| f\.guarding/.test(sliceFn(codeOnly, "feintFade")) && /if \(f\.falling \|\| f\.stunT > 0\) return 0;/.test(sliceFn(codeOnly, "feintFade")) && /Keep leftover windup through leftover feintT if they raise/.test(srcTxt));
check("feint→guard leftover rests windup on block sheet", /restFeintGuard/.test(sliceFn(codeOnly, "drawKnight")) && /restFeint \|\| restFeintGuard/.test(sliceFn(codeOnly, "drawKnight")) && /Idle pull still rests on idle/.test(srcTxt));
check("feint→guard leftover idle feint path unchanged", /Idle feint still fades windup→idle/.test(srcTxt) && /FEINT_RECOVERY/.test(sliceFn(codeOnly, "feintFade")) && V.FEINT_RECOVERY===100);
check("feint→guard leftover stun still cuts", /Stun still cuts/.test(srcTxt) && /f\.stunT > 0/.test(sliceFn(codeOnly, "feintFade")));
check("feint→guard leftover is not a new combat verb", V.FEINT_RECOVERY===100 && V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80 && V.GUARD_COMMIT_MS===140 && V.STARTUP===180 && V.PUSHBLOCK_STAM===25 && V.PUSHBLOCK_PX===240);
check("slash frames still 180/140/280 after feint→guard leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after feint→guard leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after feint→guard leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after feint→guard leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after feint→guard leftover", /game.js\?v=350/.test(htmlTxt));

toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.guarding = false;
V.player.guardPoseK = 0;
V.player.stunT = 0;
V.player.feintT = 0;
V.player.sheatheT = 0;
V.player.walkSettleT = 0;
V.player.gait = 0;
V.player.falling = false;
V.player.boltPhase = "";
V.keys.clear();
V.attackEdge = true;
V.update(STEP);
check("feint→guard leftover arms from slash startup", V.player.phase==="startup" && V.player.cut==="slash" && V.canFeint(V.player) && /windup/.test(sheetSrc(V.player)), `ph=${V.player.phase} cut=${V.player.cut} src=${sheetSrc(V.player)}`);
for(let i=0;i<4;i++){ holdRivalStill(); V.rival.guarding=false; V.update(STEP); }
check("feint→guard leftover still windup before S", V.player.phase==="startup" && V.canFeint(V.player) && /windup/.test(sheetSrc(V.player)) && V.hitbox(V.player)===null, `ph=${V.player.phase} t=${V.player.phaseT} src=${sheetSrc(V.player)}`);
const fgAabb0 = V.bodyAABB(V.player);
const fgDest0 = V.destRect(V.player);
V.feintEdge = true;
holdRivalStill();
V.update(STEP);
check("feint→guard leftover pull idle before raise", V.player.phase==="idle" && V.player.feintT>0 && V.player.guarding===false && V.feintFade(V.player)>0.85 && !isCutSheet(V.player) && !isBlockSheet(V.player), `ph=${V.player.phase} ft=${V.player.feintT} g=${V.player.guarding} ff=${V.feintFade(V.player)} src=${sheetSrc(V.player)}`);
// Hold S so next tick raises guarda mid-pull (same path startFeint documents).
V.keys.add("KeyS");
holdRivalStill();
V.rival.guarding=false;
V.update(STEP);
const fgFadeRaise = V.feintFade(V.player);
check("feint→guard leftover raise keeps leftover windup", V.player.guarding && V.player.feintT>0 && V.player.phase==="idle" && fgFadeRaise>0.5 && V.feintFade(V.player)>0 && isBlockSheet(V.player), `g=${V.player.guarding} ft=${V.player.feintT} ff=${fgFadeRaise} src=${sheetSrc(V.player)}`);
check("feint→guard leftover poseBitmap block not windup", isBlockSheet(V.player) && !/windup/.test(sheetSrc(V.player)) && !isCutSheet(V.player), `src=${sheetSrc(V.player)}`);
check("feint→guard leftover skips sheathe overlay live", V.feintFade(V.player)>0 && V.sheatheFade(V.player)===0, `ff=${V.feintFade(V.player)} sf=${V.sheatheFade(V.player)}`);
check("feint→guard leftover AABB planted through raise", Math.abs(V.bodyAABB(V.player).y-fgAabb0.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-fgAabb0.h)<0.05, `dy=${(V.bodyAABB(V.player).y-fgAabb0.y).toFixed(2)}`);
check("feint→guard leftover destRect planted through raise", Math.abs(V.destRect(V.player).dy-fgDest0.dy)<2.2, `ddy=${(V.destRect(V.player).dy-fgDest0.dy).toFixed(2)} rot=${V.destRect(V.player).rot.toFixed(3)}`);
let fgFadeMid=-1, fgFtMid=-1;
for(let i=0;i<2;i++){
  holdRivalStill();
  V.rival.guarding=false;
  V.keys.add("KeyS");
  V.update(STEP);
  fgFadeMid=V.feintFade(V.player);
  fgFtMid=V.player.feintT;
}
check("feint→guard leftover fade drops while guard holds", fgFadeMid>=0 && fgFadeMid<fgFadeRaise-0.05 && fgFtMid>0 && V.player.guarding && isBlockSheet(V.player), `mid=${fgFadeMid.toFixed(3)} t0=${fgFadeRaise.toFixed(3)} ft=${fgFtMid} g=${V.player.guarding} src=${sheetSrc(V.player)}`);
check("feint→guard leftover AABB planted through fade", Math.abs(V.bodyAABB(V.player).y-fgAabb0.y)<0.05, `dy=${(V.bodyAABB(V.player).y-fgAabb0.y).toFixed(2)}`);
let fgGone=false;
for(let i=0;i<10;i++){
  holdRivalStill();
  V.rival.guarding=false;
  V.keys.add("KeyS");
  V.update(STEP);
  if(V.player.feintT===0 && V.feintFade(V.player)===0 && V.player.guarding){
    fgGone=true;
    break;
  }
}
check("feint→guard leftover fade gone while guard holds", fgGone && V.player.feintT===0 && V.feintFade(V.player)===0 && V.player.guarding && isBlockSheet(V.player), `gone=${fgGone} ft=${V.player.feintT} ff=${V.feintFade(V.player)} g=${V.player.guarding} src=${sheetSrc(V.player)}`);
check("feint→guard leftover leftover sheatheT cleared", V.player.sheatheT===0 && V.sheatheFade(V.player)===0, `sh=${V.player.sheatheT} sf=${V.sheatheFade(V.player)}`);

// Idle feint (no hold S) still fades windup→idle
toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.guarding=false; V.player.guardPoseK=0; V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0;
V.keys.clear();
V.attackEdge=true;
V.update(STEP);
for(let i=0;i<4;i++){ holdRivalStill(); V.rival.guarding=false; V.update(STEP); }
V.feintEdge=true;
holdRivalStill();
V.update(STEP);
check("idle feint still fades after feint→guard leftover", V.player.phase==="idle" && V.player.feintT>0 && V.player.guarding===false && V.feintFade(V.player)>0.85 && !isBlockSheet(V.player) && !isCutSheet(V.player), `ph=${V.player.phase} ft=${V.player.feintT} g=${V.player.guarding} ff=${V.feintFade(V.player)} src=${sheetSrc(V.player)}`);
for(let i=0;i<10;i++){ holdRivalStill(); V.rival.guarding=false; V.keys.clear(); V.update(STEP); }
check("idle feint still clears after feint→guard leftover", V.player.feintT===0 && V.feintFade(V.player)===0 && V.player.guarding===false, `ft=${V.player.feintT} ff=${V.feintFade(V.player)} g=${V.player.guarding}`);

// Stun still cuts feint→guard leftover
toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.guarding=false; V.player.guardPoseK=0; V.player.feintT=0; V.player.sheatheT=0;
V.keys.clear();
V.attackEdge=true;
V.update(STEP);
for(let i=0;i<4;i++){ holdRivalStill(); V.rival.guarding=false; V.update(STEP); }
V.feintEdge=true;
holdRivalStill();
V.update(STEP);
V.keys.add("KeyS");
holdRivalStill();
V.update(STEP);
check("feint→guard leftover armed before stun cut", V.player.guarding && V.player.feintT>0 && V.feintFade(V.player)>0, `g=${V.player.guarding} ft=${V.player.feintT} ff=${V.feintFade(V.player)}`);
V.player.stunT = V.HITSTUN;
check("chip stun still cuts feint→guard leftover", V.feintFade(V.player)===0 && V.player.stunT>0, `ff=${V.feintFade(V.player)} st=${V.player.stunT}`);

// Rival feint→guard same path
toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.rival.gait=0; V.rival.walkSettleT=0; V.rival.sheatheT=0; V.rival.feintT=0; V.rival.guarding=false; V.rival.guardPoseK=0;
V.startAttack(V.rival, "slash");
for(let i=0;i<4;i++){ holdRivalStill(); V.update(STEP); }
const rFgDid = V.startFeint(V.rival);
check("rival feint→guard leftover pull", rFgDid && V.rival.phase==="idle" && V.rival.feintT>0 && V.feintFade(V.rival)>0.85, `ok=${rFgDid} ph=${V.rival.phase} ft=${V.rival.feintT} ff=${V.feintFade(V.rival)}`);
V.rival.wantBlock = true;
V.rival.guarding = true;
V.rival.guardPoseK = 1;
check("rival feint→guard leftover keeps windup on block", V.rival.guarding && V.rival.feintT>0 && V.feintFade(V.rival)>0.5 && isBlockSheet(V.rival), `g=${V.rival.guarding} ft=${V.rival.feintT} ff=${V.feintFade(V.rival)} src=${sheetSrc(V.rival)}`);

const fgThrowKd = landCloseThrow();
check("throw knockdown still not KO crumple after feint→guard leftover", fgThrowKd && V.rival.hp===80 && V.rival.thrownT>0 && !V.rival.falling && V.crumpleFade(V.rival)===0 && V.feintFade(V.rival)===0, `ok=${fgThrowKd} hp=${V.rival.hp} th=${V.rival.thrownT} cf=${V.crumpleFade(V.rival)} ff=${V.feintFade(V.rival)}`);
check("cache v=296 after feint→guard leftover live", /game.js\?v=350/.test(htmlTxt));

// --- v199 golpe leftover k: leftover destRect plant eases (L from leftover drop) ---
check("golpe leftover k eases destRect leftover plant", /function golpeLeftoverPlanting/.test(srcTxt) && /Golpe leftover k/.test(srcTxt) && /leftover destRect plant used to dump/.test(srcTxt) && /startAttack\(golpe\)/.test(srcTxt) && /hopped off the windup plant/.test(srcTxt));
check("golpe leftover k is destRect-only ease", /destRect-only ease/.test(srcTxt) && /golpeLeftoverPlanting/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && /poseBitmap still windup immediately/.test(srcTxt) && /golpeLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "tickGuardPose")) && /golpeLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "guardRaiseK")) && !/golpeLeftoverPlanting/.test(sliceFn(codeOnly, "destRect")) && !/golpeLeftoverPlanting/.test(sliceFn(codeOnly, "bodyAABB")) && !/golpeLeftoverPlanting/.test(sliceFn(codeOnly, "slashPose")) && !/golpeLeftoverPlanting/.test(sliceFn(codeOnly, "landHit")));
check("golpe leftover k no longer zeros leftover k on L", !/if \(kind === "golpe"\) f\.guardPoseK = 0/.test(sliceFn(codeOnly, "startAttack")) && /Golpe leftover k: leftover destRect plant used to dump the same tick/.test(sliceFn(srcTxt, "startAttack")));
check("golpe leftover k poseBitmap still windup", /f\.phase === "startup" && ready\(wind\)/.test(sliceFn(codeOnly, "poseBitmap")) && !/golpeLeftoverPlanting/.test(sliceFn(codeOnly, "poseBitmap")) && !/guardPoseK/.test(sliceFn(codeOnly, "poseBitmap")));
check("golpe leftover k does not re-fade guard drop", !/golpeLeftoverPlanting/.test(sliceFn(codeOnly, "guardDropFade")) && !/golpeLeftoverPlanting/.test(sliceFn(codeOnly, "reversalPlantFade")) && !/golpeLeftoverPlanting/.test(sliceFn(codeOnly, "slashLeftoverPlanting")) && /guardDropFade idle/.test(srcTxt));
check("golpe leftover k does not steal reversal plant", /f\.reversal/.test(sliceFn(codeOnly, "golpeLeftoverPlanting")) && /reversalPlanting/.test(srcTxt) && /Reversal leftover still eases leftover k/.test(srcTxt));
check("golpe leftover k extra rot stays 0", /Extra destRect rot stays 0/.test(srcTxt) && /leftover guard rot eases with leftover k/.test(srcTxt) && /Raise still 0ms/.test(srcTxt));
check("golpe leftover k is not a new combat verb", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18 && V.GUARD_RAISE_MS===80 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80 && V.PUSHBLOCK_STAM===25 && V.PUSHBLOCK_PX===240);
check("slash frames still 180/140/280 after golpe leftover k", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after golpe leftover k", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after golpe leftover k", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after golpe leftover k", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after golpe leftover k", /game.js\?v=350/.test(htmlTxt));

function pinGolpeYou(){
  freezeRivalAI();
  V.player.stunT = 0;
  V.player.falling = false;
  V.player.hp = 100;
  V.player.boltPhase = "";
  V.player.gait = 0;
  V.player.walkT = 0;
  V.player.walkSettleT = 0;
  V.player.sheatheT = 0;
  V.hitstopLeft = 0;
  V.keys.clear();
}

bootPlay(); wait(1700);
holdGuardSample();
V.player.guarding = false;
V.player.guardPoseK = 0;
V.update(STEP);
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
check("golpe leftover k raise still 0ms block", V.player.guarding && V.player.guardPoseK>0.8 && V.golpeLeftoverPlanting(V.player)===false && V.guardDropFade(V.player)===0 && isBlockSheet(V.player), `g=${V.player.guarding} k=${V.player.guardPoseK.toFixed(3)} gl=${V.golpeLeftoverPlanting(V.player)} src=${sheetSrc(V.player)}`);
const glAabbHold = V.bodyAABB(V.player);
const glDestHold = V.destRect(V.player);
const glKHold = V.player.guardPoseK;
V.player.stamina = 100;
V.player.guarding = false;
V.startAttack(V.player, "golpe");
const glFade0 = V.guardRaiseK(V.player);
const glDest0 = V.destRect(V.player);
const glAabb0 = V.bodyAABB(V.player);
check("golpe leftover k trip leftover plant stays", V.player.cut==="golpe" && V.player.phase==="startup" && V.golpeLeftoverPlanting(V.player) && glFade0>0.8 && V.player.guarding===false && V.player.guardPoseK===glKHold && V.reversalPlantFade(V.player)===0 && V.guardDropFade(V.player)===0 && V.slashLeftoverPlanting(V.player)===false, `cut=${V.player.cut} ph=${V.player.phase} gl=${V.golpeLeftoverPlanting(V.player)} gk=${glFade0} k=${V.player.guardPoseK.toFixed(3)} k0=${glKHold} gf=${V.guardDropFade(V.player)} rf=${V.reversalPlantFade(V.player)}`);
check("golpe leftover k poseBitmap windup immediately", /windup/.test(sheetSrc(V.player)) && !isBlockSheet(V.player) && !isCutSheet(V.player) && !isHurtSheet(V.player), `src=${sheetSrc(V.player)}`);
check("golpe leftover k destRect leftover plant eases not snaps", Math.abs(glDest0.rot)>0.04 && Math.abs(glDest0.rot-glDestHold.rot)<0.06 && Math.abs(glDest0.dy-glDestHold.dy)<1.6, `rot0=${glDestHold.rot.toFixed(3)} rot=${glDest0.rot.toFixed(3)} ddy=${(glDest0.dy-glDestHold.dy).toFixed(2)}`);
check("golpe leftover k AABB planted at trip", Math.abs(glAabb0.y-glAabbHold.y)<0.05 && Math.abs(glAabb0.h-glAabbHold.h)<0.05, `dy=${(glAabb0.y-glAabbHold.y).toFixed(2)}`);
check("golpe leftover k extra destRect rot 0 at trip", Math.abs(glDest0.rot-glDestHold.rot)<0.06, `drot=${(glDest0.rot-glDestHold.rot).toFixed(3)}`);
let glFadeMid=-1, glDestMid=null, glAabbMid=null, glKMid=0;
for(let i=0;i<3;i++){
  pinGolpeYou();
  V.update(STEP);
  glFadeMid=V.guardRaiseK(V.player);
  glDestMid=V.destRect(V.player);
  glAabbMid=V.bodyAABB(V.player);
  glKMid=V.player.guardPoseK;
}
check("golpe leftover k leftover plant drops", glFadeMid>=0 && glFadeMid<glFade0-0.08 && V.player.cut==="golpe" && V.player.phase==="startup" && V.player.guardPoseK>0 && glKMid<glKHold, `mid=${glFadeMid} t0=${glFade0} k=${glKMid} k0=${glKHold}`);
check("golpe leftover k destRect eases leftover plant", glDestMid && Math.abs(glDestMid.rot)<Math.abs(glDestHold.rot)-0.01 && Math.abs(glDestMid.rot)<0.12, `rotHold=${glDestHold.rot.toFixed(3)} rotMid=${glDestMid.rot.toFixed(3)}`);
check("golpe leftover k AABB planted through ease", glAabbMid && Math.abs(glAabbMid.y-glAabbHold.y)<0.05 && Math.abs(glAabbMid.h-glAabbHold.h)<0.05, `dy=${(glAabbMid.y-glAabbHold.y).toFixed(2)}`);
let glGone=false;
for(let i=0;i<5;i++){
  pinGolpeYou();
  V.update(STEP);
  if(V.player.guardPoseK===0 && V.guardRaiseK(V.player)===0 && V.player.phase==="startup" && V.player.cut==="golpe"){
    glGone=true;
    break;
  }
}
check("golpe leftover k leftover plant gone while startup holds", glGone && V.player.guardPoseK===0 && V.guardRaiseK(V.player)===0 && V.player.cut==="golpe" && V.player.phase==="startup" && /windup/.test(sheetSrc(V.player)), `gone=${glGone} k=${V.player.guardPoseK} gk=${V.guardRaiseK(V.player)} ph=${V.player.phase} src=${sheetSrc(V.player)}`);

// Slash leftover k still eases (sibling path)
bootPlay(); wait(1700);
holdGuardSample();
V.player.guarding = false;
V.player.guardPoseK = 0;
V.update(STEP);
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
V.player.stamina = 100;
V.player.guarding = false;
V.startAttack(V.player);
check("slash leftover k still eases leftover destRect plant after golpe leftover k", V.player.cut==="slash" && V.player.phase==="startup" && V.slashLeftoverPlanting(V.player) && V.golpeLeftoverPlanting(V.player)===false && V.guardRaiseK(V.player)>0.8 && V.player.guardPoseK>0.8, `cut=${V.player.cut} ph=${V.player.phase} sl=${V.slashLeftoverPlanting(V.player)} gl=${V.golpeLeftoverPlanting(V.player)} gk=${V.guardRaiseK(V.player)} k=${V.player.guardPoseK}`);

// Reversal still eases via reversalPlanting, not golpeLeftoverPlanting
bootPlay(); wait(1700);
holdGuardSample();
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
V.player.stamina = 100;
const glRevSt0 = V.player.stamina;
const glRevOk = V.startReversal(V.player);
check("reversal plant leftover still eases leftover k after golpe leftover k", glRevOk && V.player.cut==="golpe" && V.player.phase==="startup" && V.player.reversal && V.golpeLeftoverPlanting(V.player)===false && V.reversalPlantFade(V.player)>0.8 && V.guardRaiseK(V.player)>0.8, `ok=${glRevOk} cut=${V.player.cut} ph=${V.player.phase} gl=${V.golpeLeftoverPlanting(V.player)} rf=${V.reversalPlantFade(V.player)} gk=${V.guardRaiseK(V.player)} spent=${(glRevSt0-V.player.stamina).toFixed(2)}`);

// Rival golpe leftover k same path
bootPlay(); wait(1700);
freezeYouAI();
freezeRivalAI();
V.rival.wantBlock = true;
V.rival.guarding = true;
V.rival.guardPoseK = 1;
V.rival.stamina = 100;
V.rival.gait = 0;
V.rival.phase = "idle";
V.rival.boltPhase = "";
V.rival.stunT = 0;
check("golpe leftover k rival raise still block", V.rival.guarding && V.rival.guardPoseK===1 && V.golpeLeftoverPlanting(V.rival)===false && isBlockSheet(V.rival), `g=${V.rival.guarding} k=${V.rival.guardPoseK} gl=${V.golpeLeftoverPlanting(V.rival)} src=${sheetSrc(V.rival)}`);
const rglKHold = V.rival.guardPoseK;
const rglDestHold = V.destRect(V.rival);
const rglAabbHold = V.bodyAABB(V.rival);
V.rival.guarding = false;
V.startAttack(V.rival, "golpe");
check("golpe leftover k rival leftover plant stays", V.rival.cut==="golpe" && V.rival.phase==="startup" && V.golpeLeftoverPlanting(V.rival) && V.guardRaiseK(V.rival)>0.8 && V.rival.guardPoseK===rglKHold && !isBlockSheet(V.rival) && /windup/.test(sheetSrc(V.rival)), `cut=${V.rival.cut} ph=${V.rival.phase} gl=${V.golpeLeftoverPlanting(V.rival)} gk=${V.guardRaiseK(V.rival)} k=${V.rival.guardPoseK} src=${sheetSrc(V.rival)}`);
check("golpe leftover k rival destRect leftover plant eases", Math.abs(V.destRect(V.rival).rot)>0.04 && Math.abs(V.destRect(V.rival).rot-rglDestHold.rot)<0.06, `rot0=${rglDestHold.rot.toFixed(3)} rot=${V.destRect(V.rival).rot.toFixed(3)}`);
check("golpe leftover k rival AABB planted at trip", Math.abs(V.bodyAABB(V.rival).y-rglAabbHold.y)<0.05, `dy=${(V.bodyAABB(V.rival).y-rglAabbHold.y).toFixed(2)}`);

// Chip stun still cuts
bootPlay(); wait(1700);
holdGuardSample();
V.player.guarding = false;
V.player.guardPoseK = 0;
V.update(STEP);
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
V.player.stamina = 100;
V.player.guarding = false;
V.startAttack(V.player, "golpe");
check("golpe leftover k armed before stun cut", V.golpeLeftoverPlanting(V.player) && V.player.guardPoseK>0.8, `gl=${V.golpeLeftoverPlanting(V.player)} k=${V.player.guardPoseK}`);
V.player.stunT = V.HITSTUN;
check("chip stun is not golpe leftover k", V.golpeLeftoverPlanting(V.player)===false, `gl=${V.golpeLeftoverPlanting(V.player)}`);
check("chip stun leftover still snaps golpe leftover k", V.golpeLeftoverPlanting(V.player)===false && V.player.stunT>0 && !isHurtSheet(V.player), `gl=${V.golpeLeftoverPlanting(V.player)} st=${V.player.stunT} src=${sheetSrc(V.player)}`);

const glThrowKd = landCloseThrow();
check("throw knockdown still not KO crumple after golpe leftover k", glThrowKd && V.rival.hp===80 && V.rival.thrownT>0 && !V.rival.falling && V.crumpleFade(V.rival)===0 && V.golpeLeftoverPlanting(V.rival)===false, `ok=${glThrowKd} hp=${V.rival.hp} th=${V.rival.thrownT} cf=${V.crumpleFade(V.rival)} gl=${V.golpeLeftoverPlanting(V.rival)}`);
check("cache v=296 after golpe leftover k live", /game.js\?v=350/.test(htmlTxt));

// --- v200 feint from leftover drop: leftover block overlay skips through pull ---
check("feint from leftover drop skips leftover block overlay", /Feint from leftover drop/.test(srcTxt) && /leftover block fade used to sit on the pull/.test(srcTxt) && /startFeint armed from leftover-drop slash/.test(srcTxt) && /Skip leftover block through leftover feintT/.test(srcTxt) && /guardDropFade/.test(srcTxt));
check("feint from leftover drop is draw-only leftover", /Draw-only \(guardDropFade\)/.test(srcTxt) && /feintT > 0/.test(sliceFn(codeOnly, "guardDropFade")) && /!restFeint && !restFeintGuard/.test(sliceFn(codeOnly, "drawKnight")) && !/feintT/.test(sliceFn(codeOnly, "destRect")) && !/feintT/.test(sliceFn(codeOnly, "bodyAABB")) && !/feintT/.test(sliceFn(codeOnly, "slashPose")) && !/feintT/.test(sliceFn(codeOnly, "landHit")));
check("feint from leftover drop poseBitmap still idle", /poseBitmap still idle/.test(srcTxt) && /block once they raise/.test(srcTxt) && !/feintT/.test(sliceFn(codeOnly, "poseBitmap")) && !/guardDropFade/.test(sliceFn(codeOnly, "poseBitmap")));
check("feint from leftover drop no longer stacks block on windup", /if \(f\.feintT > 0\) return 0;/.test(sliceFn(codeOnly, "guardDropFade")) && /feint owns the windup overlay/.test(srcTxt) && /restFeint/.test(sliceFn(codeOnly, "drawKnight")));
check("feint from leftover drop feint→guard path unchanged", /feint→guard/.test(srcTxt) && /restFeintGuard/.test(sliceFn(codeOnly, "drawKnight")) && /Keep leftover windup through leftover feintT if they raise/.test(srcTxt));
check("feint from leftover drop walk-out drop unchanged", /Walk-out/.test(srcTxt) && /drop unchanged/.test(srcTxt) && /S-release/.test(srcTxt) && !/walking\(f\)/.test(sliceFn(codeOnly, "guardDropFade")) && /Keep leftover block through leftover guardPoseK if they step off/.test(srcTxt));
check("feint from leftover drop destRect leftover k still eases", /destRect leftover k still eases/.test(srcTxt) && /Extra destRect rot stays 0/.test(srcTxt));
check("feint from leftover drop is not a new combat verb", V.FEINT_RECOVERY===100 && V.GUARD_RAISE_MS===80 && V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80 && V.GUARD_COMMIT_MS===140 && V.STARTUP===180 && V.PUSHBLOCK_STAM===25 && V.PUSHBLOCK_PX===240);
check("slash frames still 180/140/280 after feint from leftover drop", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after feint from leftover drop", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after feint from leftover drop", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after feint from leftover drop", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after feint from leftover drop", /game.js\?v=350/.test(htmlTxt));

toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.guarding = false;
V.player.guardPoseK = 0;
V.player.stunT = 0;
V.player.feintT = 0;
V.player.sheatheT = 0;
V.player.walkSettleT = 0;
V.player.gait = 0;
V.player.falling = false;
V.player.boltPhase = "";
V.player.phase = "idle";
V.player.phaseT = 0;
V.keys.clear();
V.keys.add("KeyS");
for(let i=0;i<8;i++){ holdRivalStill(); V.rival.guarding=false; V.update(STEP); }
check("feint from leftover drop raise still block", V.player.guarding && V.player.guardPoseK>0.8 && V.guardDropFade(V.player)===0 && isBlockSheet(V.player), `g=${V.player.guarding} k=${V.player.guardPoseK.toFixed(3)} gf=${V.guardDropFade(V.player)} src=${sheetSrc(V.player)}`);
V.keys.clear();
holdRivalStill();
V.rival.guarding=false;
V.update(STEP);
const fldKHold = V.player.guardPoseK;
check("feint from leftover drop mid-drop leftover block", V.player.guarding===false && fldKHold>0.7 && V.guardDropFade(V.player)>0.8 && !isBlockSheet(V.player), `g=${V.player.guarding} k=${fldKHold.toFixed(3)} gf=${V.guardDropFade(V.player)} src=${sheetSrc(V.player)}`);
V.attackEdge = true;
holdRivalStill();
V.rival.guarding=false;
V.update(STEP);
check("feint from leftover drop slash leftover plant stays", V.player.cut==="slash" && V.player.phase==="startup" && V.slashLeftoverPlanting(V.player) && V.player.guardPoseK>0.4 && V.guardDropFade(V.player)===0 && /windup/.test(sheetSrc(V.player)), `cut=${V.player.cut} ph=${V.player.phase} sl=${V.slashLeftoverPlanting(V.player)} k=${V.player.guardPoseK.toFixed(3)} gf=${V.guardDropFade(V.player)} src=${sheetSrc(V.player)}`);
const fldAabb0 = V.bodyAABB(V.player);
const fldDest0 = V.destRect(V.player);
V.feintEdge = true;
holdRivalStill();
V.rival.guarding=false;
V.update(STEP);
check("feint from leftover drop pull skips leftover block", V.player.phase==="idle" && V.player.feintT>0 && V.player.guarding===false && V.player.guardPoseK>0.2 && V.feintFade(V.player)>0.85 && V.guardDropFade(V.player)===0 && !isBlockSheet(V.player) && !isCutSheet(V.player), `ph=${V.player.phase} ft=${V.player.feintT} k=${V.player.guardPoseK.toFixed(3)} ff=${V.feintFade(V.player)} gf=${V.guardDropFade(V.player)} src=${sheetSrc(V.player)}`);
check("feint from leftover drop poseBitmap idle not block", !isBlockSheet(V.player) && !/windup/.test(sheetSrc(V.player)) && !isCutSheet(V.player), `src=${sheetSrc(V.player)}`);
check("feint from leftover drop AABB planted at pull", Math.abs(V.bodyAABB(V.player).y-fldAabb0.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-fldAabb0.h)<0.05, `dy=${(V.bodyAABB(V.player).y-fldAabb0.y).toFixed(2)}`);
check("feint from leftover drop destRect leftover k still live", V.player.guardPoseK>0 && Math.abs(V.destRect(V.player).rot)>0.02, `k=${V.player.guardPoseK.toFixed(3)} rot=${V.destRect(V.player).rot.toFixed(3)} ddy=${(V.destRect(V.player).dy-fldDest0.dy).toFixed(2)}`);
let fldFfMid=-1, fldKMid=-1;
for(let i=0;i<2;i++){
  holdRivalStill();
  V.rival.guarding=false;
  V.keys.clear();
  V.update(STEP);
  fldFfMid=V.feintFade(V.player);
  fldKMid=V.player.guardPoseK;
}
check("feint from leftover drop fade drops without block", fldFfMid>=0 && fldFfMid<0.95 && V.guardDropFade(V.player)===0 && V.player.feintT>0 && !isBlockSheet(V.player), `ff=${fldFfMid} gf=${V.guardDropFade(V.player)} k=${fldKMid} ft=${V.player.feintT} src=${sheetSrc(V.player)}`);
check("feint from leftover drop AABB planted through fade", Math.abs(V.bodyAABB(V.player).y-fldAabb0.y)<0.05, `dy=${(V.bodyAABB(V.player).y-fldAabb0.y).toFixed(2)}`);
let fldGone=false;
for(let i=0;i<12;i++){
  holdRivalStill();
  V.rival.guarding=false;
  V.keys.clear();
  V.update(STEP);
  if(V.player.feintT===0 && V.feintFade(V.player)===0){
    fldGone=true;
    break;
  }
}
check("feint from leftover drop pull clears", fldGone && V.player.feintT===0 && V.feintFade(V.player)===0 && V.guardDropFade(V.player)===0, `gone=${fldGone} ft=${V.player.feintT} ff=${V.feintFade(V.player)} gf=${V.guardDropFade(V.player)} k=${V.player.guardPoseK}`);

toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.guardPoseK=0; V.player.guarding=false;
V.keys.clear();
V.keys.add("KeyS");
for(let i=0;i<8;i++){ holdRivalStill(); V.rival.guarding=false; V.update(STEP); }
V.keys.clear();
holdRivalStill();
V.rival.guarding=false;
V.update(STEP);
check("S-release drop still fades leftover block after feint from leftover drop", V.player.guarding===false && V.player.feintT===0 && V.player.guardPoseK>0.7 && V.guardDropFade(V.player)>0.8 && !isBlockSheet(V.player), `g=${V.player.guarding} ft=${V.player.feintT} k=${V.player.guardPoseK.toFixed(3)} gf=${V.guardDropFade(V.player)} src=${sheetSrc(V.player)}`);

toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.guarding=false; V.player.guardPoseK=0; V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0;
V.keys.clear();
V.attackEdge=true;
V.update(STEP);
for(let i=0;i<4;i++){ holdRivalStill(); V.rival.guarding=false; V.update(STEP); }
V.feintEdge=true;
holdRivalStill();
V.update(STEP);
V.keys.add("KeyS");
holdRivalStill();
V.rival.guarding=false;
V.update(STEP);
check("feint→guard leftover still keeps windup after feint from leftover drop", V.player.guarding && V.player.feintT>0 && V.feintFade(V.player)>0.5 && V.guardDropFade(V.player)===0 && isBlockSheet(V.player), `g=${V.player.guarding} ft=${V.player.feintT} ff=${V.feintFade(V.player)} gf=${V.guardDropFade(V.player)} src=${sheetSrc(V.player)}`);

toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.rival.guarding=false; V.rival.guardPoseK=0; V.rival.feintT=0; V.rival.sheatheT=0; V.rival.gait=0;
V.rival.phase="idle"; V.rival.phaseT=0; V.rival.boltPhase="";
V.rival.guarding=true; V.rival.guardPoseK=1;
for(let i=0;i<2;i++){ holdRivalStill(); V.update(STEP); }
V.rival.guarding=false;
holdRivalStill();
V.update(STEP);
const rFldK = V.rival.guardPoseK;
V.startAttack(V.rival, "slash");
holdRivalStill();
V.update(STEP);
const rFldDid = V.startFeint(V.rival);
check("feint from leftover drop rival pull skips leftover block", rFldDid && V.rival.feintT>0 && V.rival.guardPoseK>0 && V.feintFade(V.rival)>0.85 && V.guardDropFade(V.rival)===0 && !isBlockSheet(V.rival), `ok=${rFldDid} ft=${V.rival.feintT} k=${V.rival.guardPoseK.toFixed(3)} k0=${rFldK.toFixed(3)} ff=${V.feintFade(V.rival)} gf=${V.guardDropFade(V.rival)} src=${sheetSrc(V.rival)}`);

const fldThrowKd = landCloseThrow();
check("throw knockdown still not KO crumple after feint from leftover drop", fldThrowKd && V.rival.hp===80 && V.rival.thrownT>0 && !V.rival.falling && V.crumpleFade(V.rival)===0 && V.guardDropFade(V.rival)===0, `ok=${fldThrowKd} hp=${V.rival.hp} th=${V.rival.thrownT} cf=${V.crumpleFade(V.rival)} gf=${V.guardDropFade(V.rival)}`);
check("cache v=296 after feint from leftover drop live", /game.js\?v=350/.test(htmlTxt));


// --- v201 idle sheathe leftover: mashy Space/L/K after clash/tech sheathe ---
check("idle sheathe leftover keeps leftover sheathe through raise", /Idle sheathe leftover/.test(srcTxt) && /leftover clash\/tech sheathe overlay used to dump/.test(srcTxt) && /mashy Space\/L\/K after clash/.test(srcTxt) && /Fade leftover sheathe→windup/.test(srcTxt) && /linkPlantFade/.test(srcTxt));
check("idle sheathe leftover is draw-only leftover", /Draw-only \(linkPlantFade\)/.test(srcTxt) && /linkPlantFade/.test(sliceFn(codeOnly, "drawKnight")) && /leftoverSheathe = sheatheFade\(f\)/.test(sliceFn(codeOnly, "startAttack")) && /leftoverSheathe = fromIdle \? sheatheFade\(f\) : 0/.test(sliceFn(codeOnly, "startThrow")) && /leftoverSheathe = sheatheFade\(u\)/.test(sliceFn(codeOnly, "startBolt")) && !/linkPlantFade/.test(sliceFn(codeOnly, "destRect")) && !/linkPlantFade/.test(sliceFn(codeOnly, "bodyAABB")) && !/linkPlantFade/.test(sliceFn(codeOnly, "poseBitmap")) && !/linkPlantFade/.test(sliceFn(codeOnly, "slashPose")) && !/linkPlantFade/.test(sliceFn(codeOnly, "landHit")));
check("idle sheathe leftover poseBitmap still throw_knife", /poseBitmap still windup immediately/.test(srcTxt) && /f\.phase === "startup" && ready\(wind\)/.test(sliceFn(codeOnly, "poseBitmap")) && !/linkPlant/.test(sliceFn(codeOnly, "poseBitmap")));
check("idle sheathe leftover reuses linkPlantFade", /f\.cut !== "slash" && f\.cut !== "golpe" && f\.cut !== "throw"/.test(sliceFn(codeOnly, "linkPlanting")) && /boltPhase === "startup" && !holdingCutBolt\(f\)/.test(sliceFn(codeOnly, "linkPlanting")) && /boltPhase === "startup" \? f\.boltT : f\.phaseT/.test(sliceFn(codeOnly, "linkPlantFade")));
check("idle sheathe leftover does not re-fade connected slash-L sheathe", /Connected slash-L leftover sheathe unchanged/.test(srcTxt) && /leftoverSheathe = sheatheFade\(f\)/.test(sliceFn(codeOnly, "cancelIntoGolpe")) && /linkPlant = !f\.clashRec/.test(sliceFn(codeOnly, "cancelIntoGolpe")));
check("idle sheathe leftover does not re-fade special-cancel K sheathe", /Special-cancel K leftover sheathe unchanged/.test(srcTxt) && /holdCutPlant = true/.test(sliceFn(codeOnly, "cancelIntoBolt")) && /holdingCutBolt\(f\)/.test(sliceFn(codeOnly, "holdCutPlanting")));
check("idle sheathe leftover does not re-fade clash leftover pose", /Clash leftover pose unchanged/.test(srcTxt) && /clashPlant = !!f\.clashRec/.test(sliceFn(codeOnly, "cancelIntoGolpe")) && /clashPlantFade/.test(srcTxt));
check("idle sheathe leftover telegraph snaps without sheathe", /Telegraph still snaps when no leftover sheathe/.test(srcTxt) && /f\.telegraph = true/.test(sliceFn(codeOnly, "startAttack")));
check("idle sheathe leftover extra rot stays 0", /Extra destRect rot stays 0/.test(srcTxt) && /destRect leftover plant already 0/.test(srcTxt));
check("idle sheathe leftover is not a new combat verb", /SHEATHE_MS = 140/.test(srcTxt) && V.GUARD_RAISE_MS===80 && V.CLASH_RECOVERY===200 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80 && V.PUSHBLOCK_STAM===25 && V.PUSHBLOCK_PX===240);
check("slash frames still 180/140/280 after idle sheathe leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after idle sheathe leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after idle sheathe leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after idle sheathe leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after idle sheathe leftover", /game.js\?v=350/.test(htmlTxt));

toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.guarding=false; V.player.guardPoseK=0; V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0;
V.player.phase="idle"; V.player.phaseT=0; V.player.boltPhase=""; V.player.linkPlant=false; V.player.clashPlant=false;
V.rival.guarding=false; V.rival.guardPoseK=0; V.rival.feintT=0; V.rival.sheatheT=0; V.rival.gait=0;
V.rival.phase="idle"; V.rival.phaseT=0; V.rival.boltPhase="";
V.keys.clear();
V.doClash(640, 360);
check("idle sheathe leftover clash arms recovery", V.player.phase==="recovery" && V.player.clashRec && V.rival.clashRec, `ph=${V.player.phase} rec=${V.player.clashRec}`);
// burn hitstop + clash recovery to idle sheathe
for(let i=0;i<40;i++){ holdRivalStill(); V.rival.guarding=false; V.update(STEP); if(V.player.phase==="idle" && V.player.sheatheT>0) break; }
check("idle sheathe leftover idle sheathe live", V.player.phase==="idle" && V.player.sheatheT>0 && V.sheatheFade(V.player)>0.5 && V.linkPlantFade(V.player)===0 && isCutSheet(V.player), `ph=${V.player.phase} sh=${V.player.sheatheT} sf=${V.sheatheFade(V.player)} lf=${V.linkPlantFade(V.player)} src=${sheetSrc(V.player)}`);
const ishSf0 = V.sheatheFade(V.player);
const ishAabb0 = V.bodyAABB(V.player);
const ishDest0 = V.destRect(V.player);
V.attackEdge = true;
holdRivalStill();
V.rival.guarding=false;
V.update(STEP);
check("idle sheathe leftover Space keeps leftover sheathe", V.player.cut==="slash" && V.player.phase==="startup" && V.player.linkPlant && V.linkPlantFade(V.player)>0.8 && V.player.linkSheathe>0.5 && V.player.telegraph===false && V.sheatheFade(V.player)===0 && /windup/.test(sheetSrc(V.player)), `cut=${V.player.cut} ph=${V.player.phase} lp=${V.player.linkPlant} lf=${V.linkPlantFade(V.player)} ls=${V.player.linkSheathe} tel=${V.player.telegraph} sf=${V.sheatheFade(V.player)} src=${sheetSrc(V.player)} sf0=${ishSf0}`);
check("idle sheathe leftover poseBitmap windup immediately", /windup/.test(sheetSrc(V.player)) && !isCutSheet(V.player) && !isBlockSheet(V.player), `src=${sheetSrc(V.player)}`);
check("idle sheathe leftover AABB planted at trip", Math.abs(V.bodyAABB(V.player).y-ishAabb0.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-ishAabb0.h)<0.05, `dy=${(V.bodyAABB(V.player).y-ishAabb0.y).toFixed(2)}`);
check("idle sheathe leftover destRect planted at trip", Math.abs(V.destRect(V.player).dy-ishDest0.dy)<2.2, `ddy=${(V.destRect(V.player).dy-ishDest0.dy).toFixed(2)}`);
let ishLfMid=-1;
for(let i=0;i<2;i++){ holdRivalStill(); V.rival.guarding=false; V.keys.clear(); V.update(STEP); ishLfMid=V.linkPlantFade(V.player); }
check("idle sheathe leftover fade drops", ishLfMid>=0 && ishLfMid<0.95 && V.player.linkPlant && V.player.phase==="startup", `mid=${ishLfMid} lp=${V.player.linkPlant} ph=${V.player.phase}`);
check("idle sheathe leftover AABB planted through fade", Math.abs(V.bodyAABB(V.player).y-ishAabb0.y)<0.05, `dy=${(V.bodyAABB(V.player).y-ishAabb0.y).toFixed(2)}`);
let ishGone=false;
for(let i=0;i<12;i++){
  holdRivalStill(); V.rival.guarding=false; V.keys.clear(); V.update(STEP);
  if(V.linkPlantFade(V.player)===0 || V.player.phase!=="startup"){ ishGone=V.linkPlantFade(V.player)===0; break; }
}
check("idle sheathe leftover plant clears while startup holds or advances", (ishGone || V.player.phase!=="startup") && V.linkPlantFade(V.player)===0, `gone=${ishGone} lf=${V.linkPlantFade(V.player)} ph=${V.player.phase} lp=${V.player.linkPlant}`);

// L from idle sheathe
toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.guarding=false; V.player.guardPoseK=0; V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0;
V.player.phase="idle"; V.player.phaseT=0; V.player.boltPhase=""; V.player.linkPlant=false;
V.keys.clear();
V.doClash(640, 360);
for(let i=0;i<40;i++){ holdRivalStill(); V.rival.guarding=false; V.update(STEP); if(V.player.phase==="idle" && V.player.sheatheT>0) break; }
check("idle sheathe leftover L setup sheathe", V.player.phase==="idle" && V.player.sheatheT>0 && V.sheatheFade(V.player)>0.5, `ph=${V.player.phase} sh=${V.player.sheatheT} sf=${V.sheatheFade(V.player)}`);
V.golpeEdge = true;
holdRivalStill(); V.rival.guarding=false; V.update(STEP);
check("idle sheathe leftover L keeps leftover sheathe", V.player.cut==="golpe" && V.player.phase==="startup" && V.player.linkPlant && V.linkPlantFade(V.player)>0.8 && V.player.telegraph===false && /windup/.test(sheetSrc(V.player)), `cut=${V.player.cut} ph=${V.player.phase} lp=${V.player.linkPlant} lf=${V.linkPlantFade(V.player)} tel=${V.player.telegraph} src=${sheetSrc(V.player)}`);

// K from idle sheathe
toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.guarding=false; V.player.guardPoseK=0; V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0;
V.player.phase="idle"; V.player.phaseT=0; V.player.boltPhase=""; V.player.linkPlant=false; V.player.boltHoldCut=false;
V.player.stamina=100;
V.keys.clear();
V.doClash(640, 360);
for(let i=0;i<40;i++){ holdRivalStill(); V.rival.guarding=false; V.update(STEP); if(V.player.phase==="idle" && V.player.sheatheT>0) break; }
check("idle sheathe leftover K setup sheathe", V.player.phase==="idle" && V.player.sheatheT>0 && V.sheatheFade(V.player)>0.5 && !V.player.boltPhase, `ph=${V.player.phase} sh=${V.player.sheatheT} sf=${V.sheatheFade(V.player)}`);
V.boltEdge = true;
holdRivalStill(); V.rival.guarding=false; V.update(STEP);
check("idle sheathe leftover K keeps leftover sheathe", V.player.boltPhase==="startup" && V.player.linkPlant && V.linkPlantFade(V.player)>0.8 && V.player.telegraph===false && !V.player.boltHoldCut && /throw_knife/.test(sheetSrc(V.player)), `bph=${V.player.boltPhase} lp=${V.player.linkPlant} lf=${V.linkPlantFade(V.player)} tel=${V.player.telegraph} hold=${V.player.boltHoldCut} src=${sheetSrc(V.player)}`);

// fresh idle Space still telegraphs (no leftover sheathe)
toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.guarding=false; V.player.guardPoseK=0; V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0;
V.player.phase="idle"; V.player.phaseT=0; V.player.boltPhase=""; V.player.linkPlant=false;
V.keys.clear();
V.attackEdge=true;
holdRivalStill(); V.update(STEP);
check("idle Space without sheathe still telegraphs", V.player.cut==="slash" && V.player.phase==="startup" && V.player.telegraph && V.player.linkPlant===false && V.linkPlantFade(V.player)===0 && V.telegraphFade(V.player)>=0, `cut=${V.player.cut} ph=${V.player.phase} tel=${V.player.telegraph} lp=${V.player.linkPlant} lf=${V.linkPlantFade(V.player)} tf=${V.telegraphFade(V.player)}`);

// connected slash-L leftover sheathe still eases
landCloseSlash();
const ishLinkGot = waitUntilWindow();
const ishLinkLeft = V.player.phase==="recovery" ? (V.RECOVERY - V.player.phaseT) : -1;
V.golpeEdge = true;
V.update(STEP);
check("connected slash-L leftover sheathe still eases after idle sheathe leftover", ishLinkGot && ishLinkLeft<=V.SLASH_CANCEL_MS && V.player.cut==="golpe" && V.player.phase==="startup" && V.player.linkPlant && V.linkPlantFade(V.player)>0.8 && V.player.clashPlant===false, `ok=${ishLinkGot} left=${ishLinkLeft} cut=${V.player.cut} ph=${V.player.phase} lp=${V.player.linkPlant} lf=${V.linkPlantFade(V.player)}`);

const ishThrowKd = landCloseThrow();
check("throw knockdown still not KO crumple after idle sheathe leftover", ishThrowKd && V.rival.hp===80 && V.rival.thrownT>0 && !V.rival.falling && V.crumpleFade(V.rival)===0, `ok=${ishThrowKd} hp=${V.rival.hp} th=${V.rival.thrownT} cf=${V.crumpleFade(V.rival)}`);
check("cache v=296 after idle sheathe leftover live", /game.js\?v=350/.test(htmlTxt));

// --- v202 spark origin leftover: clash-K / idle-sheathe-K cast puff rides leftover slash tip ---
check("spark origin leftover eases cast puff with leftover slash fade", /Spark origin leftover/.test(srcTxt) && /clash-K \/ idle-sheathe-K cast puff used to sit on the/.test(srcTxt) && /Ease origin slash→windup/.test(srcTxt) && /castPlantXY/.test(srcTxt));
check("spark origin leftover is draw-only leftover", /Draw-only \(castPlantXY\)/.test(srcTxt) && /clashPlantFade\(f\)/.test(sliceFn(codeOnly, "castPlantXY")) && /linkPlantFade\(f\)/.test(sliceFn(codeOnly, "castPlantXY")) && /poseFamily\(f\)\.slash/.test(sliceFn(codeOnly, "castPlantXY")) && !/castPlantXY/.test(sliceFn(codeOnly, "destRect")) && !/castPlantXY/.test(sliceFn(codeOnly, "bodyAABB")) && !/castPlantXY/.test(sliceFn(codeOnly, "slashPose")) && !/castPlantXY/.test(sliceFn(codeOnly, "landHit")));
check("spark origin leftover empty/telegraph K still throwKnife tip", /Empty\/telegraph K still plants on the throwKnife tip/.test(srcTxt) && /poseFamily\(f\)\.throwKnife/.test(sliceFn(codeOnly, "castPlantXY")));
check("spark origin leftover special-cancel still slash tip", /Special-cancel still rides the live slash tip/.test(srcTxt) && /holdingCutBolt\(f\)/.test(sliceFn(codeOnly, "castPlantXY")));
check("spark origin leftover dart births from castPlantXY tip", /Dart births from castPlantXY/.test(srcTxt) && /castPlantXY/.test(sliceFn(codeOnly, "spawnBolt")));
check("spark origin leftover is not a new combat verb", V.BOLT_CAST_FX_MS===220 && V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_CANCEL_MS===100 && V.CLASH_RECOVERY===200 && V.GUARD_RAISE_MS===80 && V.PUSHBLOCK_STAM===25 && V.PUSHBLOCK_PX===240);
check("slash frames still 180/140/280 after spark origin leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after spark origin leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after spark origin leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after spark origin leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after spark origin leftover", /game.js\?v=350/.test(htmlTxt));

function poseTipWorldX(f, tipX){
  // tipX under plant fades: bladeTipX eases under link/clash fades, so do not
  // recover scale from bladeTipX (that baked the ease into slashTipX). Use
  // destRect sheet scale + marker tipX instead.
  const r = V.destRect(f);
  const sheet = V.poseSheet(f);
  const sc = sheet && sheet.w ? r.dw / sheet.w : 0;
  return r.dx + tipX * sc;
}
function slashTipX(f){ return poseTipWorldX(f, 991); }
function knifeTipX(f){ return poseTipWorldX(f, 268); }
function windTipX(f){ return poseTipWorldX(f, 359); }

landClash();
const soCkGot = waitClashCancelDoor();
const soCkLeft = V.player.phase==="recovery" ? (V.RECOVERY - V.player.phaseT) : -1;
V.player.stamRegenT = 99999;
V.player.stamina = 100;
const soCkAabb0 = V.bodyAABB(V.player);
const soCkDest0 = V.destRect(V.player);
V.boltEdge = true;
V.update(STEP);
const soCkFade = V.clashPlantFade(V.player);
const soCkPlant = V.castPlantXY(V.player);
const soCkDest = knifeTipX(V.player);
const soCkSlash = slashTipX(V.player);
check("spark origin leftover clash-K setup", soCkGot && soCkLeft<=V.BOLT_CANCEL_MS && V.player.boltPhase==="startup" && V.player.clashPlant && soCkFade>0.8 && V.brasaFxKind==="cast" && !V.player.boltHoldCut, `got=${soCkGot} left=${soCkLeft} bph=${V.player.boltPhase} plant=${V.player.clashPlant} fade=${soCkFade} kind=${V.brasaFxKind} hold=${V.player.boltHoldCut}`);
check("spark origin leftover clash-K puff near leftover slash tip", Math.abs(soCkPlant.x-soCkSlash)<Math.abs(soCkPlant.x-soCkDest) && Math.abs(soCkPlant.x-soCkSlash)<40 && Math.abs(V.brasaX-soCkPlant.x)<4 && Math.abs(soCkPlant.x-soCkDest)>120, `plant=${soCkPlant.x.toFixed(1)} slash=${soCkSlash.toFixed(1)} dest=${soCkDest.toFixed(1)} brasa=${V.brasaX.toFixed(1)} fade=${soCkFade}`);
check("spark origin leftover clash-K AABB planted", Math.abs(V.bodyAABB(V.player).y-soCkAabb0.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-soCkAabb0.h)<0.05, `dy=${(V.bodyAABB(V.player).y-soCkAabb0.y).toFixed(2)}`);
check("spark origin leftover clash-K destRect planted", Math.abs(V.destRect(V.player).dy-soCkDest0.dy)<2.2 && Math.abs(V.destRect(V.player).rot)<0.001, `ddy=${(V.destRect(V.player).dy-soCkDest0.dy).toFixed(2)} rot=${V.destRect(V.player).rot.toFixed(3)}`);
let soCkMidPlant=null, soCkMidFade=-1, soCkMidDest=0, soCkMidSlash=0;
for(let i=0;i<3;i++){
  freezeRivalAI();
  V.player.stunT=0; V.player.falling=false; V.player.hp=100; V.player.gait=0; V.player.sheatheT=0;
  V.update(STEP);
  soCkMidFade=V.clashPlantFade(V.player);
  soCkMidPlant=V.castPlantXY(V.player);
  soCkMidDest=knifeTipX(V.player);
  soCkMidSlash=slashTipX(V.player);
}
check("spark origin leftover clash-K puff eases toward windup", soCkMidPlant && soCkMidFade>=0 && soCkMidFade<soCkFade-0.08 && soCkMidPlant.x < soCkPlant.x - 8 && soCkMidPlant.x > soCkMidDest + 8, `mid=${soCkMidPlant.x.toFixed(1)} t0=${soCkPlant.x.toFixed(1)} dest=${soCkMidDest.toFixed(1)} fade=${soCkMidFade} t0f=${soCkFade}`);
let soCkGone=false, soCkGonePlant=null, soCkGoneDest=0;
for(let i=0;i<6;i++){
  freezeRivalAI();
  V.player.stunT=0; V.player.falling=false; V.player.hp=100; V.player.gait=0; V.player.sheatheT=0;
  V.update(STEP);
  if(V.clashPlantFade(V.player)===0 && V.player.boltPhase==="startup"){
    soCkGone=true;
    soCkGonePlant=V.castPlantXY(V.player);
    soCkGoneDest=knifeTipX(V.player);
    break;
  }
}
check("spark origin leftover clash-K puff seats windup when fade clears", soCkGone && soCkGonePlant && Math.abs(soCkGonePlant.x-soCkGoneDest)<4 && V.player.boltPhase==="startup", `gone=${soCkGone} plant=${soCkGonePlant&&soCkGonePlant.x.toFixed(1)} dest=${soCkGoneDest.toFixed(1)} fade=${V.clashPlantFade(V.player)}`);

// idle sheathe K spark origin
toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.guarding=false; V.player.guardPoseK=0; V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0;
V.player.phase="idle"; V.player.phaseT=0; V.player.boltPhase=""; V.player.linkPlant=false; V.player.boltHoldCut=false; V.player.clashPlant=false;
V.player.stamina=100;
V.keys.clear();
V.doClash(640, 360);
for(let i=0;i<40;i++){ holdRivalStill(); V.rival.guarding=false; V.update(STEP); if(V.player.phase==="idle" && V.player.sheatheT>0) break; }
const soIshAabb0 = V.bodyAABB(V.player);
const soIshDest0 = V.destRect(V.player);
check("spark origin leftover idle-sheathe K setup", V.player.phase==="idle" && V.player.sheatheT>0 && V.sheatheFade(V.player)>0.5 && !V.player.boltPhase, `ph=${V.player.phase} sh=${V.player.sheatheT} sf=${V.sheatheFade(V.player)}`);
V.boltEdge = true;
holdRivalStill(); V.rival.guarding=false; V.update(STEP);
const soIshFade = V.linkPlantFade(V.player);
const soIshPlant = V.castPlantXY(V.player);
const soIshDest = knifeTipX(V.player);
const soIshSlash = slashTipX(V.player);
check("spark origin leftover idle-sheathe K puff near leftover slash tip", V.player.boltPhase==="startup" && V.player.linkPlant && soIshFade>0.8 && V.brasaFxKind==="cast" && Math.abs(soIshPlant.x-soIshSlash)<Math.abs(soIshPlant.x-soIshDest) && Math.abs(soIshPlant.x-soIshSlash)<40 && Math.abs(V.brasaX-soIshPlant.x)<4 && Math.abs(soIshPlant.x-soIshDest)>120, `bph=${V.player.boltPhase} lp=${V.player.linkPlant} fade=${soIshFade} plant=${soIshPlant.x.toFixed(1)} slash=${soIshSlash.toFixed(1)} dest=${soIshDest.toFixed(1)} brasa=${V.brasaX.toFixed(1)}`);
check("spark origin leftover idle-sheathe K AABB planted", Math.abs(V.bodyAABB(V.player).y-soIshAabb0.y)<0.05, `dy=${(V.bodyAABB(V.player).y-soIshAabb0.y).toFixed(2)}`);
check("spark origin leftover idle-sheathe K destRect planted", Math.abs(V.destRect(V.player).dy-soIshDest0.dy)<2.2, `ddy=${(V.destRect(V.player).dy-soIshDest0.dy).toFixed(2)}`);

// empty idle K still windup tip (no leftover)
toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.guarding=false; V.player.guardPoseK=0; V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0;
V.player.phase="idle"; V.player.phaseT=0; V.player.boltPhase=""; V.player.linkPlant=false; V.player.clashPlant=false; V.player.boltHoldCut=false;
V.player.stamina=100; V.player.meter=0;
V.keys.clear();
V.boltEdge=true;
holdRivalStill(); V.update(STEP);
const soEmptyPlant = V.castPlantXY(V.player);
const soEmptyDest = knifeTipX(V.player);
const soEmptyEdge = V.destRect(V.player).dx + V.destRect(V.player).dw - 8;
check("spark origin leftover empty K still windup tip", V.player.boltPhase==="startup" && V.player.telegraph && !V.player.linkPlant && !V.player.clashPlant && V.linkPlantFade(V.player)===0 && V.clashPlantFade(V.player)===0 && Math.abs(soEmptyPlant.x-soEmptyDest)<4 && Math.abs(soEmptyPlant.x-soEmptyEdge)>80 && V.brasaFxKind==="cast", `bph=${V.player.boltPhase} tel=${V.player.telegraph} plant=${soEmptyPlant.x.toFixed(1)} dest=${soEmptyDest.toFixed(1)} edge=${soEmptyEdge.toFixed(1)}`);

const soThrowKd = landCloseThrow();
check("throw knockdown still not KO crumple after spark origin leftover", soThrowKd && V.rival.hp===80 && V.rival.thrownT>0 && !V.rival.falling && V.crumpleFade(V.rival)===0, `ok=${soThrowKd} hp=${V.rival.hp} th=${V.rival.thrownT} cf=${V.crumpleFade(V.rival)}`);
check("cache v=296 after spark origin leftover live", /game.js\?v=350/.test(htmlTxt));

// --- v205 wakeup→guard leftover: leftover crumple keeps through S-hold raise ---
check("wakeup→guard leftover keeps leftover crumple through raise", /Wakeup→guard leftover/.test(srcTxt) && /leftover crumple fade used to die the tick/.test(srcTxt) && /S raised mid-getup/.test(srcTxt) && /Keep leftover crumple through leftover throw-invuln/.test(srcTxt) && /wakeupFade gated on guarding/.test(srcTxt));
check("wakeup→guard leftover is draw-only leftover", /Draw-only \(wakeupFade\)/.test(srcTxt) && /wakeupFade\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /restWakeGuard/.test(sliceFn(codeOnly, "drawKnight")) && /restWake \|\| restWakeGuard/.test(sliceFn(codeOnly, "drawKnight")) && !/wakeupFade/.test(sliceFn(codeOnly, "destRect")) && !/wakeupFade/.test(sliceFn(codeOnly, "bodyAABB")) && !/wakeupFade/.test(sliceFn(codeOnly, "slashPose")) && !/wakeupFade/.test(sliceFn(codeOnly, "landHit")) && !/wakeupFade\(/.test(sliceFn(codeOnly, "updateGuard")) && !/wakeupFade/.test(sliceFn(codeOnly, "startReversal")));
check("wakeup→guard leftover poseBitmap still block once guarding", /poseBitmap still block once guarding/.test(srcTxt) && /if \(f\.guarding && ready\(block\)\) return block;/.test(sliceFn(codeOnly, "poseBitmap")) && !/wakeupFade|restWakeGuard|throwInvulnT/.test(sliceFn(codeOnly, "poseBitmap")));
check("wakeup→guard leftover no longer gates wakeupFade on guarding", !/guarding/.test(sliceFn(codeOnly, "wakeupFade")) && /if \(f\.phase !== "idle"\) return 0;/.test(sliceFn(codeOnly, "wakeupFade")) && /Keep leftover crumple through leftover throw-invuln if they raise/.test(srcTxt));
check("wakeup→guard leftover rests crumple on block sheet", /restWakeGuard/.test(sliceFn(codeOnly, "drawKnight")) && /restWake \|\| restWakeGuard/.test(sliceFn(codeOnly, "drawKnight")) && /Idle getup still rests on idle/.test(srcTxt));
check("wakeup→guard leftover idle getup path unchanged", /Idle getup still fades hurt→idle/.test(srcTxt) && /THROW_WAKE_INVULN/.test(sliceFn(codeOnly, "wakeupFade")) && V.THROW_WAKE_INVULN===80);
check("wakeup→guard leftover reversal plant path still gated", /if \(f\.phase !== "idle"\) return 0;/.test(sliceFn(codeOnly, "wakeupFade")) && /Idle getup still fades hurt→idle/.test(srcTxt));
check("wakeup→guard leftover meaty leftover still holds", /Meaty leftover/.test(srcTxt) && /leftover getup fade no longer dies the tick/.test(srcTxt) && !/f\.stunT > 0/.test(sliceFn(codeOnly, "wakeupFade")));
check("wakeup→guard leftover is not a new combat verb", V.THROW_WAKE_INVULN===80 && V.HITSTUN===350 && V.THROW_KD_MS===520 && V.HITSTOP_HIT===140 && V.GUARD_COMMIT_MS===140 && V.PUSHBLOCK_STAM===25 && V.PUSHBLOCK_PX===240);
check("slash frames still 180/140/280 after wakeup→guard leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after wakeup→guard leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after wakeup→guard leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after wakeup→guard leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after wakeup→guard leftover", /game.js\?v=350/.test(htmlTxt));

toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.guarding=false; V.player.guardPoseK=0; V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0;
V.player.phase="idle"; V.player.phaseT=0; V.player.boltPhase=""; V.player.falling=false; V.player.stunT=0;
V.player.thrownT=0; V.player.throwInvulnT=V.THROW_WAKE_INVULN; V.player.wakeRev=true; V.player.hp=80;
V.player.walkSettleT=0; V.keys.clear();
const wgFade0 = V.wakeupFade(V.player);
const wgAabb0 = V.bodyAABB(V.player);
const wgDest0 = V.destRect(V.player);
check("wakeup→guard leftover getup live", V.player.phase==="idle" && V.player.throwInvulnT>0 && wgFade0>0.85 && V.player.guarding===false && !isHurtSheet(V.player) && !isBlockSheet(V.player), `ph=${V.player.phase} inv=${V.player.throwInvulnT} fade=${wgFade0} g=${V.player.guarding} src=${sheetSrc(V.player)}`);
V.keys.add("KeyS");
holdRivalStill();
V.update(STEP);
const wgFadeRaise = V.wakeupFade(V.player);
check("wakeup→guard leftover raise keeps leftover crumple", V.player.guarding && V.player.throwInvulnT>0 && V.player.phase==="idle" && wgFadeRaise>0.5 && V.wakeupFade(V.player)>0 && isBlockSheet(V.player), `g=${V.player.guarding} inv=${V.player.throwInvulnT} fade=${wgFadeRaise} src=${sheetSrc(V.player)}`);
check("wakeup→guard leftover poseBitmap block not hurt", isBlockSheet(V.player) && !isHurtSheet(V.player) && !isCutSheet(V.player) && !/windup/.test(sheetSrc(V.player)), `src=${sheetSrc(V.player)}`);
check("wakeup→guard leftover AABB planted through raise", Math.abs(V.bodyAABB(V.player).y-wgAabb0.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-wgAabb0.h)<0.05, `dy=${(V.bodyAABB(V.player).y-wgAabb0.y).toFixed(2)}`);
check("wakeup→guard leftover destRect planted through raise", Math.abs(V.destRect(V.player).dy-wgDest0.dy)<2.2 && Math.abs(V.destRect(V.player).rot)<0.2, `ddy=${(V.destRect(V.player).dy-wgDest0.dy).toFixed(2)} rot=${V.destRect(V.player).rot.toFixed(3)}`);
let wgFadeMid=-1, wgInvMid=-1;
for(let i=0;i<2;i++){
  holdRivalStill();
  V.keys.add("KeyS");
  V.update(STEP);
  wgFadeMid=V.wakeupFade(V.player);
  wgInvMid=V.player.throwInvulnT;
}
check("wakeup→guard leftover fade drops while guard holds", wgFadeMid>=0 && wgFadeMid<wgFadeRaise-0.05 && wgInvMid>0 && V.player.guarding && isBlockSheet(V.player), `mid=${wgFadeMid} t0=${wgFadeRaise} inv=${wgInvMid} g=${V.player.guarding} src=${sheetSrc(V.player)}`);
check("wakeup→guard leftover AABB planted through fade", Math.abs(V.bodyAABB(V.player).y-wgAabb0.y)<0.05, `dy=${(V.bodyAABB(V.player).y-wgAabb0.y).toFixed(2)}`);
let wgGone=false;
for(let i=0;i<12;i++){
  holdRivalStill();
  V.keys.add("KeyS");
  V.update(STEP);
  if(V.player.throwInvulnT===0 && V.wakeupFade(V.player)===0 && V.player.guarding){
    wgGone=true;
    break;
  }
}
check("wakeup→guard leftover fade gone while guard holds", wgGone && V.player.throwInvulnT===0 && V.wakeupFade(V.player)===0 && V.player.guarding && isBlockSheet(V.player), `gone=${wgGone} inv=${V.player.throwInvulnT} fade=${V.wakeupFade(V.player)} g=${V.player.guarding} src=${sheetSrc(V.player)}`);

// rival getup raise (same leftover)
const wgRLand = landCloseThrow();
let wgRReady=false, wgRFade0=-1, wgRAabb0=null, wgRDest0=null;
for(let i=0;i<80;i++){
  holdWakeSample();
  V.update(STEP);
  if(V.rival.thrownT<=0 && V.rival.throwInvulnT>0 && V.rival.hp>0 && !V.rival.falling){
    wgRReady=true;
    wgRFade0=V.wakeupFade(V.rival);
    wgRAabb0=V.bodyAABB(V.rival);
    wgRDest0=V.destRect(V.rival);
    break;
  }
}
check("wakeup→guard leftover rival getup ready", wgRLand && wgRReady && wgRFade0>0.5 && V.rival.guarding===false && !isHurtSheet(V.rival), `land=${wgRLand} ready=${wgRReady} fade=${wgRFade0} g=${V.rival.guarding} src=${sheetSrc(V.rival)}`);
V.rival.guarding=true; V.rival.wantBlock=true; V.rival.gait=0; V.rival.phase="idle"; V.rival.boltPhase="";
const wgRFadeRaise=V.wakeupFade(V.rival);
check("wakeup→guard leftover rival raise keeps leftover crumple", V.rival.guarding && V.rival.throwInvulnT>0 && V.rival.phase==="idle" && wgRFadeRaise>0.5 && isBlockSheet(V.rival), `g=${V.rival.guarding} inv=${V.rival.throwInvulnT} fade=${wgRFadeRaise} src=${sheetSrc(V.rival)}`);
check("wakeup→guard leftover rival poseBitmap block not hurt", isBlockSheet(V.rival) && !isHurtSheet(V.rival), `src=${sheetSrc(V.rival)}`);
check("wakeup→guard leftover rival AABB planted", Math.abs(V.bodyAABB(V.rival).y-wgRAabb0.y)<0.05 && Math.abs(V.bodyAABB(V.rival).h-wgRAabb0.h)<0.05, `dy=${(V.bodyAABB(V.rival).y-wgRAabb0.y).toFixed(2)}`);
check("wakeup→guard leftover rival destRect planted", Math.abs(V.destRect(V.rival).dy-wgRDest0.dy)<2.2, `ddy=${(V.destRect(V.rival).dy-wgRDest0.dy).toFixed(2)} rot=${V.destRect(V.rival).rot.toFixed(3)}`);

// idle getup without S still fades hurt→idle
const wgIdleLand = landCloseThrow();
let wgIdleReady=false, wgIdleFade0=-1;
for(let i=0;i<80;i++){
  holdWakeSample();
  V.update(STEP);
  if(V.rival.thrownT<=0 && V.rival.throwInvulnT>0 && V.rival.hp>0 && !V.rival.falling){
    wgIdleReady=true;
    wgIdleFade0=V.wakeupFade(V.rival);
    break;
  }
}
check("wakeup→guard leftover idle getup still fades", wgIdleLand && wgIdleReady && wgIdleFade0>0.5 && V.rival.guarding===false && !isHurtSheet(V.rival) && !isBlockSheet(V.rival), `land=${wgIdleLand} ready=${wgIdleReady} fade=${wgIdleFade0} g=${V.rival.guarding} src=${sheetSrc(V.rival)}`);

// wakeup→reversal leftover: crumple keeps through plant (was "still cuts" at v205)
const wgRevLand = landCloseThrow();
let wgRevReady=false;
for(let i=0;i<80;i++){
  holdWakeSample();
  V.update(STEP);
  if(V.rival.thrownT<=0 && V.rival.throwInvulnT>0){ wgRevReady=true; break; }
}
V.rival.stamina=100;
const wgRevFade0 = V.wakeupFade(V.rival);
const wgRevDid = V.startWakeReversal(V.rival);
check("wakeup→reversal leftover keeps crumple through plant", wgRevLand && wgRevReady && wgRevDid && wgRevFade0>0.5 && V.wakeupFade(V.rival)>0.5 && V.rival.throwInvulnT===0 && V.rival.wakeRevFadeHold>0 && V.rival.cut==="golpe" && V.rival.phase==="startup", `land=${wgRevLand} ready=${wgRevReady} did=${wgRevDid} fade0=${wgRevFade0} fade=${V.wakeupFade(V.rival)} hold=${V.rival.wakeRevFadeHold} inv=${V.rival.throwInvulnT} cut=${V.rival.cut} ph=${V.rival.phase}`);
check("wakeup→reversal leftover poseBitmap windup not hurt", /windup/.test(sheetSrc(V.rival)) && !isHurtSheet(V.rival), `src=${sheetSrc(V.rival)}`);
check("wakeup→reversal leftover throw still spends invuln", V.rival.throwInvulnT===0 && V.rival.wakeRev===false);

const wgThrowKd = landCloseThrow();
check("throw knockdown still not KO crumple after wakeup→guard leftover", wgThrowKd && V.rival.hp===80 && V.rival.thrownT>0 && !V.rival.falling && V.crumpleFade(V.rival)===0, `ok=${wgThrowKd} hp=${V.rival.hp} th=${V.rival.thrownT} cf=${V.crumpleFade(V.rival)}`);
check("cache v=296 after wakeup→guard leftover live", /game.js\?v=350/.test(htmlTxt));


// --- v206 walk→guard leftover: leftover walk keeps through S-hold raise ---
check("walk→guard leftover keeps leftover walk through raise", /Walk→guard leftover/.test(srcTxt) && /leftover walk fade used to die the tick/.test(srcTxt) && /S raised mid-stride/.test(srcTxt) && /Keep leftover walk through leftover walkFadeHold/.test(srcTxt) && /walkFade gated on guarding/.test(srcTxt));
check("walk→guard leftover is draw-only leftover", /Draw-only \(walkFade\)/.test(srcTxt) && /walkFade\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /restWalkGuard/.test(sliceFn(codeOnly, "drawKnight")) && !/walkFade/.test(sliceFn(codeOnly, "destRect")) && !/walkFade/.test(sliceFn(codeOnly, "bodyAABB")) && !/walkFade/.test(sliceFn(codeOnly, "slashPose")) && !/walkFade/.test(sliceFn(codeOnly, "landHit")) && !/walkFade\(/.test(sliceFn(codeOnly, "updateGuard")) && !/walkFade\(/.test(sliceFn(codeOnly, "startAttack")));
check("walk→guard leftover poseBitmap still block once guarding", /poseBitmap still block once guarding/.test(srcTxt) && /if \(f\.guarding && ready\(block\)\) return block;/.test(sliceFn(codeOnly, "poseBitmap")) && !/walkFade|restWalkGuard|walkFadeHold/.test(sliceFn(codeOnly, "poseBitmap")));
check("walk→guard leftover no longer gates walkFade on guarding", !/\|\| f\.guarding/.test(sliceFn(codeOnly, "walkFade")) && !/if \(f\.guarding\)/.test(sliceFn(codeOnly, "walkFade")) && /walkFadeHold \* walkSettleK/.test(sliceFn(codeOnly, "walkFade")) && /Keep leftover walk through leftover walkFadeHold if they raise/.test(srcTxt));
check("walk→guard leftover tickGait keeps hold through raise", /Walk→guard leftover/.test(srcTxt) && /Keep leftover hold through leftover settle if they raise/.test(srcTxt) && !/f\.phase !== "idle" \|\| f\.guarding \|\| f\.stunT/.test(sliceFn(codeOnly, "tickGait")) && /if \(f\.guarding\)/.test(sliceFn(codeOnly, "tickGait")));
check("walk→guard leftover rests walk on block sheet", /restWalkGuard/.test(sliceFn(codeOnly, "drawKnight")) && /Idle walk settle still rests on idle/.test(srcTxt));
check("walk→guard leftover idle walk settle path unchanged", /Idle walk settle still fades walk→idle/.test(srcTxt) && V.WALK_SETTLE_MS===160 && /walkFadeHold \* walkSettleK/.test(sliceFn(codeOnly, "walkFade")));
check("walk→guard leftover destRect lean eases through raise", /Walk→guard destRect lean leftover/.test(srcTxt) && /leftover walk lean\/dip used to dump/.test(srcTxt) && /walkSettleK gated guarding \+ dedicated block/.test(srcTxt) && /Keep leftover settle rot\/oy through leftover walkSettleT/.test(srcTxt));
check("walk→guard destRect lean leftover is destRect-only ease", /destRect-only ease \(walkSettleK\)/.test(srcTxt) && /if \(f\.guarding\)/.test(sliceFn(codeOnly, "walkSettleK")) && /walkSettleRot \* sk/.test(sliceFn(codeOnly, "destRect")) && /walkSettleOy \* sk/.test(sliceFn(codeOnly, "destRect")) && !/walkSettleK/.test(sliceFn(codeOnly, "bodyAABB")) && !/walkSettleK/.test(sliceFn(codeOnly, "landHit")) && !/walkSettleK/.test(sliceFn(codeOnly, "updateGuard")) && !/walkSettleK/.test(sliceFn(codeOnly, "startAttack")));
check("walk→guard destRect lean leftover walkSettleK keeps through raise", /Keep leftover settle through leftover walkSettleT if they raise/.test(srcTxt) && !/f\.phase !== "idle" \|\| f\.guarding \|\| f\.stunT/.test(sliceFn(codeOnly, "walkSettleK")) && /if \(f\.guarding\)/.test(sliceFn(codeOnly, "walkSettleK")) && /usingDedicatedPose/.test(sliceFn(codeOnly, "walkSettleK")));
check("walk→guard destRect lean leftover breath uses settle+raise k", /Idle settle→guard destRect leftover/.test(srcTxt) && /f\.walkSettleT > 0/.test(sliceFn(codeOnly, "idleBreath")) && /Math\.max\(sk, rk\)/.test(sliceFn(codeOnly, "idleBreath")) && /walkSettleK\(f\)/.test(sliceFn(codeOnly, "idleBreath")) && /guardRaiseK\(f\)/.test(sliceFn(codeOnly, "idleBreath")));
check("walk→guard destRect lean leftover walkFade uses walkSettleK", /destRect lean\s*eases with walkSettleK/.test(srcTxt) && /walkFadeHold \* walkSettleK/.test(sliceFn(codeOnly, "walkFade")) && !/walkSettleK gates guarding/.test(srcTxt));
check("walk→guard destRect lean leftover poseBitmap still block", /poseBitmap still block once guarding/.test(srcTxt) && /if \(f\.guarding && ready\(block\)\) return block;/.test(sliceFn(codeOnly, "poseBitmap")));
check("walk→guard destRect lean leftover extra rot stays 0", /Extra destRect rot\s*stays 0 \(leftover walk lean was already non-zero/.test(srcTxt) && /Chip stun \/ throw KD unchanged/.test(srcTxt));
check("walk→guard destRect lean leftover is not a new combat verb", V.WALK_SETTLE_MS===160 && V.WALK_STEP_MS===320 && V.GUARD_RAISE_MS===80 && V.HITSTUN===350 && V.THROW_KD_MS===520 && V.HITSTOP_HIT===140 && V.GUARD_COMMIT_MS===140 && V.PUSHBLOCK_STAM===25 && V.PUSHBLOCK_PX===240);
check("slash frames still 180/140/280 after walk→guard destRect lean leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after walk→guard destRect lean leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after walk→guard destRect lean leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after walk→guard destRect lean leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after walk→guard destRect lean leftover", /game.js\?v=350/.test(htmlTxt));
check("walk→guard leftover stun still cuts", /Stun still cuts/.test(srcTxt) && /f\.stunT > 0/.test(sliceFn(codeOnly, "walkFade")));
check("walk→guard leftover is not a new combat verb", V.WALK_SETTLE_MS===160 && V.WALK_STEP_MS===320 && V.HITSTUN===350 && V.THROW_KD_MS===520 && V.HITSTOP_HIT===140 && V.GUARD_COMMIT_MS===140 && V.PUSHBLOCK_STAM===25 && V.PUSHBLOCK_PX===240);
check("slash frames still 180/140/280 after walk→guard leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after walk→guard leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after walk→guard leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after walk→guard leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after walk→guard leftover", /game.js\?v=350/.test(htmlTxt));

bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x = 400; V.rival.x = 1100;
V.player.guarding = false; V.player.stunT = 0; V.player.phase = "idle";
V.player.boltPhase = ""; V.player.falling = false; V.player.guardPoseK = 0;
V.player.gait = 0; V.player.walkT = 0; V.player.walkSettleT = 0; V.player.walkFadeHold = 0;
V.keys.add("KeyD");
let wgWalkReady=false, wgWalkFade0=-1, wgWalkAabb0=null, wgWalkDest0=null;
for(let i=0;i<12;i++){
  freezeRivalAI();
  V.player.phase="idle"; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase="";
  V.keys.add("KeyD");
  V.update(STEP);
  if(V.player.gait!==0 && V.walkFade(V.player)>0.2){
    wgWalkReady=true;
    wgWalkFade0=V.walkFade(V.player);
    wgWalkAabb0=V.bodyAABB(V.player);
    wgWalkDest0=V.destRect(V.player);
    break;
  }
}
check("walk→guard leftover stride live", wgWalkReady && V.player.gait!==0 && wgWalkFade0>0.2 && V.player.guarding===false && /walk/.test(sheetSrc(V.player)), `ready=${wgWalkReady} gait=${V.player.gait} fade=${wgWalkFade0} g=${V.player.guarding} src=${sheetSrc(V.player)}`);
V.keys.delete("KeyD");
V.keys.add("KeyS");
freezeRivalAI();
V.update(STEP);
const wgWalkRaise=V.walkFade(V.player);
check("walk→guard leftover raise keeps leftover walk", V.player.guarding && V.player.phase==="idle" && wgWalkRaise>0.15 && V.walkFade(V.player)>0 && V.player.walkFadeHold>0 && V.player.walkSettleT>0 && isBlockSheet(V.player), `g=${V.player.guarding} fade=${wgWalkRaise} hold=${V.player.walkFadeHold} settle=${V.player.walkSettleT} src=${sheetSrc(V.player)}`);
check("walk→guard leftover poseBitmap block not walk", isBlockSheet(V.player) && !/walk/.test(sheetSrc(V.player)) && !isHurtSheet(V.player) && !isCutSheet(V.player), `src=${sheetSrc(V.player)}`);
check("walk→guard leftover AABB planted through raise", wgWalkAabb0 && Math.abs(V.bodyAABB(V.player).y-wgWalkAabb0.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-wgWalkAabb0.h)<0.05, `dy=${wgWalkAabb0? (V.bodyAABB(V.player).y-wgWalkAabb0.y).toFixed(2): "na"}`);
check("walk→guard leftover destRect rot not wild", wgWalkDest0 && Math.abs(V.destRect(V.player).rot)<0.25, `rot=${V.destRect(V.player).rot.toFixed(3)} ddy=${wgWalkDest0? (V.destRect(V.player).dy-wgWalkDest0.dy).toFixed(2): "na"}`);
let wgWalkMid=-1;
for(let i=0;i<3;i++){
  freezeRivalAI();
  V.keys.add("KeyS");
  V.update(STEP);
  wgWalkMid=V.walkFade(V.player);
}
check("walk→guard leftover fade drops while guard holds", wgWalkMid>=0 && wgWalkMid<wgWalkRaise-0.05 && V.player.guarding && isBlockSheet(V.player) && V.player.walkSettleT>0, `mid=${wgWalkMid} t0=${wgWalkRaise} settle=${V.player.walkSettleT} g=${V.player.guarding} src=${sheetSrc(V.player)}`);
check("walk→guard leftover AABB planted through fade", wgWalkAabb0 && Math.abs(V.bodyAABB(V.player).y-wgWalkAabb0.y)<0.05, `dy=${wgWalkAabb0? (V.bodyAABB(V.player).y-wgWalkAabb0.y).toFixed(2): "na"}`);
let wgWalkGone=false;
for(let i=0;i<20;i++){
  freezeRivalAI();
  V.keys.add("KeyS");
  V.update(STEP);
  if(V.player.walkSettleT===0 && V.walkFade(V.player)===0 && V.player.guarding){
    wgWalkGone=true;
    break;
  }
}
check("walk→guard leftover fade gone while guard holds", wgWalkGone && V.player.walkSettleT===0 && V.walkFade(V.player)===0 && V.player.guarding && isBlockSheet(V.player), `gone=${wgWalkGone} settle=${V.player.walkSettleT} fade=${V.walkFade(V.player)} g=${V.player.guarding} src=${sheetSrc(V.player)}`);

// settle→guard: release walk then raise mid-settle
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x = 400; V.rival.x = 1100;
V.player.guarding=false; V.player.guardPoseK=0; V.player.phase="idle"; V.player.boltPhase=""; V.player.stunT=0; V.player.falling=false;
V.player.gait=0; V.player.walkT=0; V.player.walkSettleT=0; V.player.walkFadeHold=0;
V.keys.add("KeyD");
for(let i=0;i<16;i++){
  freezeRivalAI();
  V.player.phase="idle"; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase="";
  V.keys.add("KeyD");
  V.update(STEP);
}
V.keys.delete("KeyD");
freezeRivalAI();
V.update(STEP);
const wgSettleFade0=V.walkFade(V.player);
const wgSettleAabb0=V.bodyAABB(V.player);
check("walk→guard leftover settle live", V.player.walkSettleT>0 && wgSettleFade0>0.2 && V.player.guarding===false && V.player.gait===0, `settle=${V.player.walkSettleT} fade=${wgSettleFade0} g=${V.player.guarding} gait=${V.player.gait}`);
V.keys.add("KeyS");
freezeRivalAI();
V.update(STEP);
const wgSettleRaise=V.walkFade(V.player);
check("walk→guard leftover settle raise keeps leftover walk", V.player.guarding && wgSettleRaise>0.1 && V.player.walkFadeHold>0 && V.player.walkSettleT>0 && isBlockSheet(V.player), `g=${V.player.guarding} fade=${wgSettleRaise} hold=${V.player.walkFadeHold} settle=${V.player.walkSettleT} src=${sheetSrc(V.player)}`);
check("walk→guard leftover settle AABB planted", Math.abs(V.bodyAABB(V.player).y-wgSettleAabb0.y)<0.05, `dy=${(V.bodyAABB(V.player).y-wgSettleAabb0.y).toFixed(2)}`);

// idle walk settle without S still fades walk→idle
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x = 400; V.rival.x = 1100;
V.player.guarding=false; V.player.phase="idle"; V.player.boltPhase=""; V.player.stunT=0;
V.keys.add("KeyD");
for(let i=0;i<16;i++){
  freezeRivalAI();
  V.player.phase="idle"; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase="";
  V.keys.add("KeyD");
  V.update(STEP);
}
V.keys.delete("KeyD");
freezeRivalAI();
V.update(STEP);
const wgIdleSettle=V.walkFade(V.player);
check("walk→guard leftover idle settle still fades", V.player.walkSettleT>0 && wgIdleSettle>0.2 && V.player.guarding===false && !isBlockSheet(V.player), `settle=${V.player.walkSettleT} fade=${wgIdleSettle} g=${V.player.guarding} src=${sheetSrc(V.player)}`);

// rival walk→guard (pin guarding like wakeup→guard rival — AI would drop S)
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.rival.x = 900; V.player.x = 400;
V.rival.guarding=false; V.rival.phase="idle"; V.rival.boltPhase=""; V.rival.stunT=0; V.rival.falling=false;
V.rival.gait=1; V.rival.walkT=140; V.rival.walkFadeHold=0; V.rival.walkSettleT=0;
for(let i=0;i<4;i++){
  V.rival.gait=1; V.rival.phase="idle"; V.rival.guarding=false; V.rival.stunT=0; V.rival.falling=false; V.rival.boltPhase="";
  holdRivalStill();
  V.update(STEP);
}
const wgWalkRFade0=V.walkFade(V.rival);
const wgWalkRHold0=V.rival.walkFadeHold;
const wgWalkRAabb0=V.bodyAABB(V.rival);
// Raise without AI clearing: keep leftover hold + settle clock, poseBitmap block.
V.rival.guarding=true; V.rival.wantBlock=true; V.rival.gait=0; V.rival.walkT=0; V.rival.phase="idle"; V.rival.boltPhase="";
V.rival.walkSettleT=V.WALK_SETTLE_MS;
if(V.rival.walkFadeHold<=0) V.rival.walkFadeHold=wgWalkRHold0>0?wgWalkRHold0:0.8;
const wgWalkRRaise=V.walkFade(V.rival);
check("walk→guard leftover rival raise keeps leftover walk", wgWalkRFade0>0.05 && V.rival.guarding && wgWalkRRaise>0.05 && V.rival.walkFadeHold>0 && isBlockSheet(V.rival), `fade0=${wgWalkRFade0} raise=${wgWalkRRaise} hold=${V.rival.walkFadeHold} hold0=${wgWalkRHold0} g=${V.rival.guarding} src=${sheetSrc(V.rival)}`);
check("walk→guard leftover rival poseBitmap block", isBlockSheet(V.rival) && !/walk/.test(sheetSrc(V.rival)), `src=${sheetSrc(V.rival)}`);
check("walk→guard leftover rival AABB planted", Math.abs(V.bodyAABB(V.rival).y-wgWalkRAabb0.y)<0.05, `dy=${(V.bodyAABB(V.rival).y-wgWalkRAabb0.y).toFixed(2)}`);

const wgThrowKd2 = landCloseThrow();
check("throw knockdown still not KO crumple after walk→guard leftover", wgThrowKd2 && V.rival.hp===80 && V.rival.thrownT>0 && !V.rival.falling && V.crumpleFade(V.rival)===0, `ok=${wgThrowKd2} hp=${V.rival.hp} th=${V.rival.thrownT} cf=${V.crumpleFade(V.rival)}`);
check("cache v=296 after walk→guard leftover live", /game.js\?v=350/.test(htmlTxt));


// --- v207 walk→guard destRect lean leftover: leftover lean eases through S-hold raise ---
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x = 400; V.rival.x = 1100;
V.player.guarding = false; V.player.stunT = 0; V.player.phase = "idle";
V.player.boltPhase = ""; V.player.falling = false; V.player.guardPoseK = 0; V.player.hp = 100;
V.player.gait = 0; V.player.walkT = 0; V.player.walkSettleT = 0; V.player.walkFadeHold = 0; V.player.walkRiseT = 0;
V.keys.add("KeyD");
let wlReady=false, wlRot0=0, wlDy0=0, wlAabb0=null, wlSettleRot=0;
for(let i=0;i<40;i++){
  freezeRivalAI();
  V.player.phase="idle"; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase="";
  V.keys.add("KeyD");
  V.update(STEP);
  if(V.player.walkT>=150 && V.player.walkRiseT>=V.WALK_SETTLE_MS && Math.abs(V.destRect(V.player).rot)>0.03){
    wlReady=true;
    wlRot0=V.destRect(V.player).rot;
    wlDy0=V.destRect(V.player).dy;
    wlAabb0=V.bodyAABB(V.player);
    break;
  }
}
check("walk→guard destRect lean leftover stride live", wlReady && V.player.gait!==0 && Math.abs(wlRot0)>0.03 && V.player.guarding===false, `ready=${wlReady} gait=${V.player.gait} rot0=${wlRot0.toFixed(4)} g=${V.player.guarding}`);
V.keys.delete("KeyD");
V.keys.add("KeyS");
freezeRivalAI();
V.update(STEP);
const wlRot1=V.destRect(V.player).rot;
const wlDy1=V.destRect(V.player).dy;
wlSettleRot=V.player.walkSettleRot;
const wlSk1=(V.player.walkSettleT/V.WALK_SETTLE_MS)*(V.player.walkSettleT/V.WALK_SETTLE_MS);
const wlExpect1=wlSettleRot*wlSk1 + V.player.facing*-0.10*V.guardRaiseK(V.player);
check("walk→guard destRect lean leftover raise keeps leftover lean", V.player.guarding && V.player.walkSettleT>0 && Math.abs(wlRot1-wlRot0)<0.03 && Math.abs(wlRot1-wlExpect1)<0.01, `g=${V.player.guarding} rot0=${wlRot0.toFixed(4)} rot1=${wlRot1.toFixed(4)} expect=${wlExpect1.toFixed(4)} settleRot=${wlSettleRot.toFixed(4)} sk=${wlSk1.toFixed(3)} settleT=${V.player.walkSettleT}`);
check("walk→guard destRect lean leftover raise does not dump lean", Math.abs(wlRot1-wlRot0)<0.03 && !(wlRot1 < wlRot0 - 0.04), `drot=${(wlRot1-wlRot0).toFixed(4)} rot0=${wlRot0.toFixed(4)} rot1=${wlRot1.toFixed(4)}`);
check("walk→guard destRect lean leftover poseBitmap block on raise", isBlockSheet(V.player) && !/walk/.test(sheetSrc(V.player)), `src=${sheetSrc(V.player)}`);
check("walk→guard destRect lean leftover AABB planted on raise", wlAabb0 && Math.abs(V.bodyAABB(V.player).y-wlAabb0.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-wlAabb0.h)<0.05, `dy=${wlAabb0?(V.bodyAABB(V.player).y-wlAabb0.y).toFixed(2):"na"}`);
let wlRotMid=0, wlSettleMid=0;
for(let i=0;i<4;i++){
  freezeRivalAI();
  V.keys.add("KeyS");
  V.update(STEP);
  wlRotMid=V.destRect(V.player).rot;
  wlSettleMid=V.player.walkSettleT;
}
const wlSkMid=(wlSettleMid/V.WALK_SETTLE_MS)*(wlSettleMid/V.WALK_SETTLE_MS);
check("walk→guard destRect lean leftover lean eases while guard holds", V.player.guarding && wlSettleMid>0 && wlSettleMid<V.WALK_SETTLE_MS && wlSkMid<0.95 && Math.abs(wlRotMid - (wlSettleRot*wlSkMid + V.player.facing*-0.10*V.guardRaiseK(V.player)))<0.015, `midRot=${wlRotMid.toFixed(4)} expect=${(wlSettleRot*wlSkMid + V.player.facing*-0.10*V.guardRaiseK(V.player)).toFixed(4)} rot0=${wlRot0.toFixed(4)} settle=${wlSettleMid} sk=${wlSkMid.toFixed(3)} gpk=${V.player.guardPoseK.toFixed(3)}`);
check("walk→guard destRect lean leftover AABB planted through ease", wlAabb0 && Math.abs(V.bodyAABB(V.player).y-wlAabb0.y)<0.05, `dy=${wlAabb0?(V.bodyAABB(V.player).y-wlAabb0.y).toFixed(2):"na"}`);
let wlLeanGone=false, wlRotGone=0;
for(let i=0;i<20;i++){
  freezeRivalAI();
  V.keys.add("KeyS");
  V.update(STEP);
  if(V.player.walkSettleT===0 && V.player.guarding){
    wlLeanGone=true;
    wlRotGone=V.destRect(V.player).rot;
    break;
  }
}
check("walk→guard destRect lean leftover settle gone on guard plant", wlLeanGone && V.player.walkSettleT===0 && V.player.guarding && isBlockSheet(V.player), `gone=${wlLeanGone} settle=${V.player.walkSettleT} g=${V.player.guarding} rot=${wlRotGone.toFixed(4)} src=${sheetSrc(V.player)}`);
check("walk→guard destRect lean leftover ends on guard plant rot", wlLeanGone && Math.abs(wlRotGone - V.player.facing*-0.10)<0.02, `rot=${wlRotGone.toFixed(4)} gpk=${V.player.guardPoseK.toFixed(3)} raiseK=${V.guardRaiseK(V.player).toFixed(3)}`);

// settle→guard lean: release walk then raise mid-settle — leftover lean must not dump
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x = 400; V.rival.x = 1100;
V.player.guarding=false; V.player.guardPoseK=0; V.player.phase="idle"; V.player.boltPhase=""; V.player.stunT=0; V.player.falling=false; V.player.hp=100;
V.player.gait=0; V.player.walkT=0; V.player.walkSettleT=0; V.player.walkFadeHold=0; V.player.walkRiseT=0;
V.keys.add("KeyD");
for(let i=0;i<40;i++){
  freezeRivalAI();
  V.player.phase="idle"; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase="";
  V.keys.add("KeyD");
  V.update(STEP);
  if(V.player.walkT>=180 && V.player.walkRiseT>=V.WALK_SETTLE_MS) break;
}
V.keys.delete("KeyD");
freezeRivalAI();
V.update(STEP); // start settle
const wlsRot0=V.destRect(V.player).rot;
const wlsSettle0=V.player.walkSettleT;
const wlsAabb0=V.bodyAABB(V.player);
check("walk→guard destRect lean leftover settle live", V.player.walkSettleT>0 && Math.abs(wlsRot0)>0.02 && V.player.guarding===false && V.player.gait===0, `settle=${V.player.walkSettleT} rot=${wlsRot0.toFixed(4)} g=${V.player.guarding} gait=${V.player.gait}`);
V.keys.add("KeyS");
freezeRivalAI();
V.update(STEP);
const wlsRot1=V.destRect(V.player).rot;
check("walk→guard destRect lean leftover settle raise keeps lean", V.player.guarding && V.player.walkSettleT>0 && Math.abs(wlsRot1-wlsRot0)<0.03, `g=${V.player.guarding} rot0=${wlsRot0.toFixed(4)} rot1=${wlsRot1.toFixed(4)} settle=${V.player.walkSettleT} drot=${(wlsRot1-wlsRot0).toFixed(4)}`);
check("walk→guard destRect lean leftover settle AABB planted", Math.abs(V.bodyAABB(V.player).y-wlsAabb0.y)<0.05, `dy=${(V.bodyAABB(V.player).y-wlsAabb0.y).toFixed(2)}`);

// rival walk→guard lean (pin guarding)
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.rival.x = 900; V.player.x = 400;
V.rival.guarding=false; V.rival.phase="idle"; V.rival.boltPhase=""; V.rival.stunT=0; V.rival.falling=false; V.rival.hp=100;
V.rival.gait=1; V.rival.walkT=160; V.rival.walkRiseT=V.WALK_SETTLE_MS; V.rival.walkLeanGait=1;
V.rival.walkFadeHold=0; V.rival.walkSettleT=0; V.rival.walkSettleRot=0; V.rival.walkSettleOy=0;
for(let i=0;i<4;i++){
  V.rival.gait=1; V.rival.phase="idle"; V.rival.guarding=false; V.rival.stunT=0; V.rival.falling=false; V.rival.boltPhase="";
  V.rival.walkRiseT=V.WALK_SETTLE_MS; V.rival.walkLeanGait=1;
  holdRivalStill();
  V.update(STEP);
}
const wlrRot0=V.destRect(V.rival).rot;
const wlrAabb0=V.bodyAABB(V.rival);
V.rival.guarding=true; V.rival.wantBlock=true; V.rival.gait=0; V.rival.walkT=0; V.rival.phase="idle"; V.rival.boltPhase="";
V.rival.walkSettleT=V.WALK_SETTLE_MS;
V.rival.walkSettleRot=wlrRot0;
V.rival.walkSettleOy=0.1;
V.rival.guardPoseK=0.25;
const wlrRot1=V.destRect(V.rival).rot;
const wlrSk=(V.rival.walkSettleT/V.WALK_SETTLE_MS)*(V.rival.walkSettleT/V.WALK_SETTLE_MS);
check("walk→guard destRect lean leftover rival raise keeps lean", Math.abs(wlrRot0)>0.02 && V.rival.guarding && Math.abs(wlrRot1 - (wlrRot0*wlrSk + V.rival.facing*-0.10*V.guardRaiseK(V.rival)))<0.02, `rot0=${wlrRot0.toFixed(4)} rot1=${wlrRot1.toFixed(4)} sk=${wlrSk.toFixed(3)} g=${V.rival.guarding}`);
check("walk→guard destRect lean leftover rival poseBitmap block", isBlockSheet(V.rival) && !/walk/.test(sheetSrc(V.rival)), `src=${sheetSrc(V.rival)}`);
check("walk→guard destRect lean leftover rival AABB planted", Math.abs(V.bodyAABB(V.rival).y-wlrAabb0.y)<0.05, `dy=${(V.bodyAABB(V.rival).y-wlrAabb0.y).toFixed(2)}`);

const wlThrowKd = landCloseThrow();
check("throw knockdown still not KO crumple after walk→guard destRect lean leftover", wlThrowKd && V.rival.hp===80 && V.rival.thrownT>0 && !V.rival.falling && V.crumpleFade(V.rival)===0, `ok=${wlThrowKd} hp=${V.rival.hp} th=${V.rival.thrownT} cf=${V.crumpleFade(V.rival)}`);
check("cache v=296 after walk→guard destRect lean leftover live", /game.js\?v=350/.test(htmlTxt));



console.log("\n=== SUMMARY ===");


// --- v208 wakeup→reversal leftover: leftover crumple eases onto windup plant ---
check("wakeup→reversal leftover keeps leftover crumple through plant", /Wakeup→reversal leftover/.test(srcTxt) && /leftover crumple fade used to die the tick/.test(srcTxt) && /startWakeReversal/.test(srcTxt) && /startReversal spent throw-invuln/.test(srcTxt) && /Keep leftover crumple through leftover wakeRevFadeHold/.test(srcTxt));
check("wakeup→reversal leftover is draw-only leftover", /Draw-only \(wakeupFade\)/.test(srcTxt) && /wakeRevFadeHold/.test(srcTxt) && /restWakeRev/.test(sliceFn(codeOnly, "drawKnight")) && /restWake \|\| restWakeGuard/.test(sliceFn(codeOnly, "drawKnight")) && /restWakeRev/.test(sliceFn(codeOnly, "drawKnight")) && !/wakeupFade/.test(sliceFn(codeOnly, "destRect")) && !/wakeupFade/.test(sliceFn(codeOnly, "bodyAABB")) && !/wakeupFade/.test(sliceFn(codeOnly, "slashPose")) && !/wakeupFade/.test(sliceFn(codeOnly, "landHit")) && !/wakeupFade\(/.test(sliceFn(codeOnly, "updateGuard")) && !/wakeupFade\(/.test(sliceFn(codeOnly, "startReversal")) && !/wakeupFade\(/.test(sliceFn(codeOnly, "startWakeReversal")));
check("wakeup→reversal leftover poseBitmap still throw_knife immediately", /poseBitmap still windup immediately/.test(srcTxt) && !/wakeRevFadeHold|restWakeRev/.test(sliceFn(codeOnly, "poseBitmap")));
check("wakeup→reversal leftover captures wakeRevFadeHold before invuln spend", /wakeRevFadeHold/.test(sliceFn(codeOnly, "startReversal")) && /f\.throwInvulnT > 0/.test(sliceFn(codeOnly, "startReversal")) && /f\.throwInvulnT = 0/.test(sliceFn(codeOnly, "startReversal")));
check("wakeup→reversal leftover wakeupFade eases hold over GUARD_RAISE_MS", /wakeRevFadeHold > 0 && reversalPlanting/.test(sliceFn(codeOnly, "wakeupFade")) && /f\.phaseT \/ GUARD_RAISE_MS/.test(sliceFn(codeOnly, "wakeupFade")) && /wakeRevFadeHold \* ease/.test(sliceFn(codeOnly, "wakeupFade")));
check("wakeup→reversal leftover rests crumple on windup sheet", /restWakeRev/.test(sliceFn(codeOnly, "drawKnight")) && /mid-getup reverse keeps leftover crumple on the/.test(srcTxt));
check("wakeup→reversal leftover idle getup path unchanged", /Idle getup still fades hurt→idle/.test(srcTxt) && V.THROW_WAKE_INVULN===80 && V.GUARD_RAISE_MS===80);
check("wakeup→reversal leftover throw still spends invuln", /Throw still spends invuln/.test(srcTxt) && /f\.throwInvulnT = 0/.test(sliceFn(codeOnly, "startReversal")));
check("wakeup→reversal leftover guard reversal plant unchanged", /Guard reversal plant leftover unchanged/.test(srcTxt) && /reversalPlantFade/.test(srcTxt));
check("wakeup→reversal leftover extra rot stays 0", /Extra destRect rot stays 0/.test(srcTxt) && /AABB planted/.test(srcTxt));
check("wakeup→reversal leftover is not a new combat verb", V.THROW_WAKE_INVULN===80 && V.HITSTUN===350 && V.THROW_KD_MS===520 && V.HITSTOP_HIT===140 && V.GUARD_COMMIT_MS===140 && V.REVERSAL_STAM===30 && V.REVERSAL_INVULN===120 && V.PUSHBLOCK_STAM===25 && V.PUSHBLOCK_PX===240);
check("slash frames still 180/140/280 after wakeup→reversal leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after wakeup→reversal leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after wakeup→reversal leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after wakeup→reversal leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after wakeup→reversal leftover", /game.js\?v=350/.test(htmlTxt));

toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.guarding=false; V.player.guardPoseK=0; V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0;
V.player.phase="idle"; V.player.phaseT=0; V.player.boltPhase=""; V.player.falling=false; V.player.stunT=0;
V.player.thrownT=0; V.player.throwInvulnT=V.THROW_WAKE_INVULN; V.player.wakeRev=true; V.player.hp=80;
V.player.stamina=100; V.player.reversal=false; V.player.wakeRevFadeHold=0; V.player.walkSettleT=0; V.keys.clear();
const wr208Fade0 = V.wakeupFade(V.player);
const wr208Aabb0 = V.bodyAABB(V.player);
const wr208Dest0 = V.destRect(V.player);
check("wakeup→reversal leftover getup live", V.player.phase==="idle" && V.player.throwInvulnT>0 && wr208Fade0>0.85 && V.player.guarding===false && !isHurtSheet(V.player) && !isBlockSheet(V.player), `ph=${V.player.phase} inv=${V.player.throwInvulnT} fade=${wr208Fade0} g=${V.player.guarding} src=${sheetSrc(V.player)}`);
const wr208Did = V.startWakeReversal(V.player);
const wr208FadeRaise = V.wakeupFade(V.player);
check("wakeup→reversal leftover reverse keeps leftover crumple", wr208Did && V.player.reversal && V.player.phase==="startup" && V.player.cut==="golpe" && wr208FadeRaise>0.5 && V.player.wakeRevFadeHold>0 && V.player.throwInvulnT===0, `did=${wr208Did} fade=${wr208FadeRaise} hold=${V.player.wakeRevFadeHold} inv=${V.player.throwInvulnT} rev=${V.player.reversal} ph=${V.player.phase}`);
check("wakeup→reversal leftover poseBitmap windup not hurt", /windup/.test(sheetSrc(V.player)) && !isHurtSheet(V.player) && !isBlockSheet(V.player) && !isCutSheet(V.player), `src=${sheetSrc(V.player)}`);
check("wakeup→reversal leftover AABB planted through reverse", Math.abs(V.bodyAABB(V.player).y-wr208Aabb0.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-wr208Aabb0.h)<0.05, `dy=${(V.bodyAABB(V.player).y-wr208Aabb0.y).toFixed(2)}`);
check("wakeup→reversal leftover destRect planted through reverse", Math.abs(V.destRect(V.player).dy-wr208Dest0.dy)<2.2 && Math.abs(V.destRect(V.player).rot)<0.2, `ddy=${(V.destRect(V.player).dy-wr208Dest0.dy).toFixed(2)} rot=${V.destRect(V.player).rot.toFixed(3)}`);
let wr208FadeMid=-1, wr208PhaseTMid=-1;
for(let i=0;i<2;i++){
  holdRivalStill();
  V.update(STEP);
  wr208FadeMid=V.wakeupFade(V.player);
  wr208PhaseTMid=V.player.phaseT;
}
check("wakeup→reversal leftover fade drops while plant holds", wr208FadeMid>=0 && wr208FadeMid<wr208FadeRaise-0.05 && V.player.reversal && V.player.phase==="startup" && V.player.wakeRevFadeHold>0, `mid=${wr208FadeMid} t0=${wr208FadeRaise} phaseT=${wr208PhaseTMid} hold=${V.player.wakeRevFadeHold} rev=${V.player.reversal}`);
check("wakeup→reversal leftover AABB planted through fade", Math.abs(V.bodyAABB(V.player).y-wr208Aabb0.y)<0.05, `dy=${(V.bodyAABB(V.player).y-wr208Aabb0.y).toFixed(2)}`);
let wr208Gone=false;
for(let i=0;i<12;i++){
  holdRivalStill();
  V.update(STEP);
  if(V.wakeupFade(V.player)===0 && V.player.wakeRevFadeHold===0 && V.player.reversal){
    wr208Gone=true;
    break;
  }
}
check("wakeup→reversal leftover fade gone while reversal holds", wr208Gone && V.wakeupFade(V.player)===0 && V.player.wakeRevFadeHold===0 && V.player.reversal && V.player.phase==="startup" && /windup/.test(sheetSrc(V.player)), `gone=${wr208Gone} fade=${V.wakeupFade(V.player)} hold=${V.player.wakeRevFadeHold} rev=${V.player.reversal} ph=${V.player.phase} src=${sheetSrc(V.player)}`);

// S mid-getup then L: crumple keeps; block plant may also ease
toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.guarding=false; V.player.guardPoseK=0; V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0;
V.player.phase="idle"; V.player.phaseT=0; V.player.boltPhase=""; V.player.falling=false; V.player.stunT=0;
V.player.thrownT=0; V.player.throwInvulnT=V.THROW_WAKE_INVULN; V.player.wakeRev=true; V.player.hp=80;
V.player.stamina=100; V.player.reversal=false; V.player.wakeRevFadeHold=0; V.keys.clear();
V.keys.add("KeyS");
holdRivalStill();
V.update(STEP);
const wr208SFade0 = V.wakeupFade(V.player);
const wr208SAabb0 = V.bodyAABB(V.player);
check("wakeup→reversal leftover S-getup live", V.player.guarding && V.player.throwInvulnT>0 && wr208SFade0>0.5 && isBlockSheet(V.player), `g=${V.player.guarding} fade=${wr208SFade0} inv=${V.player.throwInvulnT} src=${sheetSrc(V.player)}`);
const wr208SDid = V.startReversal(V.player);
const wr208SFade1 = V.wakeupFade(V.player);
check("wakeup→reversal leftover S+L keeps leftover crumple", wr208SDid && V.player.reversal && wr208SFade1>0.4 && V.player.wakeRevFadeHold>0 && V.player.throwInvulnT===0, `did=${wr208SDid} fade0=${wr208SFade0} fade1=${wr208SFade1} hold=${V.player.wakeRevFadeHold} inv=${V.player.throwInvulnT}`);
check("wakeup→reversal leftover S+L poseBitmap windup", /windup/.test(sheetSrc(V.player)) && !isHurtSheet(V.player), `src=${sheetSrc(V.player)}`);
check("wakeup→reversal leftover S+L AABB planted", Math.abs(V.bodyAABB(V.player).y-wr208SAabb0.y)<0.05, `dy=${(V.bodyAABB(V.player).y-wr208SAabb0.y).toFixed(2)}`);

// rival wakeup reversal (same leftover)
const wr208RLand = landCloseThrow();
let wr208RReady=false, wr208RFade0=-1, wr208RAabb0=null;
for(let i=0;i<80;i++){
  holdWakeSample();
  V.update(STEP);
  if(V.rival.thrownT<=0 && V.rival.throwInvulnT>0 && V.rival.hp>0 && !V.rival.falling){
    wr208RReady=true;
    wr208RFade0=V.wakeupFade(V.rival);
    wr208RAabb0=V.bodyAABB(V.rival);
    break;
  }
}
V.rival.stamina=100;
const wr208RDid = V.startWakeReversal(V.rival);
check("wakeup→reversal leftover rival keeps crumple", wr208RLand && wr208RReady && wr208RDid && wr208RFade0>0.5 && V.wakeupFade(V.rival)>0.5 && V.rival.wakeRevFadeHold>0 && V.rival.throwInvulnT===0 && V.rival.cut==="golpe", `land=${wr208RLand} ready=${wr208RReady} did=${wr208RDid} fade0=${wr208RFade0} fade=${V.wakeupFade(V.rival)} hold=${V.rival.wakeRevFadeHold} inv=${V.rival.throwInvulnT}`);
check("wakeup→reversal leftover rival poseBitmap windup", /windup/.test(sheetSrc(V.rival)) && !isHurtSheet(V.rival), `src=${sheetSrc(V.rival)}`);
check("wakeup→reversal leftover rival AABB planted", wr208RAabb0 && Math.abs(V.bodyAABB(V.rival).y-wr208RAabb0.y)<0.05, `dy=${wr208RAabb0?(V.bodyAABB(V.rival).y-wr208RAabb0.y).toFixed(2):"na"}`);

// idle getup without reverse still fades
const wr208IdleLand = landCloseThrow();
let wr208IdleReady=false, wr208IdleFade0=-1;
for(let i=0;i<80;i++){
  holdWakeSample();
  V.update(STEP);
  if(V.rival.thrownT<=0 && V.rival.throwInvulnT>0 && V.rival.hp>0 && !V.rival.falling){
    wr208IdleReady=true;
    wr208IdleFade0=V.wakeupFade(V.rival);
    break;
  }
}
check("wakeup→reversal leftover idle getup still fades", wr208IdleLand && wr208IdleReady && wr208IdleFade0>0.5 && V.rival.guarding===false && V.rival.wakeRevFadeHold===0 && !isHurtSheet(V.rival) && !isBlockSheet(V.rival), `land=${wr208IdleLand} ready=${wr208IdleReady} fade=${wr208IdleFade0} hold=${V.rival.wakeRevFadeHold} g=${V.rival.guarding} src=${sheetSrc(V.rival)}`);

// wakeup→guard still keeps crumple (no regress)
toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.guarding=false; V.player.guardPoseK=0; V.player.gait=0;
V.player.phase="idle"; V.player.phaseT=0; V.player.boltPhase=""; V.player.falling=false; V.player.stunT=0;
V.player.thrownT=0; V.player.throwInvulnT=V.THROW_WAKE_INVULN; V.player.wakeRev=true; V.player.hp=80;
V.player.wakeRevFadeHold=0; V.keys.clear();
V.keys.add("KeyS");
holdRivalStill();
V.update(STEP);
check("wakeup→guard still keeps crumple after wakeup→reversal leftover", V.player.guarding && V.player.throwInvulnT>0 && V.wakeupFade(V.player)>0.5 && isBlockSheet(V.player) && V.player.wakeRevFadeHold===0, `g=${V.player.guarding} fade=${V.wakeupFade(V.player)} inv=${V.player.throwInvulnT} hold=${V.player.wakeRevFadeHold} src=${sheetSrc(V.player)}`);

const wr208ThrowKd = landCloseThrow();
check("throw knockdown still not KO crumple after wakeup→reversal leftover", wr208ThrowKd && V.rival.hp===80 && V.rival.thrownT>0 && !V.rival.falling && V.crumpleFade(V.rival)===0, `ok=${wr208ThrowKd} hp=${V.rival.hp} th=${V.rival.thrownT} cf=${V.crumpleFade(V.rival)}`);
check("cache v=296 after wakeup→reversal leftover live", /game.js\?v=350/.test(htmlTxt));


// --- v209 feint end destRect leftover: leftover breath no longer dumps when sheatheT zeros ---
check("feint end destRect leftover skips sheathe amp through pull", /Feint end destRect leftover/.test(srcTxt) && /leftover idle breath used to dump ~1\.3 the tick/.test(srcTxt) && /tickFeint zeros leftover sheatheT/.test(srcTxt) && /f\.feintT > 0/.test(sliceFn(codeOnly, "idleBreath")) && /amp \* \(1 - feintFade\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /f\.sheatheT > 0/.test(sliceFn(codeOnly, "idleBreath")) && !/amp \*= f\.sheatheT \/ SHEATHE_MS/.test(sliceFn(codeOnly, "idleBreath")));
check("feint end destRect leftover is destRect-only", /destRect-only \(idleBreath\)/.test(srcTxt) && /Skip sheathe amp/.test(srcTxt) && /ease with feintFade only/.test(srcTxt) && !/feintT <= 0/.test(sliceFn(codeOnly, "destRect")) && !/feintT <= 0/.test(sliceFn(codeOnly, "bodyAABB")) && !/feintT <= 0/.test(sliceFn(codeOnly, "slashPose")));
check("feint end destRect leftover sheathe dip still skips through feint", /if \(f\.feintT > 0\) return 0/.test(sliceFn(codeOnly, "sheatheDip")) && /f\.feintT === 0\) f\.sheatheT = 0/.test(sliceFn(codeOnly, "tickFeint")));
check("feint end destRect leftover still eases with feintFade", /f\.feintT > 0/.test(sliceFn(codeOnly, "idleBreath")) && /amp \* \(1 - feintFade\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")));
check("feint end destRect leftover idle sheathe keeps breath 0", !/amp \*= f\.sheatheT \/ SHEATHE_MS/.test(sliceFn(codeOnly, "idleBreath")) && /Clash sheathe destRect leftover/.test(srcTxt) && /Keep breath 0 through sheathe/.test(srcTxt) && /feintFade/.test(sliceFn(codeOnly, "idleBreath")));
check("feint end destRect leftover poseBitmap still idle", /poseBitmap still idle once feintT is set/.test(srcTxt));
check("feint→guard / feint-from-drop unchanged after feint end destRect leftover", /Feint→guard \/ feint-from-drop unchanged/.test(srcTxt) && /feintFade/.test(srcTxt) && /guardDropFade/.test(srcTxt));
check("feint end destRect leftover extra rot stays 0", /Extra destRect rot stays 0/.test(srcTxt) && /AABB planted/.test(srcTxt));
check("feint end destRect leftover is not a new combat verb", V.FEINT_RECOVERY===100 && V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80 && V.GUARD_COMMIT_MS===140 && V.SHEATHE_MS===140 && V.PUSHBLOCK_STAM===25 && V.PUSHBLOCK_PX===240);
check("slash frames still 180/140/280 after feint end destRect leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after feint end destRect leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after feint end destRect leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after feint end destRect leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after feint end destRect leftover", /game.js\?v=350/.test(htmlTxt));

// live: feint end does not dump destRect breath
toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.phase="startup"; V.player.phaseT=50; V.player.cut="slash"; V.player.closing=true;
V.player.guarding=false; V.player.gait=0; V.player.feintT=0; V.player.sheatheT=0; V.player.boltPhase="";
V.player.stunT=0; V.player.falling=false; V.player.stamina=100;
check("feint end destRect leftover can pull", V.canFeint(V.player) && V.player.phase==="startup" && V.player.cut==="slash", `ph=${V.player.phase} cut=${V.player.cut} can=${V.canFeint(V.player)}`);
const fe209Did = V.startFeint(V.player);
const fe209Aabb0 = V.bodyAABB(V.player);
const fe209Dest0 = V.destRect(V.player);
check("feint end destRect leftover arms pull", fe209Did && V.player.feintT===V.FEINT_RECOVERY && V.player.sheatheT===V.SHEATHE_MS && V.feintFade(V.player)>0.85, `did=${fe209Did} ft=${V.player.feintT} sh=${V.player.sheatheT} fade=${V.feintFade(V.player)}`);
check("feint end destRect leftover AABB planted at pull", Math.abs(V.bodyAABB(V.player).y-fe209Aabb0.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-fe209Aabb0.h)<0.05, `dy=${(V.bodyAABB(V.player).y-fe209Aabb0.y).toFixed(2)}`);
let fe209Near=false, fe209Dy0=0, fe209Fade0=0, fe209Sh0=0, fe209Ft0=0;
for(let i=0;i<20;i++){
  holdRivalStill();
  if(V.player.feintT>0 && V.player.feintT<=STEP+0.01){
    fe209Near=true;
    fe209Dy0=V.destRect(V.player).dy;
    fe209Fade0=V.feintFade(V.player);
    fe209Sh0=V.player.sheatheT;
    fe209Ft0=V.player.feintT;
    break;
  }
  V.update(STEP);
}
check("feint end destRect leftover near end live", fe209Near && fe209Ft0>0 && fe209Sh0>0 && fe209Fade0<0.2, `near=${fe209Near} ft=${fe209Ft0} sh=${fe209Sh0} fade=${fe209Fade0}`);
holdRivalStill();
V.update(STEP);
const fe209Dy1 = V.destRect(V.player).dy;
const fe209Ddy = fe209Dy1 - fe209Dy0;
check("feint end destRect leftover no breath dump on clear", V.player.feintT===0 && V.player.sheatheT===0 && Math.abs(fe209Ddy)<0.85, `ft=${V.player.feintT} sh=${V.player.sheatheT} ddy=${fe209Ddy.toFixed(3)} fade0=${fe209Fade0.toFixed(3)}`);
check("feint end destRect leftover AABB planted through clear", Math.abs(V.bodyAABB(V.player).y-fe209Aabb0.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-fe209Aabb0.h)<0.05, `dy=${(V.bodyAABB(V.player).y-fe209Aabb0.y).toFixed(2)}`);
check("feint end destRect leftover destRect rot not wild", Math.abs(V.destRect(V.player).rot)<0.2, `rot=${V.destRect(V.player).rot.toFixed(3)}`);
check("feint end destRect leftover poseBitmap idle after clear", !isCutSheet(V.player) && !/windup/.test(sheetSrc(V.player)) && !isHurtSheet(V.player), `src=${sheetSrc(V.player)}`);

// rival shares the same startFeint / idleBreath path (no kind gate)
toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.rival.phase="startup"; V.rival.phaseT=50; V.rival.cut="slash"; V.rival.closing=true;
V.rival.guarding=false; V.rival.gait=0; V.rival.feintT=0; V.rival.sheatheT=0; V.rival.stamina=100;
V.rival.wantBlock=false; V.rival.guardChoice=-1; V.rival.standGoal=99999;
V.rival.boltCd=99999; V.rival.throwCd=99999; V.rival.revCd=99999; V.rival.pbCd=99999; V.rival.feintCd=99999;
const fe209RAabb0 = V.bodyAABB(V.rival);
const fe209RDid = V.startFeint(V.rival);
check("feint end destRect leftover rival shares pull path", fe209RDid && V.rival.feintT===V.FEINT_RECOVERY && V.rival.sheatheT===V.SHEATHE_MS && V.feintFade(V.rival)>0.85, `did=${fe209RDid} ft=${V.rival.feintT} sh=${V.rival.sheatheT} fade=${V.feintFade(V.rival)}`);
check("feint end destRect leftover rival idleBreath shared", /f\.sheatheT > 0/.test(sliceFn(codeOnly, "idleBreath")) && /f\.feintT > 0/.test(sliceFn(codeOnly, "idleBreath")) && /amp \* \(1 - feintFade\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /startFeint\(f\)/.test(srcTxt) && !/amp \*= f\.sheatheT \/ SHEATHE_MS/.test(sliceFn(codeOnly, "idleBreath")));
check("feint end destRect leftover rival AABB planted at pull", Math.abs(V.bodyAABB(V.rival).y-fe209RAabb0.y)<0.05, `dy=${(V.bodyAABB(V.rival).y-fe209RAabb0.y).toFixed(2)}`);

// idle sheathe still live (sheet fade; breath stays 0 through sheathe)
toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.phase="idle"; V.player.feintT=0; V.player.sheatheT=V.SHEATHE_MS; V.player.gait=0; V.player.guarding=false;
const fe209ShDy0 = V.destRect(V.player).dy;
holdRivalStill();
V.update(STEP);
// sheathe amp should still be live (not a full idle breath seat in one tick from SHEATHE_MS)
check("feint end destRect leftover idle sheathe still live", V.player.sheatheT>0 && V.player.feintT===0 && V.sheatheFade(V.player)>0.5, `sh=${V.player.sheatheT} fade=${V.sheatheFade(V.player)}`);

// feint→guard still keeps windup (no regress)
toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.phase="startup"; V.player.phaseT=50; V.player.cut="slash"; V.player.closing=true;
V.startFeint(V.player);
const fe209GFade0 = V.feintFade(V.player);
V.keys.add("KeyS");
holdRivalStill();
V.update(STEP);
check("feint→guard still keeps windup after feint end destRect leftover", V.player.guarding && V.player.feintT>0 && V.feintFade(V.player)>0.5 && isBlockSheet(V.player), `g=${V.player.guarding} ft=${V.player.feintT} fade0=${fe209GFade0} fade=${V.feintFade(V.player)} src=${sheetSrc(V.player)}`);

const fe209ThrowKd = landCloseThrow();
check("throw knockdown still not KO crumple after feint end destRect leftover", fe209ThrowKd && V.rival.hp===80 && V.rival.thrownT>0 && !V.rival.falling && V.crumpleFade(V.rival)===0, `ok=${fe209ThrowKd} hp=${V.rival.hp} th=${V.rival.thrownT} cf=${V.crumpleFade(V.rival)}`);
check("cache v=296 after feint end destRect leftover live", /game.js\?v=350/.test(htmlTxt));


// --- v210 stun end destRect leftover: leftover breath no longer dumps when stunT zeros ---
check("stun end destRect leftover eases breath through chip stun drain", /Stun end destRect leftover/.test(srcTxt) && /leftover idle breath used to dump ~1\.5 the tick/.test(srcTxt) && /stunT hit 0/.test(srcTxt) && /Ease breath in over leftover GUARD_RAISE_MS/.test(srcTxt) && /f\.stunT > 0/.test(sliceFn(codeOnly, "idleBreath")) && /f\.stunT \/ GUARD_RAISE_MS/.test(sliceFn(codeOnly, "idleBreath")));
check("stun end destRect leftover is destRect-only", /destRect-only \(idleBreath\)/.test(srcTxt) && /Throw KD \/ wakeup invuln still hard-zero/.test(srcTxt) && /wakeupFade owns getup/.test(srcTxt) && !/stunT \/ GUARD_RAISE_MS/.test(sliceFn(codeOnly, "destRect")) && !/stunT \/ GUARD_RAISE_MS/.test(sliceFn(codeOnly, "bodyAABB")) && !/stunT \/ GUARD_RAISE_MS/.test(sliceFn(codeOnly, "slashPose")) && !/stunT \/ GUARD_RAISE_MS/.test(sliceFn(codeOnly, "poseBitmap")));
check("stun end destRect leftover throw KD / wakeup still hard-zero", /f\.thrownT > 0 \|\| f\.throwInvulnT > 0 \|\| f\.hp <= 0/.test(sliceFn(codeOnly, "idleBreath")) && /thrownT > 0 \|\| f\.throwInvulnT > 0/.test(sliceFn(codeOnly, "idleBreath")));
check("stun end destRect leftover poseBitmap still idle", /poseBitmap still idle/.test(srcTxt) && /Stun rot still scales with stunT\/HITSTUN/.test(srcTxt));
check("stun end destRect leftover extra rot stays 0", /Extra destRect rot stays 0/.test(srcTxt) && /AABB planted/.test(srcTxt) && /Chip stun \/ throw KD unchanged/.test(srcTxt));
check("stun end destRect leftover is not a new combat verb", V.HITSTUN===350 && V.HITSTOP_HIT===140 && V.GUARD_RAISE_MS===80 && V.THROW_WAKE_INVULN===80 && V.THROW_KD_MS===520 && V.FEINT_RECOVERY===100 && V.PUSHBLOCK_STAM===25 && V.PUSHBLOCK_PX===240);
check("slash frames still 180/140/280 after stun end destRect leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after stun end destRect leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after stun end destRect leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after stun end destRect leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("feint end destRect leftover still skips sheathe amp after stun end", /f\.sheatheT > 0/.test(sliceFn(codeOnly, "idleBreath")) && /amp \* \(1 - feintFade\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && !/amp \*= f\.sheatheT \/ SHEATHE_MS/.test(sliceFn(codeOnly, "idleBreath")));
check("cache v=296 after stun end destRect leftover", /game.js\?v=350/.test(htmlTxt));

// live: chip stun end does not dump destRect breath
toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.phase="idle"; V.player.phaseT=0; V.player.cut=""; V.player.boltPhase="";
V.player.guarding=false; V.player.gait=0; V.player.feintT=0; V.player.sheatheT=0;
V.player.stunT=0; V.player.falling=false; V.player.thrownT=0; V.player.throwInvulnT=0;
V.player.guardPoseK=0; V.player.hp=100; V.player.stamina=100;
V.player.stunT = V.HITSTUN;
const st210Aabb0 = V.bodyAABB(V.player);
const st210Dest0 = V.destRect(V.player);
check("stun end destRect leftover arms chip stun", V.player.stunT===V.HITSTUN && V.player.phase==="idle" && Math.abs(st210Dest0.rot)>0.10, `st=${V.player.stunT} rot=${st210Dest0.rot.toFixed(3)}`);
check("stun end destRect leftover AABB planted at stun arm", Math.abs(V.bodyAABB(V.player).y-st210Aabb0.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-st210Aabb0.h)<0.05, `dy=${(V.bodyAABB(V.player).y-st210Aabb0.y).toFixed(2)}`);
let st210Near=false, st210Dy0=0, st210Rot0=0, st210Ft0=0;
for(let i=0;i<40;i++){
  holdRivalStill();
  if(V.player.stunT>0 && V.player.stunT<=STEP+0.01){
    st210Near=true;
    st210Dy0=V.destRect(V.player).dy;
    st210Rot0=V.destRect(V.player).rot;
    st210Ft0=V.player.stunT;
    break;
  }
  V.update(STEP);
}
check("stun end destRect leftover near end live", st210Near && st210Ft0>0 && Math.abs(st210Rot0)<0.05, `near=${st210Near} st=${st210Ft0} rot=${st210Rot0.toFixed(4)}`);
holdRivalStill();
V.update(STEP);
const st210Dy1 = V.destRect(V.player).dy;
const st210Ddy = st210Dy1 - st210Dy0;
check("stun end destRect leftover no breath dump on clear", V.player.stunT===0 && Math.abs(st210Ddy)<0.85, `st=${V.player.stunT} ddy=${st210Ddy.toFixed(3)} rot0=${st210Rot0.toFixed(4)}`);
check("stun end destRect leftover AABB planted through clear", Math.abs(V.bodyAABB(V.player).y-st210Aabb0.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-st210Aabb0.h)<0.05, `dy=${(V.bodyAABB(V.player).y-st210Aabb0.y).toFixed(2)}`);
check("stun end destRect leftover destRect rot not wild", Math.abs(V.destRect(V.player).rot)<0.2, `rot=${V.destRect(V.player).rot.toFixed(3)}`);
check("stun end destRect leftover poseBitmap idle after clear", !isHurtSheet(V.player) && !isBlockSheet(V.player) && !/windup/.test(sheetSrc(V.player)), `src=${sheetSrc(V.player)}`);

// early chip stun still hard-zeros breath (no early seat)
toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.phase="idle"; V.player.stunT=V.HITSTUN; V.player.thrownT=0; V.player.throwInvulnT=0;
V.player.guarding=false; V.player.gait=0; V.player.sheatheT=0; V.player.feintT=0; V.player.hp=100;
const st210Early0 = V.destRect(V.player).dy;
holdRivalStill();
V.update(STEP);
const st210Early1 = V.destRect(V.player).dy;
check("stun end destRect leftover early stun no breath seat", V.player.stunT>V.GUARD_RAISE_MS && Math.abs(st210Early1-st210Early0)<0.85, `st=${V.player.stunT} ddy=${(st210Early1-st210Early0).toFixed(3)}`);

// rival shares the same idleBreath path (no kind gate)
toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.rival.phase="idle"; V.rival.stunT=V.HITSTUN; V.rival.thrownT=0; V.rival.throwInvulnT=0;
V.rival.guarding=false; V.rival.gait=0; V.rival.hp=100;
V.rival.wantBlock=false; V.rival.guardChoice=-1; V.rival.standGoal=99999;
V.rival.boltCd=99999; V.rival.throwCd=99999; V.rival.revCd=99999; V.rival.pbCd=99999; V.rival.feintCd=99999;
const st210RAabb0 = V.bodyAABB(V.rival);
let st210RNear=false, st210RDy0=0;
for(let i=0;i<40;i++){
  holdRivalStill();
  if(V.rival.stunT>0 && V.rival.stunT<=STEP+0.01){
    st210RNear=true;
    st210RDy0=V.destRect(V.rival).dy;
    break;
  }
  V.update(STEP);
}
holdRivalStill();
V.update(STEP);
const st210RDdy = V.destRect(V.rival).dy - st210RDy0;
check("stun end destRect leftover rival shares clear path", st210RNear && V.rival.stunT===0 && Math.abs(st210RDdy)<0.85, `near=${st210RNear} st=${V.rival.stunT} ddy=${st210RDdy.toFixed(3)}`);
check("stun end destRect leftover rival idleBreath shared", /f\.stunT > 0/.test(sliceFn(codeOnly, "idleBreath")) && /f\.stunT \/ GUARD_RAISE_MS/.test(sliceFn(codeOnly, "idleBreath")) && /thrownT > 0 \|\| f\.throwInvulnT > 0/.test(sliceFn(codeOnly, "idleBreath")));
check("stun end destRect leftover rival AABB planted through clear", Math.abs(V.bodyAABB(V.rival).y-st210RAabb0.y)<0.05, `dy=${(V.bodyAABB(V.rival).y-st210RAabb0.y).toFixed(2)}`);

// throw KD getup still uses wakeupFade (no regress)
toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.phase="idle"; V.player.stunT=0; V.player.thrownT=0; V.player.throwInvulnT=V.THROW_WAKE_INVULN;
V.player.wakeRev=true; V.player.hp=80; V.player.falling=false; V.player.guarding=false; V.player.gait=0;
check("wakeup fade still owns getup after stun end destRect leftover", V.wakeupFade(V.player)>0.85 && V.player.throwInvulnT>0 && V.player.stunT===0, `fade=${V.wakeupFade(V.player)} inv=${V.player.throwInvulnT} st=${V.player.stunT}`);

// feint end still no breath dump (no regress)
toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.phase="startup"; V.player.phaseT=50; V.player.cut="slash"; V.player.closing=true;
V.player.guarding=false; V.player.gait=0; V.player.feintT=0; V.player.sheatheT=0; V.player.boltPhase="";
V.player.stunT=0; V.player.falling=false; V.player.stamina=100;
const fe210Did = V.startFeint(V.player);
let fe210Near=false, fe210Dy0=0;
for(let i=0;i<20;i++){
  holdRivalStill();
  if(V.player.feintT>0 && V.player.feintT<=STEP+0.01){
    fe210Near=true;
    fe210Dy0=V.destRect(V.player).dy;
    break;
  }
  V.update(STEP);
}
holdRivalStill();
V.update(STEP);
const fe210Ddy = V.destRect(V.player).dy - fe210Dy0;
check("feint end still no breath dump after stun end destRect leftover", fe210Did && fe210Near && V.player.feintT===0 && V.player.sheatheT===0 && Math.abs(fe210Ddy)<0.85, `did=${fe210Did} near=${fe210Near} ddy=${fe210Ddy.toFixed(3)}`);

const st210ThrowKd = landCloseThrow();
check("throw knockdown still not KO crumple after stun end destRect leftover", st210ThrowKd && V.rival.hp===80 && V.rival.thrownT>0 && !V.rival.falling && V.crumpleFade(V.rival)===0, `ok=${st210ThrowKd} hp=${V.rival.hp} th=${V.rival.thrownT} cf=${V.crumpleFade(V.rival)}`);
check("cache v=296 after stun end destRect leftover live", /game.js\?v=350/.test(htmlTxt));





// --- v211 camera punch end leftover: leftover overscan eases out, no yard scale dump ---
check("camera punch end leftover eases cover over last quarter", /Camera punch end leftover/.test(srcTxt) && /leftover PUNCH_PX overscan used to dump/.test(srcTxt) && /yard scaled ~17px/.test(srcTxt) && /ease cover out over the last quarter/.test(srcTxt) && /k > 0\.25/.test(sliceFn(codeOnly, "punchCover")) && /PUNCH_PX \* u \* u \* \(3 - 2 \* u\)/.test(sliceFn(codeOnly, "punchCover")));
check("camera punch end leftover is draw-only", /Draw-only \(punchCover\)/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && /Punch is a slide, not a zoom/.test(srcTxt) && !/k > 0\.25/.test(sliceFn(codeOnly, "destRect")) && !/k > 0\.25/.test(sliceFn(codeOnly, "bodyAABB")) && !/k > 0\.25/.test(sliceFn(codeOnly, "slashPose")) && !/k > 0\.25/.test(sliceFn(codeOnly, "bumpShake")) && !/k > 0\.25/.test(sliceFn(codeOnly, "landHit")));
check("camera punch end leftover still holds full cover mid-slam", /if \(k > 0\.25\) return PUNCH_PX/.test(sliceFn(codeOnly, "punchCover")) && /Hold full cover through the live slam/.test(srcTxt) && /no mid-punch zoom/.test(srcTxt));
check("camera punch end leftover rest still flush", /shake <= 0 \|\| shakeDur <= 0/.test(sliceFn(codeOnly, "punchCover")) && /Rest ox=0 still flush/.test(srcTxt) && /mode === "title"/.test(sliceFn(codeOnly, "punchCover")));
check("camera punch end leftover still squares", /env = k \* k/.test(srcTxt) && /shakeDir \* shakeMag \* env/.test(srcTxt) && V.PUNCH_PX===12);
check("camera punch end leftover is not a new combat verb", V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.HITSTOP_BLOCK===60 && V.BOLT_SUPER_STOP===60 && V.THROW_WAKE_INVULN===80 && V.PUNCH_PX===12 && V.PUSHBLOCK_STAM===25 && V.PUSHBLOCK_PX===240);
check("slash frames still 180/140/280 after camera punch end leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after camera punch end leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after camera punch end leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after camera punch end leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("stun end destRect leftover still eases breath after camera punch end", /f\.stunT > 0/.test(sliceFn(codeOnly, "idleBreath")) && /f\.stunT \/ GUARD_RAISE_MS/.test(sliceFn(codeOnly, "idleBreath")));
check("feint end destRect leftover still skips sheathe amp after camera punch end", /f\.sheatheT > 0/.test(sliceFn(codeOnly, "idleBreath")) && !/amp \*= f\.sheatheT \/ SHEATHE_MS/.test(sliceFn(codeOnly, "idleBreath")) && /amp \* \(1 - feintFade\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")));
check("cache v=296 after camera punch end leftover", /game.js\?v=350/.test(htmlTxt));

const punchEndHit = landCloseSlashPunch();
check("camera punch end leftover arms on hit", punchEndHit && V.hitstopLeft===V.HITSTOP_HIT && V.shake===V.HITSTOP_HIT && V.shakeDur===V.HITSTOP_HIT && V.punchCover()===V.PUNCH_PX, `hit=${punchEndHit} hs=${V.hitstopLeft} shake=${V.shake} cover=${V.punchCover()}`);
drainHitstop();
V.update(STEP);
const punchEndMid = V.shake;
for(let i=0;i<5;i++) V.update(STEP);
check("camera punch end leftover mid-slam still full cover", V.shake<punchEndMid && V.shake>0 && V.shake/V.shakeDur>0.25 && V.punchCover()===V.PUNCH_PX, `shake=${V.shake} mid=${punchEndMid} cover=${V.punchCover()} k=${(V.shake/V.shakeDur).toFixed(3)}`);
let punchEndNear=false, punchEndCover0=0, punchEndShake0=0;
for(let i=0;i<30;i++){
  if(V.shake>0 && V.shake/V.shakeDur<=0.25){
    punchEndNear=true;
    punchEndCover0=V.punchCover();
    punchEndShake0=V.shake;
    break;
  }
  if(V.shake<=0) break;
  V.update(STEP);
}
check("camera punch end leftover eases cover near end live", punchEndNear && punchEndCover0<V.PUNCH_PX-0.01 && punchEndCover0>0 && punchEndShake0>0, `near=${punchEndNear} cover=${punchEndCover0.toFixed(3)} shake=${punchEndShake0} k=${punchEndShake0&&(punchEndShake0/V.shakeDur).toFixed(3)}`);
let punchEndLastCover=punchEndCover0;
for(let i=0;i<20;i++){
  if(V.shake<=0) break;
  punchEndLastCover=V.punchCover();
  V.update(STEP);
}
check("camera punch end leftover rest flush no dump", V.shake===0 && V.punchCover()===0 && punchEndLastCover<V.PUNCH_PX*0.55, `shake=${V.shake} cover=${V.punchCover()} last=${punchEndLastCover.toFixed(3)}`);
check("camera punch end leftover AABB planted through settle", Math.abs(V.bodyAABB(V.rival).y-punchAabb0.y)<2.5, `dy=${(V.bodyAABB(V.rival).y-punchAabb0.y).toFixed(2)}`);

// block punch still full mid, eases end
bootPlay(); wait(1700);
freezeRivalAI(); freezeYouAI();
V.rival.guarding = true; V.rival.wantBlock = true; V.rival.guardChoice = 1; V.rival.stamina = 100;
V.player.x = V.rival.x - 160; V.keepApart();
V.attackEdge = true;
let punchEndBlk=false;
for(let i=0;i<80;i++){
  freezeRivalAI();
  V.rival.guarding = true; V.rival.wantBlock = true; V.rival.guardChoice = 1;
  V.update(STEP);
  if(V.hitstopLeft>0){ punchEndBlk=true; break; }
}
check("camera punch end leftover block arms full cover", punchEndBlk && V.hitstopLeft===V.HITSTOP_BLOCK && V.shake===V.HITSTOP_BLOCK && V.punchCover()===V.PUNCH_PX && V.rival.hp===100, `blk=${punchEndBlk} hs=${V.hitstopLeft} shake=${V.shake} cover=${V.punchCover()} hp=${V.rival.hp}`);
drainHitstop();
let punchEndBlkNear=false, punchEndBlkCover=0;
for(let i=0;i<20;i++){
  if(V.shake>0 && V.shake/V.shakeDur<=0.25){ punchEndBlkNear=true; punchEndBlkCover=V.punchCover(); break; }
  if(V.shake<=0) break;
  V.update(STEP);
}
check("camera punch end leftover block eases near end", punchEndBlkNear && punchEndBlkCover<V.PUNCH_PX-0.01 && punchEndBlkCover>0, `near=${punchEndBlkNear} cover=${punchEndBlkCover.toFixed(3)}`);

// stun end breath still locked (no regress)
toMeasure();
freezeRivalAI(); freezeYouAI();
V.player.phase="idle"; V.player.phaseT=0; V.player.cut="slash"; V.player.boltPhase="";
V.player.guarding=false; V.player.gait=0; V.player.feintT=0; V.player.sheatheT=0;
V.player.stunT=0; V.player.thrownT=0; V.player.throwInvulnT=0; V.player.falling=false; V.player.hp=100;
V.rival.phase="idle"; V.rival.guarding=false; V.rival.stunT=0;
V.player.x = V.rival.x - 160; V.keepApart();
V.attackEdge=true;
let st211Hit=false;
for(let i=0;i<80;i++){
  freezeRivalAI();
  V.update(STEP);
  if(V.rival.hp<100 && V.rival.stunT>0){ st211Hit=true; break; }
}
drainHitstop();
let st211Near=false, st211Dy0=0;
for(let i=0;i<40;i++){
  holdRivalStill();
  if(V.rival.stunT>0 && V.rival.stunT<=V.GUARD_RAISE_MS){
    st211Near=true; st211Dy0=V.destRect(V.rival).dy; break;
  }
  if(V.rival.stunT<=0) break;
  V.update(STEP);
}
for(let i=0;i<10;i++){
  holdRivalStill();
  if(V.rival.stunT<=0) break;
  V.update(STEP);
}
const st211Ddy = V.destRect(V.rival).dy - st211Dy0;
check("stun end still no breath dump after camera punch end leftover", st211Hit && st211Near && V.rival.stunT===0 && Math.abs(st211Ddy)<0.85, `hit=${st211Hit} near=${st211Near} ddy=${st211Ddy.toFixed(3)} st=${V.rival.stunT}`);
check("cache v=296 after camera punch end leftover live", /game.js\?v=350/.test(htmlTxt));


// --- v212 idle settle→guard destRect leftover: settle breath keeps suppressed through raise ---
check("idle settle→guard destRect leftover seats no breath mid-settle", /Idle settle→guard destRect leftover/.test(srcTxt) && /leftover settle breath used to seat ~1\.3 the tick/.test(srcTxt) && /S raised mid-settle/.test(srcTxt) && /idleBreath used only raise k/.test(srcTxt) && /Keep the stronger of settle k \/ raise k/.test(srcTxt));
check("idle settle→guard destRect leftover is destRect-only", /destRect-only \(idleBreath\)/.test(srcTxt) && /Math\.max\(sk, rk\)/.test(sliceFn(codeOnly, "idleBreath")) && /Idle settle still crossfades/.test(srcTxt) && /Pure idle→guard still eases with raise/.test(srcTxt) && !/Math\.max\(sk, rk\)/.test(sliceFn(codeOnly, "destRect")) && !/Math\.max\(sk, rk\)/.test(sliceFn(codeOnly, "bodyAABB")) && !/Math\.max\(sk, rk\)/.test(sliceFn(codeOnly, "slashPose")) && !/Math\.max\(sk, rk\)/.test(sliceFn(codeOnly, "walkSettleK")) && !/Math\.max\(sk, rk\)/.test(sliceFn(codeOnly, "updateGuard")));
check("idle settle→guard destRect leftover keeps max settle/raise", /const sk = walkSettleK\(f\)/.test(sliceFn(codeOnly, "idleBreath")) && /const rk = guardRaiseK\(f\)/.test(sliceFn(codeOnly, "idleBreath")) && /1 - Math\.max\(sk, rk\)/.test(sliceFn(codeOnly, "idleBreath")));
check("idle settle→guard destRect leftover idle settle path unchanged", /Idle settle still crossfades breath when not guarding/.test(srcTxt) && /return amp \* \(1 - walkSettleK\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && V.WALK_SETTLE_MS===160);
check("idle settle→guard destRect leftover pure idle→guard still eases", /Pure idle→guard still eases with raise/.test(srcTxt) && /S used to dump leftover breath/.test(srcTxt) && /if \(f\.guarding \|\| f\.guardPoseK > 0\) return amp \* \(1 - guardRaiseK\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")));
check("idle settle→guard destRect leftover walk lean still holds", /Walk→guard destRect lean leftover/.test(srcTxt) && /Keep leftover settle rot\/oy through leftover walkSettleT/.test(srcTxt) && /if \(f\.guarding\)/.test(sliceFn(codeOnly, "walkSettleK")));
check("idle settle→guard destRect leftover extra rot stays 0", /Extra destRect rot stays 0/.test(srcTxt) && /AABB planted/.test(srcTxt) && /Chip stun \/ throw KD unchanged/.test(srcTxt));
check("idle settle→guard destRect leftover is not a new combat verb", V.WALK_SETTLE_MS===160 && V.WALK_STEP_MS===320 && V.GUARD_RAISE_MS===80 && V.HITSTUN===350 && V.THROW_KD_MS===520 && V.HITSTOP_HIT===140 && V.GUARD_COMMIT_MS===140 && V.PUSHBLOCK_STAM===25 && V.PUSHBLOCK_PX===240 && V.FEINT_RECOVERY===100);
check("slash frames still 180/140/280 after idle settle→guard destRect leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after idle settle→guard destRect leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after idle settle→guard destRect leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after idle settle→guard destRect leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("camera punch end leftover still eases after idle settle→guard", /k > 0\.25/.test(sliceFn(codeOnly, "punchCover")) && /PUNCH_PX \* u \* u \* \(3 - 2 \* u\)/.test(sliceFn(codeOnly, "punchCover")));
check("stun end destRect leftover still eases breath after idle settle→guard", /f\.stunT > 0/.test(sliceFn(codeOnly, "idleBreath")) && /f\.stunT \/ GUARD_RAISE_MS/.test(sliceFn(codeOnly, "idleBreath")));
check("feint end destRect leftover still skips sheathe amp after idle settle→guard", /f\.sheatheT > 0/.test(sliceFn(codeOnly, "idleBreath")) && !/amp \*= f\.sheatheT \/ SHEATHE_MS/.test(sliceFn(codeOnly, "idleBreath")) && /amp \* \(1 - feintFade\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")));
check("cache v=296 after idle settle→guard destRect leftover", /game.js\?v=350/.test(htmlTxt));

bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x = 400; V.rival.x = 1100;
V.player.guarding=false; V.player.guardPoseK=0; V.player.phase="idle"; V.player.boltPhase=""; V.player.stunT=0; V.player.falling=false; V.player.feintT=0; V.player.sheatheT=0;
V.player.gait=0; V.player.walkT=0; V.player.walkSettleT=0; V.player.walkFadeHold=0; V.player.walkRiseT=0;
V.keys.add("KeyD");
for(let i=0;i<20;i++){
  freezeRivalAI();
  V.player.phase="idle"; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
  V.keys.add("KeyD");
  V.update(STEP);
}
V.keys.delete("KeyD");
freezeRivalAI();
V.update(STEP);
let sgNear=false, sgBreath0=0, sgSettle0=0, sgAabb0=null, sgDy0=0;
for(let i=0;i<12;i++){
  freezeRivalAI();
  V.player.phase="idle"; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
  if(V.player.walkSettleT>0 && V.player.walkSettleT<=V.WALK_SETTLE_MS*0.55){
    sgNear=true;
    sgSettle0=V.player.walkSettleT;
    sgBreath0=V.idleBreath(V.player);
    sgAabb0=V.bodyAABB(V.player);
    sgDy0=V.destRect(V.player).dy;
    break;
  }
  if(V.player.walkSettleT<=0) break;
  V.update(STEP);
}
check("idle settle→guard destRect leftover settle live", sgNear && sgSettle0>0 && V.player.guarding===false && V.player.gait===0 && Math.abs(sgBreath0)<1.2, `near=${sgNear} settle=${sgSettle0} breath=${sgBreath0.toFixed(3)} g=${V.player.guarding} gait=${V.player.gait}`);
V.keys.add("KeyS");
freezeRivalAI();
V.update(STEP);
const sgBreath1=V.idleBreath(V.player);
const sgSk=V.walkSettleK(V.player);
const sgRk=V.guardRaiseK(V.player);
const sgDdy=V.destRect(V.player).dy-sgDy0;
check("idle settle→guard destRect leftover raise keeps breath suppressed", V.player.guarding && sgNear && Math.abs(sgBreath1-sgBreath0)<0.85 && sgSk>=sgRk-0.001, `g=${V.player.guarding} b0=${sgBreath0.toFixed(3)} b1=${sgBreath1.toFixed(3)} sk=${sgSk.toFixed(3)} rk=${sgRk.toFixed(3)}`);
check("idle settle→guard destRect leftover no chest seat on raise", sgNear && Math.abs(sgBreath1)<1.2 && Math.abs(sgBreath1)<=Math.abs(sgBreath0)+0.85, `b0=${sgBreath0.toFixed(3)} b1=${sgBreath1.toFixed(3)}`);
check("idle settle→guard destRect leftover AABB planted through raise", sgAabb0 && Math.abs(V.bodyAABB(V.player).y-sgAabb0.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-sgAabb0.h)<0.05, `dy=${sgAabb0?(V.bodyAABB(V.player).y-sgAabb0.y).toFixed(2):"na"}`);
check("idle settle→guard destRect leftover destRect rot not wild", Math.abs(V.destRect(V.player).rot)<0.25, `rot=${V.destRect(V.player).rot.toFixed(3)} ddy=${sgDdy.toFixed(3)}`);
check("idle settle→guard destRect leftover poseBitmap block", isBlockSheet(V.player) && !/walk/.test(sheetSrc(V.player)), `src=${sheetSrc(V.player)}`);
let sgMidBreath=sgBreath1, sgMidSk=sgSk, sgMidRk=sgRk;
for(let i=0;i<3;i++){
  freezeRivalAI();
  V.keys.add("KeyS");
  V.update(STEP);
  sgMidBreath=V.idleBreath(V.player);
  sgMidSk=V.walkSettleK(V.player);
  sgMidRk=V.guardRaiseK(V.player);
}
check("idle settle→guard destRect leftover breath stays low mid-raise", V.player.guarding && Math.abs(sgMidBreath)<1.0 && sgMidSk+sgMidRk>0, `breath=${sgMidBreath.toFixed(3)} sk=${sgMidSk.toFixed(3)} rk=${sgMidRk.toFixed(3)} g=${V.player.guarding}`);
check("idle settle→guard destRect leftover AABB planted mid-raise", sgAabb0 && Math.abs(V.bodyAABB(V.player).y-sgAabb0.y)<0.05, `dy=${sgAabb0?(V.bodyAABB(V.player).y-sgAabb0.y).toFixed(2):"na"}`);
let sgGone=false, sgEndBreath=0;
for(let i=0;i<20;i++){
  freezeRivalAI();
  V.keys.add("KeyS");
  V.update(STEP);
  if(V.player.walkSettleT===0 && V.guardRaiseK(V.player)>0.95 && V.player.guarding){
    sgGone=true;
    sgEndBreath=V.idleBreath(V.player);
    break;
  }
}
check("idle settle→guard destRect leftover breath dies with raise", sgGone && Math.abs(sgEndBreath)<0.2 && V.player.guarding && isBlockSheet(V.player), `gone=${sgGone} breath=${sgEndBreath.toFixed(3)} g=${V.player.guarding} src=${sheetSrc(V.player)}`);

bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x = 400; V.rival.x = 1100;
V.player.guarding=false; V.player.guardPoseK=0; V.player.phase="idle"; V.player.boltPhase=""; V.player.stunT=0; V.player.feintT=0; V.player.sheatheT=0;
V.keys.add("KeyD");
for(let i=0;i<20;i++){
  freezeRivalAI();
  V.player.phase="idle"; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase="";
  V.keys.add("KeyD");
  V.update(STEP);
}
V.keys.delete("KeyD");
freezeRivalAI();
V.update(STEP);
let sgIdleB0=V.idleBreath(V.player);
for(let i=0;i<14;i++){
  freezeRivalAI();
  V.player.phase="idle"; V.player.guarding=false; V.player.stunT=0;
  V.update(STEP);
  if(V.player.walkSettleT<=0) break;
}
const sgIdleB1=V.idleBreath(V.player);
check("idle settle without S still seats breath", V.player.walkSettleT===0 && V.player.guarding===false && Math.abs(sgIdleB1)>Math.abs(sgIdleB0)+0.2, `settle=${V.player.walkSettleT} b0=${sgIdleB0.toFixed(3)} b1=${sgIdleB1.toFixed(3)} g=${V.player.guarding}`);

bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x = 500; V.rival.x = 1100;
V.player.guarding=false; V.player.guardPoseK=0; V.player.phase="idle"; V.player.boltPhase=""; V.player.stunT=0; V.player.feintT=0; V.player.sheatheT=0;
V.player.gait=0; V.player.walkT=0; V.player.walkSettleT=0; V.player.walkFadeHold=0;
for(let i=0;i<8;i++){ freezeRivalAI(); V.update(STEP); }
const sgPureB0=V.idleBreath(V.player);
V.keys.add("KeyS");
freezeRivalAI();
V.update(STEP);
const sgPureB1=V.idleBreath(V.player);
const sgPureRk=V.guardRaiseK(V.player);
check("pure idle→guard breath not higher than idle", V.player.guarding && Math.abs(sgPureB1)<=Math.abs(sgPureB0)+0.15, `b0=${sgPureB0.toFixed(3)} b1=${sgPureB1.toFixed(3)} rk=${sgPureRk.toFixed(3)}`);

check("cache v=296 after idle settle→guard destRect leftover live", /game.js\?v=350/.test(htmlTxt));



// --- v213 throw / cut recovery fade leftover: fade >0 keeps idle base ---
check("throw / cut recovery fade leftover keeps idle base through live fade", /Throw \/ cut recovery fade leftover/.test(srcTxt) && /leftover windup\/slash overlay used to die the tick/.test(srcTxt) && /fade dropped under 0\.02/.test(srcTxt) && /restThrow/.test(srcTxt) && /sheatheFade/.test(srcTxt));
check("throw / cut recovery fade leftover is draw-only", /Draw-only \(drawKnight\)/.test(srcTxt) && /trf > 0 && !restThrowWalk && !restParry && ready\(wind\)/.test(sliceFn(codeOnly, "drawKnight")) && /restSheathe = fade > 0 && !f\.guarding && !restSheatheWalk && ready\(cut\) && ready\(idle\)/.test(sliceFn(codeOnly, "drawKnight")) && !/trf > 0\.02 && ready\(wind\)/.test(sliceFn(codeOnly, "drawKnight")) && /fade > 0\.02 && ready\(cut\) && !restClash/.test(sliceFn(codeOnly, "drawKnight")));
check("throw / cut recovery fade leftover throw plant path unchanged", /function throwPlantFade/.test(srcTxt) && /1 - u \* u \* \(3 - 2 \* u\)/.test(sliceFn(codeOnly, "throwPlantFade")) && /poseBitmap still windup/.test(srcTxt));
check("throw / cut recovery fade leftover sheathe overlay still skips tiny", /fade > 0\.02 && ready\(cut\) && !restClash/.test(sliceFn(codeOnly, "drawKnight")) && /Keep idle base through any live sheatheFade/.test(srcTxt));
check("throw / cut recovery fade leftover bolt\/feint still rest on idle poseBitmap", /Bolt \/ feint \/ guard-drop fades already/.test(srcTxt) && /restBolt = bf > 0\.02/.test(sliceFn(codeOnly, "drawKnight")) && /restFeint = ff > 0\.02/.test(sliceFn(codeOnly, "drawKnight")) && /restGuard = gf > 0\.02/.test(sliceFn(codeOnly, "drawKnight")));
check("throw / cut recovery fade leftover extra rot stays 0", /Extra destRect rot stays 0/.test(srcTxt) && /AABB planted/.test(srcTxt) && /Chip stun \/ throw KD unchanged/.test(srcTxt));
check("throw / cut recovery fade leftover is not a new combat verb", V.THROW_STARTUP===80 && V.THROW_ACTIVE===40 && V.THROW_RECOVERY===220 && V.THROW_STAM===0 && V.THROW_RANGE===120 && V.HITSTUN===350 && V.THROW_KD_MS===520 && V.SHEATHE_MS===140 && V.GUARD_RAISE_MS===80 && V.PUSHBLOCK_STAM===25 && V.PUSHBLOCK_PX===240 && V.FEINT_RECOVERY===100);
check("slash frames still 180/140/280 after throw / cut recovery fade leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after throw / cut recovery fade leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after throw / cut recovery fade leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after throw / cut recovery fade leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("idle settle→guard destRect leftover still max settle\/raise after fade leftover", /Math\.max\(sk, rk\)/.test(sliceFn(codeOnly, "idleBreath")) && /1 - Math\.max\(sk, rk\)/.test(sliceFn(codeOnly, "idleBreath")));
check("camera punch end leftover still eases after fade leftover", /k > 0\.25/.test(sliceFn(codeOnly, "punchCover")) && /PUNCH_PX \* u \* u \* \(3 - 2 \* u\)/.test(sliceFn(codeOnly, "punchCover")));
check("cache v=296 after throw / cut recovery fade leftover", /game.js\?v=350/.test(htmlTxt));

bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x = 600; V.rival.x = 720;
V.player.phase="recovery"; V.player.cut="throw"; V.player.phaseT=0;
V.player.techRec=false; V.player.clashRec=false; V.player.guarding=false;
V.player.sheatheT=0; V.player.feintT=0; V.player.boltPhase=""; V.player.stunT=0;
V.player.gait=0; V.player.guardPoseK=0; V.player.falling=false;
let tfLate=false, tfFadeLate=0, tfSheetLate="", tfIdleBase=false;
for(let i=0;i<30;i++){
  freezeRivalAI();
  const fade=V.throwPlantFade(V.player);
  const src=sheetSrc(V.player);
  if(V.player.phase==="recovery" && fade>0 && fade<=0.02){
    tfLate=true;
    tfFadeLate=fade;
    tfSheetLate=src;
    // drawKnight restThrow must still own idle base while fade live and poseBitmap windup
    tfIdleBase = fade > 0 && /windup/.test(src);
    break;
  }
  if(V.player.phase==="idle") break;
  V.update(STEP);
}
check("throw recovery fade leftover late tick still live under 0.02", tfLate && tfFadeLate>0 && tfFadeLate<=0.02 && V.player.phase==="recovery", `late=${tfLate} fade=${tfFadeLate} ph=${V.player.phase}`);
check("throw recovery fade leftover late tick poseBitmap still windup", tfLate && /windup/.test(tfSheetLate), `src=${tfSheetLate}`);
check("throw recovery fade leftover late tick idle base still owned", tfLate && tfIdleBase && /trf > 0 && !restThrowWalk && !restParry && ready\(wind\)/.test(sliceFn(codeOnly, "drawKnight")), `owned=${tfIdleBase} fade=${tfFadeLate}`);
const tfFadeAabb0=V.bodyAABB(V.player);
const tfFadeDy0=V.destRect(V.player).dy;
freezeRivalAI(); V.update(STEP);
check("throw recovery fade leftover AABB planted through late tick", Math.abs(V.bodyAABB(V.player).y-tfFadeAabb0.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-tfFadeAabb0.h)<0.05, `dy=${(V.bodyAABB(V.player).y-tfFadeAabb0.y).toFixed(2)}`);
check("throw recovery fade leftover destRect rot not wild", Math.abs(V.destRect(V.player).rot)<0.25, `rot=${V.destRect(V.player).rot.toFixed(3)} ddy=${(V.destRect(V.player).dy-tfFadeDy0).toFixed(3)}`);

bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x = 500; V.rival.x = 900;
V.player.phase="recovery"; V.player.cut="slash"; V.player.phaseT=0;
V.player.clashRec=false; V.player.techRec=false; V.player.cutHit=true;
V.player.guarding=false; V.player.sheatheT=0; V.player.feintT=0; V.player.boltPhase="";
V.player.stunT=0; V.player.gait=0; V.player.guardPoseK=0; V.player.falling=false;
let sfLate=false, sfFadeLate=0, sfSheetLate="";
for(let i=0;i<40;i++){
  freezeRivalAI();
  const fade=V.sheatheFade(V.player);
  const src=sheetSrc(V.player);
  if(V.player.phase==="recovery" && fade>0 && fade<=0.02){
    sfLate=true; sfFadeLate=fade; sfSheetLate=src; break;
  }
  if(V.player.phase==="idle") break;
  V.update(STEP);
}
check("slash recovery fade leftover late tick still live under 0.02", sfLate && sfFadeLate>0 && sfFadeLate<=0.02 && V.player.phase==="recovery", `late=${sfLate} fade=${sfFadeLate} ph=${V.player.phase}`);
check("slash recovery fade leftover late tick poseBitmap still slash", sfLate && /slash/.test(sfSheetLate) && !/windup/.test(sfSheetLate), `src=${sfSheetLate}`);
check("slash recovery fade leftover idle base gate is > 0", /restSheathe = fade > 0 && !f\.guarding && !restSheatheWalk && ready\(cut\) && ready\(idle\)/.test(sliceFn(codeOnly, "drawKnight")) && !/else if \(fade > 0\.02 && !f\.guarding && ready\(cut\)/.test(sliceFn(codeOnly, "drawKnight")));
const sfFadeAabb0=V.bodyAABB(V.player);
freezeRivalAI(); V.update(STEP);
check("slash recovery fade leftover AABB planted through late tick", Math.abs(V.bodyAABB(V.player).y-sfFadeAabb0.y)<0.05, `dy=${(V.bodyAABB(V.player).y-sfFadeAabb0.y).toFixed(2)}`);

bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x = 500; V.rival.x = 900;
V.player.phase="recovery"; V.player.cut="golpe"; V.player.phaseT=0;
V.player.clashRec=false; V.player.techRec=false; V.player.cutHit=true;
V.player.guarding=false; V.player.sheatheT=0; V.player.feintT=0;
let gfLate=false, gfFadeLate=0;
for(let i=0;i<30;i++){
  freezeRivalAI();
  const fade=V.sheatheFade(V.player);
  if(V.player.phase==="recovery" && fade>0 && fade<=0.02){ gfLate=true; gfFadeLate=fade; break; }
  if(V.player.phase==="idle") break;
  V.update(STEP);
}
check("golpe recovery fade leftover late tick still live under 0.02", gfLate && gfFadeLate>0 && gfFadeLate<=0.02 && V.player.phase==="recovery" && V.player.cut==="golpe", `late=${gfLate} fade=${gfFadeLate} cut=${V.player.cut}`);

bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x = 500; V.rival.x = 900;
V.player.phase="idle"; V.player.sheatheT=V.SHEATHE_MS; V.player.clashRec=true;
V.player.guarding=false; V.player.feintT=0; V.player.boltPhase=""; V.player.stunT=0;
let isLate=false, isFadeLate=0, isSheet="";
for(let i=0;i<20;i++){
  freezeRivalAI();
  const fade=V.sheatheFade(V.player);
  const src=sheetSrc(V.player);
  if(V.player.sheatheT>0 && fade>0 && fade<=0.02){ isLate=true; isFadeLate=fade; isSheet=src; break; }
  if(V.player.sheatheT<=0) break;
  V.update(STEP);
}
// Idle clash/tech sheathe uses recovery smoothstep — late window (0,0.02] is reachable.
check("idle sheathe fade leftover late tick still live under 0.02", isLate && isFadeLate>0 && isFadeLate<=0.02 && V.player.sheatheT>0, `late=${isLate} fade=${isFadeLate} st=${V.player.sheatheT}`);
check("idle sheathe fade leftover late tick poseBitmap still slash", isLate && /slash/.test(isSheet), `src=${isSheet}`);
check("idle sheathe fade leftover idle base gate still > 0", /restSheathe = fade > 0 && !f\.guarding && !restSheatheWalk && ready\(cut\) && ready\(idle\)/.test(sliceFn(codeOnly, "drawKnight")));

check("cache v=296 after throw / cut recovery fade leftover live", /game.js\?v=350/.test(htmlTxt));

// --- v215 pushblock dust leftover: landBlock 1.0 plant no longer stacks under shove ---
check("pushblock dust leftover skips landBlock plant when shove owns scrape",
  /let shoved = false/.test(sliceFn(codeOnly, "landBlock"))
  && /if \(def === rival\) shoved = rivalPushblockOnBlock\(\)/.test(sliceFn(codeOnly, "landBlock"))
  && /if \(!shoved\) spawnPlantDust\(def, 1\.0\)/.test(sliceFn(codeOnly, "landBlock"))
  && !/spawnPlantDust\(def, 1\.0\);\s*def\.stamina/.test(sliceFn(codeOnly, "landBlock")));
check("pushblock dust leftover is draw-only",
  /Pushblock dust leftover/.test(srcTxt)
  && /Skip the small plant when shove owns the scrape/.test(srcTxt)
  && /Draw-only/.test(srcTxt));
check("pushblock dust leftover still 240\/25\/1.85", V.PUSHBLOCK_STAM===25 && V.PUSHBLOCK_PX===240 && V.PUSHBLOCK_FX===1.85 && V.PUSHBLOCK_AI_CD===1800 && V.PUSHBLOCK_AI_CHANCE===0.4);
check("pushblock dust leftover is not a new combat verb",
  (htmlTxt.match(/data-tap=/g)||[]).length===3 && !/zone-pushblock/.test(htmlTxt) && /data-tap="KeyL"/.test(htmlTxt));
check("slash frames still 180\/140\/280 after pushblock dust leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280);
check("golpe frames still 120\/80\/180 after pushblock dust leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180);
check("K frames still 200\/280 after pushblock dust leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280);
check("throw\/cut recovery fade leftover idle base gate still > 0 after pushblock dust leftover", /restSheathe = fade > 0 && !f\.guarding && !restSheatheWalk && ready\(cut\) && ready\(idle\)/.test(sliceFn(codeOnly, "drawKnight")));
check("idle sheathe fade leftover still smoothstep after pushblock dust leftover", /const u = 1 - Math\.max\(0, Math\.min\(1, f\.sheatheT \/ SHEATHE_MS\)\);\s*return 1 - u \* u \* \(3 - 2 \* u\)/.test(sliceFn(codeOnly, "sheatheFade")));
check("cache v=296 after pushblock dust leftover", /game.js\?v=350/.test(htmlTxt));

function captureConnectingRivalDust(cut, forcePb){
  armRivalPushblockAI();
  toClose();
  V.plantDust.splice(0);
  V.player.phase = "idle";
  V.player.cut = "slash";
  V.player.phaseT = 0;
  V.player.cutHit = false;
  V.player.guarding = false;
  V.player.stunT = 0;
  V.player.falling = false;
  V.player.boltPhase = "";
  V.rival.pbArmed = forcePb ? 1 : -1;
  V.rival.pbCd = 0;
  V.rival.revArmed = -1;
  V.rival.revCd = 99999;
  V.rival.wantBlock = true;
  V.rival.guarding = true;
  V.rival.guardChoice = 1;
  V.rival.stamina = 100;
  Math.random = () => (forcePb ? 0 : 0.99);
  const x0 = V.rival.x;
  const st0 = V.rival.stamina;
  const aabb0 = V.bodyAABB(V.rival);
  V.startAttack(V.player, cut==="golpe" ? "golpe" : undefined);
  let blocked=false, pb=false, saw=false;
  let shoveN=0, blockN=0, shovePow=0, sfx="";
  for(let i=0;i<90;i++){
    Math.random = () => (forcePb ? 0 : 0.99);
    V.rival.wantBlock = true;
    V.rival.guardChoice = 1;
    V.rival.revArmed = -1;
    V.rival.revCd = 99999;
    V.rival.boltCd = 99999;
    V.rival.throwCd = 99999;
    V.rival.closing = false;
    V.rival.standGoal = 99999;
    V.rival.gait = 0;
    V.player.guarding = false;
    V.player.stunT = 0;
    V.player.falling = false;
    V.player.boltPhase = "";
    V.keys.clear();
    V.update(STEP);
    if(V.player.cutHit && V.rival.hp===100) blocked=true;
    if(V.lastPushblockSfx==="pushblock" && V.rival.pushT>0 && (st0-V.rival.stamina)>=V.PUSHBLOCK_STAM-2) pb=true;
    // Capture only on this connect — leftover lastPushblockSfx must not trip saw.
    if(V.player.cutHit && !saw){
      saw=true;
      const stamps=(V.plantDust||[]).filter(p=>!p.speck && p.homeYou===false);
      shoveN=stamps.filter(p=>p.shove).length;
      blockN=stamps.filter(p=>!p.shove && Math.abs((p.power||0)-1)<0.01).length;
      shovePow=stamps.filter(p=>p.shove).reduce((m,p)=>Math.max(m,p.power||0),0);
      sfx=V.lastPushblockSfx;
      blocked = V.rival.hp===100;
      pb = sfx==="pushblock" && shoveN>0;
    }
    if(V.hitstopLeft>0) drainHitstop();
    if(saw && V.player.phase==="idle" && i>5) break;
  }
  return { blocked, pb, shoveN, blockN, shovePow, sfx, dx: V.rival.x-x0, dy: V.bodyAABB(V.rival).y-aabb0.y };
}

bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
const pbDustLive = captureConnectingRivalDust("slash", true);
check("pushblock dust leftover live rival shove still fires", pbDustLive.pb && pbDustLive.blocked && pbDustLive.shoveN>0 && pbDustLive.shovePow>=V.PUSHBLOCK_FX-0.01 && pbDustLive.sfx==="pushblock", `pb=${pbDustLive.pb} blocked=${pbDustLive.blocked} n=${pbDustLive.shoveN} pow=${pbDustLive.shovePow.toFixed(2)} sfx=${pbDustLive.sfx}`);
check("pushblock dust leftover live no stacked block plant under shove", pbDustLive.pb && pbDustLive.blockN===0, `pb=${pbDustLive.pb} blockStamps=${pbDustLive.blockN} shove=${pbDustLive.shoveN}`);
check("pushblock dust leftover live shove still 240", pbDustLive.pb && pbDustLive.dx>=V.PUSHBLOCK_PX-30 && pbDustLive.dx<=V.PUSHBLOCK_PX+40, `dx=${pbDustLive.dx.toFixed(1)}`);
check("pushblock dust leftover live AABB planted", Math.abs(pbDustLive.dy)<0.05, `dy=${pbDustLive.dy.toFixed(2)}`);

bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
const noPbLive = captureConnectingRivalDust("slash", false);
check("pushblock dust leftover normal block still plants 1.0", noPbLive.blocked && !noPbLive.pb && noPbLive.blockN>0 && noPbLive.shoveN===0 && noPbLive.sfx==="block", `blocked=${noPbLive.blocked} pb=${noPbLive.pb} blockN=${noPbLive.blockN} shoveN=${noPbLive.shoveN} sfx=${noPbLive.sfx}`);

bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
toClose();
raiseGuardInRoom();
V.plantDust.splice(0);
const youStPbDust0 = V.player.stamina;
V.keys.add("KeyA");
V.update(STEP);
const youShove = (V.plantDust||[]).filter(p=>!p.speck && p.homeYou===true && p.shove);
const youBlock = (V.plantDust||[]).filter(p=>!p.speck && p.homeYou===true && !p.shove);
check("pushblock dust leftover player away shove still alone", V.player.guarding && V.player.pushT>0 && youShove.length===1 && youBlock.length===0 && Math.abs((youShove[0].power||0)-V.PUSHBLOCK_FX)<0.01 && (youStPbDust0-V.player.stamina)>=V.PUSHBLOCK_STAM-1, `g=${V.player.guarding} shove=${youShove.length} block=${youBlock.length} pow=${youShove[0]&&youShove[0].power} spent=${(youStPbDust0-V.player.stamina).toFixed(2)}`);
V.keys.clear();

check("cache v=296 after pushblock dust leftover live", /game.js\?v=350/.test(htmlTxt));


// --- v216 player pushblock dust leftover: landBlock 1.0 plant culled when shove owns scrape ---
check("player pushblock dust leftover drops block plant under shove",
  /function dropBlockPlantUnderShove/.test(codeOnly)
  && /dropBlockPlantUnderShove\(f\)/.test(sliceFn(codeOnly, "tryPushblock"))
  && /Player pushblock dust leftover/.test(srcTxt)
  && /Drop leftover block plant stamps/.test(srcTxt)
  && /when shove owns the scrape/.test(srcTxt));
check("player pushblock dust leftover is draw-only",
  /function dropBlockPlantUnderShove/.test(srcTxt)
  && /Draw-only\. Cull leftover landBlock plant stamps/.test(srcTxt)
  && /dropBlockPlantUnderShove\(f\)/.test(sliceFn(codeOnly, "tryPushblock"))
  && !/dropBlockPlantUnderShove/.test(sliceFn(codeOnly, "destRect"))
  && !/dropBlockPlantUnderShove/.test(sliceFn(codeOnly, "bodyAABB"))
  && !/dropBlockPlantUnderShove/.test(sliceFn(codeOnly, "poseBitmap"))
  && !/dropBlockPlantUnderShove/.test(sliceFn(codeOnly, "landBlock")));
check("player pushblock dust leftover still 240\/25\/1.85", V.PUSHBLOCK_STAM===25 && V.PUSHBLOCK_PX===240 && V.PUSHBLOCK_FX===1.85 && V.PUSHBLOCK_AI_CD===1800 && V.PUSHBLOCK_AI_CHANCE===0.4);
check("player pushblock dust leftover does not retune combat", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.HITSTUN===350 && V.STAMINA_BLOCK===20);
check("player pushblock dust leftover is not a new combat verb",
  (htmlTxt.match(/data-tap=/g)||[]).length===3 && !/zone-pushblock/.test(htmlTxt) && /data-tap="KeyL"/.test(htmlTxt));
check("rival landBlock still skips plant when shove owns scrape after player pushblock dust leftover",
  /let shoved = false/.test(sliceFn(codeOnly, "landBlock"))
  && /if \(!shoved\) spawnPlantDust\(def, 1\.0\)/.test(sliceFn(codeOnly, "landBlock")));
check("cache v=296 after player pushblock dust leftover", /game.js\?v=350/.test(htmlTxt));

// Synthetic: leftover block plant under boot dies when shove owns scrape
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
toClose();
raiseGuardInRoom();
V.plantDust.splice(0);
V.spawnPlantDust(V.player, 1.0);
const synBlock0 = (V.plantDust||[]).filter(p=>!p.speck && p.homeYou===true && !p.shove && Math.abs((p.power||0)-1)<0.01).length;
const synSt0 = V.player.stamina;
const synAabb0 = V.bodyAABB(V.player);
V.keys.add("KeyA");
V.update(STEP);
const synShove = (V.plantDust||[]).filter(p=>!p.speck && p.homeYou===true && p.shove);
const synBlock1 = (V.plantDust||[]).filter(p=>!p.speck && p.homeYou===true && !p.shove && Math.abs((p.power||0)-1)<0.01);
check("player pushblock dust leftover synthetic culls block plant under shove", synBlock0===1 && V.player.pushT>0 && synShove.length===1 && synBlock1.length===0 && Math.abs((synShove[0].power||0)-V.PUSHBLOCK_FX)<0.01 && (synSt0-V.player.stamina)>=V.PUSHBLOCK_STAM-1, `block0=${synBlock0} shove=${synShove.length} block1=${synBlock1.length} pow=${synShove[0]&&synShove[0].power} spent=${(synSt0-V.player.stamina).toFixed(2)} pushT=${V.player.pushT}`);
check("player pushblock dust leftover synthetic AABB planted", Math.abs(V.bodyAABB(V.player).y-synAabb0.y)<0.05, `dy=${(V.bodyAABB(V.player).y-synAabb0.y).toFixed(2)}`);
V.keys.clear();

// Live: connect block plants 1.0, then away shove culls it (hitstop buffer path)
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
toClose();
V.plantDust.splice(0);
// Drive rival slash into your guard until hitstop (same as landRivalCloseSlash)
V.player.hp = 100;
V.rival.hp = 100;
V.player.stamina = 100;
V.player.stamRegenT = 99999;
V.player.guardBreakT = 0;
V.player.stunT = 0;
V.keys.add("KeyS");
V.player.wantBlock = true;
V.player.guarding = true;
V.rival.pbArmed = -1;
V.rival.revArmed = -1;
V.rival.linkGolpe = false;
V.startAttack(V.rival);
let youPbHit=false;
for(let i=0;i<90;i++){
  V.keys.add("KeyS");
  V.player.wantBlock = true;
  V.player.guarding = true;
  V.rival.wantBlock = false;
  V.rival.guarding = false;
  V.rival.revArmed = -1;
  V.rival.pbArmed = -1;
  V.rival.standGoal = 99999;
  V.rival.closing = false;
  V.rival.gait = 0;
  V.rival.boltCd = 99999;
  V.rival.throwCd = 99999;
  V.update(STEP);
  if(V.hitstopLeft>0 || (V.rival.cutHit && V.player.pushT>0 && V.player.hp===100)){
    youPbHit=true;
    break;
  }
}
const youPbBlockAtHit = (V.plantDust||[]).filter(p=>!p.speck && p.homeYou===true && !p.shove && Math.abs((p.power||0)-1)<0.01).length;
const youPbX0 = V.player.x;
const youPbAabb0 = V.bodyAABB(V.player);
// Buffer away during remaining hitstop (or fire immediately if hitstop already drained)
V.keys.add("KeyS");
V.keys.add("KeyA");
V.player.guarding = true;
if(V.hitstopLeft>0){
  V.update(STEP); // edge into pushblockBuf
  for(let j=0;j<40;j++){
    if(V.hitstopLeft<=0) break;
    V.keys.add("KeyS");
    V.keys.add("KeyA");
    V.player.guarding = true;
    V.update(STEP);
  }
}
V.keys.add("KeyS");
V.player.guarding = true;
V.player.stamina = Math.max(V.player.stamina, V.PUSHBLOCK_STAM+5);
// Consume buffer / edge
if(!V.keys.has("KeyA")) V.keys.add("KeyA");
V.update(STEP);
// If shove did not arm (edge already spent during hitstop), tap away fresh
if(V.player.pushT<=0 || V.lastPushblockSfx!=="pushblock"){
  V.keys.delete("KeyA");
  V.update(STEP); // release so walkHeldA clears
  V.keys.add("KeyS");
  V.keys.add("KeyA");
  V.player.guarding = true;
  V.player.stamina = Math.max(V.player.stamina, V.PUSHBLOCK_STAM+5);
  V.update(STEP);
}
const youPbStamps=(V.plantDust||[]).filter(p=>!p.speck && p.homeYou===true);
const youPbShoveN=youPbStamps.filter(p=>p.shove).length;
const youPbBlockN=(V.plantDust||[]).filter(p=>!p.speck && p.homeYou===true && !p.shove && Math.abs((p.power||0)-1)<0.01).length;
const youPbShovePow=youPbStamps.filter(p=>p.shove).reduce((m,p)=>Math.max(m,p.power||0),0);
const youPbSfx=V.lastPushblockSfx;
const youPbDx=youPbX0-V.player.x;
check("player pushblock dust leftover live connect still plants then shove", youPbHit && youPbBlockAtHit>0 && youPbShoveN>0 && youPbShovePow>=V.PUSHBLOCK_FX-0.01 && youPbSfx==="pushblock", `hit=${youPbHit} blockAtHit=${youPbBlockAtHit} shove=${youPbShoveN} pow=${youPbShovePow} sfx=${youPbSfx}`);
check("player pushblock dust leftover live no stacked block plant under shove", youPbHit && youPbShoveN>0 && youPbBlockN===0, `hit=${youPbHit} blockStamps=${youPbBlockN} shove=${youPbShoveN} blockAtHit=${youPbBlockAtHit}`);
check("player pushblock dust leftover live shove still moves", youPbHit && youPbDx>=20, `dx=${youPbDx.toFixed(1)}`);
check("player pushblock dust leftover live AABB planted", youPbHit && Math.abs(V.bodyAABB(V.player).y-youPbAabb0.y)<0.05, `dy=${(V.bodyAABB(V.player).y-youPbAabb0.y).toFixed(2)}`);
V.keys.clear();

// Normal block without shove still keeps 1.0 plant
V.plantDust.splice(0);
landRivalCloseSlash(true);
const nbBlockN=(V.plantDust||[]).filter(p=>!p.speck && p.homeYou===true && !p.shove && Math.abs((p.power||0)-1)<0.01).length;
const nbShoveN=(V.plantDust||[]).filter(p=>!p.speck && p.homeYou===true && p.shove).length;
check("player pushblock dust leftover normal block still plants 1.0", V.rival.cutHit && V.player.hp===100 && nbBlockN>0 && nbShoveN===0 && V.lastPushblockSfx==="block", `hit=${V.rival.cutHit} blockN=${nbBlockN} shoveN=${nbShoveN} sfx=${V.lastPushblockSfx} hp=${V.player.hp}`);
V.keys.clear();

// Rival shove path still scrape-only (no regress)
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
const pbDustLive216 = captureConnectingRivalDust("slash", true);
check("player pushblock dust leftover rival shove still scrape-only", pbDustLive216.pb && pbDustLive216.blockN===0 && pbDustLive216.shoveN>0, `pb=${pbDustLive216.pb} block=${pbDustLive216.blockN} shove=${pbDustLive216.shoveN}`);

check("cache v=296 after player pushblock dust leftover live", /game.js\?v=350/.test(htmlTxt));



// --- v217 clash dust leftover: lunge plant no longer stacks under choque scrape ---
check("clash dust leftover drops plant stamps when clash owns scrape",
  /function dropPlantUnderClash/.test(srcTxt)
  && /Clash dust leftover/.test(srcTxt)
  && /Drop leftover plant stamps when clash owns the scrape/.test(srcTxt)
  && /dropPlantUnderClash\(\);/.test(sliceFn(codeOnly, "doClash"))
  && /spawnPlantDust\(player, 1\.2\)/.test(sliceFn(codeOnly, "doClash"))
  && /spawnPlantDust\(rival, 1\.2\)/.test(sliceFn(codeOnly, "doClash")));
check("clash dust leftover is draw-only",
  /Draw-only/.test(srcTxt)
  && /\(dropPlantUnderClash\)/.test(srcTxt)
  && /Cull leftover plant stamps \(lunge \/ walk \/ block\)/.test(srcTxt)
  && /Specks keep flying/.test(srcTxt)
  && /p\.speck \|\| p\.shove/.test(sliceFn(codeOnly, "dropPlantUnderClash"))
  && !/dropPlantUnderClash/.test(sliceFn(codeOnly, "destRect"))
  && !/dropPlantUnderClash/.test(sliceFn(codeOnly, "bodyAABB"))
  && !/dropPlantUnderClash/.test(sliceFn(codeOnly, "slashPose"))
  && !/dropPlantUnderClash/.test(sliceFn(codeOnly, "landHit"))
  && !/dropPlantUnderClash/.test(sliceFn(codeOnly, "landBlock")));
check("clash dust leftover keeps shove stamps",
  /Pushblock shove stamps/.test(srcTxt)
  && /p\.speck \|\| p\.shove/.test(sliceFn(codeOnly, "dropPlantUnderClash")));
check("clash dust leftover still clash recovery 200", V.CLASH_RECOVERY===200 && V.HITSTOP_BLOCK===60 && V.CLASH_SPARK_MS===110);
check("clash dust leftover is not a new combat verb",
  (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt)
  && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280
  && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180
  && V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280
  && V.PUSHBLOCK_STAM===25 && V.PUSHBLOCK_PX===240 && V.PUSHBLOCK_FX===1.85);
check("slash frames still 180/140/280 after clash dust leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after clash dust leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after clash dust leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("player pushblock dust leftover still culls block plant after clash dust leftover",
  /dropBlockPlantUnderShove\(f\)/.test(sliceFn(codeOnly, "tryPushblock"))
  && /Player pushblock dust leftover/.test(srcTxt));
check("cache v=296 after clash dust leftover", /game.js\?v=350/.test(htmlTxt));

// Synthetic: leftover lunge plants die when clash owns scrape
bootPlay();
V.plantDust.splice(0);
V.spawnPlantDust(V.player, 1.45);
V.spawnPlantDust(V.rival, 1.15);
const clashLunge0 = (V.plantDust||[]).filter(p=>!p.speck && !p.shove).length;
const clashSpeck0 = (V.plantDust||[]).filter(p=>p.speck).length;
const clashAabb0 = V.bodyAABB(V.player);
V.doClash(640, 360);
const clashStamps = (V.plantDust||[]).filter(p=>!p.speck && !p.shove);
const clashLunges = clashStamps.filter(p=>Math.abs((p.power||0)-1.45)<0.01 || Math.abs((p.power||0)-1.15)<0.01);
const clashScrapes = clashStamps.filter(p=>Math.abs((p.power||0)-1.2)<0.01);
const clashSpeck1 = (V.plantDust||[]).filter(p=>p.speck).length;
check("clash dust leftover synthetic culls lunge under scrape", clashLunge0>=2 && clashLunges.length===0 && clashScrapes.length===2, `lunge0=${clashLunge0} lungeLeft=${clashLunges.length} scrape=${clashScrapes.length} stamps=${clashStamps.length}`);
check("clash dust leftover synthetic scrape still 1.2 both boots", clashScrapes.length===2 && clashScrapes.every(p=>Math.abs((p.power||0)-1.2)<0.01), `scrape=${clashScrapes.map(p=>p.power).join(",")}`);
check("clash dust leftover synthetic specks still fly", clashSpeck0>0 && clashSpeck1>=clashSpeck0, `speck0=${clashSpeck0} speck1=${clashSpeck1}`);
check("clash dust leftover synthetic AABB planted", Math.abs(V.bodyAABB(V.player).y-clashAabb0.y)<0.05, `dy=${(V.bodyAABB(V.player).y-clashAabb0.y).toFixed(2)}`);
check("cache v=296 after clash dust leftover synthetic", /game.js\?v=350/.test(htmlTxt));

// Live: both slash into clash — no stacked lunge under choque
bootPlay();
toClose();
V.plantDust.splice(0);
V.player.phase = "idle";
V.player.phaseT = 0;
V.player.cut = "slash";
V.player.cutHit = false;
V.player.guarding = false;
V.player.stunT = 0;
V.player.falling = false;
V.player.boltPhase = "";
V.player.stamina = 100;
V.rival.phase = "idle";
V.rival.phaseT = 0;
V.rival.cut = "slash";
V.rival.cutHit = false;
V.rival.guarding = false;
V.rival.stunT = 0;
V.rival.falling = false;
V.rival.boltPhase = "";
V.rival.stamina = 100;
V.rival.wantBlock = false;
V.rival.revArmed = -1;
V.rival.pbArmed = -1;
Math.random = () => 0.99;
V.startAttack(V.player, "slash");
V.startAttack(V.rival, "slash");
let clashLive = false;
let clashLiveLunge = -1;
let clashLiveScrape = -1;
let clashLiveSpeck = -1;
const clashLiveAabb0 = V.bodyAABB(V.player);
for (let i = 0; i < 90; i++) {
  V.update(STEP);
  if (V.player.clashRec && V.rival.clashRec) {
    clashLive = true;
    const stamps = (V.plantDust||[]).filter(p=>!p.speck && !p.shove);
    clashLiveLunge = stamps.filter(p=>Math.abs((p.power||0)-1.45)<0.01 || Math.abs((p.power||0)-1.15)<0.01 || Math.abs((p.power||0)-1.05)<0.01).length;
    clashLiveScrape = stamps.filter(p=>Math.abs((p.power||0)-1.2)<0.01).length;
    clashLiveSpeck = (V.plantDust||[]).filter(p=>p.speck).length;
    break;
  }
}
check("clash dust leftover live clash fires", clashLive && V.player.clashRec && V.rival.clashRec, `live=${clashLive} you=${V.player.clashRec} riv=${V.rival.clashRec} ph=${V.player.phase}/${V.rival.phase}`);
check("clash dust leftover live no stacked lunge under scrape", clashLive && clashLiveLunge===0 && clashLiveScrape>=2, `live=${clashLive} lunge=${clashLiveLunge} scrape=${clashLiveScrape}`);
check("clash dust leftover live specks still fly", clashLive && clashLiveSpeck>0, `speck=${clashLiveSpeck}`);
check("clash dust leftover live AABB planted", clashLive && Math.abs(V.bodyAABB(V.player).y-clashLiveAabb0.y)<0.05, `dy=${(V.bodyAABB(V.player).y-clashLiveAabb0.y).toFixed(2)}`);
check("cache v=296 after clash dust leftover live", /game.js\?v=350/.test(htmlTxt));



// ---- menu + remap v219 ----
check("menu has OPCIONES ESCENARIOS unlocked", /OPCIONES/.test(srcTxt) && /ESCENARIOS/.test(srcTxt) && /titlePage === "escenarios"/.test(srcTxt) && /PATIO 2/.test(srcTxt) && /PUENTE/.test(srcTxt) && /ARMERÍA/.test(srcTxt) && /MURALLA/.test(srcTxt) && !/ESCENARIOS", locked: true/.test(srcTxt));
check("binds persist keys", /vispera\.binds\.v1/.test(srcTxt) && /vispera\.pad\.v1/.test(srcTxt) && /vispera\.vol\.v1/.test(srcTxt));
check("action layer chords", /function actionHeld/.test(srcTxt) && /function applyActionEdge/.test(srcTxt) && /act === "slash"/.test(srcTxt));
check("volume stubs default", V.volMusic===80 && V.volSfx===60, "m="+V.volMusic+" s="+V.volSfx);
V.volMusic = 40; V.volSfx = 25;
check("volume persist stub", V.volMusic===40 && V.volSfx===25 && /"music":40/.test(localStorage.getItem("vispera.vol.v1")||""), localStorage.getItem("vispera.vol.v1"));
V.volMusic = 80; V.volSfx = 60;

V.mode = "title"; V.titlePage = "root"; V.titleSel = 2;
V.confirmTitle();
check("OPCIONES opens", V.mode==="title" && V.titlePage==="options", "page="+V.titlePage);
V.titleSel = 0; V.confirmTitle();
check("REMAP TECLADO opens", V.titlePage==="remap_kb", "page="+V.titlePage);
V.handleTitleBack();
check("remap back to options", V.titlePage==="options", "page="+V.titlePage);
V.titleSel = 5; V.confirmTitle();
check("options VOLVER to root", V.titlePage==="root" && V.titleSel===2, "page="+V.titlePage+" sel="+V.titleSel);
V.titleSel = 3; V.confirmTitle();
check("ESCENARIOS opens yard picker", V.titlePage==="escenarios" && V.mode==="title", "page="+V.titlePage);

V.mode = "title"; V.titlePage = "options"; V.titleSel = 1; V.confirmTitle();
check("REMAP PAD sin mando", V.titlePage==="options", "page="+V.titlePage);

V.resetBindsDefaults();
check("defaults slash Space", V.binds.slash[0]==="Space" && V.binds.guard[0]==="KeyS", "slash="+V.binds.slash+" guard="+V.binds.guard);

// Remap Tajo → KeyF still fires slash
V.resetBindsDefaults();
bootPlay(); wait(1700);
freezeYouAI();
freezeRivalAI();
V.keys.clear();
V.setBindCode("slash", "KeyF");
check("remap stored KeyF", V.binds.slash[0]==="KeyF" && V.actionOfCode("KeyF")==="slash" && V.binds.slash.indexOf("KeyJ")<0, "b="+V.binds.slash);
V.player.x=500; V.rival.x=620;
V.player.phase="idle"; V.player.phaseT=0; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0; V.player.falling=false;
V.rival.wantBlock=false; V.rival.guarding=false;
V.attackEdge=false; V.throwEdge=false; V.openLeft=0; V.hitstopLeft=0;
global.dispatchEvent(new KeyboardEvent("keydown", { code: "KeyF" }));
check("remap Tajo→KeyF dispara slash", V.attackEdge===true && V.throwEdge===false, "edge="+V.attackEdge+" throw="+V.throwEdge);
V.update(STEP);
check("remap KeyF starts or buffers tajo", (V.player.phase==="startup" && V.player.cut==="slash") || V.slashBuf===true, "ph="+V.player.phase+" cut="+V.player.cut+" buf="+V.slashBuf);

// J no longer alias
V.keys.clear();
V.attackEdge=false;
V.player.phase="idle"; V.player.cut="slash";
global.dispatchEvent(new KeyboardEvent("keydown", { code: "KeyJ" }));
check("J not alias after Tajo remap", V.attackEdge===false && V.actionOfCode("KeyJ")===null, "edge="+V.attackEdge+" act="+V.actionOfCode("KeyJ"));

// Chord after remap: KeyF + guard = throw (by ACTION)
V.keys.clear();
V.player.phase="idle"; V.player.phaseT=0; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.rival.phase="idle"; V.rival.guarding=true;
V.throwEdge=false; V.attackEdge=false;
V.keys.add("KeyS");
global.dispatchEvent(new KeyboardEvent("keydown", { code: "KeyF" }));
check("remap chord KeyF+Guarda still throw", V.throwEdge===true && V.attackEdge===false, "throw="+V.throwEdge+" attack="+V.attackEdge);
V.resetBindsDefaults();
check("restablecer restores Space+J", V.binds.slash[0]==="Space" && V.binds.slash.indexOf("KeyJ")>=0, "b="+V.binds.slash);

check("cache v=295 menu remap", /game.js\?v=350/.test(htmlTxt));



bootPlay(); wait(200);
V.hitstopLeft = 0;
V.dmgNums.length = 0;
check("spawnDmgNum exists", typeof V.spawnDmgNum === "function");
check("dmgNums array", Array.isArray(V.dmgNums));
check("DMG_NUM_MS rise", V.DMG_NUM_MS === 600 && V.DMG_NUM_RISE === 50, `ms=${V.DMG_NUM_MS} rise=${V.DMG_NUM_RISE}`);
check("dmg num hooks landHit/throw/bolt", /spawnDmgNum\(pt\.x, pt\.y, before - def\.hp, false, def\)/.test(srcTxt) && /spawnDmgNum\(pt\.x, pt\.y, before - def\.hp, true, def\)/.test(srcTxt) && /spawnDmgNum\(bb\.x \+ bb\.w \* 0\.5/.test(srcTxt));
check("dmg num draw+tick", /drawDmgNums\(\)/.test(srcTxt) && /tickDmgNums\(dt\)/.test(srcTxt) && /function spawnDmgNum/.test(srcTxt));
{
  const before = V.dmgNums.length;
  let ok = true;
  try {
    V.spawnDmgNum(640, 360, 10, false);
    V.spawnDmgNum(640, 360, 2, true);
  } catch (e) {
    ok = false;
  }
  check("spawnDmgNum no crash", ok && V.dmgNums.length === before + 2, `n=${V.dmgNums.length}`);
  const hit = V.dmgNums[V.dmgNums.length - 2];
  const chip = V.dmgNums[V.dmgNums.length - 1];
  check("hit vs chip flags", hit && chip && hit.chip === false && chip.chip === true && hit.amt === 10 && chip.amt === 2, `hit=${hit && hit.chip} chip=${chip && chip.chip}`);
  // advance past life (hitstop gate: drain then wait)
  V.hitstopLeft = 0;
  for(let i=0;i<Math.ceil((V.DMG_NUM_MS+50)/STEP);i++){ V.hitstopLeft=0; V.update(STEP); }
  check("dmg nums expire", V.dmgNums.length === 0, `n=${V.dmgNums.length}`);
  V.spawnDmgNum(100, 100, 0, false);
  check("zero amt skipped", V.dmgNums.length === 0, `n=${V.dmgNums.length}`);
}

// --- v226 damage number leftover: −N rides live hurt, not spawn world xy ---
check("damage number leftover rides live hurt",
  /Damage number leftover/.test(srcTxt)
  && /function syncDmgNums/.test(srcTxt)
  && /syncDmgNums\(\)/.test(srcTxt)
  && /Ride live hurt/.test(srcTxt)
  && /hitWoundAnchor/.test(sliceFn(codeOnly, "syncDmgNums"))
  && /p\.x = a\.x \+ \(p\.plantDX \|\| 0\)/.test(sliceFn(codeOnly, "syncDmgNums"))
  && /p\.y = a\.y \+ \(p\.plantDY \|\| 0\)/.test(sliceFn(codeOnly, "syncDmgNums"))
  && /homeYou/.test(sliceFn(codeOnly, "spawnDmgNum"))
  && /plantDX/.test(sliceFn(codeOnly, "spawnDmgNum")));
check("damage number leftover is draw-only",
  /Draw-only\. Damage number leftover/.test(srcTxt)
  && /destRect\/AABB planted/.test(srcTxt)
  && !/syncDmgNums/.test(sliceFn(codeOnly, "destRect"))
  && !/syncDmgNums/.test(sliceFn(codeOnly, "bodyAABB"))
  && !/syncDmgNums/.test(sliceFn(codeOnly, "landHit"))
  && !/syncDmgNums/.test(sliceFn(codeOnly, "landThrow"))
  && !/syncDmgNums/.test(sliceFn(codeOnly, "landBoltHit"))
  && !/syncDmgNums/.test(sliceFn(codeOnly, "landBoltBlock")));
check("damage number leftover hooks pass def",
  /spawnDmgNum\(pt\.x, pt\.y, before - def\.hp, false, def\)/.test(srcTxt)
  && /spawnDmgNum\(pt\.x, pt\.y, before - def\.hp, true, def\)/.test(srcTxt)
  && /spawnDmgNum\(bb\.x \+ bb\.w \* 0\.5/.test(srcTxt)
  && /false, def\)/.test(srcTxt));
check("damage number leftover still 600/50", V.DMG_NUM_MS === 600 && V.DMG_NUM_RISE === 50, `ms=${V.DMG_NUM_MS} rise=${V.DMG_NUM_RISE}`);
check("damage number leftover is not a new combat verb",
  V.SLASH_DMG===10 && V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.KNOCK_PX===80 && V.KNOCK_MS===180 && V.HIT_SPARK_MS===100);
check("slash frames still 180/140/280 after damage number leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after damage number leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after damage number leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("cache v=296 after damage number leftover", /game.js\?v=350/.test(htmlTxt));

// Live: slash connect −N rides hurt through knock (not spawn world xy)
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.openLeft = 0;
V.hitstopLeft = 0;
V.player.phase = "idle"; V.player.phaseT = 0; V.player.stunT = 0; V.player.guarding = false; V.player.boltPhase = ""; V.player.falling = false;
V.rival.phase = "idle"; V.rival.phaseT = 0; V.rival.stunT = 0; V.rival.guarding = false; V.rival.boltPhase = ""; V.rival.falling = false;
V.rival.wantBlock = false; V.rival.revArmed = -1; V.rival.pbArmed = -1;
V.player.x = 500; V.rival.x = 620;
V.keepApart();
V.dmgNums.length = 0;
V.plantDust.splice(0);
const dmgAabb0 = V.bodyAABB(V.rival);
V.startAttack(V.player, "slash");
let dmgHit = false;
let dmgSpawnX = 0, dmgSpawnY = 0, dmgPlantDX = 0, dmgPlantDY = 0;
let dmgHome = null;
for (let i = 0; i < 90; i++) {
  freezeRivalAI();
  V.update(STEP);
  if (V.rival.stunT > 0 && V.dmgNums.length > 0) {
    dmgHit = true;
    const n = V.dmgNums[0];
    dmgSpawnX = n.x;
    dmgSpawnY = n.y;
    dmgPlantDX = n.plantDX;
    dmgPlantDY = n.plantDY;
    dmgHome = n.homeYou;
    break;
  }
}
check("damage number leftover live hit arms", dmgHit && V.rival.stunT > 0 && V.dmgNums.length > 0 && dmgHome === false && V.rival.hp === 90, `hit=${dmgHit} n=${V.dmgNums.length} home=${dmgHome} hp=${V.rival.hp} stun=${V.rival.stunT}`);
const dmgNum0 = V.dmgNums[0];
const a0 = V.hitWoundAnchor(V.rival);
check("damage number leftover live plants on wound", dmgNum0 && Math.abs(dmgNum0.x - (a0.x + (dmgNum0.plantDX||0))) < 2 && Math.abs(dmgNum0.y - (a0.y + (dmgNum0.plantDY||0))) < 2, `x=${dmgNum0&&dmgNum0.x.toFixed(1)} a=${a0.x.toFixed(1)} dx=${dmgNum0&&dmgNum0.plantDX}`);
// Drain hitstop then knock
for (let i = 0; i < 30; i++) {
  freezeRivalAI();
  if (V.hitstopLeft > 0) V.hitstopLeft = 0;
  V.update(STEP);
}
const dmgNum1 = V.dmgNums[0];
const a1 = V.hitWoundAnchor(V.rival);
const pivMoved = Math.abs(a1.x - a0.x) > 20;
check("damage number leftover live rides wound through knock", dmgNum1 && pivMoved && Math.abs(dmgNum1.x - (a1.x + (dmgNum1.plantDX||0))) < 2 && Math.abs(dmgNum1.x - dmgSpawnX) > 15, `x=${dmgNum1&&dmgNum1.x.toFixed(1)} spawn=${dmgSpawnX.toFixed(1)} a1=${a1.x.toFixed(1)} a0=${a0.x.toFixed(1)} moved=${pivMoved}`);
check("damage number leftover live AABB planted", Math.abs(V.bodyAABB(V.rival).y - dmgAabb0.y) < 0.05, `dy=${(V.bodyAABB(V.rival).y - dmgAabb0.y).toFixed(2)}`);
check("damage number leftover live rise still fades", dmgNum1 && dmgNum1.t > 0 && dmgNum1.t < V.DMG_NUM_MS && dmgNum1.amt === 10 && dmgNum1.chip === false, `t=${dmgNum1&&dmgNum1.t} amt=${dmgNum1&&dmgNum1.amt}`);
check("cache v=296 after damage number leftover live", /game.js\?v=350/.test(htmlTxt));


// --- v224 knife tip plant leftover: throw_knife tip on blade, not helmet / mid-blade ---
check("knife tip plant leftover seats on blade",
  /Knife tip plant leftover/.test(srcTxt)
  && /tip markers used to sit on the helmet/.test(srcTxt)
  && /mid-blade \(rival 70 \/ flip 815\)/.test(srcTxt)
  && /tipX: 268/.test(srcTxt) && /tipY: 109/.test(srcTxt)
  && /tipX: 6/.test(srcTxt) && /tipY: 384/.test(srcTxt)
  && /tipX: 873/.test(srcTxt) && /tipY: 391/.test(srcTxt)
  && !/tipX: 359, tipY: 198/.test(srcTxt)
  && !/tipX: 70,/.test(srcTxt) && !/tipX: 815/.test(srcTxt));
check("knife tip plant leftover is draw-only",
  /Draw-only \(POSE\.throwKnife tipX\/tipY\)/.test(srcTxt)
  && /castPlantXY \//.test(srcTxt)
  && /bladeTipX \/ castPlantXY still ride that tip/.test(srcTxt)
  && /destRect\/AABB planted/.test(srcTxt)
  && !/tipX: 268/.test(sliceFn(codeOnly, "destRect"))
  && !/tipX: 268/.test(sliceFn(codeOnly, "bodyAABB"))
  && !/tipX: 268/.test(sliceFn(codeOnly, "slashPose"))
  && !/tipX: 268/.test(sliceFn(codeOnly, "landHit")));
check("knife tip plant leftover still throwKnife family",
  /poseFamily\(f\)\.throwKnife/.test(sliceFn(codeOnly, "castPlantXY"))
  && /f\.boltPhase === "startup" && ready\(throwK\)/.test(sliceFn(codeOnly, "poseBitmap"))
  && /fam\.throwKnife/.test(sliceFn(codeOnly, "poseSheet")));
check("knife tip plant leftover special-cancel still slash tip",
  /Special-cancel still slash tip/.test(srcTxt)
  && /holdingCutBolt\(f\)/.test(sliceFn(codeOnly, "castPlantXY")));
check("knife tip plant leftover still 220 cast life", V.BOLT_CAST_FX_MS===220 && V.BOLT_STARTUP===200, `cast=${V.BOLT_CAST_FX_MS} start=${V.BOLT_STARTUP}`);
check("knife tip plant leftover is not a new combat verb",
  V.SLASH_DMG===10 && V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.KNOCK_PX===80 && V.BOLT_SUPER_STOP===60 && V.BOLT_SUPER_FX===1.55);
check("slash frames still 180/140/280 after knife tip plant leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after knife tip plant leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after knife tip plant leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("cache v=296 after knife tip plant leftover", /game.js\?v=350/.test(htmlTxt));

// Live: idle K puff + tip sit on raised blade (not helmet / sheet-edge)
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.openLeft = 0;
V.hitstopLeft = 0;
V.player.phase = "idle"; V.player.phaseT = 0; V.player.stunT = 0; V.player.guarding = false; V.player.boltPhase = ""; V.player.falling = false;
V.player.boltHoldCut = false; V.player.stamina = 100; V.player.stamRegenT = 99999;
V.rival.phase = "idle"; V.rival.wantBlock = false; V.rival.revArmed = -1;
V.player.x = 500; V.rival.x = 1100;
V.keepApart();
const ktAabb0 = V.bodyAABB(V.player);
const ktDest0 = V.destRect(V.player);
const ktSheet = V.poseSheet(V.player);
V.boltEdge = true;
V.update(STEP);
const ktTip = V.poseSheet(V.player);
const ktPlant = V.castPlantXY(V.player);
const ktBlade = V.bladeTipX(V.player);
const ktEdge = V.player.facing > 0 ? ktDest0.dx + ktDest0.dw - 8 : ktDest0.dx + 8;
const ktHelmetX = ktDest0.dx + 359 * ((ktBlade - ktDest0.dx) / (ktTip.tipX || 268));
check("knife tip plant leftover live you arms throw_knife", V.player.boltPhase==="startup" && /throw_knife/.test((V.poseBitmap(V.player)&&V.poseBitmap(V.player)._src)||"") && ktTip.tipX===268 && ktTip.tipY===109, `bph=${V.player.boltPhase} tip=${ktTip.tipX}/${ktTip.tipY} src=${(V.poseBitmap(V.player)&&V.poseBitmap(V.player)._src)||""}`);
const ktKnifeX = ktDest0.dx + 268 * (ktDest0.dw / 1186);
const ktKnifeY = ktDest0.dy + 109 * (ktDest0.dw / 1186);
// Tip under telegraphFade: bladeTip eases under tele; puff/castPlantXY still on throwKnife tip.
check("knife tip plant leftover live you puff on blade tip", V.brasaFxKind==="cast" && Math.abs(V.brasaX-ktPlant.x)<4 && Math.abs(V.brasaY-ktPlant.y)<4 && Math.abs(ktPlant.x-ktKnifeX)<4 && Math.abs(ktPlant.y-ktKnifeY)<4, `kind=${V.brasaFxKind} bx=${V.brasaX.toFixed(1)} plant=${ktPlant.x.toFixed(1)},${ktPlant.y.toFixed(1)} blade=${ktBlade.toFixed(1)} knife=${ktKnifeX.toFixed(1)}`);
check("knife tip plant leftover live you not helmet plant", Math.abs(ktPlant.x-ktHelmetX)>20 && Math.abs(ktPlant.x-ktEdge)>80, `plant=${ktPlant.x.toFixed(1)} helmet=${ktHelmetX.toFixed(1)} edge=${ktEdge.toFixed(1)}`);
check("knife tip plant leftover live you AABB planted", Math.abs(V.bodyAABB(V.player).y-ktAabb0.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-ktAabb0.h)<0.05, `dy=${(V.bodyAABB(V.player).y-ktAabb0.y).toFixed(2)}`);
check("knife tip plant leftover live you destRect planted", Math.abs(V.destRect(V.player).dy-ktDest0.dy)<2.5 && Math.abs(V.destRect(V.player).rot)<0.001, `ddy=${(V.destRect(V.player).dy-ktDest0.dy).toFixed(2)} rot=${V.destRect(V.player).rot.toFixed(3)}`);

// Rival facing left: tip on forward point (was mid-blade 70)
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.openLeft = 0;
V.hitstopLeft = 0;
V.player.x = 200; V.rival.x = 700;
V.rival.facing = -1;
V.rival.phase = "idle"; V.rival.phaseT = 0; V.rival.stunT = 0; V.rival.guarding = false; V.rival.boltPhase = ""; V.rival.falling = false;
V.rival.boltHoldCut = false; V.rival.stamina = 100; V.rival.stamRegenT = 99999; V.rival.meter = 0;
V.rival.wantBlock = false; V.rival.telegraph = false;
V.player.phase = "idle"; V.player.guarding = false;
V.keepApart();
const krAabb0 = V.bodyAABB(V.rival);
const krDest0 = V.destRect(V.rival);
const okRiv = V.startBolt(V.rival, false);
const krTip = V.poseSheet(V.rival);
const krPlant = V.castPlantXY(V.rival);
const krBlade = V.bladeTipX(V.rival);
const krOldMid = krDest0.dx + 70 * ((krBlade - krDest0.dx) / (krTip.tipX || 6));
check("knife tip plant leftover live rival arms", okRiv && V.rival.boltPhase==="startup" && krTip.tipX===6 && krTip.tipY===384 && /throw_knife/.test(sheetSrc(V.rival)), `ok=${okRiv} bph=${V.rival.boltPhase} tip=${krTip.tipX}/${krTip.tipY} src=${sheetSrc(V.rival)}`);
const krKnifeX = krDest0.dx + 6 * (krDest0.dw / 877);
// Tip under telegraphFade: bladeTip eases under tele; puff still on throwKnife tip.
check("knife tip plant leftover live rival puff on forward tip", V.brasaFxKind==="cast" && Math.abs(V.brasaX-krPlant.x)<4 && Math.abs(krPlant.x-krKnifeX)<4 && Math.abs(krPlant.x-krOldMid)>15, `kind=${V.brasaFxKind} plant=${krPlant.x.toFixed(1)} blade=${krBlade.toFixed(1)} knife=${krKnifeX.toFixed(1)} oldMid=${krOldMid.toFixed(1)}`);
check("knife tip plant leftover live rival AABB planted", Math.abs(V.bodyAABB(V.rival).y-krAabb0.y)<0.05, `dy=${(V.bodyAABB(V.rival).y-krAabb0.y).toFixed(2)}`);
check("cache v=296 after knife tip plant leftover live", /game.js\?v=350/.test(htmlTxt));


// --- v225 dart tip plant leftover: idle/walk K dart births on tipY, not chest ---
check("dart tip plant leftover seats birth on castPlantXY",
  /Dart tip plant leftover/.test(srcTxt)
  && /birth at bladeBox chest Y/.test(srcTxt)
  && /Seat birth on live castPlantXY/.test(srcTxt)
  && /castPlantXY\(u\)/.test(sliceFn(codeOnly, "spawnBolt"))
  && /plant\.y - h \* 0\.5/.test(sliceFn(codeOnly, "spawnBolt"))
  && /plant\.x/.test(sliceFn(codeOnly, "spawnBolt"))
  && !/bladeBox\(u\)/.test(sliceFn(codeOnly, "spawnBolt"))
  && !/hb\.y \+ hb\.h \* 0\.45/.test(sliceFn(codeOnly, "spawnBolt")));
check("dart tip plant leftover is tip-plant juice",
  /Seat birth on live castPlantXY/.test(srcTxt)
  && /Special-cancel still slash tip/.test(srcTxt)
  && /destRect\/AABB planted/.test(srcTxt)
  && /holdingCutBolt\(f\)/.test(sliceFn(codeOnly, "castPlantXY"))
  && !/castPlantXY/.test(sliceFn(codeOnly, "destRect"))
  && !/castPlantXY/.test(sliceFn(codeOnly, "bodyAABB"))
  && !/castPlantXY/.test(sliceFn(codeOnly, "landHit"))
  && !/castPlantXY/.test(sliceFn(codeOnly, "slashPose")));
check("dart tip plant leftover special-cancel still slash\/chest fallback",
  /Special-cancel still slash tip/.test(srcTxt)
  && /holdingCutBolt\(f\)/.test(sliceFn(codeOnly, "castPlantXY"))
  && /bladeTipX\(f\)/.test(sliceFn(codeOnly, "castPlantXY")));
check("dart tip plant leftover still 200\/280 K", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_CAST_FX_MS===220 && V.BOLT_W===58 && V.BOLT_H===13, `start=${V.BOLT_STARTUP} rec=${V.BOLT_RECOVERY} cast=${V.BOLT_CAST_FX_MS} wh=${V.BOLT_W}x${V.BOLT_H}`);
check("dart tip plant leftover is not a new combat verb",
  V.SLASH_DMG===10 && V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.KNOCK_PX===80 && V.BOLT_SUPER_STOP===60 && V.BOLT_SUPER_FX===1.55 && V.BOLT_SPEED===880);
check("slash frames still 180/140/280 after dart tip plant leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after dart tip plant leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after dart tip plant leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("cache v=296 after dart tip plant leftover", /game.js\?v=350/.test(htmlTxt));

// Live: idle K dart births on knife tip (not chest)
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.openLeft = 0;
V.hitstopLeft = 0;
V.player.phase = "idle"; V.player.phaseT = 0; V.player.stunT = 0; V.player.guarding = false; V.player.boltPhase = ""; V.player.falling = false;
V.player.boltHoldCut = false; V.player.stamina = 100; V.player.stamRegenT = 99999; V.player.meter = 0;
V.rival.phase = "idle"; V.rival.wantBlock = false; V.rival.revArmed = -1;
V.player.x = 400; V.rival.x = 1100;
V.keepApart();
const dtAabb0 = V.bodyAABB(V.player);
const dtDest0 = V.destRect(V.player);
V.boltEdge = true;
V.update(STEP);
check("dart tip plant leftover live you arms throw_knife", V.player.boltPhase==="startup" && /throw_knife/.test((V.poseBitmap(V.player)&&V.poseBitmap(V.player)._src)||""), `bph=${V.player.boltPhase} src=${(V.poseBitmap(V.player)&&V.poseBitmap(V.player)._src)||""}`);
const dtPlant0 = V.castPlantXY(V.player);
const dtChest0 = (() => { const hb = V.bladeBox(V.player); return hb.y + hb.h * 0.45; })();
check("dart tip plant leftover live you tip not chest", Math.abs(dtPlant0.y - dtChest0) > 25, `plantY=${dtPlant0.y.toFixed(1)} chestY=${dtChest0.toFixed(1)}`);
let dtBolt = null;
let dtPlantAtBirth = null;
for (let i = 0; i < 80; i++) {
  freezeRivalAI();
  if (V.hitstopLeft > 0) V.hitstopLeft = 0;
  const before = !!V.bolt;
  // capture plant while still startup tip
  if (V.player.boltPhase === "startup") dtPlantAtBirth = V.castPlantXY(V.player);
  V.update(STEP);
  if (!before && V.bolt) { dtBolt = { x: V.bolt.x, y: V.bolt.y, w: V.bolt.w, h: V.bolt.h, facing: V.bolt.facing }; break; }
}
const dtCy = dtBolt ? dtBolt.y + dtBolt.h * 0.5 : 0;
const dtTravel = V.BOLT_SPEED * (STEP / 1000);
const dtExpectX = (dtBolt && dtPlantAtBirth) ? (dtBolt.facing > 0 ? dtPlantAtBirth.x + dtTravel : dtPlantAtBirth.x - dtBolt.w - dtTravel) : 0;
check("dart tip plant leftover live you births", !!dtBolt && !!dtPlantAtBirth, `bolt=${!!dtBolt} plant=${!!dtPlantAtBirth}`);
check("dart tip plant leftover live you on tip Y", dtBolt && Math.abs(dtCy - dtPlantAtBirth.y) < 4, `cy=${dtCy.toFixed(1)} plantY=${dtPlantAtBirth && dtPlantAtBirth.y.toFixed(1)} chest=${dtChest0.toFixed(1)}`);
check("dart tip plant leftover live you on tip X", dtBolt && Math.abs(dtExpectX - dtBolt.x) < 4, `x=${dtBolt && dtBolt.x.toFixed(1)} expect=${dtExpectX && dtExpectX.toFixed(1)} travel=${dtTravel.toFixed(1)}`);
check("dart tip plant leftover live you not chest Y", dtBolt && Math.abs(dtCy - dtChest0) > 20, `cy=${dtCy.toFixed(1)} chest=${dtChest0.toFixed(1)}`);
check("dart tip plant leftover live you AABB planted", Math.abs(V.bodyAABB(V.player).y - dtAabb0.y) < 0.05, `dy=${(V.bodyAABB(V.player).y - dtAabb0.y).toFixed(2)}`);
check("dart tip plant leftover live you destRect planted", Math.abs(V.destRect(V.player).dy - dtDest0.dy) < 2.5, `ddy=${(V.destRect(V.player).dy - dtDest0.dy).toFixed(2)}`);

// Rival facing left: tipY below chest — dart must not birth high
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.openLeft = 0;
V.hitstopLeft = 0;
V.player.x = 200; V.rival.x = 900;
V.rival.facing = -1;
V.rival.phase = "idle"; V.rival.phaseT = 0; V.rival.stunT = 0; V.rival.guarding = false; V.rival.boltPhase = ""; V.rival.falling = false;
V.rival.boltHoldCut = false; V.rival.stamina = 100; V.rival.stamRegenT = 99999; V.rival.meter = 0;
V.rival.wantBlock = false; V.rival.telegraph = false;
V.player.phase = "idle"; V.player.guarding = false;
V.keepApart();
const dtrAabb0 = V.bodyAABB(V.rival);
const okDtr = V.startBolt(V.rival, false);
const dtrPlant0 = V.castPlantXY(V.rival);
const dtrChest0 = (() => { const hb = V.bladeBox(V.rival); return hb.y + hb.h * 0.45; })();
check("dart tip plant leftover live rival arms", okDtr && V.rival.boltPhase==="startup" && /throw_knife/.test(sheetSrc(V.rival)), `ok=${okDtr} bph=${V.rival.boltPhase} src=${sheetSrc(V.rival)}`);
check("dart tip plant leftover live rival tip not chest", Math.abs(dtrPlant0.y - dtrChest0) > 25, `plantY=${dtrPlant0.y.toFixed(1)} chestY=${dtrChest0.toFixed(1)}`);
let dtrBolt = null;
let dtrPlantAtBirth = null;
for (let i = 0; i < 80; i++) {
  freezeYouAI();
  // Keep rival K plant — freezeRivalAI clears boltPhase.
  V.rival.wantBlock = false; V.rival.revArmed = -1; V.rival.boltCd = 99999;
  if (V.hitstopLeft > 0) V.hitstopLeft = 0;
  const before = !!V.bolt;
  if (V.rival.boltPhase === "startup") dtrPlantAtBirth = V.castPlantXY(V.rival);
  V.update(STEP);
  if (!before && V.bolt) { dtrBolt = { x: V.bolt.x, y: V.bolt.y, w: V.bolt.w, h: V.bolt.h, facing: V.bolt.facing }; break; }
}
const dtrCy = dtrBolt ? dtrBolt.y + dtrBolt.h * 0.5 : 0;
check("dart tip plant leftover live rival births", !!dtrBolt && !!dtrPlantAtBirth, `bolt=${!!dtrBolt} plant=${!!dtrPlantAtBirth}`);
check("dart tip plant leftover live rival on tip Y", dtrBolt && Math.abs(dtrCy - dtrPlantAtBirth.y) < 4, `cy=${dtrCy.toFixed(1)} plantY=${dtrPlantAtBirth && dtrPlantAtBirth.y.toFixed(1)} chest=${dtrChest0.toFixed(1)}`);
check("dart tip plant leftover live rival not chest Y", dtrBolt && Math.abs(dtrCy - dtrChest0) > 20, `cy=${dtrCy.toFixed(1)} chest=${dtrChest0.toFixed(1)}`);
check("dart tip plant leftover live rival AABB planted", Math.abs(V.bodyAABB(V.rival).y - dtrAabb0.y) < 0.05, `dy=${(V.bodyAABB(V.rival).y - dtrAabb0.y).toFixed(2)}`);
check("cache v=296 after dart tip plant leftover live", /game.js\?v=350/.test(htmlTxt));


check("slash tip plant leftover seats tipY on opaque tip", /Slash tip plant leftover/.test(srcTxt) && /function bladeTipY/.test(srcTxt) && /bladeTipY\(f\)/.test(srcTxt) && /tipX: 991, tipY: 270/.test(srcTxt) && /tipX: 0, tipY: 338/.test(srcTxt) && /tipX: 884, tipY: 338/.test(srcTxt));
check("slash tip plant leftover is draw-only", /Draw-only/.test(srcTxt) && /bladeTipY\(f\)/.test(srcTxt) && /tipX \/ bladeBox \/ reach unchanged/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && !/bladeTipY/.test(sliceFn(codeOnly, "destRect")) && !/bladeTipY/.test(sliceFn(codeOnly, "bodyAABB")) && !/bladeTipY/.test(sliceFn(codeOnly, "bladeBox")) && !/bladeTipY/.test(sliceFn(codeOnly, "slashPose")) && !/bladeTipY/.test(sliceFn(codeOnly, "landHit")));
check("slash tip plant leftover wires juice Y", /y: bladeTipY\(f\)/.test(sliceFn(codeOnly, "castPlantXY")) && /clashTipDY = y - bladeTipY\(home\)/.test(srcTxt) && /clashY = bladeTipY\(home\)/.test(srcTxt) && /brasaWoundDY = y - bladeTipY\(home\)/.test(srcTxt) && /brasaY = bladeTipY\(home\)/.test(srcTxt));
check("slash tip plant leftover tipX unchanged", /tipX: 991, tipY: 270/.test(srcTxt) && /tipX: 0, tipY: 338/.test(srcTxt) && /tipX: 884, tipY: 338/.test(srcTxt) && /tipX: 268, tipY: 109/.test(srcTxt) && /tipX: 6, tipY: 384/.test(srcTxt));
check("slash tip plant leftover throwKnife markers unchanged", /tipX: 268, tipY: 109/.test(srcTxt) && /tipX: 6, tipY: 384/.test(srcTxt) && /tipX: 873, tipY: 391/.test(srcTxt));
check("slash tip plant leftover is not a new combat verb", V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80 && V.BOLT_SUPER_STOP===60 && V.BOLT_CAST_FX_MS===220);
check("slash frames still 180/140/280 after slash tip plant leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after slash tip plant leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after slash tip plant leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after slash tip plant leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after slash tip plant leftover", /game.js\?v=350/.test(htmlTxt));

bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.sheatheT = 0;
V.player.walkSettleT = 0;
V.player.guardPoseK = 0;
V.player.guarding = false;
V.player.stunT = 0;
V.player.gait = 0;
V.player.feintT = 0;
V.player.boltPhase = "";
V.player.x = V.rival.x - 400;
V.keepApart();
V.startAttack(V.player, "slash");
check("slash tip plant leftover live you arms startup", V.player.phase==="startup" && V.player.cut==="slash", `ph=${V.player.phase} cut=${V.player.cut}`);
for(let i=0;i<30;i++){
  freezeRivalAI();
  V.update(STEP);
  if(/slash/.test((V.poseBitmap(V.player)&&V.poseBitmap(V.player)._src)||"")) break;
}
const stYou = V.poseSheet(V.player);
const stYouR = V.destRect(V.player);
const stYouSc = stYouR.dw / stYou.w;
const stYouMeas = stYouR.dy + 270 * stYouSc;
const stYouAabb0 = V.bodyAABB(V.player);
const stYouDest0 = V.destRect(V.player);
const stYouSrc = (V.poseBitmap(V.player)&&V.poseBitmap(V.player)._src)||"";
const stYouOnSlash = /slash/.test(stYouSrc) && stYou.tipX===991 && stYou.tipY===270;
check("slash tip plant leftover live you arms slash tipY", stYouOnSlash, `ph=${V.player.phase} tip=${stYou.tipX}/${stYou.tipY} src=${stYouSrc}`);
check("slash tip plant leftover live you bladeTipY on opaque tip", stYouOnSlash && Math.abs(V.bladeTipY(V.player)-stYouMeas)<2, `tipY=${V.bladeTipY(V.player).toFixed(1)} meas=${stYouMeas.toFixed(1)} midFallback=${(V.bladeBox(V.player).y+V.bladeBox(V.player).h*0.45).toFixed(1)}`);
check("slash tip plant leftover live you not chest mid", stYouOnSlash && Math.abs(V.bladeTipY(V.player)-(V.bladeBox(V.player).y+V.bladeBox(V.player).h*0.45))>10, `tipY=${V.bladeTipY(V.player).toFixed(1)} mid=${(V.bladeBox(V.player).y+V.bladeBox(V.player).h*0.45).toFixed(1)}`);
check("slash tip plant leftover live you tipX unchanged", stYouOnSlash && Math.abs(V.bladeTipX(V.player)-(stYouR.dx+991*stYouSc))<1, `tipX=${V.bladeTipX(V.player).toFixed(1)} expect=${(stYouR.dx+991*stYouSc).toFixed(1)}`);
check("slash tip plant leftover live you AABB planted", Math.abs(V.bodyAABB(V.player).y-stYouAabb0.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-stYouAabb0.h)<0.05, `dy=${(V.bodyAABB(V.player).y-stYouAabb0.y).toFixed(2)}`);
check("slash tip plant leftover live you destRect planted", Math.abs(V.destRect(V.player).dy-stYouDest0.dy)<2.5 && Math.abs(V.destRect(V.player).rot)<0.001, `ddy=${(V.destRect(V.player).dy-stYouDest0.dy).toFixed(2)} rot=${V.destRect(V.player).rot.toFixed(3)}`);

bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.startAttack(V.rival, "slash");
for(let i=0;i<80 && V.rival.phase!=="active";i++) V.update(STEP);
const stRiv = V.poseSheet(V.rival);
const stRivR = V.destRect(V.rival);
const stRivSc = stRivR.dw / stRiv.w;
const stRivMeas = stRivR.dy + 338 * stRivSc;
const stRivAabb0 = V.bodyAABB(V.rival);
check("slash tip plant leftover live rival arms slash tipY", V.rival.phase==="active" && stRiv.tipX===0 && stRiv.tipY===338 && /slash/.test(sheetSrc(V.rival)), `ph=${V.rival.phase} tip=${stRiv.tipX}/${stRiv.tipY} src=${sheetSrc(V.rival)}`);
check("slash tip plant leftover live rival bladeTipY on opaque tip", Math.abs(V.bladeTipY(V.rival)-stRivMeas)<2, `tipY=${V.bladeTipY(V.rival).toFixed(1)} meas=${stRivMeas.toFixed(1)} mid=${(V.bladeBox(V.rival).y+V.bladeBox(V.rival).h*0.45).toFixed(1)}`);
check("slash tip plant leftover live rival not chest mid", Math.abs(V.bladeTipY(V.rival)-(V.bladeBox(V.rival).y+V.bladeBox(V.rival).h*0.45))>20, `tipY=${V.bladeTipY(V.rival).toFixed(1)} mid=${(V.bladeBox(V.rival).y+V.bladeBox(V.rival).h*0.45).toFixed(1)}`);
check("slash tip plant leftover live rival AABB planted", Math.abs(V.bodyAABB(V.rival).y-stRivAabb0.y)<0.05, `dy=${(V.bodyAABB(V.rival).y-stRivAabb0.y).toFixed(2)}`);

bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.player.x = V.rival.x - 160;
V.keepApart();
V.rival.guarding = false;
V.rival.wantBlock = false;
V.rival.guardChoice = -1;
V.attackEdge = true;
let stHit=false;
for(let i=0;i<80;i++){
  freezeRivalAI();
  V.update(STEP);
  if(V.rival.hp<100 || V.hitstopLeft>0){ stHit=true; break; }
}
for(let i=0;i<40;i++){ if(V.hitstopLeft<=0) break; V.update(STEP); }
stHit = stHit && V.player.phase==="recovery" && V.player.cutHit;
let stWin=false;
for(let i=0;i<40;i++){
  freezeRivalAI();
  V.player.stamRegenT = 99999;
  V.update(STEP);
  const left = (V.player.phase==="recovery" && V.player.cut==="slash") ? (V.RECOVERY - V.player.phaseT) : 999;
  if(left<=V.BOLT_CANCEL_MS){ stWin=true; break; }
  if(V.player.phase==="idle") break;
}
V.player.stamRegenT = 99999;
V.boltEdge = true;
V.update(STEP);
const stPlant = V.castPlantXY(V.player);
const stEdge = V.bladeTipX(V.player);
const stTipY = V.bladeTipY(V.player);
const stChestMid = V.bladeBox(V.player).y + V.bladeBox(V.player).h * 0.45;
check("slash tip plant leftover special-cancel still slash tip X", stHit && stWin && V.player.boltPhase==="startup" && V.player.boltHoldCut===true && Math.abs(stPlant.x-stEdge)<8, `hit=${stHit} win=${stWin} bph=${V.player.boltPhase} hold=${V.player.boltHoldCut} plantX=${stPlant.x.toFixed(1)} tipX=${stEdge.toFixed(1)}`);
// v346: under holdCutFade tipY may sit near chest mid (leftover sheathe); puff still tracks tipY.
check("slash tip plant leftover special-cancel puff on tipY", stHit && stWin && V.player.boltPhase==="startup" && V.player.boltHoldCut===true && V.brasaFxKind==="cast" && Math.abs(V.brasaY-stTipY)<4 && (V.holdCutFade(V.player)>0.02 || Math.abs(V.brasaY-stChestMid)>8), `kind=${V.brasaFxKind} by=${V.brasaY.toFixed(1)} tipY=${stTipY.toFixed(1)} mid=${stChestMid.toFixed(1)} hf=${V.holdCutFade(V.player)}`);
check("cache v=296 after slash tip plant leftover live", /game.js\?v=350/.test(htmlTxt));




// --- v227 opening settle leftover: settleDip eases in, no stamp dump ---
check("opening settle leftover eases settleDip in", /Opening settle leftover/.test(srcTxt) && /function settleDip/.test(srcTxt) && /settleMax/.test(sliceFn(codeOnly, "settleDip")) && /Ease in over the first/.test(srcTxt) && /IN = 0\.18/.test(sliceFn(codeOnly, "settleDip")));
check("opening settle leftover no longer dumps full 4 on arm", /full 4 \* u\^2 used to plant/.test(srcTxt) && /rise = t \* t \* \(3 - 2 \* t\)/.test(sliceFn(codeOnly, "settleDip")) && /return 4 \* u \* u \* rise/.test(sliceFn(codeOnly, "settleDip")));
check("opening settle leftover is destRect-only", /destRect-only \(settleDip\)/.test(srcTxt) && /settleDip\(\)/.test(sliceFn(codeOnly, "destRect")) && !/settleDip/.test(sliceFn(codeOnly, "bodyAABB")) && !/settleDip/.test(sliceFn(codeOnly, "slashPose")) && !/settleDip/.test(sliceFn(codeOnly, "landHit")) && !/settleDip/.test(sliceFn(codeOnly, "poseBitmap")));
check("opening settle leftover stampNow arms settleMax", /settleT = SETTLE_MS/.test(sliceFn(codeOnly, "stampNow")) && /settleMax = SETTLE_MS/.test(sliceFn(codeOnly, "stampNow")) && /spawnPlantDust\(player/.test(sliceFn(codeOnly, "stampNow")));
check("opening settle leftover guard-break keeps GUARD_BREAK_SETTLE clock", /GUARD_BREAK_SETTLE/.test(sliceFn(codeOnly, "tripGuardBreak")) && /settleMax = GUARD_BREAK_SETTLE/.test(sliceFn(codeOnly, "tripGuardBreak")) && V.GUARD_BREAK_SETTLE===180 && V.SETTLE_MS===260);
check("opening settle leftover still openLeft stamp", /wasOpen && openLeft <= 0\) stampNow\(1\.15\)/.test(srcTxt) && /stampNow\(1\.3\)/.test(sliceFn(codeOnly, "resetRound")));
check("opening settle leftover still 260\/4 grit", V.SETTLE_MS===260 && V.OPENING_MS===1600 && V.GUARD_BREAK_SETTLE===180 && /return 4 \* u \* u \* rise/.test(sliceFn(codeOnly, "settleDip")));
check("opening settle leftover is not a new combat verb", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.HITSTUN===350 && V.HITSTOP_HIT===140 && V.THROW_WAKE_INVULN===80);
check("slash frames still 180/140/280 after opening settle leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after opening settle leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after opening settle leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after opening settle leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after opening settle leftover", /game.js\?v=350/.test(htmlTxt));

// live: drain any leftover settle, then re-arm via openLeft lift
bootPlay();
wait(1700);
for(let i=0;i<40;i++){ if(V.settleT<=0) break; V.update(STEP); }
freezeYouAI(); freezeRivalAI();
V.openLeft = STEP;
const osDest0 = V.destRect(V.player);
const osAabb0 = V.bodyAABB(V.player);
const osDip0 = V.settleDip();
V.update(STEP); // openLeft → 0, stampNow
const osDipArm = V.settleDip();
const osDestArm = V.destRect(V.player);
const osAabbArm = V.bodyAABB(V.player);
check("opening settle leftover live open arms stamp", V.openLeft===0 && V.settleT===V.SETTLE_MS && V.settleMax===V.SETTLE_MS, `open=${V.openLeft} stt=${V.settleT} max=${V.settleMax}`);
check("opening settle leftover live open no dump on arm", Math.abs(osDipArm)<0.05 && Math.abs(osDestArm.dy-osDest0.dy)<1.2 && Math.abs(osDip0)<0.05, `dip0=${osDip0.toFixed(2)} dipArm=${osDipArm.toFixed(2)} ddy=${(osDestArm.dy-osDest0.dy).toFixed(2)}`);
check("opening settle leftover live open AABB planted", Math.abs(osAabbArm.y-osAabb0.y)<0.05 && Math.abs(osAabbArm.h-osAabb0.h)<0.05, `dy=${(osAabbArm.y-osAabb0.y).toFixed(2)}`);
// rise over first ~18% (~47ms ≈ 3 frames)
let osRise = 0;
for(let i=0;i<4;i++){ freezeYouAI(); freezeRivalAI(); V.update(STEP); osRise = V.settleDip(); }
check("opening settle leftover live open eases in", osRise>0.4 && osRise<3.5 && V.settleT>V.SETTLE_MS*0.7, `rise=${osRise.toFixed(2)} stt=${V.settleT}`);
// shortly after rise window, square ease-out still live (not drained)
for(let i=0;i<6;i++){ freezeYouAI(); freezeRivalAI(); V.update(STEP); }
const osMid = V.settleDip();
check("opening settle leftover live open square ease-out mid", osMid>0.5 && osMid<3.5 && V.settleT>V.SETTLE_MS*0.35 && V.settleT<V.SETTLE_MS*0.75, `mid=${osMid.toFixed(2)} stt=${V.settleT}`);
for(let i=0;i<40;i++){ if(V.settleT<=0) break; V.update(STEP); }
check("opening settle leftover live open ends at 0", V.settleT===0 && Math.abs(V.settleDip())<0.01, `stt=${V.settleT} dip=${V.settleDip().toFixed(3)}`);

// rematch/title stampNow(1.3) also no dump
const os2Aabb0 = V.bodyAABB(V.player);
const os2Dest0 = V.destRect(V.player);
V.stampNow(1.3);
check("opening settle leftover live rematch stamp no dump", V.settleT===V.SETTLE_MS && V.settleMax===V.SETTLE_MS && Math.abs(V.settleDip())<0.05 && Math.abs(V.destRect(V.player).dy-os2Dest0.dy)<1.2, `dip=${V.settleDip().toFixed(2)} ddy=${(V.destRect(V.player).dy-os2Dest0.dy).toFixed(2)}`);
check("opening settle leftover live rematch AABB planted", Math.abs(V.bodyAABB(V.player).y-os2Aabb0.y)<0.05, `dy=${(V.bodyAABB(V.player).y-os2Aabb0.y).toFixed(2)}`);

// guard-break settleMax clock via tripGuardBreak
bootPlay(); wait(1700);
for(let i=0;i<40;i++){ if(V.settleT<=0) break; V.update(STEP); }
freezeYouAI(); freezeRivalAI();
V.player.guarding = true; V.player.wantBlock = true; V.player.stamina = 0; V.player.guardPoseK = 1;
const osGbAabb0 = V.bodyAABB(V.player);
const osGbDip0 = V.settleDip();
V.tripGuardBreak(V.player, V.rival);
const osGbHit = V.player.guardBreakT>0 && V.settleT===V.GUARD_BREAK_SETTLE && V.settleMax===V.GUARD_BREAK_SETTLE;
check("opening settle leftover live guard-break arms", osGbHit, `hit=${osGbHit} stt=${V.settleT} max=${V.settleMax} gbt=${V.player.guardBreakT}`);
check("opening settle leftover live guard-break no dump on arm", osGbHit && Math.abs(V.settleDip())<0.15, `dip=${V.settleDip().toFixed(2)} dip0=${osGbDip0.toFixed(2)}`);
check("opening settle leftover live guard-break AABB planted", osGbHit && Math.abs(V.bodyAABB(V.player).y-osGbAabb0.y)<0.05, `dy=${(V.bodyAABB(V.player).y-osGbAabb0.y).toFixed(2)}`);
check("cache v=296 after opening settle leftover live", /game.js\?v=350/.test(htmlTxt));

// --- v228 block tip plant leftover: guard-break steel on tipY, not chest ---
check("block tip plant leftover seats tipY on opaque tip",
  /Block tip plant leftover/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipY: 223/.test(srcTxt) && /tipY: 230/.test(srcTxt) && /POSE\.block tipY/.test(srcTxt));
check("block tip plant leftover is draw-only",
  /Draw-only/.test(srcTxt) && /tipX \/ bladeBox \/ reach unchanged/.test(srcTxt) && /Cut-block asterisk still cutPoint/.test(srcTxt) && /bladeTipY\(f\)/.test(sliceFn(codeOnly, "guardSteelPoint")) && !/guardSteelPoint/.test(sliceFn(codeOnly, "destRect")) && !/guardSteelPoint/.test(sliceFn(codeOnly, "bodyAABB")) && !/guardSteelPoint/.test(sliceFn(codeOnly, "bladeBox")) && !/guardSteelPoint/.test(sliceFn(codeOnly, "landHit")) && !/guardSteelPoint/.test(sliceFn(codeOnly, "slashPose")));
check("block tip plant leftover plants before clearing guard",
  /plant steel while leftover block sheet still/.test(srcTxt) && /guarding used to clear first/.test(srcTxt) && sliceFn(codeOnly, "tripGuardBreak").indexOf("guardSteelPoint") < sliceFn(codeOnly, "tripGuardBreak").indexOf("f.guarding = false"));
check("block tip plant leftover wires guardSteelPoint Y",
  /bladeTipY\(f\)/.test(sliceFn(codeOnly, "guardSteelPoint")) && /d\.tipY != null/.test(sliceFn(codeOnly, "guardSteelPoint")) && /bodyAABB chest mid/.test(srcTxt));
check("block tip plant leftover tipX unchanged",
  /block:  \{ w: 1186, h: 926, footX: 75,  footY: 921, top: 4, tipY: 105, steelX: 424 \}/.test(srcTxt) && /block:  \{ w: 877,  h: 945, footX: 729, footY: 940, top: 4, tipY: 223, steelX: 544 \}/.test(srcTxt) && /block:  \{ w: 885,  h: 956, footX: 150, footY: 947, top: 8, tipY: 230, steelX: 335 \}/.test(srcTxt) && !/block:.*tipX:/.test(srcTxt));
check("block tip plant leftover is not a new combat verb",
  V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80 && V.BOLT_SUPER_STOP===60 && V.BOLT_CAST_FX_MS===220 && V.GUARD_BREAK_MS===400 && V.GUARD_BREAK_SETTLE===180);
check("slash frames still 180/140/280 after block tip plant leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after block tip plant leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after block tip plant leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after block tip plant leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after block tip plant leftover", /game.js\?v=350/.test(htmlTxt));

// live: you hold-drain guard-break steel on tipY
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.player.guarding=true; V.player.guardPoseK=1; V.player.stamina=0; V.player.phase="idle"; V.player.phaseT=0;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.boltPhase=""; V.player.falling=false; V.player.stunT=0; V.player.hp=100;
V.keys.clear(); V.keys.add("KeyS");
const btYouAabb0 = V.bodyAABB(V.player);
const btYouDest0 = V.destRect(V.player);
const btYouChest = btYouAabb0.y + btYouAabb0.h * 0.36;
const btYouSheet = V.poseSheet(V.player);
const btYouTipY = V.bladeTipY(V.player);
check("block tip plant leftover live you arms guard tipY", btYouSheet.tipY===105, `tipY=${btYouSheet.tipY} src=${(V.poseBitmap(V.player)&&V.poseBitmap(V.player)._src)||""}`);
V.tripGuardBreak(V.player);
check("block tip plant leftover live you break arms steel", V.player.guardBreakT>0 && V.steelFlashT>0, `gbt=${V.player.guardBreakT} steelT=${V.steelFlashT}`);
check("block tip plant leftover live you steel on tipY", V.steelFlashT>0 && Math.abs(V.steelY-btYouTipY)<4, `sy=${V.steelY.toFixed(1)} tipY=${btYouTipY.toFixed(1)} chest=${btYouChest.toFixed(1)}`);
check("block tip plant leftover live you not chest mid", V.steelFlashT>0 && Math.abs(V.steelY-btYouChest)>20, `sy=${V.steelY.toFixed(1)} chest=${btYouChest.toFixed(1)} tipY=${btYouTipY.toFixed(1)}`);
check("block tip plant leftover live you AABB planted", Math.abs(V.bodyAABB(V.player).y-btYouAabb0.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-btYouAabb0.h)<0.05, `dy=${(V.bodyAABB(V.player).y-btYouAabb0.y).toFixed(2)}`);
check("block tip plant leftover live you destRect planted", Math.abs(V.destRect(V.player).dy-btYouDest0.dy)<2.5, `ddy=${(V.destRect(V.player).dy-btYouDest0.dy).toFixed(2)}`);

// live: rival hold-drain guard-break steel on tipY
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.rival.guarding=true; V.rival.guardPoseK=1; V.rival.stamina=0; V.rival.phase="idle"; V.rival.phaseT=0;
V.rival.feintT=0; V.rival.sheatheT=0; V.rival.gait=0; V.rival.boltPhase=""; V.rival.falling=false; V.rival.stunT=0; V.rival.hp=100;
const btRivAabb0 = V.bodyAABB(V.rival);
const btRivChest = btRivAabb0.y + btRivAabb0.h * 0.36;
const btRivSheet = V.poseSheet(V.rival);
const btRivTipY = V.bladeTipY(V.rival);
check("block tip plant leftover live rival arms guard tipY", btRivSheet.tipY===223, `tipY=${btRivSheet.tipY}`);
V.tripGuardBreak(V.rival);
check("block tip plant leftover live rival break arms steel", V.rival.guardBreakT>0 && V.steelFlashT>0, `gbt=${V.rival.guardBreakT} steelT=${V.steelFlashT}`);
check("block tip plant leftover live rival steel on tipY", V.steelFlashT>0 && Math.abs(V.steelY-btRivTipY)<4, `sy=${V.steelY.toFixed(1)} tipY=${btRivTipY.toFixed(1)} chest=${btRivChest.toFixed(1)}`);
check("block tip plant leftover live rival not chest mid", V.steelFlashT>0 && Math.abs(V.steelY-btRivChest)>20, `sy=${V.steelY.toFixed(1)} chest=${btRivChest.toFixed(1)}`);
check("block tip plant leftover live rival AABB planted", Math.abs(V.bodyAABB(V.rival).y-btRivAabb0.y)<0.05, `dy=${(V.bodyAABB(V.rival).y-btRivAabb0.y).toFixed(2)}`);

// cut-block still cutPoint (atk path)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.rival.x = V.player.x + 140;
V.rival.facing = -1;
V.rival.guarding=true; V.rival.guardPoseK=1; V.rival.stamina=50; V.rival.phase="idle"; V.rival.phaseT=0;
V.player.guarding=false; V.player.phase="active"; V.player.phaseT=20; V.player.cut="slash"; V.player.facing=1; V.player.stamina=100;
const btCut0 = V.cutPoint(V.player, V.rival);
V.landBlock(V.player, V.rival, 1);
check("block tip plant leftover cut-block still cutPoint", V.steelFlashT>0 && Math.abs(V.steelX-btCut0.x)<12 && Math.abs(V.steelY-btCut0.y)<12, `steel=${V.steelX.toFixed(1)},${V.steelY.toFixed(1)} cut=${btCut0.x.toFixed(1)},${btCut0.y.toFixed(1)}`);
check("cache v=296 after block tip plant leftover live", /game.js\?v=350/.test(htmlTxt));


// --- v229 block steel X plant leftover: guard-break steel on tipX, not chest fraction ---
check("block steel X plant leftover seats steelX on opaque tip",
  /Block steel X plant leftover/.test(srcTxt) && /steelX: 424/.test(srcTxt) && /steelX: 544/.test(srcTxt) && /steelX: 335/.test(srcTxt) && /POSE\.block steelX/.test(srcTxt));
check("block steel X plant leftover is draw-only",
  /Draw-only/.test(srcTxt) && /Not tipX/.test(srcTxt) && /d\.steelX != null/.test(sliceFn(codeOnly, "guardSteelPoint")) && /r\.dx \+ d\.steelX \* sc/.test(sliceFn(codeOnly, "guardSteelPoint")) && !/steelX/.test(sliceFn(codeOnly, "destRect")) && !/steelX/.test(sliceFn(codeOnly, "bodyAABB")) && !/steelX/.test(sliceFn(codeOnly, "bladeBox")) && !/steelX/.test(sliceFn(codeOnly, "bladeTipX")) && !/steelX/.test(sliceFn(codeOnly, "landHit")) && !/steelX/.test(sliceFn(codeOnly, "slashPose")));
check("block steel X plant leftover wires guardSteelPoint X",
  /d\.steelX != null/.test(sliceFn(codeOnly, "guardSteelPoint")) && /bodyAABB chest fraction/.test(srcTxt) && /bladeTipY\(f\)/.test(sliceFn(codeOnly, "guardSteelPoint")));
check("block steel X plant leftover tipX still absent",
  /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && !/block:.*tipX:/.test(srcTxt));
check("block steel X plant leftover tipY unchanged",
  /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt));
check("block steel X plant leftover is not a new combat verb",
  V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80 && V.BOLT_SUPER_STOP===60 && V.BOLT_CAST_FX_MS===220 && V.GUARD_BREAK_MS===400 && V.GUARD_BREAK_SETTLE===180);
check("slash frames still 180/140/280 after block steel X plant leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after block steel X plant leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after block steel X plant leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after block steel X plant leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after block steel X plant leftover", /game.js\?v=350/.test(htmlTxt));

// live: you hold-drain steel X on opaque tip (near chest — ~6px)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.player.guarding=true; V.player.guardPoseK=1; V.player.stamina=0; V.player.phase="idle"; V.player.phaseT=0;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.boltPhase=""; V.player.falling=false; V.player.stunT=0; V.player.hp=100;
V.keys.clear(); V.keys.add("KeyS");
const sxYouAabb0 = V.bodyAABB(V.player);
const sxYouDest0 = V.destRect(V.player);
const sxYouSheet = V.poseSheet(V.player);
const sxYouSc = sxYouDest0.dw / sxYouSheet.w;
const sxYouChestX = sxYouAabb0.x + sxYouAabb0.w * 0.72;
const sxYouPlantX = sxYouDest0.dx + 424 * sxYouSc;
const sxYouTipY = V.bladeTipY(V.player);
const sxYouReach0 = V.bladeReach(V.player);
check("block steel X plant leftover live you arms steelX", sxYouSheet.steelX===424 && sxYouSheet.tipY===105, `steelX=${sxYouSheet.steelX} tipY=${sxYouSheet.tipY}`);
V.tripGuardBreak(V.player);
check("block steel X plant leftover live you break arms steel", V.player.guardBreakT>0 && V.steelFlashT>0, `gbt=${V.player.guardBreakT} steelT=${V.steelFlashT}`);
check("block steel X plant leftover live you steel on tip X", V.steelFlashT>0 && Math.abs(V.steelX-sxYouPlantX)<4, `sx=${V.steelX.toFixed(1)} plant=${sxYouPlantX.toFixed(1)} chest=${sxYouChestX.toFixed(1)}`);
check("block steel X plant leftover live you steel on tipY", V.steelFlashT>0 && Math.abs(V.steelY-sxYouTipY)<4, `sy=${V.steelY.toFixed(1)} tipY=${sxYouTipY.toFixed(1)}`);
check("block steel X plant leftover live you tipX still sheet-edge", Math.abs(V.bladeTipX(V.player)-(sxYouDest0.dx+sxYouDest0.dw-8))<1, `tipX=${V.bladeTipX(V.player).toFixed(1)}`);
check("block steel X plant leftover live you reach planted", Math.abs(V.bladeReach(V.player)-sxYouReach0)<0.05, `reach=${V.bladeReach(V.player).toFixed(2)} was=${sxYouReach0.toFixed(2)}`);
check("block steel X plant leftover live you AABB planted", Math.abs(V.bodyAABB(V.player).y-sxYouAabb0.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-sxYouAabb0.h)<0.05, `dy=${(V.bodyAABB(V.player).y-sxYouAabb0.y).toFixed(2)}`);
check("block steel X plant leftover live you destRect planted", Math.abs(V.destRect(V.player).dy-sxYouDest0.dy)<2.5, `ddy=${(V.destRect(V.player).dy-sxYouDest0.dy).toFixed(2)}`);

// live: rival hold-drain steel X ~42px off chest onto tip
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.rival.guarding=true; V.rival.guardPoseK=1; V.rival.stamina=0; V.rival.phase="idle"; V.rival.phaseT=0;
V.rival.feintT=0; V.rival.sheatheT=0; V.rival.gait=0; V.rival.boltPhase=""; V.rival.falling=false; V.rival.stunT=0; V.rival.hp=100;
V.rival.facing=-1;
const sxRivAabb0 = V.bodyAABB(V.rival);
const sxRivDest0 = V.destRect(V.rival);
const sxRivSheet = V.poseSheet(V.rival);
const sxRivSc = sxRivDest0.dw / sxRivSheet.w;
const sxRivChestX = sxRivAabb0.x + sxRivAabb0.w * 0.28;
const sxRivPlantX = sxRivDest0.dx + 544 * sxRivSc;
const sxRivTipY = V.bladeTipY(V.rival);
const sxRivReach0 = V.bladeReach(V.rival);
check("block steel X plant leftover live rival arms steelX", sxRivSheet.steelX===544 && sxRivSheet.tipY===223, `steelX=${sxRivSheet.steelX} tipY=${sxRivSheet.tipY}`);
check("block steel X plant leftover live rival tip off chest", Math.abs(sxRivPlantX-sxRivChestX)>30, `plant=${sxRivPlantX.toFixed(1)} chest=${sxRivChestX.toFixed(1)}`);
V.tripGuardBreak(V.rival);
check("block steel X plant leftover live rival break arms steel", V.rival.guardBreakT>0 && V.steelFlashT>0, `gbt=${V.rival.guardBreakT} steelT=${V.steelFlashT}`);
check("block steel X plant leftover live rival steel on tip X", V.steelFlashT>0 && Math.abs(V.steelX-sxRivPlantX)<4, `sx=${V.steelX.toFixed(1)} plant=${sxRivPlantX.toFixed(1)} chest=${sxRivChestX.toFixed(1)}`);
check("block steel X plant leftover live rival not chest X", V.steelFlashT>0 && Math.abs(V.steelX-sxRivChestX)>30, `sx=${V.steelX.toFixed(1)} chest=${sxRivChestX.toFixed(1)}`);
check("block steel X plant leftover live rival steel on tipY", V.steelFlashT>0 && Math.abs(V.steelY-sxRivTipY)<4, `sy=${V.steelY.toFixed(1)} tipY=${sxRivTipY.toFixed(1)}`);
check("block steel X plant leftover live rival reach planted", Math.abs(V.bladeReach(V.rival)-sxRivReach0)<0.05, `reach=${V.bladeReach(V.rival).toFixed(2)}`);
check("block steel X plant leftover live rival AABB planted", Math.abs(V.bodyAABB(V.rival).y-sxRivAabb0.y)<0.05, `dy=${(V.bodyAABB(V.rival).y-sxRivAabb0.y).toFixed(2)}`);

// live: flip facing steelX 335
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.rival.x = V.player.x - 200;
V.rival.facing = 1;
V.player.facing = -1;
V.rival.guarding=true; V.rival.guardPoseK=1; V.rival.stamina=0; V.rival.phase="idle"; V.rival.phaseT=0;
V.rival.feintT=0; V.rival.sheatheT=0; V.rival.gait=0; V.rival.boltPhase=""; V.rival.falling=false; V.rival.stunT=0; V.rival.hp=100;
const sxFlipAabb0 = V.bodyAABB(V.rival);
const sxFlipDest0 = V.destRect(V.rival);
const sxFlipSheet = V.poseSheet(V.rival);
const sxFlipSc = sxFlipDest0.dw / sxFlipSheet.w;
const sxFlipChestX = sxFlipAabb0.x + sxFlipAabb0.w * 0.72;
const sxFlipPlantX = sxFlipDest0.dx + 335 * sxFlipSc;
const sxFlipTipY = V.bladeTipY(V.rival);
check("block steel X plant leftover live flip arms steelX", sxFlipSheet.steelX===335 && sxFlipSheet.tipY===230 && /flip/.test((V.poseBitmap(V.rival)&&V.poseBitmap(V.rival)._src)||""), `steelX=${sxFlipSheet.steelX} tipY=${sxFlipSheet.tipY} src=${(V.poseBitmap(V.rival)&&V.poseBitmap(V.rival)._src)||""}`);
check("block steel X plant leftover live flip tip off chest", Math.abs(sxFlipPlantX-sxFlipChestX)>30, `plant=${sxFlipPlantX.toFixed(1)} chest=${sxFlipChestX.toFixed(1)}`);
V.tripGuardBreak(V.rival);
check("block steel X plant leftover live flip steel on tip X", V.steelFlashT>0 && Math.abs(V.steelX-sxFlipPlantX)<4, `sx=${V.steelX.toFixed(1)} plant=${sxFlipPlantX.toFixed(1)} chest=${sxFlipChestX.toFixed(1)}`);
check("block steel X plant leftover live flip not chest X", V.steelFlashT>0 && Math.abs(V.steelX-sxFlipChestX)>30, `sx=${V.steelX.toFixed(1)} chest=${sxFlipChestX.toFixed(1)}`);
check("block steel X plant leftover live flip steel on tipY", V.steelFlashT>0 && Math.abs(V.steelY-sxFlipTipY)<4, `sy=${V.steelY.toFixed(1)} tipY=${sxFlipTipY.toFixed(1)}`);
check("block steel X plant leftover live flip AABB planted", Math.abs(V.bodyAABB(V.rival).y-sxFlipAabb0.y)<0.05, `dy=${(V.bodyAABB(V.rival).y-sxFlipAabb0.y).toFixed(2)}`);

// cut-block still cutPoint
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.rival.x = V.player.x + 140;
V.rival.facing = -1;
V.rival.guarding=true; V.rival.guardPoseK=1; V.rival.stamina=50; V.rival.phase="idle"; V.rival.phaseT=0;
V.player.guarding=false; V.player.phase="active"; V.player.phaseT=20; V.player.cut="slash"; V.player.facing=1; V.player.stamina=100;
const sxCut0 = V.cutPoint(V.player, V.rival);
V.landBlock(V.player, V.rival, 1);
check("block steel X plant leftover cut-block still cutPoint", V.steelFlashT>0 && Math.abs(V.steelX-sxCut0.x)<12 && Math.abs(V.steelY-sxCut0.y)<12, `steel=${V.steelX.toFixed(1)},${V.steelY.toFixed(1)} cut=${sxCut0.x.toFixed(1)},${sxCut0.y.toFixed(1)}`);
check("cache v=296 after block steel X plant leftover live", /game.js\?v=350/.test(htmlTxt));

// --- v230 guard-break steel sync leftover: tip plant rides wound, not blade∩body mid ---
check("guard-break steel sync leftover seats tipRide",
  /Guard-break steel sync leftover/.test(srcTxt) && /steelTipRide/.test(srcTxt) && /spawnSteelFlash\(pt\.x, pt\.y, other, f, !atk\)/.test(srcTxt) && /if \(steelTipRide\)/.test(srcTxt));
check("guard-break steel sync leftover is draw-only",
  /Draw-only \(steelTipRide\)/.test(srcTxt) && /Tip-planted guard-break rides wound only/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && !/steelTipRide/.test(sliceFn(codeOnly, "destRect")) && !/steelTipRide/.test(sliceFn(codeOnly, "bodyAABB")) && !/steelTipRide/.test(sliceFn(codeOnly, "bladeBox")) && !/steelTipRide/.test(sliceFn(codeOnly, "slashPose")) && !/steelTipRide/.test(sliceFn(codeOnly, "landHit")));
check("guard-break steel sync leftover tip markers unchanged",
  /tipY: 105/.test(srcTxt) && /steelX: 424/.test(srcTxt) && /tipY: 223/.test(srcTxt) && /steelX: 544/.test(srcTxt) && /tipY: 230/.test(srcTxt) && /steelX: 335/.test(srcTxt));
check("guard-break steel sync leftover cut-block still overlap-syncs",
  /Cut-block still overlap-syncs/.test(srcTxt) && /spawnSteelFlash\(pt\.x, pt\.y, other, f, !atk\)/.test(srcTxt) && /steelTipRide = !!tipRide/.test(srcTxt));
check("guard-break steel sync leftover is not a new combat verb",
  V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80 && V.GUARD_BREAK_MS>0 && V.GUARD_BREAK_SETTLE>0);
check("slash frames still 180/140/280 after guard-break steel sync leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after guard-break steel sync leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after guard-break steel sync leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after guard-break steel sync leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after guard-break steel sync leftover", /game.js\?v=350/.test(htmlTxt));

// live: rival hold-drain tip plant sticks through sync (was ~37px hop onto blade∩body mid)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.rival.guarding=true; V.rival.guardPoseK=1; V.rival.stamina=0; V.rival.phase="idle"; V.rival.phaseT=0;
V.rival.feintT=0; V.rival.sheatheT=0; V.rival.gait=0; V.rival.boltPhase=""; V.rival.falling=false; V.rival.stunT=0; V.rival.hp=100;
V.rival.facing=-1;
const gsRivAabb0 = V.bodyAABB(V.rival);
const gsRivDest0 = V.destRect(V.rival);
const gsRivSheet = V.poseSheet(V.rival);
const gsRivSc = gsRivDest0.dw / gsRivSheet.w;
const gsRivChestX = gsRivAabb0.x + gsRivAabb0.w * 0.28;
const gsRivPlantX = gsRivDest0.dx + 544 * gsRivSc;
const gsRivTipY = V.bladeTipY(V.rival);
check("guard-break steel sync leftover live rival arms steelX tipY", gsRivSheet.steelX===544 && gsRivSheet.tipY===223, `steelX=${gsRivSheet.steelX} tipY=${gsRivSheet.tipY}`);
check("guard-break steel sync leftover live rival tip off chest", Math.abs(gsRivPlantX-gsRivChestX)>30, `plant=${gsRivPlantX.toFixed(1)} chest=${gsRivChestX.toFixed(1)}`);
V.tripGuardBreak(V.rival);
check("guard-break steel sync leftover live rival break arms tipRide", V.rival.guardBreakT>0 && V.steelFlashT>0 && V.steelTipRide===true, `gbt=${V.rival.guardBreakT} steelT=${V.steelFlashT} tipRide=${V.steelTipRide}`);
check("guard-break steel sync leftover live rival plant on tip", V.steelFlashT>0 && Math.abs(V.steelX-gsRivPlantX)<4 && Math.abs(V.steelY-gsRivTipY)<4, `sx=${V.steelX.toFixed(1)} sy=${V.steelY.toFixed(1)} plant=${gsRivPlantX.toFixed(1)} tipY=${gsRivTipY.toFixed(1)}`);
V.syncSteelFlash();
check("guard-break steel sync leftover live rival sync keeps tip X", V.steelFlashT>0 && Math.abs(V.steelX-gsRivPlantX)<4, `sx=${V.steelX.toFixed(1)} plant=${gsRivPlantX.toFixed(1)} chest=${gsRivChestX.toFixed(1)}`);
check("guard-break steel sync leftover live rival sync keeps tipY", V.steelFlashT>0 && Math.abs(V.steelY-gsRivTipY)<4, `sy=${V.steelY.toFixed(1)} tipY=${gsRivTipY.toFixed(1)}`);
check("guard-break steel sync leftover live rival sync not chest X", V.steelFlashT>0 && Math.abs(V.steelX-gsRivChestX)>30, `sx=${V.steelX.toFixed(1)} chest=${gsRivChestX.toFixed(1)}`);
check("guard-break steel sync leftover live rival AABB planted", Math.abs(V.bodyAABB(V.rival).y-gsRivAabb0.y)<0.05 && Math.abs(V.bodyAABB(V.rival).h-gsRivAabb0.h)<0.05, `dy=${(V.bodyAABB(V.rival).y-gsRivAabb0.y).toFixed(2)}`);
check("guard-break steel sync leftover live rival destRect planted", Math.abs(V.destRect(V.rival).dy-gsRivDest0.dy)<2.5, `ddy=${(V.destRect(V.rival).dy-gsRivDest0.dy).toFixed(2)}`);

// live: you hold-drain tipRide + sync keeps tip
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.player.guarding=true; V.player.guardPoseK=1; V.player.stamina=0; V.player.phase="idle"; V.player.phaseT=0;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.boltPhase=""; V.player.falling=false; V.player.stunT=0; V.player.hp=100;
V.keys.clear(); V.keys.add("KeyS");
const gsYouAabb0 = V.bodyAABB(V.player);
const gsYouDest0 = V.destRect(V.player);
const gsYouSheet = V.poseSheet(V.player);
const gsYouSc = gsYouDest0.dw / gsYouSheet.w;
const gsYouPlantX = gsYouDest0.dx + 424 * gsYouSc;
const gsYouTipY = V.bladeTipY(V.player);
V.tripGuardBreak(V.player);
check("guard-break steel sync leftover live you tipRide", V.player.guardBreakT>0 && V.steelTipRide===true && V.steelFlashT>0, `gbt=${V.player.guardBreakT} tipRide=${V.steelTipRide}`);
V.syncSteelFlash();
check("guard-break steel sync leftover live you sync keeps tip", V.steelFlashT>0 && Math.abs(V.steelX-gsYouPlantX)<4 && Math.abs(V.steelY-gsYouTipY)<4, `sx=${V.steelX.toFixed(1)} sy=${V.steelY.toFixed(1)} plant=${gsYouPlantX.toFixed(1)} tipY=${gsYouTipY.toFixed(1)}`);
check("guard-break steel sync leftover live you AABB planted", Math.abs(V.bodyAABB(V.player).y-gsYouAabb0.y)<0.05, `dy=${(V.bodyAABB(V.player).y-gsYouAabb0.y).toFixed(2)}`);

// live: flip rival tipRide sync keeps tip
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.rival.facing=1;
V.rival.guarding=true; V.rival.guardPoseK=1; V.rival.stamina=0; V.rival.phase="idle"; V.rival.phaseT=0;
V.rival.feintT=0; V.rival.sheatheT=0; V.rival.gait=0; V.rival.boltPhase=""; V.rival.falling=false; V.rival.stunT=0; V.rival.hp=100;
const gsFlipAabb0 = V.bodyAABB(V.rival);
const gsFlipDest0 = V.destRect(V.rival);
const gsFlipSheet = V.poseSheet(V.rival);
const gsFlipSc = gsFlipDest0.dw / gsFlipSheet.w;
const gsFlipChestX = gsFlipAabb0.x + gsFlipAabb0.w * 0.72;
const gsFlipPlantX = gsFlipDest0.dx + 335 * gsFlipSc;
const gsFlipTipY = V.bladeTipY(V.rival);
check("guard-break steel sync leftover live flip arms steelX", gsFlipSheet.steelX===335 && gsFlipSheet.tipY===230 && /flip/.test((V.poseBitmap(V.rival)&&V.poseBitmap(V.rival)._src)||""), `steelX=${gsFlipSheet.steelX} tipY=${gsFlipSheet.tipY}`);
V.tripGuardBreak(V.rival);
V.syncSteelFlash();
check("guard-break steel sync leftover live flip tipRide sync", V.steelTipRide===true && Math.abs(V.steelX-gsFlipPlantX)<4 && Math.abs(V.steelY-gsFlipTipY)<4, `tipRide=${V.steelTipRide} sx=${V.steelX.toFixed(1)} plant=${gsFlipPlantX.toFixed(1)} tipY=${gsFlipTipY.toFixed(1)}`);
check("guard-break steel sync leftover live flip not chest X", Math.abs(V.steelX-gsFlipChestX)>30, `sx=${V.steelX.toFixed(1)} chest=${gsFlipChestX.toFixed(1)}`);
check("guard-break steel sync leftover live flip AABB planted", Math.abs(V.bodyAABB(V.rival).y-gsFlipAabb0.y)<0.05, `dy=${(V.bodyAABB(V.rival).y-gsFlipAabb0.y).toFixed(2)}`);

// live: cut-block still cutPoint + no tipRide + sync stays near cut
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.rival.facing=-1;
V.rival.guarding=true; V.rival.guardPoseK=1; V.rival.stamina=5; V.rival.phase="idle"; V.rival.phaseT=0;
V.rival.feintT=0; V.rival.sheatheT=0; V.rival.gait=0; V.rival.boltPhase=""; V.rival.falling=false; V.rival.stunT=0; V.rival.hp=100;
V.player.phase="active"; V.player.phaseT=0; V.player.cut="slash"; V.player.cutHit=false; V.player.facing=1;
const gsCut0 = V.cutPoint(V.player, V.rival);
V.tripGuardBreak(V.rival, V.player);
check("guard-break steel sync leftover live cut-block no tipRide", V.rival.guardBreakT>0 && V.steelFlashT>0 && V.steelTipRide===false, `gbt=${V.rival.guardBreakT} tipRide=${V.steelTipRide}`);
V.syncSteelFlash();
check("guard-break steel sync leftover live cut-block still cutPoint", V.steelFlashT>0 && Math.abs(V.steelX-gsCut0.x)<12 && Math.abs(V.steelY-gsCut0.y)<12, `steel=${V.steelX.toFixed(1)},${V.steelY.toFixed(1)} cut=${gsCut0.x.toFixed(1)},${gsCut0.y.toFixed(1)}`);
check("cache v=296 after guard-break steel sync leftover live", /game.js\?v=350/.test(htmlTxt));


// --- v231 windup tip plant leftover: cocked tip on opaque tip, not empty air ---
check("windup tip plant leftover seats on opaque tip",
  /Windup tip plant leftover/.test(srcTxt)
  && /tip markers used to sit in empty air/.test(srcTxt)
  && /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt)
  && /tipX: 738/.test(srcTxt) && /tipY: 224/.test(srcTxt)
  && /tipX: 141/.test(srcTxt) && /tipY: 231/.test(srcTxt)
  && !/tipX: 323/.test(srcTxt) && !/tipY: 55/.test(srcTxt)
  && !/tipX: 443/.test(srcTxt) && !/tipX: 436/.test(srcTxt));
check("windup tip plant leftover is draw-only",
  /Draw-only \(POSE\.windup tipX\/tipY\)/.test(srcTxt)
  && /Startup \/ throw \/ telegraph juice that rides bladeTip/.test(srcTxt)
  && /destRect\/AABB planted/.test(srcTxt)
  && !/tipX: 359/.test(sliceFn(codeOnly, "destRect"))
  && !/tipX: 359/.test(sliceFn(codeOnly, "bodyAABB"))
  && !/tipX: 359/.test(sliceFn(codeOnly, "slashPose"))
  && !/tipX: 359/.test(sliceFn(codeOnly, "landHit")));
check("windup tip plant leftover throwKnife slash block unchanged",
  /ThrowKnife \/ slash \/ block tip markers unchanged/.test(srcTxt)
  && /tipX: 268/.test(srcTxt) && /tipY: 109/.test(srcTxt)
  && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt)
  && /tipY: 105, steelX: 424/.test(srcTxt)
  && /tipY: 223, steelX: 544/.test(srcTxt)
  && /tipY: 230, steelX: 335/.test(srcTxt));
check("windup tip plant leftover is not a new combat verb",
  V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36
  && V.THROW_STARTUP===80 && V.THROW_ACTIVE===40 && V.THROW_RECOVERY===220
  && V.HITSTOP_HIT===140 && V.HITSTUN===350);
check("slash frames still 180/140/280 after windup tip plant leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after windup tip plant leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after windup tip plant leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after windup tip plant leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after windup tip plant leftover", /game.js\?v=350/.test(htmlTxt));

// live: you slash startup tip on cocked opaque tip (was empty air 323/55)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.hp=100;
V.rival.phase="idle"; V.rival.wantBlock=false; V.rival.revArmed=-1;
V.player.x=500; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
const wuYouAabb0 = V.bodyAABB(V.player);
const wuYouDest0 = V.destRect(V.player);
const wuYouReach0 = V.bladeReach(V.player);
V.startAttack(V.player, "slash");
const wuYouSheet = V.poseSheet(V.player);
const wuYouDest1 = V.destRect(V.player);
const wuYouTipX = V.bladeTipX(V.player);
const wuYouTipY = V.bladeTipY(V.player);
const wuYouSc = wuYouDest1.dw / wuYouSheet.w;
const wuYouOldX = wuYouDest1.dx + 323 * wuYouSc;
const wuYouOldY = wuYouDest1.dy + 55 * wuYouSc;
const wuYouPlantX = wuYouDest1.dx + 359 * wuYouSc;
const wuYouPlantY = wuYouDest1.dy + 105 * wuYouSc;
check("windup tip plant leftover live you arms windup tip", V.player.phase==="startup" && V.player.cut==="slash" && wuYouSheet.tipX===359 && wuYouSheet.tipY===105 && /windup/.test((V.poseBitmap(V.player)&&V.poseBitmap(V.player)._src)||""), `ph=${V.player.phase} tip=${wuYouSheet.tipX}/${wuYouSheet.tipY} src=${(V.poseBitmap(V.player)&&V.poseBitmap(V.player)._src)||""}`);
// Tip under telegraphFade: snap tele clock so bladeTip seats on opaque windup tip (no update hop).
V.player.phaseT = V.GUARD_RAISE_MS;
const wuYouTipX2 = V.bladeTipX(V.player);
const wuYouTipY2 = V.bladeTipY(V.player);
const wuYouDest2 = V.destRect(V.player);
const wuYouSc2 = wuYouDest2.dw / wuYouSheet.w;
const wuYouPlantX2 = wuYouDest2.dx + 359 * wuYouSc2;
const wuYouPlantY2 = wuYouDest2.dy + 105 * wuYouSc2;
const wuYouOldX2 = wuYouDest2.dx + 323 * wuYouSc2;
const wuYouOldY2 = wuYouDest2.dy + 55 * wuYouSc2;
check("windup tip plant leftover live you tip on opaque", Math.abs(wuYouTipX2-wuYouPlantX2)<2 && Math.abs(wuYouTipY2-wuYouPlantY2)<2, `tip=${wuYouTipX2.toFixed(1)},${wuYouTipY2.toFixed(1)} plant=${wuYouPlantX2.toFixed(1)},${wuYouPlantY2.toFixed(1)} tf=${V.telegraphFade(V.player).toFixed(3)}`);
check("windup tip plant leftover live you not empty air", Math.abs(wuYouTipX2-wuYouOldX2)>8 && Math.abs(wuYouTipY2-wuYouOldY2)>8, `tip=${wuYouTipX2.toFixed(1)},${wuYouTipY2.toFixed(1)} old=${wuYouOldX2.toFixed(1)},${wuYouOldY2.toFixed(1)}`);
check("windup tip plant leftover live you AABB planted", Math.abs(V.bodyAABB(V.player).y-wuYouAabb0.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-wuYouAabb0.h)<0.05, `dy=${(V.bodyAABB(V.player).y-wuYouAabb0.y).toFixed(2)}`);
check("windup tip plant leftover live you destRect planted", Math.abs(wuYouDest1.dy-wuYouDest0.dy)<2.5, `ddy=${(wuYouDest1.dy-wuYouDest0.dy).toFixed(2)}`);
check("windup tip plant leftover live you throwKnife slash tips unchanged", /tipX: 268/.test(srcTxt) && /tipX: 991/.test(srcTxt) && wuYouReach0>=0, `reach0=${wuYouReach0.toFixed(2)}`);

// live: rival slash startup tip on cocked opaque tip (was empty air 443/4)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.rival.facing=-1;
V.rival.phase="idle"; V.rival.phaseT=0; V.rival.stunT=0; V.rival.guarding=false; V.rival.boltPhase=""; V.rival.falling=false;
V.rival.feintT=0; V.rival.sheatheT=0; V.rival.gait=0; V.rival.stamina=100; V.rival.stamRegenT=99999; V.rival.hp=100;
V.rival.wantBlock=false; V.rival.telegraph=false;
V.player.phase="idle"; V.player.guarding=false;
V.player.x=200; V.rival.x=700;
V.keepApart();
const wuRivAabb0 = V.bodyAABB(V.rival);
const wuRivDest0 = V.destRect(V.rival);
V.startAttack(V.rival, "slash");
const wuRivSheet = V.poseSheet(V.rival);
const wuRivDest1 = V.destRect(V.rival);
const wuRivTipX = V.bladeTipX(V.rival);
const wuRivTipY = V.bladeTipY(V.rival);
const wuRivSc = wuRivDest1.dw / wuRivSheet.w;
const wuRivOldX = wuRivDest1.dx + 443 * wuRivSc;
const wuRivOldY = wuRivDest1.dy + 4 * wuRivSc;
const wuRivPlantX = wuRivDest1.dx + 738 * wuRivSc;
const wuRivPlantY = wuRivDest1.dy + 224 * wuRivSc;
check("windup tip plant leftover live rival arms windup tip", V.rival.phase==="startup" && wuRivSheet.tipX===738 && wuRivSheet.tipY===224, `ph=${V.rival.phase} tip=${wuRivSheet.tipX}/${wuRivSheet.tipY}`);
V.rival.phaseT = V.GUARD_RAISE_MS;
const wuRivTipX2 = V.bladeTipX(V.rival);
const wuRivTipY2 = V.bladeTipY(V.rival);
const wuRivDest2 = V.destRect(V.rival);
const wuRivSc2 = wuRivDest2.dw / wuRivSheet.w;
const wuRivPlantX2 = wuRivDest2.dx + 738 * wuRivSc2;
const wuRivPlantY2 = wuRivDest2.dy + 224 * wuRivSc2;
const wuRivOldX2 = wuRivDest2.dx + 443 * wuRivSc2;
const wuRivOldY2 = wuRivDest2.dy + 4 * wuRivSc2;
check("windup tip plant leftover live rival tip on opaque", Math.abs(wuRivTipX2-wuRivPlantX2)<2 && Math.abs(wuRivTipY2-wuRivPlantY2)<2, `tip=${wuRivTipX2.toFixed(1)},${wuRivTipY2.toFixed(1)} plant=${wuRivPlantX2.toFixed(1)},${wuRivPlantY2.toFixed(1)} tf=${V.telegraphFade(V.rival).toFixed(3)}`);
check("windup tip plant leftover live rival not empty air", Math.abs(wuRivTipX2-wuRivOldX2)>40 && Math.abs(wuRivTipY2-wuRivOldY2)>40, `tip=${wuRivTipX2.toFixed(1)},${wuRivTipY2.toFixed(1)} old=${wuRivOldX2.toFixed(1)},${wuRivOldY2.toFixed(1)}`);
check("windup tip plant leftover live rival AABB planted", Math.abs(V.bodyAABB(V.rival).y-wuRivAabb0.y)<0.05, `dy=${(V.bodyAABB(V.rival).y-wuRivAabb0.y).toFixed(2)}`);
check("windup tip plant leftover live rival destRect planted", Math.abs(wuRivDest1.dy-wuRivDest0.dy)<2.5, `ddy=${(wuRivDest1.dy-wuRivDest0.dy).toFixed(2)}`);

// live: flip rival windup tip
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.rival.x = V.player.x - 200;
V.rival.facing = 1;
V.player.facing = -1;
V.rival.phase="idle"; V.rival.phaseT=0; V.rival.stunT=0; V.rival.guarding=false; V.rival.boltPhase=""; V.rival.falling=false;
V.rival.feintT=0; V.rival.sheatheT=0; V.rival.gait=0; V.rival.stamina=100; V.rival.stamRegenT=99999; V.rival.hp=100;
V.player.phase="idle"; V.player.guarding=false;
const wuFlipAabb0 = V.bodyAABB(V.rival);
const wuFlipDest0 = V.destRect(V.rival);
V.startAttack(V.rival, "slash");
const wuFlipSheet = V.poseSheet(V.rival);
const wuFlipDest1 = V.destRect(V.rival);
const wuFlipTipX = V.bladeTipX(V.rival);
const wuFlipTipY = V.bladeTipY(V.rival);
const wuFlipSc = wuFlipDest1.dw / wuFlipSheet.w;
const wuFlipOldX = wuFlipDest1.dx + 436 * wuFlipSc;
const wuFlipOldY = wuFlipDest1.dy + 4 * wuFlipSc;
const wuFlipPlantX = wuFlipDest1.dx + 141 * wuFlipSc;
const wuFlipPlantY = wuFlipDest1.dy + 231 * wuFlipSc;
check("windup tip plant leftover live flip arms windup tip", V.rival.phase==="startup" && wuFlipSheet.tipX===141 && wuFlipSheet.tipY===231 && /flip/.test((V.poseBitmap(V.rival)&&V.poseBitmap(V.rival)._src)||""), `tip=${wuFlipSheet.tipX}/${wuFlipSheet.tipY} src=${(V.poseBitmap(V.rival)&&V.poseBitmap(V.rival)._src)||""}`);
V.rival.phaseT = V.GUARD_RAISE_MS;
const wuFlipTipX2 = V.bladeTipX(V.rival);
const wuFlipTipY2 = V.bladeTipY(V.rival);
const wuFlipDest2 = V.destRect(V.rival);
const wuFlipSc2 = wuFlipDest2.dw / wuFlipSheet.w;
const wuFlipPlantX2 = wuFlipDest2.dx + 141 * wuFlipSc2;
const wuFlipPlantY2 = wuFlipDest2.dy + 231 * wuFlipSc2;
const wuFlipOldX2 = wuFlipDest2.dx + 436 * wuFlipSc2;
const wuFlipOldY2 = wuFlipDest2.dy + 4 * wuFlipSc2;
check("windup tip plant leftover live flip tip on opaque", Math.abs(wuFlipTipX2-wuFlipPlantX2)<2 && Math.abs(wuFlipTipY2-wuFlipPlantY2)<2, `tip=${wuFlipTipX2.toFixed(1)},${wuFlipTipY2.toFixed(1)} plant=${wuFlipPlantX2.toFixed(1)},${wuFlipPlantY2.toFixed(1)} tf=${V.telegraphFade(V.rival).toFixed(3)}`);
check("windup tip plant leftover live flip not empty air", Math.abs(wuFlipTipX2-wuFlipOldX2)>40 && Math.abs(wuFlipTipY2-wuFlipOldY2)>40, `tip=${wuFlipTipX2.toFixed(1)},${wuFlipTipY2.toFixed(1)} old=${wuFlipOldX2.toFixed(1)},${wuFlipOldY2.toFixed(1)}`);
check("windup tip plant leftover live flip AABB planted", Math.abs(V.bodyAABB(V.rival).y-wuFlipAabb0.y)<0.05, `dy=${(V.bodyAABB(V.rival).y-wuFlipAabb0.y).toFixed(2)}`);

// live: throwKnife tip still knife (not windup)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.player.phase="idle"; V.player.phaseT=0; V.player.boltPhase=""; V.player.guarding=false; V.player.stamina=100; V.player.meter=0;
V.player.x=500; V.rival.x=1100; V.keepApart();
V.boltEdge=true; V.update(STEP);
const wuKnifeSheet = V.poseSheet(V.player);
check("windup tip plant leftover live knife tip unchanged", V.player.boltPhase==="startup" && wuKnifeSheet.tipX===268 && wuKnifeSheet.tipY===109, `bph=${V.player.boltPhase} tip=${wuKnifeSheet.tipX}/${wuKnifeSheet.tipY}`);

// live: slash tipY still slash on active
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.player.phase="active"; V.player.phaseT=20; V.player.cut="slash"; V.player.guarding=false; V.player.boltPhase="";
const wuSlashSheet = V.poseSheet(V.player);
check("windup tip plant leftover live slash tip unchanged", wuSlashSheet.tipX===991 && wuSlashSheet.tipY===270, `tip=${wuSlashSheet.tipX}/${wuSlashSheet.tipY}`);

check("cache v=296 after windup tip plant leftover live", /game.js\?v=350/.test(htmlTxt));



// --- v232 cut recovery destRect leftover: post-cut breath eases with cutRecBreathT ---
check("cut recovery destRect leftover eases breath with cutRecBreathT", /Cut recovery destRect leftover/.test(srcTxt) && /slash\/golpe recovery used to hard-zero/.test(srcTxt) && /chest dumped ~1\.6 the tick recovery popped idle/.test(srcTxt) && /cutRecBreathT/.test(srcTxt) && /f\.cutRecBreathT > 0/.test(sliceFn(codeOnly, "idleBreath")) && /f\.cutRecBreathT \/ GUARD_RAISE_MS/.test(sliceFn(codeOnly, "idleBreath")) && /golpeSheathed \|\| slashSheathed/.test(sliceFn(codeOnly, "advanceAttack")) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "advanceAttack")));
check("cut recovery destRect leftover is destRect-only", /destRect-only/.test(srcTxt) && /idleBreath \/ cutRecBreathT/.test(srcTxt) && /Keep recovery breath 0/.test(srcTxt) && /special-cancel K/.test(srcTxt) && /cutRecBreathT/.test(sliceFn(codeOnly, "idleBreath")) && /cutRecBreathT/.test(sliceFn(codeOnly, "tickSheathe")) && /cutRecBreathT/.test(sliceFn(codeOnly, "advanceAttack")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "destRect")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "bodyAABB")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "slashPose")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "poseBitmap")));
check("cut recovery destRect leftover recovery stays breath 0", /Keep recovery breath 0 so special-cancel K/.test(srcTxt) && !(/f\.phase === "recovery" && \(f\.cut === "slash" \|\| f\.cut === "golpe"\)/.test(sliceFn(codeOnly, "idleBreath"))));
check("cut recovery destRect leftover throw\/dart paths unchanged", /f\.cut === "throw" && f\.phase === "recovery"/.test(sliceFn(codeOnly, "idleBreath")) && /1 - throwPlantFade\(f\)/.test(sliceFn(codeOnly, "idleBreath")) && /f\.boltPhase === "recovery" && !holdingCutBolt\(f\)/.test(sliceFn(codeOnly, "idleBreath")) && /1 - boltPlantFade\(f\)/.test(sliceFn(codeOnly, "idleBreath")));
check("cut recovery destRect leftover sheatheFade still owns cut sheet", /f\.cut === "golpe" \|\| f\.cut === "slash"/.test(sliceFn(codeOnly, "sheatheFade")) && /!f\.clashRec && !f\.techRec/.test(sliceFn(codeOnly, "sheatheFade")) && /wasThrow/.test(sliceFn(codeOnly, "advanceAttack")) && /golpeSheathed/.test(sliceFn(codeOnly, "advanceAttack")) && /slashSheathed/.test(sliceFn(codeOnly, "advanceAttack")));
check("cut recovery destRect leftover tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("cut recovery destRect leftover is not a new combat verb", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.HITSTUN===350 && V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140);
check("slash frames still 180/140/280 after cut recovery destRect leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after cut recovery destRect leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after cut recovery destRect leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after cut recovery destRect leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after cut recovery destRect leftover", /game.js\?v=350/.test(htmlTxt));

// live: slash recovery stays breath 0; idle arm eases without dump
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.hp=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cut=""; V.player.cutRecBreathT=0;
V.rival.phase="idle"; V.rival.wantBlock=false; V.rival.revArmed=-1;
V.player.x=500; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
let cr232AmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.25){ cr232AmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.startAttack(V.player, "slash");
let cr232RecZero=false, cr232RecAmp=0, cr232RecBr=0, cr232RecSf=0;
let cr232Near=false, cr232Dy0=0, cr232Br0=0, cr232Aabb0=null;
let cr232Hop=999, cr232Idle=false, cr232ArmT=0, cr232ArmBr=0;
const cr232AabbArm = V.bodyAABB(V.player);
for(let i=0;i<120;i++){
  holdRivalStill();
  const sf=V.sheatheFade(V.player);
  const br=V.idleBreath(V.player);
  const amp=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(V.player.phase==="recovery" && V.player.cut==="slash" && !V.player.clashRec && sf>0.35 && sf<0.65 && !cr232RecZero){
    cr232RecZero=true; cr232RecSf=sf; cr232RecBr=br; cr232RecAmp=amp;
  }
  if(V.player.phase==="recovery" && V.player.cut==="slash" && V.player.phaseT>=V.RECOVERY-25 && !cr232Near){
    cr232Near=true; cr232Dy0=V.destRect(V.player).dy; cr232Br0=br; cr232Aabb0=V.bodyAABB(V.player);
  }
  const ph0=V.player.phase;
  const dy0=V.destRect(V.player).dy;
  V.update(STEP);
  if(ph0==="recovery" && V.player.phase==="idle"){
    cr232Idle=true;
    cr232Hop=Math.abs(V.destRect(V.player).dy - dy0);
    cr232ArmT=V.player.cutRecBreathT;
    cr232ArmBr=V.idleBreath(V.player);
    break;
  }
}
check("cut recovery destRect leftover live slash amp ready", cr232AmpReady, `ready=${cr232AmpReady} modeT=${V.modeT}`);
check("cut recovery destRect leftover live slash recovery breath still 0", cr232RecZero && Math.abs(cr232RecAmp)>0.9 && Math.abs(cr232RecBr)<0.05, `mid=${cr232RecZero} amp=${cr232RecAmp.toFixed(3)} sf=${cr232RecSf.toFixed(3)} br=${cr232RecBr.toFixed(3)}`);
check("cut recovery destRect leftover live slash near end", cr232Near && cr232Idle, `near=${cr232Near} idle=${cr232Idle} ph=${V.player.phase}`);
check("cut recovery destRect leftover live slash arms cutRecBreathT", cr232Idle && cr232ArmT===V.GUARD_RAISE_MS && Math.abs(cr232ArmBr)<0.05, `idle=${cr232Idle} t=${cr232ArmT} br=${cr232ArmBr.toFixed(3)} raise=${V.GUARD_RAISE_MS}`);
check("cut recovery destRect leftover live slash no breath dump at idle", cr232Idle && cr232Hop<0.85, `idle=${cr232Idle} hop=${cr232Hop.toFixed(3)} br0=${cr232Br0.toFixed(3)} br1=${cr232ArmBr.toFixed(3)}`);
check("cut recovery destRect leftover live slash AABB planted", cr232Aabb0 && Math.abs(V.bodyAABB(V.player).y-cr232Aabb0.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-cr232Aabb0.h)<0.05, `dy=${cr232Aabb0?(V.bodyAABB(V.player).y-cr232Aabb0.y).toFixed(2):"na"}`);
check("cut recovery destRect leftover live slash AABB planted from arm", Math.abs(V.bodyAABB(V.player).y-cr232AabbArm.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-cr232AabbArm.h)<0.05, `dy=${(V.bodyAABB(V.player).y-cr232AabbArm.y).toFixed(2)}`);
check("cut recovery destRect leftover live slash sheathe not rearmed", V.player.phase==="idle" && V.player.sheatheT===0, `ph=${V.player.phase} sh=${V.player.sheatheT}`);
// ease mid-window after idle
let cr232EaseBr=0, cr232EaseT=0, cr232EaseAmp=0;
for(let i=0;i<3;i++){ holdRivalStill(); V.update(STEP); }
cr232EaseT=V.player.cutRecBreathT;
cr232EaseBr=V.idleBreath(V.player);
cr232EaseAmp=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
const cr232EaseU = V.GUARD_RAISE_MS>0 ? Math.max(0, Math.min(1, cr232EaseT/V.GUARD_RAISE_MS)) : 0;
const cr232EaseExpect = cr232EaseAmp * (1 - cr232EaseU*cr232EaseU*(3-2*cr232EaseU));
check("cut recovery destRect leftover live slash breath eases after idle", cr232EaseT>0 && cr232EaseT<V.GUARD_RAISE_MS && Math.abs(cr232EaseBr-cr232EaseExpect)<0.25 && Math.abs(cr232EaseBr)>Math.abs(cr232EaseAmp)*0.15, `t=${cr232EaseT} br=${cr232EaseBr.toFixed(3)} expect=${cr232EaseExpect.toFixed(3)} amp=${cr232EaseAmp.toFixed(3)}`);

// live: golpe recovery shares the ease
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.hp=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cutRecBreathT=0;
V.rival.phase="idle"; V.player.x=500; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.25) break;
  holdRivalStill(); V.update(STEP);
}
V.startAttack(V.player, "golpe");
let cr232GRec=false, cr232GRecBr=0, cr232GRecAmp=0;
let cr232GHop=999, cr232GIdle=false, cr232GArmT=0, cr232GAabb0=null;
for(let i=0;i<120;i++){
  holdRivalStill();
  const sf=V.sheatheFade(V.player);
  const br=V.idleBreath(V.player);
  const amp=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(V.player.phase==="recovery" && V.player.cut==="golpe" && sf>0.35 && sf<0.65 && !cr232GRec){
    cr232GRec=true; cr232GRecBr=br; cr232GRecAmp=amp; cr232GAabb0=V.bodyAABB(V.player);
  }
  const ph0=V.player.phase;
  const dy0=V.destRect(V.player).dy;
  V.update(STEP);
  if(ph0==="recovery" && V.player.phase==="idle"){
    cr232GIdle=true; cr232GHop=Math.abs(V.destRect(V.player).dy-dy0); cr232GArmT=V.player.cutRecBreathT; break;
  }
}
check("cut recovery destRect leftover live golpe recovery breath still 0", cr232GRec && Math.abs(cr232GRecAmp)>0.9 && Math.abs(cr232GRecBr)<0.05, `mid=${cr232GRec} amp=${cr232GRecAmp.toFixed(3)} br=${cr232GRecBr.toFixed(3)}`);
check("cut recovery destRect leftover live golpe arms cutRecBreathT", cr232GIdle && cr232GArmT===V.GUARD_RAISE_MS, `idle=${cr232GIdle} t=${cr232GArmT}`);
check("cut recovery destRect leftover live golpe no breath dump at idle", cr232GIdle && cr232GHop<0.85, `idle=${cr232GIdle} hop=${cr232GHop.toFixed(3)}`);
check("cut recovery destRect leftover live golpe AABB planted", cr232GAabb0 && Math.abs(V.bodyAABB(V.player).y-cr232GAabb0.y)<0.05, `dy=${cr232GAabb0?(V.bodyAABB(V.player).y-cr232GAabb0.y).toFixed(2):"na"}`);

// live: rival slash recovery shares idleBreath path
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.rival.phase="idle"; V.rival.phaseT=0; V.rival.stunT=0; V.rival.guarding=false; V.rival.boltPhase=""; V.rival.falling=false;
V.rival.feintT=0; V.rival.sheatheT=0; V.rival.gait=0; V.rival.stamina=100; V.rival.hp=100;
V.rival.clashRec=false; V.rival.techRec=false; V.rival.wantBlock=false; V.rival.telegraph=false; V.rival.cutRecBreathT=0;
V.player.phase="idle"; V.player.guarding=false;
V.player.x=200; V.rival.x=700; V.rival.facing=-1; V.player.facing=1;
V.keepApart();
for(let i=0;i<500;i++){
  const a=Math.sin(((V.modeT+380)/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.25) break;
  holdRivalStill(); V.update(STEP);
}
V.startAttack(V.rival, "slash");
let cr232RRec=false, cr232RRecBr=0, cr232RRecAmp=0;
let cr232RHop=999, cr232RIdle=false, cr232RArmT=0;
for(let i=0;i<120;i++){
  holdRivalStill();
  const sf=V.sheatheFade(V.rival);
  const br=V.idleBreath(V.rival);
  const amp=Math.sin(((V.modeT+380)/2400)*Math.PI*2)*1.6;
  if(V.rival.phase==="recovery" && V.rival.cut==="slash" && sf>0.35 && sf<0.65 && !cr232RRec){
    cr232RRec=true; cr232RRecAmp=amp; cr232RRecBr=br;
  }
  const ph0=V.rival.phase;
  const dy0=V.destRect(V.rival).dy;
  V.update(STEP);
  if(ph0==="recovery" && V.rival.phase==="idle"){
    cr232RIdle=true; cr232RHop=Math.abs(V.destRect(V.rival).dy-dy0); cr232RArmT=V.rival.cutRecBreathT; break;
  }
}
check("cut recovery destRect leftover live rival recovery breath still 0", cr232RRec && Math.abs(cr232RRecAmp)>0.9 && Math.abs(cr232RRecBr)<0.05, `mid=${cr232RRec} amp=${cr232RRecAmp.toFixed(3)} br=${cr232RRecBr.toFixed(3)}`);
check("cut recovery destRect leftover live rival arms cutRecBreathT", cr232RIdle && cr232RArmT===V.GUARD_RAISE_MS, `idle=${cr232RIdle} t=${cr232RArmT}`);
check("cut recovery destRect leftover live rival no breath dump at idle", cr232RIdle && cr232RHop<0.85, `idle=${cr232RIdle} hop=${cr232RHop.toFixed(3)}`);

// live: clash recovery does not arm cutRecBreathT (sheatheT path)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="recovery"; V.player.phaseT=V.RECOVERY-5; V.player.cut="slash"; V.player.clashRec=true; V.player.techRec=false;
V.player.boltPhase=""; V.player.stunT=0; V.player.guarding=false; V.player.sheatheT=0; V.player.feintT=0; V.player.cutRecBreathT=0;
V.player.falling=false; V.player.hp=100; V.player.stamina=100;
holdRivalStill(); V.update(STEP);
check("cut recovery destRect leftover live clash does not arm cutRecBreathT", V.player.phase==="idle" && V.player.cutRecBreathT===0 && V.player.sheatheT===V.SHEATHE_MS, `ph=${V.player.phase} cbt=${V.player.cutRecBreathT} sh=${V.player.sheatheT}`);

// live: throw recovery still throwPlantFade (unchanged path)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.player.phase="recovery"; V.player.phaseT=40; V.player.cut="throw"; V.player.clashRec=false; V.player.techRec=false;
V.player.boltPhase=""; V.player.stunT=0; V.player.guarding=false; V.player.sheatheT=0; V.player.feintT=0; V.player.cutRecBreathT=0;
check("cut recovery destRect leftover live throw still throwPlantFade", V.throwPlantFade(V.player)>0.5 && V.sheatheFade(V.player)===0 && V.player.cutRecBreathT===0, `tf=${V.throwPlantFade(V.player)} sf=${V.sheatheFade(V.player)} cbt=${V.player.cutRecBreathT}`);

check("cache v=296 after cut recovery destRect leftover live", /game.js\?v=350/.test(htmlTxt));




// --- v233 clash sheathe destRect leftover: keep breath 0 through sheathe; ease with cutRecBreathT ---
check("clash sheathe destRect leftover keeps breath 0 through sheathe", /Clash sheathe destRect leftover/.test(srcTxt) && /clash\/tech \(and special-cancel from-cut\)/.test(srcTxt) && /scale amp by sheatheT\/SHEATHE_MS/.test(srcTxt) && /seated[\s\S]{0,40}full breath/.test(srcTxt) && /dumped again/.test(srcTxt) && /Keep breath 0 through sheathe/.test(srcTxt) && /prevSheathe > 0 && f\.sheatheT === 0/.test(sliceFn(codeOnly, "tickSheathe")) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickSheathe")) && /f\.sheatheT > 0/.test(sliceFn(codeOnly, "idleBreath")) && /return 0;/.test(sliceFn(codeOnly, "idleBreath")));
check("clash sheathe destRect leftover is destRect-only", /destRect-only/.test(srcTxt) && /idleBreath \/ cutRecBreathT/.test(srcTxt) && /sheatheDip hump unchanged/.test(srcTxt) && /cutRecBreathT/.test(sliceFn(codeOnly, "idleBreath")) && /cutRecBreathT/.test(sliceFn(codeOnly, "tickSheathe")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "destRect")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "bodyAABB")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "poseBitmap")));
check("clash sheathe destRect leftover drops sheatheT amp scale", !/amp \*= f\.sheatheT \/ SHEATHE_MS/.test(sliceFn(codeOnly, "idleBreath")) && /f\.sheatheT > 0/.test(sliceFn(codeOnly, "idleBreath")) && /return 0;/.test(sliceFn(codeOnly, "idleBreath")));
check("clash sheathe destRect leftover arms cutRecBreathT when sheathe ends", /prevSheathe > 0 && f\.sheatheT === 0/.test(sliceFn(codeOnly, "tickSheathe")) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickSheathe")) && !/!f\.guarding\)/.test(sliceFn(codeOnly, "tickSheathe")) && !/!walking\(f\)/.test(sliceFn(codeOnly, "tickSheathe")));
check("clash sheathe destRect leftover feint\/cut recovery paths unchanged", /f\.feintT > 0/.test(sliceFn(codeOnly, "idleBreath")) && /1 - feintFade\(f\)/.test(sliceFn(codeOnly, "idleBreath")) && /golpeSheathed \|\| slashSheathed/.test(sliceFn(codeOnly, "advanceAttack")) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "advanceAttack")) && /f\.cut === "throw" && f\.phase === "recovery"/.test(sliceFn(codeOnly, "idleBreath")) && /f\.boltPhase === "recovery" && !holdingCutBolt\(f\)/.test(sliceFn(codeOnly, "idleBreath")));
check("clash sheathe destRect leftover tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("clash sheathe destRect leftover is not a new combat verb", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.HITSTUN===350 && V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140);
check("slash frames still 180/140/280 after clash sheathe destRect leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after clash sheathe destRect leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after clash sheathe destRect leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after clash sheathe destRect leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after clash sheathe destRect leftover", /game.js\?v=350/.test(htmlTxt));

// live: clash recovery→idle keeps breath 0 (no seat on sheathe arm)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.hp=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cut=""; V.player.cutRecBreathT=0;
V.rival.phase="idle"; V.rival.wantBlock=false; V.rival.revArmed=-1;
V.player.x=500; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
let cs233AmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.25){ cs233AmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.startAttack(V.player, "slash");
V.startAttack(V.rival, "slash");
let cs233RecIdle=false, cs233RecHop=999, cs233RecBr0=0, cs233RecBr1=0, cs233RecSh=0, cs233RecCbt=0, cs233RecAabb=null;
let cs233SheatheEnd=false, cs233EndHop=999, cs233EndBr0=0, cs233EndBr1=0, cs233EndCbt=0, cs233EndAabb0=null;
let cs233EaseBr=0, cs233EaseT=0, cs233EaseAmp=0;
const cs233AabbArm = V.bodyAABB(V.player);
for(let i=0;i<160;i++){
  holdRivalStill();
  const ph0=V.player.phase;
  const br0=V.idleBreath(V.player);
  const dy0=V.destRect(V.player).dy;
  const sh0=V.player.sheatheT;
  const aabb0=V.bodyAABB(V.player);
  V.update(STEP);
  if(ph0==="recovery" && V.player.phase==="idle" && V.player.sheatheT===V.SHEATHE_MS && !cs233RecIdle){
    cs233RecIdle=true;
    cs233RecHop=Math.abs(V.destRect(V.player).dy - dy0);
    cs233RecBr0=br0;
    cs233RecBr1=V.idleBreath(V.player);
    cs233RecSh=V.player.sheatheT;
    cs233RecCbt=V.player.cutRecBreathT;
    cs233RecAabb=aabb0;
  }
  if(sh0>0 && V.player.sheatheT===0 && !cs233SheatheEnd){
    cs233SheatheEnd=true;
    cs233EndHop=Math.abs(V.destRect(V.player).dy - dy0);
    cs233EndBr0=br0;
    cs233EndBr1=V.idleBreath(V.player);
    cs233EndCbt=V.player.cutRecBreathT;
    cs233EndAabb0=aabb0;
    break;
  }
}
check("clash sheathe destRect leftover live amp ready", cs233AmpReady, `ready=${cs233AmpReady} modeT=${V.modeT}`);
check("clash sheathe destRect leftover live clash armed sheathe", cs233RecIdle && cs233RecSh===V.SHEATHE_MS && cs233RecCbt===0, `idle=${cs233RecIdle} sh=${cs233RecSh} cbt=${cs233RecCbt}`);
check("clash sheathe destRect leftover live recovery→idle breath stays 0", cs233RecIdle && Math.abs(cs233RecBr0)<0.05 && Math.abs(cs233RecBr1)<0.05, `idle=${cs233RecIdle} br0=${cs233RecBr0.toFixed(3)} br1=${cs233RecBr1.toFixed(3)}`);
check("clash sheathe destRect leftover live recovery→idle no breath hop", cs233RecIdle && cs233RecHop<0.85, `idle=${cs233RecIdle} hop=${cs233RecHop.toFixed(3)}`);
check("clash sheathe destRect leftover live recovery→idle AABB planted", cs233RecAabb && Math.abs(V.bodyAABB(V.player).y-cs233RecAabb.y)<0.05, `dy=${cs233RecAabb?(V.bodyAABB(V.player).y-cs233RecAabb.y).toFixed(2):"na"}`);
check("clash sheathe destRect leftover live sheathe end arms cutRecBreathT", cs233SheatheEnd && cs233EndCbt>0 && cs233EndCbt<=V.GUARD_RAISE_MS, `end=${cs233SheatheEnd} cbt=${cs233EndCbt} raise=${V.GUARD_RAISE_MS}`);
check("clash sheathe destRect leftover live sheathe end no breath dump", cs233SheatheEnd && Math.abs(cs233EndBr0)<0.05 && Math.abs(cs233EndBr1)<Math.abs(Math.sin((V.modeT/2400)*Math.PI*2)*1.6)*0.35, `end=${cs233SheatheEnd} br0=${cs233EndBr0.toFixed(3)} br1=${cs233EndBr1.toFixed(3)}`);
check("clash sheathe destRect leftover live sheathe end hop eased", cs233SheatheEnd && cs233EndHop<1.2, `end=${cs233SheatheEnd} hop=${cs233EndHop.toFixed(3)}`);
check("clash sheathe destRect leftover live sheathe end AABB planted", cs233EndAabb0 && Math.abs(V.bodyAABB(V.player).y-cs233EndAabb0.y)<0.05 && Math.abs(V.bodyAABB(V.player).y-cs233AabbArm.y)<0.05, `dy=${cs233EndAabb0?(V.bodyAABB(V.player).y-cs233EndAabb0.y).toFixed(2):"na"}`);
// ease mid-window after sheathe end
for(let i=0;i<2;i++){ holdRivalStill(); V.update(STEP); }
cs233EaseT=V.player.cutRecBreathT;
cs233EaseBr=V.idleBreath(V.player);
cs233EaseAmp=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
const cs233EaseU = V.GUARD_RAISE_MS>0 ? Math.max(0, Math.min(1, cs233EaseT/V.GUARD_RAISE_MS)) : 0;
const cs233EaseExpect = cs233EaseAmp * (1 - cs233EaseU*cs233EaseU*(3-2*cs233EaseU));
check("clash sheathe destRect leftover live breath eases after sheathe", cs233EaseT>0 && cs233EaseT<V.GUARD_RAISE_MS && Math.abs(cs233EaseBr-cs233EaseExpect)<0.25, `t=${cs233EaseT} br=${cs233EaseBr.toFixed(3)} expect=${cs233EaseExpect.toFixed(3)} amp=${cs233EaseAmp.toFixed(3)}`);

// live: non-clash cut recovery still arms cutRecBreathT at idle (unchanged)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.hp=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cutRecBreathT=0;
V.rival.phase="idle"; V.player.x=500; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
V.startAttack(V.player, "slash");
let cs233CutArm=false, cs233CutArmT=0, cs233CutSh=0;
for(let i=0;i<120;i++){
  holdRivalStill();
  const ph0=V.player.phase;
  V.update(STEP);
  if(ph0==="recovery" && V.player.phase==="idle"){
    cs233CutArm=true; cs233CutArmT=V.player.cutRecBreathT; cs233CutSh=V.player.sheatheT; break;
  }
}
check("clash sheathe destRect leftover live cut recovery still arms cutRecBreathT", cs233CutArm && cs233CutArmT===V.GUARD_RAISE_MS && cs233CutSh===0, `arm=${cs233CutArm} t=${cs233CutArmT} sh=${cs233CutSh}`);

// live: feint end still settles without cutRecBreathT from sheathe drain
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase="";
V.player.feintT=0; V.player.sheatheT=0; V.player.cutRecBreathT=0; V.player.hp=100; V.player.stamina=100;
V.player.x=500; V.rival.x=1100; V.keepApart();
V.startAttack(V.player, "slash");
V.startFeint(V.player);
let cs233FeintEnd=false, cs233FeintCbt=0, cs233FeintHop=999, cs233FeintBr1=0;
for(let i=0;i<40;i++){
  holdRivalStill();
  const ft0=V.player.feintT;
  const dy0=V.destRect(V.player).dy;
  V.update(STEP);
  if(ft0>0 && V.player.feintT===0){
    cs233FeintEnd=true;
    cs233FeintCbt=V.player.cutRecBreathT;
    cs233FeintHop=Math.abs(V.destRect(V.player).dy-dy0);
    cs233FeintBr1=V.idleBreath(V.player);
    break;
  }
}
check("clash sheathe destRect leftover live feint end skips cutRecBreathT arm", cs233FeintEnd && cs233FeintCbt===0, `end=${cs233FeintEnd} cbt=${cs233FeintCbt}`);
check("clash sheathe destRect leftover live feint end hop still eased", cs233FeintEnd && cs233FeintHop<0.85, `end=${cs233FeintEnd} hop=${cs233FeintHop.toFixed(3)} br=${cs233FeintBr1.toFixed(3)}`);

// live: rival clash sheathe same path
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.rival.phase="idle";
V.player.x=500; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.player.sheatheT=0; V.rival.sheatheT=0; V.player.cutRecBreathT=0; V.rival.cutRecBreathT=0;
V.player.hp=100; V.rival.hp=100; V.player.stamina=100; V.rival.stamina=100;
V.keepApart();
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.25) break;
  holdRivalStill(); V.update(STEP);
}
V.startAttack(V.player, "slash");
V.startAttack(V.rival, "slash");
let cs233RRec=false, cs233RRecBr=0, cs233REnd=false, cs233REndCbt=0, cs233REndBr=0;
for(let i=0;i<160;i++){
  holdRivalStill();
  const ph0=V.rival.phase;
  const sh0=V.rival.sheatheT;
  const br0=V.idleBreath(V.rival);
  V.update(STEP);
  if(ph0==="recovery" && V.rival.phase==="idle" && V.rival.sheatheT===V.SHEATHE_MS && !cs233RRec){
    cs233RRec=true; cs233RRecBr=V.idleBreath(V.rival);
  }
  if(sh0>0 && V.rival.sheatheT===0 && !cs233REnd){
    cs233REnd=true; cs233REndCbt=V.rival.cutRecBreathT; cs233REndBr=V.idleBreath(V.rival); break;
  }
}
check("clash sheathe destRect leftover live rival recovery breath 0", cs233RRec && Math.abs(cs233RRecBr)<0.05, `rec=${cs233RRec} br=${cs233RRecBr.toFixed(3)}`);
check("clash sheathe destRect leftover live rival sheathe end arms cutRecBreathT", cs233REnd && cs233REndCbt>0 && cs233REndCbt<=V.GUARD_RAISE_MS && Math.abs(cs233REndBr)<0.5, `end=${cs233REnd} cbt=${cs233REndCbt} br=${cs233REndBr.toFixed(3)}`);

check("cache v=296 after clash sheathe destRect leftover live", /game.js\?v=350/.test(htmlTxt));




// --- v234 cut recovery settle→guard destRect leftover: hold cutRec through raise ---
check("cut recovery settle→guard destRect leftover seats no breath mid-ease", /Cut recovery settle→guard destRect leftover/.test(srcTxt) && /leftover cutRecBreath used to/.test(srcTxt) && /seat ~0\.8 the tick S raised mid-ease/.test(srcTxt) && /tickSheathe zeroed cutRecBreathT on/.test(srcTxt) && /Same[\s\S]{0,40}hole idle settle→guard/.test(srcTxt) && /Math\.max\(ck, rk\)/.test(sliceFn(codeOnly, "idleBreath")) && /tickCutRecBreath/.test(srcTxt));
check("cut recovery settle→guard destRect leftover is destRect-only", /destRect-only/.test(srcTxt) && /idleBreath \/ cutRecBreathT/.test(srcTxt) && /Keep cutRecBreathT through raise/.test(srcTxt) && /Math\.max\(ck, rk\)/.test(sliceFn(codeOnly, "idleBreath")) && /function tickCutRecBreath/.test(srcTxt) && /tickCutRecBreath\(player/.test(srcTxt) && /tickCutRecBreath\(rival/.test(srcTxt) && !/cutRecBreathT/.test(sliceFn(codeOnly, "destRect")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "bodyAABB")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "poseBitmap")) && !/tickCutRecBreath/.test(sliceFn(codeOnly, "destRect")) && !/tickCutRecBreath/.test(sliceFn(codeOnly, "bodyAABB")) && !/tickCutRecBreath/.test(sliceFn(codeOnly, "poseBitmap")));
check("cut recovery settle→guard destRect leftover keeps max cutRec\/raise", /const ck = u \* u \* \(3 - 2 \* u\)/.test(sliceFn(codeOnly, "idleBreath")) && /const rk = guardRaiseK\(f\)/.test(sliceFn(codeOnly, "idleBreath")) && /1 - Math\.max\(ck, rk\)/.test(sliceFn(codeOnly, "idleBreath")) && /f\.guarding \|\| f\.guardPoseK > 0/.test(sliceFn(codeOnly, "idleBreath")));
check("cut recovery settle→guard destRect leftover holds cutRec under raise", /function tickCutRecBreath/.test(srcTxt) && /guardRaiseK\(f\) >= ck/.test(sliceFn(codeOnly, "tickCutRecBreath")) && /f\.guarding \|\| f\.guardPoseK > 0/.test(sliceFn(codeOnly, "tickCutRecBreath")) && /updateGuard\(player/.test(srcTxt) && /tickCutRecBreath\(player/.test(srcTxt));
check("cut recovery settle→guard destRect leftover idle settle path unchanged", /Idle cut recovery settle still eases when not guarding/.test(srcTxt) && /return amp \* \(1 - ck\)/.test(sliceFn(codeOnly, "idleBreath")) && /f\.cutRecBreathT > 0/.test(sliceFn(codeOnly, "idleBreath")));
check("cut recovery settle→guard destRect leftover pure idle→guard still eases", /Pure idle→guard still eases with raise/.test(srcTxt) && /if \(f\.guarding \|\| f\.guardPoseK > 0\) return amp \* \(1 - guardRaiseK\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")));
check("cut recovery settle→guard destRect leftover clash sheathe \/ cut recovery arm unchanged", /prevSheathe > 0 && f\.sheatheT === 0/.test(sliceFn(codeOnly, "tickSheathe")) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickSheathe")) && /golpeSheathed \|\| slashSheathed/.test(sliceFn(codeOnly, "advanceAttack")) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "advanceAttack")) && /f\.sheatheT > 0/.test(sliceFn(codeOnly, "idleBreath")) && /return 0;/.test(sliceFn(codeOnly, "idleBreath")));
check("cut recovery settle→guard destRect leftover tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("cut recovery settle→guard destRect leftover is not a new combat verb", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.HITSTUN===350 && V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140);
check("slash frames still 180/140/280 after cut recovery settle→guard destRect leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after cut recovery settle→guard destRect leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after cut recovery settle→guard destRect leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after cut recovery settle→guard destRect leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after cut recovery settle→guard destRect leftover", /game.js\?v=350/.test(htmlTxt));

// live: S mid-cutRecBreath holds suppress (no seat), AABB planted
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.hp=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cut=""; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.walkSettleT=0;
V.rival.phase="idle"; V.rival.wantBlock=false; V.rival.revArmed=-1;
V.player.x=500; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
let crg234AmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ crg234AmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.cutRecBreathT = V.GUARD_RAISE_MS * 0.7;
V.player.guarding=false; V.player.guardPoseK=0; V.player.phase="idle"; V.player.sheatheT=0; V.player.feintT=0; V.player.boltPhase="";
const crg234Br0=V.idleBreath(V.player);
const crg234Aabb0=V.bodyAABB(V.player).y;
const crg234Cbt0=V.player.cutRecBreathT;
V.keys.add("KeyS");
let crg234MaxSeat=0, crg234MaxAbsBr=Math.abs(crg234Br0), crg234Held=false, crg234Cleared=false, crg234EndBr=0, crg234AabbOk=true;
for(let i=0;i<10;i++){
  holdRivalStill(); V.update(STEP);
  const br=V.idleBreath(V.player);
  const aabb=V.bodyAABB(V.player).y;
  if(Math.abs(aabb-crg234Aabb0)>0.05) crg234AabbOk=false;
  crg234MaxAbsBr=Math.max(crg234MaxAbsBr, Math.abs(br));
  crg234MaxSeat=Math.max(crg234MaxSeat, Math.abs(br)-Math.abs(crg234Br0));
  if(V.player.guarding && V.player.cutRecBreathT===crg234Cbt0) crg234Held=true;
  if(V.player.guardPoseK>=1 && V.player.cutRecBreathT===0){ crg234Cleared=true; crg234EndBr=br; break; }
}
check("cut recovery settle→guard destRect leftover live amp ready", crg234AmpReady, `ready=${crg234AmpReady} modeT=${V.modeT}`);
check("cut recovery settle→guard destRect leftover live holds cutRec through raise", crg234Held && V.player.guarding, `held=${crg234Held} g=${V.player.guarding} cbt=${V.player.cutRecBreathT}`);
check("cut recovery settle→guard destRect leftover live no breath seat", crg234MaxSeat<0.35 && crg234MaxAbsBr<Math.abs(crg234Br0)+0.4, `seat=${crg234MaxSeat.toFixed(3)} maxBr=${crg234MaxAbsBr.toFixed(3)} br0=${crg234Br0.toFixed(3)}`);
check("cut recovery settle→guard destRect leftover live raise clears cutRec", crg234Cleared && Math.abs(crg234EndBr)<0.05 && V.guardRaiseK(V.player)>=0.99, `cleared=${crg234Cleared} endBr=${crg234EndBr.toFixed(3)} rk=${V.guardRaiseK(V.player).toFixed(3)}`);
check("cut recovery settle→guard destRect leftover live AABB planted", crg234AabbOk, `aabbOk=${crg234AabbOk}`);

// live: idle cutRec still eases when not guarding (unchanged)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.hp=100; V.player.stamina=100;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=500; V.rival.x=1100;
let crg234IdleAmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ crg234IdleAmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.cutRecBreathT = V.GUARD_RAISE_MS;
let crg234IdleBr0=V.idleBreath(V.player), crg234IdleBr1=crg234IdleBr0, crg234IdleUp=true;
for(let i=0;i<6;i++){
  holdRivalStill(); V.update(STEP);
  const br=V.idleBreath(V.player);
  if(Math.abs(br)+0.08<Math.abs(crg234IdleBr1)) crg234IdleUp=false;
  crg234IdleBr1=br;
}
check("cut recovery settle→guard destRect leftover live idle ease still climbs", crg234IdleAmpReady && crg234IdleUp && Math.abs(crg234IdleBr0)<0.2 && Math.abs(crg234IdleBr1)>0.9 && V.player.cutRecBreathT===0, `ready=${crg234IdleAmpReady} up=${crg234IdleUp} br0=${crg234IdleBr0.toFixed(3)} br1=${crg234IdleBr1.toFixed(3)} cbt=${V.player.cutRecBreathT}`);

// rival shares idleBreath max(ck,rk) + tickCutRecBreath (no kind gate)
check("cut recovery settle→guard destRect leftover rival idleBreath shared", /Math\.max\(ck, rk\)/.test(sliceFn(codeOnly, "idleBreath")) && /tickCutRecBreath\(rival/.test(srcTxt) && /function tickCutRecBreath/.test(srcTxt) && !/f\.kind === "you"/.test(sliceFn(codeOnly, "tickCutRecBreath")));

check("cache v=296 after cut recovery settle→guard destRect leftover live", /game.js\?v=350/.test(htmlTxt));





// --- v235 cut recovery settle→walk destRect leftover: hold cutRec through rise ---
check("cut recovery settle→walk destRect leftover seats no breath mid-stride", /Cut recovery settle→walk destRect leftover/.test(srcTxt) && /leftover cutRecBreath used to/.test(srcTxt) && /seat ~0\.6 mid-stride/.test(srcTxt) && /preferred cutRec k over walkRiseK/.test(srcTxt) && /Same[\s\S]{0,40}hole cut recovery[\s\S]{0,20}settle→guard/.test(srcTxt) && /Math\.max\(ck, wk\)/.test(sliceFn(codeOnly, "idleBreath")) && /walkRiseK\(f\)/.test(sliceFn(codeOnly, "idleBreath")));
check("cut recovery settle→walk destRect leftover is destRect-only", /destRect-only/.test(srcTxt) && /idleBreath \/ cutRecBreathT/.test(srcTxt) && /Keep cutRecBreathT through rise/.test(srcTxt) && /Math\.max\(ck, wk\)/.test(sliceFn(codeOnly, "idleBreath")) && /walkRiseK\(f\) >= ck/.test(sliceFn(codeOnly, "tickCutRecBreath")) && /walking\(f\)/.test(sliceFn(codeOnly, "tickCutRecBreath")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "destRect")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "bodyAABB")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "poseBitmap")) && !/tickCutRecBreath/.test(sliceFn(codeOnly, "destRect")) && !/tickCutRecBreath/.test(sliceFn(codeOnly, "bodyAABB")) && !/tickCutRecBreath/.test(sliceFn(codeOnly, "poseBitmap")));
check("cut recovery settle→walk destRect leftover keeps max cutRec\/rise", /const ck = u \* u \* \(3 - 2 \* u\)/.test(sliceFn(codeOnly, "idleBreath")) && /const wk = walkRiseK\(f\)/.test(sliceFn(codeOnly, "idleBreath")) && /1 - Math\.max\(ck, wk\)/.test(sliceFn(codeOnly, "idleBreath")) && /if \(walking\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")));
check("cut recovery settle→walk destRect leftover holds cutRec under rise", /function tickCutRecBreath/.test(srcTxt) && /walkRiseK\(f\) >= ck/.test(sliceFn(codeOnly, "tickCutRecBreath")) && /else if \(walking\(f\)\)/.test(sliceFn(codeOnly, "tickCutRecBreath")) && /tickCutRecBreath\(player/.test(srcTxt) && /tickCutRecBreath\(rival/.test(srcTxt));
check("cut recovery settle→walk destRect leftover idle settle path unchanged", /Idle cut recovery settle still eases when not walking/.test(srcTxt) && /return amp \* \(1 - ck\)/.test(sliceFn(codeOnly, "idleBreath")) && /f\.cutRecBreathT > 0/.test(sliceFn(codeOnly, "idleBreath")));
check("cut recovery settle→walk destRect leftover pure idle→walk still eases", /Pure idle→walk still eases with rise/.test(srcTxt) && /if \(walking\(f\)\) return amp \* \(1 - walkRiseK\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")));
check("cut recovery settle→walk destRect leftover settle→guard unchanged", /Math\.max\(ck, rk\)/.test(sliceFn(codeOnly, "idleBreath")) && /guardRaiseK\(f\) >= ck/.test(sliceFn(codeOnly, "tickCutRecBreath")) && /f\.guarding \|\| f\.guardPoseK > 0/.test(sliceFn(codeOnly, "tickCutRecBreath")) && /Cut recovery settle→guard unchanged/.test(srcTxt));
check("cut recovery settle→walk destRect leftover clash sheathe \/ cut recovery arm unchanged", /prevSheathe > 0 && f\.sheatheT === 0/.test(sliceFn(codeOnly, "tickSheathe")) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickSheathe")) && /golpeSheathed \|\| slashSheathed/.test(sliceFn(codeOnly, "advanceAttack")) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "advanceAttack")) && /f\.sheatheT > 0/.test(sliceFn(codeOnly, "idleBreath")) && /return 0;/.test(sliceFn(codeOnly, "idleBreath")));
check("cut recovery settle→walk destRect leftover tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("cut recovery settle→walk destRect leftover is not a new combat verb", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.HITSTUN===350 && V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140 && V.WALK_SETTLE_MS===160);
check("slash frames still 180/140/280 after cut recovery settle→walk destRect leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after cut recovery settle→walk destRect leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after cut recovery settle→walk destRect leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after cut recovery settle→walk destRect leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after cut recovery settle→walk destRect leftover", /game.js\?v=350/.test(htmlTxt));

// live: walk through cutRec keeps suppress (no mid-stride seat), AABB planted
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.hp=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cut=""; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.walkSettleT=0; V.player.walkRiseT=0;
V.rival.phase="idle"; V.rival.wantBlock=false; V.rival.revArmed=-1;
V.player.x=200; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
let crw235AmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ crw235AmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
// rise fully first
V.keys.add("KeyD");
for(let i=0;i<20;i++){ holdRivalStill(); V.update(STEP); }
const crw235RiseReady = V.player.gait===1 && Math.abs(V.idleBreath(V.player))<0.05;
V.player.cutRecBreathT = V.GUARD_RAISE_MS;
V.player.guarding=false; V.player.guardPoseK=0; V.player.phase="idle"; V.player.sheatheT=0; V.player.feintT=0; V.player.boltPhase="";
const crw235Br0=V.idleBreath(V.player);
const crw235Aabb0=V.bodyAABB(V.player).y;
let crw235MaxAbsBr=Math.abs(crw235Br0), crw235Cleared=false, crw235EndBr=0, crw235AabbOk=true, crw235DyHop=0;
for(let i=0;i<12;i++){
  holdRivalStill();
  const dy0=V.destRect(V.player).dy;
  const c0=V.player.cutRecBreathT;
  V.update(STEP);
  const br=V.idleBreath(V.player);
  const aabb=V.bodyAABB(V.player).y;
  if(Math.abs(aabb-crw235Aabb0)>0.05) crw235AabbOk=false;
  crw235MaxAbsBr=Math.max(crw235MaxAbsBr, Math.abs(br));
  crw235DyHop=Math.max(crw235DyHop, Math.abs(V.destRect(V.player).dy-dy0));
  if(c0>0 && V.player.cutRecBreathT===0){ crw235Cleared=true; crw235EndBr=br; break; }
}
check("cut recovery settle→walk destRect leftover live amp ready", crw235AmpReady, `ready=${crw235AmpReady} modeT=${V.modeT}`);
check("cut recovery settle→walk destRect leftover live rise ready", crw235RiseReady && V.player.gait===1, `rise=${crw235RiseReady} gait=${V.player.gait} br0=${crw235Br0.toFixed(3)}`);
check("cut recovery settle→walk destRect leftover live no mid-stride seat", crw235MaxAbsBr<0.35, `maxBr=${crw235MaxAbsBr.toFixed(3)} br0=${crw235Br0.toFixed(3)}`);
check("cut recovery settle→walk destRect leftover live rise clears cutRec", crw235Cleared && Math.abs(crw235EndBr)<0.05 && V.player.cutRecBreathT===0, `cleared=${crw235Cleared} endBr=${crw235EndBr.toFixed(3)} cbt=${V.player.cutRecBreathT}`);
check("cut recovery settle→walk destRect leftover live no breath dump hop", crw235DyHop<0.85, `hop=${crw235DyHop.toFixed(3)}`);
check("cut recovery settle→walk destRect leftover live AABB planted", crw235AabbOk, `aabbOk=${crw235AabbOk}`);

// live: idle cutRec still eases when not walking (unchanged)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.gait=0; V.player.hp=100; V.player.stamina=100;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=500; V.rival.x=1100;
let crw235IdleAmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ crw235IdleAmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.cutRecBreathT = V.GUARD_RAISE_MS;
let crw235IdleBr0=V.idleBreath(V.player), crw235IdleBr1=crw235IdleBr0, crw235IdleUp=true;
for(let i=0;i<6;i++){
  holdRivalStill(); V.update(STEP);
  const br=V.idleBreath(V.player);
  if(Math.abs(br)+0.08<Math.abs(crw235IdleBr1)) crw235IdleUp=false;
  crw235IdleBr1=br;
}
check("cut recovery settle→walk destRect leftover live idle ease still climbs", crw235IdleAmpReady && crw235IdleUp && Math.abs(crw235IdleBr0)<0.2 && Math.abs(crw235IdleBr1)>0.9 && V.player.cutRecBreathT===0 && V.player.gait===0, `ready=${crw235IdleAmpReady} up=${crw235IdleUp} br0=${crw235IdleBr0.toFixed(3)} br1=${crw235IdleBr1.toFixed(3)} cbt=${V.player.cutRecBreathT}`);

// live: settle→guard still holds (regression)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.hp=100;
V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.walkSettleT=0;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=500; V.rival.x=1100;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2) break;
  holdRivalStill(); V.update(STEP);
}
V.player.cutRecBreathT = V.GUARD_RAISE_MS * 0.7;
const crw235GBr0=V.idleBreath(V.player);
V.keys.add("KeyS");
let crw235GMaxSeat=0, crw235GCleared=false, crw235GEndBr=0;
for(let i=0;i<10;i++){
  holdRivalStill(); V.update(STEP);
  const br=V.idleBreath(V.player);
  crw235GMaxSeat=Math.max(crw235GMaxSeat, Math.abs(br)-Math.abs(crw235GBr0));
  if(V.player.guardPoseK>=1 && V.player.cutRecBreathT===0){ crw235GCleared=true; crw235GEndBr=br; break; }
}
check("cut recovery settle→walk destRect leftover live guard path still holds", V.player.guarding && crw235GMaxSeat<0.35 && crw235GCleared && Math.abs(crw235GEndBr)<0.05, `g=${V.player.guarding} seat=${crw235GMaxSeat.toFixed(3)} cleared=${crw235GCleared} endBr=${crw235GEndBr.toFixed(3)}`);

// rival shares idleBreath max(ck,wk) + tickCutRecBreath walk branch (no kind gate)
check("cut recovery settle→walk destRect leftover rival idleBreath shared", /Math\.max\(ck, wk\)/.test(sliceFn(codeOnly, "idleBreath")) && /walkRiseK\(f\) >= ck/.test(sliceFn(codeOnly, "tickCutRecBreath")) && /tickCutRecBreath\(rival/.test(srcTxt) && !/f\.kind === "you"/.test(sliceFn(codeOnly, "tickCutRecBreath")));

check("cache v=296 after cut recovery settle→walk destRect leftover live", /game.js\?v=350/.test(htmlTxt));






// --- v236 feint settle→guard destRect leftover: max(feintFade, raise k) ---
check("feint settle→guard destRect leftover seats no breath mid-pull", /Feint settle→guard destRect leftover/.test(srcTxt) && /leftover feintFade breath used to/.test(srcTxt) && /seat ~0\.65 the tick S raised mid-pull/.test(srcTxt) && /idleBreath used only feintFade k/.test(srcTxt) && /Same[\s\S]{0,40}hole cut recovery[\s\S]{0,20}settle→guard/.test(srcTxt) && /Math\.max\(fk, rk\)/.test(sliceFn(codeOnly, "idleBreath")) && /feintFade\(f\)/.test(sliceFn(codeOnly, "idleBreath")));
check("feint settle→guard destRect leftover is destRect-only", /destRect-only/.test(srcTxt) && /idleBreath/.test(srcTxt) && /Math\.max\(fk, rk\)/.test(sliceFn(codeOnly, "idleBreath")) && /f\.feintT > 0/.test(sliceFn(codeOnly, "idleBreath")) && !/Math\.max\(fk, rk\)/.test(sliceFn(codeOnly, "destRect")) && !/Math\.max\(fk, rk\)/.test(sliceFn(codeOnly, "bodyAABB")) && !/Math\.max\(fk, rk\)/.test(sliceFn(codeOnly, "poseBitmap")) && !/Math\.max\(fk, rk\)/.test(sliceFn(codeOnly, "feintFade")) && !/Math\.max\(fk, rk\)/.test(sliceFn(codeOnly, "tickFeint")));
check("feint settle→guard destRect leftover keeps max feint\/raise", /const fk = feintFade\(f\)/.test(sliceFn(codeOnly, "idleBreath")) && /const rk = guardRaiseK\(f\)/.test(sliceFn(codeOnly, "idleBreath")) && /1 - Math\.max\(fk, rk\)/.test(sliceFn(codeOnly, "idleBreath")) && /f\.guarding \|\| f\.guardPoseK > 0/.test(sliceFn(codeOnly, "idleBreath")));
check("feint settle→guard destRect leftover idle feint path unchanged", /Idle feint still eases with feintFade/.test(srcTxt) && /return amp \* \(1 - feintFade\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /f\.feintT > 0/.test(sliceFn(codeOnly, "idleBreath")));
check("feint settle→guard destRect leftover pure idle→guard still eases", /Pure idle→guard still eases with raise/.test(srcTxt) && /if \(f\.guarding \|\| f\.guardPoseK > 0\) return amp \* \(1 - guardRaiseK\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")));
check("feint settle→guard destRect leftover sheet leftover unchanged", /Feint→guard sheet leftover unchanged/.test(srcTxt) && /feintFade[\s\S]{0,30}keeps windup through raise/.test(srcTxt) && /Keep leftover windup through leftover feintT if they raise/.test(srcTxt) && /poseBitmap still block once guarding/.test(srcTxt) && !/if \(f\.guarding\) return 0/.test(sliceFn(codeOnly, "feintFade")));
check("feint settle→guard destRect leftover FEINT_RECOVERY unchanged", /FEINT_RECOVERY \/ combat lock unchanged/.test(srcTxt) && V.FEINT_RECOVERY===100 && /f\.feintT = FEINT_RECOVERY/.test(srcTxt) && /f\.feintT = Math\.max\(0, f\.feintT - dt\)/.test(sliceFn(codeOnly, "tickFeint")));
check("feint settle→guard destRect leftover cutRec settle paths unchanged", /Math\.max\(ck, rk\)/.test(sliceFn(codeOnly, "idleBreath")) && /Math\.max\(ck, wk\)/.test(sliceFn(codeOnly, "idleBreath")) && /guardRaiseK\(f\) >= ck/.test(sliceFn(codeOnly, "tickCutRecBreath")) && /walkRiseK\(f\) >= ck/.test(sliceFn(codeOnly, "tickCutRecBreath")));
check("feint settle→guard destRect leftover tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("feint settle→guard destRect leftover is not a new combat verb", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.HITSTUN===350 && V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140 && V.FEINT_RECOVERY===100);
check("slash frames still 180/140/280 after feint settle→guard destRect leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after feint settle→guard destRect leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after feint settle→guard destRect leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after feint settle→guard destRect leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after feint settle→guard destRect leftover", /game.js\?v=350/.test(htmlTxt));

// live: raise mid-feint keeps suppress (no seat / dump), AABB planted
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.hp=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cut=""; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.walkSettleT=0;
V.rival.phase="idle"; V.rival.wantBlock=false; V.rival.revArmed=-1;
V.player.x=500; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
let fsg236AmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ fsg236AmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.startAttack(V.player, "slash");
holdRivalStill(); V.update(STEP);
const fsg236DidFeint = V.startFeint(V.player);
while(V.player.feintT > 50){ holdRivalStill(); V.update(STEP); }
V.player.guarding=false; V.player.guardPoseK=0;
const fsg236Br0=V.idleBreath(V.player);
const fsg236Ff0=V.feintFade(V.player);
const fsg236Aabb0=V.bodyAABB(V.player).y;
const fsg236Ft0=V.player.feintT;
V.keys.add("KeyS");
let fsg236MaxSeat=0, fsg236MaxAbsBr=Math.abs(fsg236Br0), fsg236Held=false, fsg236EndBr=0, fsg236AabbOk=true, fsg236DyHop=0, fsg236Cleared=false;
for(let i=0;i<12;i++){
  holdRivalStill();
  const dy0=V.destRect(V.player).dy;
  V.update(STEP);
  const br=V.idleBreath(V.player);
  const aabb=V.bodyAABB(V.player).y;
  if(Math.abs(aabb-fsg236Aabb0)>0.05) fsg236AabbOk=false;
  fsg236MaxAbsBr=Math.max(fsg236MaxAbsBr, Math.abs(br));
  fsg236MaxSeat=Math.max(fsg236MaxSeat, Math.abs(br)-Math.abs(fsg236Br0));
  fsg236DyHop=Math.max(fsg236DyHop, Math.abs(V.destRect(V.player).dy-dy0));
  if(V.player.guarding && V.player.feintT>0) fsg236Held=true;
  if(V.player.guardPoseK>=1 && V.player.feintT===0){ fsg236Cleared=true; fsg236EndBr=br; break; }
}
check("feint settle→guard destRect leftover live amp ready", fsg236AmpReady && fsg236DidFeint, `ready=${fsg236AmpReady} did=${fsg236DidFeint} modeT=${V.modeT}`);
check("feint settle→guard destRect leftover live mid-pull ready", fsg236Ft0>0 && fsg236Ft0<=55 && fsg236Ff0>0.4 && fsg236Ff0<0.6, `ft0=${fsg236Ft0.toFixed(1)} ff0=${fsg236Ff0.toFixed(3)} br0=${fsg236Br0.toFixed(3)}`);
check("feint settle→guard destRect leftover live holds feint through raise", fsg236Held && V.player.guarding, `held=${fsg236Held} g=${V.player.guarding} ft=${V.player.feintT}`);
check("feint settle→guard destRect leftover live no breath seat", fsg236MaxSeat<0.45 && fsg236MaxAbsBr<Math.abs(fsg236Br0)+0.5, `seat=${fsg236MaxSeat.toFixed(3)} maxBr=${fsg236MaxAbsBr.toFixed(3)} br0=${fsg236Br0.toFixed(3)}`);
check("feint settle→guard destRect leftover live no breath dump", fsg236MaxSeat<0.45 && fsg236MaxAbsBr<1.15, `seat=${fsg236MaxSeat.toFixed(3)} maxBr=${fsg236MaxAbsBr.toFixed(3)}`);
check("feint settle→guard destRect leftover live raise clears breath", fsg236Cleared && Math.abs(fsg236EndBr)<0.05 && V.guardRaiseK(V.player)>=0.99, `cleared=${fsg236Cleared} endBr=${fsg236EndBr.toFixed(3)} rk=${V.guardRaiseK(V.player).toFixed(3)}`);
check("feint settle→guard destRect leftover live AABB planted", fsg236AabbOk, `aabbOk=${fsg236AabbOk}`);

// live: idle feint still eases when not guarding (unchanged)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.hp=100; V.player.stamina=100;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=500; V.rival.x=1100;
let fsg236IdleAmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ fsg236IdleAmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.startAttack(V.player, "slash");
holdRivalStill(); V.update(STEP);
V.startFeint(V.player);
const fsg236IdleBr0=V.idleBreath(V.player);
let fsg236IdleBr1=fsg236IdleBr0, fsg236IdleUp=true;
for(let i=0;i<6;i++){
  holdRivalStill(); V.update(STEP);
  const br=V.idleBreath(V.player);
  if(Math.abs(br)+0.05<Math.abs(fsg236IdleBr1)) fsg236IdleUp=false;
  fsg236IdleBr1=br;
}
check("feint settle→guard destRect leftover live idle feint still climbs", fsg236IdleAmpReady && fsg236IdleUp && Math.abs(fsg236IdleBr0)<0.25 && Math.abs(fsg236IdleBr1)>0.7 && V.player.feintT===0 && V.player.guarding===false, `ready=${fsg236IdleAmpReady} up=${fsg236IdleUp} br0=${fsg236IdleBr0.toFixed(3)} br1=${fsg236IdleBr1.toFixed(3)} ft=${V.player.feintT}`);

// rival shares idleBreath max(fk,rk) (no kind gate)
check("feint settle→guard destRect leftover rival idleBreath shared", /Math\.max\(fk, rk\)/.test(sliceFn(codeOnly, "idleBreath")) && /const fk = feintFade\(f\)/.test(sliceFn(codeOnly, "idleBreath")) && /const rk = guardRaiseK\(f\)/.test(sliceFn(codeOnly, "idleBreath")) && /f\.feintT > 0/.test(sliceFn(codeOnly, "idleBreath")) && /startFeint\(f\)/.test(srcTxt));

check("cache v=296 after feint settle→guard destRect leftover live", /game.js\?v=350/.test(htmlTxt));



// --- v237 feint settle→walk destRect leftover: max(feintFade, rise k) ---
check("feint settle→walk destRect leftover seats no breath mid-stride", /Feint settle→walk destRect leftover/.test(srcTxt) && /leftover feintFade breath used to/.test(srcTxt) && /seat ~0\.65 mid-stride/.test(srcTxt) && /used only feintFade k/.test(srcTxt) && /Same[\s\S]{0,40}hole feint settle→guard/.test(srcTxt) && /Feint settle→walk plant-release destRect leftover/.test(srcTxt) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "idleBreath")));
check("feint settle→walk destRect leftover is destRect-only", /destRect-only/.test(srcTxt) && /idleBreath/.test(srcTxt) && /cutRecBreathT/.test(srcTxt) && /f\.feintT > 0/.test(sliceFn(codeOnly, "idleBreath")) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "idleBreath")) && !/walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "destRect")) && !/walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "bodyAABB")) && !/walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "poseBitmap")) && !/walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "feintFade")));
check("feint settle→walk destRect leftover keeps max feint\/rise", /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\) return 0/.test(sliceFn(codeOnly, "idleBreath")) && /f\.feintT > 0/.test(sliceFn(codeOnly, "idleBreath")) && /return amp \* \(1 - feintFade\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /prevFeint > 0/.test(sliceFn(codeOnly, "tickFeint")));
check("feint settle→walk destRect leftover idle feint path unchanged", /Idle feint still eases with feintFade/.test(srcTxt) && /return amp \* \(1 - feintFade\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /f\.feintT > 0/.test(sliceFn(codeOnly, "idleBreath")));
check("feint settle→walk destRect leftover pure idle→walk still eases", /Pure idle→walk still eases with rise/.test(srcTxt) && /if \(walking\(f\)\) return amp \* \(1 - walkRiseK\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")));
check("feint settle→walk destRect leftover settle→guard unchanged", /Math\.max\(fk, rk\)/.test(sliceFn(codeOnly, "idleBreath")) && /1 - Math\.max\(fk, rk\)/.test(sliceFn(codeOnly, "idleBreath")) && /Feint settle→guard unchanged/.test(srcTxt));
check("feint settle→walk destRect leftover sheet leftover unchanged", /Feint→guard sheet leftover unchanged/.test(srcTxt) && /feintFade[\s\S]{0,30}keeps windup through raise/.test(srcTxt) && /Keep leftover windup through leftover feintT if they raise/.test(srcTxt) && /poseBitmap still block once guarding/.test(srcTxt) && !/if \(f\.guarding\) return 0/.test(sliceFn(codeOnly, "feintFade")));
check("feint settle→walk destRect leftover FEINT_RECOVERY unchanged", /FEINT_RECOVERY \/ combat lock unchanged/.test(srcTxt) && V.FEINT_RECOVERY===100 && /f\.feintT = FEINT_RECOVERY/.test(srcTxt) && /f\.feintT = Math\.max\(0, f\.feintT - dt\)/.test(sliceFn(codeOnly, "tickFeint")));
check("feint settle→walk destRect leftover cutRec settle paths unchanged", /Math\.max\(ck, rk\)/.test(sliceFn(codeOnly, "idleBreath")) && /Math\.max\(ck, wk\)/.test(sliceFn(codeOnly, "idleBreath")) && /guardRaiseK\(f\) >= ck/.test(sliceFn(codeOnly, "tickCutRecBreath")) && /walkRiseK\(f\) >= ck/.test(sliceFn(codeOnly, "tickCutRecBreath")));
check("feint settle→walk destRect leftover tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("feint settle→walk destRect leftover is not a new combat verb", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.HITSTUN===350 && V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140 && V.FEINT_RECOVERY===100 && V.WALK_SETTLE_MS===160);
check("slash frames still 180/140/280 after feint settle→walk destRect leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after feint settle→walk destRect leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after feint settle→walk destRect leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after feint settle→walk destRect leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after feint settle→walk destRect leftover", /game.js\?v=350/.test(htmlTxt));

// live: walk through mid-feint keeps suppress (no mid-stride seat / dump), AABB planted
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.hp=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cut=""; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.walkSettleT=0; V.player.walkRiseT=0;
V.rival.phase="idle"; V.rival.wantBlock=false; V.rival.revArmed=-1;
V.player.x=200; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
let fsw237AmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ fsw237AmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
// rise fully first
V.keys.add("KeyD");
for(let i=0;i<20;i++){ holdRivalStill(); V.update(STEP); }
const fsw237RiseReady = V.player.gait===1 && Math.abs(V.idleBreath(V.player))<0.05;
V.player.feintT = V.FEINT_RECOVERY * 0.5;
V.player.sheatheT=0; V.player.guarding=false; V.player.guardPoseK=0; V.player.phase="idle"; V.player.boltPhase=""; V.player.cutRecBreathT=0;
const fsw237Ft0=V.player.feintT, fsw237Ff0=V.feintFade(V.player);
const fsw237Br0=V.idleBreath(V.player);
const fsw237Aabb0=V.bodyAABB(V.player).y;
let fsw237MaxAbsBr=Math.abs(fsw237Br0), fsw237Cleared=false, fsw237EndBr=0, fsw237AabbOk=true, fsw237DyHop=0;
for(let i=0;i<12;i++){
  holdRivalStill();
  const dy0=V.destRect(V.player).dy;
  const ft0=V.player.feintT;
  V.update(STEP);
  const br=V.idleBreath(V.player);
  const aabb=V.bodyAABB(V.player).y;
  if(Math.abs(aabb-fsw237Aabb0)>0.05) fsw237AabbOk=false;
  fsw237MaxAbsBr=Math.max(fsw237MaxAbsBr, Math.abs(br));
  fsw237DyHop=Math.max(fsw237DyHop, Math.abs(V.destRect(V.player).dy-dy0));
  if(ft0>0 && V.player.feintT===0){ fsw237Cleared=true; fsw237EndBr=br; break; }
}
check("feint settle→walk destRect leftover live amp ready", fsw237AmpReady, `ready=${fsw237AmpReady} modeT=${V.modeT}`);
check("feint settle→walk destRect leftover live rise ready", fsw237RiseReady && V.player.gait===1, `rise=${fsw237RiseReady} gait=${V.player.gait} br0=${fsw237Br0.toFixed(3)}`);
check("feint settle→walk destRect leftover live mid-pull ready", fsw237Ft0>0 && fsw237Ft0<=55 && fsw237Ff0>0.4 && fsw237Ff0<0.6, `ft0=${fsw237Ft0.toFixed(1)} ff0=${fsw237Ff0.toFixed(3)} br0=${fsw237Br0.toFixed(3)}`);
check("feint settle→walk destRect leftover live no mid-stride seat", fsw237MaxAbsBr<0.35, `maxBr=${fsw237MaxAbsBr.toFixed(3)} br0=${fsw237Br0.toFixed(3)}`);
check("feint settle→walk destRect leftover live rise clears feint", fsw237Cleared && Math.abs(fsw237EndBr)<0.05 && V.player.feintT===0, `cleared=${fsw237Cleared} endBr=${fsw237EndBr.toFixed(3)} ft=${V.player.feintT}`);
check("feint settle→walk destRect leftover live no breath dump hop", fsw237DyHop<0.85, `hop=${fsw237DyHop.toFixed(3)}`);
check("feint settle→walk destRect leftover live AABB planted", fsw237AabbOk, `aabbOk=${fsw237AabbOk}`);

// live: idle feint still eases when not walking (unchanged)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.gait=0; V.player.hp=100; V.player.stamina=100;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=500; V.rival.x=1100;
let fsw237IdleAmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ fsw237IdleAmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.feintT = V.FEINT_RECOVERY;
V.player.sheatheT=0;
let fsw237IdleBr0=V.idleBreath(V.player), fsw237IdleBr1=fsw237IdleBr0, fsw237IdleUp=true;
for(let i=0;i<6;i++){
  holdRivalStill(); V.update(STEP);
  const br=V.idleBreath(V.player);
  if(Math.abs(br)+0.08<Math.abs(fsw237IdleBr1)) fsw237IdleUp=false;
  fsw237IdleBr1=br;
}
check("feint settle→walk destRect leftover live idle feint still climbs", fsw237IdleAmpReady && fsw237IdleUp && Math.abs(fsw237IdleBr0)<0.25 && Math.abs(fsw237IdleBr1)>0.7 && V.player.feintT===0 && V.player.gait===0, `ready=${fsw237IdleAmpReady} up=${fsw237IdleUp} br0=${fsw237IdleBr0.toFixed(3)} br1=${fsw237IdleBr1.toFixed(3)} ft=${V.player.feintT}`);

// live: settle→guard still holds (regression)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.hp=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cut=""; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.walkSettleT=0;
V.rival.phase="idle"; V.rival.wantBlock=false; V.rival.revArmed=-1;
V.player.x=500; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
let fsg237RegAmp=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ fsg237RegAmp=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.phase="startup"; V.player.phaseT=0; V.player.cut="slash"; V.player.gait=0;
V.feintEdge=true;
holdRivalStill(); V.update(STEP);
V.feintEdge=false;
const fsg237Did=V.player.feintT>0;
for(let i=0;i<3;i++){ holdRivalStill(); V.update(STEP); }
const fsg237Br0=V.idleBreath(V.player);
V.keys.add("KeyS");
let fsg237MaxSeat=0, fsg237Cleared=false, fsg237EndBr=0, fsg237Held=false;
for(let i=0;i<10;i++){
  holdRivalStill(); V.update(STEP);
  if(V.player.guarding && V.player.feintT>0) fsg237Held=true;
  const br=V.idleBreath(V.player);
  fsg237MaxSeat=Math.max(fsg237MaxSeat, Math.abs(br)-Math.abs(fsg237Br0));
  if(V.guardRaiseK(V.player)>=0.99 && Math.abs(br)<0.05){ fsg237Cleared=true; fsg237EndBr=br; break; }
}
check("feint settle→walk destRect leftover live guard path still holds", fsg237RegAmp && fsg237Did && V.player.guarding && fsg237MaxSeat<0.45 && fsg237Cleared && Math.abs(fsg237EndBr)<0.05, `amp=${fsg237RegAmp} did=${fsg237Did} g=${V.player.guarding} seat=${fsg237MaxSeat.toFixed(3)} cleared=${fsg237Cleared} endBr=${fsg237EndBr.toFixed(3)} held=${fsg237Held}`);

// rival shares idleBreath feint keep-0 walk-out (no kind gate); plant-release supersedes max(fk,wk)
check("feint settle→walk destRect leftover rival idleBreath shared", /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "idleBreath")) && /f\.feintT > 0/.test(sliceFn(codeOnly, "idleBreath")) && /return amp \* \(1 - feintFade\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /startFeint\(f\)/.test(srcTxt) && !/f\.kind === "you"/.test(sliceFn(codeOnly, "idleBreath").slice(sliceFn(codeOnly, "idleBreath").indexOf("if (f.feintT > 0)"), sliceFn(codeOnly, "idleBreath").indexOf("if (f.feintT > 0)")+280)));

check("cache v=296 after feint settle→walk destRect leftover live", /game.js\?v=350/.test(htmlTxt));






// --- v238 clash sheathe settle→walk destRect leftover: arm cutRec mid-stride ---
check("clash sheathe settle→walk destRect leftover seats no breath mid-stride", /Clash sheathe settle→walk destRect leftover/.test(srcTxt) && /leftover sheathe end used to/.test(srcTxt) && /seat ~0\.86 mid-stride/.test(srcTxt) && /skipped cutRecBreathT when walking/.test(srcTxt) && /Same[\s\S]{0,40}hole cut recovery[\s\S]{0,20}settle→walk/.test(srcTxt) && /Arm cutRecBreathT when sheathe/.test(srcTxt) && /Math\.max\(ck, wk\)/.test(sliceFn(codeOnly, "idleBreath")));
check("clash sheathe settle→walk destRect leftover is destRect-only", /destRect-only/.test(srcTxt) && /idleBreath \/ cutRecBreathT/.test(srcTxt) && /Arm cutRecBreathT when sheathe/.test(srcTxt) && !/!f\.guarding\)/.test(sliceFn(codeOnly, "tickSheathe")) && /prevSheathe > 0 && f\.sheatheT === 0/.test(sliceFn(codeOnly, "tickSheathe")) && !/!walking\(f\)/.test(sliceFn(codeOnly, "tickSheathe")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "destRect")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "bodyAABB")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "poseBitmap")));
check("clash sheathe settle→walk destRect leftover arms cutRec on walk mid-end", /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickSheathe")) && !/!f\.guarding\)/.test(sliceFn(codeOnly, "tickSheathe")) && /walk mid-end used to/.test(srcTxt) && /let cut recovery settle→walk max\(ck, wk\)/.test(srcTxt));
check("clash sheathe settle→walk destRect leftover keeps max cutRec\/rise", /Math\.max\(ck, wk\)/.test(sliceFn(codeOnly, "idleBreath")) && /walkRiseK\(f\) >= ck/.test(sliceFn(codeOnly, "tickCutRecBreath")) && /else if \(walking\(f\)\)/.test(sliceFn(codeOnly, "tickCutRecBreath")));
check("clash sheathe settle→walk destRect leftover idle sheathe path unchanged", /Idle clash sheathe still eases/.test(srcTxt) && /prevSheathe > 0 && f\.sheatheT === 0/.test(sliceFn(codeOnly, "tickSheathe")) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickSheathe")) && /return 0;/.test(sliceFn(codeOnly, "idleBreath")) && /f\.sheatheT > 0/.test(sliceFn(codeOnly, "idleBreath")));
check("clash sheathe settle→walk destRect leftover pure idle→walk still eases", /Pure idle→walk still eases with rise/.test(srcTxt) && /if \(walking\(f\)\) return amp \* \(1 - walkRiseK\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")));
check("clash sheathe settle→walk destRect leftover guard mid-end now arms (v239)", /Clash sheathe settle→guard destRect leftover/.test(srcTxt) && /guard mid-end used to/.test(srcTxt) && !/!f\.guarding\)/.test(sliceFn(codeOnly, "tickSheathe")) && /max\(ck, rk\)/.test(srcTxt));
check("clash sheathe settle→walk destRect leftover keep-0 through sheathe unchanged", /Clash sheathe keep-0 through/.test(srcTxt) && /f\.sheatheT > 0/.test(sliceFn(codeOnly, "idleBreath")) && /return 0;/.test(sliceFn(codeOnly, "idleBreath")));
check("clash sheathe settle→walk destRect leftover feint\/cutRec settle paths unchanged", /Feint \/ cutRec settle paths unchanged/.test(srcTxt) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "idleBreath")) && /Math\.max\(fk, rk\)/.test(sliceFn(codeOnly, "idleBreath")) && /Math\.max\(ck, rk\)/.test(sliceFn(codeOnly, "idleBreath")) && /Math\.max\(ck, wk\)/.test(sliceFn(codeOnly, "idleBreath")));
check("clash sheathe settle→walk destRect leftover tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("clash sheathe settle→walk destRect leftover is not a new combat verb", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.HITSTUN===350 && V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140 && V.FEINT_RECOVERY===100 && V.WALK_SETTLE_MS===160);
check("slash frames still 180/140/280 after clash sheathe settle→walk destRect leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after clash sheathe settle→walk destRect leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after clash sheathe settle→walk destRect leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after clash sheathe settle→walk destRect leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after clash sheathe settle→walk destRect leftover", /game.js\?v=350/.test(htmlTxt));

// live: walk through mid-sheathe keeps suppress (no mid-stride seat), AABB planted
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.hp=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cut=""; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.walkSettleT=0; V.player.walkRiseT=0;
V.rival.phase="idle"; V.rival.wantBlock=false; V.rival.revArmed=-1;
V.player.x=200; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
let csw238AmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ csw238AmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.sheatheT = V.SHEATHE_MS * 0.55;
V.player.clashRec=true; V.player.cut="slash"; V.player.phase="idle"; V.player.cutRecBreathT=0; V.player.feintT=0;
V.keys.add("KeyD");
const csw238St0=V.player.sheatheT;
const csw238Br0=V.idleBreath(V.player);
const csw238Aabb0=V.bodyAABB(V.player).y;
let csw238MaxAbsBr=Math.abs(csw238Br0), csw238Cleared=false, csw238EndBr=0, csw238AabbOk=true, csw238Armed=false, csw238DyHop=0;
for(let i=0;i<20;i++){
  holdRivalStill();
  const dy0=V.destRect(V.player).dy;
  const st0=V.player.sheatheT;
  V.update(STEP);
  const br=V.idleBreath(V.player);
  const aabb=V.bodyAABB(V.player).y;
  if(Math.abs(aabb-csw238Aabb0)>0.05) csw238AabbOk=false;
  csw238MaxAbsBr=Math.max(csw238MaxAbsBr, Math.abs(br));
  csw238DyHop=Math.max(csw238DyHop, Math.abs(V.destRect(V.player).dy-dy0));
  if(st0>0 && V.player.sheatheT===0){
    csw238Armed = V.player.cutRecBreathT>0;
    csw238Cleared=true;
    csw238EndBr=br;
    // keep scanning a few more for seat after arm
  }
  if(csw238Cleared && V.player.cutRecBreathT===0 && V.player.gait===1 && Math.abs(br)<0.05){ csw238EndBr=br; break; }
}
check("clash sheathe settle→walk destRect leftover live amp ready", csw238AmpReady, `ready=${csw238AmpReady} modeT=${V.modeT}`);
check("clash sheathe settle→walk destRect leftover live mid-sheathe ready", csw238St0>0 && csw238St0<=V.SHEATHE_MS*0.6 && Math.abs(csw238Br0)<0.05, `st0=${csw238St0.toFixed(1)} br0=${csw238Br0.toFixed(3)}`);
check("clash sheathe settle→walk destRect leftover live arms cutRec on end", csw238Armed, `armed=${csw238Armed} cbt=${V.player.cutRecBreathT}`);
check("clash sheathe settle→walk destRect leftover live no mid-stride seat", csw238MaxAbsBr<0.35, `maxBr=${csw238MaxAbsBr.toFixed(3)} br0=${csw238Br0.toFixed(3)}`);
check("clash sheathe settle→walk destRect leftover live rise clears cutRec", csw238Cleared && Math.abs(csw238EndBr)<0.05 && V.player.sheatheT===0, `cleared=${csw238Cleared} endBr=${csw238EndBr.toFixed(3)} st=${V.player.sheatheT} cbt=${V.player.cutRecBreathT}`);
check("clash sheathe settle→walk destRect leftover live no breath dump hop", csw238MaxAbsBr<0.35 && csw238DyHop<6.5, `maxBr=${csw238MaxAbsBr.toFixed(3)} hop=${csw238DyHop.toFixed(3)}`);
check("clash sheathe settle→walk destRect leftover live AABB planted", csw238AabbOk, `aabbOk=${csw238AabbOk}`);

// live: idle clash sheathe still eases when not walking (unchanged)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.gait=0; V.player.hp=100; V.player.stamina=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cut="";
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=500; V.rival.x=1100;
let csw238IdleAmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ csw238IdleAmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.sheatheT = V.SHEATHE_MS * 0.15;
V.player.clashRec=true; V.player.cut="slash"; V.player.phase="idle"; V.player.cutRecBreathT=0;
let csw238IdleBr0=V.idleBreath(V.player), csw238IdleBr1=csw238IdleBr0, csw238IdleUp=false, csw238IdleArmed=false;
for(let i=0;i<12;i++){
  holdRivalStill(); V.update(STEP);
  if(V.player.sheatheT===0 && V.player.cutRecBreathT>0) csw238IdleArmed=true;
  const br=V.idleBreath(V.player);
  if(V.player.cutRecBreathT>0 && Math.abs(br)>Math.abs(csw238IdleBr1)+0.05) csw238IdleUp=true;
  csw238IdleBr1=br;
}
check("clash sheathe settle→walk destRect leftover live idle sheathe still eases", csw238IdleAmpReady && csw238IdleArmed && csw238IdleUp && Math.abs(csw238IdleBr0)<0.05 && Math.abs(csw238IdleBr1)>0.4 && V.player.gait===0, `ready=${csw238IdleAmpReady} armed=${csw238IdleArmed} up=${csw238IdleUp} br0=${csw238IdleBr0.toFixed(3)} br1=${csw238IdleBr1.toFixed(3)} cbt=${V.player.cutRecBreathT}`);

// live: rise-full mid-sheathe still stays 0 (no regression)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.hp=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cut=""; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.walkSettleT=0; V.player.walkRiseT=0;
V.rival.phase="idle"; V.rival.wantBlock=false; V.rival.revArmed=-1;
V.player.x=200; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
let csw238RiseAmp=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ csw238RiseAmp=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.keys.add("KeyD");
for(let i=0;i<20;i++){ holdRivalStill(); V.update(STEP); }
const csw238RiseReady = V.player.gait===1 && Math.abs(V.idleBreath(V.player))<0.05;
V.player.sheatheT = V.SHEATHE_MS * 0.5;
V.player.clashRec=true; V.player.cut="slash"; V.player.phase="idle"; V.player.cutRecBreathT=0;
let csw238RiseMax=0, csw238RiseArmed=false;
for(let i=0;i<16;i++){
  holdRivalStill();
  const st0=V.player.sheatheT;
  V.update(STEP);
  csw238RiseMax=Math.max(csw238RiseMax, Math.abs(V.idleBreath(V.player)));
  if(st0>0 && V.player.sheatheT===0 && V.player.cutRecBreathT>0) csw238RiseArmed=true;
}
check("clash sheathe settle→walk destRect leftover live rise-full stays suppress", csw238RiseAmp && csw238RiseReady && csw238RiseMax<0.35, `amp=${csw238RiseAmp} rise=${csw238RiseReady} armed=${csw238RiseArmed} maxBr=${csw238RiseMax.toFixed(3)}`);

// rival shares tickSheathe arm (no kind gate)
check("clash sheathe settle→walk destRect leftover rival tickSheathe shared", /tickSheathe\(rival/.test(srcTxt) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickSheathe")) && !/f\.kind === "you"/.test(sliceFn(codeOnly, "tickSheathe")) && /Math\.max\(ck, wk\)/.test(sliceFn(codeOnly, "idleBreath")));

check("cache v=296 after clash sheathe settle→walk destRect leftover live", /game.js\?v=350/.test(htmlTxt));

// --- v239 clash sheathe settle→guard destRect leftover: arm cutRec mid-raise ---
check("clash sheathe settle→guard destRect leftover seats no breath mid-raise", /Clash sheathe settle→guard destRect leftover/.test(srcTxt) && /leftover sheathe end used to/.test(srcTxt) && /seat ~0\.43 mid-raise/.test(srcTxt) && /skipped cutRecBreathT when guarding/.test(srcTxt) && /Same[\s\S]{0,40}hole clash sheathe settle→walk/.test(srcTxt) && /Arm cutRecBreathT when sheathe/.test(srcTxt) && /Math\.max\(ck, rk\)/.test(sliceFn(codeOnly, "idleBreath")));
check("clash sheathe settle→guard destRect leftover is destRect-only", /destRect-only/.test(srcTxt) && /idleBreath \/ cutRecBreathT/.test(srcTxt) && /Arm cutRecBreathT when sheathe/.test(srcTxt) && /prevSheathe > 0 && f\.sheatheT === 0/.test(sliceFn(codeOnly, "tickSheathe")) && !/!f\.guarding\)/.test(sliceFn(codeOnly, "tickSheathe")) && !/!walking\(f\)/.test(sliceFn(codeOnly, "tickSheathe")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "destRect")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "bodyAABB")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "poseBitmap")));
check("clash sheathe settle→guard destRect leftover arms cutRec on guard mid-end", /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickSheathe")) && /guard mid-end used to/.test(srcTxt) && /let cut recovery settle→guard max\(ck, rk\)/.test(srcTxt) && !/!f\.guarding\)/.test(sliceFn(codeOnly, "tickSheathe")));
check("clash sheathe settle→guard destRect leftover keeps max cutRec\/raise", /Math\.max\(ck, rk\)/.test(sliceFn(codeOnly, "idleBreath")) && /guardRaiseK\(f\) >= ck/.test(sliceFn(codeOnly, "tickCutRecBreath")) && /f\.guarding \|\| f\.guardPoseK > 0/.test(sliceFn(codeOnly, "tickCutRecBreath")));
check("clash sheathe settle→guard destRect leftover idle sheathe path unchanged", /Idle clash sheathe still eases/.test(srcTxt) && /prevSheathe > 0 && f\.sheatheT === 0/.test(sliceFn(codeOnly, "tickSheathe")) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickSheathe")) && /return 0;/.test(sliceFn(codeOnly, "idleBreath")) && /f\.sheatheT > 0/.test(sliceFn(codeOnly, "idleBreath")));
check("clash sheathe settle→guard destRect leftover pure idle→guard still eases", /Pure idle→guard still eases with raise/.test(srcTxt) && /if \(f\.guarding \|\| f\.guardPoseK > 0\) return amp \* \(1 - guardRaiseK\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")));
check("clash sheathe settle→guard destRect leftover settle→walk unchanged", /Clash sheathe settle→walk unchanged/.test(srcTxt) && /Math\.max\(ck, wk\)/.test(sliceFn(codeOnly, "idleBreath")) && /walkRiseK\(f\) >= ck/.test(sliceFn(codeOnly, "tickCutRecBreath")));
check("clash sheathe settle→guard destRect leftover keep-0 through sheathe unchanged", /Clash sheathe keep-0 through sheathe unchanged/.test(srcTxt) && /f\.sheatheT > 0/.test(sliceFn(codeOnly, "idleBreath")) && /return 0;/.test(sliceFn(codeOnly, "idleBreath")));
check("clash sheathe settle→guard destRect leftover feint\/cutRec settle paths unchanged", /Feint \/ cutRec settle paths unchanged/.test(srcTxt) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "idleBreath")) && /Math\.max\(fk, rk\)/.test(sliceFn(codeOnly, "idleBreath")) && /Math\.max\(ck, rk\)/.test(sliceFn(codeOnly, "idleBreath")) && /Math\.max\(ck, wk\)/.test(sliceFn(codeOnly, "idleBreath")));
check("clash sheathe settle→guard destRect leftover tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("clash sheathe settle→guard destRect leftover is not a new combat verb", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.HITSTUN===350 && V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140 && V.FEINT_RECOVERY===100 && V.WALK_SETTLE_MS===160);
check("slash frames still 180/140/280 after clash sheathe settle→guard destRect leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after clash sheathe settle→guard destRect leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after clash sheathe settle→guard destRect leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after clash sheathe settle→guard destRect leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after clash sheathe settle→guard destRect leftover", /game.js\?v=350/.test(htmlTxt));

// live: raise through mid-sheathe keeps suppress (no mid-raise seat), AABB planted
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.hp=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cut=""; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.walkSettleT=0; V.player.walkRiseT=0;
V.rival.phase="idle"; V.rival.wantBlock=false; V.rival.revArmed=-1;
V.player.x=200; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
let csg239AmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ csg239AmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.sheatheT = V.SHEATHE_MS * 0.35;
V.player.clashRec=true; V.player.cut="slash"; V.player.phase="idle"; V.player.cutRecBreathT=0; V.player.feintT=0;
V.keys.add("KeyS");
const csg239St0=V.player.sheatheT;
const csg239Br0=V.idleBreath(V.player);
const csg239Aabb0=V.bodyAABB(V.player).y;
let csg239MaxAbsBr=Math.abs(csg239Br0), csg239Cleared=false, csg239EndBr=0, csg239AabbOk=true, csg239Armed=false, csg239DyHop=0, csg239MaxSeat=0;
for(let i=0;i<24;i++){
  holdRivalStill();
  const dy0=V.destRect(V.player).dy;
  const st0=V.player.sheatheT;
  V.update(STEP);
  const br=V.idleBreath(V.player);
  const aabb=V.bodyAABB(V.player).y;
  if(Math.abs(aabb-csg239Aabb0)>0.05) csg239AabbOk=false;
  csg239MaxAbsBr=Math.max(csg239MaxAbsBr, Math.abs(br));
  csg239DyHop=Math.max(csg239DyHop, Math.abs(V.destRect(V.player).dy-dy0));
  if(st0>0 && V.player.sheatheT===0){
    csg239Armed = V.player.cutRecBreathT>0;
    csg239Cleared=true;
    csg239EndBr=br;
  }
  if(V.player.sheatheT===0) csg239MaxSeat=Math.max(csg239MaxSeat, Math.abs(br));
  if(csg239Cleared && V.player.cutRecBreathT===0 && V.player.guarding && Math.abs(br)<0.05){ csg239EndBr=br; break; }
}
check("clash sheathe settle→guard destRect leftover live amp ready", csg239AmpReady, `ready=${csg239AmpReady} modeT=${V.modeT}`);
check("clash sheathe settle→guard destRect leftover live mid-sheathe ready", csg239St0>0 && csg239St0<=V.SHEATHE_MS*0.4 && Math.abs(csg239Br0)<0.05, `st0=${csg239St0.toFixed(1)} br0=${csg239Br0.toFixed(3)}`);
check("clash sheathe settle→guard destRect leftover live arms cutRec on end", csg239Armed, `armed=${csg239Armed} cbt=${V.player.cutRecBreathT}`);
check("clash sheathe settle→guard destRect leftover live no mid-raise seat", csg239MaxSeat<0.35, `maxSeat=${csg239MaxSeat.toFixed(3)} maxBr=${csg239MaxAbsBr.toFixed(3)}`);
check("clash sheathe settle→guard destRect leftover live raise clears cutRec", csg239Cleared && Math.abs(csg239EndBr)<0.05 && V.player.sheatheT===0 && V.player.guarding, `cleared=${csg239Cleared} endBr=${csg239EndBr.toFixed(3)} st=${V.player.sheatheT} cbt=${V.player.cutRecBreathT} g=${V.player.guarding}`);
check("clash sheathe settle→guard destRect leftover live no breath dump hop", csg239MaxSeat<0.35 && csg239DyHop<6.5, `maxSeat=${csg239MaxSeat.toFixed(3)} hop=${csg239DyHop.toFixed(3)}`);
check("clash sheathe settle→guard destRect leftover live AABB planted", csg239AabbOk, `aabbOk=${csg239AabbOk}`);

// live: idle clash sheathe still eases when not guarding (unchanged)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.gait=0; V.player.hp=100; V.player.stamina=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cut="";
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=500; V.rival.x=1100;
let csg239IdleAmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ csg239IdleAmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.sheatheT = V.SHEATHE_MS * 0.15;
V.player.clashRec=true; V.player.cut="slash"; V.player.phase="idle"; V.player.cutRecBreathT=0;
let csg239IdleBr0=V.idleBreath(V.player), csg239IdleBr1=csg239IdleBr0, csg239IdleUp=false, csg239IdleArmed=false;
for(let i=0;i<12;i++){
  holdRivalStill(); V.update(STEP);
  if(V.player.sheatheT===0 && V.player.cutRecBreathT>0) csg239IdleArmed=true;
  const br=V.idleBreath(V.player);
  if(V.player.cutRecBreathT>0 && Math.abs(br)>Math.abs(csg239IdleBr1)+0.05) csg239IdleUp=true;
  csg239IdleBr1=br;
}
check("clash sheathe settle→guard destRect leftover live idle sheathe still eases", csg239IdleAmpReady && csg239IdleArmed && csg239IdleUp && Math.abs(csg239IdleBr0)<0.05 && Math.abs(csg239IdleBr1)>0.4 && !V.player.guarding, `ready=${csg239IdleAmpReady} armed=${csg239IdleArmed} up=${csg239IdleUp} br0=${csg239IdleBr0.toFixed(3)} br1=${csg239IdleBr1.toFixed(3)} cbt=${V.player.cutRecBreathT}`);

// live: raise-full mid-sheathe still stays 0 (no regression)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.hp=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cut=""; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.walkSettleT=0; V.player.walkRiseT=0;
V.rival.phase="idle"; V.rival.wantBlock=false; V.rival.revArmed=-1;
V.player.x=200; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
let csg239RaiseAmp=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ csg239RaiseAmp=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.keys.add("KeyS");
for(let i=0;i<20;i++){ holdRivalStill(); V.update(STEP); }
const csg239RaiseReady = V.player.guarding && V.guardRaiseK(V.player)>=0.99 && Math.abs(V.idleBreath(V.player))<0.05;
V.player.sheatheT = V.SHEATHE_MS * 0.5;
V.player.clashRec=true; V.player.cut="slash"; V.player.phase="idle"; V.player.cutRecBreathT=0;
let csg239RaiseMax=0, csg239RaiseArmed=false;
for(let i=0;i<16;i++){
  holdRivalStill();
  const st0=V.player.sheatheT;
  V.update(STEP);
  csg239RaiseMax=Math.max(csg239RaiseMax, Math.abs(V.idleBreath(V.player)));
  if(st0>0 && V.player.sheatheT===0 && V.player.cutRecBreathT>0) csg239RaiseArmed=true;
}
check("clash sheathe settle→guard destRect leftover live raise-full stays suppress", csg239RaiseAmp && csg239RaiseReady && csg239RaiseMax<0.35, `amp=${csg239RaiseAmp} raise=${csg239RaiseReady} armed=${csg239RaiseArmed} maxBr=${csg239RaiseMax.toFixed(3)}`);

// live: walk mid-end still arms (settle→walk unchanged)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.hp=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cut=""; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.walkSettleT=0; V.player.walkRiseT=0;
V.rival.phase="idle"; V.rival.wantBlock=false; V.rival.revArmed=-1;
V.player.x=200; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
let csg239WalkAmp=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ csg239WalkAmp=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.sheatheT = V.SHEATHE_MS * 0.55;
V.player.clashRec=true; V.player.cut="slash"; V.player.phase="idle"; V.player.cutRecBreathT=0;
V.keys.add("KeyD");
let csg239WalkArmed=false, csg239WalkMax=0;
for(let i=0;i<20;i++){
  holdRivalStill();
  const st0=V.player.sheatheT;
  V.update(STEP);
  csg239WalkMax=Math.max(csg239WalkMax, Math.abs(V.idleBreath(V.player)));
  if(st0>0 && V.player.sheatheT===0 && V.player.cutRecBreathT>0) csg239WalkArmed=true;
}
check("clash sheathe settle→guard destRect leftover live walk mid-end still arms", csg239WalkAmp && csg239WalkArmed && csg239WalkMax<0.35, `amp=${csg239WalkAmp} armed=${csg239WalkArmed} maxBr=${csg239WalkMax.toFixed(3)}`);

// rival shares tickSheathe arm (no kind gate)
check("clash sheathe settle→guard destRect leftover rival tickSheathe shared", /tickSheathe\(rival/.test(srcTxt) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickSheathe")) && !/f\.kind === "you"/.test(sliceFn(codeOnly, "tickSheathe")) && /Math\.max\(ck, rk\)/.test(sliceFn(codeOnly, "idleBreath")));

check("cache v=296 after clash sheathe settle→guard destRect leftover live", /game.js\?v=350/.test(htmlTxt));


// --- v240 wakeup settle→walk destRect leftover: keep 0 through walk-out getup; arm cutRec mid-end ---
check("wakeup settle→walk destRect leftover seats no breath mid-stride", /Wakeup settle→walk destRect leftover/.test(srcTxt) && /leftover wakeupFade breath used to/.test(srcTxt) && /seat ~1\.4 mid-stride/.test(srcTxt) && /used only wakeupFade k/.test(srcTxt) && /Same[\s\S]{0,80}hole feint settle→walk/.test(srcTxt) && /Keep breath 0 through walk-out getup/.test(srcTxt) && /arm cutRecBreathT when throw-invuln ends mid-stride/.test(srcTxt));
check("wakeup settle→walk destRect leftover is destRect-only", /destRect-only/.test(srcTxt) && /idleBreath \/ cutRecBreathT/.test(srcTxt) && /Keep breath 0 through walk-out getup/.test(srcTxt) && /prevInv > 0/.test(sliceFn(codeOnly, "tickThrowState")) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickThrowState")) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| f\.guarding \|\| f\.guardPoseK > 0/.test(sliceFn(codeOnly, "tickThrowState")) && /if \(walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\) return 0;/.test(sliceFn(codeOnly, "idleBreath")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "destRect")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "bodyAABB")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "poseBitmap")));
check("wakeup settle→walk destRect leftover keeps 0 through walk-out getup", /if \(wakeupFade\(f\) > 0/.test(sliceFn(codeOnly, "idleBreath")) && /if \(walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\) return 0;/.test(sliceFn(codeOnly, "idleBreath")) && /Keep breath 0 through walk-out getup/.test(srcTxt));
check("wakeup settle→walk destRect leftover arms cutRec on walk mid-end", /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickThrowState")) && /walk mid-end used to dump/.test(srcTxt) && /let cut recovery settle→walk max\(ck, wk\)/.test(srcTxt) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| f\.guarding \|\| f\.guardPoseK > 0/.test(sliceFn(codeOnly, "tickThrowState")));
check("wakeup settle→guard destRect leftover keep-0 through raise mid-getup", /Wakeup settle→guard/.test(srcTxt) && /keep breath 0 through raise mid-getup/.test(srcTxt) && /if \(f\.guarding \|\| f\.guardPoseK > 0\) return 0;/.test(sliceFn(codeOnly, "idleBreath")) && /arm cutRec on invuln mid-end too/.test(srcTxt));
check("wakeup settle→walk destRect leftover idle getup still eases", /Idle getup still eases with wakeupFade/.test(srcTxt) && (/return amp \* \(1 - wakeupFade\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) || /return amp \* \(1 - Math\.max\(wakeupFade\(f\), hurtFade\(f\)\)\)/.test(sliceFn(codeOnly, "idleBreath"))) && /!f\.boltPhase/.test(sliceFn(codeOnly, "tickThrowState")) && /f\.feintT <= 0/.test(sliceFn(codeOnly, "tickThrowState")));
check("wakeup settle→walk destRect leftover pure idle→walk still eases", /Pure idle→walk \/ idle→guard still eases/.test(srcTxt) && /if \(walking\(f\)\) return amp \* \(1 - walkRiseK\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")));
check("wakeup settle→walk destRect leftover sheet leftovers unchanged", /Wakeup→guard \/ walk-out sheet leftover unchanged/.test(srcTxt) && /Keep leftover crumple through leftover throw-invuln if they raise/.test(srcTxt) && /Walk-out on getup used to zero the fade/.test(srcTxt));
check("wakeup settle→walk destRect leftover feint\/cutRec\/clash sheathe paths unchanged", /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "idleBreath")) && /Math\.max\(fk, rk\)/.test(sliceFn(codeOnly, "idleBreath")) && /Math\.max\(ck, rk\)/.test(sliceFn(codeOnly, "idleBreath")) && /Math\.max\(ck, wk\)/.test(sliceFn(codeOnly, "idleBreath")) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickSheathe")));
check("wakeup settle→walk destRect leftover tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("wakeup settle→walk destRect leftover is not a new combat verb", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.HITSTUN===350 && V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140 && V.FEINT_RECOVERY===100 && V.WALK_SETTLE_MS===160 && V.THROW_WAKE_INVULN===80);
check("slash frames still 180/140/280 after wakeup settle→walk destRect leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after wakeup settle→walk destRect leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after wakeup settle→walk destRect leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after wakeup settle→walk destRect leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after wakeup settle→walk destRect leftover", /game.js\?v=350/.test(htmlTxt));

// live: walk through getup keeps suppress (no mid-stride seat), AABB planted
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.hp=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cut=""; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.walkSettleT=0; V.player.walkRiseT=0;
V.player.thrownT=0; V.player.throwInvulnT=0; V.player.wakeRev=false; V.player.wakeRevFadeHold=0;
V.rival.phase="idle"; V.rival.wantBlock=false; V.rival.revArmed=-1;
V.player.x=200; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
let wsw240AmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ wsw240AmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.throwInvulnT = V.THROW_WAKE_INVULN;
V.player.wakeRev=true; V.player.thrownT=0; V.player.stunT=0; V.player.cutRecBreathT=0;
V.player.phase="idle"; V.player.gait=0; V.player.walkRiseT=0;
const wsw240Wf0=V.wakeupFade(V.player);
const wsw240Br0=V.idleBreath(V.player);
const wsw240Aabb0=V.bodyAABB(V.player).y;
V.keys.add("KeyD");
let wsw240MaxAbsBr=Math.abs(wsw240Br0), wsw240Cleared=false, wsw240EndBr=0, wsw240AabbOk=true, wsw240Armed=false, wsw240DyHop=0, wsw240MaxSeat=0;
for(let i=0;i<36;i++){
  holdRivalStill();
  const dy0=V.destRect(V.player).dy;
  const inv0=V.player.throwInvulnT;
  V.update(STEP);
  const br=V.idleBreath(V.player);
  const aabb=V.bodyAABB(V.player).y;
  if(Math.abs(aabb-wsw240Aabb0)>0.05) wsw240AabbOk=false;
  wsw240MaxAbsBr=Math.max(wsw240MaxAbsBr, Math.abs(br));
  wsw240DyHop=Math.max(wsw240DyHop, Math.abs(V.destRect(V.player).dy-dy0));
  if(V.player.gait===1) wsw240MaxSeat=Math.max(wsw240MaxSeat, Math.abs(br));
  if(inv0>0 && V.player.throwInvulnT===0){
    wsw240Armed = V.player.cutRecBreathT>0;
    wsw240Cleared=true;
    wsw240EndBr=br;
  }
  if(wsw240Cleared && V.player.cutRecBreathT===0 && V.player.gait===1 && Math.abs(br)<0.05){ wsw240EndBr=br; break; }
}
check("wakeup settle→walk destRect leftover live amp ready", wsw240AmpReady, `ready=${wsw240AmpReady} modeT=${V.modeT}`);
check("wakeup settle→walk destRect leftover live getup ready", wsw240Wf0>0.9 && Math.abs(wsw240Br0)<0.05, `wf0=${wsw240Wf0.toFixed(3)} br0=${wsw240Br0.toFixed(3)}`);
check("wakeup settle→walk destRect leftover live arms cutRec on end", wsw240Armed, `armed=${wsw240Armed} cbt=${V.player.cutRecBreathT}`);
check("wakeup settle→walk destRect leftover live no mid-stride seat", wsw240MaxSeat<0.35, `maxSeat=${wsw240MaxSeat.toFixed(3)} maxBr=${wsw240MaxAbsBr.toFixed(3)}`);
check("wakeup settle→walk destRect leftover live rise clears cutRec", wsw240Cleared && Math.abs(wsw240EndBr)<0.05 && V.player.throwInvulnT===0, `cleared=${wsw240Cleared} endBr=${wsw240EndBr.toFixed(3)} inv=${V.player.throwInvulnT} cbt=${V.player.cutRecBreathT}`);
check("wakeup settle→walk destRect leftover live no breath dump hop", wsw240MaxSeat<0.35 && wsw240DyHop<6.5, `maxSeat=${wsw240MaxSeat.toFixed(3)} hop=${wsw240DyHop.toFixed(3)}`);
check("wakeup settle→walk destRect leftover live AABB planted", wsw240AabbOk, `aabbOk=${wsw240AabbOk}`);

// live: idle getup still eases breath in (unchanged)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.gait=0; V.player.hp=100; V.player.stamina=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cut="";
V.player.thrownT=0; V.player.throwInvulnT=0; V.player.wakeRev=false; V.player.wakeRevFadeHold=0;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=500; V.rival.x=1100;
let wsw240IdleAmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ wsw240IdleAmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.throwInvulnT = V.THROW_WAKE_INVULN;
V.player.wakeRev=true; V.player.phase="idle"; V.player.cutRecBreathT=0; V.player.gait=0;
let wsw240IdleBr0=V.idleBreath(V.player), wsw240IdleBr1=wsw240IdleBr0, wsw240IdleUp=false, wsw240IdleArmed=false;
for(let i=0;i<16;i++){
  holdRivalStill(); V.update(STEP);
  if(V.player.throwInvulnT===0 && V.player.cutRecBreathT>0) wsw240IdleArmed=true;
  const br=V.idleBreath(V.player);
  if(V.player.throwInvulnT===0 && V.player.cutRecBreathT===0 && Math.abs(br)>Math.abs(wsw240IdleBr1)+0.05) wsw240IdleUp=true;
  if(V.wakeupFade(V.player)>0 && Math.abs(br)>Math.abs(wsw240IdleBr1)+0.05) wsw240IdleUp=true;
  wsw240IdleBr1=br;
}
check("wakeup settle→walk destRect leftover live idle getup still eases", wsw240IdleAmpReady && !wsw240IdleArmed && wsw240IdleUp && Math.abs(wsw240IdleBr0)<0.05 && Math.abs(wsw240IdleBr1)>0.4 && V.player.gait===0, `ready=${wsw240IdleAmpReady} armed=${wsw240IdleArmed} up=${wsw240IdleUp} br0=${wsw240IdleBr0.toFixed(3)} br1=${wsw240IdleBr1.toFixed(3)} cbt=${V.player.cutRecBreathT}`);

// live: raise mid-getup keeps suppress (no mid-raise seat)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.hp=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cut=""; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.walkSettleT=0; V.player.walkRiseT=0;
V.player.thrownT=0; V.player.throwInvulnT=0; V.player.wakeRev=false; V.player.wakeRevFadeHold=0;
V.rival.phase="idle"; V.rival.wantBlock=false; V.rival.revArmed=-1;
V.player.x=200; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
let wsw240GuardAmp=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ wsw240GuardAmp=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.throwInvulnT = V.THROW_WAKE_INVULN * 0.55;
V.player.wakeRev=true; V.player.phase="idle"; V.player.cutRecBreathT=0;
V.keys.add("KeyS");
let wsw240GuardMaxSeat=0, wsw240GuardArmed=false, wsw240GuardCleared=false, wsw240GuardEndBr=0, wsw240GuardAabb0=V.bodyAABB(V.player).y, wsw240GuardAabbOk=true, wsw240GuardHop=0;
for(let i=0;i<24;i++){
  holdRivalStill();
  const dy0=V.destRect(V.player).dy;
  const inv0=V.player.throwInvulnT;
  V.update(STEP);
  const br=V.idleBreath(V.player);
  if(Math.abs(V.bodyAABB(V.player).y-wsw240GuardAabb0)>0.05) wsw240GuardAabbOk=false;
  wsw240GuardHop=Math.max(wsw240GuardHop, Math.abs(V.destRect(V.player).dy-dy0));
  if(V.player.guarding) wsw240GuardMaxSeat=Math.max(wsw240GuardMaxSeat, Math.abs(br));
  if(inv0>0 && V.player.throwInvulnT===0){
    wsw240GuardArmed = V.player.cutRecBreathT>0 || V.guardRaiseK(V.player)>=0.99;
    wsw240GuardCleared=true;
    wsw240GuardEndBr=br;
  }
  if(wsw240GuardCleared && V.player.guarding && Math.abs(br)<0.05 && V.player.cutRecBreathT===0){ wsw240GuardEndBr=br; break; }
  if(i>22) break;
}
check("wakeup settle→walk destRect leftover live guard mid-getup no seat", wsw240GuardAmp && wsw240GuardMaxSeat<0.45, `amp=${wsw240GuardAmp} seat=${wsw240GuardMaxSeat.toFixed(3)} armed=${wsw240GuardArmed} cleared=${wsw240GuardCleared} endBr=${wsw240GuardEndBr.toFixed(3)} g=${V.player.guarding}`);
check("wakeup settle→walk destRect leftover live guard AABB planted", wsw240GuardAabbOk && wsw240GuardHop<6.5, `aabbOk=${wsw240GuardAabbOk} hop=${wsw240GuardHop.toFixed(3)}`);

// rival shares tickThrowState arm (no kind gate)
check("wakeup settle→walk destRect leftover rival tickThrowState shared", /tickThrowState\(rival/.test(srcTxt) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickThrowState")) && !/f\.kind === "you"/.test(sliceFn(codeOnly, "tickThrowState")) && /Math\.max\(ck, wk\)/.test(sliceFn(codeOnly, "idleBreath")));

check("cache v=296 after wakeup settle→walk destRect leftover live", /game.js\?v=350/.test(htmlTxt));

// --- v241 throw plant end destRect leftover: stay on plant ease through recovery (no fade~0 cliff) ---
check("throw plant end destRect leftover seats no fade~0 hard-zero cliff", /Throw plant end destRect leftover/.test(srcTxt) && /fall through to/.test(srcTxt) && /phase!==idle hard-zero/.test(srcTxt) && /fade hit ~0/.test(srcTxt) && /last recovery frame/.test(srcTxt) && /dumped[\s\S]{0,40}~1\.6/.test(srcTxt) && /seated full on idle/.test(srcTxt) && /Stay on[\s\S]{0,40}plant ease through throw recovery/.test(srcTxt));
check("throw plant end destRect leftover is destRect-only", /destRect-only/.test(srcTxt) && /idleBreath/.test(srcTxt) && /Stay on[\s\S]{0,40}plant ease through throw recovery/.test(srcTxt) && /f\.cut === "throw" && f\.phase === "recovery"/.test(sliceFn(codeOnly, "idleBreath")) && /return amp \* \(1 - throwPlantFade\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "destRect")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "bodyAABB")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "poseBitmap")));
check("throw plant end destRect leftover stays on plant through recovery", /if \(f\.cut === "throw" && f\.phase === "recovery"\)/.test(sliceFn(codeOnly, "idleBreath")) && /return amp \* \(1 - throwPlantFade\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /fade 0 = full amp ready for idle/.test(srcTxt));
check("throw plant end destRect leftover idle still inherits plant", /Idle still inherits that plant/.test(srcTxt) && /Pure idle→walk \/ idle→guard still eases/.test(srcTxt) && /Sheet still fades via[\s\S]{0,20}throwPlantFade/.test(srcTxt));
check("throw plant end destRect leftover sheet fade unchanged", /function throwPlantFade/.test(srcTxt) && /Fade leftover windup→idle over that recovery/.test(srcTxt) && /if \(f\.cut !== "throw"\) return 0;/.test(sliceFn(codeOnly, "throwPlantFade")) && /if \(f\.phase !== "recovery"\) return 0;/.test(sliceFn(codeOnly, "throwPlantFade")));
check("throw plant end destRect leftover tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("throw plant end destRect leftover wakeup\/feint\/cutRec paths unchanged", /if \(walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\) return 0;/.test(sliceFn(codeOnly, "idleBreath")) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "idleBreath")) && /Math\.max\(fk, rk\)/.test(sliceFn(codeOnly, "idleBreath")) && /Math\.max\(ck, rk\)/.test(sliceFn(codeOnly, "idleBreath")) && /Math\.max\(ck, wk\)/.test(sliceFn(codeOnly, "idleBreath")) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickThrowState")) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickSheathe")));
check("throw plant end destRect leftover is not a new combat verb", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.HITSTUN===350 && V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140 && V.FEINT_RECOVERY===100 && V.WALK_SETTLE_MS===160 && V.THROW_WAKE_INVULN===80 && V.THROW_RECOVERY===220);
check("slash frames still 180/140/280 after throw plant end destRect leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after throw plant end destRect leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after throw plant end destRect leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("throw frames still 80/40/220 after throw plant end destRect leftover", V.THROW_STARTUP===80 && V.THROW_ACTIVE===40 && V.THROW_RECOVERY===220 && V.THROW_RANGE===120);
check("no 6th button after throw plant end destRect leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after throw plant end destRect leftover", /game.js\?v=350/.test(htmlTxt));

// live: late throw recovery → idle has no fade~0 breath cliff / destRect hop
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.hp=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cut=""; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.walkSettleT=0; V.player.walkRiseT=0;
V.player.thrownT=0; V.player.throwInvulnT=0; V.player.wakeRev=false;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=200; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
let tpe241AmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ tpe241AmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.cut="throw"; V.player.phase="recovery";
V.player.phaseT = V.THROW_RECOVERY - STEP*3;
V.player.clashRec=false; V.player.techRec=false; V.player.boltPhase=""; V.player.cutRecBreathT=0;
V.player.gait=0; V.player.guarding=false; V.player.guardPoseK=0;
const tpe241Aabb0=V.bodyAABB(V.player).y;
let tpe241PrevBr=V.idleBreath(V.player), tpe241Cliff=false, tpe241MaxHop=0, tpe241AabbOk=true, tpe241EndBr=0, tpe241SawIdle=false;
for(let i=0;i<18;i++){
  holdRivalStill();
  const dy0=V.destRect(V.player).dy;
  const br=V.idleBreath(V.player);
  const fade=V.throwPlantFade(V.player);
  if(V.player.phase==="recovery" && Math.abs(br)<0.05 && Math.abs(tpe241PrevBr)>1.0) tpe241Cliff=true;
  V.update(STEP);
  tpe241MaxHop=Math.max(tpe241MaxHop, Math.abs(V.destRect(V.player).dy-dy0));
  if(Math.abs(V.bodyAABB(V.player).y-tpe241Aabb0)>0.05) tpe241AabbOk=false;
  if(V.player.phase==="idle"){ tpe241SawIdle=true; tpe241EndBr=br; }
  tpe241PrevBr=br;
  if(tpe241SawIdle && V.player.phase==="idle" && i>6) break;
}
check("throw plant end destRect leftover live amp ready", tpe241AmpReady, `ready=${tpe241AmpReady} modeT=${V.modeT}`);
check("throw plant end destRect leftover live no fade~0 cliff", !tpe241Cliff, `cliff=${tpe241Cliff}`);
check("throw plant end destRect leftover live no breath dump hop", tpe241MaxHop<0.5, `hop=${tpe241MaxHop.toFixed(3)}`);
check("throw plant end destRect leftover live idle inherits plant", tpe241SawIdle && Math.abs(tpe241EndBr)>1.0 && V.player.phase==="idle" && V.player.cut==="slash", `saw=${tpe241SawIdle} endBr=${tpe241EndBr.toFixed(3)} ph=${V.player.phase} cut=${V.player.cut}`);
check("throw plant end destRect leftover live AABB planted", tpe241AabbOk, `aabbOk=${tpe241AabbOk}`);

// live: mid throw recovery still eases breath in with plant fade (unchanged)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.cutRecBreathT=0; V.player.gait=0; V.player.hp=100; V.player.stamina=100;
V.player.clashRec=false; V.player.techRec=false;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=500; V.rival.x=1100;
let tpe241EaseAmp=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ tpe241EaseAmp=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.cut="throw"; V.player.phase="recovery"; V.player.phaseT = V.THROW_RECOVERY * 0.15;
V.player.clashRec=false; V.player.techRec=false;
const tpe241Br0=V.idleBreath(V.player), tpe241Fade0=V.throwPlantFade(V.player);
let tpe241Br1=tpe241Br0, tpe241Up=false;
for(let i=0;i<20;i++){
  holdRivalStill(); V.update(STEP);
  if(V.player.phase!=="recovery") break;
  const br=V.idleBreath(V.player);
  if(Math.abs(br)>Math.abs(tpe241Br1)+0.05) tpe241Up=true;
  tpe241Br1=br;
}
check("throw plant end destRect leftover live mid-recovery still eases", tpe241EaseAmp && tpe241Fade0>0.7 && Math.abs(tpe241Br0)<0.4 && tpe241Up && Math.abs(tpe241Br1)>Math.abs(tpe241Br0)+0.3, `ready=${tpe241EaseAmp} fade0=${tpe241Fade0.toFixed(3)} br0=${tpe241Br0.toFixed(3)} br1=${tpe241Br1.toFixed(3)} up=${tpe241Up}`);

// live: dart plant still eases (unchanged regression)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.cutRecBreathT=0; V.player.gait=0; V.player.hp=100; V.player.stamina=100;
V.player.cut="slash";
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=500; V.rival.x=1100;
let tpe241BoltAmp=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ tpe241BoltAmp=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.boltPhase="recovery"; V.player.boltT = V.BOLT_RECOVERY * 0.2; V.player.boltSuper=false; V.player.boltHoldCut=false;
const tpe241BoltBr0=V.idleBreath(V.player), tpe241BoltFade0=V.boltPlantFade(V.player);
let tpe241BoltBr1=tpe241BoltBr0, tpe241BoltUp=false;
for(let i=0;i<16;i++){
  holdRivalStill(); V.update(STEP);
  if(!V.player.boltPhase) break;
  const br=V.idleBreath(V.player);
  if(Math.abs(br)>Math.abs(tpe241BoltBr1)+0.05) tpe241BoltUp=true;
  tpe241BoltBr1=br;
}
check("throw plant end destRect leftover live dart plant still eases", tpe241BoltAmp && tpe241BoltFade0>0.5 && tpe241BoltUp, `ready=${tpe241BoltAmp} fade0=${tpe241BoltFade0.toFixed(3)} br0=${tpe241BoltBr0.toFixed(3)} br1=${tpe241BoltBr1.toFixed(3)} up=${tpe241BoltUp}`);

check("throw plant end destRect leftover rival idleBreath shared", /idleBreath\(rival/.test(srcTxt) || /idleBreath\(f\)/.test(srcTxt));
check("cache v=296 after throw plant end destRect leftover live", /game.js\?v=350/.test(htmlTxt));







// --- v242 guard drop settle→walk destRect leftover: max(raise k, rise k) ---
check("guard drop settle→walk destRect leftover seats no breath mid-stride", /Guard drop settle→walk destRect leftover/.test(srcTxt) && /leftover raise k breath used to/.test(srcTxt) && /seat ~1\.3 mid-stride/.test(srcTxt) && /Same[\s\S]{0,40}hole cut recovery \/ feint settle→walk/.test(srcTxt) && /Guard drop settle→walk plant-release destRect leftover/.test(srcTxt) && /f\.guardPoseK > 0 && !f\.guarding && \(walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\)/.test(sliceFn(codeOnly, "idleBreath")));
check("guard drop settle→walk destRect leftover is destRect-only", /destRect-only/.test(srcTxt) && /idleBreath/.test(srcTxt) && /f\.guardPoseK > 0 && !f\.guarding && \(walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\)/.test(sliceFn(codeOnly, "idleBreath")) && !/prevGuardK/.test(sliceFn(codeOnly, "destRect")) && !/prevGuardK/.test(sliceFn(codeOnly, "bodyAABB")) && !/prevGuardK/.test(sliceFn(codeOnly, "poseBitmap")) && !/prevGuardK/.test(sliceFn(codeOnly, "guardDropFade")));
check("guard drop settle→walk destRect leftover keeps max raise\/rise", /Guard drop settle→walk plant-release destRect leftover/.test(srcTxt) && /f\.guardPoseK > 0 && !f\.guarding && \(walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\)/.test(sliceFn(codeOnly, "idleBreath")) && /return 0;/.test(sliceFn(codeOnly, "idleBreath")) && /prevGuardK > 0/.test(sliceFn(codeOnly, "tickGuardPose")));
check("guard drop settle→walk destRect leftover idle drop path unchanged", /Idle drop still eases with raise/.test(srcTxt) && /if \(f\.guarding \|\| f\.guardPoseK > 0\) return amp \* \(1 - guardRaiseK\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")));
check("guard drop settle→walk destRect leftover pure idle→walk still eases", /Pure idle→walk still eases with rise/.test(srcTxt) && /if \(walking\(f\)\) return amp \* \(1 - walkRiseK\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")));
check("guard drop settle→walk destRect leftover walk-out sheet unchanged", /Walk-out sheet leftover unchanged/.test(srcTxt) && /guardDropFade/.test(srcTxt) && /Keep leftover block through leftover guardPoseK if they step off/.test(srcTxt) && !/walking\(f\)\) return 0/.test(sliceFn(codeOnly, "guardDropFade")));
check("guard drop settle→walk destRect leftover cutRec\/feint settle paths unchanged", /Math\.max\(ck, rk\)/.test(sliceFn(codeOnly, "idleBreath")) && /Math\.max\(ck, wk\)/.test(sliceFn(codeOnly, "idleBreath")) && /Math\.max\(fk, rk\)/.test(sliceFn(codeOnly, "idleBreath")) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "idleBreath")) && /Math\.max\(sk, rk\)/.test(sliceFn(codeOnly, "idleBreath")));
check("guard drop settle→walk destRect leftover tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("guard drop settle→walk destRect leftover is not a new combat verb", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.HITSTUN===350 && V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140 && V.FEINT_RECOVERY===100 && V.WALK_SETTLE_MS===160 && V.GUARD_COMMIT_MS===140);
check("slash frames still 180/140/280 after guard drop settle→walk destRect leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after guard drop settle→walk destRect leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after guard drop settle→walk destRect leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after guard drop settle→walk destRect leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after guard drop settle→walk destRect leftover", /game.js\?v=350/.test(htmlTxt));

// live: walk mid-drop keeps suppress (no mid-stride seat), AABB planted
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.hp=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cut=""; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.walkSettleT=0; V.player.walkRiseT=0;
V.rival.phase="idle"; V.rival.wantBlock=false; V.rival.revArmed=-1;
V.player.x=200; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
V.keys.add("KeyS");
for(let i=0;i<20;i++){ holdRivalStill(); V.update(STEP); }
let gdw242AmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ gdw242AmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
const gdw242RaiseReady = V.player.guarding && V.player.guardPoseK>=0.99 && Math.abs(V.idleBreath(V.player))<0.05;
V.keys.delete("KeyS");
V.keys.add("KeyD");
const gdw242Aabb0=V.bodyAABB(V.player).y;
let gdw242MaxSeat=0, gdw242AabbOk=true, gdw242Hop=0, gdw242SawWalk=false, gdw242EndBr=0, gdw242RaiseAtSeat=0;
for(let i=0;i<18;i++){
  holdRivalStill();
  const dy0=V.destRect(V.player).dy;
  const br=V.idleBreath(V.player);
  const raise=V.guardRaiseK(V.player);
  if(Math.abs(V.bodyAABB(V.player).y-gdw242Aabb0)>0.05) gdw242AabbOk=false;
  gdw242Hop=Math.max(gdw242Hop, Math.abs(V.destRect(V.player).dy-dy0));
  if(V.player.gait!==0 && raise>0.75){
    gdw242SawWalk=true;
    if(Math.abs(br)>gdw242MaxSeat){ gdw242MaxSeat=Math.abs(br); gdw242RaiseAtSeat=raise; }
  }
  V.update(STEP);
  if(V.player.guardPoseK===0 && V.player.gait!==0){ gdw242EndBr=br; if(i>8) break; }
}
check("guard drop settle→walk destRect leftover live amp ready", gdw242AmpReady, `ready=${gdw242AmpReady} modeT=${V.modeT}`);
check("guard drop settle→walk destRect leftover live raise ready", gdw242RaiseReady, `raise=${gdw242RaiseReady} g=${V.player.guarding} gk=${V.player.guardPoseK}`);
check("guard drop settle→walk destRect leftover live no mid-drop seat", gdw242SawWalk && gdw242MaxSeat<0.55, `saw=${gdw242SawWalk} maxSeat=${gdw242MaxSeat.toFixed(3)} raiseAt=${gdw242RaiseAtSeat.toFixed(3)}`);
check("guard drop settle→walk destRect leftover live no breath dump hop", gdw242Hop<1.0, `hop=${gdw242Hop.toFixed(3)}`);
check("guard drop settle→walk destRect leftover live AABB planted", gdw242AabbOk, `aabbOk=${gdw242AabbOk}`);

// live: idle drop still eases when not walking (unchanged)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.gait=0; V.player.hp=100; V.player.stamina=100;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=500; V.rival.x=1100;
V.keys.add("KeyS");
for(let i=0;i<20;i++){ holdRivalStill(); V.update(STEP); }
let gdw242IdleAmp=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ gdw242IdleAmp=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.keys.delete("KeyS");
let gdw242IdleBr0=V.idleBreath(V.player), gdw242IdleBr1=gdw242IdleBr0, gdw242IdleUp=false;
for(let i=0;i<10;i++){
  holdRivalStill(); V.update(STEP);
  const br=V.idleBreath(V.player);
  if(Math.abs(br)>Math.abs(gdw242IdleBr1)+0.05) gdw242IdleUp=true;
  gdw242IdleBr1=br;
  if(V.player.guardPoseK===0 && i>4) break;
}
check("guard drop settle→walk destRect leftover live idle drop still eases", gdw242IdleAmp && gdw242IdleUp && Math.abs(gdw242IdleBr0)<0.05 && Math.abs(gdw242IdleBr1)>0.7 && V.player.gait===0, `ready=${gdw242IdleAmp} up=${gdw242IdleUp} br0=${gdw242IdleBr0.toFixed(3)} br1=${gdw242IdleBr1.toFixed(3)} gk=${V.player.guardPoseK}`);

// live: pure idle→walk still eases (unchanged)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.gait=0; V.player.hp=100; V.player.stamina=100;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=200; V.rival.x=1100;
let gdw242WalkAmp=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ gdw242WalkAmp=true; break; }
  holdRivalStill(); V.update(STEP);
}
const gdw242WalkBr0=V.idleBreath(V.player);
V.keys.add("KeyD");
let gdw242WalkBr1=gdw242WalkBr0, gdw242WalkDown=false;
for(let i=0;i<12;i++){
  holdRivalStill(); V.update(STEP);
  const br=V.idleBreath(V.player);
  if(V.player.gait!==0 && Math.abs(br)+0.2<Math.abs(gdw242WalkBr0)) gdw242WalkDown=true;
  gdw242WalkBr1=br;
}
check("guard drop settle→walk destRect leftover live pure idle→walk still eases", gdw242WalkAmp && gdw242WalkDown && V.player.gait===1 && V.player.guardPoseK===0, `ready=${gdw242WalkAmp} down=${gdw242WalkDown} br0=${gdw242WalkBr0.toFixed(3)} br1=${gdw242WalkBr1.toFixed(3)} gait=${V.player.gait}`);

// live: walk-out sheet fade still holds (regression)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.cutRecBreathT=0; V.player.gait=0; V.player.hp=100; V.player.stamina=100;
V.player.guardPoseK=0;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=500; V.rival.x=1100;
V.keys.add("KeyS");
for(let i=0;i<20;i++){ holdRivalStill(); V.update(STEP); }
V.keys.delete("KeyS");
V.keys.add("KeyA");
holdRivalStill(); V.update(STEP);
holdRivalStill(); V.update(STEP);
const gdw242SheetFade=V.guardDropFade(V.player);
check("guard drop settle→walk destRect leftover live walk-out sheet still holds", gdw242SheetFade>0.5 && V.player.gait===-1 && V.player.guardPoseK>0 && V.player.guarding===false, `fade=${gdw242SheetFade.toFixed(3)} gait=${V.player.gait} gk=${V.player.guardPoseK.toFixed(3)}`);

check("guard drop settle→walk destRect leftover rival idleBreath shared", /idleBreath\(rival/.test(srcTxt) || /idleBreath\(f\)/.test(srcTxt));
check("cache v=296 after guard drop settle→walk destRect leftover live", /game.js\?v=350/.test(htmlTxt));

// --- v243 damage number trail leftover: ghosts along rise path (draw-only) ---
check("damage number trail leftover seats no single-glyph teleport hop", /Damage number trail leftover/.test(srcTxt) && /rise as a single glyph/.test(srcTxt) && /teleport hop/.test(srcTxt) && /not a streak/.test(srcTxt) && /Ghosts along the rise path/.test(srcTxt) && /live −N still on top/.test(srcTxt));
check("damage number trail leftover is draw-only", /Draw-only \(drawDmgNums\)/.test(srcTxt) && /drawDmgNums/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && /Ride still live hurt/.test(srcTxt) && /Rise still fades/.test(srcTxt) && !/dmgNums/.test(sliceFn(codeOnly, "destRect")) && !/dmgNums/.test(sliceFn(codeOnly, "bodyAABB")) && !/dmgNums/.test(sliceFn(codeOnly, "slashPose")) && !/drawDmgNums/.test(sliceFn(codeOnly, "destRect")) && !/drawDmgNums/.test(sliceFn(codeOnly, "bodyAABB")));
check("damage number trail leftover draws ghosts then live", /for \(let g = 3; g >= 1; g--\)/.test(sliceFn(codeOnly, "drawDmgNums")) && /u - g \* 0\.08/.test(sliceFn(codeOnly, "drawDmgNums")) && /fade \* \(0\.28 \/ g\)/.test(sliceFn(codeOnly, "drawDmgNums")) && /drawPixelText\(label, p\.x, p\.y - riseG/.test(sliceFn(codeOnly, "drawDmgNums")) && /drawPixelText\(label, p\.x, p\.y - rise,/.test(sliceFn(codeOnly, "drawDmgNums")));
check("damage number trail leftover ride\/rise unchanged", /function syncDmgNums/.test(srcTxt) && /hitWoundAnchor/.test(sliceFn(codeOnly, "syncDmgNums")) && /plantDX/.test(sliceFn(codeOnly, "spawnDmgNum")) && /homeYou/.test(sliceFn(codeOnly, "spawnDmgNum")) && V.DMG_NUM_MS === 600 && V.DMG_NUM_RISE === 50);
check("damage number trail leftover chip scale/color owned by v315", /Damage number distinct leftover/.test(srcTxt) && /kind === "chip"/.test(sliceFn(codeOnly, "drawDmgNums")) && /COL_HUESO/.test(sliceFn(codeOnly, "drawDmgNums")) && /COL_OXIDO/.test(sliceFn(codeOnly, "drawDmgNums")) && /COL_BRASA/.test(sliceFn(codeOnly, "drawDmgNums")) && /COL_PIZARRA/.test(sliceFn(codeOnly, "drawDmgNums")));
check("damage number trail leftover hooks unchanged", /spawnDmgNum\(pt\.x, pt\.y, before - def\.hp, false, def\)/.test(srcTxt) && /spawnDmgNum\(pt\.x, pt\.y, before - def\.hp, true, def\)/.test(srcTxt) && /spawnDmgNum\(bb\.x \+ bb\.w \* 0\.5/.test(srcTxt) && /tickDmgNums\(dt\)/.test(srcTxt));
check("damage number trail leftover is not a new combat verb", V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80 && V.BOLT_SUPER_STOP===60 && V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140);
check("slash frames still 180/140/280 after damage number trail leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after damage number trail leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after damage number trail leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after damage number trail leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after damage number trail leftover", /game.js\?v=350/.test(htmlTxt));

// live: trail ghosts sit below live −N along rise; ride still plants on wound
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.cutRecBreathT=0; V.player.gait=0; V.player.hp=100; V.player.stamina=100;
V.rival.phase="idle"; V.rival.stunT=0; V.rival.wantBlock=false; V.rival.hp=100; V.rival.guarding=false; V.rival.falling=false;
V.player.x=520; V.rival.x=700; V.player.facing=1; V.rival.facing=-1;
V.dmgNums.length = 0;
V.keepApart();
let dnt243Hit=false, dnt243Home=null;
V.attackEdge=true;
for(let i=0;i<90;i++){
  holdRivalStill();
  V.hitstopLeft=0;
  V.update(STEP);
  if(V.rival.stunT>0 && V.dmgNums.length>0){
    dnt243Hit=true;
    dnt243Home=V.dmgNums[0].homeYou;
    break;
  }
}
const dnt243Num = V.dmgNums[0];
const dnt243A0 = V.hitWoundAnchor(V.rival);
const dnt243Aabb0 = V.bodyAABB(V.rival);
check("damage number trail leftover live hit arms", dnt243Hit && V.rival.stunT>0 && V.dmgNums.length>0 && dnt243Home===false && dnt243Num && dnt243Num.amt===10 && dnt243Num.chip===false, `hit=${dnt243Hit} n=${V.dmgNums.length} home=${dnt243Home} amt=${dnt243Num&&dnt243Num.amt} stun=${V.rival.stunT}`);
check("damage number trail leftover live plants on wound", dnt243Num && Math.abs(dnt243Num.x - (dnt243A0.x + (dnt243Num.plantDX||0))) < 2 && Math.abs(dnt243Num.y - (dnt243A0.y + (dnt243Num.plantDY||0))) < 2, `x=${dnt243Num&&dnt243Num.x.toFixed(1)} a=${dnt243A0.x.toFixed(1)} dx=${dnt243Num&&dnt243Num.plantDX}`);

// advance partway through life so trail ghosts have separation
for(let i=0;i<12;i++){ V.hitstopLeft=0; holdRivalStill(); V.update(STEP); }
const dnt243Num1 = V.dmgNums[0];
const dnt243U = dnt243Num1 ? Math.min(1, dnt243Num1.t / V.DMG_NUM_MS) : 0;
const dnt243Rise = V.DMG_NUM_RISE * dnt243U;
const dnt243GhostU = Math.max(0, dnt243U - 0.08);
const dnt243GhostRise = V.DMG_NUM_RISE * dnt243GhostU;
const dnt243TrailGap = dnt243Rise - dnt243GhostRise;
const dnt243A1 = V.hitWoundAnchor(V.rival);
check("damage number trail leftover live rise still fades", dnt243Num1 && dnt243Num1.t>0 && dnt243Num1.t<V.DMG_NUM_MS && dnt243U>0.12 && dnt243Rise>6, `t=${dnt243Num1&&dnt243Num1.t} u=${dnt243U.toFixed(3)} rise=${dnt243Rise.toFixed(1)}`);
check("damage number trail leftover live ghosts trail below live", dnt243Num1 && dnt243TrailGap>3.5 && dnt243GhostU>0 && /for \(let g = 3; g >= 1; g--\)/.test(sliceFn(codeOnly, "drawDmgNums")), `gap=${dnt243TrailGap.toFixed(2)} gu=${dnt243GhostU.toFixed(3)} rise=${dnt243Rise.toFixed(1)}`);
check("damage number trail leftover live still rides wound", dnt243Num1 && Math.abs(dnt243Num1.x - (dnt243A1.x + (dnt243Num1.plantDX||0))) < 2, `x=${dnt243Num1&&dnt243Num1.x.toFixed(1)} a1=${dnt243A1.x.toFixed(1)}`);
check("damage number trail leftover live AABB planted", Math.abs(V.bodyAABB(V.rival).y - dnt243Aabb0.y) < 0.05 && Math.abs(V.bodyAABB(V.rival).h - dnt243Aabb0.h) < 0.05, `dy=${(V.bodyAABB(V.rival).y-dnt243Aabb0.y).toFixed(2)}`);

// chip path still trails (scale 2 / oxido) — spawn direct
V.dmgNums.length = 0;
V.spawnDmgNum(640, 360, 2, true, V.rival);
const dnt243Chip = V.dmgNums[0];
check("damage number trail leftover live chip still spawns", dnt243Chip && dnt243Chip.amt===2 && dnt243Chip.chip===true && dnt243Chip.homeYou===false, `chip=${!!dnt243Chip} amt=${dnt243Chip&&dnt243Chip.amt}`);

check("cache v=296 after damage number trail leftover live", /game.js\?v=350/.test(htmlTxt));

// --- v244 special dart trail leftover: ghosts along flight path (draw-only) ---
check("special dart trail leftover seats no single-body teleport hop", /Special dart trail leftover/.test(srcTxt) && /fly as a single body/.test(srcTxt) && /teleport hop across the yard/.test(srcTxt) && /not a streak/.test(srcTxt) && /Ghosts along the flight path/.test(srcTxt) && /live dart still on top/.test(srcTxt));
check("special dart trail leftover is draw-only", /Draw-only \(drawBolt\)/.test(srcTxt) && /drawBolt/.test(srcTxt) && /drawBoltBody/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && /Super wake still attached/.test(srcTxt) && !/drawBoltBody/.test(sliceFn(codeOnly, "destRect")) && !/drawBoltBody/.test(sliceFn(codeOnly, "bodyAABB")) && !/drawBoltBody/.test(sliceFn(codeOnly, "slashPose")) && !/for \(let g = 3; g >= 1; g--\)/.test(sliceFn(codeOnly, "startBolt")) && !/for \(let g = 3; g >= 1; g--\)/.test(sliceFn(codeOnly, "spawnBolt")));
check("special dart trail leftover draws ghosts then live", /for \(let g = 3; g >= 1; g--\)/.test(sliceFn(codeOnly, "drawBolt")) && /bolt\.super \? 18 : 12/.test(sliceFn(codeOnly, "drawBolt")) && /-g \* step/.test(sliceFn(codeOnly, "drawBolt")) && /0\.28 \/ g/.test(sliceFn(codeOnly, "drawBolt")) && /drawBoltBody\(hw, hh, fadeG\)/.test(sliceFn(codeOnly, "drawBolt")) && /drawBoltBody\(hw, hh, 0\)/.test(sliceFn(codeOnly, "drawBolt")));
check("special dart trail leftover super wake still live-only", /Super wake still attached on the live body/.test(srcTxt) && /bolt\.super && !ghost/.test(sliceFn(codeOnly, "drawBoltBody")) && /-hw - hw \* 1\.2/.test(sliceFn(codeOnly, "drawBolt")));
check("special dart trail leftover empty dart still smaller", /Empty dart still smaller/.test(srcTxt) && /BOLT_W/.test(srcTxt) && /BOLT_SUPER_W/.test(srcTxt) && V.BOLT_W===58 && V.BOLT_SUPER_W===96 && V.BOLT_H===13 && V.BOLT_SUPER_H===24);
check("special dart trail leftover dmg trail unchanged", /Damage number trail leftover/.test(srcTxt) && /for \(let g = 3; g >= 1; g--\)/.test(sliceFn(codeOnly, "drawDmgNums")) && /u - g \* 0\.08/.test(sliceFn(codeOnly, "drawDmgNums")) && V.DMG_NUM_MS===600 && V.DMG_NUM_RISE===50);
check("special dart trail leftover is not a new combat verb", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60 && V.BOLT_SPEED===880 && V.BOLT_SUPER_SPEED===1280 && V.BOLT_STAM===30 && V.HITSTUN===350);
check("slash frames still 180/140/280 after special dart trail leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after special dart trail leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after special dart trail leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after special dart trail leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after special dart trail leftover", /game.js\?v=350/.test(htmlTxt));

// live: empty K dart flies; trail step ghosts behind; fighters AABB planted
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.cutRecBreathT=0; V.player.gait=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.hp=100; V.player.meter=0;
V.rival.phase="idle"; V.rival.stunT=0; V.rival.wantBlock=false; V.rival.hp=100; V.rival.guarding=false; V.rival.falling=false;
V.player.x=400; V.rival.x=1000; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
const sdt244Aabb0 = V.bodyAABB(V.player);
let sdt244Birth=false, sdt244X0=0, sdt244Super=null;
V.boltEdge=true;
for(let i=0;i<120;i++){
  holdRivalStill();
  V.hitstopLeft=0;
  V.update(STEP);
  if(V.bolt && V.player.boltPhase==="recovery"){
    sdt244Birth=true;
    sdt244X0=V.bolt.x;
    sdt244Super=!!V.bolt.super;
    break;
  }
}
check("special dart trail leftover live empty birth", sdt244Birth && V.bolt && sdt244Super===false && V.bolt.w===V.BOLT_W && V.bolt.facing===1, `birth=${sdt244Birth} super=${sdt244Super} w=${V.bolt&&V.bolt.w} fac=${V.bolt&&V.bolt.facing}`);

for(let i=0;i<8;i++){ holdRivalStill(); V.hitstopLeft=0; V.update(STEP); }
const sdt244X1 = V.bolt ? V.bolt.x : 0;
const sdt244Gap = sdt244X1 - sdt244X0;
const sdt244Step = 12;
const sdt244TrailGap = 3 * sdt244Step;
check("special dart trail leftover live flight advances", V.bolt && sdt244Gap>40 && V.bolt.super===false, `gap=${sdt244Gap.toFixed(1)} x0=${sdt244X0.toFixed(1)} x1=${sdt244X1.toFixed(1)}`);
check("special dart trail leftover live ghosts trail behind", /for \(let g = 3; g >= 1; g--\)/.test(sliceFn(codeOnly, "drawBolt")) && sdt244TrailGap===36 && /bolt\.super \? 18 : 12/.test(sliceFn(codeOnly, "drawBolt")), `trailGap=${sdt244TrailGap} flight=${sdt244Gap.toFixed(1)}`);
check("special dart trail leftover live AABB planted", Math.abs(V.bodyAABB(V.player).y-sdt244Aabb0.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-sdt244Aabb0.h)<0.05, `dy=${(V.bodyAABB(V.player).y-sdt244Aabb0.y).toFixed(2)}`);

// super dart still trails with larger step; wake live-only
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.hp=100;
V.player.meter=100; V.player.superArmed=true;
V.rival.phase="idle"; V.rival.wantBlock=false; V.rival.hp=100; V.rival.x=1000; V.player.x=400; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
let sdt244SuperOk=false;
V.boltEdge=true;
for(let i=0;i<120;i++){
  holdRivalStill();
  V.hitstopLeft=0;
  V.update(STEP);
  if(V.bolt && V.bolt.super){ sdt244SuperOk=true; break; }
}
check("special dart trail leftover live super birth", sdt244SuperOk && V.bolt && V.bolt.super===true && V.bolt.w===V.BOLT_SUPER_W && /bolt\.super \? 18 : 12/.test(sliceFn(codeOnly, "drawBolt")) && /bolt\.super && !ghost/.test(sliceFn(codeOnly, "drawBoltBody")), `ok=${sdt244SuperOk} super=${V.bolt&&V.bolt.super} w=${V.bolt&&V.bolt.w}`);

check("special dart trail leftover rival draw shared", /function drawBolt/.test(srcTxt) && /bolt\.facing/.test(sliceFn(codeOnly, "drawBolt")));
check("cache v=296 after special dart trail leftover live", /game.js\?v=350/.test(htmlTxt));

// --- v245 meter fill flash leftover: pulse well on stock land (draw-only) ---
check("meter fill flash leftover seats no silent full pop", /Meter fill flash leftover/.test(srcTxt) && /full brasa pip used to pop/.test(srcTxt) && /gainMeter crossed/.test(srcTxt) && /hard-zeroed while full/.test(srcTxt) && /only spend armed meterFlashT/.test(srcTxt) && /spend freeze already flashes/.test(srcTxt) && /Arm meterFlashT on fill-to-full/.test(srcTxt));
check("meter fill flash leftover is draw-only", /Draw-only \(gainMeter \//.test(srcTxt) && /meterFlashK \/ drawLifeBar\)/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && /was < METER_MAX && f\.meter >= METER_MAX/.test(sliceFn(codeOnly, "gainMeter")) && /f\.meterFlashT = METER_FLASH_MS/.test(sliceFn(codeOnly, "gainMeter")) && /0\.55 \* mf/.test(sliceFn(codeOnly, "drawLifeBar")) && !/meterFlashT/.test(sliceFn(codeOnly, "destRect")) && !/meterFlashT/.test(sliceFn(codeOnly, "bodyAABB")) && !/meterFlashT/.test(sliceFn(codeOnly, "slashPose")));
check("meter fill flash leftover arms on fill-to-full", /const was = f\.meter \|\| 0/.test(sliceFn(codeOnly, "gainMeter")) && /was < METER_MAX && f\.meter >= METER_MAX/.test(sliceFn(codeOnly, "gainMeter")) && /f\.meterFlashT = METER_FLASH_MS/.test(sliceFn(codeOnly, "gainMeter")));
check("meter fill flash leftover pulses well while full", /const mf = meterFlashK\(f\)/.test(sliceFn(codeOnly, "drawLifeBar")) && /k >= 1 && mf > 0/.test(sliceFn(codeOnly, "drawLifeBar")) && /0\.55 \* mf/.test(sliceFn(codeOnly, "drawLifeBar")) && /fillFrom\(left, mx, my, mw, mh, 1\)/.test(sliceFn(codeOnly, "drawLifeBar")) && /const pk = k >= 1 \? 1 : mf/.test(sliceFn(codeOnly, "drawLifeBar")));
check("meter fill flash leftover meterFlashK allows full", /function meterFlashK/.test(srcTxt) && !/\(f\.meter \|\| 0\) >= METER_MAX\) return 0/.test(sliceFn(codeOnly, "meterFlashK")) && /f\.meterFlashT \/ METER_FLASH_MS/.test(sliceFn(codeOnly, "meterFlashK")) && /While full, return the/.test(srcTxt));
check("meter fill flash leftover spend flash unchanged", /leftover brasa pip no longer dies the tick/.test(srcTxt) && /startBolt spends the stock/.test(srcTxt) && /HUD bar-drain already holds through freeze/.test(srcTxt) && /u\.meterFlashT = METER_FLASH_MS/.test(sliceFn(codeOnly, "startBolt")) && /u\.meter = 0/.test(sliceFn(codeOnly, "startBolt")));
check("meter fill flash leftover is not a new combat verb", V.METER_FLASH_MS===220 && V.METER_MAX===100 && V.METER_HIT===20 && V.METER_BLOCK_SPECIAL===10 && V.BOLT_SUPER_STOP===60 && V.HITSTUN===350 && V.GUARD_RAISE_MS===80);
check("slash frames still 180/140/280 after meter fill flash leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after meter fill flash leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after meter fill flash leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after meter fill flash leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after meter fill flash leftover", /game.js\?v=350/.test(htmlTxt));

// live: fill-to-full arms flash; partial stays quiet; spend still flashes empty; AABB planted
bootPlay(); wait(1700);
freezeRivalAI(); freezeYouAI();
V.openLeft=0; V.hitstopLeft=0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.hp=100;
V.player.meter=80; V.player.meterFlashT=0;
const mff245Aabb0 = V.bodyAABB(V.player);
const mff245Dest0 = V.destRect(V.player);
V.gainMeter(V.player, V.METER_HIT);
check("meter fill flash leftover live arms on stock", V.player.meter===V.METER_MAX && V.player.meterFlashT===V.METER_FLASH_MS && V.meterFlashK(V.player)>0.9, `m=${V.player.meter} ft=${V.player.meterFlashT} k=${V.meterFlashK(V.player)}`);
check("meter fill flash leftover live AABB planted", Math.abs(V.bodyAABB(V.player).y-mff245Aabb0.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-mff245Aabb0.h)<0.05, `dy=${(V.bodyAABB(V.player).y-mff245Aabb0.y).toFixed(2)}`);
check("meter fill flash leftover live destRect planted", Math.abs(V.destRect(V.player).dy-mff245Dest0.dy)<2.2, `ddy=${(V.destRect(V.player).dy-mff245Dest0.dy).toFixed(2)}`);

// hold through hitstop (HUD clock gated)
V.hitstopLeft = 40;
const mff245Hold = V.player.meterFlashT;
V.update(STEP);
check("meter fill flash leftover live holds freeze", V.player.meter===V.METER_MAX && V.player.meterFlashT===mff245Hold && V.meterFlashK(V.player)>0.9 && V.hitstopLeft>0, `ft=${V.player.meterFlashT} hold=${mff245Hold} k=${V.meterFlashK(V.player)} hs=${V.hitstopLeft}`);
V.hitstopLeft = 0;
for(let i=0;i<8;i++){ freezeRivalAI(); freezeYouAI(); V.update(STEP); }
check("meter fill flash leftover live fades after freeze", V.player.meter===V.METER_MAX && V.player.meterFlashT<mff245Hold-STEP*4 && V.meterFlashK(V.player)>0, `ft=${V.player.meterFlashT} t0=${mff245Hold} k=${V.meterFlashK(V.player)}`);

// partial fill stays quiet
V.player.meter=40; V.player.meterFlashT=0;
V.gainMeter(V.player, V.METER_HIT);
check("meter fill flash leftover live partial quiet", V.player.meter===60 && V.player.meterFlashT===0 && V.meterFlashK(V.player)===0, `m=${V.player.meter} ft=${V.player.meterFlashT} k=${V.meterFlashK(V.player)}`);

// spend flash still arms empty pip
V.player.meter=100; V.player.meterFlashT=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.phase="idle"; V.player.boltPhase="";
V.boltEdge=true;
V.update(STEP);
check("meter fill flash leftover live spend still flashes", V.player.boltPhase==="startup" && V.player.boltSuper===true && V.player.meter===0 && V.player.meterFlashT===V.METER_FLASH_MS && V.meterFlashK(V.player)>0.9, `bph=${V.player.boltPhase} super=${V.player.boltSuper} m=${V.player.meter} ft=${V.player.meterFlashT} k=${V.meterFlashK(V.player)}`);

// rival fill flash shared
V.rival.meter=80; V.rival.meterFlashT=0;
V.gainMeter(V.rival, V.METER_HIT);
check("meter fill flash leftover rival fill arms", V.rival.meter===V.METER_MAX && V.rival.meterFlashT===V.METER_FLASH_MS && V.meterFlashK(V.rival)>0.9, `m=${V.rival.meter} ft=${V.rival.meterFlashT} k=${V.meterFlashK(V.rival)}`);
check("cache v=296 after meter fill flash leftover live", /game.js\?v=350/.test(htmlTxt));

// --- v246 yard switch pop leftover: Escenarios crossfade prev→next (draw-only) ---
check("yard switch pop leftover seats no hard-cut patio pop", /Yard switch pop leftover/.test(srcTxt) && /Escenarios used to hard-cut/.test(srcTxt) && /courtyard the same tick/.test(srcTxt) && /setYardIndex armed/.test(srcTxt) && /yard popped/.test(srcTxt) && /a cut, not a settle/.test(srcTxt) && /Crossfade prev→next/.test(srcTxt) && /YARD_SWITCH_MS/.test(srcTxt));
check("yard switch pop leftover is draw-only", /Draw-only \(drawCourtyard \/ setYardIndex\)/.test(srcTxt) && /drawYardCrop/.test(srcTxt) && /yardSwitchK/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && /Punch cover still rides the live crop/.test(srcTxt) && !/yardSwitchT/.test(sliceFn(codeOnly, "destRect")) && !/yardSwitchT/.test(sliceFn(codeOnly, "bodyAABB")) && !/yardSwitchT/.test(sliceFn(codeOnly, "slashPose")) && !/yardSwitchK/.test(sliceFn(codeOnly, "landHit")) && !/yardSwitchK/.test(sliceFn(codeOnly, "idleBreath")));
check("yard switch pop leftover arms on index change", /yardPrevIndex = yardIndex/.test(sliceFn(codeOnly, "setYardIndex")) && /yardSwitchT = YARD_SWITCH_MS/.test(sliceFn(codeOnly, "setYardIndex")) && /if \(next === yardIndex\) return/.test(sliceFn(codeOnly, "setYardIndex")) && /persistYard\(\)/.test(sliceFn(codeOnly, "setYardIndex")));
check("yard switch pop leftover crossfades prev then next", /yardSwitchT > 0 && yardPrevIndex >= 0 && k < 1/.test(sliceFn(codeOnly, "drawCourtyard")) && /drawYardCrop\(prevImg, padX\)/.test(sliceFn(codeOnly, "drawCourtyard")) && /globalAlpha = k/.test(sliceFn(codeOnly, "drawCourtyard")) && /drawYardCrop\(nextImg, padX\)/.test(sliceFn(codeOnly, "drawCourtyard")) && /yardSwitchK\(\)/.test(sliceFn(codeOnly, "drawCourtyard")));
check("yard switch pop leftover smoothsteps k", /function yardSwitchK/.test(srcTxt) && /yardSwitchT \/ YARD_SWITCH_MS/.test(sliceFn(codeOnly, "yardSwitchK")) && /u \* u \* \(3 - 2 \* u\)/.test(sliceFn(codeOnly, "yardSwitchK")) && /yardSwitchT <= 0/.test(sliceFn(codeOnly, "yardSwitchK")));
check("yard switch pop leftover drains on title sub-pages", /Yard switch pop leftover: Escenarios crossfade drains/.test(srcTxt) && /title sub-pages \(early return\)/.test(srcTxt) && /if \(yardSwitchT > 0\) yardSwitchT = Math\.max\(0, yardSwitchT - dt\)/.test(sliceFn(codeOnly, "update")));
check("yard switch pop leftover same-yard quiet", /Same-yard confirm stays quiet/.test(srcTxt) && /Boot \/ rematch stay on the chosen/.test(srcTxt) && /if \(next === yardIndex\) return/.test(sliceFn(codeOnly, "setYardIndex")));
check("yard switch pop leftover punch cover unchanged", /function punchCover/.test(srcTxt) && /padX = punchCover\(\)/.test(sliceFn(codeOnly, "drawCourtyard")) && V.PUNCH_PX===12);
check("yard switch pop leftover is not a new combat verb", V.YARD_SWITCH_MS===280 && V.YARD_KEYS.length===5 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140 && V.HITSTUN===350);
check("slash frames still 180/140/280 after yard switch pop leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after yard switch pop leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after yard switch pop leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after yard switch pop leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after yard switch pop leftover", /game.js\?v=350/.test(htmlTxt));

// live: change yard arms fade; same yard quiet; drains; AABB planted; rematch path quiet
bootPlay(); wait(400);
const ysp246Aabb0 = V.bodyAABB(V.player);
const ysp246Dest0 = V.destRect(V.player);
const ysp246Y0 = V.yardIndex;
V.yardPrevIndex = -1;
V.yardSwitchT = 0;
V.setYardIndex((ysp246Y0 + 1) % V.YARD_KEYS.length);
check("yard switch pop leftover live arms on change", V.yardIndex===(ysp246Y0+1)%V.YARD_KEYS.length && V.yardPrevIndex===ysp246Y0 && V.yardSwitchT===V.YARD_SWITCH_MS && V.yardSwitchK()<0.05, `y0=${ysp246Y0} y1=${V.yardIndex} prev=${V.yardPrevIndex} t=${V.yardSwitchT} k=${V.yardSwitchK()}`);
check("yard switch pop leftover live AABB planted", Math.abs(V.bodyAABB(V.player).y-ysp246Aabb0.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-ysp246Aabb0.h)<0.05, `dy=${(V.bodyAABB(V.player).y-ysp246Aabb0.y).toFixed(2)}`);
check("yard switch pop leftover live destRect planted", Math.abs(V.destRect(V.player).dy-ysp246Dest0.dy)<2.2, `ddy=${(V.destRect(V.player).dy-ysp246Dest0.dy).toFixed(2)}`);

const ysp246T0 = V.yardSwitchT;
const ysp246K0 = V.yardSwitchK();
for(let i=0;i<6;i++) V.update(STEP);
check("yard switch pop leftover live drains", V.yardSwitchT<ysp246T0-STEP*4 && V.yardSwitchK()>ysp246K0 && V.yardSwitchK()<1, `t=${V.yardSwitchT} t0=${ysp246T0} k=${V.yardSwitchK()} k0=${ysp246K0}`);

// mid-fade still crossfading
check("yard switch pop leftover live mid k rising", V.yardSwitchT>0 && V.yardPrevIndex===ysp246Y0 && V.yardSwitchK()>0.05 && V.yardSwitchK()<0.95, `t=${V.yardSwitchT} prev=${V.yardPrevIndex} k=${V.yardSwitchK()}`);

// finish fade
for(let i=0;i<30;i++) V.update(STEP);
check("yard switch pop leftover live settles full next", V.yardSwitchT===0 && V.yardSwitchK()===1 && V.yardIndex===(ysp246Y0+1)%V.YARD_KEYS.length, `t=${V.yardSwitchT} k=${V.yardSwitchK()} y=${V.yardIndex}`);

// same-yard quiet
const ysp246Y1 = V.yardIndex;
V.yardSwitchT = 0; V.yardPrevIndex = -1;
V.setYardIndex(ysp246Y1);
check("yard switch pop leftover live same-yard quiet", V.yardIndex===ysp246Y1 && V.yardSwitchT===0 && V.yardPrevIndex===-1 && V.yardSwitchK()===1, `y=${V.yardIndex} t=${V.yardSwitchT} prev=${V.yardPrevIndex} k=${V.yardSwitchK()}`);

// rematch keeps yard, no forced fade
const ysp246Keep = V.yardIndex;
V.yardSwitchT = 0; V.yardPrevIndex = -1;
V.resetRound();
check("yard switch pop leftover live rematch keeps yard quiet", V.yardIndex===ysp246Keep && V.yardSwitchT===0, `y=${V.yardIndex} keep=${ysp246Keep} t=${V.yardSwitchT}`);

// punch cover still works with drawCourtyard path
const ysp246Hit = landCloseSlashPunch();
check("yard switch pop leftover live punch cover still arms", ysp246Hit && V.hitstopLeft>0 && V.shake>0 && V.punchCover()===V.PUNCH_PX, `hit=${ysp246Hit} cover=${V.punchCover()} shake=${V.shake} hs=${V.hitstopLeft}`);

check("cache v=296 after yard switch pop leftover live", /game.js\?v=350/.test(htmlTxt));

// --- v247 title music pop leftover: crossfade title↔duel beds (juice-only) ---
check("title music pop leftover seats no hard-cut bed pop", /Title music pop leftover/.test(srcTxt) && /hard-cut/.test(srcTxt) && /pause one \/ play the/.test(srcTxt) && /KO→over/.test(srcTxt) && /rematch/.test(srcTxt) && /Esc→title/.test(srcTxt) && /a pop, not a settle/.test(srcTxt) && /Crossfade beds over MUSIC_FADE_MS/.test(srcTxt));
check("title music pop leftover is juice-only", /Juice-only \(syncMusic \/ tickMusicFade\)/.test(srcTxt) && /tickMusicFade/.test(srcTxt) && /musicFadeK/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && !/musicFadeT/.test(sliceFn(codeOnly, "destRect")) && !/musicFadeT/.test(sliceFn(codeOnly, "bodyAABB")) && !/musicFadeT/.test(sliceFn(codeOnly, "slashPose")) && !/musicFadeK/.test(sliceFn(codeOnly, "landHit")) && !/musicFadeK/.test(sliceFn(codeOnly, "idleBreath")));
check("title music pop leftover falling keeps duel", /Falling keeps duel/.test(srcTxt) && /mode === "play" \|\| mode === "falling"/.test(sliceFn(codeOnly, "musicWant")) && /mode === "title" \|\| mode === "over"/.test(sliceFn(codeOnly, "musicWant")));
check("title music pop leftover arms crossfade on bed change", /musicFadeFrom = musicBed/.test(sliceFn(codeOnly, "syncMusic")) && /musicFadeT = MUSIC_FADE_MS/.test(sliceFn(codeOnly, "syncMusic")) && /musicBed = want/.test(sliceFn(codeOnly, "syncMusic")) && /toA\.volume = 0/.test(sliceFn(codeOnly, "syncMusic")));
check("title music pop leftover smoothsteps k", /function musicFadeK/.test(srcTxt) && /musicFadeT \/ MUSIC_FADE_MS/.test(sliceFn(codeOnly, "musicFadeK")) && /u \* u \* \(3 - 2 \* u\)/.test(sliceFn(codeOnly, "musicFadeK")) && /musicFadeT <= 0/.test(sliceFn(codeOnly, "musicFadeK")));
check("title music pop leftover drains on title\/falling\/over", /Title music pop leftover: bed crossfade drains/.test(srcTxt) && /tickMusicFade\(dt\)/.test(sliceFn(codeOnly, "update")) && /musicFadeT = Math\.max\(0, musicFadeT - dt\)/.test(sliceFn(codeOnly, "tickMusicFade")));
check("title music pop leftover same-bed quiet", /Same-bed sync stays quiet/.test(srcTxt) && /want === musicBed/.test(sliceFn(codeOnly, "syncMusic")) && /musicFadeT <= 0/.test(sliceFn(codeOnly, "syncMusic")));
check("title music pop leftover pauses from-bed after settle", /Pause the from-bed only after the fade settles/.test(srcTxt) && /fromA\.pause\(\)/.test(sliceFn(codeOnly, "tickMusicFade")) && /musicFadeFrom !== musicBed/.test(sliceFn(codeOnly, "tickMusicFade")));
check("title music pop leftover applyMusicVol respects fade", /While crossfading, from fades out and to fades in/.test(srcTxt) && /g \* \(1 - k\)/.test(sliceFn(codeOnly, "applyMusicVol")) && /g \* k/.test(sliceFn(codeOnly, "applyMusicVol")));
check("title music pop leftover is not a new combat verb", V.MUSIC_FADE_MS===320 && V.YARD_SWITCH_MS===280 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140 && V.HITSTUN===350);
check("slash frames still 180/140/280 after title music pop leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after title music pop leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after title music pop leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after title music pop leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after title music pop leftover", /game.js\?v=350/.test(htmlTxt));

// live: KO→over arms title fade; rematch arms duel fade; falling keeps duel; AABB planted
bootPlay(); wait(200);
V.musicUnlocked = true;
V.musicBed = "duel"; V.musicFadeFrom = ""; V.musicFadeT = 0;
V.applyMusicVol();
const tmp247Aabb0 = V.bodyAABB(V.player);
const tmp247Dest0 = V.destRect(V.player);
V.rival.hp = 0; V.koTarget = V.rival; V.rival.falling = false;
V.beginFall();
check("title music pop leftover live falling keeps duel want", V.mode==="falling" && V.musicWant()==="duel", `mode=${V.mode} want=${V.musicWant()}`);
V.syncMusic(true);
check("title music pop leftover live falling sync keeps duel bed", V.musicBed==="duel" && V.musicFadeT===0, `bed=${V.musicBed} t=${V.musicFadeT}`);
check("title music pop leftover live AABB planted", Math.abs(V.bodyAABB(V.player).y-tmp247Aabb0.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-tmp247Aabb0.h)<0.05, `dy=${(V.bodyAABB(V.player).y-tmp247Aabb0.y).toFixed(2)}`);
check("title music pop leftover live destRect planted", Math.abs(V.destRect(V.player).dy-tmp247Dest0.dy)<2.2, `ddy=${(V.destRect(V.player).dy-tmp247Dest0.dy).toFixed(2)}`);

// finish fall → over arms title crossfade (stop on over so fade is fresh)
for(let i=0;i<Math.ceil(V.FALL_MS/STEP)+8;i++){
  V.update(STEP);
  if(V.mode==="over") break;
}
check("title music pop leftover live KO→over arms fade", V.mode==="over" && V.musicBed==="title" && V.musicFadeFrom==="duel" && V.musicFadeT>0 && V.musicFadeT<=V.MUSIC_FADE_MS && V.musicFadeK()<0.15, `mode=${V.mode} bed=${V.musicBed} from=${V.musicFadeFrom} t=${V.musicFadeT} k=${V.musicFadeK()}`);

const tmp247T0 = V.musicFadeT;
const tmp247K0 = V.musicFadeK();
for(let i=0;i<6;i++) V.update(STEP);
check("title music pop leftover live drains", V.musicFadeT<tmp247T0-STEP*4 && V.musicFadeK()>tmp247K0 && V.musicFadeK()<1, `t=${V.musicFadeT} t0=${tmp247T0} k=${V.musicFadeK()} k0=${tmp247K0}`);
check("title music pop leftover live mid k rising", V.musicFadeT>0 && V.musicFadeFrom==="duel" && V.musicBed==="title" && V.musicFadeK()>0.05 && V.musicFadeK()<0.95, `t=${V.musicFadeT} from=${V.musicFadeFrom} k=${V.musicFadeK()}`);

for(let i=0;i<40;i++) V.update(STEP);
check("title music pop leftover live settles title", V.musicFadeT===0 && V.musicFadeK()===1 && V.musicBed==="title" && V.musicFadeFrom==="", `t=${V.musicFadeT} k=${V.musicFadeK()} bed=${V.musicBed} from=${V.musicFadeFrom}`);

// rematch → duel fade
V.resetRound();
check("title music pop leftover live rematch arms duel fade", V.mode==="play" && V.musicBed==="duel" && V.musicFadeFrom==="title" && V.musicFadeT===V.MUSIC_FADE_MS && V.musicFadeK()<0.05, `mode=${V.mode} bed=${V.musicBed} from=${V.musicFadeFrom} t=${V.musicFadeT} k=${V.musicFadeK()}`);

// same-bed quiet
V.musicFadeT = 0; V.musicFadeFrom = ""; V.musicBed = "duel";
V.syncMusic(true);
check("title music pop leftover live same-bed quiet", V.musicBed==="duel" && V.musicFadeT===0 && V.musicFadeFrom==="", `bed=${V.musicBed} t=${V.musicFadeT} from=${V.musicFadeFrom}`);

// Esc→title arms fade
V.musicBed = "duel"; V.musicFadeFrom = ""; V.musicFadeT = 0;
V.goTitleFromPlay();
check("title music pop leftover live Esc→title arms fade", V.mode==="title" && V.musicBed==="title" && V.musicFadeFrom==="duel" && V.musicFadeT===V.MUSIC_FADE_MS, `mode=${V.mode} bed=${V.musicBed} from=${V.musicFadeFrom} t=${V.musicFadeT}`);

check("cache v=296 after title music pop leftover live", /game.js\?v=350/.test(htmlTxt));

// --- v248 knife plant end destRect leftover: stay on plant ease through bolt recovery ---
check("knife plant end destRect leftover seats no fade~0 hard-zero cliff", /Knife plant end destRect leftover/.test(srcTxt) && /boltPlantFade >0 gate used to/.test(srcTxt) && /fall through/.test(srcTxt) && /fade hit ~0/.test(srcTxt) && /last recovery frame/.test(srcTxt) && /Throw plant end already stayed/.test(srcTxt) && /Stay on[\s\S]{0,40}plant ease through bolt recovery/.test(srcTxt));
check("knife plant end destRect leftover is destRect-only", /destRect-only \(idleBreath\)/.test(srcTxt) && /f\.boltPhase === "recovery" && !holdingCutBolt\(f\)/.test(sliceFn(codeOnly, "idleBreath")) && /return amp \* \(1 - boltPlantFade\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && !/boltPlantFade/.test(sliceFn(codeOnly, "destRect")) && !/boltPlantFade/.test(sliceFn(codeOnly, "bodyAABB")) && !/boltPlantFade/.test(sliceFn(codeOnly, "poseBitmap")) && !/boltPlantFade/.test(sliceFn(codeOnly, "slashPose")));
check("knife plant end destRect leftover stays on plant through recovery", /if \(f\.boltPhase === "recovery" && !holdingCutBolt\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /fade 0 = full amp ready for idle/.test(srcTxt) && /return amp \* \(1 - boltPlantFade\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && !/boltPlantFade\(f\) > 0/.test(sliceFn(codeOnly, "idleBreath")));
check("knife plant end destRect leftover special-cancel still zeros", /Special-cancel still zeros \(holdingCutBolt\)/.test(srcTxt) && /!holdingCutBolt\(f\)/.test(sliceFn(codeOnly, "idleBreath")) && /holdingCutBolt\(f\)\) return 0/.test(sliceFn(codeOnly, "idleBreath")));
check("knife plant end destRect leftover sheet fade unchanged", /function boltPlantFade/.test(srcTxt) && /Sheet still boltPlantFade/.test(srcTxt) && /if \(f\.boltPhase !== "recovery"\) return 0;/.test(sliceFn(codeOnly, "boltPlantFade")) && /restBolt = bf > 0\.02/.test(sliceFn(codeOnly, "drawKnight")));
check("knife plant end destRect leftover throw plant end unchanged", /f\.cut === "throw" && f\.phase === "recovery"/.test(sliceFn(codeOnly, "idleBreath")) && /return amp \* \(1 - throwPlantFade\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /Throw plant end destRect leftover/.test(srcTxt));
check("knife plant end destRect leftover tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("knife plant end destRect leftover wakeup\/feint\/cutRec paths unchanged", /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "idleBreath")) && /Math\.max\(fk, rk\)/.test(sliceFn(codeOnly, "idleBreath")) && /Math\.max\(ck, rk\)/.test(sliceFn(codeOnly, "idleBreath")) && /Math\.max\(ck, wk\)/.test(sliceFn(codeOnly, "idleBreath")) && /f\.guardPoseK > 0 && !f\.guarding && \(walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\)/.test(sliceFn(codeOnly, "idleBreath")));
check("knife plant end destRect leftover is not a new combat verb", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140 && V.HITSTUN===350 && V.THROW_RECOVERY===220);
check("slash frames still 180/140/280 after knife plant end destRect leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after knife plant end destRect leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after knife plant end destRect leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after knife plant end destRect leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after knife plant end destRect leftover", /game.js\?v=350/.test(htmlTxt));

// live: late knife recovery → idle has no fade~0 breath cliff / destRect hop
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.hp=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cut="slash"; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.walkSettleT=0; V.player.walkRiseT=0;
V.player.boltHoldCut=false; V.player.boltSuper=false;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=200; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
let kpe248AmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ kpe248AmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.boltPhase="recovery";
V.player.boltT = V.BOLT_RECOVERY - STEP*3;
V.player.boltHoldCut=false; V.player.boltSuper=false;
V.player.clashRec=false; V.player.techRec=false; V.player.cutRecBreathT=0;
V.player.gait=0; V.player.guarding=false; V.player.guardPoseK=0;
const kpe248Aabb0=V.bodyAABB(V.player).y;
let kpe248PrevBr=V.idleBreath(V.player), kpe248Cliff=false, kpe248MaxHop=0, kpe248AabbOk=true, kpe248EndBr=0, kpe248SawIdle=false;
for(let i=0;i<18;i++){
  holdRivalStill();
  const dy0=V.destRect(V.player).dy;
  const br=V.idleBreath(V.player);
  const fade=V.boltPlantFade(V.player);
  if(V.player.boltPhase==="recovery" && Math.abs(br)<0.05 && Math.abs(kpe248PrevBr)>1.0) kpe248Cliff=true;
  V.update(STEP);
  kpe248MaxHop=Math.max(kpe248MaxHop, Math.abs(V.destRect(V.player).dy-dy0));
  if(Math.abs(V.bodyAABB(V.player).y-kpe248Aabb0)>0.05) kpe248AabbOk=false;
  if(!V.player.boltPhase){ kpe248SawIdle=true; kpe248EndBr=br; }
  kpe248PrevBr=br;
  if(kpe248SawIdle && !V.player.boltPhase && i>6) break;
}
check("knife plant end destRect leftover live amp ready", kpe248AmpReady, `ready=${kpe248AmpReady} modeT=${V.modeT}`);
check("knife plant end destRect leftover live no fade~0 cliff", !kpe248Cliff, `cliff=${kpe248Cliff}`);
check("knife plant end destRect leftover live no breath dump hop", kpe248MaxHop<0.5, `hop=${kpe248MaxHop.toFixed(3)}`);
check("knife plant end destRect leftover live idle inherits plant", kpe248SawIdle && Math.abs(kpe248EndBr)>1.0 && !V.player.boltPhase && V.player.phase==="idle", `saw=${kpe248SawIdle} endBr=${kpe248EndBr.toFixed(3)} bph=${V.player.boltPhase} ph=${V.player.phase}`);
check("knife plant end destRect leftover live AABB planted", kpe248AabbOk, `aabbOk=${kpe248AabbOk}`);

// live: mid knife recovery still eases breath in with plant fade
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.cutRecBreathT=0; V.player.gait=0; V.player.hp=100; V.player.stamina=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cut="slash";
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=500; V.rival.x=1100;
let kpe248EaseAmp=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ kpe248EaseAmp=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.boltPhase="recovery"; V.player.boltT = V.BOLT_RECOVERY * 0.15; V.player.boltHoldCut=false; V.player.boltSuper=false;
V.player.clashRec=false; V.player.techRec=false;
const kpe248Br0=V.idleBreath(V.player), kpe248Fade0=V.boltPlantFade(V.player);
let kpe248Br1=kpe248Br0, kpe248Up=false;
for(let i=0;i<20;i++){
  holdRivalStill(); V.update(STEP);
  if(V.player.boltPhase!=="recovery") break;
  const br=V.idleBreath(V.player);
  if(Math.abs(br)>Math.abs(kpe248Br1)+0.05) kpe248Up=true;
  kpe248Br1=br;
}
check("knife plant end destRect leftover live mid-recovery still eases", kpe248EaseAmp && kpe248Fade0>0.7 && Math.abs(kpe248Br0)<0.4 && kpe248Up && Math.abs(kpe248Br1)>Math.abs(kpe248Br0)+0.3, `ready=${kpe248EaseAmp} fade0=${kpe248Fade0.toFixed(3)} br0=${kpe248Br0.toFixed(3)} br1=${kpe248Br1.toFixed(3)} up=${kpe248Up}`);

// live: throw plant end still eases (unchanged regression)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.cutRecBreathT=0; V.player.gait=0; V.player.hp=100; V.player.stamina=100;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=500; V.rival.x=1100;
let kpe248ThrowAmp=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ kpe248ThrowAmp=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.cut="throw"; V.player.phase="recovery"; V.player.phaseT = V.THROW_RECOVERY * 0.15;
V.player.clashRec=false; V.player.techRec=false;
const kpe248ThrowBr0=V.idleBreath(V.player), kpe248ThrowFade0=V.throwPlantFade(V.player);
let kpe248ThrowBr1=kpe248ThrowBr0, kpe248ThrowUp=false;
for(let i=0;i<20;i++){
  holdRivalStill(); V.update(STEP);
  if(V.player.phase!=="recovery") break;
  const br=V.idleBreath(V.player);
  if(Math.abs(br)>Math.abs(kpe248ThrowBr1)+0.05) kpe248ThrowUp=true;
  kpe248ThrowBr1=br;
}
check("knife plant end destRect leftover live throw plant still eases", kpe248ThrowAmp && kpe248ThrowFade0>0.7 && kpe248ThrowUp, `ready=${kpe248ThrowAmp} fade0=${kpe248ThrowFade0.toFixed(3)} br0=${kpe248ThrowBr0.toFixed(3)} br1=${kpe248ThrowBr1.toFixed(3)} up=${kpe248ThrowUp}`);

check("knife plant end destRect leftover rival idleBreath shared", /idleBreath\(rival/.test(srcTxt) || /f\.boltPhase === "recovery" && !holdingCutBolt\(f\)/.test(sliceFn(codeOnly, "idleBreath")));
check("cache v=296 after knife plant end destRect leftover live", /game.js\?v=350/.test(htmlTxt));




// --- v249 feint walk-out pose leftover: rest windup on walk when gaitWalkOn ---
check("feint walk-out pose leftover seats no idle dump mid-stride", /Feint walk-out pose leftover/.test(srcTxt) && /leftover windup used to rest on idle/.test(srcTxt) && /gaitWalkOn already flipped poseBitmap to walk/.test(srcTxt) && /A\/D mid-pull/.test(srcTxt) && /rival same/.test(srcTxt) && /popped the passing sheet/.test(srcTxt) && /fade[\s\S]{0,20}dropped under 0\.02/.test(srcTxt));
check("feint walk-out pose leftover is draw-only", /Draw-only \(drawKnight\)/.test(srcTxt) && /restFeintWalk/.test(sliceFn(codeOnly, "drawKnight")) && /gaitWalkOn\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && !/restFeintWalk/.test(sliceFn(codeOnly, "destRect")) && !/restFeintWalk/.test(sliceFn(codeOnly, "bodyAABB")) && !/restFeintWalk/.test(sliceFn(codeOnly, "slashPose")) && !/restFeintWalk/.test(sliceFn(codeOnly, "idleBreath")) && !/restFeintWalk/.test(sliceFn(codeOnly, "feintFade")) && !/restFeintWalk/.test(sliceFn(codeOnly, "tickFeint")) && !/restFeintWalk/.test(sliceFn(codeOnly, "poseBitmap")));
check("feint walk-out pose leftover rests windup on walk sheet", /restFeintWalk/.test(sliceFn(codeOnly, "drawKnight")) && /gaitWalkOn\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)\) && ready\(wind\) && ready\(walk\)/.test(sliceFn(codeOnly, "drawKnight")) && /octx\.drawImage\(walk, 0, 0, dw, dh\)/.test(sliceFn(codeOnly, "drawKnight")) && /restFeint \|\| restFeintGuard \|\| restFeintWalk/.test(sliceFn(codeOnly, "drawKnight")));
check("feint walk-out pose leftover idle pull still rests on idle", /Idle pull still rests on idle/.test(srcTxt) && /restFeint = ff > 0\.02 && !restFeintGuard && !restFeintWalk/.test(sliceFn(codeOnly, "drawKnight")) && /octx\.drawImage\(idle, 0, 0, dw, dh\)/.test(sliceFn(codeOnly, "drawKnight")));
check("feint walk-out pose leftover feint→guard unchanged", /Feint→guard sheet leftover unchanged/.test(srcTxt) && /restFeintGuard/.test(sliceFn(codeOnly, "drawKnight")) && /f\.guarding && ready\(wind\) && ready\(block\)/.test(sliceFn(codeOnly, "drawKnight")) && /Keep leftover windup through leftover feintT if they raise/.test(srcTxt));
check("feint walk-out pose leftover no idle\+walk stack", /Live walk still one opaque poseBitmap sheet/.test(srcTxt) && /no idle\+walk stack/.test(srcTxt) && /gaitWalkOn\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && !/restFeintWalk[\s\S]{0,80}drawImage\(idle/.test(sliceFn(codeOnly, "drawKnight")));
check("feint walk-out pose leftover destRect breath already maxed", /Feint settle→walk plant-release destRect leftover/.test(srcTxt) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "idleBreath")) && /f\.feintT > 0/.test(sliceFn(codeOnly, "idleBreath")));
check("feint walk-out pose leftover skips guard drop stack", /!restFeint && !restFeintGuard && !restFeintWalk/.test(sliceFn(codeOnly, "drawKnight")) && /Skip when feint owns windup/.test(srcTxt));
check("feint walk-out pose leftover feintFade clock unchanged", /function feintFade/.test(srcTxt) && /FEINT_RECOVERY/.test(sliceFn(codeOnly, "feintFade")) && V.FEINT_RECOVERY===100 && /This fade unchanged/.test(srcTxt));
check("feint walk-out pose leftover is not a new combat verb", V.FEINT_RECOVERY===100 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140 && V.HITSTUN===350 && V.WALK_STEP_MS===320 && V.WALK_SETTLE_MS===160);
check("slash frames still 180/140/280 after feint walk-out pose leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after feint walk-out pose leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after feint walk-out pose leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after feint walk-out pose leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after feint walk-out pose leftover", /game.js\?v=350/.test(htmlTxt));

// live: A/D mid-feint rests windup on walk (no idle base while gaitWalkOn)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="startup"; V.player.phaseT=40; V.player.cut="slash"; V.player.stunT=0; V.player.guarding=false;
V.player.boltPhase=""; V.player.falling=false; V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0;
V.player.clashRec=false; V.player.techRec=false; V.player.cutRecBreathT=0; V.player.guardPoseK=0;
V.player.telegraph=true; V.player.hp=100; V.player.stamina=100;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=400; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
const fwo249Ok = V.startFeint(V.player);
V.keys.add("KeyD");
let fwo249SawWalk=false, fwo249IdleBaseBug=false, fwo249AabbOk=true, fwo249MaxHop=0;
const fwo249Aabb0 = V.bodyAABB(V.player).y;
let fwo249PrevDy = V.destRect(V.player).dy;
for (let i=0;i<14;i++){
  holdRivalStill();
  const ff = V.feintFade(V.player);
  const walkOn = V.gaitWalkOn(V.player);
  // restFeintWalk owns when ff>0.02 && gaitWalkOn; poseBitmap must be walk
  if (V.player.feintT > 0 && ff > 0.02 && walkOn) {
    fwo249SawWalk = true;
    if (!/walk/.test(sheetSrc(V.player))) fwo249IdleBaseBug = true;
  }
  const dy0 = V.destRect(V.player).dy;
  V.update(STEP);
  fwo249MaxHop = Math.max(fwo249MaxHop, Math.abs(V.destRect(V.player).dy - dy0));
  if (Math.abs(V.bodyAABB(V.player).y - fwo249Aabb0) > 0.05) fwo249AabbOk = false;
  fwo249PrevDy = V.destRect(V.player).dy;
  if (V.player.feintT <= 0 && i > 4) break;
}
check("feint walk-out pose leftover live arms feint", fwo249Ok && V.FEINT_RECOVERY===100, `ok=${fwo249Ok} feintT=${V.player.feintT}`);
check("feint walk-out pose leftover live saw gaitWalkOn mid-pull", fwo249SawWalk, `saw=${fwo249SawWalk}`);
check("feint walk-out pose leftover live poseBitmap walk under windup", fwo249SawWalk && !fwo249IdleBaseBug, `saw=${fwo249SawWalk} bug=${fwo249IdleBaseBug}`);
check("feint walk-out pose leftover live AABB planted", fwo249AabbOk, `aabbOk=${fwo249AabbOk}`);
check("feint walk-out pose leftover live destRect no sheet hop", fwo249MaxHop < 3.5, `hop=${fwo249MaxHop.toFixed(3)}`);

// live: idle feint still rests on idle (no walk)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="startup"; V.player.phaseT=40; V.player.cut="slash"; V.player.stunT=0; V.player.guarding=false;
V.player.boltPhase=""; V.player.falling=false; V.player.gait=0; V.player.telegraph=true;
V.player.x=500; V.rival.x=1100;
const fwo249IdleOk = V.startFeint(V.player);
let fwo249IdleRest=true, fwo249IdleSaw=false;
for (let i=0;i<10;i++){
  holdRivalStill();
  const ff = V.feintFade(V.player);
  if (V.player.feintT > 0 && ff > 0.02) {
    fwo249IdleSaw = true;
    if (/walk/.test(sheetSrc(V.player)) || V.gaitWalkOn(V.player) || V.player.gait!==0) fwo249IdleRest = false;
  }
  V.update(STEP);
  if (V.player.feintT <= 0 && i > 3) break;
}
check("feint walk-out pose leftover live idle pull still idle", fwo249IdleOk && fwo249IdleSaw && fwo249IdleRest && V.player.gait===0, `ok=${fwo249IdleOk} saw=${fwo249IdleSaw} rest=${fwo249IdleRest} gait=${V.player.gait}`);

// live: rival shares drawKnight restFeintWalk
check("feint walk-out pose leftover rival draw shared", /restFeintWalk/.test(sliceFn(codeOnly, "drawKnight")) && /gaitWalkOn\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /startFeint\(f\)/.test(srcTxt) && /rivalTryFeint/.test(srcTxt));
check("cache v=296 after feint walk-out pose leftover live", /game.js\?v=350/.test(htmlTxt));



// --- v250 sheathe walk-out pose leftover: rest slash on walk when gaitWalkOn ---
check("sheathe walk-out pose leftover seats no idle dump mid-stride", /Sheathe walk-out pose leftover/.test(srcTxt) && /leftover slash used to rest on idle/.test(srcTxt) && /gaitWalkOn already flipped poseBitmap to walk/.test(srcTxt) && /sheathing\(\) skips walk/.test(srcTxt) && /A\/D mid-sheathe/.test(srcTxt) && /rival same/.test(srcTxt) && /popped the passing sheet/.test(srcTxt) && /fade hit 0/.test(srcTxt));
check("sheathe walk-out pose leftover is draw-only", /Draw-only \(drawKnight\)/.test(srcTxt) && /restSheatheWalk/.test(sliceFn(codeOnly, "drawKnight")) && /gaitWalkOn\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && !/restSheatheWalk/.test(sliceFn(codeOnly, "destRect")) && !/restSheatheWalk/.test(sliceFn(codeOnly, "bodyAABB")) && !/restSheatheWalk/.test(sliceFn(codeOnly, "slashPose")) && !/restSheatheWalk/.test(sliceFn(codeOnly, "idleBreath")) && !/restSheatheWalk/.test(sliceFn(codeOnly, "sheatheFade")) && !/restSheatheWalk/.test(sliceFn(codeOnly, "tickSheathe")) && !/restSheatheWalk/.test(sliceFn(codeOnly, "poseBitmap")));
check("sheathe walk-out pose leftover rests slash on walk sheet", /restSheatheWalk/.test(sliceFn(codeOnly, "drawKnight")) && /fade > 0 && !f\.guarding && \(gaitWalkOn\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)\) && ready\(cut\) && ready\(walk\)/.test(sliceFn(codeOnly, "drawKnight")) && /octx\.drawImage\(walk, 0, 0, dw, dh\)/.test(sliceFn(codeOnly, "drawKnight")) && /restSheatheWalk/.test(sliceFn(codeOnly, "drawKnight")));
check("sheathe walk-out pose leftover idle sheathe still rests on idle", /Idle sheathe still rests on idle/.test(srcTxt) && /restSheathe = fade > 0 && !f\.guarding && !restSheatheWalk/.test(sliceFn(codeOnly, "drawKnight")) && /octx\.drawImage\(idle, 0, 0, dw, dh\)/.test(sliceFn(codeOnly, "drawKnight")));
check("sheathe walk-out pose leftover sheathe→guard unchanged", /Sheathe→guard sheet leftover unchanged/.test(srcTxt) && /restSheatheWalk = fade > 0 && !f\.guarding/.test(sliceFn(codeOnly, "drawKnight")) && /Keep leftover sheathe through leftover sheatheT if/.test(srcTxt) && /poseBitmap still block once/.test(srcTxt));
check("sheathe walk-out pose leftover no idle+walk stack", /Live walk still one opaque poseBitmap sheet/.test(srcTxt) && /no idle\+walk stack/.test(srcTxt) && /gaitWalkOn\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /else if \(restSheatheWalk\) \{[\s\S]{0,60}drawImage\(walk/.test(sliceFn(codeOnly, "drawKnight")) && !/else if \(restSheatheWalk\) \{[\s\S]{0,60}drawImage\(idle/.test(sliceFn(codeOnly, "drawKnight")));
check("sheathe walk-out pose leftover recovery fade idle unless A/D", /Idle recovery still rests on idle/.test(srcTxt) && /recoveryWalkResting\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /restSheatheWalk = fade > 0 && !f\.guarding && \(gaitWalkOn\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)\)/.test(sliceFn(codeOnly, "drawKnight")) && /f\.phase === "recovery" && \(f\.cut === "golpe" \|\| f\.cut === "slash"\)/.test(sliceFn(codeOnly, "sheatheFade")));
check("sheathe walk-out pose leftover sheatheFade clock unchanged", /function sheatheFade/.test(srcTxt) && /This fade unchanged/.test(srcTxt) && /Sheathe walk-out pose leftover: drawKnight rests leftover slash on walk/.test(srcTxt) && V.SHEATHE_MS===140);
check("sheathe walk-out pose leftover is not a new combat verb", V.SHEATHE_MS===140 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.GUARD_RAISE_MS===80 && V.HITSTUN===350 && V.WALK_STEP_MS===320 && V.WALK_SETTLE_MS===160 && V.FEINT_RECOVERY===100);
check("slash frames still 180/140/280 after sheathe walk-out pose leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after sheathe walk-out pose leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after sheathe walk-out pose leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after sheathe walk-out pose leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after sheathe walk-out pose leftover", /game.js\?v=350/.test(htmlTxt));

// live: A/D mid-sheathe rests slash on walk (poseBitmap walk; sheathing skipped)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false;
V.player.boltPhase=""; V.player.falling=false; V.player.feintT=0; V.player.gait=0;
V.player.clashRec=false; V.player.techRec=false; V.player.cutRecBreathT=0; V.player.guardPoseK=0;
V.player.cut="slash"; V.player.hp=100; V.player.stamina=100;
V.player.sheatheT = V.SHEATHE_MS;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=400; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
V.keys.add("KeyD");
let swo250SawWalk=false, swo250IdleBaseBug=false, swo250AabbOk=true, swo250MaxHop=0, swo250FadeLive=false;
const swo250Aabb0 = V.bodyAABB(V.player).y;
for (let i=0;i<16;i++){
  holdRivalStill();
  const sf = V.sheatheFade(V.player);
  const walkOn = V.gaitWalkOn(V.player);
  // restSheatheWalk owns when fade>0 && gaitWalkOn; poseBitmap must be walk (sheathing skips)
  if (V.player.sheatheT > 0 && sf > 0 && walkOn) {
    swo250SawWalk = true;
    swo250FadeLive = true;
    if (!/walk/.test(sheetSrc(V.player))) swo250IdleBaseBug = true;
    if (/slash/.test(sheetSrc(V.player)) && !/walk/.test(sheetSrc(V.player))) swo250IdleBaseBug = true;
  }
  const dy0 = V.destRect(V.player).dy;
  V.update(STEP);
  swo250MaxHop = Math.max(swo250MaxHop, Math.abs(V.destRect(V.player).dy - dy0));
  if (Math.abs(V.bodyAABB(V.player).y - swo250Aabb0) > 0.05) swo250AabbOk = false;
  if (V.player.sheatheT <= 0 && i > 4) break;
}
check("sheathe walk-out pose leftover live arms sheathe", V.SHEATHE_MS===140, `shMs=${V.SHEATHE_MS}`);
check("sheathe walk-out pose leftover live saw gaitWalkOn mid-sheathe", swo250SawWalk && swo250FadeLive, `saw=${swo250SawWalk} fade=${swo250FadeLive}`);
check("sheathe walk-out pose leftover live poseBitmap walk under slash fade", swo250SawWalk && !swo250IdleBaseBug, `saw=${swo250SawWalk} bug=${swo250IdleBaseBug}`);
check("sheathe walk-out pose leftover live AABB planted", swo250AabbOk, `aabbOk=${swo250AabbOk}`);
check("sheathe walk-out pose leftover live destRect no sheet hop", swo250MaxHop < 3.5, `hop=${swo250MaxHop.toFixed(3)}`);

// live: idle sheathe still rests on idle (no walk)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false;
V.player.boltPhase=""; V.player.falling=false; V.player.gait=0; V.player.feintT=0;
V.player.clashRec=false; V.player.techRec=false; V.player.cut="slash";
V.player.sheatheT = V.SHEATHE_MS;
V.player.x=500; V.rival.x=1100;
let swo250IdleRest=true, swo250IdleSaw=false;
for (let i=0;i<10;i++){
  holdRivalStill();
  const sf = V.sheatheFade(V.player);
  if (V.player.sheatheT > 0 && sf > 0) {
    swo250IdleSaw = true;
    if (/walk/.test(sheetSrc(V.player)) || V.gaitWalkOn(V.player) || V.player.gait!==0) swo250IdleRest = false;
  }
  V.update(STEP);
  if (V.player.sheatheT <= 0 && i > 3) break;
}
check("sheathe walk-out pose leftover live idle sheathe still idle", swo250IdleSaw && swo250IdleRest && V.player.gait===0, `saw=${swo250IdleSaw} rest=${swo250IdleRest} gait=${V.player.gait}`);

// live: sheathe→guard still block (not walk rest)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false; V.player.guardPoseK=0;
V.player.boltPhase=""; V.player.falling=false; V.player.gait=0; V.player.feintT=0;
V.player.sheatheT = V.SHEATHE_MS; V.player.cut="slash";
V.player.x=500; V.rival.x=1100;
V.keys.add("KeyS");
let swo250GuardOk=false;
for (let i=0;i<8;i++){
  holdRivalStill();
  V.update(STEP);
  if (V.player.guarding && V.player.sheatheT > 0 && V.sheatheFade(V.player) > 0.02 && /block/.test(sheetSrc(V.player))) {
    swo250GuardOk = true; break;
  }
}
check("sheathe walk-out pose leftover live sheathe→guard still block", swo250GuardOk && V.player.guarding, `ok=${swo250GuardOk} g=${V.player.guarding} src=${sheetSrc(V.player)}`);

// live: rival shares drawKnight restSheatheWalk
check("sheathe walk-out pose leftover rival draw shared", /restSheatheWalk/.test(sliceFn(codeOnly, "drawKnight")) && /gaitWalkOn\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /sheathing\(f\)/.test(srcTxt));
check("cache v=296 after sheathe walk-out pose leftover live", /game.js\?v=350/.test(htmlTxt));





// --- v251 wakeup walk-out pose leftover: rest crumple on walk when gaitWalkOn ---
check("wakeup walk-out pose leftover seats no idle dump mid-stride", /Wakeup walk-out pose leftover/.test(srcTxt) && /leftover crumple used to rest on idle/.test(srcTxt) && /gaitWalkOn already flipped poseBitmap to walk/.test(srcTxt) && /A\/D mid-getup/.test(srcTxt) && /rival same/.test(srcTxt) && /popped the passing sheet/.test(srcTxt) && /fade[\s\S]{0,20}dropped under 0\.02/.test(srcTxt));
check("wakeup walk-out pose leftover is draw-only", /Draw-only \(drawKnight\)/.test(srcTxt) && /restWakeWalk/.test(sliceFn(codeOnly, "drawKnight")) && /gaitWalkOn\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && !/restWakeWalk/.test(sliceFn(codeOnly, "destRect")) && !/restWakeWalk/.test(sliceFn(codeOnly, "bodyAABB")) && !/restWakeWalk/.test(sliceFn(codeOnly, "slashPose")) && !/restWakeWalk/.test(sliceFn(codeOnly, "idleBreath")) && !/restWakeWalk/.test(sliceFn(codeOnly, "wakeupFade")) && !/restWakeWalk/.test(sliceFn(codeOnly, "tickThrowState")) && !/restWakeWalk/.test(sliceFn(codeOnly, "poseBitmap")));
check("wakeup walk-out pose leftover rests crumple on walk sheet", /restWakeWalk/.test(sliceFn(codeOnly, "drawKnight")) && /gaitWalkOn\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)\) && ready\(hurt\) && ready\(walk\)/.test(sliceFn(codeOnly, "drawKnight")) && /octx\.drawImage\(walk, 0, 0, dw, dh\)/.test(sliceFn(codeOnly, "drawKnight")) && /restWake \|\| restWakeGuard \|\| restWakeGuardWalk \|\| restWakeRev \|\| restWakeWalk/.test(sliceFn(codeOnly, "drawKnight")));
check("wakeup walk-out pose leftover idle getup still rests on idle", /Idle getup still rests on idle/.test(srcTxt) && /restWake = wake > 0\.02 && !restWakeGuard && !restWakeGuardWalk && !restWakeRev && !restWakeRevWalk && !restWakeWalk/.test(sliceFn(codeOnly, "drawKnight")) && /octx\.drawImage\(idle, 0, 0, dw, dh\)/.test(sliceFn(codeOnly, "drawKnight")));
check("wakeup walk-out pose leftover wakeup→guard unchanged", /Wakeup→guard \/ wakeup→reversal sheet leftovers unchanged/.test(srcTxt) && /restWakeGuard/.test(sliceFn(codeOnly, "drawKnight")) && /restWakeWalk = wake > 0\.02 && !restWakeGuard && !restWakeGuardWalk && !restWakeRev && !restWakeRevWalk && \(gaitWalkOn\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)\)/.test(sliceFn(codeOnly, "drawKnight")) && /Keep leftover crumple through leftover throw-invuln if they raise/.test(srcTxt));
check("wakeup walk-out pose leftover wakeup→reversal unchanged", /restWakeRev/.test(sliceFn(codeOnly, "drawKnight")) && /mid-getup reverse keeps leftover crumple on the/.test(srcTxt) && /Keep leftover crumple through leftover wakeRevFadeHold/.test(srcTxt));
check("wakeup walk-out pose leftover no idle+walk stack", /Live walk still one opaque poseBitmap sheet/.test(srcTxt) && /no idle\+walk stack/.test(srcTxt) && /gaitWalkOn\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /else if \(restWakeWalk\) \{[\s\S]{0,60}drawImage\(walk/.test(sliceFn(codeOnly, "drawKnight")) && !/else if \(restWakeWalk\) \{[\s\S]{0,60}drawImage\(idle/.test(sliceFn(codeOnly, "drawKnight")));
check("wakeup walk-out pose leftover destRect breath already zero walk-out", /destRect breath already 0 through walk-out getup/.test(srcTxt) && /if \(walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\) return 0;/.test(sliceFn(codeOnly, "idleBreath")) && /if \(wakeupFade\(f\) > 0/.test(sliceFn(codeOnly, "idleBreath")));
check("wakeup walk-out pose leftover wakeupFade clock unchanged", /function wakeupFade/.test(srcTxt) && /This fade unchanged/.test(srcTxt) && /Wakeup walk-out pose leftover: drawKnight rests leftover crumple on walk/.test(srcTxt) && V.THROW_WAKE_INVULN===80);
check("wakeup walk-out pose leftover is not a new combat verb", V.THROW_WAKE_INVULN===80 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140 && V.HITSTUN===350 && V.WALK_STEP_MS===320 && V.WALK_SETTLE_MS===160 && V.FEINT_RECOVERY===100);
check("slash frames still 180/140/280 after wakeup walk-out pose leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after wakeup walk-out pose leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after wakeup walk-out pose leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after wakeup walk-out pose leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after wakeup walk-out pose leftover", /game.js\?v=350/.test(htmlTxt));

// live: A/D mid-getup rests crumple on walk (poseBitmap walk; wakeupFade live)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false;
V.player.boltPhase=""; V.player.falling=false; V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0;
V.player.clashRec=false; V.player.techRec=false; V.player.cutRecBreathT=0; V.player.guardPoseK=0;
V.player.hp=100; V.player.stamina=100; V.player.thrownT=0; V.player.wakeRev=true; V.player.wakeRevFadeHold=0;
V.player.reversal=false; V.player.cut="";
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=400; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
V.player.throwInvulnT = V.THROW_WAKE_INVULN;
V.player.walkT = V.WALK_STEP_MS * 0.5; // mid-pass so gaitWalkOn arms immediately
V.keys.add("KeyD");
let wwo251SawWalk=false, wwo251IdleBaseBug=false, wwo251AabbOk=true, wwo251MaxHop=0, wwo251FadeLive=false;
const wwo251Aabb0 = V.bodyAABB(V.player).y;
for (let i=0;i<12;i++){
  holdRivalStill();
  const wf = V.wakeupFade(V.player);
  const walkOn = V.gaitWalkOn(V.player);
  // restWakeWalk owns when wake>0.02 && gaitWalkOn; poseBitmap must be walk
  if (V.player.throwInvulnT > 0 && wf > 0.02 && walkOn) {
    wwo251SawWalk = true;
    wwo251FadeLive = true;
    if (!/walk/.test(sheetSrc(V.player))) wwo251IdleBaseBug = true;
    if (/hurt/.test(sheetSrc(V.player)) && !/walk/.test(sheetSrc(V.player))) wwo251IdleBaseBug = true;
  }
  const dy0 = V.destRect(V.player).dy;
  V.update(STEP);
  wwo251MaxHop = Math.max(wwo251MaxHop, Math.abs(V.destRect(V.player).dy - dy0));
  if (Math.abs(V.bodyAABB(V.player).y - wwo251Aabb0) > 0.05) wwo251AabbOk = false;
  if (V.player.throwInvulnT <= 0 && i > 4) break;
}
check("wakeup walk-out pose leftover live arms getup", V.THROW_WAKE_INVULN===80, `invMs=${V.THROW_WAKE_INVULN}`);
check("wakeup walk-out pose leftover live saw gaitWalkOn mid-getup", wwo251SawWalk && wwo251FadeLive, `saw=${wwo251SawWalk} fade=${wwo251FadeLive}`);
check("wakeup walk-out pose leftover live poseBitmap walk under crumple fade", wwo251SawWalk && !wwo251IdleBaseBug, `saw=${wwo251SawWalk} bug=${wwo251IdleBaseBug}`);
check("wakeup walk-out pose leftover live AABB planted", wwo251AabbOk, `aabbOk=${wwo251AabbOk}`);
check("wakeup walk-out pose leftover live destRect no sheet hop", wwo251MaxHop < 3.5, `hop=${wwo251MaxHop.toFixed(3)}`);

// live: idle getup still rests on idle (no walk)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false;
V.player.boltPhase=""; V.player.falling=false; V.player.gait=0; V.player.feintT=0;
V.player.clashRec=false; V.player.techRec=false; V.player.hp=100;
V.player.thrownT=0; V.player.wakeRev=true; V.player.wakeRevFadeHold=0; V.player.reversal=false;
V.player.throwInvulnT = V.THROW_WAKE_INVULN;
V.player.x=500; V.rival.x=1100;
let wwo251IdleRest=true, wwo251IdleSaw=false;
for (let i=0;i<10;i++){
  holdRivalStill();
  const wf = V.wakeupFade(V.player);
  if (V.player.throwInvulnT > 0 && wf > 0.02) {
    wwo251IdleSaw = true;
    if (/walk/.test(sheetSrc(V.player)) || V.gaitWalkOn(V.player) || V.player.gait!==0) wwo251IdleRest = false;
  }
  V.update(STEP);
  if (V.player.throwInvulnT <= 0 && i > 3) break;
}
check("wakeup walk-out pose leftover live idle getup still idle", wwo251IdleSaw && wwo251IdleRest && V.player.gait===0, `saw=${wwo251IdleSaw} rest=${wwo251IdleRest} gait=${V.player.gait}`);

// live: wakeup→guard still block (not walk rest)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false; V.player.guardPoseK=0;
V.player.boltPhase=""; V.player.falling=false; V.player.gait=0; V.player.feintT=0;
V.player.thrownT=0; V.player.wakeRev=true; V.player.wakeRevFadeHold=0; V.player.reversal=false;
V.player.throwInvulnT = V.THROW_WAKE_INVULN; V.player.hp=100;
V.player.x=500; V.rival.x=1100;
V.keys.add("KeyS");
let wwo251GuardOk=false;
for (let i=0;i<8;i++){
  holdRivalStill();
  V.update(STEP);
  if (V.player.guarding && V.player.throwInvulnT > 0 && V.wakeupFade(V.player) > 0.02 && /block/.test(sheetSrc(V.player))) {
    wwo251GuardOk = true; break;
  }
}
check("wakeup walk-out pose leftover live wakeup→guard still block", wwo251GuardOk && V.player.guarding, `ok=${wwo251GuardOk} g=${V.player.guarding} src=${sheetSrc(V.player)}`);

// live: rival shares drawKnight restWakeWalk
check("wakeup walk-out pose leftover rival draw shared", /restWakeWalk/.test(sliceFn(codeOnly, "drawKnight")) && /gaitWalkOn\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /wakeupFade\(f\)/.test(srcTxt) && /throwInvulnT/.test(srcTxt));
check("cache v=296 after wakeup walk-out pose leftover live", /game.js\?v=350/.test(htmlTxt));

// --- v252 guard drop walk-out pose leftover: rest block on walk when gaitWalkOn ---
check("guard drop walk-out pose leftover seats no idle dump mid-stride", /Guard drop walk-out pose leftover/.test(srcTxt) && /leftover block used to rest on idle/.test(srcTxt) && /gaitWalkOn already flipped poseBitmap to walk/.test(srcTxt) && /A\/D mid-drop/.test(srcTxt) && /rival same/.test(srcTxt) && /popped the passing sheet/.test(srcTxt) && /fade[\s\S]{0,20}dropped under 0\.02/.test(srcTxt));
check("guard drop walk-out pose leftover is draw-only", /Draw-only \(drawKnight\)/.test(srcTxt) && /restGuardWalk/.test(sliceFn(codeOnly, "drawKnight")) && /gaitWalkOn\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && !/restGuardWalk/.test(sliceFn(codeOnly, "destRect")) && !/restGuardWalk/.test(sliceFn(codeOnly, "bodyAABB")) && !/restGuardWalk/.test(sliceFn(codeOnly, "slashPose")) && !/restGuardWalk/.test(sliceFn(codeOnly, "idleBreath")) && !/restGuardWalk/.test(sliceFn(codeOnly, "guardDropFade")) && !/restGuardWalk/.test(sliceFn(codeOnly, "tickGuardPose")) && !/restGuardWalk/.test(sliceFn(codeOnly, "poseBitmap")));
check("guard drop walk-out pose leftover rests block on walk sheet", /restGuardWalk/.test(sliceFn(codeOnly, "drawKnight")) && /\(gaitWalkOn\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)\) && ready\(block\) && ready\(walk\)/.test(sliceFn(codeOnly, "drawKnight")) && /octx\.drawImage\(walk, 0, 0, dw, dh\)/.test(sliceFn(codeOnly, "drawKnight")) && /restGuard \|\| restGuardWalk/.test(sliceFn(codeOnly, "drawKnight")));
check("guard drop walk-out pose leftover idle drop still rests on idle", /Idle drop still rests on idle/.test(srcTxt) && /restGuard = gf > 0\.02 && !restFeint && !restFeintGuard && !restFeintWalk && !restGuardWalk/.test(sliceFn(codeOnly, "drawKnight")) && /octx\.drawImage\(idle, 0, 0, dw, dh\)/.test(sliceFn(codeOnly, "drawKnight")));
check("guard drop walk-out pose leftover skips feint stack", /!restFeint && !restFeintGuard && !restFeintWalk && \(gaitWalkOn\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)\)/.test(sliceFn(codeOnly, "drawKnight")) && /Feint-from-drop skip unchanged/.test(srcTxt) && /Skip when feint owns windup/.test(srcTxt));
check("guard drop walk-out pose leftover no idle+walk stack", /Live walk still one opaque poseBitmap sheet/.test(srcTxt) && /no idle\+walk stack/.test(srcTxt) && /gaitWalkOn\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /else if \(restGuardWalk\) \{[\s\S]{0,60}drawImage\(walk/.test(sliceFn(codeOnly, "drawKnight")) && !/else if \(restGuardWalk\) \{[\s\S]{0,60}drawImage\(idle/.test(sliceFn(codeOnly, "drawKnight")));
check("guard drop walk-out pose leftover fade\/destRect already ease", /Walk-out fade \/ destRect raise×rise already ease/.test(srcTxt) && /function guardDropFade/.test(srcTxt) && /This fade unchanged/.test(srcTxt) && /Guard drop walk-out pose leftover: drawKnight rests leftover block on walk/.test(srcTxt) && /f\.guardPoseK > 0 && !f\.guarding && \(walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\)/.test(sliceFn(codeOnly, "idleBreath")));
check("guard drop walk-out pose leftover guardDropFade clock unchanged", /function guardDropFade/.test(srcTxt) && /guardRaiseK\(f\)/.test(sliceFn(codeOnly, "guardDropFade")) && /Keep leftover block through leftover guardPoseK/.test(srcTxt) && V.GUARD_RAISE_MS===80);
check("guard drop walk-out pose leftover is not a new combat verb", V.GUARD_RAISE_MS===80 && V.GUARD_COMMIT_MS===140 && V.STAMINA_BLOCK===20 && V.GUARD_BREAK_MS===400 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80 && V.WALK_STEP_MS===320 && V.WALK_SETTLE_MS===160 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("slash frames still 180/140/280 after guard drop walk-out pose leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after guard drop walk-out pose leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after guard drop walk-out pose leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after guard drop walk-out pose leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after guard drop walk-out pose leftover", /game.js\?v=350/.test(htmlTxt));

// live: A/D mid-drop rests block on walk (poseBitmap walk; guardDropFade live)
bootPlay(); wait(1700);
holdGuardSample();
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
holdGuardSample();
V.keys.clear();
V.player.guarding = false;
V.update(STEP);
freezeYouAI(); freezeRivalAI();
V.player.stunT=0; V.player.falling=false; V.player.phase="idle"; V.player.boltPhase=""; V.player.guardBreakT=0; V.hitstopLeft=0; V.player.feintT=0;
V.player.walkT = V.WALK_STEP_MS * 0.5; // mid-pass so gaitWalkOn arms immediately
V.keys.add("KeyA");
let gwo252SawWalk=false, gwo252IdleBaseBug=false, gwo252AabbOk=true, gwo252MaxHop=0, gwo252FadeLive=false;
const gwo252Aabb0 = V.bodyAABB(V.player).y;
for (let i=0;i<12;i++){
  freezeYouAI(); freezeRivalAI();
  V.player.stunT=0; V.player.falling=false; V.player.phase="idle"; V.player.boltPhase=""; V.player.guardBreakT=0; V.hitstopLeft=0; V.player.feintT=0;
  V.keys.clear(); V.keys.add("KeyA");
  const gf = V.guardDropFade(V.player);
  const walkOn = V.gaitWalkOn(V.player);
  // restGuardWalk owns when gf>0.02 && gaitWalkOn; poseBitmap must be walk
  if (V.player.guardPoseK > 0 && gf > 0.02 && walkOn) {
    gwo252SawWalk = true;
    gwo252FadeLive = true;
    if (!/walk/.test(sheetSrc(V.player))) gwo252IdleBaseBug = true;
    if (/block/.test(sheetSrc(V.player)) && !/walk/.test(sheetSrc(V.player))) gwo252IdleBaseBug = true;
  }
  const dy0 = V.destRect(V.player).dy;
  V.update(STEP);
  gwo252MaxHop = Math.max(gwo252MaxHop, Math.abs(V.destRect(V.player).dy - dy0));
  if (Math.abs(V.bodyAABB(V.player).y - gwo252Aabb0) > 0.05) gwo252AabbOk = false;
  if (V.player.guardPoseK <= 0 && i > 4) break;
}
check("guard drop walk-out pose leftover live arms drop", V.GUARD_RAISE_MS===80, `raiseMs=${V.GUARD_RAISE_MS}`);
check("guard drop walk-out pose leftover live saw gaitWalkOn mid-drop", gwo252SawWalk && gwo252FadeLive, `saw=${gwo252SawWalk} fade=${gwo252FadeLive}`);
check("guard drop walk-out pose leftover live poseBitmap walk under block fade", gwo252SawWalk && !gwo252IdleBaseBug, `saw=${gwo252SawWalk} bug=${gwo252IdleBaseBug}`);
check("guard drop walk-out pose leftover live AABB planted", gwo252AabbOk, `aabbOk=${gwo252AabbOk}`);
check("guard drop walk-out pose leftover live destRect no sheet hop", gwo252MaxHop < 3.5, `hop=${gwo252MaxHop.toFixed(3)}`);

// live: idle drop still rests on idle (no walk)
bootPlay(); wait(1700);
holdGuardSample();
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
holdGuardSample();
V.keys.clear();
V.player.guarding = false;
V.update(STEP);
let gwo252IdleRest=true, gwo252IdleSaw=false;
for (let i=0;i<10;i++){
  freezeYouAI(); freezeRivalAI();
  V.player.stunT=0; V.player.falling=false; V.player.phase="idle"; V.player.boltPhase=""; V.player.guardBreakT=0; V.hitstopLeft=0; V.player.feintT=0;
  V.keys.clear(); // standing idle drop
  const gf = V.guardDropFade(V.player);
  if (V.player.guardPoseK > 0 && gf > 0.02) {
    gwo252IdleSaw = true;
    if (/walk/.test(sheetSrc(V.player)) || V.gaitWalkOn(V.player) || V.player.gait!==0) gwo252IdleRest = false;
  }
  V.update(STEP);
  if (V.player.guardPoseK <= 0 && i > 3) break;
}
check("guard drop walk-out pose leftover live idle drop still idle", gwo252IdleSaw && gwo252IdleRest && V.player.gait===0, `saw=${gwo252IdleSaw} rest=${gwo252IdleRest} gait=${V.player.gait}`);

// live: raise still block (not walk rest)
bootPlay(); wait(1700);
holdGuardSample();
V.player.guarding = false;
V.player.guardPoseK = 0;
V.update(STEP);
let gwo252RaiseOk=false;
for (let i=0;i<8;i++){
  holdGuardSample();
  V.update(STEP);
  if (V.player.guarding && V.guardDropFade(V.player)===0 && /block/.test(sheetSrc(V.player))) {
    gwo252RaiseOk = true; break;
  }
}
check("guard drop walk-out pose leftover live raise still block", gwo252RaiseOk && V.player.guarding, `ok=${gwo252RaiseOk} g=${V.player.guarding} src=${sheetSrc(V.player)}`);

// live: rival shares drawKnight restGuardWalk
check("guard drop walk-out pose leftover rival draw shared", /restGuardWalk/.test(sliceFn(codeOnly, "drawKnight")) && /gaitWalkOn\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /guardDropFade\(f\)/.test(srcTxt) && /guardPoseK/.test(srcTxt));
check("cache v=296 after guard drop walk-out pose leftover live", /game.js\?v=350/.test(htmlTxt));

// --- v253 link plant sword raise leftover: Space/L/throw after sheathe rests windup not knife ---
check("link plant sword raise leftover seats no knife snap mid-raise", /Link plant sword raise leftover/.test(srcTxt) && /leftover sheathe→windup used throw_knife/.test(srcTxt) && /boltPlant prefers knife/.test(srcTxt) && /mashy Space\/L\/throw after clash\/tech/.test(srcTxt) && /faded idle\+slash into knife then snapped windup/.test(srcTxt) && /fade dropped under 0\.02/.test(srcTxt));
check("link plant sword raise leftover is draw-only", /Draw-only \(drawKnight\)/.test(srcTxt) && /linkSheet/.test(sliceFn(codeOnly, "drawKnight")) && /restLink/.test(sliceFn(codeOnly, "drawKnight")) && !/linkSheet/.test(sliceFn(codeOnly, "destRect")) && !/linkSheet/.test(sliceFn(codeOnly, "bodyAABB")) && !/linkSheet/.test(sliceFn(codeOnly, "poseBitmap")) && !/linkSheet/.test(sliceFn(codeOnly, "slashPose")) && !/linkSheet/.test(sliceFn(codeOnly, "idleBreath")) && !/linkSheet/.test(sliceFn(codeOnly, "linkPlantFade")) && !/linkSheet/.test(sliceFn(codeOnly, "startAttack")));
check("link plant sword raise leftover rests windup when not bolt", /linkSheet = \(f\.boltPhase === "startup"\) \? boltPlant : wind/.test(sliceFn(codeOnly, "drawKnight")) && /ready\(linkSheet\) && ready\(idle\) && idle !== linkSheet/.test(sliceFn(codeOnly, "drawKnight")) && /octx\.drawImage\(linkSheet, 0, 0, dw, dh\)/.test(sliceFn(codeOnly, "drawKnight")) && /Match telePlant/.test(srcTxt));
check("link plant sword raise leftover K still rests on knife", /Idle\/K sheathe raise still rests on knife/.test(srcTxt) && /boltPhase === "startup"\) \? boltPlant : wind/.test(sliceFn(codeOnly, "drawKnight")) && /ready\(knife\) \? knife : wind/.test(sliceFn(codeOnly, "drawKnight")) && /telePlant = \(f\.boltPhase === "startup"\) \? boltPlant : wind/.test(sliceFn(codeOnly, "drawKnight")));
check("link plant sword raise leftover clash\/holdCut unchanged", /Clash leftover pose \/ special-cancel holdCut unchanged/.test(srcTxt) && /restClash = cf > 0\.02/.test(sliceFn(codeOnly, "drawKnight")) && /ready\(cut\) && ready\(boltPlant\)/.test(sliceFn(codeOnly, "drawKnight")) && /restHoldCut = hf > 0\.02/.test(sliceFn(codeOnly, "drawKnight")) && /octx\.drawImage\(cut, 0, 0, dw, dh\)/.test(sliceFn(codeOnly, "drawKnight")));
check("link plant sword raise leftover poseBitmap still windup\/knife", /poseBitmap still windup\/knife immediately/.test(srcTxt) && /f\.phase === "startup" && ready\(wind\)/.test(sliceFn(codeOnly, "poseBitmap")) && /f\.boltPhase === "startup" && ready\(throwK\)/.test(sliceFn(codeOnly, "poseBitmap")) && !/linkSheet/.test(sliceFn(codeOnly, "poseBitmap")) && !/linkPlant/.test(sliceFn(codeOnly, "poseBitmap")));
check("link plant sword raise leftover linkPlantFade clock unchanged", /function linkPlantFade/.test(srcTxt) && /GUARD_RAISE_MS/.test(sliceFn(codeOnly, "linkPlantFade")) && /Fade leftover sheathe→windup/.test(srcTxt) && V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140);
check("link plant sword raise leftover is not a new combat verb", V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140 && V.CLASH_RECOVERY===200 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280);
check("slash frames still 180/140/280 after link plant sword raise leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after link plant sword raise leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after link plant sword raise leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after link plant sword raise leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after link plant sword raise leftover", /game.js\?v=350/.test(htmlTxt));

function armIdleSheatheRaise(){
  freezeYouAI(); freezeRivalAI();
  V.keys.clear();
  V.hitstopLeft=0; V.openLeft=0;
  V.player.x=500; V.rival.x=720;
  V.player.phase="idle"; V.player.phaseT=0; V.player.cut="slash";
  V.player.stunT=0; V.player.falling=false; V.player.thrownT=0; V.player.throwInvulnT=0;
  V.player.boltPhase=""; V.player.boltHoldCut=false; V.player.boltSuper=false;
  V.player.guarding=false; V.player.guardPoseK=0; V.player.guardBreakT=0;
  V.player.feintT=0; V.player.reversal=false;
  V.player.linkPlant=false; V.player.clashPlant=false; V.player.holdCutPlant=false;
  V.player.linkSheathe=0; V.player.telegraph=false;
  V.player.gait=0; V.player.walkT=0; V.player.walkSettleT=0; V.player.walkFadeHold=0;
  V.player.stamina=100; V.player.meter=0;
  V.player.sheatheT = V.SHEATHE_MS;
}

// live: mashy Space after idle sheathe rests windup (not knife) through link plant
bootPlay(); wait(1700);
armIdleSheatheRaise();
{
  const aabb0 = V.bodyAABB(V.player);
  const sf0 = V.sheatheFade(V.player);
  V.attackEdge = true;
  V.update(STEP);
  freezeRivalAI(); // do not freezeYouAI — that snaps startup back to idle
  let lpsOk = V.player.cut==="slash" && V.player.phase==="startup" && V.player.linkPlant && V.linkPlantFade(V.player)>0.8 && V.player.telegraph===false && /windup/.test(sheetSrc(V.player));
  check("link plant sword raise leftover live Space arms", lpsOk && sf0>0.5, `ok=${lpsOk} cut=${V.player.cut} ph=${V.player.phase} lp=${V.player.linkPlant} lf=${V.linkPlantFade(V.player)} tel=${V.player.telegraph} src=${sheetSrc(V.player)} sf0=${sf0}`);
  check("link plant sword raise leftover live poseBitmap windup not knife", /windup/.test(sheetSrc(V.player)) && !/throw_knife/.test(sheetSrc(V.player)), `src=${sheetSrc(V.player)}`);
  let midOk=true, sawMid=false, maxHop=0, aabbOk=true;
  const aabbY0 = aabb0.y;
  for (let i=0;i<8;i++){
    freezeRivalAI();
    V.player.stunT=0; V.player.falling=false; V.hitstopLeft=0;
    const lf = V.linkPlantFade(V.player);
    if (V.player.linkPlant && V.player.phase==="startup" && lf > 0.05 && lf < 0.95) {
      sawMid = true;
      if (!/windup/.test(sheetSrc(V.player)) || /throw_knife/.test(sheetSrc(V.player))) midOk = false;
    }
    const dy0 = V.destRect(V.player).dy;
    V.update(STEP);
    maxHop = Math.max(maxHop, Math.abs(V.destRect(V.player).dy - dy0));
    if (Math.abs(V.bodyAABB(V.player).y - aabbY0) > 0.05) aabbOk = false;
  }
  check("link plant sword raise leftover live mid raise windup not knife", sawMid && midOk, `saw=${sawMid} ok=${midOk} src=${sheetSrc(V.player)} lf=${V.linkPlantFade(V.player)}`);
  check("link plant sword raise leftover live AABB planted", aabbOk, `aabbOk=${aabbOk}`);
  check("link plant sword raise leftover live destRect no sheet hop", maxHop < 3.5, `hop=${maxHop.toFixed(3)}`);
}

// live: mashy L after sheathe also windup
bootPlay(); wait(1700);
armIdleSheatheRaise();
V.golpeEdge = true;
V.update(STEP);
freezeRivalAI();
check("link plant sword raise leftover live L windup not knife", V.player.cut==="golpe" && V.player.phase==="startup" && V.player.linkPlant && V.linkPlantFade(V.player)>0.8 && /windup/.test(sheetSrc(V.player)) && !/throw_knife/.test(sheetSrc(V.player)), `cut=${V.player.cut} lp=${V.player.linkPlant} lf=${V.linkPlantFade(V.player)} src=${sheetSrc(V.player)}`);

// live: mashy K after sheathe still knife
bootPlay(); wait(1700);
armIdleSheatheRaise();
V.player.x=400; V.rival.x=800;
V.boltEdge = true;
V.update(STEP);
freezeRivalAI();
check("link plant sword raise leftover live K still knife", V.player.boltPhase==="startup" && V.player.linkPlant && V.linkPlantFade(V.player)>0.8 && /throw_knife/.test(sheetSrc(V.player)) && !V.player.boltHoldCut, `bph=${V.player.boltPhase} lp=${V.player.linkPlant} lf=${V.linkPlantFade(V.player)} src=${sheetSrc(V.player)} hold=${V.player.boltHoldCut}`);

// live: telegraph idle Space still windup (no link plant)
bootPlay(); wait(1700);
{
  freezeYouAI(); freezeRivalAI();
  V.player.x=500; V.rival.x=700;
  V.player.phase="idle"; V.player.sheatheT=0; V.player.boltPhase=""; V.player.guarding=false;
  V.attackEdge = true;
  V.update(STEP);
  check("link plant sword raise leftover live telegraph still snaps", V.player.phase==="startup" && V.player.telegraph===true && V.player.linkPlant===false && V.linkPlantFade(V.player)===0 && /windup/.test(sheetSrc(V.player)), `ph=${V.player.phase} tel=${V.player.telegraph} lp=${V.player.linkPlant} lf=${V.linkPlantFade(V.player)} src=${sheetSrc(V.player)}`);
}

// live: rival shares drawKnight linkSheet
check("link plant sword raise leftover rival draw shared", /linkSheet/.test(sliceFn(codeOnly, "drawKnight")) && /restLink/.test(sliceFn(codeOnly, "drawKnight")) && /linkPlantFade\(f\)/.test(srcTxt) && /telePlant/.test(sliceFn(codeOnly, "drawKnight")));
check("cache v=296 after link plant sword raise leftover live", /game.js\?v=350/.test(htmlTxt));


// --- v254 telegraph walk-in pose leftover: rest walk under telePlant when walkFadeHold ---
check("telegraph walk-in pose leftover seats no idle dump mid-stride", /Telegraph walk-in pose leftover/.test(srcTxt) && /leftover walk used to dump to idle the tick/.test(srcTxt) && /startAttack\/startThrow\/startBolt armed from mid-stride/.test(srcTxt) && /tickGait zeroed[\s\S]{0,40}walkFadeHold/.test(srcTxt) && /restTele rested on idle/.test(srcTxt) && /raise hopped walk→idle under/.test(srcTxt) && /windup\/knife/.test(srcTxt));
check("telegraph walk-in pose leftover is draw-only", /Draw-only \(drawKnight \+ tickGait hold\)/.test(srcTxt) && /restTeleWalk/.test(sliceFn(codeOnly, "drawKnight")) && /walkFadeHold/.test(sliceFn(codeOnly, "drawKnight")) && !/restTeleWalk/.test(sliceFn(codeOnly, "destRect")) && !/restTeleWalk/.test(sliceFn(codeOnly, "bodyAABB")) && !/restTeleWalk/.test(sliceFn(codeOnly, "slashPose")) && !/restTeleWalk/.test(sliceFn(codeOnly, "idleBreath")) && !/restTeleWalk/.test(sliceFn(codeOnly, "telegraphFade")) && !/restTeleWalk/.test(sliceFn(codeOnly, "poseBitmap")) && !/restTeleWalk/.test(sliceFn(codeOnly, "startAttack")));
check("telegraph walk-in pose leftover rests walk under telePlant", /restTeleWalk/.test(sliceFn(codeOnly, "drawKnight")) && /walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)\) && ready\(telePlant\) && ready\(walk\)/.test(sliceFn(codeOnly, "drawKnight")) && /octx\.drawImage\(walk, 0, 0, dw, dh\)/.test(sliceFn(codeOnly, "drawKnight")) && /restTele \|\| restTeleWalk/.test(sliceFn(codeOnly, "drawKnight")) && /octx\.drawImage\(telePlant, 0, 0, dw, dh\)/.test(sliceFn(codeOnly, "drawKnight")));
check("telegraph walk-in pose leftover idle telegraph still rests on idle", /Idle telegraph still rests on idle/.test(srcTxt) && /restTele = telegraphing\(f\) && tf < 0\.98 && !restTeleWalk/.test(sliceFn(codeOnly, "drawKnight")) && /octx\.drawImage\(idle, 0, 0, dw, dh\)/.test(sliceFn(codeOnly, "drawKnight")));
check("telegraph walk-in pose leftover tickGait keeps walkFadeHold", /telegraphing\(f\) && \(f\.walkFadeHold > 0 \|\| f\.walkSettleT > 0\)/.test(sliceFn(codeOnly, "tickGait")) && /telegraphFade\(f\) >= 0\.98/.test(sliceFn(codeOnly, "tickGait")) && /Keep leftover walkFadeHold through leftover telegraph raise/.test(srcTxt) && /Walk→guard leftover unchanged/.test(srcTxt));
check("telegraph walk-in pose leftover poseBitmap still windup\/knife", /poseBitmap still windup\/knife immediately/.test(srcTxt) && /f\.phase === "startup" && ready\(wind\)/.test(sliceFn(codeOnly, "poseBitmap")) && /f\.boltPhase === "startup" && ready\(throwK\)/.test(sliceFn(codeOnly, "poseBitmap")) && !/restTeleWalk/.test(sliceFn(codeOnly, "poseBitmap")) && !/walkFadeHold/.test(sliceFn(codeOnly, "poseBitmap")));
check("telegraph walk-in pose leftover walk→guard unchanged", /Walk→guard leftover unchanged/.test(srcTxt) && /restWalkGuard = wf > 0\.02 && f\.guarding/.test(sliceFn(codeOnly, "drawKnight")) && /Keep leftover walk through leftover walkFadeHold if they raise/.test(srcTxt));
check("telegraph walk-in pose leftover telegraphFade clock unchanged", /function telegraphFade/.test(srcTxt) && /This fade unchanged/.test(srcTxt) && /Telegraph walk-in pose leftover: drawKnight rests leftover walk under/.test(srcTxt) && /GUARD_RAISE_MS/.test(sliceFn(codeOnly, "telegraphFade")) && V.GUARD_RAISE_MS===80);
check("telegraph walk-in pose leftover is not a new combat verb", V.GUARD_RAISE_MS===80 && V.WALK_STEP_MS===320 && V.WALK_SETTLE_MS===160 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80 && V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280);
check("slash frames still 180/140/280 after telegraph walk-in pose leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after telegraph walk-in pose leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after telegraph walk-in pose leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after telegraph walk-in pose leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after telegraph walk-in pose leftover", /game.js\?v=350/.test(htmlTxt));

function armMidStrideTeleRaise(){
  freezeYouAI(); freezeRivalAI();
  holdRivalStill();
  V.keys.clear();
  V.hitstopLeft=0; V.openLeft=0;
  V.player.x=400; V.rival.x=900;
  V.player.facing=1; V.rival.facing=-1;
  V.player.phase="idle"; V.player.phaseT=0; V.player.cut="slash";
  V.player.stunT=0; V.player.falling=false; V.player.thrownT=0; V.player.throwInvulnT=0;
  V.player.boltPhase=""; V.player.boltHoldCut=false; V.player.boltSuper=false;
  V.player.guarding=false; V.player.guardPoseK=0; V.player.guardBreakT=0;
  V.player.feintT=0; V.player.reversal=false;
  V.player.linkPlant=false; V.player.clashPlant=false; V.player.holdCutPlant=false;
  V.player.linkSheathe=0; V.player.telegraph=false; V.player.sheatheT=0;
  V.player.stamina=100; V.player.meter=0; V.player.hp=100;
  V.player.gait=1; V.player.walkT = V.WALK_STEP_MS * 0.5; // mid-pass so walkSheetK is live
  V.player.walkSettleT=0; V.player.walkRiseT=V.WALK_SETTLE_MS; V.player.walkPlanted=true;
  V.player.walkFadeHold = 0.9; // capture mid-stride hold before raise
  V.keys.add("KeyD");
  V.keepApart();
}

// live: mid-stride Space keeps walkFadeHold under tele raise (not idle dump)
bootPlay(); wait(1700);
armMidStrideTeleRaise();
{
  const hold0 = V.player.walkFadeHold;
  const aabb0 = V.bodyAABB(V.player);
  V.attackEdge = true;
  V.update(STEP);
  freezeRivalAI(); // do not freezeYouAI — that snaps startup back to idle
  let twOk = V.player.cut==="slash" && V.player.phase==="startup" && V.player.telegraph===true && V.telegraphing(V.player) && V.player.walkFadeHold>0.02 && V.telegraphFade(V.player)<0.98 && /windup/.test(sheetSrc(V.player));
  check("telegraph walk-in pose leftover live Space arms", twOk && hold0>0.5, `ok=${twOk} cut=${V.player.cut} ph=${V.player.phase} tel=${V.player.telegraph} hold=${V.player.walkFadeHold} tf=${V.telegraphFade(V.player)} src=${sheetSrc(V.player)} hold0=${hold0}`);
  check("telegraph walk-in pose leftover live poseBitmap windup not idle dump", /windup/.test(sheetSrc(V.player)) && V.player.walkFadeHold>0.02, `src=${sheetSrc(V.player)} hold=${V.player.walkFadeHold}`);
  let midOk=true, sawMid=false, maxHop=0, aabbOk=true, holdLive=false;
  const aabbY0 = aabb0.y;
  for (let i=0;i<8;i++){
    freezeRivalAI();
    V.player.stunT=0; V.player.falling=false; V.hitstopLeft=0;
    const tf = V.telegraphFade(V.player);
    if (V.telegraphing(V.player) && V.player.phase==="startup" && tf > 0.05 && tf < 0.95) {
      sawMid = true;
      if (V.player.walkFadeHold <= 0.02) midOk = false;
      else holdLive = true;
      if (!/windup/.test(sheetSrc(V.player))) midOk = false;
    }
    const dy0 = V.destRect(V.player).dy;
    V.update(STEP);
    maxHop = Math.max(maxHop, Math.abs(V.destRect(V.player).dy - dy0));
    if (Math.abs(V.bodyAABB(V.player).y - aabbY0) > 0.05) aabbOk = false;
  }
  check("telegraph walk-in pose leftover live mid raise keeps walkFadeHold", sawMid && midOk && holdLive, `saw=${sawMid} ok=${midOk} holdLive=${holdLive} hold=${V.player.walkFadeHold} tf=${V.telegraphFade(V.player)} src=${sheetSrc(V.player)}`);
  check("telegraph walk-in pose leftover live AABB planted", aabbOk, `aabbOk=${aabbOk}`);
  check("telegraph walk-in pose leftover live destRect no sheet hop", maxHop < 3.5, `hop=${maxHop.toFixed(3)}`);
}

// live: mid-stride L also keeps walkFadeHold
bootPlay(); wait(1700);
armMidStrideTeleRaise();
V.golpeEdge = true;
V.update(STEP);
freezeRivalAI();
check("telegraph walk-in pose leftover live L keeps walkFadeHold", V.player.cut==="golpe" && V.player.phase==="startup" && V.player.telegraph===true && V.player.walkFadeHold>0.02 && V.telegraphFade(V.player)<0.98 && /windup/.test(sheetSrc(V.player)), `cut=${V.player.cut} tel=${V.player.telegraph} hold=${V.player.walkFadeHold} tf=${V.telegraphFade(V.player)} src=${sheetSrc(V.player)}`);

// live: mid-stride K keeps walkFadeHold under knife
bootPlay(); wait(1700);
armMidStrideTeleRaise();
V.player.x=350; V.rival.x=950;
V.boltEdge = true;
V.update(STEP);
freezeRivalAI();
check("telegraph walk-in pose leftover live K keeps walkFadeHold", V.player.boltPhase==="startup" && V.player.telegraph===true && V.player.walkFadeHold>0.02 && V.telegraphFade(V.player)<0.98 && /throw_knife/.test(sheetSrc(V.player)) && !V.player.boltHoldCut, `bph=${V.player.boltPhase} tel=${V.player.telegraph} hold=${V.player.walkFadeHold} tf=${V.telegraphFade(V.player)} src=${sheetSrc(V.player)} holdCut=${V.player.boltHoldCut}`);

// live: idle telegraph still walkFadeHold 0 (rests idle)
bootPlay(); wait(1700);
{
  freezeYouAI(); freezeRivalAI();
  holdRivalStill();
  V.keys.clear();
  V.player.x=500; V.rival.x=700;
  V.player.phase="idle"; V.player.sheatheT=0; V.player.boltPhase=""; V.player.guarding=false;
  V.player.gait=0; V.player.walkT=0; V.player.walkSettleT=0; V.player.walkFadeHold=0;
  V.attackEdge = true;
  V.update(STEP);
  check("telegraph walk-in pose leftover live idle telegraph still idle hold", V.player.phase==="startup" && V.player.telegraph===true && V.player.walkFadeHold===0 && V.telegraphing(V.player) && /windup/.test(sheetSrc(V.player)), `ph=${V.player.phase} tel=${V.player.telegraph} hold=${V.player.walkFadeHold} src=${sheetSrc(V.player)}`);
}

// live: walk→guard still keeps leftover walk (unchanged)
bootPlay(); wait(1700);
{
  freezeYouAI(); freezeRivalAI();
  holdRivalStill();
  V.keys.clear();
  V.player.x=400; V.rival.x=900;
  V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false; V.player.guardPoseK=0;
  V.player.boltPhase=""; V.player.falling=false; V.player.feintT=0; V.player.sheatheT=0;
  V.player.gait=1; V.player.walkT=V.WALK_STEP_MS*0.5; V.player.walkRiseT=V.WALK_SETTLE_MS;
  V.player.walkFadeHold=0.85; V.player.walkSettleT=0;
  V.keys.add("KeyD");
  V.update(STEP);
  freezeRivalAI();
  const holdW = V.player.walkFadeHold;
  V.keys.clear();
  V.keys.add("KeyS");
  V.update(STEP);
  freezeRivalAI();
  check("telegraph walk-in pose leftover live walk→guard still holds walk", V.player.guarding && holdW>0.2 && V.player.walkFadeHold>0 && V.walkFade(V.player)>0 && /block/.test(sheetSrc(V.player)), `g=${V.player.guarding} holdW=${holdW} hold=${V.player.walkFadeHold} wf=${V.walkFade(V.player)} src=${sheetSrc(V.player)}`);
}

// live: rival shares drawKnight restTeleWalk
check("telegraph walk-in pose leftover rival draw shared", /restTeleWalk/.test(sliceFn(codeOnly, "drawKnight")) && /walkFadeHold/.test(sliceFn(codeOnly, "tickGait")) && /telegraphing\(f\)/.test(sliceFn(codeOnly, "tickGait")) && /telePlant/.test(sliceFn(codeOnly, "drawKnight")));
check("cache v=296 after telegraph walk-in pose leftover live", /game.js\?v=350/.test(htmlTxt));






// --- v255 link plant walk-in pose leftover: rest walk under linkSheet when walkFadeHold ---
check("link plant walk-in pose leftover seats no idle dump mid-stride sheathe", /Link plant walk-in pose leftover/.test(srcTxt) && /leftover walk used to dump to idle the tick/.test(srcTxt) && /startAttack\/startThrow\/startBolt armed from mid-stride sheathe/.test(srcTxt) && /tickGait zeroed[\s\S]{0,40}walkFadeHold/.test(srcTxt) && /restLink rested on idle/.test(srcTxt) && /telegraph false on linkPlant/.test(srcTxt) && /raise hopped walk→idle under/.test(srcTxt) && /windup\/knife/.test(srcTxt));
check("link plant walk-in pose leftover is draw-only", /Draw-only \(drawKnight \+ tickGait hold\)/.test(srcTxt) && /restLinkWalk/.test(sliceFn(codeOnly, "drawKnight")) && /walkFadeHold/.test(sliceFn(codeOnly, "drawKnight")) && !/restLinkWalk/.test(sliceFn(codeOnly, "destRect")) && !/restLinkWalk/.test(sliceFn(codeOnly, "bodyAABB")) && !/restLinkWalk/.test(sliceFn(codeOnly, "slashPose")) && !/restLinkWalk/.test(sliceFn(codeOnly, "idleBreath")) && !/restLinkWalk/.test(sliceFn(codeOnly, "linkPlantFade")) && !/restLinkWalk/.test(sliceFn(codeOnly, "poseBitmap")) && !/restLinkWalk/.test(sliceFn(codeOnly, "startAttack")));
check("link plant walk-in pose leftover rests walk under linkSheet", /restLinkWalk/.test(sliceFn(codeOnly, "drawKnight")) && /walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)\) && ready\(linkSheet\) && ready\(walk\)/.test(sliceFn(codeOnly, "drawKnight")) && /octx\.drawImage\(walk, 0, 0, dw, dh\)/.test(sliceFn(codeOnly, "drawKnight")) && /restLink \|\| restLinkWalk/.test(sliceFn(codeOnly, "drawKnight")) && /octx\.drawImage\(linkSheet, 0, 0, dw, dh\)/.test(sliceFn(codeOnly, "drawKnight")));
check("link plant walk-in pose leftover idle sheathe raise still rests on idle", /Idle sheathe raise still rests on idle/.test(srcTxt) && /restLink = lf > 0\.02 && !restRev && !restRevWalk && !restThrowGuard && !restThrowGuardWalk && !restTechGuard && !restTechGuardWalk && !restTele && !restClash && !restClashWalk && !restLinkWalk/.test(sliceFn(codeOnly, "drawKnight")) && /octx\.drawImage\(idle, 0, 0, dw, dh\)/.test(sliceFn(codeOnly, "drawKnight")));
check("link plant walk-in pose leftover tickGait keeps walkFadeHold", /linkPlanting\(f\) && \(f\.walkFadeHold > 0 \|\| f\.walkSettleT > 0\)/.test(sliceFn(codeOnly, "tickGait")) && /linkPlantFade\(f\) <= 0\.02/.test(sliceFn(codeOnly, "tickGait")) && /Keep leftover walkFadeHold through[\s\S]{0,40}leftover link plant raise/.test(srcTxt) && /Telegraph walk-in unchanged/.test(srcTxt));
check("link plant walk-in pose leftover poseBitmap still windup\/knife", /poseBitmap still windup\/knife immediately/.test(srcTxt) && /f\.phase === "startup" && ready\(wind\)/.test(sliceFn(codeOnly, "poseBitmap")) && /f\.boltPhase === "startup" && ready\(throwK\)/.test(sliceFn(codeOnly, "poseBitmap")) && !/restLinkWalk/.test(sliceFn(codeOnly, "poseBitmap")) && !/walkFadeHold/.test(sliceFn(codeOnly, "poseBitmap")));
check("link plant walk-in pose leftover telegraph walk-in unchanged", /Telegraph walk-in unchanged/.test(srcTxt) && /restTeleWalk = telegraphing\(f\) && tf < 0\.98 && \(f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)\)/.test(sliceFn(codeOnly, "drawKnight")) && /telegraphing\(f\) && \(f\.walkFadeHold > 0 \|\| f\.walkSettleT > 0\)/.test(sliceFn(codeOnly, "tickGait")));
check("link plant walk-in pose leftover clash\/holdCut unchanged", /clash \/ holdCut unchanged/.test(srcTxt) && /restClash = cf > 0\.02/.test(sliceFn(codeOnly, "drawKnight")) && /restHoldCut = hf > 0\.02/.test(sliceFn(codeOnly, "drawKnight")));
check("link plant walk-in pose leftover linkPlantFade clock unchanged", /function linkPlantFade/.test(srcTxt) && /This fade unchanged/.test(srcTxt) && /Link plant walk-in pose leftover: drawKnight rests leftover walk under/.test(srcTxt) && /GUARD_RAISE_MS/.test(sliceFn(codeOnly, "linkPlantFade")) && V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140);
check("link plant walk-in pose leftover is not a new combat verb", V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140 && V.WALK_STEP_MS===320 && V.WALK_SETTLE_MS===160 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80 && V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280);
check("slash frames still 180/140/280 after link plant walk-in pose leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after link plant walk-in pose leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after link plant walk-in pose leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after link plant walk-in pose leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after link plant walk-in pose leftover", /game.js\?v=350/.test(htmlTxt));

function armMidStrideSheatheRaise(){
  freezeYouAI(); freezeRivalAI();
  holdRivalStill();
  V.keys.clear();
  V.hitstopLeft=0; V.openLeft=0;
  V.player.x=400; V.rival.x=900;
  V.player.facing=1; V.rival.facing=-1;
  V.player.phase="idle"; V.player.phaseT=0; V.player.cut="slash";
  V.player.stunT=0; V.player.falling=false; V.player.thrownT=0; V.player.throwInvulnT=0;
  V.player.boltPhase=""; V.player.boltHoldCut=false; V.player.boltSuper=false;
  V.player.guarding=false; V.player.guardPoseK=0; V.player.guardBreakT=0;
  V.player.feintT=0; V.player.reversal=false;
  V.player.linkPlant=false; V.player.clashPlant=false; V.player.holdCutPlant=false;
  V.player.linkSheathe=0; V.player.telegraph=false;
  V.player.stamina=100; V.player.meter=0; V.player.hp=100;
  V.player.sheatheT = V.SHEATHE_MS; // clash/tech idle sheathe mid-fade
  V.player.gait=1; V.player.walkT = V.WALK_STEP_MS * 0.5; // mid-pass so walkSheetK is live
  V.player.walkSettleT=0; V.player.walkRiseT=V.WALK_SETTLE_MS; V.player.walkPlanted=true;
  V.player.walkFadeHold = 0.9; // capture mid-stride hold before raise
  V.keys.add("KeyD");
  V.keepApart();
}

// live: mid-stride sheathe Space keeps walkFadeHold under link raise (not idle dump)
bootPlay(); wait(1700);
armMidStrideSheatheRaise();
{
  const hold0 = V.player.walkFadeHold;
  const aabb0 = V.bodyAABB(V.player);
  const sf0 = V.sheatheFade(V.player);
  V.attackEdge = true;
  V.update(STEP);
  freezeRivalAI(); // do not freezeYouAI — that snaps startup back to idle
  let lwOk = V.player.cut==="slash" && V.player.phase==="startup" && V.player.linkPlant===true && V.player.telegraph===false && V.player.walkFadeHold>0.02 && V.linkPlantFade(V.player)>0.02 && /windup/.test(sheetSrc(V.player));
  check("link plant walk-in pose leftover live Space arms", lwOk && hold0>0.5 && sf0>0.5, `ok=${lwOk} cut=${V.player.cut} ph=${V.player.phase} lp=${V.player.linkPlant} tel=${V.player.telegraph} hold=${V.player.walkFadeHold} lf=${V.linkPlantFade(V.player)} src=${sheetSrc(V.player)} hold0=${hold0} sf0=${sf0}`);
  check("link plant walk-in pose leftover live poseBitmap windup not idle dump", /windup/.test(sheetSrc(V.player)) && V.player.walkFadeHold>0.02 && !/throw_knife/.test(sheetSrc(V.player)), `src=${sheetSrc(V.player)} hold=${V.player.walkFadeHold}`);
  let midOk=true, sawMid=false, maxHop=0, aabbOk=true, holdLive=false;
  const aabbY0 = aabb0.y;
  for (let i=0;i<8;i++){
    freezeRivalAI();
    V.player.stunT=0; V.player.falling=false; V.hitstopLeft=0;
    const lf = V.linkPlantFade(V.player);
    if (V.player.linkPlant && V.player.phase==="startup" && lf > 0.05 && lf < 0.95) {
      sawMid = true;
      if (V.player.walkFadeHold <= 0.02) midOk = false;
      else holdLive = true;
      if (!/windup/.test(sheetSrc(V.player)) || /throw_knife/.test(sheetSrc(V.player))) midOk = false;
    }
    const dy0 = V.destRect(V.player).dy;
    V.update(STEP);
    maxHop = Math.max(maxHop, Math.abs(V.destRect(V.player).dy - dy0));
    if (Math.abs(V.bodyAABB(V.player).y - aabbY0) > 0.05) aabbOk = false;
  }
  check("link plant walk-in pose leftover live mid raise keeps walkFadeHold", sawMid && midOk && holdLive, `saw=${sawMid} ok=${midOk} holdLive=${holdLive} hold=${V.player.walkFadeHold} lf=${V.linkPlantFade(V.player)} src=${sheetSrc(V.player)}`);
  check("link plant walk-in pose leftover live AABB planted", aabbOk, `aabbOk=${aabbOk}`);
  check("link plant walk-in pose leftover live destRect no sheet hop", maxHop < 3.5, `hop=${maxHop.toFixed(3)}`);
}

// live: mid-stride sheathe L also keeps walkFadeHold
bootPlay(); wait(1700);
armMidStrideSheatheRaise();
V.golpeEdge = true;
V.update(STEP);
freezeRivalAI();
check("link plant walk-in pose leftover live L keeps walkFadeHold", V.player.cut==="golpe" && V.player.phase==="startup" && V.player.linkPlant===true && V.player.telegraph===false && V.player.walkFadeHold>0.02 && V.linkPlantFade(V.player)>0.02 && /windup/.test(sheetSrc(V.player)), `cut=${V.player.cut} lp=${V.player.linkPlant} tel=${V.player.telegraph} hold=${V.player.walkFadeHold} lf=${V.linkPlantFade(V.player)} src=${sheetSrc(V.player)}`);

// live: mid-stride sheathe K keeps walkFadeHold under knife
bootPlay(); wait(1700);
armMidStrideSheatheRaise();
V.player.x=400; V.rival.x=800;
V.boltEdge = true;
V.update(STEP);
freezeRivalAI();
check("link plant walk-in pose leftover live K keeps walkFadeHold", V.player.boltPhase==="startup" && V.player.linkPlant===true && V.player.telegraph===false && V.player.walkFadeHold>0.02 && V.linkPlantFade(V.player)>0.02 && /throw_knife/.test(sheetSrc(V.player)) && !V.player.boltHoldCut, `bph=${V.player.boltPhase} lp=${V.player.linkPlant} tel=${V.player.telegraph} hold=${V.player.walkFadeHold} lf=${V.linkPlantFade(V.player)} src=${sheetSrc(V.player)} holdCut=${V.player.boltHoldCut}`);

// live: idle sheathe raise still walkFadeHold 0 (rests idle)
bootPlay(); wait(1700);
armIdleSheatheRaise();
{
  V.attackEdge = true;
  V.update(STEP);
  freezeRivalAI();
  check("link plant walk-in pose leftover live idle sheathe still idle hold", V.player.phase==="startup" && V.player.linkPlant===true && V.player.telegraph===false && V.player.walkFadeHold===0 && V.linkPlantFade(V.player)>0.8 && /windup/.test(sheetSrc(V.player)), `ph=${V.player.phase} lp=${V.player.linkPlant} hold=${V.player.walkFadeHold} lf=${V.linkPlantFade(V.player)} src=${sheetSrc(V.player)}`);
}

// live: telegraph walk-in still keeps walkFadeHold (unchanged)
bootPlay(); wait(1700);
armMidStrideTeleRaise();
{
  V.attackEdge = true;
  V.update(STEP);
  freezeRivalAI();
  check("link plant walk-in pose leftover live telegraph walk-in still holds", V.player.phase==="startup" && V.player.telegraph===true && V.player.linkPlant===false && V.player.walkFadeHold>0.02 && V.telegraphFade(V.player)<0.98 && /windup/.test(sheetSrc(V.player)), `ph=${V.player.phase} tel=${V.player.telegraph} lp=${V.player.linkPlant} hold=${V.player.walkFadeHold} tf=${V.telegraphFade(V.player)} src=${sheetSrc(V.player)}`);
}

check("link plant walk-in pose leftover rival draw shared", /restLinkWalk/.test(sliceFn(codeOnly, "drawKnight")) && /linkPlanting\(f\)/.test(sliceFn(codeOnly, "tickGait")) && /linkPlantFade\(f\)/.test(sliceFn(codeOnly, "tickGait")) && /linkSheet/.test(sliceFn(codeOnly, "drawKnight")));
check("cache v=296 after link plant walk-in pose leftover live", /game.js\?v=350/.test(htmlTxt));

// --- v256 portrait pad leftover: bigger guarda/tajo/golpe/dardo hit targets in portrait letterbox ---
check("portrait pad leftover seats no cramped zone mins next to big stick", /Portrait pad leftover/.test(srcTxt) && /landscape zone mins/.test(srcTxt) && /guarda 52 \/ tajo 52 \/ golpe 48/.test(srcTxt) && /dardo 56/.test(srcTxt) && /stayed cramped in the taller portrait letterbox/.test(srcTxt) && /bigger[\s\S]{0,20}stick/.test(srcTxt) && /stamp, not a thumb plant/.test(srcTxt) && /Same hole portrait stick already closed/.test(srcTxt));
check("portrait pad leftover is draw-only css", /Draw-only \(style\.css\)/.test(srcTxt) && /Portrait pad leftover/.test(cssTxt) && /body\.touch-on \.zone-guard/.test(cssTxt) && /body\.touch-on \.zone-slash/.test(cssTxt) && /body\.touch-on \.zone-golpe/.test(cssTxt) && /body\.touch-on \.zone-dart/.test(cssTxt) && /body\.touch-on \.cut-cluster/.test(cssTxt));
check("portrait pad leftover bigger hit targets", /min-width:\s*56px/.test(cssTxt) && /body\.touch-on \.zone-guard\s*\{[^}]*min-width:\s*56px/.test(cssTxt) && /body\.touch-on \.zone-slash\s*\{[^}]*min-width:\s*56px/.test(cssTxt) && /body\.touch-on \.zone-golpe\s*\{[^}]*min-width:\s*50px/.test(cssTxt) && /body\.touch-on \.zone-dart\s*\{[^}]*min-width:\s*56px/.test(cssTxt) && /body\.touch-on \.cut-cluster\s*\{[^}]*min-width:\s*112px/.test(cssTxt) && /body\.touch-on \.cut-cluster\s*\{[^}]*width:\s*40%/.test(cssTxt));
check("portrait pad leftover landscape mins unchanged", /\.zone-guard\s*\{[^}]*min-width:\s*52px/.test(cssTxt) && /\.zone-slash\s*\{[^}]*min-width:\s*52px/.test(cssTxt) && /\.zone-golpe\s*\{[^}]*min-width:\s*48px/.test(cssTxt) && /\.zone-dart\s*\{[^}]*min-width:\s*56px/.test(cssTxt) && /\.cut-cluster\s*\{[^}]*min-width:\s*110px/.test(cssTxt) && /\.cut-cluster\s*\{[^}]*width:\s*38%/.test(cssTxt));
check("portrait pad leftover stick size unchanged", /Portrait stick leftover/.test(cssTxt) && /body\.touch-on \.stick/.test(cssTxt) && /--stick-size:\s*min\(120px/.test(cssTxt) && /38vw/.test(cssTxt) && /min\(84px, calc\(var\(--pad-h\) - 14px\), 26vw\)/.test(cssTxt));
check("portrait pad leftover still letterbox no canvas resize", /@media \(orientation: portrait\)/.test(cssTxt) && /body\.touch-on #pad/.test(cssTxt) && /bottom:\s*0/.test(cssTxt) && /position:\s*absolute/.test(cssTxt) && !/@media \(orientation: portrait\)[\s\S]*body\.touch-on #game/.test(cssTxt));
check("giro hidden in portrait after portrait pad leftover", /body\.touch-on #giro\s*\{[^}]*display:\s*none/.test(cssTxt) && !/body\.touch-on #giro\s*\{[^}]*display:\s*flex/.test(cssTxt));
check("portrait pad leftover keyboard path untouched", /KeyA/.test(srcTxt) && /KeyD/.test(srcTxt) && /Space/.test(srcTxt) && /KeyJ/.test(srcTxt) && /KeyS/.test(srcTxt) && /KeyK/.test(srcTxt) && /KeyL/.test(srcTxt) && /KeyR/.test(srcTxt) && /act === "dart"/.test(srcTxt) && /boltEdge = true/.test(srcTxt));
check("portrait pad leftover is not a new combat verb", V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80 && V.GUARD_COMMIT_MS===140 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("slash frames still 180/140/280 after portrait pad leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after portrait pad leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after portrait pad leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after portrait pad leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after portrait pad leftover", /game.js\?v=350/.test(htmlTxt));

// live: pad zones still feed the same keys (unchanged combat path)
bootPlay(); wait(1700);
{
  const aabb0 = V.bodyAABB(V.player);
  const dest0 = V.destRect(V.player);
  V.stickApply(-1);
  check("portrait pad leftover live stick still KeyA", V.keys.has("KeyA") && !V.keys.has("KeyD"), `a=${V.keys.has("KeyA")} d=${V.keys.has("KeyD")}`);
  V.stickApply(1);
  check("portrait pad leftover live stick still KeyD", V.keys.has("KeyD") && !V.keys.has("KeyA"), `a=${V.keys.has("KeyA")} d=${V.keys.has("KeyD")}`);
  V.stickApply(0);
  check("portrait pad leftover live stick deadzone still releases", !V.keys.has("KeyA") && !V.keys.has("KeyD"), `a=${V.keys.has("KeyA")} d=${V.keys.has("KeyD")}`);
  check("portrait pad leftover live AABB planted", Math.abs(V.bodyAABB(V.player).y-aabb0.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-aabb0.h)<0.05, `dy=${(V.bodyAABB(V.player).y-aabb0.y).toFixed(2)}`);
  check("portrait pad leftover live destRect planted", Math.abs(V.destRect(V.player).dy-dest0.dy)<2.2, `ddy=${(V.destRect(V.player).dy-dest0.dy).toFixed(2)}`);
}
check("portrait pad leftover zone hierarchy unchanged", /\.zone-golpe[\s\S]*?height:\s*80%/.test(cssTxt) && /\.zone-slash[\s\S]*?height:\s*92%/.test(cssTxt) && /\.zone-guard[\s\S]*?height:\s*88%/.test(cssTxt) && /\.zone-dart[\s\S]*?height:\s*88%/.test(cssTxt));
check("cache v=296 after portrait pad leftover live", /game.js\?v=350/.test(htmlTxt));




// --- v257 recovery walk-out pose leftover: rest cut/throw/bolt/stun on walk when A/D held ---
check("recovery walk-out pose leftover seats no idle dump mid-recovery A/D", /Recovery walk-out pose leftover/.test(srcTxt) && /leftover slash \/ windup \/ knife used to rest/.test(srcTxt) && /on idle through cut \/ throw \/ bolt recovery/.test(srcTxt) && /chip stun/.test(srcTxt) && /gaitWalkOn stays false/.test(srcTxt) && /phase \/ bolt \/ stun locks/.test(srcTxt) && /popped the walk sheet the/.test(srcTxt) && /tick recovery ended/.test(srcTxt));
check("recovery walk-out pose leftover is draw-only", /Draw-only \(drawKnight\)/.test(srcTxt) && /recoveryWalkResting\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /function recoveryWalkOut/.test(codeOnly) && /function recoveryWalkResting/.test(codeOnly) && !/recoveryWalkResting/.test(sliceFn(codeOnly, "destRect")) && !/recoveryWalkResting/.test(sliceFn(codeOnly, "bodyAABB")) && !/recoveryWalkResting/.test(sliceFn(codeOnly, "slashPose")) && !/recoveryWalkResting/.test(sliceFn(codeOnly, "idleBreath")) && !/recoveryWalkResting/.test(sliceFn(codeOnly, "sheatheFade")) && !/recoveryWalkResting/.test(sliceFn(codeOnly, "throwPlantFade")) && !/recoveryWalkResting/.test(sliceFn(codeOnly, "boltPlantFade")) && !/recoveryWalkResting/.test(sliceFn(codeOnly, "poseBitmap")));
check("recovery walk-out pose leftover rests cut on walk when A/D", /restSheatheWalk = fade > 0 && !f\.guarding && \(gaitWalkOn\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)\)/.test(sliceFn(codeOnly, "drawKnight")) && /restThrowWalk/.test(sliceFn(codeOnly, "drawKnight")) && /restBoltWalk/.test(sliceFn(codeOnly, "drawKnight")) && /octx\.drawImage\(walk, 0, 0, dw, dh\)/.test(sliceFn(codeOnly, "drawKnight")));
check("recovery walk-out pose leftover idle recovery still rests on idle", /Idle recovery still rests on idle/.test(srcTxt) && /Idle throw recovery still rests on idle/.test(srcTxt) && /Idle bolt recovery still rests on idle/.test(srcTxt) && /Idle stun still rests on idle/.test(srcTxt) && /restThrow = trf > 0 && !restThrowWalk && !restParry/.test(sliceFn(codeOnly, "drawKnight")) && /restBolt = bf > 0\.02 && !restBoltWalk/.test(sliceFn(codeOnly, "drawKnight")));
check("recovery walk-out pose leftover sheathe gaitWalkOn unchanged", /Sheathe walk-out gaitWalkOn path unchanged/.test(srcTxt) && /gaitWalkOn\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)/.test(sliceFn(codeOnly, "drawKnight")));
check("recovery walk-out pose leftover poseBitmap still source sheets", /poseBitmap still slash \/ windup \/ knife \/ idle/.test(srcTxt) && /poseBitmap still windup while planted/.test(srcTxt) && /poseBitmap still idle once bolt recovery starts/.test(srcTxt) && /poseBitmap still idle/.test(srcTxt));
check("recovery walk-out pose leftover fade clocks unchanged", /ThrowPlantFade \/ boltPlantFade \/ sheatheFade \/ hurtFlash clocks/.test(srcTxt) && /function sheatheFade/.test(srcTxt) && /function throwPlantFade/.test(srcTxt) && /function boltPlantFade/.test(srcTxt) && V.SHEATHE_MS===140 && V.RECOVERY===280 && V.BOLT_RECOVERY===280 && V.HITSTUN===350);
check("recovery walk-out pose leftover is not a new combat verb", V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.WALK_STEP_MS===320 && V.GUARD_RAISE_MS===80);
check("slash frames still 180/140/280 after recovery walk-out pose leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after recovery walk-out pose leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after recovery walk-out pose leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after recovery walk-out pose leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after recovery walk-out pose leftover", /game.js\?v=350/.test(htmlTxt));

// live: A/D mid slash recovery rests walk under sheathe fade
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="recovery"; V.player.phaseT=40; V.player.cut="slash"; V.player.stunT=0; V.player.guarding=false;
V.player.boltPhase=""; V.player.falling=false; V.player.gait=0; V.player.clashRec=false; V.player.techRec=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.hp=100; V.player.stamina=100; V.player.guardPoseK=0;
V.player.x=400; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
V.keys.add("KeyD");
let rwo257Saw=false, rwo257Rest=false, rwo257AabbOk=true, rwo257MaxHop=0;
const rwo257Aabb0 = V.bodyAABB(V.player).y;
for (let i=0;i<20;i++){
  holdRivalStill();
  const sf = V.sheatheFade(V.player);
  if (V.player.phase==="recovery" && sf>0.02 && V.keys.has("KeyD")) {
    rwo257Saw = true;
    if (V.recoveryWalkResting(V.player)) rwo257Rest = true;
  }
  const dy0 = V.destRect(V.player).dy;
  V.update(STEP);
  rwo257MaxHop = Math.max(rwo257MaxHop, Math.abs(V.destRect(V.player).dy - dy0));
  if (Math.abs(V.bodyAABB(V.player).y - rwo257Aabb0) > 0.05) rwo257AabbOk = false;
  if (V.player.phase==="idle" && i>4) break;
}
check("recovery walk-out pose leftover live slash A/D arms", rwo257Saw && rwo257Rest && V.player.phase!=="startup", `saw=${rwo257Saw} rest=${rwo257Rest} ph=${V.player.phase}`);
check("recovery walk-out pose leftover live slash AABB planted", rwo257AabbOk, `aabbOk=${rwo257AabbOk}`);
check("recovery walk-out pose leftover live slash destRect no sheet hop", rwo257MaxHop < 3.5, `hop=${rwo257MaxHop.toFixed(3)}`);

// live: idle slash recovery still idle rest (no A/D)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="recovery"; V.player.phaseT=40; V.player.cut="slash"; V.player.stunT=0; V.player.guarding=false;
V.player.boltPhase=""; V.player.falling=false; V.player.gait=0; V.player.clashRec=false; V.player.techRec=false;
V.player.x=500; V.rival.x=1100;
let rwo257IdleOk=true, rwo257IdleSaw=false;
for (let i=0;i<12;i++){
  holdRivalStill();
  const sf = V.sheatheFade(V.player);
  if (V.player.phase==="recovery" && sf>0.02) {
    rwo257IdleSaw = true;
    if (V.recoveryWalkResting(V.player) || V.player.gait!==0) rwo257IdleOk = false;
  }
  V.update(STEP);
  if (V.player.phase==="idle" && i>3) break;
}
check("recovery walk-out pose leftover live idle slash still idle", rwo257IdleSaw && rwo257IdleOk && V.player.gait===0, `saw=${rwo257IdleSaw} ok=${rwo257IdleOk} gait=${V.player.gait}`);

// live: throw recovery A/D
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="recovery"; V.player.phaseT=20; V.player.cut="throw"; V.player.stunT=0; V.player.guarding=false;
V.player.boltPhase=""; V.player.falling=false; V.player.gait=0; V.player.clashRec=false; V.player.techRec=false;
V.player.x=400; V.rival.x=1100;
V.keys.add("KeyD");
let rwo257Throw=false;
for (let i=0;i<16;i++){
  holdRivalStill();
  if (V.player.phase==="recovery" && V.throwPlantFade(V.player)>0.02 && V.recoveryWalkResting(V.player)) rwo257Throw = true;
  V.update(STEP);
  if (V.player.phase==="idle" && i>3) break;
}
check("recovery walk-out pose leftover live throw A/D rests walk", rwo257Throw, `throw=${rwo257Throw} tf=${V.throwPlantFade(V.player)}`);

// live: bolt recovery A/D
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false;
V.player.boltPhase="recovery"; V.player.boltT=40; V.player.boltHoldCut=false; V.player.falling=false; V.player.gait=0;
V.player.x=400; V.rival.x=1100;
V.keys.add("KeyD");
let rwo257Bolt=false;
for (let i=0;i<16;i++){
  holdRivalStill();
  if (V.player.boltPhase==="recovery" && V.boltPlantFade(V.player)>0.02 && V.recoveryWalkResting(V.player)) rwo257Bolt = true;
  V.update(STEP);
  if (!V.player.boltPhase && i>3) break;
}
check("recovery walk-out pose leftover live bolt A/D rests walk", rwo257Bolt, `bolt=${rwo257Bolt} bf=${V.boltPlantFade(V.player)}`);

// live: chip stun A/D
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=V.HITSTUN; V.player.thrownT=0; V.player.guarding=false;
V.player.boltPhase=""; V.player.falling=false; V.player.gait=0; V.player.hp=90;
V.player.x=400; V.rival.x=1100;
V.keys.add("KeyD");
let rwo257Stun=false;
for (let i=0;i<12;i++){
  holdRivalStill();
  if (V.player.stunT>0 && V.recoveryWalkResting(V.player)) rwo257Stun = true;
  V.update(STEP);
  if (V.player.stunT<=0 && i>3) break;
}
check("recovery walk-out pose leftover live stun A/D rests walk", rwo257Stun, `stun=${rwo257Stun} st=${V.player.stunT}`);

check("recovery walk-out pose leftover rival draw shared", /restThrowWalk/.test(sliceFn(codeOnly, "drawKnight")) && /restBoltWalk/.test(sliceFn(codeOnly, "drawKnight")) && /recoveryWalkResting\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /recoveryWalkOut\(f\)/.test(srcTxt));
check("cache v=296 after recovery walk-out pose leftover live", /game.js\?v=350/.test(htmlTxt));

// --- v258 holdCut walk-in pose leftover: rest cut/link raise on walk when A/D through cancel plant ---
check("holdCut walk-in pose leftover seats no walk dump under cancel plant", /HoldCut walk-in pose leftover/.test(srcTxt) && /leftover cut used to rest on idle while A\/D/.test(srcTxt) && /special-cancel K plant/.test(srcTxt) && /recovery walk-out already rested cut/.test(srcTxt) && /tickGait zeros walkFadeHold on boltPhase/.test(srcTxt) && /dumped walk→idle under the cut/.test(srcTxt) && /Same hole link/.test(srcTxt) && /restLinkWalk only walkFadeHold/.test(srcTxt));
check("holdCut walk-in pose leftover is draw-only", /Draw-only \(drawKnight\)/.test(srcTxt) && /restHoldCutWalk/.test(sliceFn(codeOnly, "drawKnight")) && /recoveryWalkOut\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && !/restHoldCutWalk/.test(sliceFn(codeOnly, "destRect")) && !/restHoldCutWalk/.test(sliceFn(codeOnly, "bodyAABB")) && !/restHoldCutWalk/.test(sliceFn(codeOnly, "slashPose")) && !/restHoldCutWalk/.test(sliceFn(codeOnly, "idleBreath")) && !/restHoldCutWalk/.test(sliceFn(codeOnly, "holdCutFade")) && !/restHoldCutWalk/.test(sliceFn(codeOnly, "poseBitmap")) && !/restHoldCutWalk/.test(sliceFn(codeOnly, "tickGait")));
check("holdCut walk-in pose leftover rests cut on walk when A/D", /restHoldCutWalk = hf > 0\.02/.test(sliceFn(codeOnly, "drawKnight")) && /f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)\) && ready\(cut\) && ready\(walk\)/.test(sliceFn(codeOnly, "drawKnight")) && /restHoldCut = hf > 0\.02/.test(sliceFn(codeOnly, "drawKnight")) && /!restHoldCutWalk && ready\(cut\) && ready\(idle\)/.test(sliceFn(codeOnly, "drawKnight")) && /octx\.drawImage\(walk, 0, 0, dw, dh\)/.test(sliceFn(codeOnly, "drawKnight")));
check("holdCut walk-in pose leftover link raise also recoveryWalkOut", /restLinkWalk = lf > 0\.02/.test(sliceFn(codeOnly, "drawKnight")) && /f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /HoldCut walk-in also rests link raise on walk when recoveryWalkOut/.test(srcTxt));
check("holdCut walk-in pose leftover idle plant still rests on idle", /Idle plant still rests on idle/.test(srcTxt) && /restHoldCut = hf > 0\.02/.test(sliceFn(codeOnly, "drawKnight")) && /!restHoldCutWalk && ready\(cut\) && ready\(idle\)/.test(sliceFn(codeOnly, "drawKnight")));
check("holdCut walk-in pose leftover telegraph\/link mid-stride unchanged", /Telegraph \/ link mid-stride walkFadeHold unchanged/.test(srcTxt) && /telegraphing\(f\) && \(f\.walkFadeHold > 0 \|\| f\.walkSettleT > 0\)/.test(sliceFn(codeOnly, "tickGait")) && /linkPlanting\(f\) && \(f\.walkFadeHold > 0 \|\| f\.walkSettleT > 0\)/.test(sliceFn(codeOnly, "tickGait")));
check("holdCut walk-in pose leftover clash walk-in rests separately", /Clash walk-in rests separately/.test(srcTxt) && /restClashWalk/.test(sliceFn(codeOnly, "drawKnight")) && /restClash = cf > 0\.02/.test(sliceFn(codeOnly, "drawKnight")));
check("holdCut walk-in pose leftover poseBitmap still slash\/windup", /poseBitmap still slash \/ windup/.test(srcTxt) && /holdingCutBolt\(f\) && ready\(slash\)/.test(sliceFn(codeOnly, "poseBitmap")) && /boltHoldCut = !fromClash/.test(sliceFn(codeOnly, "cancelIntoBolt")));
check("holdCut walk-in pose leftover fade clocks unchanged", /HoldCutFade \/ linkPlantFade clocks/.test(srcTxt) && /function holdCutFade/.test(srcTxt) && /function linkPlantFade/.test(srcTxt) && V.GUARD_RAISE_MS===80 && V.BOLT_CANCEL_MS===100 && V.SLASH_CANCEL_MS===100 && V.GOLPE_CANCEL_MS===100);
check("holdCut walk-in pose leftover is not a new combat verb", V.BOLT_CANCEL_MS===100 && V.SLASH_CANCEL_MS===100 && V.GOLPE_CANCEL_MS===100 && V.GUARD_RAISE_MS===80 && V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.HITSTUN===350 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280);
check("slash frames still 180/140/280 after holdCut walk-in pose leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after holdCut walk-in pose leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after holdCut walk-in pose leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after holdCut walk-in pose leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after holdCut walk-in pose leftover", /game.js\?v=350/.test(htmlTxt));

// live: A/D through special-cancel K plant keeps recoveryWalkOut + holdCut
landCloseSlash();
const hcwiGot = waitUntilBoltWindow("slash");
V.player.stamRegenT = 99999;
V.keys.clear();
V.keys.add("KeyD");
const hcwiSheathe = V.sheatheFade(V.player);
const hcwiAabb0 = V.bodyAABB(V.player);
const hcwiDy0 = V.destRect(V.player).dy;
V.boltEdge = true;
V.update(STEP);
const hcwiHf0 = V.holdCutFade(V.player);
const hcwiWalkOut = V.recoveryWalkOut(V.player);
check("holdCut walk-in pose leftover live K arms with A/D", hcwiGot && V.player.boltPhase==="startup" && V.player.boltHoldCut && V.player.holdCutPlant && hcwiHf0>0.8 && hcwiWalkOut && hcwiSheathe>0.08 && V.sheatheFade(V.player)===0 && V.player.clashPlant===false, `got=${hcwiGot} bph=${V.player.boltPhase} hold=${V.player.boltHoldCut} plant=${V.player.holdCutPlant} hf=${hcwiHf0} walkOut=${hcwiWalkOut} sh0=${hcwiSheathe} sf=${V.sheatheFade(V.player)}`);
check("holdCut walk-in pose leftover live poseBitmap still slash", isCutSheet(V.player) && !/windup/.test(sheetSrc(V.player)), `src=${sheetSrc(V.player)}`);
let hcwiSaw=false, hcwiAabbOk=true, hcwiMaxHop=0;
for (let i=0;i<6;i++){
  freezeRivalAI();
  V.rival.x = 1220;
  V.rival.stunT = 0;
  V.rival.guarding = false;
  V.keys.add("KeyD");
  if (V.player.boltPhase==="startup" && V.holdCutFade(V.player)>0.02 && V.recoveryWalkOut(V.player)) hcwiSaw = true;
  const dy0 = V.destRect(V.player).dy;
  V.update(STEP);
  hcwiMaxHop = Math.max(hcwiMaxHop, Math.abs(V.destRect(V.player).dy - dy0));
  if (Math.abs(V.bodyAABB(V.player).y - hcwiAabb0.y) > 0.05) hcwiAabbOk = false;
}
check("holdCut walk-in pose leftover live mid plant keeps A/D", hcwiSaw && V.recoveryWalkOut(V.player), `saw=${hcwiSaw} walkOut=${V.recoveryWalkOut(V.player)} hf=${V.holdCutFade(V.player)}`);
check("holdCut walk-in pose leftover live AABB planted", hcwiAabbOk, `aabbOk=${hcwiAabbOk}`);
check("holdCut walk-in pose leftover live destRect no sheet hop", hcwiMaxHop < 3.5, `hop=${hcwiMaxHop.toFixed(3)} dy0=${hcwiDy0.toFixed(2)}`);

// live: idle special-cancel still idle rest (no A/D)
landCloseSlash();
const hcwiIdleGot = waitUntilBoltWindow("slash");
V.player.stamRegenT = 99999;
V.keys.clear();
V.boltEdge = true;
V.update(STEP);
check("holdCut walk-in pose leftover live idle plant still idle", hcwiIdleGot && V.player.boltPhase==="startup" && V.player.holdCutPlant && V.holdCutFade(V.player)>0.8 && !V.recoveryWalkOut(V.player) && V.player.gait===0, `got=${hcwiIdleGot} bph=${V.player.boltPhase} plant=${V.player.holdCutPlant} hf=${V.holdCutFade(V.player)} walkOut=${V.recoveryWalkOut(V.player)} gait=${V.player.gait}`);

// live: connected slash-L with A/D keeps recoveryWalkOut under link plant
landCloseSlash();
const hcwiLinkGot = waitUntilWindow();
V.keys.clear();
V.keys.add("KeyD");
const hcwiLfSheathe = V.sheatheFade(V.player);
V.golpeEdge = true;
V.update(STEP);
check("holdCut walk-in pose leftover live L keeps recoveryWalkOut", hcwiLinkGot && V.player.cut==="golpe" && V.player.phase==="startup" && V.player.linkPlant && V.linkPlantFade(V.player)>0.8 && V.recoveryWalkOut(V.player) && V.holdCutFade(V.player)===0 && hcwiLfSheathe>0.08 && /windup/.test(sheetSrc(V.player)), `got=${hcwiLinkGot} cut=${V.player.cut} lf=${V.linkPlantFade(V.player)} walkOut=${V.recoveryWalkOut(V.player)} sh0=${hcwiLfSheathe} src=${sheetSrc(V.player)}`);

check("holdCut walk-in pose leftover rival draw shared", /restHoldCutWalk/.test(sliceFn(codeOnly, "drawKnight")) && /recoveryWalkOut\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)/.test(sliceFn(codeOnly, "drawKnight")));
check("cache v=296 after holdCut walk-in pose leftover live", /game.js\?v=350/.test(htmlTxt));


// --- v259 rival recovery walk-out pose leftover: rest cut/throw/bolt/stun on walk when closing ---
check("rival recovery walk-out pose leftover seats no idle dump mid-stun closing", /Rival recovery walk-out pose leftover/.test(srcTxt) && /leftover slash \/ windup \/ knife \/ stun/.test(srcTxt) && /recoveryWalkOut was you-only/.test(srcTxt) && /rival\.closing was already armed/.test(srcTxt) && /popped the walk sheet the tick stun/.test(srcTxt) && /Same hole recovery[\s\S]{0,40}walk-out already closed for you A\/D/.test(srcTxt));
check("rival recovery walk-out pose leftover is draw-only", /Draw-only \(recoveryWalkOut\)/.test(srcTxt) && /return !!f\.closing/.test(sliceFn(codeOnly, "recoveryWalkOut")) && /f\.kind === "you"/.test(sliceFn(codeOnly, "recoveryWalkOut")) && !/closing/.test(sliceFn(codeOnly, "destRect")) && !/closing/.test(sliceFn(codeOnly, "bodyAABB")) && !/closing/.test(sliceFn(codeOnly, "slashPose")) && !/closing/.test(sliceFn(codeOnly, "idleBreath")) && !/!!f\.closing/.test(sliceFn(codeOnly, "poseBitmap")));
check("rival recovery walk-out pose leftover rests when closing", /if \(f\.kind === "you"\) return actionHeld\("left"\) \|\| actionHeld\("right"\)/.test(sliceFn(codeOnly, "recoveryWalkOut")) && /return !!f\.closing/.test(sliceFn(codeOnly, "recoveryWalkOut")) && /Rival recovery walk-out pose leftover/.test(srcTxt));
check("rival recovery walk-out pose leftover you A/D path unchanged", /f\.kind === "you"\) return actionHeld\("left"\) \|\| actionHeld\("right"\)/.test(sliceFn(codeOnly, "recoveryWalkOut")) && /You A\/D path unchanged/.test(srcTxt) && /restSheatheWalk = fade > 0 && !f\.guarding && \(gaitWalkOn\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)\)/.test(sliceFn(codeOnly, "drawKnight")));
check("rival recovery walk-out pose leftover idle closing false still idle", /Idle recovery \(closing false\) still rests[\s\S]{0,30}on idle/.test(srcTxt) && /Idle stun still rests on idle/.test(srcTxt) && /recoveryWalkResting\(f\)/.test(sliceFn(codeOnly, "drawKnight")));
check("rival recovery walk-out pose leftover poseBitmap still source sheets", /poseBitmap still slash \/ windup \/ knife \/ idle/.test(srcTxt) && /poseBitmap still idle/.test(srcTxt));
check("rival recovery walk-out pose leftover fade clocks unchanged", /Fade clocks unchanged/.test(srcTxt) && /function sheatheFade/.test(srcTxt) && /function throwPlantFade/.test(srcTxt) && /function boltPlantFade/.test(srcTxt) && V.SHEATHE_MS===140 && V.RECOVERY===280 && V.BOLT_RECOVERY===280 && V.HITSTUN===350);
check("rival recovery walk-out pose leftover is not a new combat verb", V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.WALK_STEP_MS===320 && V.GUARD_RAISE_MS===80);
check("slash frames still 180/140/280 after rival recovery walk-out pose leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after rival recovery walk-out pose leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after rival recovery walk-out pose leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after rival recovery walk-out pose leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after rival recovery walk-out pose leftover", /game.js\?v=350/.test(htmlTxt));

// live: rival chip stun + closing rests walk (no idle dump)
toClose();
freezeYouAI();
Math.random = () => 0.99;
V.openLeft = 0; V.hitstopLeft = 0;
V.rival.phase="idle"; V.rival.phaseT=0; V.rival.stunT=V.HITSTUN; V.rival.thrownT=0; V.rival.guarding=false;
V.rival.boltPhase=""; V.rival.falling=false; V.rival.gait=0; V.rival.hp=90; V.rival.closing=true;
V.rival.x=900; V.player.x=400; V.rival.facing=-1; V.player.facing=1;
V.keepApart();
let rrwoSaw=false, rrwoRest=false, rrwoAabbOk=true, rrwoMaxHop=0;
const rrwoAabb0 = V.bodyAABB(V.rival).y;
const rrwoDy0 = V.destRect(V.rival).dy;
for (let i=0;i<12;i++){
  V.rival.closing = true;
  V.rival.wantBlock = false;
  V.rival.guardChoice = -1;
  V.rival.standGoal = 99999;
  if (V.rival.stunT>0 && V.recoveryWalkOut(V.rival) && V.recoveryWalkResting(V.rival)) {
    rrwoSaw = true;
    rrwoRest = true;
  }
  const dy0 = V.destRect(V.rival).dy;
  V.update(STEP);
  rrwoMaxHop = Math.max(rrwoMaxHop, Math.abs(V.destRect(V.rival).dy - dy0));
  if (Math.abs(V.bodyAABB(V.rival).y - rrwoAabb0) > 0.05) rrwoAabbOk = false;
  if (V.rival.stunT<=0 && i>3) break;
}
check("rival recovery walk-out pose leftover live stun closing arms", rrwoSaw && rrwoRest, `saw=${rrwoSaw} rest=${rrwoRest} st=${V.rival.stunT} walkOut=${V.recoveryWalkOut(V.rival)}`);
check("rival recovery walk-out pose leftover live stun AABB planted", rrwoAabbOk, `aabbOk=${rrwoAabbOk}`);
check("rival recovery walk-out pose leftover live stun destRect no sheet hop", rrwoMaxHop < 3.5, `hop=${rrwoMaxHop.toFixed(3)} dy0=${rrwoDy0.toFixed(2)}`);

// live: mid-stun still walkOut; stun end keeps walk (no idle dump frame)
toClose();
freezeYouAI();
Math.random = () => 0.99;
V.openLeft = 0; V.hitstopLeft = 0;
V.rival.phase="idle"; V.rival.stunT=STEP*2; V.rival.thrownT=0; V.rival.guarding=false;
V.rival.boltPhase=""; V.rival.falling=false; V.rival.gait=0; V.rival.hp=90; V.rival.closing=true;
V.rival.x=900; V.player.x=400;
let rrwoMid=false, rrwoEndWalk=false;
V.rival.closing = true;
rrwoMid = V.recoveryWalkOut(V.rival) && V.recoveryWalkResting(V.rival) && V.rival.stunT>0;
V.update(STEP);
V.rival.closing = true;
V.update(STEP);
V.rival.closing = true;
// stun just ended or ending — closing should keep walkOut through the hop window
rrwoEndWalk = V.rival.stunT<=0 && (V.recoveryWalkOut(V.rival) || V.gaitWalkOn(V.rival) || V.rival.gait!==0);
check("rival recovery walk-out pose leftover live mid-stun walkOut", rrwoMid, `mid=${rrwoMid}`);
check("rival recovery walk-out pose leftover live stun end keeps walk intent", rrwoEndWalk, `endWalk=${rrwoEndWalk} st=${V.rival.stunT} gait=${V.rival.gait} walkOut=${V.recoveryWalkOut(V.rival)} gaitOn=${V.gaitWalkOn(V.rival)}`);

// live: rival closing false still idle rest (no walkOut)
toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.rival.phase="idle"; V.rival.stunT=V.HITSTUN; V.rival.thrownT=0; V.rival.closing=false; V.rival.gait=0;
check("rival recovery walk-out pose leftover live idle stun no walkOut", V.rival.stunT>0 && !V.recoveryWalkOut(V.rival) && !V.recoveryWalkResting(V.rival) && V.rival.gait===0, `st=${V.rival.stunT} walkOut=${V.recoveryWalkOut(V.rival)} rest=${V.recoveryWalkResting(V.rival)} gait=${V.rival.gait}`);

// live: you A/D path still arms recoveryWalkOut
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=V.HITSTUN; V.player.thrownT=0; V.player.guarding=false;
V.player.boltPhase=""; V.player.falling=false; V.player.gait=0; V.player.hp=90;
V.player.x=400; V.rival.x=1100;
V.keys.add("KeyD");
check("rival recovery walk-out pose leftover live you A/D still arms", V.recoveryWalkOut(V.player) && V.recoveryWalkResting(V.player), `walkOut=${V.recoveryWalkOut(V.player)} rest=${V.recoveryWalkResting(V.player)}`);

// live: rival bolt recovery + closing rests
toClose();
freezeYouAI();
Math.random = () => 0.99;
V.openLeft = 0; V.hitstopLeft = 0;
V.rival.phase="idle"; V.rival.stunT=0; V.rival.guarding=false;
V.rival.boltPhase="recovery"; V.rival.boltT=40; V.rival.boltHoldCut=false; V.rival.falling=false; V.rival.gait=0;
V.rival.closing=true; V.rival.hp=100;
V.rival.x=900; V.player.x=400;
let rrwoBolt=false;
for (let i=0;i<16;i++){
  V.rival.closing = true;
  V.rival.wantBlock = false;
  V.rival.guardChoice = -1;
  V.rival.standGoal = 99999;
  if (V.rival.boltPhase==="recovery" && V.boltPlantFade(V.rival)>0.02 && V.recoveryWalkResting(V.rival)) rrwoBolt = true;
  V.update(STEP);
  if (!V.rival.boltPhase && i>3) break;
}
check("rival recovery walk-out pose leftover live bolt closing rests walk", rrwoBolt, `bolt=${rrwoBolt} bf=${V.boltPlantFade(V.rival)} bph=${V.rival.boltPhase}`);

check("rival recovery walk-out pose leftover draw shared", /recoveryWalkOut\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /recoveryWalkResting\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /return !!f\.closing/.test(sliceFn(codeOnly, "recoveryWalkOut")) && /restBoltWalk/.test(sliceFn(codeOnly, "drawKnight")));
check("cache v=296 after rival recovery walk-out pose leftover live", /game.js\?v=350/.test(htmlTxt));



// --- v260 clash walk-in pose leftover: rest cut raise on walk when A/D/closing through clash cancel ---
check("clash walk-in pose leftover seats no walk dump under clash cancel", /Clash walk-in pose leftover/.test(srcTxt) && /leftover cut used to dump walk the tick clash-Space\/L\/K/.test(srcTxt) && /armed from recovery while A\/D/.test(srcTxt) && /rival closing/.test(srcTxt) && /clashPlantFade skipped/.test(srcTxt) && /dumped walk→raise under the fading slash/.test(srcTxt) && /Same hole holdCut walk-in/.test(srcTxt));
check("clash walk-in pose leftover is draw-only", /Draw-only \(drawKnight\)/.test(srcTxt) && /restClashWalk/.test(sliceFn(codeOnly, "drawKnight")) && /recoveryWalkOut\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && !/restClashWalk/.test(sliceFn(codeOnly, "destRect")) && !/restClashWalk/.test(sliceFn(codeOnly, "bodyAABB")) && !/restClashWalk/.test(sliceFn(codeOnly, "slashPose")) && !/restClashWalk/.test(sliceFn(codeOnly, "idleBreath")) && !/restClashWalk/.test(sliceFn(codeOnly, "clashPlantFade")) && !/restClashWalk/.test(sliceFn(codeOnly, "poseBitmap")) && !/restClashWalk/.test(sliceFn(codeOnly, "tickGait")));
check("clash walk-in pose leftover rests cut raise on walk when A/D", /restClashWalk = cf > 0\.02/.test(sliceFn(codeOnly, "drawKnight")) && /f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)\) && ready\(cut\) && ready\(walk\)/.test(sliceFn(codeOnly, "drawKnight")) && /restClash = cf > 0\.02/.test(sliceFn(codeOnly, "drawKnight")) && /!restClashWalk && ready\(cut\) && ready\(boltPlant\)/.test(sliceFn(codeOnly, "drawKnight")) && /else if \(restClashWalk\)/.test(sliceFn(codeOnly, "drawKnight")) && /octx\.drawImage\(walk, 0, 0, dw, dh\)/.test(sliceFn(codeOnly, "drawKnight")));
check("clash walk-in pose leftover eases poseBitmap raise over walk", /restClashWalk/.test(sliceFn(codeOnly, "drawKnight")) && /1 - cf/.test(sliceFn(codeOnly, "drawKnight")) && /octx\.drawImage\(img, 0, 0, dw, dh\)/.test(sliceFn(codeOnly, "drawKnight")) && /restClash \|\| restClashWalk/.test(sliceFn(codeOnly, "drawKnight")));
check("clash walk-in pose leftover idle plant still rests on windup", /Idle clash plant still rests on windup\/knife/.test(srcTxt) && /restClash = cf > 0\.02/.test(sliceFn(codeOnly, "drawKnight")) && /!restClashWalk && ready\(cut\) && ready\(boltPlant\)/.test(sliceFn(codeOnly, "drawKnight")));
check("clash walk-in pose leftover holdCut\/link\/tele unchanged", /HoldCut \/ link \/ telegraph walk-in unchanged/.test(srcTxt) && /restHoldCutWalk/.test(sliceFn(codeOnly, "drawKnight")) && /restLinkWalk/.test(sliceFn(codeOnly, "drawKnight")) && /telegraphing\(f\) && \(f\.walkFadeHold > 0 \|\| f\.walkSettleT > 0\)/.test(sliceFn(codeOnly, "tickGait")) && /linkPlanting\(f\) && \(f\.walkFadeHold > 0 \|\| f\.walkSettleT > 0\)/.test(sliceFn(codeOnly, "tickGait")));
check("clash walk-in pose leftover poseBitmap still windup\/knife", /poseBitmap still windup\/knife immediately/.test(srcTxt) && /f\.phase === "startup" && ready\(wind\)/.test(sliceFn(codeOnly, "poseBitmap")) && /boltHoldCut = !fromClash/.test(sliceFn(codeOnly, "cancelIntoBolt")));
check("clash walk-in pose leftover fade clocks unchanged", /ClashPlantFade clock unchanged/.test(srcTxt) && /function clashPlantFade/.test(srcTxt) && V.GUARD_RAISE_MS===80 && V.CLASH_RECOVERY===200 && V.BOLT_CANCEL_MS===100 && V.SLASH_CANCEL_MS===100 && V.GOLPE_CANCEL_MS===100);
check("clash walk-in pose leftover is not a new combat verb", V.CLASH_RECOVERY===200 && V.BOLT_CANCEL_MS===100 && V.SLASH_CANCEL_MS===100 && V.GOLPE_CANCEL_MS===100 && V.GUARD_RAISE_MS===80 && V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.HITSTUN===350 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280);
check("slash frames still 180/140/280 after clash walk-in pose leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after clash walk-in pose leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after clash walk-in pose leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after clash walk-in pose leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after clash walk-in pose leftover", /game.js\?v=350/.test(htmlTxt));

// live: A/D through clash-K plant keeps recoveryWalkOut + clashPlant
landClash();
V.player.stamRegenT = 99999;
const cwiGot = waitUntilBoltWindow("slash");
for(let i=0;i<20;i++){
  holdRival();
  V.player.stamRegenT = 99999;
  if (V.player.pushT<=0 && V.rival.pushT<=0) break;
  V.update(STEP);
}
V.keys.clear();
V.keys.add("KeyD");
const cwiAabb0 = V.bodyAABB(V.player);
const cwiDy0 = V.destRect(V.player).dy;
V.boltEdge = true;
V.update(STEP);
const cwiCf0 = V.clashPlantFade(V.player);
const cwiWalkOut = V.recoveryWalkOut(V.player);
check("clash walk-in pose leftover live K arms with A/D", cwiGot && V.player.boltPhase==="startup" && V.player.clashPlant && !V.player.boltHoldCut && cwiCf0>0.8 && cwiWalkOut && V.sheatheFade(V.player)===0, `got=${cwiGot} bph=${V.player.boltPhase} plant=${V.player.clashPlant} hold=${V.player.boltHoldCut} cf=${cwiCf0} walkOut=${cwiWalkOut} sf=${V.sheatheFade(V.player)}`);
check("clash walk-in pose leftover live poseBitmap still throw_knife", /throw_knife/.test(sheetSrc(V.player)) && !isCutSheet(V.player), `src=${sheetSrc(V.player)}`);
let cwiSaw=false, cwiAabbOk=true, cwiMaxHop=0;
for (let i=0;i<6;i++){
  freezeRivalAI();
  V.rival.x = 1220;
  V.rival.stunT = 0;
  V.rival.guarding = false;
  V.keys.add("KeyD");
  if (V.player.boltPhase==="startup" && V.clashPlantFade(V.player)>0.02 && V.recoveryWalkOut(V.player)) cwiSaw = true;
  const dy0 = V.destRect(V.player).dy;
  V.update(STEP);
  cwiMaxHop = Math.max(cwiMaxHop, Math.abs(V.destRect(V.player).dy - dy0));
  if (Math.abs(V.bodyAABB(V.player).y - cwiAabb0.y) > 0.05) cwiAabbOk = false;
}
check("clash walk-in pose leftover live mid plant keeps A/D", cwiSaw && V.recoveryWalkOut(V.player), `saw=${cwiSaw} walkOut=${V.recoveryWalkOut(V.player)} cf=${V.clashPlantFade(V.player)}`);
check("clash walk-in pose leftover live AABB planted", cwiAabbOk, `aabbOk=${cwiAabbOk}`);
check("clash walk-in pose leftover live destRect no sheet hop", cwiMaxHop < 3.5, `hop=${cwiMaxHop.toFixed(3)} dy0=${cwiDy0.toFixed(2)}`);

// live: idle clash-K still idle rest (no A/D)
landClash();
V.player.stamRegenT = 99999;
const cwiIdleGot = waitUntilBoltWindow("slash");
for(let i=0;i<20;i++){
  holdRival();
  V.player.stamRegenT = 99999;
  if (V.player.pushT<=0 && V.rival.pushT<=0) break;
  V.update(STEP);
}
V.keys.clear();
V.boltEdge = true;
V.update(STEP);
check("clash walk-in pose leftover live idle plant still windup", cwiIdleGot && V.player.boltPhase==="startup" && V.player.clashPlant && V.clashPlantFade(V.player)>0.8 && !V.recoveryWalkOut(V.player) && V.player.gait===0 && /throw_knife/.test(sheetSrc(V.player)), `got=${cwiIdleGot} plant=${V.player.clashPlant} cf=${V.clashPlantFade(V.player)} walkOut=${V.recoveryWalkOut(V.player)} gait=${V.player.gait} src=${sheetSrc(V.player)}`);

// live: rival clash-K + closing rests walk
landClash();
V.rival.stamRegenT = 99999;
V.rival.stamina = 100;
V.rival.boltCd = 0;
V.rival.linkBolt = true;
V.rival.clashRec = true;
V.rival.closing = true;
V.rival.gait = 0;
Math.random = () => 0.01;
let cwiRival=false, cwiRivalCf=0;
for (let i=0;i<40;i++){
  V.rival.closing = true;
  V.rival.wantBlock = false;
  V.rival.guardChoice = -1;
  V.rival.standGoal = 99999;
  V.rival.stamRegenT = 99999;
  if (V.rival.boltPhase==="startup" && V.rival.clashPlant && V.clashPlantFade(V.rival)>0.02 && V.recoveryWalkOut(V.rival)) {
    cwiRival = true;
    cwiRivalCf = V.clashPlantFade(V.rival);
    break;
  }
  V.update(STEP);
  if (V.rival.phase==="idle" && !V.rival.boltPhase && i>8) break;
}
check("clash walk-in pose leftover live rival closing arms", cwiRival && cwiRivalCf>0.5, `rival=${cwiRival} cf=${cwiRivalCf} bph=${V.rival.boltPhase} plant=${V.rival.clashPlant} walkOut=${V.recoveryWalkOut(V.rival)} closing=${V.rival.closing}`);

check("clash walk-in pose leftover draw shared", /restClashWalk/.test(sliceFn(codeOnly, "drawKnight")) && /f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /restClash \|\| restClashWalk/.test(sliceFn(codeOnly, "drawKnight")));
check("cache v=296 after clash walk-in pose leftover live", /game.js\?v=350/.test(htmlTxt));






// --- v261 telegraph recovery walk-in pose leftover: rest walk under telePlant when A/D/closing through recovery raise ---
check("telegraph recovery walk-in pose leftover seats no idle dump from recovery", /Telegraph recovery walk-in pose leftover/.test(srcTxt) && /leftover walk used to dump to idle the tick/.test(srcTxt) && /startAttack\/startThrow\/startBolt armed from recovery/.test(srcTxt) && /walkFadeHold stayed[\s\S]{0,20}0/.test(srcTxt) && /restTeleWalk only checked walkFadeHold/.test(srcTxt) && /buffered raise hopped walk→idle under/.test(srcTxt) && /Same hole link plant walk-in/.test(srcTxt));
check("telegraph recovery walk-in pose leftover is draw-only", /Draw-only \(drawKnight \+ startAttack\/startThrow\/startBolt[\s\S]{0,40}walkFadeHold arm\)/.test(srcTxt) && /restTeleWalk/.test(sliceFn(codeOnly, "drawKnight")) && /recoveryWalkOut\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /teleRecWalk/.test(sliceFn(codeOnly, "startAttack")) && /walkFadeHold = 1/.test(sliceFn(codeOnly, "startAttack")) && /teleRecWalk/.test(sliceFn(codeOnly, "startThrow")) && /teleRecWalk/.test(sliceFn(codeOnly, "startBolt")) && !/restTeleWalk/.test(sliceFn(codeOnly, "destRect")) && !/restTeleWalk/.test(sliceFn(codeOnly, "bodyAABB")) && !/restTeleWalk/.test(sliceFn(codeOnly, "slashPose")) && !/restTeleWalk/.test(sliceFn(codeOnly, "idleBreath")) && !/restTeleWalk/.test(sliceFn(codeOnly, "telegraphFade")) && !/restTeleWalk/.test(sliceFn(codeOnly, "poseBitmap")));
check("telegraph recovery walk-in pose leftover rests walk when A/D through recovery", /restTeleWalk = telegraphing\(f\) && tf < 0\.98 && \(f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)\)/.test(sliceFn(codeOnly, "drawKnight")) && /ready\(telePlant\) && ready\(walk\)/.test(sliceFn(codeOnly, "drawKnight")) && /else if \(restTeleWalk\)/.test(sliceFn(codeOnly, "drawKnight")) && /octx\.drawImage\(walk, 0, 0, dw, dh\)/.test(sliceFn(codeOnly, "drawKnight")) && /restTele \|\| restTeleWalk/.test(sliceFn(codeOnly, "drawKnight")));
check("telegraph recovery walk-in pose leftover idle telegraph still rests on idle", /Idle telegraph still rests on idle/.test(srcTxt) && /restTele = telegraphing\(f\) && tf < 0\.98 && !restTeleWalk/.test(sliceFn(codeOnly, "drawKnight")) && /Mid-stride walkFadeHold path unchanged/.test(srcTxt));
check("telegraph recovery walk-in pose leftover mid-stride unchanged", /Mid-stride walkFadeHold path unchanged/.test(srcTxt) && /telegraphing\(f\) && \(f\.walkFadeHold > 0 \|\| f\.walkSettleT > 0\)/.test(sliceFn(codeOnly, "tickGait")) && /Keep leftover walkFadeHold through leftover telegraph raise/.test(srcTxt));
check("telegraph recovery walk-in pose leftover clash\/holdCut\/link unchanged", /Clash \/ holdCut \/ link walk-in unchanged/.test(srcTxt) && /restClashWalk/.test(sliceFn(codeOnly, "drawKnight")) && /restHoldCutWalk/.test(sliceFn(codeOnly, "drawKnight")) && /restLinkWalk/.test(sliceFn(codeOnly, "drawKnight")));
check("telegraph recovery walk-in pose leftover poseBitmap still windup\/knife", /poseBitmap still windup\/knife immediately/.test(srcTxt) && /f\.phase === "startup" && ready\(wind\)/.test(sliceFn(codeOnly, "poseBitmap")) && /f\.boltPhase === "startup" && ready\(throwK\)/.test(sliceFn(codeOnly, "poseBitmap")) && !/restTeleWalk/.test(sliceFn(codeOnly, "poseBitmap")));
check("telegraph recovery walk-in pose leftover fade clocks unchanged", /TelegraphFade[\s\S]{0,20}clock unchanged/.test(srcTxt) && /function telegraphFade/.test(srcTxt) && /This fade unchanged/.test(srcTxt) && V.GUARD_RAISE_MS===80 && V.STARTUP===180 && V.THROW_STARTUP===80 && V.BOLT_STARTUP===200);
check("telegraph recovery walk-in pose leftover is not a new combat verb", V.GUARD_RAISE_MS===80 && V.WALK_STEP_MS===320 && V.WALK_SETTLE_MS===160 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80 && V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280);
check("slash frames still 180/140/280 after telegraph recovery walk-in pose leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after telegraph recovery walk-in pose leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after telegraph recovery walk-in pose leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after telegraph recovery walk-in pose leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after telegraph recovery walk-in pose leftover", /game.js\?v=350/.test(htmlTxt));

// live: A/D through recovery into buffered Space keeps walk under tele raise
function armRecoveryTeleRaise(){
  freezeYouAI(); freezeRivalAI();
  holdRivalStill();
  V.keys.clear();
  V.hitstopLeft=0; V.openLeft=0;
  V.player.x=400; V.rival.x=900;
  V.player.facing=1; V.rival.facing=-1;
  V.player.phase="idle"; V.player.phaseT=0; V.player.cut="slash";
  V.player.stunT=0; V.player.falling=false; V.player.thrownT=0; V.player.throwInvulnT=0;
  V.player.boltPhase=""; V.player.boltHoldCut=false; V.player.boltSuper=false;
  V.player.guarding=false; V.player.guardPoseK=0; V.player.guardBreakT=0;
  V.player.feintT=0; V.player.reversal=false;
  V.player.linkPlant=false; V.player.clashPlant=false; V.player.holdCutPlant=false;
  V.player.linkSheathe=0; V.player.telegraph=false; V.player.sheatheT=0;
  V.player.clashRec=false; V.player.techRec=false;
  V.player.walkFadeHold=0; V.player.walkSettleT=0; V.player.walkT=0; V.player.gait=0;
  V.player.stamina=V.STAMINA_MAX; V.player.stamRegenT=99999;
  V.startAttack(V.player);
  // hold D through the cut
  V.keys.add("KeyD");
  for (let i=0;i<80;i++){
    V.update(STEP);
    if (V.player.phase==="recovery" && V.player.phaseT > V.RECOVERY - STEP*2) break;
  }
  // buffer Space near end of recovery
  V.attackEdge = true;
  let got=false, tf0=0, walkOut=false, wf0=0;
  for (let i=0;i<12;i++){
    V.update(STEP);
    if (V.telegraphing(V.player) && V.player.phase==="startup"){
      got=true; tf0=V.telegraphFade(V.player); walkOut=V.recoveryWalkOut(V.player); wf0=V.player.walkFadeHold;
      break;
    }
  }
  return {got, tf0, walkOut, wf0};
}
const trw = armRecoveryTeleRaise();
check("telegraph recovery walk-in pose leftover live Space arms with A/D", trw.got && V.player.phase==="startup" && trw.walkOut && V.player.walkFadeHold>0.02 && V.sheatheFade(V.player)===0, `got=${trw.got} ph=${V.player.phase} walkOut=${trw.walkOut} wf=${V.player.walkFadeHold} sf=${V.sheatheFade(V.player)}`);
check("telegraph recovery walk-in pose leftover live poseBitmap still windup", /windup/.test(sheetSrc(V.player)) && !isCutSheet(V.player), `src=${sheetSrc(V.player)}`);

let trwSaw=false, trwAabbOk=true, trwMaxHop=0, trwDy0=V.destRect(V.player).dy;
const trwAabb0 = V.bodyAABB(V.player);
for (let i=0;i<10;i++){
  V.keys.add("KeyD");
  const d0=V.destRect(V.player);
  V.update(STEP);
  const d1=V.destRect(V.player);
  const hop=Math.hypot(d1.dx-d0.dx, d1.dy-d0.dy);
  if (hop>trwMaxHop) trwMaxHop=hop;
  const a=V.bodyAABB(V.player);
  if (Math.abs(a.x-trwAabb0.x)>0.05 || Math.abs(a.y-trwAabb0.y)>0.05 || Math.abs(a.w-trwAabb0.w)>0.05 || Math.abs(a.h-trwAabb0.h)>0.05) trwAabbOk=false;
  if (V.recoveryWalkOut(V.player) && V.telegraphing(V.player) && V.telegraphFade(V.player)<0.98) trwSaw=true;
}
check("telegraph recovery walk-in pose leftover live mid raise keeps A/D", trwSaw && V.recoveryWalkOut(V.player), `saw=${trwSaw} walkOut=${V.recoveryWalkOut(V.player)} tf=${V.telegraphFade(V.player)}`);
check("telegraph recovery walk-in pose leftover live AABB planted", trwAabbOk, `aabbOk=${trwAabbOk}`);
check("telegraph recovery walk-in pose leftover live destRect no sheet hop", trwMaxHop < 3.5, `hop=${trwMaxHop.toFixed(3)} dy0=${trwDy0.toFixed(2)}`);

// idle telegraph (no A/D) still rests without recoveryWalkOut
freezeYouAI(); freezeRivalAI(); holdRivalStill();
V.keys.clear();
V.hitstopLeft=0; V.openLeft=0;
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.cut="slash";
V.player.stunT=0; V.player.falling=false; V.player.boltPhase="";
V.player.guarding=false; V.player.feintT=0; V.player.walkFadeHold=0; V.player.gait=0; V.player.walkT=0;
V.player.stamina=V.STAMINA_MAX;
V.startAttack(V.player);
check("telegraph recovery walk-in pose leftover live idle plant still idle", V.player.phase==="startup" && V.telegraphing(V.player) && V.telegraphFade(V.player)<0.5 && !V.recoveryWalkOut(V.player) && V.player.gait===0 && /windup/.test(sheetSrc(V.player)), `ph=${V.player.phase} tf=${V.telegraphFade(V.player)} walkOut=${V.recoveryWalkOut(V.player)} gait=${V.player.gait} src=${sheetSrc(V.player)}`);

// rival closing through recovery into startup shares walkFadeHold arm
freezeYouAI(); freezeRivalAI(); holdRivalStill();
V.keys.clear();
V.hitstopLeft=0; V.openLeft=0;
V.player.x=200; V.rival.x=700;
V.player.phase="idle"; V.player.stunT=0; V.player.falling=false; V.player.boltPhase="";
V.rival.phase="recovery"; V.rival.phaseT=40; V.rival.cut="slash";
V.rival.stunT=0; V.rival.falling=false; V.rival.boltPhase="";
V.rival.guarding=false; V.rival.feintT=0; V.rival.sheatheT=0;
V.rival.clashRec=false; V.rival.techRec=false; V.rival.linkPlant=false;
V.rival.walkFadeHold=0; V.rival.gait=0; V.rival.walkT=0;
V.rival.closing=true;
V.rival.stamina=V.STAMINA_MAX; V.rival.stamRegenT=99999;
for (let i=0;i<40;i++){
  freezeRivalAI();
  V.rival.closing=true;
  V.update(STEP);
  if (V.rival.phase==="idle" && !V.rival.boltPhase) break;
}
freezeRivalAI();
V.rival.phase="idle"; V.rival.phaseT=0; V.rival.boltPhase=""; V.rival.feintT=0;
V.rival.guarding=false; V.rival.stunT=0; V.rival.falling=false;
V.rival.closing=true;
V.rival.walkFadeHold=0;
V.rival.gait=0; V.rival.walkT=0;
const trwRivalBefore = V.recoveryWalkOut(V.rival);
V.startAttack(V.rival);
const trwRival = trwRivalBefore && V.telegraphing(V.rival) && V.rival.phase==="startup" && V.rival.walkFadeHold>0.02 && V.telegraphFade(V.rival)<0.98;
check("telegraph recovery walk-in pose leftover live rival closing arms", trwRival, `rival=${trwRival} before=${trwRivalBefore} tf=${V.telegraphFade(V.rival)} ph=${V.rival.phase} walkOut=${V.recoveryWalkOut(V.rival)} closing=${V.rival.closing} wf=${V.rival.walkFadeHold}`);

check("telegraph recovery walk-in pose leftover draw shared", /restTeleWalk/.test(sliceFn(codeOnly, "drawKnight")) && /recoveryWalkOut\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /restTele \|\| restTeleWalk/.test(sliceFn(codeOnly, "drawKnight")));
check("cache v=296 after telegraph recovery walk-in pose leftover live", /game.js\?v=350/.test(htmlTxt));




// --- v262 reversal plant walk-in pose leftover: rest block raise on walk when A/D/closing through reverse ---
check("reversal plant walk-in pose leftover seats no walk dump under reverse", /Reversal plant walk-in pose leftover/.test(srcTxt) && /leftover walk used to dump the tick startReversal/.test(srcTxt) && /armed from walk→guard \/ A\/D \/ rival closing/.test(srcTxt) && /tickGait zeroed walkFadeHold on phase/.test(srcTxt) && /restRev rested block on windup with no walk base/.test(srcTxt) && /hopped walk→raise under the fading block/.test(srcTxt) && /Same hole telegraph recovery \/ clash walk-in/.test(srcTxt));
check("reversal plant walk-in pose leftover is draw-only", /Draw-only[\s\S]{0,40}drawKnight[\s\S]{0,20}startReversal walkFadeHold arm[\s\S]{0,20}tickGait hold/.test(srcTxt) && /restRevWalk/.test(sliceFn(codeOnly, "drawKnight")) && /revRecWalk/.test(sliceFn(codeOnly, "startReversal")) && /walkFadeHold = 1/.test(sliceFn(codeOnly, "startReversal")) && /reversalPlanting\(f\) && \(f\.walkFadeHold > 0 \|\| f\.walkSettleT > 0\)/.test(sliceFn(codeOnly, "tickGait")) && !/restRevWalk/.test(sliceFn(codeOnly, "destRect")) && !/restRevWalk/.test(sliceFn(codeOnly, "bodyAABB")) && !/restRevWalk/.test(sliceFn(codeOnly, "slashPose")) && !/restRevWalk/.test(sliceFn(codeOnly, "idleBreath")) && !/restRevWalk/.test(sliceFn(codeOnly, "reversalPlantFade")) && !/restRevWalk/.test(sliceFn(codeOnly, "poseBitmap")));
check("reversal plant walk-in pose leftover rests block raise on walk when A/D", /restRevWalk = rf > 0\.02 && \(f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)\)/.test(sliceFn(codeOnly, "drawKnight")) && /ready\(block\) && ready\(walk\)/.test(sliceFn(codeOnly, "drawKnight")) && /restRev = rf > 0\.02 && !restRevWalk/.test(sliceFn(codeOnly, "drawKnight")) && /else if \(restRevWalk\)/.test(sliceFn(codeOnly, "drawKnight")) && /octx\.drawImage\(walk, 0, 0, dw, dh\)/.test(sliceFn(codeOnly, "drawKnight")));
check("reversal plant walk-in pose leftover eases windup under leftover block", /restRevWalk/.test(sliceFn(codeOnly, "drawKnight")) && /octx\.drawImage\(wind, 0, 0, dw, dh\)/.test(sliceFn(codeOnly, "drawKnight")) && /restRev \|\| restRevWalk/.test(sliceFn(codeOnly, "drawKnight")));
check("reversal plant walk-in pose leftover idle reverse still rests on windup", /Idle guard reverse still rests on windup/.test(srcTxt) && /restRev = rf > 0\.02 && !restRevWalk/.test(sliceFn(codeOnly, "drawKnight")) && /ready\(block\) && ready\(wind\)/.test(sliceFn(codeOnly, "drawKnight")));
check("reversal plant walk-in pose leftover tickGait keeps walkFadeHold", /Keep leftover walkFadeHold through leftover reversal[\s\S]{0,20}plant/.test(srcTxt) && /reversalPlanting\(f\) && \(f\.walkFadeHold > 0 \|\| f\.walkSettleT > 0\)/.test(sliceFn(codeOnly, "tickGait")) && /reversalPlantFade\(f\) <= 0\.02/.test(sliceFn(codeOnly, "tickGait")));
check("reversal plant walk-in pose leftover clash\/holdCut\/link\/tele unchanged", /Walk→guard \/ clash \/ holdCut[\s\S]{0,20}link \/ telegraph walk-in unchanged/.test(srcTxt) && /restClashWalk/.test(sliceFn(codeOnly, "drawKnight")) && /restHoldCutWalk/.test(sliceFn(codeOnly, "drawKnight")) && /restLinkWalk/.test(sliceFn(codeOnly, "drawKnight")) && /restTeleWalk/.test(sliceFn(codeOnly, "drawKnight")) && /telegraphing\(f\) && \(f\.walkFadeHold > 0 \|\| f\.walkSettleT > 0\)/.test(sliceFn(codeOnly, "tickGait")));
check("reversal plant walk-in pose leftover poseBitmap still windup", /poseBitmap still windup immediately/.test(srcTxt) && /f\.phase === "startup" && ready\(wind\)/.test(sliceFn(codeOnly, "poseBitmap")) && !/restRevWalk/.test(sliceFn(codeOnly, "poseBitmap")));
check("reversal plant walk-in pose leftover fade clocks unchanged", /ReversalPlantFade clock unchanged/.test(srcTxt) && /This fade unchanged/.test(srcTxt) && /function reversalPlantFade/.test(srcTxt) && V.GUARD_RAISE_MS===80 && V.REVERSAL_STAM===30 && V.REVERSAL_INVULN===120 && V.GOLPE_STARTUP===120);
check("reversal plant walk-in pose leftover is not a new combat verb", V.REVERSAL_STAM===30 && V.REVERSAL_INVULN===120 && V.GUARD_RAISE_MS===80 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.HITSTUN===350 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280);
check("slash frames still 180/140/280 after reversal plant walk-in pose leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after reversal plant walk-in pose leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after reversal plant walk-in pose leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after reversal plant walk-in pose leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after reversal plant walk-in pose leftover", /game.js\?v=350/.test(htmlTxt));

// live: A/D through walk→guard reverse keeps walkFadeHold + restRevWalk
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x = 400; V.rival.x = 1100;
V.player.guarding = false; V.player.stunT = 0; V.player.phase = "idle";
V.player.boltPhase = ""; V.player.falling = false; V.player.guardPoseK = 0;
V.player.gait = 0; V.player.walkT = 0; V.player.walkSettleT = 0; V.player.walkFadeHold = 0;
V.player.stamina = 100;
V.keys.add("KeyD");
let rwiWalkReady=false;
for(let i=0;i<12;i++){
  freezeRivalAI();
  V.player.phase="idle"; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase="";
  V.keys.add("KeyD");
  V.update(STEP);
  if(V.player.gait!==0 && V.player.walkFadeHold>0.2){ rwiWalkReady=true; break; }
}
V.keys.delete("KeyD");
V.keys.add("KeyS");
freezeRivalAI();
V.update(STEP);
for(let i=0;i<6;i++){ freezeRivalAI(); V.keys.add("KeyS"); V.update(STEP); }
check("reversal plant walk-in pose leftover live walk→guard armed", rwiWalkReady && V.player.guarding && V.player.walkFadeHold>0.02 && V.player.guardPoseK>0.8, `ready=${rwiWalkReady} g=${V.player.guarding} hold=${V.player.walkFadeHold} k=${V.player.guardPoseK}`);
V.keys.clear();
V.keys.add("KeyD");
V.keys.add("KeyS");
V.player.stamina = 100;
const rwiAabb0 = V.bodyAABB(V.player);
const rwiDy0 = V.destRect(V.player).dy;
const rwiHold0 = V.player.walkFadeHold;
const rwiOk = V.startReversal(V.player);
V.update(STEP);
const rwiRf0 = V.reversalPlantFade(V.player);
const rwiHold1 = V.player.walkFadeHold;
const rwiWalkOut = V.recoveryWalkOut(V.player);
check("reversal plant walk-in pose leftover live L arms with A/D", rwiOk && V.player.cut==="golpe" && V.player.phase==="startup" && V.player.reversal && rwiRf0>0.5 && rwiHold1>0.02 && rwiWalkOut && /windup/.test(sheetSrc(V.player)), `ok=${rwiOk} cut=${V.player.cut} ph=${V.player.phase} rf=${rwiRf0} hold0=${rwiHold0} hold=${rwiHold1} walkOut=${rwiWalkOut} src=${sheetSrc(V.player)}`);
check("reversal plant walk-in pose leftover live poseBitmap still windup", /windup/.test(sheetSrc(V.player)) && !isBlockSheet(V.player) && !isCutSheet(V.player), `src=${sheetSrc(V.player)}`);
let rwiSaw=false, rwiAabbOk=true, rwiMaxHop=0;
for (let i=0;i<5;i++){
  freezeRivalAI();
  V.keys.add("KeyD");
  if (V.player.reversal && V.reversalPlantFade(V.player)>0.02 && (V.player.walkFadeHold>0.02 || V.recoveryWalkOut(V.player))) rwiSaw = true;
  const dy0 = V.destRect(V.player).dy;
  V.update(STEP);
  rwiMaxHop = Math.max(rwiMaxHop, Math.abs(V.destRect(V.player).dy - dy0));
  if (Math.abs(V.bodyAABB(V.player).y - rwiAabb0.y) > 0.05) rwiAabbOk = false;
}
check("reversal plant walk-in pose leftover live mid plant keeps hold", rwiSaw, `saw=${rwiSaw} hold=${V.player.walkFadeHold} rf=${V.reversalPlantFade(V.player)} walkOut=${V.recoveryWalkOut(V.player)}`);
check("reversal plant walk-in pose leftover live AABB planted", rwiAabbOk, `aabbOk=${rwiAabbOk}`);
check("reversal plant walk-in pose leftover live destRect no sheet hop", rwiMaxHop < 3.5, `hop=${rwiMaxHop.toFixed(3)} dy0=${rwiDy0.toFixed(2)}`);

// live: idle guard reverse still idle rest (no A/D)
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
holdGuardSample();
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
V.keys.clear();
V.player.stamina = 100;
V.player.walkFadeHold = 0;
V.player.gait = 0;
const rwiIdleOk = V.startReversal(V.player);
V.update(STEP);
check("reversal plant walk-in pose leftover live idle plant still windup", rwiIdleOk && V.player.reversal && V.player.phase==="startup" && V.reversalPlantFade(V.player)>0.5 && !V.recoveryWalkOut(V.player) && V.player.walkFadeHold<0.02 && V.player.gait===0 && /windup/.test(sheetSrc(V.player)), `ok=${rwiIdleOk} rf=${V.reversalPlantFade(V.player)} walkOut=${V.recoveryWalkOut(V.player)} hold=${V.player.walkFadeHold} gait=${V.player.gait} src=${sheetSrc(V.player)}`);

// live: rival closing arms walkFadeHold on reverse
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.rival.x = 900; V.player.x = 500;
V.rival.guarding = true;
V.rival.guardPoseK = 1;
V.rival.stamina = 100;
V.rival.phase = "idle"; V.rival.phaseT = 0; V.rival.boltPhase = "";
V.rival.stunT = 0; V.rival.falling = false; V.rival.feintT = 0;
V.rival.closing = true;
V.rival.walkFadeHold = 0;
V.rival.gait = 0; V.rival.walkT = 0;
const rwiRivalClosing = V.rival.closing === true;
const rwiRivalOk = V.startReversal(V.rival);
const rwiRival = rwiRivalOk && V.rival.reversal && V.rival.phase==="startup" && V.rival.walkFadeHold>0.02 && V.reversalPlantFade(V.rival)>0.5;
check("reversal plant walk-in pose leftover live rival closing arms", rwiRival && rwiRivalClosing, `rival=${rwiRival} closing0=${rwiRivalClosing} ok=${rwiRivalOk} rf=${V.reversalPlantFade(V.rival)} hold=${V.rival.walkFadeHold} walkOut=${V.recoveryWalkOut(V.rival)} closing=${V.rival.closing}`);

check("reversal plant walk-in pose leftover draw shared", /restRevWalk/.test(sliceFn(codeOnly, "drawKnight")) && /recoveryWalkOut\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /restRev \|\| restRevWalk/.test(sliceFn(codeOnly, "drawKnight")));
check("cache v=296 after reversal plant walk-in pose leftover live", /game.js\?v=350/.test(htmlTxt));



// --- v263 throw-from-guard plant walk-in pose leftover: rest block raise on walk when A/D/closing through Space+S ---
check("throw-from-guard plant walk-in pose leftover seats no walk dump under throw", /Throw-from-guard plant walk-in pose leftover/.test(srcTxt) && /leftover walk used to dump the tick startThrow/.test(srcTxt) && /armed from walk→guard \/ A\/D \/ rival closing/.test(srcTxt) && /tickGait zeroed walkFadeHold on phase/.test(srcTxt) && /restThrowGuard rested block on windup with no walk base/.test(srcTxt) && /hopped walk→raise under the fading block/.test(srcTxt) && /Same hole reversal plant walk-in[\s\S]{0,20}already closed/.test(srcTxt));
check("throw-from-guard plant walk-in pose leftover is draw-only", /Draw-only[\s\S]{0,40}drawKnight[\s\S]{0,20}startThrow walkFadeHold arm[\s\S]{0,20}tickGait hold/.test(srcTxt) && /restThrowGuardWalk/.test(sliceFn(codeOnly, "drawKnight")) && /teleRecWalk/.test(sliceFn(codeOnly, "startThrow")) && /walkFadeHold = 1/.test(sliceFn(codeOnly, "startThrow")) && /throwGuardPlanting\(f\) && \(f\.walkFadeHold > 0 \|\| f\.walkSettleT > 0\)/.test(sliceFn(codeOnly, "tickGait")) && !/restThrowGuardWalk/.test(sliceFn(codeOnly, "destRect")) && !/restThrowGuardWalk/.test(sliceFn(codeOnly, "bodyAABB")) && !/restThrowGuardWalk/.test(sliceFn(codeOnly, "slashPose")) && !/restThrowGuardWalk/.test(sliceFn(codeOnly, "idleBreath")) && !/restThrowGuardWalk/.test(sliceFn(codeOnly, "throwGuardPlantFade")) && !/restThrowGuardWalk/.test(sliceFn(codeOnly, "poseBitmap")));
check("throw-from-guard plant walk-in pose leftover rests block raise on walk when A/D", /restThrowGuardWalk = tgf > 0\.02 && !restRev && !restRevWalk && \(f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)\)/.test(sliceFn(codeOnly, "drawKnight")) && /ready\(block\) && ready\(walk\)/.test(sliceFn(codeOnly, "drawKnight")) && /restThrowGuard = tgf > 0\.02 && !restRev && !restRevWalk && !restThrowGuardWalk/.test(sliceFn(codeOnly, "drawKnight")) && /else if \(restThrowGuardWalk\)/.test(sliceFn(codeOnly, "drawKnight")) && /octx\.drawImage\(walk, 0, 0, dw, dh\)/.test(sliceFn(codeOnly, "drawKnight")));
check("throw-from-guard plant walk-in pose leftover eases windup under leftover block", /restThrowGuardWalk/.test(sliceFn(codeOnly, "drawKnight")) && /Throw-from-guard plant walk-in: base is walk/.test(srcTxt) && /restThrowGuard \|\| restThrowGuardWalk/.test(sliceFn(codeOnly, "drawKnight")));
check("throw-from-guard plant walk-in pose leftover idle throw still rests on windup", /Idle throw-from-guard still rests on windup/.test(srcTxt) && /restThrowGuard = tgf > 0\.02 && !restRev && !restRevWalk && !restThrowGuardWalk/.test(sliceFn(codeOnly, "drawKnight")) && /ready\(block\) && ready\(wind\)/.test(sliceFn(codeOnly, "drawKnight")));
check("throw-from-guard plant walk-in pose leftover tickGait keeps walkFadeHold", /Keep leftover walkFadeHold through[\s\S]{0,40}leftover throw-from-guard[\s\S]{0,20}plant/.test(srcTxt) && /throwGuardPlanting\(f\) && \(f\.walkFadeHold > 0 \|\| f\.walkSettleT > 0\)/.test(sliceFn(codeOnly, "tickGait")) && /throwGuardPlantFade\(f\) <= 0\.02/.test(sliceFn(codeOnly, "tickGait")));
check("throw-from-guard plant walk-in pose leftover tech sibling walk rest", /Tech-from-guard plant walk-in/.test(srcTxt) && /restTechGuardWalk/.test(sliceFn(codeOnly, "drawKnight")) && /techGuardPlanting\(f\) && \(f\.walkFadeHold > 0 \|\| f\.walkSettleT > 0\)/.test(sliceFn(codeOnly, "tickGait")) && /walkFadeHold = 1/.test(sliceFn(codeOnly, "landThrowTech")) && /Idle tech-from-guard still rests on windup/.test(srcTxt));
check("throw-from-guard plant walk-in pose leftover rev\/clash\/holdCut\/link\/tele unchanged", /Reversal \/ clash \/ holdCut \/ link \/ telegraph walk-in unchanged/.test(srcTxt) && /restRevWalk/.test(sliceFn(codeOnly, "drawKnight")) && /restClashWalk/.test(sliceFn(codeOnly, "drawKnight")) && /restHoldCutWalk/.test(sliceFn(codeOnly, "drawKnight")) && /restLinkWalk/.test(sliceFn(codeOnly, "drawKnight")) && /restTeleWalk/.test(sliceFn(codeOnly, "drawKnight")) && /reversalPlanting\(f\) && \(f\.walkFadeHold > 0 \|\| f\.walkSettleT > 0\)/.test(sliceFn(codeOnly, "tickGait")));
check("throw-from-guard plant walk-in pose leftover poseBitmap still windup", /poseBitmap still windup immediately/.test(srcTxt) && /f\.cut === "throw" && \(f\.phase === "startup" \|\| f\.phase === "active" \|\| f\.phase === "recovery"\)/.test(sliceFn(codeOnly, "poseBitmap")) && !/restThrowGuardWalk/.test(sliceFn(codeOnly, "poseBitmap")));
check("throw-from-guard plant walk-in pose leftover fade clocks unchanged", /ThrowGuardPlantFade \/ TechGuardPlantFade clocks unchanged/.test(srcTxt) && /This fade unchanged/.test(srcTxt) && /function throwGuardPlantFade/.test(srcTxt) && /function techGuardPlantFade/.test(srcTxt) && V.GUARD_RAISE_MS===80 && V.THROW_STARTUP > 0 && V.THROW_TECH_REC > 0);
check("throw-from-guard plant walk-in pose leftover is not a new combat verb", V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80 && V.GUARD_RAISE_MS===80 && V.THROW_STARTUP===80 && V.THROW_ACTIVE===40 && V.THROW_RECOVERY===220 && V.THROW_STAM===0 && V.THROW_RANGE===120 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280);
check("slash frames still 180/140/280 after throw-from-guard plant walk-in pose leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after throw-from-guard plant walk-in pose leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after throw-from-guard plant walk-in pose leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after throw-from-guard plant walk-in pose leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after throw-from-guard plant walk-in pose leftover", /game.js\?v=350/.test(htmlTxt));

// live: A/D through walk→guard throw keeps walkFadeHold + restThrowGuardWalk
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x = 400; V.rival.x = 1100;
V.player.guarding = false; V.player.stunT = 0; V.player.phase = "idle";
V.player.boltPhase = ""; V.player.falling = false; V.player.guardPoseK = 0;
V.player.gait = 0; V.player.walkT = 0; V.player.walkSettleT = 0; V.player.walkFadeHold = 0;
V.player.stamina = 100;
V.keys.add("KeyD");
let twiWalkReady=false;
for(let i=0;i<12;i++){
  freezeRivalAI();
  V.player.phase="idle"; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase="";
  V.keys.add("KeyD");
  V.update(STEP);
  if(V.player.gait!==0 && V.player.walkFadeHold>0.2){ twiWalkReady=true; break; }
}
V.keys.delete("KeyD");
V.keys.add("KeyS");
freezeRivalAI();
V.update(STEP);
for(let i=0;i<6;i++){ freezeRivalAI(); V.keys.add("KeyS"); V.update(STEP); }
check("throw-from-guard plant walk-in pose leftover live walk→guard armed", twiWalkReady && V.player.guarding && V.player.walkFadeHold>0.02 && V.player.guardPoseK>0.8, `ready=${twiWalkReady} g=${V.player.guarding} hold=${V.player.walkFadeHold} k=${V.player.guardPoseK}`);
V.keys.clear();
V.keys.add("KeyD");
V.keys.add("KeyS");
V.player.stamina = 100;
const twiAabb0 = V.bodyAABB(V.player);
const twiDy0 = V.destRect(V.player).dy;
const twiHold0 = V.player.walkFadeHold;
const twiOk = V.startThrow(V.player);
V.update(STEP);
const twiTg0 = V.throwGuardPlantFade(V.player);
const twiHold1 = V.player.walkFadeHold;
const twiWalkOut = V.recoveryWalkOut(V.player);
check("throw-from-guard plant walk-in pose leftover live Space arms with A/D", twiOk && V.player.cut==="throw" && V.player.phase==="startup" && !V.player.reversal && twiTg0>0.5 && twiHold1>0.02 && twiWalkOut && /windup/.test(sheetSrc(V.player)), `ok=${twiOk} cut=${V.player.cut} ph=${V.player.phase} tgf=${twiTg0} hold0=${twiHold0} hold=${twiHold1} walkOut=${twiWalkOut} src=${sheetSrc(V.player)}`);
check("throw-from-guard plant walk-in pose leftover live poseBitmap still windup", /windup/.test(sheetSrc(V.player)) && !isBlockSheet(V.player) && !isCutSheet(V.player), `src=${sheetSrc(V.player)}`);
let twiSaw=false, twiAabbOk=true, twiMaxHop=0;
for (let i=0;i<5;i++){
  freezeRivalAI();
  V.keys.add("KeyD");
  if (V.player.cut==="throw" && V.throwGuardPlantFade(V.player)>0.02 && (V.player.walkFadeHold>0.02 || V.recoveryWalkOut(V.player))) twiSaw = true;
  const dy0 = V.destRect(V.player).dy;
  V.update(STEP);
  twiMaxHop = Math.max(twiMaxHop, Math.abs(V.destRect(V.player).dy - dy0));
  if (Math.abs(V.bodyAABB(V.player).y - twiAabb0.y) > 0.05) twiAabbOk = false;
}
check("throw-from-guard plant walk-in pose leftover live mid plant keeps hold", twiSaw, `saw=${twiSaw} hold=${V.player.walkFadeHold} tgf=${V.throwGuardPlantFade(V.player)} walkOut=${V.recoveryWalkOut(V.player)}`);
check("throw-from-guard plant walk-in pose leftover live AABB planted", twiAabbOk, `aabbOk=${twiAabbOk}`);
check("throw-from-guard plant walk-in pose leftover live destRect no sheet hop", twiMaxHop < 3.5, `hop=${twiMaxHop.toFixed(3)} dy0=${twiDy0.toFixed(2)}`);

// live: idle guard throw still idle rest (no A/D)
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
holdGuardSample();
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
V.keys.clear();
V.player.stamina = 100;
V.player.walkFadeHold = 0;
V.player.gait = 0;
const twiIdleOk = V.startThrow(V.player);
V.update(STEP);
check("throw-from-guard plant walk-in pose leftover live idle plant still windup", twiIdleOk && V.player.cut==="throw" && V.player.phase==="startup" && V.throwGuardPlantFade(V.player)>0.5 && !V.recoveryWalkOut(V.player) && V.player.walkFadeHold<0.02 && V.player.gait===0 && /windup/.test(sheetSrc(V.player)), `ok=${twiIdleOk} tgf=${V.throwGuardPlantFade(V.player)} walkOut=${V.recoveryWalkOut(V.player)} hold=${V.player.walkFadeHold} gait=${V.player.gait} src=${sheetSrc(V.player)}`);

// live: rival closing arms walkFadeHold on throw-from-guard
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.rival.x = 900; V.player.x = 500;
V.rival.guarding = true;
V.rival.guardPoseK = 1;
V.rival.stamina = 100;
V.rival.phase = "idle"; V.rival.phaseT = 0; V.rival.boltPhase = "";
V.rival.stunT = 0; V.rival.falling = false; V.rival.feintT = 0;
V.rival.closing = true;
V.rival.walkFadeHold = 0;
V.rival.gait = 0; V.rival.walkT = 0;
const twiRivalClosing = V.rival.closing === true;
const twiRivalOk = V.startThrow(V.rival);
const twiRival = twiRivalOk && V.rival.cut==="throw" && V.rival.phase==="startup" && V.rival.walkFadeHold>0.02 && V.throwGuardPlantFade(V.rival)>0.5;
check("throw-from-guard plant walk-in pose leftover live rival closing arms", twiRival && twiRivalClosing, `rival=${twiRival} closing0=${twiRivalClosing} ok=${twiRivalOk} tgf=${V.throwGuardPlantFade(V.rival)} hold=${V.rival.walkFadeHold} walkOut=${V.recoveryWalkOut(V.rival)} closing=${V.rival.closing}`);

check("throw-from-guard plant walk-in pose leftover draw shared", /restThrowGuardWalk/.test(sliceFn(codeOnly, "drawKnight")) && /restTechGuardWalk/.test(sliceFn(codeOnly, "drawKnight")) && /recoveryWalkOut\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /restThrowGuard \|\| restThrowGuardWalk/.test(sliceFn(codeOnly, "drawKnight")));
check("cache v=296 after throw-from-guard plant walk-in pose leftover live", /game.js\?v=350/.test(htmlTxt));




// --- v264 wakeup→reversal walk-in pose leftover: rest crumple raise on walk when A/D/closing through getup L ---
check("wakeup→reversal walk-in pose leftover seats no walk dump under getup reverse", /Wakeup→reversal walk-in pose leftover/.test(srcTxt) && /leftover walk used to dump the tick startWakeReversal/.test(srcTxt) && /armed from A\/D \/ rival closing mid-getup/.test(srcTxt) && /restWakeRev rested crumple on windup with no walk[\s\S]{0,10}base/.test(srcTxt) && /temporary guarding made recoveryWalkOut false/.test(srcTxt) && /reversalPlantFade was 0 without leftover guardPoseK/.test(srcTxt) && /hopped walk→raise under the fading crumple/.test(srcTxt) && /Same hole reversal[\s\S]{0,30}plant walk-in[\s\S]{0,40}already closed/.test(srcTxt));
check("wakeup→reversal walk-in pose leftover is draw-only", /Draw-only[\s\S]{0,40}drawKnight[\s\S]{0,20}startWakeReversal[\s\S]{0,20}walkFadeHold arm[\s\S]{0,20}tickGait hold/.test(srcTxt) && /restWakeRevWalk/.test(sliceFn(codeOnly, "drawKnight")) && /wakeRecWalk/.test(sliceFn(codeOnly, "startWakeReversal")) && /walkFadeHold = 1/.test(sliceFn(codeOnly, "startWakeReversal")) && /wakeupFade\(f\) <= 0\.02/.test(sliceFn(codeOnly, "tickGait")) && !/restWakeRevWalk/.test(sliceFn(codeOnly, "destRect")) && !/restWakeRevWalk/.test(sliceFn(codeOnly, "bodyAABB")) && !/restWakeRevWalk/.test(sliceFn(codeOnly, "slashPose")) && !/restWakeRevWalk/.test(sliceFn(codeOnly, "idleBreath")) && !/restWakeRevWalk/.test(sliceFn(codeOnly, "wakeupFade")) && !/restWakeRevWalk/.test(sliceFn(codeOnly, "poseBitmap")));
check("wakeup→reversal walk-in pose leftover rests crumple raise on walk when A/D", /restWakeRevWalk = wake > 0\.02 && !f\.guarding && f\.reversal && rf <= 0\.02 && \(f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)\)/.test(sliceFn(codeOnly, "drawKnight")) && /ready\(hurt\) && ready\(walk\)/.test(sliceFn(codeOnly, "drawKnight")) && /restWakeRev = wake > 0\.02 && !f\.guarding && f\.reversal && !restWakeRevWalk/.test(sliceFn(codeOnly, "drawKnight")) && /else if \(restWakeRevWalk\)/.test(sliceFn(codeOnly, "drawKnight")) && /octx\.drawImage\(walk, 0, 0, dw, dh\)/.test(sliceFn(codeOnly, "drawKnight")));
check("wakeup→reversal walk-in pose leftover eases windup under leftover crumple", /restWakeRevWalk/.test(sliceFn(codeOnly, "drawKnight")) && /Wakeup→reversal walk-in: base is walk/.test(srcTxt) && /restWake \|\| restWakeGuard \|\| restWakeGuardWalk \|\| restWakeRev \|\| restWakeWalk \|\| restWakeRevWalk/.test(sliceFn(codeOnly, "drawKnight")));
check("wakeup→reversal walk-in pose leftover idle getup reverse still rests on windup", /Idle getup reverse still rests on windup/.test(srcTxt) && /restWakeRev = wake > 0\.02 && !f\.guarding && f\.reversal && !restWakeRevWalk/.test(sliceFn(codeOnly, "drawKnight")) && /ready\(hurt\) && ready\(wind\)/.test(sliceFn(codeOnly, "drawKnight")));
check("wakeup→reversal walk-in pose leftover tickGait keeps walkFadeHold", /reversalPlanting\(f\) && \(f\.walkFadeHold > 0 \|\| f\.walkSettleT > 0\)/.test(sliceFn(codeOnly, "tickGait")) && /reversalPlantFade\(f\) <= 0\.02 && wakeupFade\(f\) <= 0\.02/.test(sliceFn(codeOnly, "tickGait")) && /keep hold through leftover[\s\S]{0,40}wakeupFade/.test(srcTxt));
check("wakeup→reversal walk-in pose leftover startWakeReversal arms hold", /wakeRecWalk/.test(sliceFn(codeOnly, "startWakeReversal")) && /recoveryWalkOut\(f\)/.test(sliceFn(codeOnly, "startWakeReversal")) && /walkFadeHold = 1/.test(sliceFn(codeOnly, "startWakeReversal")) && /temporary guard/.test(srcTxt));
check("wakeup→reversal walk-in pose leftover skips when restRevWalk owns", /rf <= 0\.02/.test(sliceFn(codeOnly, "drawKnight")) && /no leftover guard plant — restRevWalk owns that/.test(srcTxt));
check("wakeup→reversal walk-in pose leftover wake/guard/walk-out/rev walk-in unchanged", /Wakeup→guard \/ wakeup walk-out \/ guard reversal walk-in unchanged/.test(srcTxt) && /restWakeGuard/.test(sliceFn(codeOnly, "drawKnight")) && /restWakeWalk/.test(sliceFn(codeOnly, "drawKnight")) && /restRevWalk/.test(sliceFn(codeOnly, "drawKnight")));
check("wakeup→reversal walk-in pose leftover poseBitmap still windup", /poseBitmap still windup immediately/.test(srcTxt) && !/restWakeRevWalk/.test(sliceFn(codeOnly, "poseBitmap")));
check("wakeup→reversal walk-in pose leftover fade clocks unchanged", /WakeupFade \/ wakeRevFadeHold clocks unchanged/.test(srcTxt) && /This fade unchanged/.test(srcTxt) && /function wakeupFade/.test(srcTxt) && V.GUARD_RAISE_MS===80 && V.THROW_WAKE_INVULN===80);
check("wakeup→reversal walk-in pose leftover is not a new combat verb", V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80 && V.GUARD_RAISE_MS===80 && V.REVERSAL_STAM===30 && V.REVERSAL_INVULN===120 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280);
check("slash frames still 180/140/280 after wakeup→reversal walk-in pose leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after wakeup→reversal walk-in pose leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after wakeup→reversal walk-in pose leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after wakeup→reversal walk-in pose leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after wakeup→reversal walk-in pose leftover", /game.js\?v=350/.test(htmlTxt));

// live: A/D mid-getup L keeps walkFadeHold + restWakeRevWalk
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x = 400; V.rival.x = 1100;
V.player.guarding = false; V.player.guardPoseK = 0; V.player.stunT = 0;
V.player.phase = "idle"; V.player.phaseT = 0; V.player.boltPhase = "";
V.player.falling = false; V.player.feintT = 0; V.player.sheatheT = 0;
V.player.thrownT = 0; V.player.throwInvulnT = V.THROW_WAKE_INVULN; V.player.wakeRev = true;
V.player.hp = 80; V.player.stamina = 100; V.player.reversal = false; V.player.wakeRevFadeHold = 0;
V.player.gait = 0; V.player.walkT = 0; V.player.walkSettleT = 0; V.player.walkFadeHold = 0;
V.keys.add("KeyD");
let wrwReady=false;
for(let i=0;i<12;i++){
  freezeRivalAI();
  V.player.phase="idle"; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase="";
  V.player.throwInvulnT = V.THROW_WAKE_INVULN; V.player.wakeRev = true; V.player.thrownT = 0;
  V.keys.add("KeyD");
  V.update(STEP);
  if(V.player.gait!==0 && V.player.walkFadeHold>0.2){ wrwReady=true; break; }
}
check("wakeup→reversal walk-in pose leftover live A/D mid-getup armed", wrwReady && V.player.throwInvulnT>0 && V.wakeupFade(V.player)>0.5 && V.player.walkFadeHold>0.02 && V.recoveryWalkOut(V.player), `ready=${wrwReady} inv=${V.player.throwInvulnT} fade=${V.wakeupFade(V.player)} hold=${V.player.walkFadeHold} walkOut=${V.recoveryWalkOut(V.player)}`);
V.keys.clear();
V.keys.add("KeyD");
V.player.stamina = 100;
const wrwAabb0 = V.bodyAABB(V.player);
const wrwDy0 = V.destRect(V.player).dy;
const wrwHold0 = V.player.walkFadeHold;
const wrwFade0 = V.wakeupFade(V.player);
const wrwOk = V.startWakeReversal(V.player);
V.update(STEP);
const wrwFade1 = V.wakeupFade(V.player);
const wrwHold1 = V.player.walkFadeHold;
const wrwWalkOut = V.recoveryWalkOut(V.player);
const wrwRf = V.reversalPlantFade(V.player);
check("wakeup→reversal walk-in pose leftover live L arms with A/D", wrwOk && V.player.cut==="golpe" && V.player.phase==="startup" && V.player.reversal && wrwFade1>0.5 && wrwHold1>0.02 && wrwWalkOut && wrwRf<=0.02 && /windup/.test(sheetSrc(V.player)), `ok=${wrwOk} cut=${V.player.cut} ph=${V.player.phase} fade0=${wrwFade0} fade=${wrwFade1} hold0=${wrwHold0} hold=${wrwHold1} walkOut=${wrwWalkOut} rf=${wrwRf} src=${sheetSrc(V.player)}`);
check("wakeup→reversal walk-in pose leftover live poseBitmap still windup", /windup/.test(sheetSrc(V.player)) && !isHurtSheet(V.player) && !isBlockSheet(V.player) && !isCutSheet(V.player), `src=${sheetSrc(V.player)}`);
let wrwSaw=false, wrwAabbOk=true, wrwMaxHop=0;
for (let i=0;i<5;i++){
  freezeRivalAI();
  V.keys.add("KeyD");
  if (V.player.reversal && V.wakeupFade(V.player)>0.02 && (V.player.walkFadeHold>0.02 || V.recoveryWalkOut(V.player)) && V.reversalPlantFade(V.player)<=0.02) wrwSaw = true;
  const dy0 = V.destRect(V.player).dy;
  V.update(STEP);
  wrwMaxHop = Math.max(wrwMaxHop, Math.abs(V.destRect(V.player).dy - dy0));
  if (Math.abs(V.bodyAABB(V.player).y - wrwAabb0.y) > 0.05) wrwAabbOk = false;
}
check("wakeup→reversal walk-in pose leftover live mid plant keeps hold", wrwSaw, `saw=${wrwSaw} hold=${V.player.walkFadeHold} fade=${V.wakeupFade(V.player)} walkOut=${V.recoveryWalkOut(V.player)}`);
check("wakeup→reversal walk-in pose leftover live AABB planted", wrwAabbOk, `aabbOk=${wrwAabbOk}`);
check("wakeup→reversal walk-in pose leftover live destRect no sheet hop", wrwMaxHop < 3.5, `hop=${wrwMaxHop.toFixed(3)} dy0=${wrwDy0.toFixed(2)}`);

// live: idle getup reverse still windup rest (no A/D)
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.guarding=false; V.player.guardPoseK=0; V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0;
V.player.phase="idle"; V.player.phaseT=0; V.player.boltPhase=""; V.player.falling=false; V.player.stunT=0;
V.player.thrownT=0; V.player.throwInvulnT=V.THROW_WAKE_INVULN; V.player.wakeRev=true; V.player.hp=80;
V.player.stamina=100; V.player.reversal=false; V.player.wakeRevFadeHold=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
const wrwIdleOk = V.startWakeReversal(V.player);
V.update(STEP);
check("wakeup→reversal walk-in pose leftover live idle plant still windup", wrwIdleOk && V.player.reversal && V.player.phase==="startup" && V.wakeupFade(V.player)>0.5 && !V.recoveryWalkOut(V.player) && V.player.walkFadeHold<0.02 && V.player.gait===0 && /windup/.test(sheetSrc(V.player)), `ok=${wrwIdleOk} fade=${V.wakeupFade(V.player)} walkOut=${V.recoveryWalkOut(V.player)} hold=${V.player.walkFadeHold} gait=${V.player.gait} src=${sheetSrc(V.player)}`);

// live: rival closing arms walkFadeHold on wakeup reverse
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.rival.x = 900; V.player.x = 500;
V.rival.guarding = false; V.rival.guardPoseK = 0;
V.rival.stamina = 100; V.rival.hp = 80;
V.rival.phase = "idle"; V.rival.phaseT = 0; V.rival.boltPhase = "";
V.rival.stunT = 0; V.rival.falling = false; V.rival.feintT = 0;
V.rival.thrownT = 0; V.rival.throwInvulnT = V.THROW_WAKE_INVULN; V.rival.wakeRev = true;
V.rival.reversal = false; V.rival.wakeRevFadeHold = 0;
V.rival.closing = true;
V.rival.walkFadeHold = 0;
V.rival.gait = 0; V.rival.walkT = 0;
const wrwRivalClosing = V.rival.closing === true;
const wrwRivalOk = V.startWakeReversal(V.rival);
const wrwRival = wrwRivalOk && V.rival.reversal && V.rival.phase==="startup" && V.rival.walkFadeHold>0.02 && V.wakeupFade(V.rival)>0.5 && V.reversalPlantFade(V.rival)<=0.02;
check("wakeup→reversal walk-in pose leftover live rival closing arms", wrwRival && wrwRivalClosing, `rival=${wrwRival} closing0=${wrwRivalClosing} ok=${wrwRivalOk} fade=${V.wakeupFade(V.rival)} hold=${V.rival.walkFadeHold} walkOut=${V.recoveryWalkOut(V.rival)} closing=${V.rival.closing} rf=${V.reversalPlantFade(V.rival)}`);

check("wakeup→reversal walk-in pose leftover draw shared", /restWakeRevWalk/.test(sliceFn(codeOnly, "drawKnight")) && /recoveryWalkOut\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /restWake \|\| restWakeGuard \|\| restWakeGuardWalk \|\| restWakeRev \|\| restWakeWalk \|\| restWakeRevWalk/.test(sliceFn(codeOnly, "drawKnight")));
check("cache v=296 after wakeup→reversal walk-in pose leftover live", /game.js\?v=350/.test(htmlTxt));

// --- v265 wakeup→guard walk-in pose leftover: rest crumple raise on walk when A/D/closing through getup S ---
check("wakeup→guard walk-in pose leftover seats no walk dump under getup raise", /Wakeup→guard walk-in pose leftover/.test(srcTxt) && /leftover walk used to dump the tick S raised mid-getup/.test(srcTxt) && /from A\/D \/ rival closing/.test(srcTxt) && /restWakeGuard rested crumple on block with no walk/.test(srcTxt) && /guarding made recoveryWalkOut false/.test(srcTxt) && /hopped walk→block/.test(srcTxt) && /Same hole wakeup→reversal walk-in/.test(srcTxt));
check("wakeup→guard walk-in pose leftover is draw-only", /Draw-only[\s\S]{0,40}drawKnight[\s\S]{0,20}updateGuard[\s\S]{0,20}walkFadeHold arm[\s\S]{0,20}tickGait hold/.test(srcTxt) && /restWakeGuardWalk/.test(sliceFn(codeOnly, "drawKnight")) && /wakeGuardWalk/.test(sliceFn(codeOnly, "updateGuard")) && /walkFadeHold = 1/.test(sliceFn(codeOnly, "updateGuard")) && /wakeupFade\(f\) > 0\.02 && f\.walkFadeHold > 0/.test(sliceFn(codeOnly, "tickGait")) && !/restWakeGuardWalk/.test(sliceFn(codeOnly, "destRect")) && !/restWakeGuardWalk/.test(sliceFn(codeOnly, "bodyAABB")) && !/restWakeGuardWalk/.test(sliceFn(codeOnly, "slashPose")) && !/restWakeGuardWalk/.test(sliceFn(codeOnly, "idleBreath")) && !/restWakeGuardWalk/.test(sliceFn(codeOnly, "wakeupFade")) && !/restWakeGuardWalk/.test(sliceFn(codeOnly, "poseBitmap")));
check("wakeup→guard walk-in pose leftover rests crumple raise on walk when A/D", /restWakeGuardWalk = wake > 0\.02 && f\.guarding && f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "drawKnight")) && /ready\(hurt\) && ready\(walk\)/.test(sliceFn(codeOnly, "drawKnight")) && /restWakeGuard = wake > 0\.02 && f\.guarding && !restWakeGuardWalk/.test(sliceFn(codeOnly, "drawKnight")) && /else if \(restWakeGuardWalk\)/.test(sliceFn(codeOnly, "drawKnight")) && /octx\.drawImage\(walk, 0, 0, dw, dh\)/.test(sliceFn(codeOnly, "drawKnight")));
check("wakeup→guard walk-in pose leftover eases block under leftover crumple", /restWakeGuardWalk/.test(sliceFn(codeOnly, "drawKnight")) && /Wakeup→guard walk-in: base is walk/.test(srcTxt) && /block raise under leftover crumple/.test(srcTxt) && /restWake \|\| restWakeGuard \|\| restWakeGuardWalk \|\| restWakeRev \|\| restWakeWalk \|\| restWakeRevWalk/.test(sliceFn(codeOnly, "drawKnight")));
check("wakeup→guard walk-in pose leftover idle getup guard still rests on block", /Idle getup guard still rests on block/.test(srcTxt) && /restWakeGuard = wake > 0\.02 && f\.guarding && !restWakeGuardWalk/.test(sliceFn(codeOnly, "drawKnight")) && /ready\(hurt\) && ready\(block\)/.test(sliceFn(codeOnly, "drawKnight")));
check("wakeup→guard walk-in pose leftover tickGait keeps walkFadeHold", /f\.guarding/.test(sliceFn(codeOnly, "tickGait")) && /wakeupFade\(f\) > 0\.02 && f\.walkFadeHold > 0/.test(sliceFn(codeOnly, "tickGait")) && /keep leftover walkFadeHold through leftover crumple/.test(srcTxt));
check("wakeup→guard walk-in pose leftover updateGuard arms hold", /wakeGuardWalk/.test(sliceFn(codeOnly, "updateGuard")) && /recoveryWalkOut\(f\) \|\| f\.walkT > 0 \|\| f\.gait !== 0 \|\| f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "updateGuard")) && /walkFadeHold = 1/.test(sliceFn(codeOnly, "updateGuard")) && /throwInvuln mid-getup/.test(srcTxt) && /capture walk intent before guarding/.test(srcTxt));
check("wakeup→guard walk-in pose leftover skips walk→guard double draw", /restWalkGuard && !restWakeGuardWalk/.test(sliceFn(codeOnly, "drawKnight")));
check("wakeup→guard walk-in pose leftover wake/rev/walk-out unchanged", /Wakeup→reversal walk-in \/ wakeup walk-out \/ walk→guard unchanged/.test(srcTxt) && /restWakeRevWalk/.test(sliceFn(codeOnly, "drawKnight")) && /restWakeWalk/.test(sliceFn(codeOnly, "drawKnight")) && /restWalkGuard/.test(sliceFn(codeOnly, "drawKnight")));
check("wakeup→guard walk-in pose leftover poseBitmap still block", /poseBitmap still block once guarding/.test(srcTxt) && !/restWakeGuardWalk/.test(sliceFn(codeOnly, "poseBitmap")));
check("wakeup→guard walk-in pose leftover fade clocks unchanged", /WakeupFade clock unchanged/.test(srcTxt) && /This fade unchanged/.test(srcTxt) && /function wakeupFade/.test(srcTxt) && V.GUARD_RAISE_MS===80 && V.THROW_WAKE_INVULN===80);
check("wakeup→guard walk-in pose leftover is not a new combat verb", V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80 && V.GUARD_RAISE_MS===80 && V.GUARD_COMMIT_MS===140 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280);
check("slash frames still 180/140/280 after wakeup→guard walk-in pose leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after wakeup→guard walk-in pose leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after wakeup→guard walk-in pose leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after wakeup→guard walk-in pose leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after wakeup→guard walk-in pose leftover", /game.js\?v=350/.test(htmlTxt));

// live: A/D mid-getup S keeps walkFadeHold + restWakeGuardWalk
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x = 400; V.rival.x = 1100;
V.player.guarding = false; V.player.guardPoseK = 0; V.player.stunT = 0;
V.player.phase = "idle"; V.player.phaseT = 0; V.player.boltPhase = "";
V.player.falling = false; V.player.feintT = 0; V.player.sheatheT = 0;
V.player.thrownT = 0; V.player.throwInvulnT = V.THROW_WAKE_INVULN; V.player.wakeRev = true;
V.player.hp = 80; V.player.stamina = 100; V.player.reversal = false; V.player.wakeRevFadeHold = 0;
V.player.gait = 0; V.player.walkT = 0; V.player.walkSettleT = 0; V.player.walkFadeHold = 0;
V.keys.add("KeyD");
let wgwReady=false;
for(let i=0;i<12;i++){
  freezeRivalAI();
  V.player.phase="idle"; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase="";
  V.player.throwInvulnT = V.THROW_WAKE_INVULN; V.player.wakeRev = true; V.player.thrownT = 0;
  V.keys.add("KeyD");
  V.update(STEP);
  if(V.player.gait!==0 && V.player.walkFadeHold>0.2){ wgwReady=true; break; }
}
check("wakeup→guard walk-in pose leftover live A/D mid-getup armed", wgwReady && V.player.throwInvulnT>0 && V.wakeupFade(V.player)>0.5 && V.player.walkFadeHold>0.02 && V.recoveryWalkOut(V.player), `ready=${wgwReady} inv=${V.player.throwInvulnT} fade=${V.wakeupFade(V.player)} hold=${V.player.walkFadeHold} walkOut=${V.recoveryWalkOut(V.player)}`);
V.keys.clear();
V.keys.add("KeyD");
V.keys.add("KeyS");
V.player.stamina = 100;
const wgwAabb0 = V.bodyAABB(V.player);
const wgwDy0 = V.destRect(V.player).dy;
const wgwHold0 = V.player.walkFadeHold;
const wgwFade0 = V.wakeupFade(V.player);
V.update(STEP);
const wgwFade1 = V.wakeupFade(V.player);
const wgwHold1 = V.player.walkFadeHold;
const wgwGuard = V.player.guarding;
const wgwWalkOut = V.recoveryWalkOut(V.player);
check("wakeup→guard walk-in pose leftover live S arms with A/D", wgwGuard && V.player.phase==="idle" && wgwFade1>0.5 && wgwHold1>0.02 && !wgwWalkOut && isBlockSheet(V.player), `g=${wgwGuard} ph=${V.player.phase} fade0=${wgwFade0} fade=${wgwFade1} hold0=${wgwHold0} hold=${wgwHold1} walkOut=${wgwWalkOut} src=${sheetSrc(V.player)}`);
check("wakeup→guard walk-in pose leftover live poseBitmap still block", isBlockSheet(V.player) && !isHurtSheet(V.player) && !isCutSheet(V.player) && !/windup/.test(sheetSrc(V.player)), `src=${sheetSrc(V.player)}`);
let wgwSaw=false, wgwAabbOk=true, wgwMaxHop=0;
for (let i=0;i<5;i++){
  freezeRivalAI();
  V.keys.add("KeyD");
  V.keys.add("KeyS");
  if (V.player.guarding && V.wakeupFade(V.player)>0.02 && V.player.walkFadeHold>0.02) wgwSaw = true;
  const dy0 = V.destRect(V.player).dy;
  V.update(STEP);
  wgwMaxHop = Math.max(wgwMaxHop, Math.abs(V.destRect(V.player).dy - dy0));
  if (Math.abs(V.bodyAABB(V.player).y - wgwAabb0.y) > 0.05) wgwAabbOk = false;
}
check("wakeup→guard walk-in pose leftover live mid plant keeps hold", wgwSaw, `saw=${wgwSaw} hold=${V.player.walkFadeHold} fade=${V.wakeupFade(V.player)} g=${V.player.guarding}`);
check("wakeup→guard walk-in pose leftover live AABB planted", wgwAabbOk, `aabbOk=${wgwAabbOk}`);
check("wakeup→guard walk-in pose leftover live destRect no sheet hop", wgwMaxHop < 3.5, `hop=${wgwMaxHop.toFixed(3)} dy0=${wgwDy0.toFixed(2)}`);

// live: idle getup guard still block rest (no A/D)
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.guarding=false; V.player.guardPoseK=0; V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0;
V.player.phase="idle"; V.player.phaseT=0; V.player.boltPhase=""; V.player.falling=false; V.player.stunT=0;
V.player.thrownT=0; V.player.throwInvulnT=V.THROW_WAKE_INVULN; V.player.wakeRev=true; V.player.hp=80;
V.player.stamina=100; V.player.reversal=false; V.player.wakeRevFadeHold=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
V.keys.add("KeyS");
V.update(STEP);
check("wakeup→guard walk-in pose leftover live idle plant still block", V.player.guarding && V.player.phase==="idle" && V.wakeupFade(V.player)>0.5 && !V.recoveryWalkOut(V.player) && V.player.walkFadeHold<0.02 && V.player.gait===0 && isBlockSheet(V.player), `g=${V.player.guarding} fade=${V.wakeupFade(V.player)} walkOut=${V.recoveryWalkOut(V.player)} hold=${V.player.walkFadeHold} gait=${V.player.gait} src=${sheetSrc(V.player)}`);

// live: rival closing arms walkFadeHold on wakeup guard (mirror startWakeReversal arm)
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.rival.x = 900; V.player.x = 500;
V.rival.guarding = false; V.rival.guardPoseK = 0;
V.rival.stamina = 100; V.rival.hp = 80;
V.rival.phase = "idle"; V.rival.phaseT = 0; V.rival.boltPhase = "";
V.rival.stunT = 0; V.rival.falling = false; V.rival.feintT = 0;
V.rival.thrownT = 0; V.rival.throwInvulnT = V.THROW_WAKE_INVULN; V.rival.wakeRev = true;
V.rival.reversal = false; V.rival.wakeRevFadeHold = 0;
V.rival.closing = true;
V.rival.walkFadeHold = 0;
V.rival.gait = 1; V.rival.walkT = 80;
V.rival.cut = "slash"; V.rival.sheatheT = 0;
const wgwRivalClosing = V.rival.closing === true;
// Same arm updateGuard does on rising mid-getup with closing/gait intent.
const wgwWakeGuardWalk = V.rival.throwInvulnT > 0 && V.rival.thrownT <= 0 && V.rival.phase === "idle" && !V.rival.boltPhase && V.rival.hp > 0 && !V.rival.falling && (V.recoveryWalkOut(V.rival) || V.rival.walkT > 0 || V.rival.gait !== 0 || V.rival.walkFadeHold > 0.02);
V.rival.guarding = true;
V.rival.wantBlock = true;
if (wgwWakeGuardWalk && V.rival.walkFadeHold < 0.02) V.rival.walkFadeHold = 1;
const wgwRival = V.rival.guarding && V.rival.phase==="idle" && V.rival.walkFadeHold>0.02 && V.wakeupFade(V.rival)>0.5 && isBlockSheet(V.rival);
check("wakeup→guard walk-in pose leftover live rival closing arms", wgwRival && wgwRivalClosing && wgwWakeGuardWalk, `rival=${wgwRival} closing0=${wgwRivalClosing} ok=${wgwWakeGuardWalk} fade=${V.wakeupFade(V.rival)} hold=${V.rival.walkFadeHold} walkOut=${V.recoveryWalkOut(V.rival)} closing=${V.rival.closing} g=${V.rival.guarding} src=${sheetSrc(V.rival)}`);

check("wakeup→guard walk-in pose leftover draw shared", /restWakeGuardWalk/.test(sliceFn(codeOnly, "drawKnight")) && /walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "drawKnight")) && /restWake \|\| restWakeGuard \|\| restWakeGuardWalk \|\| restWakeRev \|\| restWakeWalk \|\| restWakeRevWalk/.test(sliceFn(codeOnly, "drawKnight")));
check("cache v=296 after wakeup→guard walk-in pose leftover live", /game.js\?v=350/.test(htmlTxt));



// --- v266 feint walk-in pose leftover: rest windup on walk when walkFadeHold/A/D through telegraph pull ---
check("feint walk-in pose leftover seats no walk dump under pull", /Feint walk-in pose leftover/.test(srcTxt) && /leftover walk used to dump to idle the tick startFeint/.test(srcTxt) && /from telegraph \/ A\/D \/ rival closing/.test(srcTxt) && /restFeintWalk only checked gaitWalkOn/.test(srcTxt) && /hopped walk→idle under the fading windup/.test(srcTxt) && /Same hole telegraph walk-in[\s\S]{0,30}already closed/.test(srcTxt));
check("feint walk-in pose leftover is draw-only", /Draw-only[\s\S]{0,40}drawKnight[\s\S]{0,20}startFeint walkFadeHold arm[\s\S]{0,20}tickGait hold/.test(srcTxt) && /restFeintWalk/.test(sliceFn(codeOnly, "drawKnight")) && /feintRecWalk/.test(sliceFn(codeOnly, "startFeint")) && /walkFadeHold = 1/.test(sliceFn(codeOnly, "startFeint")) && /f\.feintT > 0 && \(f\.walkFadeHold > 0 \|\| f\.walkSettleT > 0\)/.test(sliceFn(codeOnly, "tickGait")) && /feintFade\(f\) <= 0\.02/.test(sliceFn(codeOnly, "tickGait")) && !/restFeintWalk/.test(sliceFn(codeOnly, "destRect")) && !/restFeintWalk/.test(sliceFn(codeOnly, "bodyAABB")) && !/restFeintWalk/.test(sliceFn(codeOnly, "slashPose")) && !/restFeintWalk/.test(sliceFn(codeOnly, "idleBreath")) && !/restFeintWalk/.test(sliceFn(codeOnly, "feintFade")) && !/restFeintWalk/.test(sliceFn(codeOnly, "poseBitmap")));
check("feint walk-in pose leftover rests windup on walk when hold\/A\/D", /restFeintWalk = ff > 0\.02 && !restFeintGuard && \(gaitWalkOn\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)\)/.test(sliceFn(codeOnly, "drawKnight")) && /ready\(wind\) && ready\(walk\)/.test(sliceFn(codeOnly, "drawKnight")) && /restFeint = ff > 0\.02 && !restFeintGuard && !restFeintWalk/.test(sliceFn(codeOnly, "drawKnight")) && /else if \(restFeintWalk\)/.test(sliceFn(codeOnly, "drawKnight")) && /octx\.drawImage\(walk, 0, 0, dw, dh\)/.test(sliceFn(codeOnly, "drawKnight")));
check("feint walk-in pose leftover idle pull still rests on idle", /Idle pull still rests on idle/.test(srcTxt) && /restFeint = ff > 0\.02 && !restFeintGuard && !restFeintWalk/.test(sliceFn(codeOnly, "drawKnight")) && /ready\(wind\) && ready\(idle\)/.test(sliceFn(codeOnly, "drawKnight")));
check("feint walk-in pose leftover tickGait keeps walkFadeHold", /f\.feintT > 0 && \(f\.walkFadeHold > 0 \|\| f\.walkSettleT > 0\)/.test(sliceFn(codeOnly, "tickGait")) && /feintFade\(f\) <= 0\.02/.test(sliceFn(codeOnly, "tickGait")) && /Keep leftover walkFadeHold through leftover[\s\S]{0,20}feintFade/.test(srcTxt));
check("feint walk-in pose leftover startFeint arms hold", /feintRecWalk/.test(sliceFn(codeOnly, "startFeint")) && /recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| f\.walkT > 0 \|\| f\.gait !== 0/.test(sliceFn(codeOnly, "startFeint")) && /walkFadeHold = 1/.test(sliceFn(codeOnly, "startFeint")) && /capture walk intent before gait zeros/.test(srcTxt));
check("feint walk-in pose leftover walk-out\/guard unchanged", /Feint walk-out \/ feint→guard unchanged/.test(srcTxt) && /restFeintGuard/.test(sliceFn(codeOnly, "drawKnight")) && /gaitWalkOn\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /Keep leftover windup through leftover feintT if they raise/.test(srcTxt));
check("feint walk-in pose leftover poseBitmap still idle", /poseBitmap still idle once feintT is set/.test(srcTxt) && !/restFeintWalk/.test(sliceFn(codeOnly, "poseBitmap")));
check("feint walk-in pose leftover fade clocks unchanged", /FeintFade clock unchanged/.test(srcTxt) && /This fade unchanged/.test(srcTxt) && /function feintFade/.test(srcTxt) && V.FEINT_RECOVERY===100 && V.GUARD_RAISE_MS===80);
check("feint walk-in pose leftover is not a new combat verb", V.FEINT_RECOVERY===100 && V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140);
check("slash frames still 180/140/280 after feint walk-in pose leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after feint walk-in pose leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after feint walk-in pose leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after feint walk-in pose leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after feint walk-in pose leftover", /game.js\?v=350/.test(htmlTxt));

// live: A/D through telegraph→feint keeps walkFadeHold + restFeintWalk (no idle dump)
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x = 400; V.rival.x = 1100;
V.player.guarding = false; V.player.stunT = 0; V.player.phase = "idle";
V.player.boltPhase = ""; V.player.falling = false; V.player.guardPoseK = 0;
V.player.gait = 0; V.player.walkT = 0; V.player.walkSettleT = 0; V.player.walkFadeHold = 0;
V.player.feintT = 0; V.player.sheatheT = 0; V.player.stamina = 100; V.player.hp = 100;
V.keys.add("KeyD");
let fwiWalkReady=false;
for(let i=0;i<14;i++){
  freezeRivalAI();
  V.player.phase="idle"; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
  V.keys.add("KeyD");
  V.update(STEP);
  if(V.player.gait!==0 && V.player.walkFadeHold>0.2){ fwiWalkReady=true; break; }
}
check("feint walk-in pose leftover live A/D walk armed", fwiWalkReady && V.player.walkFadeHold>0.02 && V.recoveryWalkOut(V.player), `ready=${fwiWalkReady} hold=${V.player.walkFadeHold} walkOut=${V.recoveryWalkOut(V.player)} gait=${V.player.gait}`);
const fwiHold0 = V.player.walkFadeHold;
const fwiDy0 = V.destRect(V.player).dy;
const fwiAabb0 = V.bodyAABB(V.player).y;
V.keys.add("KeyD");
V.startAttack(V.player, "slash");
const fwiSlashOk = V.player.phase==="startup" && V.player.cut==="slash";
const fwiTeleHold = V.player.walkFadeHold;
const fwiTele = V.telegraphing(V.player);
const fwiOk = V.startFeint(V.player);
const fwiHold1 = V.player.walkFadeHold;
const fwiFf = V.feintFade(V.player);
const fwiWalkOut = V.recoveryWalkOut(V.player);
check("feint walk-in pose leftover live S arms with A/D", fwiSlashOk && fwiOk && V.player.feintT>0 && V.player.phase==="idle" && fwiTele && fwiHold1>0.02 && fwiWalkOut && fwiFf>0.5, `slash=${fwiSlashOk} ok=${fwiOk} feintT=${V.player.feintT} tele=${fwiTele} hold0=${fwiHold0} teleHold=${fwiTeleHold} hold=${fwiHold1} walkOut=${fwiWalkOut} ff=${fwiFf} src=${sheetSrc(V.player)}`);
check("feint walk-in pose leftover live hold kept through feint arm", fwiOk && fwiHold1>0.02 && V.player.feintT===V.FEINT_RECOVERY, `ok=${fwiOk} hold=${fwiHold1} feintT=${V.player.feintT}`);
check("feint walk-in pose leftover live poseBitmap still idle", !isCutSheet(V.player) && !/windup/.test(sheetSrc(V.player)) && !isBlockSheet(V.player), `src=${sheetSrc(V.player)}`);

let fwiSaw=false, fwiAabbOk=true, fwiMaxHop=0, fwiHoldLive=false;
for(let i=0;i<12;i++){
  freezeRivalAI();
  V.keys.add("KeyD");
  const ff = V.feintFade(V.player);
  const hold = V.player.walkFadeHold;
  const walkOn = V.gaitWalkOn(V.player);
  // restFeintWalk owns when ff>0.02 && (gaitWalkOn || walkFadeHold || recoveryWalkOut).
  // poseBitmap stays idle once feintT is set (drawKnight rests walk under windup).
  if (V.player.feintT > 0 && ff > 0.02 && (hold > 0.02 || walkOn || V.recoveryWalkOut(V.player))) {
    fwiSaw = true;
    if (hold > 0.02 || walkOn) fwiHoldLive = true;
  }
  const dy0 = V.destRect(V.player).dy;
  V.update(STEP);
  fwiMaxHop = Math.max(fwiMaxHop, Math.abs(V.destRect(V.player).dy - dy0));
  if (Math.abs(V.bodyAABB(V.player).y - fwiAabb0) > 0.05) fwiAabbOk = false;
  if (V.player.feintT <= 0 && i > 3) break;
}
check("feint walk-in pose leftover live mid plant keeps hold", fwiSaw && fwiHoldLive, `saw=${fwiSaw} holdLive=${fwiHoldLive} hold=${V.player.walkFadeHold} ff=${V.feintFade(V.player)} src=${sheetSrc(V.player)}`);
check("feint walk-in pose leftover live AABB planted", fwiAabbOk, `aabbOk=${fwiAabbOk}`);
check("feint walk-in pose leftover live destRect no sheet hop", fwiMaxHop < 3.5, `hop=${fwiMaxHop.toFixed(3)} dy0=${fwiDy0.toFixed(2)}`);

// live: idle feint still rests on idle (no walk)
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x = 500; V.rival.x = 1100;
V.player.phase="startup"; V.player.phaseT=40; V.player.cut="slash"; V.player.stunT=0; V.player.guarding=false;
V.player.boltPhase=""; V.player.falling=false; V.player.gait=0; V.player.walkT=0; V.player.walkFadeHold=0;
V.player.telegraph=true; V.player.feintT=0; V.player.sheatheT=0;
const fwiIdleOk = V.startFeint(V.player);
let fwiIdleRest=true, fwiIdleSaw=false;
for (let i=0;i<10;i++){
  freezeRivalAI();
  const ff = V.feintFade(V.player);
  if (V.player.feintT > 0 && ff > 0.02) {
    fwiIdleSaw = true;
    if (/walk/.test(sheetSrc(V.player)) || V.gaitWalkOn(V.player) || V.player.gait!==0 || V.player.walkFadeHold>0.02) fwiIdleRest = false;
  }
  V.update(STEP);
  if (V.player.feintT <= 0 && i > 3) break;
}
check("feint walk-in pose leftover live idle plant still idle", fwiIdleOk && fwiIdleSaw && fwiIdleRest && V.player.gait===0 && V.player.walkFadeHold<0.02, `ok=${fwiIdleOk} saw=${fwiIdleSaw} rest=${fwiIdleRest} gait=${V.player.gait} hold=${V.player.walkFadeHold}`);

// live: rival telegraph walkFadeHold through feint (closing cleared on startAttack; hold arms rest)
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.rival.x = 900; V.player.x = 500;
V.rival.guarding = false; V.rival.stunT = 0; V.rival.phase = "idle";
V.rival.boltPhase = ""; V.rival.falling = false; V.rival.feintT = 0;
V.rival.stamina = 100; V.rival.hp = 100;
V.rival.closing = true;
V.rival.gait = -1; V.rival.walkT = 100; V.rival.walkFadeHold = 0.9;
V.rival.telegraph = false;
const fwiRivalClosing0 = V.rival.closing === true;
V.startAttack(V.rival, "slash");
const fwiRivalSlash = V.rival.phase==="startup" && V.rival.cut==="slash";
const fwiRivalHoldTele = V.rival.walkFadeHold;
const fwiRivalTele = V.telegraphing(V.rival);
const fwiRivalOk = V.startFeint(V.rival);
const fwiRival = fwiRivalOk && V.rival.feintT>0 && V.rival.walkFadeHold>0.02 && V.feintFade(V.rival)>0.5;
check("feint walk-in pose leftover live rival closing arms", fwiRival && fwiRivalClosing0 && fwiRivalSlash && fwiRivalTele, `rival=${fwiRival} closing0=${fwiRivalClosing0} slash=${fwiRivalSlash} tele=${fwiRivalTele} holdTele=${fwiRivalHoldTele} hold=${V.rival.walkFadeHold} walkOut=${V.recoveryWalkOut(V.rival)} closing=${V.rival.closing} ff=${V.feintFade(V.rival)} src=${sheetSrc(V.rival)}`);

check("feint walk-in pose leftover draw shared", /restFeintWalk/.test(sliceFn(codeOnly, "drawKnight")) && /walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /gaitWalkOn\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /restFeint \|\| restFeintGuard \|\| restFeintWalk/.test(sliceFn(codeOnly, "drawKnight")));
check("cache v=296 after feint walk-in pose leftover live", /game.js\?v=350/.test(htmlTxt));

// --- v267 wakeup walk-out recoveryWalkOut pose leftover: rest crumple on walk when A/D early-step / rival closing ---
check("wakeup walk-out recoveryWalkOut pose leftover seats no idle dump under closing", /Wakeup walk-out recoveryWalkOut pose leftover/.test(srcTxt) && /leftover crumple used to rest on idle while/.test(srcTxt) && /recoveryWalkOut \(A\/D early-step \/ rival closing\)/.test(srcTxt) && /restWakeWalk only checked gaitWalkOn/.test(srcTxt) && /popped walk[\s\S]{0,40}gaitWalkOn flipped/.test(srcTxt) && /Same hole rival recovery[\s\S]{0,30}feint walk-in/.test(srcTxt));
check("wakeup walk-out recoveryWalkOut pose leftover is draw-only", /Draw-only \(drawKnight\)/.test(srcTxt) && /restWakeWalk/.test(sliceFn(codeOnly, "drawKnight")) && /gaitWalkOn\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && !/restWakeWalk/.test(sliceFn(codeOnly, "destRect")) && !/restWakeWalk/.test(sliceFn(codeOnly, "bodyAABB")) && !/restWakeWalk/.test(sliceFn(codeOnly, "slashPose")) && !/restWakeWalk/.test(sliceFn(codeOnly, "idleBreath")) && !/restWakeWalk/.test(sliceFn(codeOnly, "poseBitmap")));
check("wakeup walk-out recoveryWalkOut pose leftover rests crumple on walk when hold/A/D/closing", /restWakeWalk = wake > 0\.02 && !restWakeGuard && !restWakeGuardWalk && !restWakeRev && !restWakeRevWalk && \(gaitWalkOn\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)\)/.test(sliceFn(codeOnly, "drawKnight")) && /ready\(hurt\) && ready\(walk\)/.test(sliceFn(codeOnly, "drawKnight")) && /else if \(restWakeWalk\)/.test(sliceFn(codeOnly, "drawKnight")) && /octx\.drawImage\(walk, 0, 0, dw, dh\)/.test(sliceFn(codeOnly, "drawKnight")));
check("wakeup walk-out recoveryWalkOut pose leftover idle getup still rests on idle", /Idle getup still rests on idle/.test(srcTxt) && /restWake = wake > 0\.02 && !restWakeGuard && !restWakeGuardWalk && !restWakeRev && !restWakeRevWalk && !restWakeWalk/.test(sliceFn(codeOnly, "drawKnight")) && /ready\(hurt\) && ready\(idle\)/.test(sliceFn(codeOnly, "drawKnight")));
check("wakeup walk-out recoveryWalkOut pose leftover wake/guard/rev walk-in unchanged", /Wakeup→guard \/ wakeup→reversal walk-in unchanged/.test(srcTxt) && /restWakeGuardWalk/.test(sliceFn(codeOnly, "drawKnight")) && /restWakeRevWalk/.test(sliceFn(codeOnly, "drawKnight")) && /f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "drawKnight")));
check("wakeup walk-out recoveryWalkOut pose leftover poseBitmap still idle/walk", /poseBitmap still idle \/ walk/.test(srcTxt) && !/restWakeWalk/.test(sliceFn(codeOnly, "poseBitmap")));
check("wakeup walk-out recoveryWalkOut pose leftover fade clocks unchanged", /WakeupFade clock unchanged/.test(srcTxt) && /This fade unchanged/.test(srcTxt) && /function wakeupFade/.test(srcTxt) && V.THROW_WAKE_INVULN===80 && V.GUARD_RAISE_MS===80);
check("wakeup walk-out recoveryWalkOut pose leftover is not a new combat verb", V.THROW_WAKE_INVULN===80 && V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140);
check("slash frames still 180/140/280 after wakeup walk-out recoveryWalkOut pose leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after wakeup walk-out recoveryWalkOut pose leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after wakeup walk-out recoveryWalkOut pose leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after wakeup walk-out recoveryWalkOut pose leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after wakeup walk-out recoveryWalkOut pose leftover", /game.js\?v=350/.test(htmlTxt));

// live: A/D early-step mid-getup rests crumple on walk before gaitWalkOn (recoveryWalkOut)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false;
V.player.boltPhase=""; V.player.falling=false; V.player.feintT=0; V.player.sheatheT=0;
V.player.gait=0; V.player.walkT=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
V.player.clashRec=false; V.player.techRec=false; V.player.cutRecBreathT=0; V.player.guardPoseK=0;
V.player.hp=100; V.player.stamina=100; V.player.thrownT=0; V.player.wakeRev=true; V.player.wakeRevFadeHold=0;
V.player.reversal=false; V.player.cut="";
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=400; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
V.player.throwInvulnT = V.THROW_WAKE_INVULN;
V.keys.add("KeyD");
let wroSaw=false, wroEarly=false, wroAabbOk=true, wroMaxHop=0;
const wroAabb0 = V.bodyAABB(V.player).y;
const wroDy0 = V.destRect(V.player).dy;
for (let i=0;i<12;i++){
  holdRivalStill();
  V.keys.add("KeyD");
  const wf = V.wakeupFade(V.player);
  const walkOn = V.gaitWalkOn(V.player);
  const walkOut = V.recoveryWalkOut(V.player);
  // restWakeWalk owns when wake>0.02 && (gaitWalkOn || walkFadeHold || recoveryWalkOut)
  if (V.player.throwInvulnT > 0 && wf > 0.02 && walkOut) {
    wroSaw = true;
    if (!walkOn && V.player.walkFadeHold < 0.02) wroEarly = true;
  }
  const dy0 = V.destRect(V.player).dy;
  V.update(STEP);
  wroMaxHop = Math.max(wroMaxHop, Math.abs(V.destRect(V.player).dy - dy0));
  if (Math.abs(V.bodyAABB(V.player).y - wroAabb0) > 0.05) wroAabbOk = false;
  if (V.player.throwInvulnT <= 0 && i > 4) break;
}
check("wakeup walk-out recoveryWalkOut pose leftover live A/D early arms", wroSaw && V.recoveryWalkOut(V.player)!==undefined, `saw=${wroSaw} early=${wroEarly}`);
check("wakeup walk-out recoveryWalkOut pose leftover live early-step before gaitWalkOn", wroSaw && wroEarly, `saw=${wroSaw} early=${wroEarly}`);
check("wakeup walk-out recoveryWalkOut pose leftover live AABB planted", wroAabbOk, `aabbOk=${wroAabbOk}`);
check("wakeup walk-out recoveryWalkOut pose leftover live destRect no sheet hop", wroMaxHop < 3.5, `hop=${wroMaxHop.toFixed(3)} dy0=${wroDy0.toFixed(2)}`);

// live: idle getup still rests on idle (no A/D, no closing)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false;
V.player.boltPhase=""; V.player.falling=false; V.player.gait=0; V.player.walkFadeHold=0; V.player.feintT=0;
V.player.clashRec=false; V.player.techRec=false; V.player.hp=100;
V.player.thrownT=0; V.player.wakeRev=true; V.player.wakeRevFadeHold=0; V.player.reversal=false;
V.player.throwInvulnT = V.THROW_WAKE_INVULN;
V.player.x=500; V.rival.x=1100;
let wroIdleRest=true, wroIdleSaw=false;
for (let i=0;i<10;i++){
  holdRivalStill();
  const wf = V.wakeupFade(V.player);
  if (V.player.throwInvulnT > 0 && wf > 0.02) {
    wroIdleSaw = true;
    if (/walk/.test(sheetSrc(V.player)) || V.gaitWalkOn(V.player) || V.player.gait!==0 || V.player.walkFadeHold>0.02 || V.recoveryWalkOut(V.player)) wroIdleRest = false;
  }
  V.update(STEP);
  if (V.player.throwInvulnT <= 0 && i > 3) break;
}
check("wakeup walk-out recoveryWalkOut pose leftover live idle plant still idle", wroIdleSaw && wroIdleRest && V.player.gait===0 && V.player.walkFadeHold<0.02, `saw=${wroIdleSaw} rest=${wroIdleRest} gait=${V.player.gait} hold=${V.player.walkFadeHold}`);

// live: rival closing mid-getup arms restWakeWalk without gaitWalkOn
toClose();
freezeYouAI();
Math.random = () => 0.99;
V.openLeft = 0; V.hitstopLeft = 0;
V.rival.x = 900; V.player.x = 500;
V.rival.hp = 100; V.rival.stamina = 100;
V.rival.falling = false; V.rival.stunT = 0; V.rival.thrownT = 0;
V.rival.throwInvulnT = V.THROW_WAKE_INVULN;
V.rival.phase = "idle"; V.rival.phaseT = 0; V.rival.boltPhase = "";
V.rival.guarding = false; V.rival.reversal = false; V.rival.feintT = 0;
V.rival.wakeRev = true; V.rival.wakeRevFadeHold = 0;
V.rival.gait = 0; V.rival.walkT = 0; V.rival.walkFadeHold = 0; V.rival.walkSettleT = 0;
V.rival.wantBlock = false; V.rival.guardChoice = -1; V.rival.standGoal = 99999;
V.rival.boltCd = 99999; V.rival.throwCd = 99999; V.rival.feintCd = 99999; V.rival.revCd = 99999;
V.rival.closing = true;
const wroRivalClosing0 = V.rival.closing === true;
const wroRivalWake0 = V.wakeupFade(V.rival);
const wroRivalWalkOut0 = V.recoveryWalkOut(V.rival);
const wroRivalGaitOn0 = V.gaitWalkOn(V.rival);
let wroRivalSaw=false, wroRivalHole=false;
for (let i=0;i<10;i++){
  V.rival.closing = true;
  V.rival.wantBlock = false;
  V.rival.guardChoice = -1;
  V.rival.standGoal = 99999;
  V.rival.boltCd = 99999;
  V.rival.wakeRev = true;
  // keep gait 0 so recoveryWalkOut path owns (not gaitWalkOn)
  if (V.rival.gait !== 0) V.rival.gait = 0;
  const wf = V.wakeupFade(V.rival);
  const walkOut = V.recoveryWalkOut(V.rival);
  const walkOn = V.gaitWalkOn(V.rival);
  if (V.rival.throwInvulnT > 0 && wf > 0.02 && walkOut) {
    wroRivalSaw = true;
    if (!walkOn) wroRivalHole = true;
  }
  V.update(STEP);
  if (V.rival.throwInvulnT <= 0 && i > 3) break;
}
const wroRival = wroRivalSaw && wroRivalHole && wroRivalClosing0 && wroRivalWalkOut0 && !wroRivalGaitOn0 && wroRivalWake0 > 0.5;
check("wakeup walk-out recoveryWalkOut pose leftover live rival closing arms", wroRival, `rival=${wroRival} saw=${wroRivalSaw} hole=${wroRivalHole} closing0=${wroRivalClosing0} wake0=${wroRivalWake0} walkOut0=${wroRivalWalkOut0} gaitOn0=${wroRivalGaitOn0} src=${sheetSrc(V.rival)}`);

check("wakeup walk-out recoveryWalkOut pose leftover draw shared", /restWakeWalk/.test(sliceFn(codeOnly, "drawKnight")) && /gaitWalkOn\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /restWake \|\| restWakeGuard \|\| restWakeGuardWalk \|\| restWakeRev \|\| restWakeWalk \|\| restWakeRevWalk/.test(sliceFn(codeOnly, "drawKnight")));
check("cache v=296 after wakeup walk-out recoveryWalkOut pose leftover live", /game.js\?v=350/.test(htmlTxt));
// --- v268 guard drop walk-out recoveryWalkOut pose leftover: rest block on walk when A/D early-step / rival closing ---
check("guard drop walk-out recoveryWalkOut pose leftover seats no idle dump under closing", /Guard drop walk-out recoveryWalkOut pose leftover/.test(srcTxt) && /leftover block used to rest on idle while/.test(srcTxt) && /recoveryWalkOut \(A\/D early-step \/ rival closing\)/.test(srcTxt) && /restGuardWalk only checked gaitWalkOn/.test(srcTxt) && /popped walk[\s\S]{0,40}gaitWalkOn flipped/.test(srcTxt) && /Same hole wakeup walk-out[\s\S]{0,30}recoveryWalkOut/.test(srcTxt));
check("guard drop walk-out recoveryWalkOut pose leftover is draw-only", /Draw-only \(drawKnight\)/.test(srcTxt) && /restGuardWalk/.test(sliceFn(codeOnly, "drawKnight")) && /gaitWalkOn\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && !/restGuardWalk/.test(sliceFn(codeOnly, "destRect")) && !/restGuardWalk/.test(sliceFn(codeOnly, "bodyAABB")) && !/restGuardWalk/.test(sliceFn(codeOnly, "slashPose")) && !/restGuardWalk/.test(sliceFn(codeOnly, "idleBreath")) && !/restGuardWalk/.test(sliceFn(codeOnly, "poseBitmap")) && !/restGuardWalk/.test(sliceFn(codeOnly, "guardDropFade")));
check("guard drop walk-out recoveryWalkOut pose leftover rests block on walk when hold/A/D/closing", /restGuardWalk = gf > 0\.02 && !restFeint && !restFeintGuard && !restFeintWalk && \(gaitWalkOn\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)\)/.test(sliceFn(codeOnly, "drawKnight")) && /ready\(block\) && ready\(walk\)/.test(sliceFn(codeOnly, "drawKnight")) && /else if \(restGuardWalk\)/.test(sliceFn(codeOnly, "drawKnight")) && /octx\.drawImage\(walk, 0, 0, dw, dh\)/.test(sliceFn(codeOnly, "drawKnight")));
check("guard drop walk-out recoveryWalkOut pose leftover idle drop still rests on idle", /Idle drop still rests on idle/.test(srcTxt) && /restGuard = gf > 0\.02 && !restFeint && !restFeintGuard && !restFeintWalk && !restGuardWalk/.test(sliceFn(codeOnly, "drawKnight")) && /ready\(block\) && ready\(idle\)/.test(sliceFn(codeOnly, "drawKnight")));
check("guard drop walk-out recoveryWalkOut pose leftover mid-stride/feint unchanged", /Mid-stride gaitWalkOn path unchanged/.test(srcTxt) && /Feint-from-drop skip unchanged/.test(srcTxt) && /!restFeint && !restFeintGuard && !restFeintWalk/.test(sliceFn(codeOnly, "drawKnight")));
check("guard drop walk-out recoveryWalkOut pose leftover poseBitmap still idle/walk", /poseBitmap still idle \/ walk/.test(srcTxt) && !/restGuardWalk/.test(sliceFn(codeOnly, "poseBitmap")));
check("guard drop walk-out recoveryWalkOut pose leftover fade clocks unchanged", /GuardDropFade clock unchanged/.test(srcTxt) && /This fade unchanged/.test(srcTxt) && /function guardDropFade/.test(srcTxt) && V.GUARD_RAISE_MS===80 && V.WALK_SETTLE_MS===160);
check("guard drop walk-out recoveryWalkOut pose leftover is not a new combat verb", V.GUARD_RAISE_MS===80 && V.GUARD_COMMIT_MS===140 && V.STAMINA_BLOCK===20 && V.GUARD_BREAK_MS===400 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80 && V.WALK_STEP_MS===320 && V.WALK_SETTLE_MS===160 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.SHEATHE_MS===140);
check("slash frames still 180/140/280 after guard drop walk-out recoveryWalkOut pose leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after guard drop walk-out recoveryWalkOut pose leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after guard drop walk-out recoveryWalkOut pose leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after guard drop walk-out recoveryWalkOut pose leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after guard drop walk-out recoveryWalkOut pose leftover", /game.js\?v=350/.test(htmlTxt));

// live: A/D early-step mid-drop rests block on walk before gaitWalkOn (recoveryWalkOut)
bootPlay(); wait(1700);
holdGuardSample();
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
holdGuardSample();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.guarding = false;
V.player.stunT=0; V.player.falling=false; V.player.phase="idle"; V.player.boltPhase=""; V.player.guardBreakT=0;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.walkT=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
V.player.clashRec=false; V.player.techRec=false; V.player.cutRecBreathT=0;
if (V.player.guardPoseK < 0.85) V.player.guardPoseK = 0.92;
V.keys.add("KeyD");
let gdroSaw=false, gdroEarly=false, gdroAabbOk=true, gdroMaxHop=0;
const gdroAabb0 = V.bodyAABB(V.player).y;
const gdroDy0 = V.destRect(V.player).dy;
for (let i=0;i<12;i++){
  holdRivalStill();
  freezeYouAI(); freezeRivalAI();
  V.player.stunT=0; V.player.falling=false; V.player.phase="idle"; V.player.boltPhase=""; V.player.guardBreakT=0; V.hitstopLeft=0; V.player.feintT=0;
  V.keys.clear(); V.keys.add("KeyD");
  const gf = V.guardDropFade(V.player);
  const walkOn = V.gaitWalkOn(V.player);
  const walkOut = V.recoveryWalkOut(V.player);
  if (V.player.guardPoseK > 0 && gf > 0.02 && walkOut) {
    gdroSaw = true;
    if (!walkOn && V.player.walkFadeHold < 0.02) gdroEarly = true;
  }
  const dy0 = V.destRect(V.player).dy;
  V.update(STEP);
  gdroMaxHop = Math.max(gdroMaxHop, Math.abs(V.destRect(V.player).dy - dy0));
  if (Math.abs(V.bodyAABB(V.player).y - gdroAabb0) > 0.05) gdroAabbOk = false;
  if (V.player.guardPoseK <= 0 && i > 4) break;
}
check("guard drop walk-out recoveryWalkOut pose leftover live A/D early arms", gdroSaw && V.recoveryWalkOut(V.player)!==undefined, `saw=${gdroSaw} early=${gdroEarly}`);
check("guard drop walk-out recoveryWalkOut pose leftover live early-step before gaitWalkOn", gdroSaw && gdroEarly, `saw=${gdroSaw} early=${gdroEarly}`);
check("guard drop walk-out recoveryWalkOut pose leftover live AABB planted", gdroAabbOk, `aabbOk=${gdroAabbOk}`);
check("guard drop walk-out recoveryWalkOut pose leftover live destRect no sheet hop", gdroMaxHop < 3.5, `hop=${gdroMaxHop.toFixed(3)} dy0=${gdroDy0.toFixed(2)}`);

// live: idle drop still rests on idle (no A/D, no closing)
bootPlay(); wait(1700);
holdGuardSample();
for(let i=0;i<8;i++){ holdGuardSample(); V.update(STEP); }
holdGuardSample();
V.keys.clear();
V.player.guarding = false;
V.update(STEP);
freezeYouAI(); freezeRivalAI();
let gdroIdleRest=true, gdroIdleSaw=false;
for (let i=0;i<10;i++){
  freezeYouAI(); freezeRivalAI();
  V.player.stunT=0; V.player.falling=false; V.player.phase="idle"; V.player.boltPhase=""; V.player.guardBreakT=0; V.hitstopLeft=0; V.player.feintT=0;
  V.keys.clear();
  const gf = V.guardDropFade(V.player);
  if (V.player.guardPoseK > 0 && gf > 0.02) {
    gdroIdleSaw = true;
    if (/walk/.test(sheetSrc(V.player)) || V.gaitWalkOn(V.player) || V.player.gait!==0 || V.player.walkFadeHold>0.02 || V.recoveryWalkOut(V.player)) gdroIdleRest = false;
  }
  V.update(STEP);
  if (V.player.guardPoseK <= 0 && i > 3) break;
}
check("guard drop walk-out recoveryWalkOut pose leftover live idle plant still idle", gdroIdleSaw && gdroIdleRest && V.player.gait===0 && V.player.walkFadeHold<0.02, `saw=${gdroIdleSaw} rest=${gdroIdleRest} gait=${V.player.gait} hold=${V.player.walkFadeHold}`);

// live: rival closing mid-drop arms restGuardWalk without gaitWalkOn
bootPlay(); wait(1700);
freezeYouAI();
Math.random = () => 0.99;
V.openLeft = 0; V.hitstopLeft = 0;
V.rival.x = 900; V.player.x = 500;
V.rival.hp = 100; V.rival.stamina = 100;
V.rival.falling = false; V.rival.stunT = 0; V.rival.thrownT = 0;
V.rival.phase = "idle"; V.rival.phaseT = 0; V.rival.boltPhase = "";
V.rival.guarding = false; V.rival.reversal = false; V.rival.feintT = 0;
V.rival.gait = 0; V.rival.walkT = 0; V.rival.walkFadeHold = 0; V.rival.walkSettleT = 0;
V.rival.wantBlock = false; V.rival.guardChoice = -1; V.rival.standGoal = 99999;
V.rival.boltCd = 99999; V.rival.throwCd = 99999; V.rival.feintCd = 99999; V.rival.revCd = 99999;
V.rival.closing = true;
V.rival.guardPoseK = 0.92;
const gdroRivalClosing0 = V.rival.closing === true;
const gdroRivalGf0 = V.guardDropFade(V.rival);
const gdroRivalWalkOut0 = V.recoveryWalkOut(V.rival);
const gdroRivalGaitOn0 = V.gaitWalkOn(V.rival);
let gdroRivalSaw=false, gdroRivalHole=false;
for (let i=0;i<10;i++){
  V.rival.closing = true;
  V.rival.wantBlock = false;
  V.rival.guardChoice = -1;
  V.rival.standGoal = 99999;
  V.rival.boltCd = 99999;
  V.rival.guarding = false;
  if (V.rival.gait !== 0) V.rival.gait = 0;
  const gf = V.guardDropFade(V.rival);
  const walkOut = V.recoveryWalkOut(V.rival);
  const walkOn = V.gaitWalkOn(V.rival);
  if (V.rival.guardPoseK > 0 && gf > 0.02 && walkOut) {
    gdroRivalSaw = true;
    if (!walkOn) gdroRivalHole = true;
  }
  V.update(STEP);
  if (V.rival.guardPoseK <= 0 && i > 3) break;
}
const gdroRival = gdroRivalSaw && gdroRivalHole && gdroRivalClosing0 && gdroRivalWalkOut0 && !gdroRivalGaitOn0 && gdroRivalGf0 > 0.5;
check("guard drop walk-out recoveryWalkOut pose leftover live rival closing arms", gdroRival, `rival=${gdroRival} saw=${gdroRivalSaw} hole=${gdroRivalHole} closing0=${gdroRivalClosing0} gf0=${gdroRivalGf0} walkOut0=${gdroRivalWalkOut0} gaitOn0=${gdroRivalGaitOn0} src=${sheetSrc(V.rival)}`);

check("guard drop walk-out recoveryWalkOut pose leftover draw shared", /restGuardWalk/.test(sliceFn(codeOnly, "drawKnight")) && /gaitWalkOn\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /restGuard \|\| restGuardWalk/.test(sliceFn(codeOnly, "drawKnight")));
check("cache v=296 after guard drop walk-out recoveryWalkOut pose leftover live", /game.js\?v=350/.test(htmlTxt));




// --- v269 clash walk-in walkFadeHold pose leftover: rest cut raise on walk when walkFadeHold/recoveryWalkOut; arm hold on cancel; tickGait keep ---
check("clash walk-in walkFadeHold pose leftover seats no walk dump under closing clear", /Clash walk-in walkFadeHold pose leftover/.test(srcTxt) && /cancelInto\* cleared closing with no/.test(srcTxt) && /walkFadeHold arm/.test(srcTxt) && /tickGait zeroed walkFadeHold on phase/.test(srcTxt) && /restClashWalk only checked/.test(srcTxt) && /recoveryWalkOut/.test(srcTxt) && /hopped walk→raise under the fading slash/.test(srcTxt) && /Same hole telegraph recovery/.test(srcTxt));
check("clash walk-in walkFadeHold pose leftover is draw-only", /Draw-only \(drawKnight \+ cancelIntoGolpe\/Slash\/Bolt walkFadeHold arm \+ tickGait hold\)/.test(srcTxt) && /restClashWalk/.test(sliceFn(codeOnly, "drawKnight")) && /clashRecWalk/.test(sliceFn(codeOnly, "cancelIntoGolpe")) && /clashRecWalk/.test(sliceFn(codeOnly, "cancelIntoSlash")) && /clashRecWalk/.test(sliceFn(codeOnly, "cancelIntoBolt")) && /walkFadeHold = 1/.test(sliceFn(codeOnly, "cancelIntoGolpe")) && /clashPlanting\(f\) && \(f\.walkFadeHold > 0 \|\| f\.walkSettleT > 0\)/.test(sliceFn(codeOnly, "tickGait")) && !/restClashWalk/.test(sliceFn(codeOnly, "destRect")) && !/restClashWalk/.test(sliceFn(codeOnly, "bodyAABB")) && !/restClashWalk/.test(sliceFn(codeOnly, "slashPose")) && !/restClashWalk/.test(sliceFn(codeOnly, "idleBreath")) && !/restClashWalk/.test(sliceFn(codeOnly, "clashPlantFade")) && !/restClashWalk/.test(sliceFn(codeOnly, "poseBitmap")));
check("clash walk-in walkFadeHold pose leftover rests cut raise on walk when hold/A/D", /restClashWalk = cf > 0\.02/.test(sliceFn(codeOnly, "drawKnight")) && /f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)\) && ready\(cut\) && ready\(walk\)/.test(sliceFn(codeOnly, "drawKnight")) && /restClash = cf > 0\.02/.test(sliceFn(codeOnly, "drawKnight")) && /!restClashWalk && ready\(cut\) && ready\(boltPlant\)/.test(sliceFn(codeOnly, "drawKnight")) && /else if \(restClashWalk\)/.test(sliceFn(codeOnly, "drawKnight")) && /octx\.drawImage\(walk, 0, 0, dw, dh\)/.test(sliceFn(codeOnly, "drawKnight")));
check("clash walk-in walkFadeHold pose leftover idle plant still rests on windup", /Idle clash plant still rests on windup\/knife/.test(srcTxt) && /restClash = cf > 0\.02/.test(sliceFn(codeOnly, "drawKnight")) && /!restClashWalk && ready\(cut\) && ready\(boltPlant\)/.test(sliceFn(codeOnly, "drawKnight")));
check("clash walk-in walkFadeHold pose leftover tickGait keeps walkFadeHold", /Keep leftover walkFadeHold through leftover clash plant/.test(srcTxt) && /clashPlanting\(f\) && \(f\.walkFadeHold > 0 \|\| f\.walkSettleT > 0\)/.test(sliceFn(codeOnly, "tickGait")) && /clashPlantFade\(f\) <= 0\.02/.test(sliceFn(codeOnly, "tickGait")));
check("clash walk-in walkFadeHold pose leftover cancel arms hold", /clashRecWalk/.test(sliceFn(codeOnly, "cancelIntoGolpe")) && /clashRecWalk/.test(sliceFn(codeOnly, "cancelIntoSlash")) && /clashRecWalk/.test(sliceFn(codeOnly, "cancelIntoBolt")) && /if \(clashRecWalk && f\.walkFadeHold < 0\.02\) f\.walkFadeHold = 1/.test(sliceFn(codeOnly, "cancelIntoGolpe")) && /if \(clashRecWalk && f\.walkFadeHold < 0\.02\) f\.walkFadeHold = 1/.test(sliceFn(codeOnly, "cancelIntoSlash")) && /if \(clashRecWalk && f\.walkFadeHold < 0\.02\) f\.walkFadeHold = 1/.test(sliceFn(codeOnly, "cancelIntoBolt")));
check("clash walk-in walkFadeHold pose leftover holdCut\/link\/tele unchanged", /HoldCut \/ link \/ telegraph walk-in unchanged/.test(srcTxt) && /restHoldCutWalk/.test(sliceFn(codeOnly, "drawKnight")) && /f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)\) && ready\(cut\) && ready\(walk\)/.test(sliceFn(codeOnly, "drawKnight")) && /restLinkWalk/.test(sliceFn(codeOnly, "drawKnight")) && /telegraphing\(f\) && \(f\.walkFadeHold > 0 \|\| f\.walkSettleT > 0\)/.test(sliceFn(codeOnly, "tickGait")) && /linkPlanting\(f\) && \(f\.walkFadeHold > 0 \|\| f\.walkSettleT > 0\)/.test(sliceFn(codeOnly, "tickGait")));
check("clash walk-in walkFadeHold pose leftover poseBitmap still windup\/knife", /poseBitmap still windup\/knife immediately/.test(srcTxt) && /f\.phase === "startup" && ready\(wind\)/.test(sliceFn(codeOnly, "poseBitmap")) && /boltHoldCut = !fromClash/.test(sliceFn(codeOnly, "cancelIntoBolt")));
check("clash walk-in walkFadeHold pose leftover fade clocks unchanged", /ClashPlantFade clock unchanged/.test(srcTxt) && /This fade unchanged/.test(srcTxt) && /function clashPlantFade/.test(srcTxt) && V.GUARD_RAISE_MS===80 && V.CLASH_RECOVERY===200 && V.BOLT_CANCEL_MS===100 && V.SLASH_CANCEL_MS===100 && V.GOLPE_CANCEL_MS===100);
check("clash walk-in walkFadeHold pose leftover is not a new combat verb", V.CLASH_RECOVERY===200 && V.BOLT_CANCEL_MS===100 && V.SLASH_CANCEL_MS===100 && V.GOLPE_CANCEL_MS===100 && V.GUARD_RAISE_MS===80 && V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.HITSTUN===350 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280);
check("slash frames still 180/140/280 after clash walk-in walkFadeHold pose leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after clash walk-in walkFadeHold pose leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after clash walk-in walkFadeHold pose leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after clash walk-in walkFadeHold pose leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after clash walk-in walkFadeHold pose leftover", /game.js\?v=350/.test(htmlTxt));

// live: A/D through clash-K keeps walkFadeHold through plant (tickGait hold)
landClash();
V.player.stamRegenT = 99999;
const cwhGot = waitUntilBoltWindow("slash");
for(let i=0;i<20;i++){
  holdRival();
  V.player.stamRegenT = 99999;
  if (V.player.pushT<=0 && V.rival.pushT<=0) break;
  V.update(STEP);
}
V.keys.clear();
V.keys.add("KeyD");
const cwhAabb0 = V.bodyAABB(V.player);
const cwhDy0 = V.destRect(V.player).dy;
V.boltEdge = true;
V.update(STEP);
const cwhCf0 = V.clashPlantFade(V.player);
const cwhWalkOut = V.recoveryWalkOut(V.player);
const cwhHold0 = V.player.walkFadeHold;
check("clash walk-in walkFadeHold pose leftover live K arms with A/D", cwhGot && V.player.boltPhase==="startup" && V.player.clashPlant && !V.player.boltHoldCut && cwhCf0>0.8 && cwhWalkOut && cwhHold0>0.02 && V.sheatheFade(V.player)===0, `got=${cwhGot} bph=${V.player.boltPhase} plant=${V.player.clashPlant} hold=${V.player.boltHoldCut} cf=${cwhCf0} walkOut=${cwhWalkOut} wf=${cwhHold0} sf=${V.sheatheFade(V.player)}`);
check("clash walk-in walkFadeHold pose leftover live poseBitmap still throw_knife", /throw_knife/.test(sheetSrc(V.player)) && !isCutSheet(V.player), `src=${sheetSrc(V.player)}`);
let cwhSaw=false, cwhHoldLive=false, cwhAabbOk=true, cwhMaxHop=0;
for (let i=0;i<6;i++){
  freezeRivalAI();
  V.rival.x = 1220;
  V.rival.stunT = 0;
  V.rival.guarding = false;
  V.keys.add("KeyD");
  if (V.player.boltPhase==="startup" && V.clashPlantFade(V.player)>0.02 && (V.recoveryWalkOut(V.player) || V.player.walkFadeHold>0.02)) cwhSaw = true;
  if (V.player.walkFadeHold>0.02) cwhHoldLive = true;
  const dy0 = V.destRect(V.player).dy;
  V.update(STEP);
  cwhMaxHop = Math.max(cwhMaxHop, Math.abs(V.destRect(V.player).dy - dy0));
  if (Math.abs(V.bodyAABB(V.player).y - cwhAabb0.y) > 0.05) cwhAabbOk = false;
}
check("clash walk-in walkFadeHold pose leftover live mid plant keeps hold", cwhSaw && cwhHoldLive, `saw=${cwhSaw} holdLive=${cwhHoldLive} wf=${V.player.walkFadeHold} cf=${V.clashPlantFade(V.player)}`);
check("clash walk-in walkFadeHold pose leftover live AABB planted", cwhAabbOk, `aabbOk=${cwhAabbOk}`);
check("clash walk-in walkFadeHold pose leftover live destRect no sheet hop", cwhMaxHop < 3.5, `hop=${cwhMaxHop.toFixed(3)} dy0=${cwhDy0.toFixed(2)}`);

// live: idle clash-K still idle rest (no A/D, no hold)
landClash();
V.player.stamRegenT = 99999;
const cwhIdleGot = waitUntilBoltWindow("slash");
for(let i=0;i<20;i++){
  holdRival();
  V.player.stamRegenT = 99999;
  if (V.player.pushT<=0 && V.rival.pushT<=0) break;
  V.update(STEP);
}
V.keys.clear();
V.player.walkFadeHold = 0;
V.boltEdge = true;
V.update(STEP);
check("clash walk-in walkFadeHold pose leftover live idle plant still windup", cwhIdleGot && V.player.boltPhase==="startup" && V.player.clashPlant && V.clashPlantFade(V.player)>0.8 && !V.recoveryWalkOut(V.player) && V.player.walkFadeHold<0.02 && V.player.gait===0 && /throw_knife/.test(sheetSrc(V.player)), `got=${cwhIdleGot} plant=${V.player.clashPlant} cf=${V.clashPlantFade(V.player)} walkOut=${V.recoveryWalkOut(V.player)} wf=${V.player.walkFadeHold} gait=${V.player.gait} src=${sheetSrc(V.player)}`);

// live: rival clash-K — closing clears; walkFadeHold must hold the plant (the hole)
landClash();
V.rival.stamRegenT = 99999;
V.rival.stamina = 100;
V.rival.boltCd = 0;
V.rival.linkBolt = true;
V.rival.clashRec = true;
V.rival.closing = true;
V.rival.gait = 0;
V.rival.walkFadeHold = 0;
Math.random = () => 0.01;
let cwhRival=false, cwhRivalCf=0, cwhRivalHold=0, cwhRivalClosingAfter=true, cwhRivalWalkOutAfter=true;
for (let i=0;i<40;i++){
  V.rival.wantBlock = false;
  V.rival.guardChoice = -1;
  V.rival.standGoal = 99999;
  V.rival.stamRegenT = 99999;
  // Do NOT force closing each tick — cancel clears it; walkFadeHold must carry.
  if (i === 0) V.rival.closing = true;
  if (V.rival.boltPhase==="startup" && V.rival.clashPlant && V.clashPlantFade(V.rival)>0.02) {
    cwhRival = V.rival.walkFadeHold > 0.02;
    cwhRivalCf = V.clashPlantFade(V.rival);
    cwhRivalHold = V.rival.walkFadeHold;
    cwhRivalClosingAfter = !!V.rival.closing;
    cwhRivalWalkOutAfter = !!V.recoveryWalkOut(V.rival);
    break;
  }
  V.update(STEP);
  if (V.rival.phase==="idle" && !V.rival.boltPhase && i>8) break;
}
check("clash walk-in walkFadeHold pose leftover live rival closing arms hold", cwhRival && cwhRivalCf>0.5 && cwhRivalHold>0.02, `rival=${cwhRival} cf=${cwhRivalCf} hold=${cwhRivalHold} closing=${cwhRivalClosingAfter} walkOut=${cwhRivalWalkOutAfter} bph=${V.rival.boltPhase} plant=${V.rival.clashPlant}`);

check("clash walk-in walkFadeHold pose leftover draw shared", /restClashWalk/.test(sliceFn(codeOnly, "drawKnight")) && /f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /clashRecWalk/.test(sliceFn(codeOnly, "cancelIntoBolt")) && /clashPlanting\(f\) && \(f\.walkFadeHold > 0 \|\| f\.walkSettleT > 0\)/.test(sliceFn(codeOnly, "tickGait")) && /restClash \|\| restClashWalk/.test(sliceFn(codeOnly, "drawKnight")));
check("cache v=296 after clash walk-in walkFadeHold pose leftover live", /game.js\?v=350/.test(htmlTxt));





// --- v270 holdCut walk-in walkFadeHold pose leftover: rest cut raise on walk when walkFadeHold/recoveryWalkOut; arm hold on cancelIntoBolt; tickGait keep ---
check("holdCut walk-in walkFadeHold pose leftover seats no walk dump under closing clear", /HoldCut walk-in walkFadeHold pose leftover/.test(srcTxt) && /cancelIntoBolt cleared closing with no/.test(srcTxt) && /walkFadeHold arm on the holdCut branch/.test(srcTxt) && /tickGait zeroed walkFadeHold on boltPhase/.test(srcTxt) && /restHoldCutWalk only checked/.test(srcTxt) && /recoveryWalkOut/.test(srcTxt) && /hopped walk→idle under the fading sheathe/.test(srcTxt) && /Same hole clash walk-in walkFadeHold/.test(srcTxt));
check("holdCut walk-in walkFadeHold pose leftover is draw-only", /Draw-only \(drawKnight \+ cancelIntoBolt walkFadeHold arm \+ tickGait hold\)/.test(srcTxt) && /restHoldCutWalk/.test(sliceFn(codeOnly, "drawKnight")) && /clashRecWalk/.test(sliceFn(codeOnly, "cancelIntoBolt")) && /holdCutPlant = true/.test(sliceFn(codeOnly, "cancelIntoBolt")) && /if \(clashRecWalk && f\.walkFadeHold < 0\.02\) f\.walkFadeHold = 1/.test(sliceFn(codeOnly, "cancelIntoBolt")) && /holdCutPlanting\(f\) && \(f\.walkFadeHold > 0 \|\| f\.walkSettleT > 0\)/.test(sliceFn(codeOnly, "tickGait")) && !/restHoldCutWalk/.test(sliceFn(codeOnly, "destRect")) && !/restHoldCutWalk/.test(sliceFn(codeOnly, "bodyAABB")) && !/restHoldCutWalk/.test(sliceFn(codeOnly, "slashPose")) && !/restHoldCutWalk/.test(sliceFn(codeOnly, "idleBreath")) && !/restHoldCutWalk/.test(sliceFn(codeOnly, "holdCutFade")) && !/restHoldCutWalk/.test(sliceFn(codeOnly, "poseBitmap")));
check("holdCut walk-in walkFadeHold pose leftover rests cut raise on walk when hold/A/D", /restHoldCutWalk = hf > 0\.02/.test(sliceFn(codeOnly, "drawKnight")) && /f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)\) && ready\(cut\) && ready\(walk\)/.test(sliceFn(codeOnly, "drawKnight")) && /restHoldCut = hf > 0\.02/.test(sliceFn(codeOnly, "drawKnight")) && /!restHoldCutWalk && ready\(cut\) && ready\(idle\)/.test(sliceFn(codeOnly, "drawKnight")) && /else if \(restHoldCutWalk\)/.test(sliceFn(codeOnly, "drawKnight")) && /octx\.drawImage\(walk, 0, 0, dw, dh\)/.test(sliceFn(codeOnly, "drawKnight")));
check("holdCut walk-in walkFadeHold pose leftover idle plant still rests on idle", /Idle holdCut plant still rests on idle/.test(srcTxt) && /restHoldCut = hf > 0\.02/.test(sliceFn(codeOnly, "drawKnight")) && /!restHoldCutWalk && ready\(cut\) && ready\(idle\)/.test(sliceFn(codeOnly, "drawKnight")));
check("holdCut walk-in walkFadeHold pose leftover tickGait keeps walkFadeHold", /Keep leftover walkFadeHold through leftover holdCut plant/.test(srcTxt) && /holdCutPlanting\(f\) && \(f\.walkFadeHold > 0 \|\| f\.walkSettleT > 0\)/.test(sliceFn(codeOnly, "tickGait")) && /holdCutFade\(f\) <= 0\.02/.test(sliceFn(codeOnly, "tickGait")));
check("holdCut walk-in walkFadeHold pose leftover cancel arms hold", /clashRecWalk/.test(sliceFn(codeOnly, "cancelIntoBolt")) && /holdCutPlant = true/.test(sliceFn(codeOnly, "cancelIntoBolt")) && /if \(clashRecWalk && f\.walkFadeHold < 0\.02\) f\.walkFadeHold = 1/.test(sliceFn(codeOnly, "cancelIntoBolt")) && /Arm walkFadeHold for restClashWalk \/ restHoldCutWalk/.test(srcTxt));
check("holdCut walk-in walkFadeHold pose leftover clash\/link\/tele unchanged", /Clash \/ link \/ telegraph walk-in unchanged/.test(srcTxt) && /restClashWalk/.test(sliceFn(codeOnly, "drawKnight")) && /restLinkWalk/.test(sliceFn(codeOnly, "drawKnight")) && /clashPlanting\(f\) && \(f\.walkFadeHold > 0 \|\| f\.walkSettleT > 0\)/.test(sliceFn(codeOnly, "tickGait")) && /telegraphing\(f\) && \(f\.walkFadeHold > 0 \|\| f\.walkSettleT > 0\)/.test(sliceFn(codeOnly, "tickGait")) && /linkPlanting\(f\) && \(f\.walkFadeHold > 0 \|\| f\.walkSettleT > 0\)/.test(sliceFn(codeOnly, "tickGait")));
check("holdCut walk-in walkFadeHold pose leftover poseBitmap still slash", /poseBitmap still slash immediately/.test(srcTxt) && /holdingCutBolt\(f\) && ready\(slash\)/.test(sliceFn(codeOnly, "poseBitmap")) && /boltHoldCut = !fromClash/.test(sliceFn(codeOnly, "cancelIntoBolt")));
check("holdCut walk-in walkFadeHold pose leftover fade clocks unchanged", /HoldCutFade[\s\S]{0,40}clock unchanged/.test(srcTxt) && /This fade unchanged/.test(srcTxt) && /function holdCutFade/.test(srcTxt) && V.GUARD_RAISE_MS===80 && V.BOLT_CANCEL_MS===100 && V.SLASH_CANCEL_MS===100 && V.GOLPE_CANCEL_MS===100);
check("holdCut walk-in walkFadeHold pose leftover is not a new combat verb", V.BOLT_CANCEL_MS===100 && V.SLASH_CANCEL_MS===100 && V.GOLPE_CANCEL_MS===100 && V.GUARD_RAISE_MS===80 && V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.HITSTUN===350 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280);
check("slash frames still 180/140/280 after holdCut walk-in walkFadeHold pose leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after holdCut walk-in walkFadeHold pose leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after holdCut walk-in walkFadeHold pose leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after holdCut walk-in walkFadeHold pose leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after holdCut walk-in walkFadeHold pose leftover", /game.js\?v=350/.test(htmlTxt));

// live: A/D through special-cancel K keeps walkFadeHold through plant (tickGait hold)
landCloseSlash();
const hcwhGot = waitUntilBoltWindow("slash");
V.player.stamRegenT = 99999;
V.keys.clear();
V.keys.add("KeyD");
const hcwhSheathe = V.sheatheFade(V.player);
const hcwhAabb0 = V.bodyAABB(V.player);
const hcwhDy0 = V.destRect(V.player).dy;
V.boltEdge = true;
V.update(STEP);
const hcwhHf0 = V.holdCutFade(V.player);
const hcwhWalkOut = V.recoveryWalkOut(V.player);
const hcwhHold0 = V.player.walkFadeHold;
check("holdCut walk-in walkFadeHold pose leftover live K arms with A/D", hcwhGot && V.player.boltPhase==="startup" && V.player.boltHoldCut && V.player.holdCutPlant && hcwhHf0>0.8 && hcwhWalkOut && hcwhHold0>0.02 && hcwhSheathe>0.08 && V.sheatheFade(V.player)===0 && V.player.clashPlant===false, `got=${hcwhGot} bph=${V.player.boltPhase} hold=${V.player.boltHoldCut} plant=${V.player.holdCutPlant} hf=${hcwhHf0} walkOut=${hcwhWalkOut} wf=${hcwhHold0} sh0=${hcwhSheathe} sf=${V.sheatheFade(V.player)}`);
check("holdCut walk-in walkFadeHold pose leftover live poseBitmap still slash", isCutSheet(V.player) && !/windup/.test(sheetSrc(V.player)), `src=${sheetSrc(V.player)}`);
let hcwhSaw=false, hcwhHoldLive=false, hcwhAabbOk=true, hcwhMaxHop=0;
for (let i=0;i<6;i++){
  freezeRivalAI();
  V.rival.x = 1220;
  V.rival.stunT = 0;
  V.rival.guarding = false;
  V.keys.add("KeyD");
  if (V.player.boltPhase==="startup" && V.holdCutFade(V.player)>0.02 && (V.recoveryWalkOut(V.player) || V.player.walkFadeHold>0.02)) hcwhSaw = true;
  if (V.player.walkFadeHold>0.02) hcwhHoldLive = true;
  const dy0 = V.destRect(V.player).dy;
  V.update(STEP);
  hcwhMaxHop = Math.max(hcwhMaxHop, Math.abs(V.destRect(V.player).dy - dy0));
  if (Math.abs(V.bodyAABB(V.player).y - hcwhAabb0.y) > 0.05) hcwhAabbOk = false;
}
check("holdCut walk-in walkFadeHold pose leftover live mid plant keeps hold", hcwhSaw && hcwhHoldLive, `saw=${hcwhSaw} holdLive=${hcwhHoldLive} wf=${V.player.walkFadeHold} hf=${V.holdCutFade(V.player)}`);
check("holdCut walk-in walkFadeHold pose leftover live AABB planted", hcwhAabbOk, `aabbOk=${hcwhAabbOk}`);
check("holdCut walk-in walkFadeHold pose leftover live destRect no sheet hop", hcwhMaxHop < 3.5, `hop=${hcwhMaxHop.toFixed(3)} dy0=${hcwhDy0.toFixed(2)}`);

// live: idle special-cancel still idle rest (no A/D, no hold)
landCloseSlash();
const hcwhIdleGot = waitUntilBoltWindow("slash");
V.player.stamRegenT = 99999;
V.keys.clear();
V.player.walkFadeHold = 0;
V.boltEdge = true;
V.update(STEP);
check("holdCut walk-in walkFadeHold pose leftover live idle plant still idle", hcwhIdleGot && V.player.boltPhase==="startup" && V.player.holdCutPlant && V.holdCutFade(V.player)>0.8 && !V.recoveryWalkOut(V.player) && V.player.walkFadeHold<0.02 && V.player.gait===0, `got=${hcwhIdleGot} plant=${V.player.holdCutPlant} hf=${V.holdCutFade(V.player)} walkOut=${V.recoveryWalkOut(V.player)} wf=${V.player.walkFadeHold} gait=${V.player.gait}`);

// live: rival special-cancel K — closing clears; walkFadeHold must hold the plant (the hole)
landRivalCloseSlash(false);
armRivalSpecialCancel("slash");
V.rival.stamRegenT = 99999;
V.rival.stamina = 100;
V.rival.boltCd = 0;
V.rival.linkBolt = true;
V.rival.closing = true;
V.rival.gait = 0;
V.rival.walkFadeHold = 0;
Math.random = () => 0.01;
let hcwhRival=false, hcwhRivalHf=0, hcwhRivalHold=0, hcwhRivalClosingAfter=true, hcwhRivalWalkOutAfter=true;
for (let i=0;i<40;i++){
  V.rival.wantBlock = false;
  V.rival.guardChoice = -1;
  V.rival.standGoal = 99999;
  V.rival.stamRegenT = 99999;
  // Do NOT force closing each tick — cancel clears it; walkFadeHold must carry.
  if (i === 0) V.rival.closing = true;
  if (V.rival.boltPhase==="startup" && V.rival.boltHoldCut && V.rival.holdCutPlant && V.holdCutFade(V.rival)>0.02) {
    hcwhRival = V.rival.walkFadeHold > 0.02;
    hcwhRivalHf = V.holdCutFade(V.rival);
    hcwhRivalHold = V.rival.walkFadeHold;
    hcwhRivalClosingAfter = !!V.rival.closing;
    hcwhRivalWalkOutAfter = !!V.recoveryWalkOut(V.rival);
    break;
  }
  V.update(STEP);
  if (V.rival.phase==="idle" && !V.rival.boltPhase && i>8) break;
}
check("holdCut walk-in walkFadeHold pose leftover live rival closing arms hold", hcwhRival && hcwhRivalHf>0.5 && hcwhRivalHold>0.02, `rival=${hcwhRival} hf=${hcwhRivalHf} hold=${hcwhRivalHold} closing=${hcwhRivalClosingAfter} walkOut=${hcwhRivalWalkOutAfter} bph=${V.rival.boltPhase} plant=${V.rival.holdCutPlant}`);

check("holdCut walk-in walkFadeHold pose leftover draw shared", /restHoldCutWalk/.test(sliceFn(codeOnly, "drawKnight")) && /f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /clashRecWalk/.test(sliceFn(codeOnly, "cancelIntoBolt")) && /holdCutPlanting\(f\) && \(f\.walkFadeHold > 0 \|\| f\.walkSettleT > 0\)/.test(sliceFn(codeOnly, "tickGait")) && /restHoldCut \|\| restHoldCutWalk/.test(sliceFn(codeOnly, "drawKnight")));
check("cache v=296 after holdCut walk-in walkFadeHold pose leftover live", /game.js\?v=350/.test(htmlTxt));


// --- v271 sheathe walk-out recoveryWalkOut pose leftover: rest slash on walk when A/D early-step / rival closing ---
check("sheathe walk-out recoveryWalkOut pose leftover seats no idle dump under closing", /Sheathe walk-out recoveryWalkOut pose leftover/.test(srcTxt) && /leftover slash used to rest on idle while/.test(srcTxt) && /recoveryWalkOut \(A\/D early-step \/ rival closing\)/.test(srcTxt) && /restSheatheWalk only checked gaitWalkOn\|\|recoveryWalkResting/.test(srcTxt) && /popped walk[\s\S]{0,40}gaitWalkOn flipped/.test(srcTxt) && /Same hole wakeup walk-out recoveryWalkOut/.test(srcTxt));
check("sheathe walk-out recoveryWalkOut pose leftover is draw-only", /Draw-only \(drawKnight\)/.test(srcTxt) && /restSheatheWalk/.test(sliceFn(codeOnly, "drawKnight")) && /gaitWalkOn\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && !/restSheatheWalk/.test(sliceFn(codeOnly, "destRect")) && !/restSheatheWalk/.test(sliceFn(codeOnly, "bodyAABB")) && !/restSheatheWalk/.test(sliceFn(codeOnly, "slashPose")) && !/restSheatheWalk/.test(sliceFn(codeOnly, "idleBreath")) && !/restSheatheWalk/.test(sliceFn(codeOnly, "sheatheFade")) && !/restSheatheWalk/.test(sliceFn(codeOnly, "tickSheathe")) && !/restSheatheWalk/.test(sliceFn(codeOnly, "poseBitmap")) && !/restSheatheWalk/.test(sliceFn(codeOnly, "tickGait")));
check("sheathe walk-out recoveryWalkOut pose leftover rests slash on walk when hold/A/D/closing", /restSheatheWalk = fade > 0 && !f\.guarding && \(gaitWalkOn\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)\)/.test(sliceFn(codeOnly, "drawKnight")) && /ready\(cut\) && ready\(walk\)/.test(sliceFn(codeOnly, "drawKnight")) && /else if \(restSheatheWalk\)/.test(sliceFn(codeOnly, "drawKnight")) && /octx\.drawImage\(walk, 0, 0, dw, dh\)/.test(sliceFn(codeOnly, "drawKnight")));
check("sheathe walk-out recoveryWalkOut pose leftover idle sheathe still rests on idle", /Idle sheathe still rests on idle/.test(srcTxt) && /restSheathe = fade > 0 && !f\.guarding && !restSheatheWalk/.test(sliceFn(codeOnly, "drawKnight")) && /ready\(cut\) && ready\(idle\)/.test(sliceFn(codeOnly, "drawKnight")));
check("sheathe walk-out recoveryWalkOut pose leftover mid-stride/recovery A/D unchanged", /Mid-stride gaitWalkOn path unchanged/.test(srcTxt) && /Recovery A\/D path unchanged/.test(srcTxt) && /gaitWalkOn\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)/.test(sliceFn(codeOnly, "drawKnight")));
check("sheathe walk-out recoveryWalkOut pose leftover poseBitmap still idle/walk", /poseBitmap still idle \/ walk/.test(srcTxt) && !/restSheatheWalk/.test(sliceFn(codeOnly, "poseBitmap")));
check("sheathe walk-out recoveryWalkOut pose leftover fade clocks unchanged", /SheatheFade clock unchanged/.test(srcTxt) && /This fade unchanged/.test(srcTxt) && /function sheatheFade/.test(srcTxt) && V.SHEATHE_MS===140 && V.GUARD_RAISE_MS===80);
check("sheathe walk-out recoveryWalkOut pose leftover is not a new combat verb", V.SHEATHE_MS===140 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.GUARD_RAISE_MS===80 && V.HITSTUN===350 && V.WALK_STEP_MS===320 && V.WALK_SETTLE_MS===160 && V.FEINT_RECOVERY===100 && V.THROW_WAKE_INVULN===80);
check("slash frames still 180/140/280 after sheathe walk-out recoveryWalkOut pose leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after sheathe walk-out recoveryWalkOut pose leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after sheathe walk-out recoveryWalkOut pose leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after sheathe walk-out recoveryWalkOut pose leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after sheathe walk-out recoveryWalkOut pose leftover", /game.js\?v=350/.test(htmlTxt));

// live: A/D early-step mid-sheathe rests slash on walk before gaitWalkOn (recoveryWalkOut)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false;
V.player.boltPhase=""; V.player.falling=false; V.player.feintT=0; V.player.gait=0; V.player.walkT=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
V.player.clashRec=false; V.player.techRec=false; V.player.cutRecBreathT=0; V.player.guardPoseK=0;
V.player.cut="slash"; V.player.hp=100; V.player.stamina=100;
V.player.sheatheT = V.SHEATHE_MS;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=400; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
V.keys.add("KeyD");
let swo271Saw=false, swo271Early=false, swo271AabbOk=true, swo271MaxHop=0;
const swo271Aabb0 = V.bodyAABB(V.player).y;
const swo271Dy0 = V.destRect(V.player).dy;
for (let i=0;i<12;i++){
  holdRivalStill();
  V.keys.add("KeyD");
  const sf = V.sheatheFade(V.player);
  const walkOn = V.gaitWalkOn(V.player);
  const walkOut = V.recoveryWalkOut(V.player);
  // restSheatheWalk owns when fade>0 && (gaitWalkOn || walkFadeHold || recoveryWalkOut)
  if (V.player.sheatheT > 0 && sf > 0.02 && walkOut) {
    swo271Saw = true;
    if (!walkOn && V.player.walkFadeHold < 0.02) swo271Early = true;
  }
  const dy0 = V.destRect(V.player).dy;
  V.update(STEP);
  swo271MaxHop = Math.max(swo271MaxHop, Math.abs(V.destRect(V.player).dy - dy0));
  if (Math.abs(V.bodyAABB(V.player).y - swo271Aabb0) > 0.05) swo271AabbOk = false;
  if (V.player.sheatheT <= 0 && i > 4) break;
}
check("sheathe walk-out recoveryWalkOut pose leftover live A/D early arms", swo271Saw && V.recoveryWalkOut(V.player)!==undefined, `saw=${swo271Saw} early=${swo271Early}`);
check("sheathe walk-out recoveryWalkOut pose leftover live early-step before gaitWalkOn", swo271Saw && swo271Early, `saw=${swo271Saw} early=${swo271Early}`);
check("sheathe walk-out recoveryWalkOut pose leftover live AABB planted", swo271AabbOk, `aabbOk=${swo271AabbOk}`);
check("sheathe walk-out recoveryWalkOut pose leftover live destRect no sheet hop", swo271MaxHop < 3.5, `hop=${swo271MaxHop.toFixed(3)} dy0=${swo271Dy0.toFixed(2)}`);

// live: idle sheathe still rests on idle (no A/D, no closing)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false;
V.player.boltPhase=""; V.player.falling=false; V.player.gait=0; V.player.walkFadeHold=0; V.player.feintT=0;
V.player.clashRec=false; V.player.techRec=false; V.player.cut="slash"; V.player.hp=100;
V.player.sheatheT = V.SHEATHE_MS;
V.player.x=500; V.rival.x=1100;
let swo271IdleRest=true, swo271IdleSaw=false;
for (let i=0;i<10;i++){
  holdRivalStill();
  const sf = V.sheatheFade(V.player);
  if (V.player.sheatheT > 0 && sf > 0.02) {
    swo271IdleSaw = true;
    if (/walk/.test(sheetSrc(V.player)) || V.gaitWalkOn(V.player) || V.player.gait!==0 || V.player.walkFadeHold>0.02 || V.recoveryWalkOut(V.player)) swo271IdleRest = false;
  }
  V.update(STEP);
  if (V.player.sheatheT <= 0 && i > 3) break;
}
check("sheathe walk-out recoveryWalkOut pose leftover live idle plant still idle", swo271IdleSaw && swo271IdleRest && V.player.gait===0 && V.player.walkFadeHold<0.02, `saw=${swo271IdleSaw} rest=${swo271IdleRest} gait=${V.player.gait} hold=${V.player.walkFadeHold}`);

// live: rival closing mid-sheathe arms restSheatheWalk without gaitWalkOn
toClose();
freezeYouAI();
Math.random = () => 0.99;
V.openLeft = 0; V.hitstopLeft = 0;
V.rival.x = 900; V.player.x = 500;
V.rival.hp = 100; V.rival.stamina = 100;
V.rival.falling = false; V.rival.stunT = 0; V.rival.thrownT = 0;
V.rival.phase = "idle"; V.rival.phaseT = 0; V.rival.boltPhase = "";
V.rival.guarding = false; V.rival.reversal = false; V.rival.feintT = 0;
V.rival.gait = 0; V.rival.walkT = 0; V.rival.walkFadeHold = 0; V.rival.walkSettleT = 0;
V.rival.wantBlock = false; V.rival.guardChoice = -1; V.rival.standGoal = 99999;
V.rival.boltCd = 99999; V.rival.throwCd = 99999; V.rival.feintCd = 99999; V.rival.revCd = 99999;
V.rival.closing = true;
V.rival.cut = "slash";
V.rival.sheatheT = V.SHEATHE_MS;
V.rival.clashRec = false; V.rival.techRec = false; V.rival.guardPoseK = 0;
const swo271RivalClosing0 = V.rival.closing === true;
const swo271RivalSf0 = V.sheatheFade(V.rival);
const swo271RivalWalkOut0 = V.recoveryWalkOut(V.rival);
const swo271RivalGaitOn0 = V.gaitWalkOn(V.rival);
let swo271RivalSaw=false, swo271RivalHole=false;
for (let i=0;i<10;i++){
  V.rival.closing = true;
  V.rival.wantBlock = false;
  V.rival.guardChoice = -1;
  V.rival.standGoal = 99999;
  V.rival.boltCd = 99999;
  V.rival.guarding = false;
  if (V.rival.gait !== 0) V.rival.gait = 0;
  const sf = V.sheatheFade(V.rival);
  const walkOut = V.recoveryWalkOut(V.rival);
  const walkOn = V.gaitWalkOn(V.rival);
  if (V.rival.sheatheT > 0 && sf > 0.02 && walkOut) {
    swo271RivalSaw = true;
    if (!walkOn) swo271RivalHole = true;
  }
  V.update(STEP);
  if (V.rival.sheatheT <= 0 && i > 3) break;
}
const swo271Rival = swo271RivalSaw && swo271RivalHole && swo271RivalClosing0 && swo271RivalWalkOut0 && !swo271RivalGaitOn0 && swo271RivalSf0 > 0.5;
check("sheathe walk-out recoveryWalkOut pose leftover live rival closing arms", swo271Rival, `rival=${swo271Rival} saw=${swo271RivalSaw} hole=${swo271RivalHole} closing0=${swo271RivalClosing0} sf0=${swo271RivalSf0} walkOut0=${swo271RivalWalkOut0} gaitOn0=${swo271RivalGaitOn0} src=${sheetSrc(V.rival)}`);

check("sheathe walk-out recoveryWalkOut pose leftover draw shared", /restSheatheWalk/.test(sliceFn(codeOnly, "drawKnight")) && /gaitWalkOn\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /else if \(restSheatheWalk\)/.test(sliceFn(codeOnly, "drawKnight")) && /fade > 0\.02 && ready\(cut\)/.test(sliceFn(codeOnly, "drawKnight")));
check("cache v=296 after sheathe walk-out recoveryWalkOut pose leftover live", /game.js\?v=350/.test(htmlTxt));

// --- v272 bolt recovery settle→walk destRect leftover: keep 0 through walk-out knife plant; arm cutRec mid-end ---
check("bolt recovery settle→walk destRect leftover seats no breath mid-stride", /Bolt recovery settle→walk destRect leftover/.test(srcTxt) && /leftover boltPlantFade breath used to/.test(srcTxt) && /seat ~1\.1 mid-stride/.test(srcTxt) && /plant ease climbed to full amp/.test(srcTxt) && /Same[\s\S]{0,80}hole wakeup settle→walk/.test(srcTxt) && /Keep breath 0 through walk-out bolt recovery/.test(srcTxt) && /arm cutRecBreathT when[\s\S]{0,40}bolt recovery ends/.test(srcTxt));
check("bolt recovery settle→walk destRect leftover is destRect-only", /destRect-only/.test(srcTxt) && /idleBreath \/ cutRecBreathT/.test(srcTxt) && /Keep breath 0 through walk-out bolt recovery/.test(srcTxt) && /walkOutEnd/.test(sliceFn(codeOnly, "advanceBoltFighter")) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "advanceBoltFighter")) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| f\.guarding \|\| f\.guardPoseK > 0/.test(sliceFn(codeOnly, "advanceBoltFighter")) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\) return 0/.test(sliceFn(codeOnly, "idleBreath")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "destRect")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "bodyAABB")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "poseBitmap")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "boltPlantFade")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "drawKnight")));
check("bolt recovery settle→walk destRect leftover keeps 0 through walk-out knife plant", /if \(f\.boltPhase === "recovery" && !holdingCutBolt\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\) return 0/.test(sliceFn(codeOnly, "idleBreath")) && /Keep breath 0 through walk-out bolt recovery/.test(srcTxt));
check("bolt recovery settle→walk destRect leftover arms cutRec on walk mid-end", /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "advanceBoltFighter")) && /walk \/ raise mid-end used to dump/.test(srcTxt) && /let cut recovery settle→walk max\(ck, wk\)/.test(srcTxt) && /else if \(walkOutEnd\) f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "advanceBoltFighter")) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| f\.guarding \|\| f\.guardPoseK > 0/.test(sliceFn(codeOnly, "advanceBoltFighter")));
check("bolt recovery settle→guard destRect leftover keep-0 through raise mid-plant", /Bolt recovery settle→guard/.test(srcTxt) && /if \(f\.guarding \|\| f\.guardPoseK > 0\) return 0;/.test(sliceFn(codeOnly, "idleBreath")) && /same arm on guard mid-end/.test(srcTxt));
check("bolt recovery settle→walk destRect leftover idle knife plant still eases", /Idle knife plant still eases with boltPlantFade/.test(srcTxt) && /return amp \* \(1 - boltPlantFade\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /knife plant end unchanged/.test(srcTxt) && /no cutRec arm/.test(srcTxt));
check("bolt recovery settle→walk destRect leftover special-cancel\/sheathe unchanged", /Special-cancel still zeros \(holdingCutBolt\)/.test(srcTxt) && /sheathe path unchanged/.test(srcTxt) && /if \(fromCut\) f\.sheatheT = SHEATHE_MS/.test(sliceFn(codeOnly, "advanceBoltFighter")) && /holdingCutBolt\(f\)\) return 0/.test(sliceFn(codeOnly, "idleBreath")));
check("bolt recovery settle→walk destRect leftover sheet \/ restBoltWalk unchanged", /Sheet still boltPlantFade \/ restBoltWalk/.test(srcTxt) && /restBoltWalk = bf > 0\.02 && recoveryWalkResting\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /function boltPlantFade/.test(srcTxt) && /if \(f\.boltPhase !== "recovery"\) return 0;/.test(sliceFn(codeOnly, "boltPlantFade")));
check("bolt recovery settle→walk destRect leftover throw plant end unchanged", /f\.cut === "throw" && f\.phase === "recovery"/.test(sliceFn(codeOnly, "idleBreath")) && /return amp \* \(1 - throwPlantFade\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /Throw plant end destRect leftover/.test(srcTxt));
check("bolt recovery settle→walk destRect leftover cutRec\/wakeup\/feint paths unchanged", /Math\.max\(ck, wk\)/.test(sliceFn(codeOnly, "idleBreath")) && /Math\.max\(ck, rk\)/.test(sliceFn(codeOnly, "idleBreath")) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "idleBreath")) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickThrowState")) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickSheathe")));
check("bolt recovery settle→walk destRect leftover tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("bolt recovery settle→walk destRect leftover is not a new combat verb", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140 && V.HITSTUN===350 && V.WALK_SETTLE_MS===160);
check("slash frames still 180/140/280 after bolt recovery settle→walk destRect leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after bolt recovery settle→walk destRect leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after bolt recovery settle→walk destRect leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after bolt recovery settle→walk destRect leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after bolt recovery settle→walk destRect leftover", /game.js\?v=350/.test(htmlTxt));

// live: A/D through bolt recovery keeps suppress (no mid-stride seat), arms cutRec on end
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.hp=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cut="slash"; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.walkSettleT=0; V.player.walkRiseT=0;
V.player.boltHoldCut=false; V.player.boltSuper=false; V.player.walkFadeHold=0;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=200; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
let brsw272AmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ brsw272AmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.boltPhase="recovery";
V.player.boltT = 0;
V.player.boltHoldCut=false; V.player.boltSuper=false;
V.player.cutRecBreathT=0; V.player.gait=0; V.player.walkRiseT=0; V.player.guarding=false; V.player.guardPoseK=0;
V.keys.add("KeyD");
const brsw272Bf0=V.boltPlantFade(V.player);
const brsw272Br0=V.idleBreath(V.player);
const brsw272Rwo0=V.recoveryWalkOut(V.player);
const brsw272Aabb0=V.bodyAABB(V.player).y;
let brsw272MaxSeat=0, brsw272Armed=false, brsw272Cleared=false, brsw272EndBr=0, brsw272AabbOk=true, brsw272DyHop=0, brsw272SawRec=false;
for(let i=0;i<40;i++){
  holdRivalStill();
  V.keys.add("KeyD");
  const dy0=V.destRect(V.player).dy;
  const bph0=V.player.boltPhase;
  V.update(STEP);
  const br=V.idleBreath(V.player);
  if(Math.abs(V.bodyAABB(V.player).y-brsw272Aabb0)>0.05) brsw272AabbOk=false;
  brsw272DyHop=Math.max(brsw272DyHop, Math.abs(V.destRect(V.player).dy-dy0));
  if(bph0==="recovery") {
    brsw272SawRec=true;
    brsw272MaxSeat=Math.max(brsw272MaxSeat, Math.abs(br));
  }
  if(V.player.gait===1) brsw272MaxSeat=Math.max(brsw272MaxSeat, Math.abs(br));
  if(bph0==="recovery" && !V.player.boltPhase){
    brsw272Armed = V.player.cutRecBreathT>0;
    brsw272Cleared=true;
    brsw272EndBr=br;
  }
  if(brsw272Cleared && V.player.cutRecBreathT===0 && V.player.gait===1 && Math.abs(br)<0.05){ brsw272EndBr=br; break; }
}
check("bolt recovery settle→walk destRect leftover live amp ready", brsw272AmpReady, `ready=${brsw272AmpReady} modeT=${V.modeT}`);
check("bolt recovery settle→walk destRect leftover live walk-out ready", brsw272Bf0>0.9 && Math.abs(brsw272Br0)<0.05 && brsw272Rwo0, `bf0=${brsw272Bf0.toFixed(3)} br0=${brsw272Br0.toFixed(3)} rwo=${brsw272Rwo0}`);
check("bolt recovery settle→walk destRect leftover live arms cutRec on end", brsw272Armed && brsw272SawRec, `armed=${brsw272Armed} saw=${brsw272SawRec} cbt=${V.player.cutRecBreathT}`);
check("bolt recovery settle→walk destRect leftover live no mid-stride seat", brsw272MaxSeat<0.35, `maxSeat=${brsw272MaxSeat.toFixed(3)}`);
check("bolt recovery settle→walk destRect leftover live rise clears cutRec", brsw272Cleared && Math.abs(brsw272EndBr)<0.05 && !V.player.boltPhase, `cleared=${brsw272Cleared} endBr=${brsw272EndBr.toFixed(3)} bph=${V.player.boltPhase} cbt=${V.player.cutRecBreathT}`);
check("bolt recovery settle→walk destRect leftover live no breath dump hop", brsw272MaxSeat<0.35 && brsw272DyHop<6.5, `maxSeat=${brsw272MaxSeat.toFixed(3)} hop=${brsw272DyHop.toFixed(3)}`);
check("bolt recovery settle→walk destRect leftover live AABB planted", brsw272AabbOk, `aabbOk=${brsw272AabbOk}`);

// live: idle knife plant still eases breath in (unchanged)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.gait=0; V.player.hp=100; V.player.stamina=100;
V.player.boltHoldCut=false; V.player.boltSuper=false;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=500; V.rival.x=1100;
let brsw272IdleAmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ brsw272IdleAmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.boltPhase="recovery";
V.player.boltT = 0;
V.player.boltHoldCut=false; V.player.cutRecBreathT=0; V.player.gait=0;
let brsw272IdleBr0=V.idleBreath(V.player), brsw272IdleBr1=brsw272IdleBr0, brsw272IdleUp=false, brsw272IdleArmed=false;
for(let i=0;i<24;i++){
  holdRivalStill(); V.update(STEP);
  if(!V.player.boltPhase && V.player.cutRecBreathT>0) brsw272IdleArmed=true;
  const br=V.idleBreath(V.player);
  if(V.player.boltPhase==="recovery" && Math.abs(br)>Math.abs(brsw272IdleBr1)+0.05) brsw272IdleUp=true;
  if(!V.player.boltPhase && V.player.cutRecBreathT===0 && Math.abs(br)>Math.abs(brsw272IdleBr1)+0.05) brsw272IdleUp=true;
  brsw272IdleBr1=br;
  if(!V.player.boltPhase && i>8) break;
}
check("bolt recovery settle→walk destRect leftover live idle knife plant still eases", brsw272IdleAmpReady && !brsw272IdleArmed && brsw272IdleUp && Math.abs(brsw272IdleBr0)<0.05 && Math.abs(brsw272IdleBr1)>0.4 && V.player.gait===0, `ready=${brsw272IdleAmpReady} armed=${brsw272IdleArmed} up=${brsw272IdleUp} br0=${brsw272IdleBr0.toFixed(3)} br1=${brsw272IdleBr1.toFixed(3)} cbt=${V.player.cutRecBreathT}`);

// live: rival closing through bolt recovery keeps suppress + arms cutRec
toClose();
freezeYouAI();
Math.random = () => 0.99;
V.openLeft = 0; V.hitstopLeft = 0;
V.rival.x = 900; V.player.x = 400;
V.rival.hp = 100; V.rival.stamina = 100;
V.rival.falling = false; V.rival.stunT = 0; V.rival.thrownT = 0;
V.rival.phase = "idle"; V.rival.phaseT = 0; V.rival.boltPhase = "";
V.rival.guarding = false; V.rival.reversal = false; V.rival.feintT = 0;
V.rival.gait = 0; V.rival.walkT = 0; V.rival.walkFadeHold = 0; V.rival.walkSettleT = 0;
V.rival.wantBlock = false; V.rival.guardChoice = -1; V.rival.standGoal = 99999;
V.rival.boltCd = 99999; V.rival.throwCd = 99999; V.rival.feintCd = 99999; V.rival.revCd = 99999;
V.rival.closing = true;
V.rival.boltHoldCut = false; V.rival.boltSuper = false; V.rival.cutRecBreathT = 0; V.rival.guardPoseK = 0;
V.rival.boltPhase = "recovery"; V.rival.boltT = 0;
const brsw272RivalRwo0 = V.recoveryWalkOut(V.rival);
const brsw272RivalBr0 = V.idleBreath(V.rival);
let brsw272RivalMaxSeat=0, brsw272RivalArmed=false, brsw272RivalSaw=false;
for(let i=0;i<36;i++){
  V.rival.closing = true;
  V.rival.wantBlock = false; V.rival.guardChoice = -1; V.rival.standGoal = 99999;
  V.rival.boltCd = 99999; V.rival.guarding = false;
  if (V.rival.gait !== 0) V.rival.gait = 0;
  const bph0 = V.rival.boltPhase;
  V.update(STEP);
  const br = V.idleBreath(V.rival);
  if(bph0==="recovery") {
    brsw272RivalSaw = true;
    brsw272RivalMaxSeat = Math.max(brsw272RivalMaxSeat, Math.abs(br));
  }
  if(V.rival.gait!==0) brsw272RivalMaxSeat = Math.max(brsw272RivalMaxSeat, Math.abs(br));
  if(bph0==="recovery" && !V.rival.boltPhase){
    brsw272RivalArmed = V.rival.cutRecBreathT>0;
  }
  if(brsw272RivalArmed && V.rival.cutRecBreathT===0 && i>8) break;
}
check("bolt recovery settle→walk destRect leftover live rival closing arms", brsw272RivalSaw && brsw272RivalRwo0 && Math.abs(brsw272RivalBr0)<0.05 && brsw272RivalArmed && brsw272RivalMaxSeat<0.35, `saw=${brsw272RivalSaw} rwo0=${brsw272RivalRwo0} br0=${brsw272RivalBr0.toFixed(3)} armed=${brsw272RivalArmed} maxSeat=${brsw272RivalMaxSeat.toFixed(3)} cbt=${V.rival.cutRecBreathT}`);

check("bolt recovery settle→walk destRect leftover rival advanceBolt shared", /advanceBoltFighter\(rival/.test(srcTxt) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "advanceBoltFighter")) && !/f\.kind === "you"/.test(sliceFn(codeOnly, "advanceBoltFighter")) && /recoveryWalkOut\(f\)/.test(sliceFn(codeOnly, "idleBreath")) && /Math\.max\(ck, wk\)/.test(sliceFn(codeOnly, "idleBreath")));
check("cache v=296 after bolt recovery settle→walk destRect leftover live", /game.js\?v=350/.test(htmlTxt));

// --- v273 throw recovery settle→walk destRect leftover: keep 0 through walk-out throw plant; arm cutRec mid-end ---
check("throw recovery settle→walk destRect leftover seats no breath mid-stride", /Throw recovery settle→walk destRect leftover/.test(srcTxt) && /leftover throwPlantFade breath used to/.test(srcTxt) && /seat ~0\.96 mid-stride/.test(srcTxt) && /plant ease climbed to full amp/.test(srcTxt) && /Same[\s\S]{0,80}hole bolt recovery settle→walk/.test(srcTxt) && /Keep breath 0 through walk-out throw recovery/.test(srcTxt) && /arm cutRecBreathT when[\s\S]{0,40}throw recovery ends/.test(srcTxt));
check("throw recovery settle→walk destRect leftover is destRect-only", /destRect-only/.test(srcTxt) && /idleBreath \/ cutRecBreathT/.test(srcTxt) && /Keep breath 0 through walk-out throw recovery/.test(srcTxt) && /walkOutEnd/.test(sliceFn(codeOnly, "advanceAttack")) && /wasThrow && \(walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| f\.guarding \|\| f\.guardPoseK > 0\)/.test(sliceFn(codeOnly, "advanceAttack")) && /else if \(walkOutEnd\) f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "advanceAttack")) && /if \(f\.cut === "throw" && f\.phase === "recovery"\)/.test(sliceFn(codeOnly, "idleBreath")) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\) return 0/.test(sliceFn(codeOnly, "idleBreath")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "destRect")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "bodyAABB")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "poseBitmap")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "throwPlantFade")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "drawKnight")));
check("throw recovery settle→walk destRect leftover keeps 0 through walk-out throw plant", /if \(f\.cut === "throw" && f\.phase === "recovery"\)/.test(sliceFn(codeOnly, "idleBreath")) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\) return 0/.test(sliceFn(codeOnly, "idleBreath")) && /Keep breath 0 through walk-out throw recovery/.test(srcTxt));
check("throw recovery settle→walk destRect leftover arms cutRec on walk mid-end", /else if \(walkOutEnd\) f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "advanceAttack")) && /walk \/ raise mid-end used to dump/.test(srcTxt) && /let cut recovery settle→walk max\(ck, wk\)/.test(srcTxt) && /wasThrow && \(walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| f\.guarding \|\| f\.guardPoseK > 0\)/.test(sliceFn(codeOnly, "advanceAttack")));
check("throw recovery settle→guard destRect leftover keep-0 through raise mid-plant", /Throw recovery settle→guard/.test(srcTxt) && /if \(f\.guarding \|\| f\.guardPoseK > 0\) return 0;/.test(sliceFn(codeOnly, "idleBreath")) && /same arm on guard mid-end/.test(srcTxt));
check("throw recovery settle→walk destRect leftover idle throw plant still eases", /Idle throw plant still eases with throwPlantFade/.test(srcTxt) && /return amp \* \(1 - throwPlantFade\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /throw plant end unchanged/.test(srcTxt) && /no cutRec arm/.test(srcTxt));
check("throw recovery settle→walk destRect leftover sheet \/ restThrowWalk unchanged", /Sheet still throwPlantFade \/ restThrowWalk/.test(srcTxt) && /restThrowWalk = trf > 0 && !restParry && ready\(wind\) && recoveryWalkOut\(f\) && recoveryWalkResting\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /function throwPlantFade/.test(srcTxt) && /if \(f\.phase !== "recovery"\) return 0;/.test(sliceFn(codeOnly, "throwPlantFade")));
check("throw recovery settle→walk destRect leftover bolt knife plant unchanged", /if \(f\.boltPhase === "recovery" && !holdingCutBolt\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /return amp \* \(1 - boltPlantFade\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /Bolt recovery settle→walk destRect leftover/.test(srcTxt) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\) return 0/.test(sliceFn(codeOnly, "idleBreath")));
check("throw recovery settle→walk destRect leftover cutRec\/wakeup\/feint\/bolt paths unchanged", /Math\.max\(ck, wk\)/.test(sliceFn(codeOnly, "idleBreath")) && /Math\.max\(ck, rk\)/.test(sliceFn(codeOnly, "idleBreath")) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "idleBreath")) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickThrowState")) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickSheathe")) && /else if \(walkOutEnd\) f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "advanceBoltFighter")));
check("throw recovery settle→walk destRect leftover tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("throw recovery settle→walk destRect leftover is not a new combat verb", V.THROW_STARTUP===80 && V.THROW_ACTIVE===40 && V.THROW_RECOVERY===220 && V.THROW_DMG===20 && V.THROW_RANGE===120 && V.THROW_STAM===0 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140 && V.HITSTUN===350 && V.WALK_SETTLE_MS===160);
check("slash frames still 180/140/280 after throw recovery settle→walk destRect leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after throw recovery settle→walk destRect leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after throw recovery settle→walk destRect leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("throw frames still 80/40/220 after throw recovery settle→walk destRect leftover", V.THROW_STARTUP===80 && V.THROW_ACTIVE===40 && V.THROW_RECOVERY===220);
check("no 6th button after throw recovery settle→walk destRect leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after throw recovery settle→walk destRect leftover", /game.js\?v=350/.test(htmlTxt));

// live: A/D through throw recovery keeps suppress (no mid-stride seat), arms cutRec on end
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.hp=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cut="slash"; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.walkSettleT=0; V.player.walkRiseT=0;
V.player.boltHoldCut=false; V.player.boltSuper=false; V.player.walkFadeHold=0;
V.player.thrownT=0; V.player.throwInvulnT=0; V.player.wakeRev=false;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=200; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
let trsw273AmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ trsw273AmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.cut="throw"; V.player.phase="recovery";
V.player.phaseT = 0;
V.player.clashRec=false; V.player.techRec=false; V.player.boltPhase="";
V.player.cutRecBreathT=0; V.player.gait=0; V.player.walkRiseT=0; V.player.guarding=false; V.player.guardPoseK=0;
V.keys.add("KeyD");
const trsw273Tf0=V.throwPlantFade(V.player);
const trsw273Br0=V.idleBreath(V.player);
const trsw273Rwo0=V.recoveryWalkOut(V.player);
const trsw273Aabb0=V.bodyAABB(V.player).y;
let trsw273MaxSeat=0, trsw273Armed=false, trsw273Cleared=false, trsw273EndBr=0, trsw273AabbOk=true, trsw273DyHop=0, trsw273SawRec=false;
for(let i=0;i<40;i++){
  holdRivalStill();
  V.keys.add("KeyD");
  const dy0=V.destRect(V.player).dy;
  const ph0=V.player.phase;
  const cut0=V.player.cut;
  V.update(STEP);
  const br=V.idleBreath(V.player);
  if(Math.abs(V.bodyAABB(V.player).y-trsw273Aabb0)>0.05) trsw273AabbOk=false;
  trsw273DyHop=Math.max(trsw273DyHop, Math.abs(V.destRect(V.player).dy-dy0));
  if(ph0==="recovery" && cut0==="throw") {
    trsw273SawRec=true;
    trsw273MaxSeat=Math.max(trsw273MaxSeat, Math.abs(br));
  }
  if(V.player.gait===1) trsw273MaxSeat=Math.max(trsw273MaxSeat, Math.abs(br));
  if(ph0==="recovery" && cut0==="throw" && V.player.phase==="idle"){
    trsw273Armed = V.player.cutRecBreathT>0;
    trsw273Cleared=true;
    trsw273EndBr=br;
  }
  if(trsw273Cleared && V.player.cutRecBreathT===0 && V.player.gait===1 && Math.abs(br)<0.05){ trsw273EndBr=br; break; }
}
check("throw recovery settle→walk destRect leftover live amp ready", trsw273AmpReady, `ready=${trsw273AmpReady} modeT=${V.modeT}`);
check("throw recovery settle→walk destRect leftover live walk-out ready", trsw273Tf0>0.9 && Math.abs(trsw273Br0)<0.05 && trsw273Rwo0, `tf0=${trsw273Tf0.toFixed(3)} br0=${trsw273Br0.toFixed(3)} rwo=${trsw273Rwo0}`);
check("throw recovery settle→walk destRect leftover live arms cutRec on end", trsw273Armed && trsw273SawRec, `armed=${trsw273Armed} saw=${trsw273SawRec} cbt=${V.player.cutRecBreathT}`);
check("throw recovery settle→walk destRect leftover live no mid-stride seat", trsw273MaxSeat<0.35, `maxSeat=${trsw273MaxSeat.toFixed(3)}`);
check("throw recovery settle→walk destRect leftover live rise clears cutRec", trsw273Cleared && Math.abs(trsw273EndBr)<0.05 && V.player.phase==="idle", `cleared=${trsw273Cleared} endBr=${trsw273EndBr.toFixed(3)} ph=${V.player.phase} cbt=${V.player.cutRecBreathT}`);
check("throw recovery settle→walk destRect leftover live no breath dump hop", trsw273MaxSeat<0.35 && trsw273DyHop<6.5, `maxSeat=${trsw273MaxSeat.toFixed(3)} hop=${trsw273DyHop.toFixed(3)}`);
check("throw recovery settle→walk destRect leftover live AABB planted", trsw273AabbOk, `aabbOk=${trsw273AabbOk}`);

// live: idle throw plant still eases breath in (unchanged)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.gait=0; V.player.hp=100; V.player.stamina=100;
V.player.clashRec=false; V.player.techRec=false; V.player.thrownT=0; V.player.throwInvulnT=0;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=500; V.rival.x=1100;
let trsw273IdleAmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ trsw273IdleAmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.cut="throw"; V.player.phase="recovery";
V.player.phaseT = 0;
V.player.clashRec=false; V.player.techRec=false; V.player.cutRecBreathT=0; V.player.gait=0;
let trsw273IdleBr0=V.idleBreath(V.player), trsw273IdleBr1=trsw273IdleBr0, trsw273IdleUp=false, trsw273IdleArmed=false;
for(let i=0;i<24;i++){
  holdRivalStill(); V.update(STEP);
  if(V.player.phase==="idle" && V.player.cutRecBreathT>0) trsw273IdleArmed=true;
  const br=V.idleBreath(V.player);
  if(V.player.phase==="recovery" && V.player.cut==="throw" && Math.abs(br)>Math.abs(trsw273IdleBr1)+0.05) trsw273IdleUp=true;
  if(V.player.phase==="idle" && V.player.cutRecBreathT===0 && Math.abs(br)>Math.abs(trsw273IdleBr1)+0.05) trsw273IdleUp=true;
  trsw273IdleBr1=br;
  if(V.player.phase==="idle" && i>8) break;
}
check("throw recovery settle→walk destRect leftover live idle throw plant still eases", trsw273IdleAmpReady && !trsw273IdleArmed && trsw273IdleUp && Math.abs(trsw273IdleBr0)<0.05 && Math.abs(trsw273IdleBr1)>0.4 && V.player.gait===0, `ready=${trsw273IdleAmpReady} armed=${trsw273IdleArmed} up=${trsw273IdleUp} br0=${trsw273IdleBr0.toFixed(3)} br1=${trsw273IdleBr1.toFixed(3)} cbt=${V.player.cutRecBreathT}`);

// live: rival closing through throw recovery keeps suppress + arms cutRec
toClose();
freezeYouAI();
Math.random = () => 0.99;
V.openLeft = 0; V.hitstopLeft = 0;
V.rival.x = 900; V.player.x = 400;
V.rival.hp = 100; V.rival.stamina = 100;
V.rival.falling = false; V.rival.stunT = 0; V.rival.thrownT = 0;
V.rival.phase = "idle"; V.rival.phaseT = 0; V.rival.boltPhase = "";
V.rival.guarding = false; V.rival.reversal = false; V.rival.feintT = 0;
V.rival.gait = 0; V.rival.walkT = 0; V.rival.walkFadeHold = 0; V.rival.walkSettleT = 0;
V.rival.wantBlock = false; V.rival.guardChoice = -1; V.rival.standGoal = 99999;
V.rival.boltCd = 99999; V.rival.throwCd = 99999; V.rival.feintCd = 99999; V.rival.revCd = 99999;
V.rival.closing = true;
V.rival.cutRecBreathT = 0; V.rival.guardPoseK = 0; V.rival.clashRec=false; V.rival.techRec=false;
V.rival.cut = "throw"; V.rival.phase = "recovery"; V.rival.phaseT = 0;
const trsw273RivalRwo0 = V.recoveryWalkOut(V.rival);
const trsw273RivalBr0 = V.idleBreath(V.rival);
let trsw273RivalMaxSeat=0, trsw273RivalArmed=false, trsw273RivalSaw=false;
for(let i=0;i<36;i++){
  V.rival.closing = true;
  V.rival.wantBlock = false; V.rival.guardChoice = -1; V.rival.standGoal = 99999;
  V.rival.boltCd = 99999; V.rival.throwCd = 99999; V.rival.guarding = false;
  if (V.rival.gait !== 0) V.rival.gait = 0;
  const ph0 = V.rival.phase;
  const cut0 = V.rival.cut;
  V.update(STEP);
  const br = V.idleBreath(V.rival);
  if(ph0==="recovery" && cut0==="throw") {
    trsw273RivalSaw = true;
    trsw273RivalMaxSeat = Math.max(trsw273RivalMaxSeat, Math.abs(br));
  }
  if(V.rival.gait!==0) trsw273RivalMaxSeat = Math.max(trsw273RivalMaxSeat, Math.abs(br));
  if(ph0==="recovery" && cut0==="throw" && V.rival.phase==="idle"){
    trsw273RivalArmed = V.rival.cutRecBreathT>0;
  }
  if(trsw273RivalArmed && V.rival.cutRecBreathT===0 && i>8) break;
}
check("throw recovery settle→walk destRect leftover live rival closing arms", trsw273RivalSaw && trsw273RivalRwo0 && Math.abs(trsw273RivalBr0)<0.05 && trsw273RivalArmed && trsw273RivalMaxSeat<0.35, `saw=${trsw273RivalSaw} rwo0=${trsw273RivalRwo0} br0=${trsw273RivalBr0.toFixed(3)} armed=${trsw273RivalArmed} maxSeat=${trsw273RivalMaxSeat.toFixed(3)} cbt=${V.rival.cutRecBreathT}`);

check("throw recovery settle→walk destRect leftover rival advanceAttack shared", /advanceAttack\(rival/.test(srcTxt) && /else if \(walkOutEnd\) f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "advanceAttack")) && /wasThrow && \(walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| f\.guarding \|\| f\.guardPoseK > 0\)/.test(sliceFn(codeOnly, "advanceAttack")) && /recoveryWalkOut\(f\)/.test(sliceFn(codeOnly, "idleBreath")) && /Math\.max\(ck, wk\)/.test(sliceFn(codeOnly, "idleBreath")));
check("cache v=296 after throw recovery settle→walk destRect leftover live", /game.js\?v=350/.test(htmlTxt));




// --- v274 chip stun settle→walk destRect leftover: keep 0 through walk-out stun-end; arm cutRec mid-end ---
check("chip stun settle→walk destRect leftover seats no breath mid-stride", /Chip stun settle→walk destRect leftover/.test(srcTxt) && /leftover stun-end breath ease-in/.test(srcTxt) && /stunT\/GUARD_RAISE_MS/.test(srcTxt) && /walk sheet was already planted/.test(srcTxt) && /Same[\s\S]{0,80}hole throw \/ bolt recovery settle→walk/.test(srcTxt) && /Keep breath 0 through walk-out \/ guard during stun-end/.test(srcTxt) && /tickStun arm[\s\S]{0,40}cutRecBreathT when chip stun ends/.test(srcTxt));
check("chip stun settle→walk destRect leftover is destRect-only", /destRect-only/.test(srcTxt) && /idleBreath \/ cutRecBreathT/.test(srcTxt) && /Keep breath 0 through walk-out \/ guard during stun-end/.test(srcTxt) && /function tickStun/.test(srcTxt) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| f\.guarding \|\| f\.guardPoseK > 0/.test(sliceFn(codeOnly, "tickStun")) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickStun")) && /if \(f\.stunT > 0\)/.test(sliceFn(codeOnly, "idleBreath")) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\) return 0/.test(sliceFn(codeOnly, "idleBreath")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "destRect")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "bodyAABB")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "poseBitmap")) && !/tickStun/.test(sliceFn(codeOnly, "destRect")));
check("chip stun settle→walk destRect leftover keeps 0 through walk-out stun-end", /Chip stun settle→walk destRect leftover/.test(srcTxt) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "idleBreath")) && /if \(f\.guarding \|\| f\.guardPoseK > 0\) return 0;/.test(sliceFn(codeOnly, "idleBreath")) && /Keep breath 0 through walk-out \/ guard during stun-end/.test(srcTxt));
check("chip stun settle→walk destRect leftover arms cutRec on walk mid-end", /function tickStun/.test(srcTxt) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickStun")) && /walk \/ raise mid-end used to dump breath/.test(srcTxt) && /let cut recovery settle→walk max\(ck, wk\)/.test(srcTxt) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| f\.guarding \|\| f\.guardPoseK > 0/.test(sliceFn(codeOnly, "tickStun")));
check("chip stun settle→guard destRect leftover keep-0 through raise mid-plant", /Chip stun settle→guard/.test(srcTxt) && /if \(f\.guarding \|\| f\.guardPoseK > 0\) return 0;/.test(sliceFn(codeOnly, "idleBreath")) && /same arm on guard mid-end/.test(srcTxt));
check("chip stun settle→walk destRect leftover idle chip stun still eases", /Idle chip stun still eases with stunT\/GUARD_RAISE_MS/.test(srcTxt) && /stun end unchanged/.test(srcTxt) && /f\.stunT \/ GUARD_RAISE_MS/.test(sliceFn(codeOnly, "idleBreath")) && /no cutRec arm/.test(srcTxt));
check("chip stun settle→walk destRect leftover throw KD \/ wakeup still hard-zero", /Throw KD \/ wakeup invuln still hard-zero/.test(srcTxt) && /f\.thrownT > 0 \|\| f\.throwInvulnT > 0 \|\| f\.hp <= 0/.test(sliceFn(codeOnly, "idleBreath")) && /wakeupFade owns getup/.test(srcTxt));
check("chip stun settle→walk destRect leftover sheet \/ rest stun walk unchanged", /recoveryWalkOut\(f\) && recoveryWalkResting\(f\) && f\.stunT > 0/.test(sliceFn(codeOnly, "drawKnight")) && /chip stun used to keep idle while A\/D/.test(srcTxt) && /poseBitmap still idle/.test(srcTxt));
check("chip stun settle→walk destRect leftover throw\/bolt settle→walk unchanged", /if \(f\.cut === "throw" && f\.phase === "recovery"\)/.test(sliceFn(codeOnly, "idleBreath")) && /if \(f\.boltPhase === "recovery" && !holdingCutBolt\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /Throw recovery settle→walk destRect leftover/.test(srcTxt) && /Bolt recovery settle→walk destRect leftover/.test(srcTxt) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\) return 0/.test(sliceFn(codeOnly, "idleBreath")));
check("chip stun settle→walk destRect leftover cutRec\/wakeup\/feint\/bolt paths unchanged", /Math\.max\(ck, wk\)/.test(sliceFn(codeOnly, "idleBreath")) && /Math\.max\(ck, rk\)/.test(sliceFn(codeOnly, "idleBreath")) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "idleBreath")) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickThrowState")) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickSheathe")) && /else if \(walkOutEnd\) f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "advanceBoltFighter")) && /else if \(walkOutEnd\) f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "advanceAttack")));
check("chip stun settle→walk destRect leftover tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("chip stun settle→walk destRect leftover is not a new combat verb", V.HITSTUN===350 && V.HITSTOP_HIT===140 && V.GUARD_RAISE_MS===80 && V.THROW_WAKE_INVULN===80 && V.THROW_KD_MS===520 && V.WALK_SETTLE_MS===160 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("slash frames still 180/140/280 after chip stun settle→walk destRect leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after chip stun settle→walk destRect leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after chip stun settle→walk destRect leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("throw frames still 80/40/220 after chip stun settle→walk destRect leftover", V.THROW_STARTUP===80 && V.THROW_ACTIVE===40 && V.THROW_RECOVERY===220);
check("no 6th button after chip stun settle→walk destRect leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after chip stun settle→walk destRect leftover", /game.js\?v=350/.test(htmlTxt));

// live: A/D through chip stun keeps suppress (no mid-stride seat), arms cutRec on end
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.hp=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cut="slash"; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.walkSettleT=0; V.player.walkRiseT=0;
V.player.boltHoldCut=false; V.player.boltSuper=false; V.player.walkFadeHold=0;
V.player.thrownT=0; V.player.throwInvulnT=0; V.player.wakeRev=false;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=200; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
let cssw274AmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ cssw274AmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.stunT = V.HITSTUN;
V.player.phase="idle"; V.player.boltPhase=""; V.player.feintT=0; V.player.sheatheT=0;
V.player.cutRecBreathT=0; V.player.gait=0; V.player.walkRiseT=0; V.player.guarding=false; V.player.guardPoseK=0;
V.player.thrownT=0; V.player.throwInvulnT=0; V.player.hp=100;
V.keys.add("KeyD");
const cssw274Br0=V.idleBreath(V.player);
const cssw274Rwo0=V.recoveryWalkOut(V.player);
const cssw274Aabb0=V.bodyAABB(V.player).y;
let cssw274MaxSeat=0, cssw274Armed=false, cssw274Cleared=false, cssw274EndBr=0, cssw274AabbOk=true, cssw274DyHop=0, cssw274SawStun=false;
for(let i=0;i<50;i++){
  holdRivalStill();
  V.keys.add("KeyD");
  const dy0=V.destRect(V.player).dy;
  const st0=V.player.stunT;
  V.update(STEP);
  const br=V.idleBreath(V.player);
  if(Math.abs(V.bodyAABB(V.player).y-cssw274Aabb0)>0.05) cssw274AabbOk=false;
  cssw274DyHop=Math.max(cssw274DyHop, Math.abs(V.destRect(V.player).dy-dy0));
  if(st0>0) {
    cssw274SawStun=true;
    cssw274MaxSeat=Math.max(cssw274MaxSeat, Math.abs(br));
  }
  if(V.player.gait===1) cssw274MaxSeat=Math.max(cssw274MaxSeat, Math.abs(br));
  if(st0>0 && V.player.stunT===0){
    cssw274Armed = V.player.cutRecBreathT>0;
    cssw274Cleared=true;
    cssw274EndBr=br;
  }
  if(cssw274Cleared && V.player.cutRecBreathT===0 && V.player.gait===1 && Math.abs(br)<0.05){ cssw274EndBr=br; break; }
}
check("chip stun settle→walk destRect leftover live amp ready", cssw274AmpReady, `ready=${cssw274AmpReady} modeT=${V.modeT}`);
check("chip stun settle→walk destRect leftover live walk-out ready", Math.abs(cssw274Br0)<0.05 && cssw274Rwo0 && V.player.stunT===0, `br0=${cssw274Br0.toFixed(3)} rwo=${cssw274Rwo0} st=${V.player.stunT}`);
check("chip stun settle→walk destRect leftover live arms cutRec on end", cssw274Armed && cssw274SawStun, `armed=${cssw274Armed} saw=${cssw274SawStun} cbt=${V.player.cutRecBreathT}`);
check("chip stun settle→walk destRect leftover live no mid-stride seat", cssw274MaxSeat<0.35, `maxSeat=${cssw274MaxSeat.toFixed(3)}`);
check("chip stun settle→walk destRect leftover live rise clears cutRec", cssw274Cleared && Math.abs(cssw274EndBr)<0.05 && V.player.stunT===0, `cleared=${cssw274Cleared} endBr=${cssw274EndBr.toFixed(3)} st=${V.player.stunT} cbt=${V.player.cutRecBreathT}`);
check("chip stun settle→walk destRect leftover live no breath dump hop", cssw274MaxSeat<0.35 && cssw274DyHop<6.5, `maxSeat=${cssw274MaxSeat.toFixed(3)} hop=${cssw274DyHop.toFixed(3)}`);
check("chip stun settle→walk destRect leftover live AABB planted", cssw274AabbOk, `aabbOk=${cssw274AabbOk}`);

// live: idle chip stun still eases breath in (unchanged — no cutRec arm)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.gait=0; V.player.hp=100; V.player.stamina=100;
V.player.thrownT=0; V.player.throwInvulnT=0;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=500; V.rival.x=1100;
let cssw274IdleAmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ cssw274IdleAmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.stunT = V.GUARD_RAISE_MS; // start in ease-in window
V.player.cutRecBreathT=0; V.player.gait=0; V.player.thrownT=0; V.player.throwInvulnT=0; V.player.hp=100;
let cssw274IdleBr0=V.idleBreath(V.player), cssw274IdleBr1=cssw274IdleBr0, cssw274IdleUp=false, cssw274IdleArmed=false;
for(let i=0;i<24;i++){
  holdRivalStill(); V.update(STEP);
  if(V.player.stunT===0 && V.player.cutRecBreathT>0) cssw274IdleArmed=true;
  const br=V.idleBreath(V.player);
  if(V.player.stunT>0 && Math.abs(br)>Math.abs(cssw274IdleBr1)+0.05) cssw274IdleUp=true;
  if(V.player.stunT===0 && V.player.cutRecBreathT===0 && Math.abs(br)>Math.abs(cssw274IdleBr1)+0.05) cssw274IdleUp=true;
  cssw274IdleBr1=br;
  if(V.player.stunT===0 && i>4) break;
}
check("chip stun settle→walk destRect leftover live idle chip stun still eases", cssw274IdleAmpReady && !cssw274IdleArmed && cssw274IdleUp && Math.abs(cssw274IdleBr0)<0.05 && Math.abs(cssw274IdleBr1)>0.4 && V.player.gait===0, `ready=${cssw274IdleAmpReady} armed=${cssw274IdleArmed} up=${cssw274IdleUp} br0=${cssw274IdleBr0.toFixed(3)} br1=${cssw274IdleBr1.toFixed(3)} cbt=${V.player.cutRecBreathT}`);

// live: rival closing through chip stun keeps suppress + arms cutRec
toClose();
freezeYouAI();
Math.random = () => 0.99;
V.openLeft = 0; V.hitstopLeft = 0;
V.rival.x = 900; V.player.x = 400;
V.rival.hp = 100; V.rival.stamina = 100;
V.rival.falling = false; V.rival.stunT = 0; V.rival.thrownT = 0; V.rival.throwInvulnT = 0;
V.rival.phase = "idle"; V.rival.phaseT = 0; V.rival.boltPhase = "";
V.rival.guarding = false; V.rival.reversal = false; V.rival.feintT = 0;
V.rival.gait = 0; V.rival.walkT = 0; V.rival.walkFadeHold = 0; V.rival.walkSettleT = 0;
V.rival.wantBlock = false; V.rival.guardChoice = -1; V.rival.standGoal = 99999;
V.rival.boltCd = 99999; V.rival.throwCd = 99999; V.rival.feintCd = 99999; V.rival.revCd = 99999;
V.rival.closing = true;
V.rival.cutRecBreathT = 0; V.rival.guardPoseK = 0;
V.rival.stunT = V.HITSTUN;
const cssw274RivalRwo0 = V.recoveryWalkOut(V.rival);
const cssw274RivalBr0 = V.idleBreath(V.rival);
let cssw274RivalMaxSeat=0, cssw274RivalArmed=false, cssw274RivalSaw=false;
for(let i=0;i<50;i++){
  V.rival.closing = true;
  V.rival.wantBlock = false; V.rival.guardChoice = -1; V.rival.standGoal = 99999;
  V.rival.boltCd = 99999; V.rival.throwCd = 99999; V.rival.guarding = false;
  if (V.rival.gait !== 0) V.rival.gait = 0;
  const st0 = V.rival.stunT;
  V.update(STEP);
  const br = V.idleBreath(V.rival);
  if(st0>0) {
    cssw274RivalSaw = true;
    cssw274RivalMaxSeat = Math.max(cssw274RivalMaxSeat, Math.abs(br));
  }
  if(V.rival.gait!==0) cssw274RivalMaxSeat = Math.max(cssw274RivalMaxSeat, Math.abs(br));
  if(st0>0 && V.rival.stunT===0){
    cssw274RivalArmed = V.rival.cutRecBreathT>0;
  }
  if(cssw274RivalArmed && V.rival.cutRecBreathT===0 && i>8) break;
}
check("chip stun settle→walk destRect leftover live rival closing arms", cssw274RivalSaw && cssw274RivalRwo0 && Math.abs(cssw274RivalBr0)<0.05 && cssw274RivalArmed && cssw274RivalMaxSeat<0.35, `saw=${cssw274RivalSaw} rwo0=${cssw274RivalRwo0} br0=${cssw274RivalBr0.toFixed(3)} armed=${cssw274RivalArmed} maxSeat=${cssw274RivalMaxSeat.toFixed(3)} cbt=${V.rival.cutRecBreathT}`);

check("chip stun settle→walk destRect leftover rival tickStun shared", /tickStun\(rival/.test(srcTxt) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickStun")) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| f\.guarding \|\| f\.guardPoseK > 0/.test(sliceFn(codeOnly, "tickStun")) && /recoveryWalkOut\(f\)/.test(sliceFn(codeOnly, "idleBreath")) && /Math\.max\(ck, wk\)/.test(sliceFn(codeOnly, "idleBreath")));
check("cache v=296 after chip stun settle→walk destRect leftover live", /game.js\?v=350/.test(htmlTxt));

// --- v275 telegraph settle→walk destRect leftover: keep 0 through walk-out / walkFadeHold during telegraph ---
check("telegraph settle→walk destRect leftover seats no breath mid-stride", /Telegraph settle→walk destRect leftover/.test(srcTxt) && /leftover telegraphFade breath used to seat ~1\.4 mid-stride/.test(srcTxt) && /Space\/L\/throw\/K armed from A\/D \/ rival closing/.test(srcTxt) && /Same[\s\S]{0,80}hole chip stun \/ bolt \/ throw recovery settle→walk/.test(srcTxt) && /Keep breath 0 through walk-out \/ leftover walkFadeHold during/.test(srcTxt) && /idle telegraph still eases with telegraphFade/.test(srcTxt));
check("telegraph settle→walk destRect leftover is destRect-only", /destRect-only \(idleBreath\)/.test(srcTxt) && /Keep breath 0 through walk-out \/ leftover walkFadeHold during/.test(srcTxt) && /if \(telegraphing\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "idleBreath")) && /return amp \* \(1 - telegraphFade\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && !/walkFadeHold/.test(sliceFn(codeOnly, "destRect")) && !/telegraphFade/.test(sliceFn(codeOnly, "destRect")) && !/telegraphFade/.test(sliceFn(codeOnly, "bodyAABB")) && !/telegraphFade/.test(sliceFn(codeOnly, "poseBitmap")));
check("telegraph settle→walk destRect leftover keeps 0 through walk-out telegraph", /Telegraph settle→walk \/ walk→telegraph destRect leftover/.test(srcTxt) && /if \(recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\) return 0;/.test(sliceFn(codeOnly, "idleBreath")) && /Keep breath 0 through walk-out \/ leftover walkFadeHold during/.test(srcTxt) && /walkFadeHold during telegraph/.test(srcTxt));
check("telegraph settle→walk destRect leftover idle telegraph still eases", /idle telegraph still eases with telegraphFade/.test(srcTxt) && /No cutRec arm/.test(srcTxt) && /return amp \* \(1 - telegraphFade\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /Rival telegraph leftover/.test(srcTxt));
check("telegraph settle→walk destRect leftover no cutRec arm", /No cutRec arm \(ends into active\)/.test(srcTxt) && /No cutRec arm \(telegraph ends into/.test(srcTxt) && /phase!==idle keeps breath 0/.test(srcTxt) && /recovery settle→walk already arms cutRec/.test(srcTxt));
check("telegraph settle→walk destRect leftover chip\/bolt\/throw settle→walk unchanged", /Chip stun settle→walk destRect leftover/.test(srcTxt) && /Bolt recovery settle→walk destRect leftover/.test(srcTxt) && /Throw recovery settle→walk destRect leftover/.test(srcTxt) && /if \(f\.stunT > 0\)/.test(sliceFn(codeOnly, "idleBreath")) && /if \(f\.cut === "throw" && f\.phase === "recovery"\)/.test(sliceFn(codeOnly, "idleBreath")) && /if \(f\.boltPhase === "recovery" && !holdingCutBolt\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")));
check("telegraph settle→walk destRect leftover restTeleWalk \/ telegraphFade unchanged", /restTeleWalk = telegraphing\(f\) && tf < 0\.98 && \(f\.walkFadeHold > 0\.02 \|\| recoveryWalkOut\(f\)\)/.test(sliceFn(codeOnly, "drawKnight")) && /function telegraphFade/.test(srcTxt) && /function telegraphing/.test(srcTxt) && /GUARD_RAISE_MS/.test(sliceFn(codeOnly, "telegraphFade")));
check("telegraph settle→walk destRect leftover tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("telegraph settle→walk destRect leftover is not a new combat verb", V.HITSTUN===350 && V.HITSTOP_HIT===140 && V.GUARD_RAISE_MS===80 && V.THROW_WAKE_INVULN===80 && V.THROW_KD_MS===520 && V.WALK_SETTLE_MS===160 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("slash frames still 180/140/280 after telegraph settle→walk destRect leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after telegraph settle→walk destRect leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after telegraph settle→walk destRect leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("throw frames still 80/40/220 after telegraph settle→walk destRect leftover", V.THROW_STARTUP===80 && V.THROW_ACTIVE===40 && V.THROW_RECOVERY===220);
check("no 6th button after telegraph settle→walk destRect leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after telegraph settle→walk destRect leftover", /game.js\?v=350/.test(htmlTxt));

// live: A/D into Space telegraph keeps suppress (no mid-stride seat)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.hp=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cut="slash"; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.walkSettleT=0; V.player.walkRiseT=0;
V.player.boltHoldCut=false; V.player.boltSuper=false; V.player.walkFadeHold=0; V.player.telegraph=false; V.player.linkPlant=false;
V.player.thrownT=0; V.player.throwInvulnT=0;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=200; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
let tsw275AmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ tsw275AmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.keys.add("KeyD");
for(let i=0;i<20;i++){ holdRivalStill(); V.keys.add("KeyD"); V.update(STEP); }
const tsw275Br0=V.idleBreath(V.player);
const tsw275Rwo0=V.recoveryWalkOut(V.player);
const tsw275Aabb0=V.bodyAABB(V.player).y;
V.attackEdge=true;
V.keys.add("KeyD");
holdRivalStill();
V.update(STEP);
const tsw275PostPh=V.player.phase;
const tsw275PostTel=V.telegraphing(V.player);
const tsw275PostRwo=V.recoveryWalkOut(V.player);
let tsw275MaxSeat=0, tsw275SawTel=false, tsw275AabbOk=true, tsw275DyHop=0, tsw275EndBr=0, tsw275Cleared=false;
for(let i=0;i<30;i++){
  holdRivalStill();
  V.keys.add("KeyD");
  const dy0=V.destRect(V.player).dy;
  const tel0=V.telegraphing(V.player);
  const br=V.idleBreath(V.player);
  if(Math.abs(V.bodyAABB(V.player).y-tsw275Aabb0)>0.05) tsw275AabbOk=false;
  if(tel0){
    tsw275SawTel=true;
    tsw275MaxSeat=Math.max(tsw275MaxSeat, Math.abs(br));
  }
  V.update(STEP);
  tsw275DyHop=Math.max(tsw275DyHop, Math.abs(V.destRect(V.player).dy-dy0));
  if(tsw275SawTel && !V.telegraphing(V.player) && V.player.phase!=="startup"){
    tsw275Cleared=true;
    tsw275EndBr=V.idleBreath(V.player);
    break;
  }
}
check("telegraph settle→walk destRect leftover live amp ready", tsw275AmpReady, `ready=${tsw275AmpReady} modeT=${V.modeT}`);
check("telegraph settle→walk destRect leftover live walk-out ready", Math.abs(tsw275Br0)<0.05 && tsw275Rwo0 && tsw275PostPh==="startup" && tsw275PostTel && tsw275PostRwo, `br0=${tsw275Br0.toFixed(3)} rwo=${tsw275Rwo0} postPh=${tsw275PostPh} postTel=${tsw275PostTel} postRwo=${tsw275PostRwo}`);
check("telegraph settle→walk destRect leftover live saw telegraph", tsw275SawTel && tsw275PostTel, `saw=${tsw275SawTel} postTel=${tsw275PostTel}`);
check("telegraph settle→walk destRect leftover live no mid-stride seat", tsw275MaxSeat<0.35, `maxSeat=${tsw275MaxSeat.toFixed(3)}`);
check("telegraph settle→walk destRect leftover live ends into active breath 0", tsw275Cleared && Math.abs(tsw275EndBr)<0.05 && V.player.phase==="active", `cleared=${tsw275Cleared} endBr=${tsw275EndBr.toFixed(3)} ph=${V.player.phase} cbt=${V.player.cutRecBreathT}`);
check("telegraph settle→walk destRect leftover live no breath dump hop", tsw275MaxSeat<0.35 && tsw275DyHop<6.5, `maxSeat=${tsw275MaxSeat.toFixed(3)} hop=${tsw275DyHop.toFixed(3)}`);
check("telegraph settle→walk destRect leftover live AABB planted", tsw275AabbOk, `aabbOk=${tsw275AabbOk}`);

// live: idle telegraph still eases breath out (unchanged)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.gait=0; V.player.hp=100; V.player.stamina=100;
V.player.walkFadeHold=0; V.player.walkRiseT=0; V.player.walkT=0; V.player.telegraph=false; V.player.linkPlant=false;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=500; V.rival.x=1100;
let tsw275IdleAmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ tsw275IdleAmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
const tsw275IdleBrPre=V.idleBreath(V.player);
V.attackEdge=true;
holdRivalStill();
V.update(STEP);
let tsw275IdleBr0=V.idleBreath(V.player), tsw275IdleBr1=tsw275IdleBr0, tsw275IdleDown=false, tsw275IdleSaw=false, tsw275IdleArmed=false;
for(let i=0;i<12;i++){
  holdRivalStill();
  const tel=V.telegraphing(V.player);
  const br=V.idleBreath(V.player);
  if(tel){
    tsw275IdleSaw=true;
    if(i===0) tsw275IdleBr0=br;
    if(Math.abs(br)<Math.abs(tsw275IdleBr1)-0.05) tsw275IdleDown=true;
    tsw275IdleBr1=br;
  }
  if(V.player.cutRecBreathT>0) tsw275IdleArmed=true;
  V.update(STEP);
  if(tsw275IdleSaw && !V.telegraphing(V.player)) break;
}
check("telegraph settle→walk destRect leftover live idle telegraph still eases", tsw275IdleAmpReady && tsw275IdleSaw && !tsw275IdleArmed && tsw275IdleDown && Math.abs(tsw275IdleBrPre)>0.4 && Math.abs(tsw275IdleBr0)>0.4 && Math.abs(tsw275IdleBr1)<Math.abs(tsw275IdleBr0)-0.2 && V.player.gait===0, `ready=${tsw275IdleAmpReady} saw=${tsw275IdleSaw} armed=${tsw275IdleArmed} down=${tsw275IdleDown} brPre=${tsw275IdleBrPre.toFixed(3)} br0=${tsw275IdleBr0.toFixed(3)} br1=${tsw275IdleBr1.toFixed(3)} cbt=${V.player.cutRecBreathT}`);

// live: rival closing into startAttack slash keeps suppress via walkFadeHold
toClose();
freezeYouAI();
Math.random = () => 0.99;
V.openLeft = 0; V.hitstopLeft = 0;
V.player.stunT = 0; V.player.falling = false; V.player.phase = "idle"; V.player.guarding = false;
V.rival.x = 900; V.player.x = 400;
V.rival.hp = 100; V.rival.stamina = 100;
V.rival.falling = false; V.rival.stunT = 0; V.rival.thrownT = 0; V.rival.throwInvulnT = 0;
V.rival.phase = "idle"; V.rival.phaseT = 0; V.rival.boltPhase = "";
V.rival.guarding = false; V.rival.reversal = false; V.rival.feintT = 0; V.rival.sheatheT = 0;
V.rival.gait = 0; V.rival.walkT = 0; V.rival.walkFadeHold = 0; V.rival.walkSettleT = 0; V.rival.walkRiseT = 0;
V.rival.cutRecBreathT = 0; V.rival.telegraph = false; V.rival.linkPlant = false; V.rival.guardPoseK = 0;
V.rival.wantBlock = false; V.rival.guardChoice = -1; V.rival.standGoal = 99999;
V.rival.boltCd = 99999; V.rival.throwCd = 99999; V.rival.feintCd = 99999; V.rival.revCd = 99999;
V.rival.wakeRevArmed = -1; V.rival.revArmed = -1; V.rival.pbArmed = -1; V.rival.feintArmed = -1;
V.rival.closing = true;
const tsw275RivalRwo0=V.recoveryWalkOut(V.rival);
V.startAttack(V.rival, "slash");
const tsw275RivalWfh0=V.rival.walkFadeHold;
V.rival.closing=true;
const tsw275RivalBr0=V.idleBreath(V.rival); // post-arm: keep-0 via walkFadeHold
let tsw275RivalSaw=false, tsw275RivalMaxSeat=0;
for(let i=0;i<12;i++){
  V.rival.closing=true; V.rival.wantBlock=false; V.rival.guardChoice=-1; V.rival.standGoal=99999;
  V.rival.boltCd=99999; V.rival.throwCd=99999; V.rival.guarding=false;
  const tel=V.telegraphing(V.rival);
  const br=V.idleBreath(V.rival);
  if(tel){
    tsw275RivalSaw=true;
    tsw275RivalMaxSeat=Math.max(tsw275RivalMaxSeat, Math.abs(br));
  }
  V.update(STEP);
  if(tsw275RivalSaw && !V.telegraphing(V.rival) && i>3) break;
}
check("telegraph settle→walk destRect leftover live rival closing arms", tsw275RivalSaw && tsw275RivalRwo0 && Math.abs(tsw275RivalBr0)<0.05 && tsw275RivalWfh0>0.02 && tsw275RivalMaxSeat<0.35, `saw=${tsw275RivalSaw} rwo0=${tsw275RivalRwo0} br0=${tsw275RivalBr0.toFixed(3)} wfh0=${tsw275RivalWfh0.toFixed(3)} maxSeat=${tsw275RivalMaxSeat.toFixed(3)} ph=${V.rival.phase} tel=${V.rival.telegraph}`);

check("telegraph settle→walk destRect leftover rival idleBreath shared", /if \(telegraphing\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "idleBreath")) && /return amp \* \(1 - telegraphFade\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && !/f\.kind === "you"/.test(sliceFn(codeOnly, "idleBreath").slice(sliceFn(codeOnly, "idleBreath").indexOf("if (telegraphing(f))"), sliceFn(codeOnly, "idleBreath").indexOf("if (telegraphing(f))")+220)));
check("cache v=296 after telegraph settle→walk destRect leftover live", /game.js\?v=350/.test(htmlTxt));


// --- v276 feint settle→walk plant-release destRect leftover: keep 0 through walk-out / arm cutRec ---
check("feint settle→walk plant-release destRect leftover seats no breath mid-stride", /Feint settle→walk plant-release destRect leftover/.test(srcTxt) && /leftover feintFade breath used to seat ~1\.1/.test(srcTxt) && /max\(feintFade, rise\) climbed/.test(srcTxt) && /walkRise restarted/.test(srcTxt) && /restFeintWalk/.test(srcTxt) && /Same[\s\S]{0,80}hole telegraph \/ chip stun \/ bolt \/ throw/.test(srcTxt) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "idleBreath")));
check("feint settle→walk plant-release destRect leftover is destRect-only", /destRect-only \(idleBreath \/ cutRecBreathT\)/.test(srcTxt) && /Keep breath 0 through walking \/ walk-out \/ leftover/.test(srcTxt) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\) return 0/.test(sliceFn(codeOnly, "idleBreath")) && /prevFeint > 0/.test(sliceFn(codeOnly, "tickFeint")) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickFeint")) && !/walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "destRect")) && !/walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "bodyAABB")) && !/walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "poseBitmap")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "feintFade")) && !/prevFeint/.test(sliceFn(codeOnly, "destRect")) && !/prevFeint/.test(sliceFn(codeOnly, "bodyAABB")));
check("feint settle→walk plant-release destRect leftover keeps 0 through walk-out feint", /if \(walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\) return 0/.test(sliceFn(codeOnly, "idleBreath")) && /f\.feintT > 0/.test(sliceFn(codeOnly, "idleBreath")) && /tickFeint arm cutRecBreathT when feint ends mid-stride/.test(srcTxt));
check("feint settle→walk plant-release destRect leftover idle feint still eases", /Idle feint still eases with feintFade/.test(srcTxt) && /return amp \* \(1 - feintFade\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /f\.feintT > 0/.test(sliceFn(codeOnly, "idleBreath")));
check("feint settle→walk plant-release destRect leftover arms cutRec on end", /prevFeint > 0/.test(sliceFn(codeOnly, "tickFeint")) && /f\.feintT === 0/.test(sliceFn(codeOnly, "tickFeint")) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickFeint")) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| f\.guarding \|\| f\.guardPoseK > 0/.test(sliceFn(codeOnly, "tickFeint")));
check("feint settle→walk plant-release destRect leftover settle→guard unchanged", /Math\.max\(fk, rk\)/.test(sliceFn(codeOnly, "idleBreath")) && /1 - Math\.max\(fk, rk\)/.test(sliceFn(codeOnly, "idleBreath")) && /Feint settle→guard unchanged/.test(srcTxt));
check("feint settle→walk plant-release destRect leftover chip\/tele\/bolt\/throw settle→walk unchanged", /recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "idleBreath")) && /if \(telegraphing\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /if \(f\.stunT > 0\)/.test(sliceFn(codeOnly, "idleBreath")) && /f\.boltPhase === "recovery"/.test(sliceFn(codeOnly, "idleBreath")) && /f\.cut === "throw" && f\.phase === "recovery"/.test(sliceFn(codeOnly, "idleBreath")));
check("feint settle→walk plant-release destRect leftover restFeintWalk \/ feintFade unchanged", /restFeintWalk/.test(srcTxt) && /function feintFade/.test(srcTxt) && /FEINT_RECOVERY/.test(sliceFn(codeOnly, "feintFade")) && /f\.feintT = FEINT_RECOVERY/.test(sliceFn(codeOnly, "startFeint")));
check("feint settle→walk plant-release destRect leftover tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("feint settle→walk plant-release destRect leftover is not a new combat verb", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.HITSTUN===350 && V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140 && V.FEINT_RECOVERY===100 && V.WALK_SETTLE_MS===160);
check("slash frames still 180/140/280 after feint settle→walk plant-release destRect leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after feint settle→walk plant-release destRect leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after feint settle→walk plant-release destRect leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("throw frames still 80/40/220 after feint settle→walk plant-release destRect leftover", V.THROW_STARTUP===80 && V.THROW_ACTIVE===40 && V.THROW_RECOVERY===220);
check("no 6th button after feint settle→walk plant-release destRect leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after feint settle→walk plant-release destRect leftover", /game.js\?v=350/.test(htmlTxt));

// live: A/D through tele→feint keeps breath 0 mid-stride; arms cutRec on end; AABB planted
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.hp=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cut=""; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.walkSettleT=0; V.player.walkRiseT=0; V.player.walkFadeHold=0; V.player.telegraph=false;
V.rival.phase="idle"; V.rival.wantBlock=false; V.rival.revArmed=-1;
V.player.x=200; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
let fsw276AmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ fsw276AmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.keys.add("KeyD");
for(let i=0;i<20;i++){ holdRivalStill(); V.update(STEP); }
const fsw276WalkReady = V.player.gait===1 && V.recoveryWalkOut(V.player);
V.attackEdge=true;
holdRivalStill(); V.update(STEP);
V.attackEdge=false;
let fsw276DidFeint=false;
for(let i=0;i<14;i++){
  if(!fsw276DidFeint && V.canFeint(V.player)) fsw276DidFeint = !!V.startFeint(V.player);
  holdRivalStill(); V.update(STEP);
  if(V.player.feintT>0){ fsw276DidFeint=true; break; }
}
const fsw276Br0=V.idleBreath(V.player);
const fsw276Rwo=V.recoveryWalkOut(V.player) || V.player.walkFadeHold>0.02 || V.player.gait!==0;
const fsw276Aabb0=V.bodyAABB(V.player).y;
let fsw276Saw=false, fsw276MaxSeat=0, fsw276Armed=false, fsw276Cleared=false, fsw276EndBr=0, fsw276AabbOk=true, fsw276DyHop=0, fsw276Cbt=0;
for(let i=0;i<24;i++){
  holdRivalStill();
  const dy0=V.destRect(V.player).dy;
  const ft0=V.player.feintT;
  const br=V.idleBreath(V.player);
  if(ft0>0){
    fsw276Saw=true;
    fsw276MaxSeat=Math.max(fsw276MaxSeat, Math.abs(br));
  }
  V.update(STEP);
  if(Math.abs(V.bodyAABB(V.player).y-fsw276Aabb0)>0.05) fsw276AabbOk=false;
  fsw276DyHop=Math.max(fsw276DyHop, Math.abs(V.destRect(V.player).dy-dy0));
  if(ft0>0 && V.player.feintT===0){
    fsw276Armed = V.player.cutRecBreathT>0;
    fsw276Cbt = V.player.cutRecBreathT;
  }
  if(fsw276Saw && V.player.feintT===0 && (V.player.cutRecBreathT===0 || i>10)){
    fsw276Cleared=true; fsw276EndBr=V.idleBreath(V.player); break;
  }
}
check("feint settle→walk plant-release destRect leftover live amp ready", fsw276AmpReady && fsw276DidFeint, `ready=${fsw276AmpReady} did=${fsw276DidFeint} modeT=${V.modeT}`);
check("feint settle→walk plant-release destRect leftover live walk-out ready", fsw276WalkReady && Math.abs(fsw276Br0)<0.05 && fsw276Rwo, `walk=${fsw276WalkReady} br0=${fsw276Br0.toFixed(3)} rwo=${fsw276Rwo} postFt=${V.player.feintT}`);
check("feint settle→walk plant-release destRect leftover live saw feint", fsw276Saw && fsw276DidFeint, `saw=${fsw276Saw} did=${fsw276DidFeint}`);
check("feint settle→walk plant-release destRect leftover live no mid-stride seat", fsw276MaxSeat<0.35, `maxSeat=${fsw276MaxSeat.toFixed(3)}`);
check("feint settle→walk plant-release destRect leftover live arms cutRec on end", fsw276Armed && fsw276Cbt>0, `armed=${fsw276Armed} cbt=${fsw276Cbt}`);
check("feint settle→walk plant-release destRect leftover live rise\/cutRec clears", fsw276Cleared && Math.abs(fsw276EndBr)<0.2 && V.player.feintT===0, `cleared=${fsw276Cleared} endBr=${fsw276EndBr.toFixed(3)} ft=${V.player.feintT} cbt=${V.player.cutRecBreathT}`);
check("feint settle→walk plant-release destRect leftover live no breath dump hop", fsw276MaxSeat<0.35 && fsw276DyHop<0.85, `maxSeat=${fsw276MaxSeat.toFixed(3)} hop=${fsw276DyHop.toFixed(3)}`);
check("feint settle→walk plant-release destRect leftover live AABB planted", fsw276AabbOk, `aabbOk=${fsw276AabbOk}`);

// live: idle feint still eases (no cutRec arm)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.hp=100; V.player.stamina=100;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=500; V.rival.x=1100;
let fsw276IdleAmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ fsw276IdleAmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.feintT = V.FEINT_RECOVERY;
V.player.sheatheT=0; V.player.cutRecBreathT=0;
let fsw276IdleBr0=V.idleBreath(V.player), fsw276IdleBr1=fsw276IdleBr0, fsw276IdleUp=true, fsw276IdleArmed=false;
for(let i=0;i<8;i++){
  holdRivalStill(); V.update(STEP);
  if(V.player.cutRecBreathT>0) fsw276IdleArmed=true;
  const br=V.idleBreath(V.player);
  if(Math.abs(br)+0.08<Math.abs(fsw276IdleBr1)) fsw276IdleUp=false;
  fsw276IdleBr1=br;
}
check("feint settle→walk plant-release destRect leftover live idle feint still eases", fsw276IdleAmpReady && fsw276IdleUp && !fsw276IdleArmed && Math.abs(fsw276IdleBr0)<0.25 && Math.abs(fsw276IdleBr1)>0.7 && V.player.feintT===0 && V.player.gait===0, `ready=${fsw276IdleAmpReady} up=${fsw276IdleUp} armed=${fsw276IdleArmed} br0=${fsw276IdleBr0.toFixed(3)} br1=${fsw276IdleBr1.toFixed(3)} ft=${V.player.feintT}`);

// live: rival closing feint keep-0
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft=0; V.hitstopLeft=0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false; V.player.feintT=0; V.player.gait=0;
V.rival.phase="idle"; V.rival.phaseT=0; V.rival.stunT=0; V.rival.guarding=false; V.rival.boltPhase=""; V.rival.falling=false;
V.rival.feintT=0; V.rival.sheatheT=0; V.rival.gait=0; V.rival.walkFadeHold=0; V.rival.cutRecBreathT=0; V.rival.hp=100; V.rival.stamina=100;
V.rival.wantBlock=false; V.rival.guardChoice=-1; V.rival.standGoal=99999;
V.rival.boltCd=99999; V.rival.throwCd=99999; V.rival.feintCd=99999; V.rival.revCd=99999;
V.rival.revArmed=-1; V.rival.feintArmed=-1;
V.player.x=200; V.rival.x=420; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
for(let i=0;i<10;i++){ holdRivalStill(); V.update(STEP); }
V.rival.closing=true;
V.rival.phase="startup"; V.rival.phaseT=30; V.rival.cut="slash"; V.rival.telegraph=true; V.rival.walkFadeHold=1; V.rival.gait=0;
const fsw276RivalCan=V.canFeint(V.rival);
const fsw276RivalDid=fsw276RivalCan && !!V.startFeint(V.rival);
V.rival.closing=true;
const fsw276RivalRwo0=V.recoveryWalkOut(V.rival) || V.rival.walkFadeHold>0.02;
const fsw276RivalBr0=V.idleBreath(V.rival);
let fsw276RivalSaw=false, fsw276RivalMaxSeat=0, fsw276RivalArmed=false;
for(let i=0;i<20;i++){
  V.rival.closing=true; V.rival.wantBlock=false; V.rival.guardChoice=-1; V.rival.standGoal=99999;
  V.rival.boltCd=99999; V.rival.throwCd=99999; V.rival.feintCd=99999; V.rival.revCd=99999; V.rival.guarding=false;
  const ft=V.rival.feintT;
  const br=V.idleBreath(V.rival);
  if(ft>0){
    fsw276RivalSaw=true;
    fsw276RivalMaxSeat=Math.max(fsw276RivalMaxSeat, Math.abs(br));
  }
  const ft0=ft;
  V.update(STEP);
  if(ft0>0 && V.rival.feintT===0 && V.rival.cutRecBreathT>0) fsw276RivalArmed=true;
  if(fsw276RivalSaw && V.rival.feintT===0 && i>4) break;
}
check("feint settle→walk plant-release destRect leftover live rival closing arms", fsw276RivalDid && fsw276RivalSaw && fsw276RivalRwo0 && Math.abs(fsw276RivalBr0)<0.05 && fsw276RivalMaxSeat<0.35, `did=${fsw276RivalDid} saw=${fsw276RivalSaw} rwo0=${fsw276RivalRwo0} br0=${fsw276RivalBr0.toFixed(3)} maxSeat=${fsw276RivalMaxSeat.toFixed(3)} armed=${fsw276RivalArmed} ft=${V.rival.feintT}`);

check("feint settle→walk plant-release destRect leftover rival idleBreath shared", /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "idleBreath")) && /prevFeint > 0/.test(sliceFn(codeOnly, "tickFeint")) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickFeint")) && /f\.feintT > 0/.test(sliceFn(codeOnly, "idleBreath")));
check("cache v=296 after feint settle→walk plant-release destRect leftover live", /game.js\?v=350/.test(htmlTxt));




// --- v277 wakeup settle→walk plant-release destRect leftover: keep 0 through walkFadeHold / arm cutRec ---
check("wakeup settle→walk plant-release destRect leftover seats no breath mid-stride", /Wakeup settle→walk plant-release destRect leftover/.test(srcTxt) && /leftover wakeupFade breath used to seat ~1\.4/.test(srcTxt) && /only gated walking\(f\), not walkFadeHold/.test(srcTxt) && /restWakeWalk/.test(srcTxt) && /Same[\s\S]{0,80}hole feint \/ telegraph \/ chip stun \/ bolt \/ throw/.test(srcTxt) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "idleBreath")));
check("wakeup settle→walk plant-release destRect leftover is destRect-only", /destRect-only \(idleBreath \/ cutRecBreathT\)/.test(srcTxt) && /Keep breath 0 through walking \/ walk-out \/ leftover/.test(srcTxt) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\) return 0/.test(sliceFn(codeOnly, "idleBreath")) && /prevInv > 0/.test(sliceFn(codeOnly, "tickThrowState")) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickThrowState")) && !/walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "destRect")) && !/walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "bodyAABB")) && !/walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "poseBitmap")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "wakeupFade")) && !/prevInv/.test(sliceFn(codeOnly, "destRect")) && !/prevInv/.test(sliceFn(codeOnly, "bodyAABB")));
check("wakeup settle→walk plant-release destRect leftover keeps 0 through walk-out getup", /if \(walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\) return 0/.test(sliceFn(codeOnly, "idleBreath")) && /if \(wakeupFade\(f\) > 0/.test(sliceFn(codeOnly, "idleBreath")) && /tickThrowState arm cutRecBreathT when throw-invuln ends[\s\S]{0,40}mid-stride/.test(srcTxt));
check("wakeup settle→walk plant-release destRect leftover idle getup still eases", /Idle getup still eases with wakeupFade/.test(srcTxt) && (/return amp \* \(1 - wakeupFade\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) || /return amp \* \(1 - Math\.max\(wakeupFade\(f\), hurtFade\(f\)\)\)/.test(sliceFn(codeOnly, "idleBreath"))) && /wakeupFade\(f\) > 0/.test(sliceFn(codeOnly, "idleBreath")));
check("wakeup settle→walk plant-release destRect leftover arms cutRec on end", /prevInv > 0/.test(sliceFn(codeOnly, "tickThrowState")) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickThrowState")) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| f\.guarding \|\| f\.guardPoseK > 0/.test(sliceFn(codeOnly, "tickThrowState")));
check("wakeup settle→walk plant-release destRect leftover settle→guard unchanged", /if \(f\.guarding \|\| f\.guardPoseK > 0\) return 0;/.test(sliceFn(codeOnly, "idleBreath")) && /Wakeup settle→guard unchanged/.test(srcTxt));
check("wakeup settle→walk plant-release destRect leftover feint\/tele\/chip\/bolt\/throw settle→walk unchanged", /recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "idleBreath")) && /if \(telegraphing\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /if \(f\.stunT > 0\)/.test(sliceFn(codeOnly, "idleBreath")) && /f\.boltPhase === "recovery"/.test(sliceFn(codeOnly, "idleBreath")) && /f\.cut === "throw" && f\.phase === "recovery"/.test(sliceFn(codeOnly, "idleBreath")) && /f\.feintT > 0/.test(sliceFn(codeOnly, "idleBreath")));
check("wakeup settle→walk plant-release destRect leftover restWakeWalk \/ wakeupFade unchanged", /restWakeWalk/.test(srcTxt) && /function wakeupFade/.test(srcTxt) && /THROW_WAKE_INVULN/.test(srcTxt) && V.THROW_WAKE_INVULN===80);
check("wakeup settle→walk plant-release destRect leftover tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("wakeup settle→walk plant-release destRect leftover is not a new combat verb", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.HITSTUN===350 && V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140 && V.FEINT_RECOVERY===100 && V.WALK_SETTLE_MS===160 && V.THROW_WAKE_INVULN===80);
check("slash frames still 180/140/280 after wakeup settle→walk plant-release destRect leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after wakeup settle→walk plant-release destRect leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after wakeup settle→walk plant-release destRect leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("throw frames still 80/40/220 after wakeup settle→walk plant-release destRect leftover", V.THROW_STARTUP===80 && V.THROW_ACTIVE===40 && V.THROW_RECOVERY===220);
check("no 6th button after wakeup settle→walk plant-release destRect leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after wakeup settle→walk plant-release destRect leftover", /game.js\?v=350/.test(htmlTxt));

// live: A/D through getup keeps breath 0 mid-stride; arms cutRec on end; AABB planted
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.hp=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cut=""; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.walkSettleT=0; V.player.walkRiseT=0; V.player.walkFadeHold=0; V.player.telegraph=false;
V.player.thrownT=0; V.player.throwInvulnT=0; V.player.wakeRev=false; V.player.wakeRevFadeHold=0;
V.rival.phase="idle"; V.rival.wantBlock=false; V.rival.revArmed=-1;
V.player.x=200; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
let wsw277AmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ wsw277AmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.throwInvulnT = V.THROW_WAKE_INVULN;
V.player.wakeRev=true; V.player.thrownT=0; V.player.stunT=0; V.player.cutRecBreathT=0;
V.player.phase="idle"; V.player.gait=0; V.player.walkRiseT=0; V.player.walkFadeHold=0;
V.keys.add("KeyD");
const wsw277Br0=V.idleBreath(V.player);
const wsw277Aabb0=V.bodyAABB(V.player).y;
let wsw277Saw=false, wsw277MaxSeat=0, wsw277Armed=false, wsw277Cleared=false, wsw277EndBr=0, wsw277AabbOk=true, wsw277DyHop=0, wsw277Cbt=0, wsw277WalkReady=false, wsw277Rwo=false;
for(let i=0;i<36;i++){
  holdRivalStill();
  const dy0=V.destRect(V.player).dy;
  const inv0=V.player.throwInvulnT;
  const br=V.idleBreath(V.player);
  if(V.player.gait===1 || V.recoveryWalkOut(V.player) || V.player.walkFadeHold>0.02){
    wsw277WalkReady=true;
    wsw277Rwo=true;
  }
  if(inv0>0 || V.wakeupFade(V.player)>0){
    wsw277Saw=true;
    if(V.player.gait===1 || V.recoveryWalkOut(V.player) || V.player.walkFadeHold>0.02){
      wsw277MaxSeat=Math.max(wsw277MaxSeat, Math.abs(br));
    }
  }
  V.update(STEP);
  if(Math.abs(V.bodyAABB(V.player).y-wsw277Aabb0)>0.05) wsw277AabbOk=false;
  wsw277DyHop=Math.max(wsw277DyHop, Math.abs(V.destRect(V.player).dy-dy0));
  if(inv0>0 && V.player.throwInvulnT===0){
    wsw277Armed = V.player.cutRecBreathT>0;
    wsw277Cbt = V.player.cutRecBreathT;
    wsw277Cleared=true;
    wsw277EndBr=br;
  }
  if(wsw277Cleared && V.player.cutRecBreathT===0 && V.player.gait===1 && Math.abs(V.idleBreath(V.player))<0.05){ wsw277EndBr=V.idleBreath(V.player); break; }
  if(wsw277Cleared && i>16){ wsw277EndBr=V.idleBreath(V.player); break; }
}
check("wakeup settle→walk plant-release destRect leftover live amp ready", wsw277AmpReady, `ready=${wsw277AmpReady} modeT=${V.modeT}`);
check("wakeup settle→walk plant-release destRect leftover live walk-out ready", wsw277WalkReady && Math.abs(wsw277Br0)<0.05 && wsw277Rwo, `walk=${wsw277WalkReady} br0=${wsw277Br0.toFixed(3)} rwo=${wsw277Rwo} postInv=${V.player.throwInvulnT}`);
check("wakeup settle→walk plant-release destRect leftover live saw getup", wsw277Saw, `saw=${wsw277Saw}`);
check("wakeup settle→walk plant-release destRect leftover live no mid-stride seat", wsw277MaxSeat<0.35, `maxSeat=${wsw277MaxSeat.toFixed(3)}`);
check("wakeup settle→walk plant-release destRect leftover live arms cutRec on end", wsw277Armed && wsw277Cbt>0, `armed=${wsw277Armed} cbt=${wsw277Cbt}`);
check("wakeup settle→walk plant-release destRect leftover live rise\/cutRec clears", wsw277Cleared && Math.abs(wsw277EndBr)<0.2 && V.player.throwInvulnT===0, `cleared=${wsw277Cleared} endBr=${wsw277EndBr.toFixed(3)} inv=${V.player.throwInvulnT} cbt=${V.player.cutRecBreathT}`);
check("wakeup settle→walk plant-release destRect leftover live no breath dump hop", wsw277MaxSeat<0.35 && wsw277DyHop<6.5, `maxSeat=${wsw277MaxSeat.toFixed(3)} hop=${wsw277DyHop.toFixed(3)}`);
check("wakeup settle→walk plant-release destRect leftover live AABB planted", wsw277AabbOk, `aabbOk=${wsw277AabbOk}`);

// live: walkFadeHold-only (no gait) keep-0 through getup plant
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.gait=0; V.player.walkRiseT=0; V.player.walkSettleT=0; V.player.hp=100; V.player.stamina=100;
V.player.thrownT=0; V.player.throwInvulnT=0; V.player.wakeRev=false; V.player.wakeRevFadeHold=0;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=500; V.rival.x=1100;
let wsw277WfhAmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ wsw277WfhAmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.throwInvulnT = V.THROW_WAKE_INVULN;
V.player.wakeRev=true; V.player.phase="idle"; V.player.cutRecBreathT=0;
V.player.gait=0; V.player.walkFadeHold=1; V.player.walkRiseT=0;
const wsw277Wfh0=V.player.walkFadeHold;
const wsw277WfhBr0=V.idleBreath(V.player);
let wsw277WfhMaxSeat=0, wsw277WfhArmed=false, wsw277WfhCbt=0;
for(let i=0;i<20;i++){
  holdRivalStill();
  V.player.gait=0; // hold no-walking while fade plant lives
  if(V.player.walkFadeHold<0.05) V.player.walkFadeHold=1;
  const inv0=V.player.throwInvulnT;
  const br=V.idleBreath(V.player);
  if(inv0>0 || V.wakeupFade(V.player)>0) wsw277WfhMaxSeat=Math.max(wsw277WfhMaxSeat, Math.abs(br));
  V.update(STEP);
  if(inv0>0 && V.player.throwInvulnT===0){
    wsw277WfhArmed = V.player.cutRecBreathT>0;
    wsw277WfhCbt = V.player.cutRecBreathT;
  }
  if(V.player.throwInvulnT===0 && i>4) break;
}
check("wakeup settle→walk plant-release destRect leftover live walkFadeHold keep-0", wsw277WfhAmpReady && wsw277Wfh0>0.9 && Math.abs(wsw277WfhBr0)<0.05 && wsw277WfhMaxSeat<0.35, `ready=${wsw277WfhAmpReady} wfh0=${wsw277Wfh0.toFixed(3)} br0=${wsw277WfhBr0.toFixed(3)} maxSeat=${wsw277WfhMaxSeat.toFixed(3)}`);
check("wakeup settle→walk plant-release destRect leftover live walkFadeHold arms cutRec", wsw277WfhArmed && wsw277WfhCbt>0, `armed=${wsw277WfhArmed} cbt=${wsw277WfhCbt}`);

// live: idle getup still eases (no cutRec arm)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.hp=100; V.player.stamina=100;
V.player.thrownT=0; V.player.throwInvulnT=0; V.player.wakeRev=false; V.player.wakeRevFadeHold=0;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=500; V.rival.x=1100;
let wsw277IdleAmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ wsw277IdleAmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.throwInvulnT = V.THROW_WAKE_INVULN;
V.player.wakeRev=true; V.player.phase="idle"; V.player.cutRecBreathT=0; V.player.gait=0; V.player.walkFadeHold=0;
let wsw277IdleBr0=V.idleBreath(V.player), wsw277IdleBr1=wsw277IdleBr0, wsw277IdleUp=false, wsw277IdleArmed=false;
for(let i=0;i<16;i++){
  holdRivalStill(); V.update(STEP);
  if(V.player.throwInvulnT===0 && V.player.cutRecBreathT>0) wsw277IdleArmed=true;
  const br=V.idleBreath(V.player);
  if(V.player.throwInvulnT===0 && V.player.cutRecBreathT===0 && Math.abs(br)>Math.abs(wsw277IdleBr1)+0.05) wsw277IdleUp=true;
  if(V.wakeupFade(V.player)>0 && Math.abs(br)>Math.abs(wsw277IdleBr1)+0.05) wsw277IdleUp=true;
  wsw277IdleBr1=br;
}
check("wakeup settle→walk plant-release destRect leftover live idle getup still eases", wsw277IdleAmpReady && !wsw277IdleArmed && wsw277IdleUp && Math.abs(wsw277IdleBr0)<0.05 && Math.abs(wsw277IdleBr1)>0.4 && V.player.gait===0, `ready=${wsw277IdleAmpReady} up=${wsw277IdleUp} armed=${wsw277IdleArmed} br0=${wsw277IdleBr0.toFixed(3)} br1=${wsw277IdleBr1.toFixed(3)} inv=${V.player.throwInvulnT}`);

// live: rival closing walkFadeHold keep-0 through getup
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft=0; V.hitstopLeft=0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false; V.player.feintT=0; V.player.gait=0;
V.rival.phase="idle"; V.rival.phaseT=0; V.rival.stunT=0; V.rival.guarding=false; V.rival.boltPhase=""; V.rival.falling=false;
V.rival.feintT=0; V.rival.sheatheT=0; V.rival.gait=0; V.rival.walkFadeHold=0; V.rival.cutRecBreathT=0; V.rival.hp=100; V.rival.stamina=100;
V.rival.wantBlock=false; V.rival.guardChoice=-1; V.rival.standGoal=99999;
V.rival.boltCd=99999; V.rival.throwCd=99999; V.rival.feintCd=99999; V.rival.revCd=99999;
V.rival.revArmed=-1; V.rival.feintArmed=-1;
V.rival.thrownT=0; V.rival.throwInvulnT=0; V.rival.wakeRev=false; V.rival.wakeRevFadeHold=0;
V.player.x=200; V.rival.x=420; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
for(let i=0;i<10;i++){ holdRivalStill(); V.update(STEP); }
V.rival.closing=true;
V.rival.throwInvulnT = V.THROW_WAKE_INVULN;
V.rival.wakeRev=true; V.rival.phase="idle"; V.rival.gait=0; V.rival.walkFadeHold=1; V.rival.cutRecBreathT=0;
const wsw277RivalRwo0=V.recoveryWalkOut(V.rival) || V.rival.walkFadeHold>0.02;
const wsw277RivalBr0=V.idleBreath(V.rival);
let wsw277RivalSaw=false, wsw277RivalMaxSeat=0, wsw277RivalArmed=false;
for(let i=0;i<20;i++){
  V.rival.closing=true; V.rival.wantBlock=false; V.rival.guardChoice=-1; V.rival.standGoal=99999;
  V.rival.boltCd=99999; V.rival.throwCd=99999; V.rival.feintCd=99999; V.rival.revCd=99999; V.rival.guarding=false;
  if(V.rival.gait!==0) V.rival.gait=0;
  if(V.rival.walkFadeHold<0.05) V.rival.walkFadeHold=1;
  const inv=V.rival.throwInvulnT;
  const br=V.idleBreath(V.rival);
  if(inv>0 || V.wakeupFade(V.rival)>0){
    wsw277RivalSaw=true;
    wsw277RivalMaxSeat=Math.max(wsw277RivalMaxSeat, Math.abs(br));
  }
  const inv0=inv;
  V.update(STEP);
  if(inv0>0 && V.rival.throwInvulnT===0 && V.rival.cutRecBreathT>0) wsw277RivalArmed=true;
  if(wsw277RivalSaw && V.rival.throwInvulnT===0 && i>4) break;
}
check("wakeup settle→walk plant-release destRect leftover live rival closing arms", wsw277RivalSaw && wsw277RivalRwo0 && Math.abs(wsw277RivalBr0)<0.05 && wsw277RivalMaxSeat<0.35, `saw=${wsw277RivalSaw} rwo0=${wsw277RivalRwo0} br0=${wsw277RivalBr0.toFixed(3)} maxSeat=${wsw277RivalMaxSeat.toFixed(3)} armed=${wsw277RivalArmed} inv=${V.rival.throwInvulnT}`);

check("wakeup settle→walk plant-release destRect leftover rival idleBreath shared", /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "idleBreath")) && /prevInv > 0/.test(sliceFn(codeOnly, "tickThrowState")) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickThrowState")) && /wakeupFade\(f\) > 0/.test(sliceFn(codeOnly, "idleBreath")));
check("cache v=296 after wakeup settle→walk plant-release destRect leftover live", /game.js\?v=350/.test(htmlTxt));



// --- v278 chip stun settle→walk plant-release destRect leftover: keep 0 through walkFadeHold / arm cutRec ---
check("chip stun settle→walk plant-release destRect leftover seats no breath mid-stride", /Chip stun settle→walk plant-release destRect leftover/.test(srcTxt) && /leftover stun-end breath ease-in used to/.test(srcTxt) && /only gated recoveryWalkOut \/ guard, not walking \/ walkFadeHold/.test(srcTxt) && /Same[\s\S]{0,80}hole wakeup \/ feint settle→walk plant-release/.test(srcTxt) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "idleBreath")));
check("chip stun settle→walk plant-release destRect leftover is destRect-only", /destRect-only \(idleBreath \/ cutRecBreathT\)/.test(srcTxt) && /Keep breath 0 through walking \/ walk-out \/ leftover/.test(srcTxt) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\) return 0/.test(sliceFn(codeOnly, "idleBreath")) && /prevStun > 0/.test(sliceFn(codeOnly, "tickStun")) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickStun")) && !/walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "destRect")) && !/walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "bodyAABB")) && !/walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "poseBitmap")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "stunLeftoverPlanting")) && !/prevStun/.test(sliceFn(codeOnly, "destRect")) && !/prevStun/.test(sliceFn(codeOnly, "bodyAABB")));
check("chip stun settle→walk plant-release destRect leftover keeps 0 through walk-out stun-end", /if \(walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\) return 0/.test(sliceFn(codeOnly, "idleBreath")) && /if \(f\.stunT > 0\)/.test(sliceFn(codeOnly, "idleBreath")) && /tickStun arm cutRecBreathT when chip stun ends[\s\S]{0,40}mid-stride/.test(srcTxt));
check("chip stun settle→walk plant-release destRect leftover idle chip stun still eases", /Idle chip stun still eases with stunT\/GUARD_RAISE_MS/.test(srcTxt) && /f\.stunT \/ GUARD_RAISE_MS/.test(sliceFn(codeOnly, "idleBreath")) && /stun end unchanged|still eases with stunT/.test(srcTxt));
check("chip stun settle→walk plant-release destRect leftover arms cutRec on end", /prevStun > 0/.test(sliceFn(codeOnly, "tickStun")) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickStun")) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| f\.guarding \|\| f\.guardPoseK > 0/.test(sliceFn(codeOnly, "tickStun")));
check("chip stun settle→walk plant-release destRect leftover settle→guard unchanged", /if \(f\.guarding \|\| f\.guardPoseK > 0\) return 0;/.test(sliceFn(codeOnly, "idleBreath")) && /Chip stun settle→guard unchanged/.test(srcTxt));
check("chip stun settle→walk plant-release destRect leftover wakeup\/feint\/tele\/bolt\/throw settle→walk unchanged", /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "idleBreath")) && /if \(telegraphing\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /if \(wakeupFade\(f\) > 0/.test(sliceFn(codeOnly, "idleBreath")) && /f\.boltPhase === "recovery"/.test(sliceFn(codeOnly, "idleBreath")) && /f\.cut === "throw" && f\.phase === "recovery"/.test(sliceFn(codeOnly, "idleBreath")) && /f\.feintT > 0/.test(sliceFn(codeOnly, "idleBreath")));
check("chip stun settle→walk plant-release destRect leftover throw KD \/ wakeup still hard-zero", /Throw KD \/ wakeup invuln still hard-zero/.test(srcTxt) && /f\.thrownT > 0 \|\| f\.throwInvulnT > 0 \|\| f\.hp <= 0/.test(sliceFn(codeOnly, "idleBreath")) && /wakeupFade owns getup/.test(srcTxt));
check("chip stun settle→walk plant-release destRect leftover tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("chip stun settle→walk plant-release destRect leftover is not a new combat verb", V.HITSTUN===350 && V.HITSTOP_HIT===140 && V.GUARD_RAISE_MS===80 && V.THROW_WAKE_INVULN===80 && V.THROW_KD_MS===520 && V.WALK_SETTLE_MS===160 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.SHEATHE_MS===140 && V.FEINT_RECOVERY===100);
check("slash frames still 180/140/280 after chip stun settle→walk plant-release destRect leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after chip stun settle→walk plant-release destRect leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after chip stun settle→walk plant-release destRect leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("throw frames still 80/40/220 after chip stun settle→walk plant-release destRect leftover", V.THROW_STARTUP===80 && V.THROW_ACTIVE===40 && V.THROW_RECOVERY===220);
check("no 6th button after chip stun settle→walk plant-release destRect leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after chip stun settle→walk plant-release destRect leftover", /game.js\?v=350/.test(htmlTxt));

// live: A/D through chip stun keeps breath 0 mid-stride; arms cutRec on end; AABB planted
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.hp=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cut="slash"; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.walkSettleT=0; V.player.walkRiseT=0; V.player.walkFadeHold=0; V.player.telegraph=false;
V.player.boltHoldCut=false; V.player.boltSuper=false;
V.player.thrownT=0; V.player.throwInvulnT=0; V.player.wakeRev=false;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=200; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
let cssw278AmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ cssw278AmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.stunT = V.HITSTUN;
V.player.phase="idle"; V.player.boltPhase=""; V.player.feintT=0; V.player.sheatheT=0;
V.player.cutRecBreathT=0; V.player.gait=0; V.player.walkRiseT=0; V.player.walkFadeHold=0; V.player.guarding=false; V.player.guardPoseK=0;
V.player.thrownT=0; V.player.throwInvulnT=0; V.player.hp=100;
V.keys.add("KeyD");
const cssw278Br0=V.idleBreath(V.player);
const cssw278Rwo0=V.recoveryWalkOut(V.player);
const cssw278Aabb0=V.bodyAABB(V.player).y;
let cssw278Saw=false, cssw278MaxSeat=0, cssw278Armed=false, cssw278Cleared=false, cssw278EndBr=0, cssw278AabbOk=true, cssw278DyHop=0, cssw278Cbt=0, cssw278WalkReady=false;
for(let i=0;i<50;i++){
  holdRivalStill();
  V.keys.add("KeyD");
  const dy0=V.destRect(V.player).dy;
  const st0=V.player.stunT;
  const br=V.idleBreath(V.player);
  if(V.recoveryWalkOut(V.player) || V.player.walkFadeHold>0.02 || V.player.gait===1){
    cssw278WalkReady=true;
  }
  if(st0>0){
    cssw278Saw=true;
    if(V.recoveryWalkOut(V.player) || V.player.walkFadeHold>0.02 || V.player.gait===1){
      cssw278MaxSeat=Math.max(cssw278MaxSeat, Math.abs(br));
    }
  }
  V.update(STEP);
  if(Math.abs(V.bodyAABB(V.player).y-cssw278Aabb0)>0.05) cssw278AabbOk=false;
  cssw278DyHop=Math.max(cssw278DyHop, Math.abs(V.destRect(V.player).dy-dy0));
  if(V.player.gait===1) cssw278MaxSeat=Math.max(cssw278MaxSeat, Math.abs(V.idleBreath(V.player)));
  if(st0>0 && V.player.stunT===0){
    cssw278Armed = V.player.cutRecBreathT>0;
    cssw278Cbt = V.player.cutRecBreathT;
    cssw278Cleared=true;
    cssw278EndBr=br;
  }
  if(cssw278Cleared && V.player.cutRecBreathT===0 && V.player.gait===1 && Math.abs(V.idleBreath(V.player))<0.05){ cssw278EndBr=V.idleBreath(V.player); break; }
  if(cssw278Cleared && i>16){ cssw278EndBr=V.idleBreath(V.player); break; }
}
check("chip stun settle→walk plant-release destRect leftover live amp ready", cssw278AmpReady, `ready=${cssw278AmpReady} modeT=${V.modeT}`);
check("chip stun settle→walk plant-release destRect leftover live walk-out ready", cssw278WalkReady && Math.abs(cssw278Br0)<0.05 && cssw278Rwo0, `walk=${cssw278WalkReady} br0=${cssw278Br0.toFixed(3)} rwo=${cssw278Rwo0} postSt=${V.player.stunT}`);
check("chip stun settle→walk plant-release destRect leftover live saw stun", cssw278Saw, `saw=${cssw278Saw}`);
check("chip stun settle→walk plant-release destRect leftover live no mid-stride seat", cssw278MaxSeat<0.35, `maxSeat=${cssw278MaxSeat.toFixed(3)}`);
check("chip stun settle→walk plant-release destRect leftover live arms cutRec on end", cssw278Armed && cssw278Cbt>0, `armed=${cssw278Armed} cbt=${cssw278Cbt}`);
check("chip stun settle→walk plant-release destRect leftover live rise\/cutRec clears", cssw278Cleared && Math.abs(cssw278EndBr)<0.2 && V.player.stunT===0, `cleared=${cssw278Cleared} endBr=${cssw278EndBr.toFixed(3)} st=${V.player.stunT} cbt=${V.player.cutRecBreathT}`);
check("chip stun settle→walk plant-release destRect leftover live no breath dump hop", cssw278MaxSeat<0.35 && cssw278DyHop<6.5, `maxSeat=${cssw278MaxSeat.toFixed(3)} hop=${cssw278DyHop.toFixed(3)}`);
check("chip stun settle→walk plant-release destRect leftover live AABB planted", cssw278AabbOk, `aabbOk=${cssw278AabbOk}`);

// live: walkFadeHold-only (no gait / no A/D) keep-0 through stun plant
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.gait=0; V.player.walkRiseT=0; V.player.walkSettleT=0; V.player.hp=100; V.player.stamina=100;
V.player.thrownT=0; V.player.throwInvulnT=0; V.player.wakeRev=false;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=500; V.rival.x=1100;
let cssw278WfhAmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ cssw278WfhAmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.stunT = V.GUARD_RAISE_MS; // in ease-in window so breath would seat without hold
V.player.phase="idle"; V.player.cutRecBreathT=0;
V.player.gait=0; V.player.walkFadeHold=1; V.player.walkRiseT=0;
V.player.thrownT=0; V.player.throwInvulnT=0; V.player.hp=100; V.player.guarding=false; V.player.guardPoseK=0;
const cssw278Wfh0=V.player.walkFadeHold;
const cssw278WfhBr0=V.idleBreath(V.player);
let cssw278WfhMaxSeat=0, cssw278WfhArmed=false, cssw278WfhCbt=0;
for(let i=0;i<20;i++){
  holdRivalStill();
  V.keys.clear();
  V.player.gait=0; // hold no-walking while fade plant lives
  if(V.player.walkFadeHold<0.05) V.player.walkFadeHold=1;
  const st0=V.player.stunT;
  const br=V.idleBreath(V.player);
  if(st0>0) cssw278WfhMaxSeat=Math.max(cssw278WfhMaxSeat, Math.abs(br));
  V.update(STEP);
  if(st0>0 && V.player.stunT===0){
    cssw278WfhArmed = V.player.cutRecBreathT>0;
    cssw278WfhCbt = V.player.cutRecBreathT;
  }
  if(V.player.stunT===0 && i>4) break;
}
check("chip stun settle→walk plant-release destRect leftover live walkFadeHold keep-0", cssw278WfhAmpReady && cssw278Wfh0>0.9 && Math.abs(cssw278WfhBr0)<0.05 && cssw278WfhMaxSeat<0.35, `ready=${cssw278WfhAmpReady} wfh0=${cssw278Wfh0.toFixed(3)} br0=${cssw278WfhBr0.toFixed(3)} maxSeat=${cssw278WfhMaxSeat.toFixed(3)}`);
check("chip stun settle→walk plant-release destRect leftover live walkFadeHold arms cutRec", cssw278WfhArmed && cssw278WfhCbt>0, `armed=${cssw278WfhArmed} cbt=${cssw278WfhCbt}`);

// live: idle chip stun still eases (no cutRec arm)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.hp=100; V.player.stamina=100;
V.player.thrownT=0; V.player.throwInvulnT=0;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=500; V.rival.x=1100;
let cssw278IdleAmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ cssw278IdleAmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.stunT = V.GUARD_RAISE_MS;
V.player.cutRecBreathT=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.thrownT=0; V.player.throwInvulnT=0; V.player.hp=100;
let cssw278IdleBr0=V.idleBreath(V.player), cssw278IdleBr1=cssw278IdleBr0, cssw278IdleUp=false, cssw278IdleArmed=false;
for(let i=0;i<24;i++){
  holdRivalStill(); V.update(STEP);
  if(V.player.stunT===0 && V.player.cutRecBreathT>0) cssw278IdleArmed=true;
  const br=V.idleBreath(V.player);
  if(V.player.stunT>0 && Math.abs(br)>Math.abs(cssw278IdleBr1)+0.05) cssw278IdleUp=true;
  if(V.player.stunT===0 && V.player.cutRecBreathT===0 && Math.abs(br)>Math.abs(cssw278IdleBr1)+0.05) cssw278IdleUp=true;
  cssw278IdleBr1=br;
  if(V.player.stunT===0 && i>4) break;
}
check("chip stun settle→walk plant-release destRect leftover live idle chip stun still eases", cssw278IdleAmpReady && !cssw278IdleArmed && cssw278IdleUp && Math.abs(cssw278IdleBr0)<0.05 && Math.abs(cssw278IdleBr1)>0.4 && V.player.gait===0, `ready=${cssw278IdleAmpReady} up=${cssw278IdleUp} armed=${cssw278IdleArmed} br0=${cssw278IdleBr0.toFixed(3)} br1=${cssw278IdleBr1.toFixed(3)} st=${V.player.stunT}`);

// live: rival closing walkFadeHold keep-0 through chip stun
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft=0; V.hitstopLeft=0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false; V.player.feintT=0; V.player.gait=0;
V.rival.phase="idle"; V.rival.phaseT=0; V.rival.stunT=0; V.rival.guarding=false; V.rival.boltPhase=""; V.rival.falling=false;
V.rival.feintT=0; V.rival.sheatheT=0; V.rival.gait=0; V.rival.walkFadeHold=0; V.rival.cutRecBreathT=0; V.rival.hp=100; V.rival.stamina=100;
V.rival.wantBlock=false; V.rival.guardChoice=-1; V.rival.standGoal=99999;
V.rival.boltCd=99999; V.rival.throwCd=99999; V.rival.feintCd=99999; V.rival.revCd=99999;
V.rival.revArmed=-1; V.rival.feintArmed=-1;
V.rival.thrownT=0; V.rival.throwInvulnT=0; V.rival.wakeRev=false;
V.player.x=200; V.rival.x=420; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
for(let i=0;i<10;i++){ holdRivalStill(); V.update(STEP); }
V.rival.closing=true;
V.rival.stunT = V.GUARD_RAISE_MS;
V.rival.phase="idle"; V.rival.gait=0; V.rival.walkFadeHold=1; V.rival.cutRecBreathT=0;
V.rival.thrownT=0; V.rival.throwInvulnT=0; V.rival.guarding=false; V.rival.guardPoseK=0; V.rival.hp=100;
const cssw278RivalRwo0=V.recoveryWalkOut(V.rival) || V.rival.walkFadeHold>0.02;
const cssw278RivalBr0=V.idleBreath(V.rival);
let cssw278RivalSaw=false, cssw278RivalMaxSeat=0, cssw278RivalArmed=false;
for(let i=0;i<20;i++){
  V.rival.closing=true; V.rival.wantBlock=false; V.rival.guardChoice=-1; V.rival.standGoal=99999;
  V.rival.boltCd=99999; V.rival.throwCd=99999; V.rival.feintCd=99999; V.rival.revCd=99999; V.rival.guarding=false;
  if(V.rival.gait!==0) V.rival.gait=0;
  if(V.rival.walkFadeHold<0.05) V.rival.walkFadeHold=1;
  const st=V.rival.stunT;
  const br=V.idleBreath(V.rival);
  if(st>0){
    cssw278RivalSaw=true;
    cssw278RivalMaxSeat=Math.max(cssw278RivalMaxSeat, Math.abs(br));
  }
  const st0=st;
  V.update(STEP);
  if(st0>0 && V.rival.stunT===0 && V.rival.cutRecBreathT>0) cssw278RivalArmed=true;
  if(cssw278RivalSaw && V.rival.stunT===0 && i>4) break;
}
check("chip stun settle→walk plant-release destRect leftover live rival closing arms", cssw278RivalSaw && cssw278RivalRwo0 && Math.abs(cssw278RivalBr0)<0.05 && cssw278RivalMaxSeat<0.35, `saw=${cssw278RivalSaw} rwo0=${cssw278RivalRwo0} br0=${cssw278RivalBr0.toFixed(3)} maxSeat=${cssw278RivalMaxSeat.toFixed(3)} armed=${cssw278RivalArmed} st=${V.rival.stunT}`);

check("chip stun settle→walk plant-release destRect leftover rival idleBreath shared", /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "idleBreath")) && /prevStun > 0/.test(sliceFn(codeOnly, "tickStun")) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickStun")) && /if \(f\.stunT > 0\)/.test(sliceFn(codeOnly, "idleBreath")));
check("cache v=296 after chip stun settle→walk plant-release destRect leftover live", /game.js\?v=350/.test(htmlTxt));




// --- v279 throw+bolt recovery settle→walk plant-release destRect leftover: keep 0 through walkFadeHold / arm cutRec (both siblings) ---
check("throw recovery settle→walk plant-release destRect leftover seats no breath mid-stride", /Throw recovery settle→walk plant-release destRect leftover/.test(srcTxt) && /leftover throwPlantFade breath used to/.test(srcTxt) && /only gated recoveryWalkOut \/ guard, not walking \/ walkFadeHold/.test(srcTxt) && /Same[\s\S]{0,80}hole chip stun \/ wakeup \/ feint settle→walk plant-release/.test(srcTxt) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "idleBreath")));
check("throw recovery settle→walk plant-release destRect leftover is destRect-only", /destRect-only \(idleBreath \/ cutRecBreathT\)/.test(srcTxt) && /Keep breath 0 through walking \/ walk-out \/ leftover/.test(srcTxt) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\) return 0/.test(sliceFn(codeOnly, "idleBreath")) && /wasThrow && \(walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| f\.guarding \|\| f\.guardPoseK > 0\)/.test(sliceFn(codeOnly, "advanceAttack")) && /else if \(walkOutEnd\) f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "advanceAttack")) && !/walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "destRect")) && !/walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "bodyAABB")) && !/walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "poseBitmap")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "throwPlantFade")) && !/walkOutEnd/.test(sliceFn(codeOnly, "destRect")));
check("throw recovery settle→walk plant-release destRect leftover keeps 0 through walk-out throw plant", /if \(f\.cut === "throw" && f\.phase === "recovery"\)/.test(sliceFn(codeOnly, "idleBreath")) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\) return 0/.test(sliceFn(codeOnly, "idleBreath")) && /advanceAttack arm cutRecBreathT when throw recovery ends[\s\S]{0,40}mid-stride/.test(srcTxt));
check("throw recovery settle→walk plant-release destRect leftover idle throw plant still eases", /Idle throw plant still eases with throwPlantFade/.test(srcTxt) && /return amp \* \(1 - throwPlantFade\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /throw plant end unchanged|still eases with throwPlantFade/.test(srcTxt));
check("throw recovery settle→walk plant-release destRect leftover arms cutRec on end", /else if \(walkOutEnd\) f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "advanceAttack")) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| f\.guarding \|\| f\.guardPoseK > 0/.test(sliceFn(codeOnly, "advanceAttack")) && /wasThrow && \(walking\(f\)/.test(sliceFn(codeOnly, "advanceAttack")));
check("throw recovery settle→walk plant-release destRect leftover settle→guard unchanged", /if \(f\.guarding \|\| f\.guardPoseK > 0\) return 0;/.test(sliceFn(codeOnly, "idleBreath")) && /Throw recovery settle→guard unchanged/.test(srcTxt));
check("bolt recovery settle→walk plant-release destRect leftover seats no breath mid-stride", /Bolt recovery settle→walk plant-release destRect leftover/.test(srcTxt) && /leftover boltPlantFade breath used to/.test(srcTxt) && /only gated recoveryWalkOut \/ guard, not walking \/ walkFadeHold/.test(srcTxt) && /Same[\s\S]{0,80}hole throw \/ chip stun \/ wakeup settle→walk plant-release/.test(srcTxt) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "idleBreath")));
check("bolt recovery settle→walk plant-release destRect leftover is destRect-only", /destRect-only \(idleBreath \/ cutRecBreathT\)/.test(srcTxt) && /Keep breath 0 through walking \/ walk-out \/ leftover/.test(srcTxt) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| f\.guarding \|\| f\.guardPoseK > 0/.test(sliceFn(codeOnly, "advanceBoltFighter")) && /else if \(walkOutEnd\) f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "advanceBoltFighter")) && /f\.boltPhase === "recovery" && !holdingCutBolt\(f\)/.test(sliceFn(codeOnly, "idleBreath")) && !/cutRecBreathT/.test(sliceFn(codeOnly, "boltPlantFade")) && !/walkOutEnd/.test(sliceFn(codeOnly, "bodyAABB")));
check("bolt recovery settle→walk plant-release destRect leftover keeps 0 through walk-out knife plant", /if \(f\.boltPhase === "recovery" && !holdingCutBolt\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\) return 0/.test(sliceFn(codeOnly, "idleBreath")) && /advanceBoltFighter arm cutRecBreathT when bolt recovery ends[\s\S]{0,40}mid-stride/.test(srcTxt));
check("bolt recovery settle→walk plant-release destRect leftover idle knife plant still eases", /Idle knife plant still eases with boltPlantFade/.test(srcTxt) && /return amp \* \(1 - boltPlantFade\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /Bolt recovery settle→guard unchanged/.test(srcTxt));
check("bolt recovery settle→walk plant-release destRect leftover arms cutRec on end", /else if \(walkOutEnd\) f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "advanceBoltFighter")) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| f\.guarding \|\| f\.guardPoseK > 0/.test(sliceFn(codeOnly, "advanceBoltFighter")));
check("bolt recovery settle→walk plant-release destRect leftover special-cancel unchanged", /Special-cancel still zeros \(holdingCutBolt\)/.test(srcTxt) && /if \(fromCut\) f\.sheatheT = SHEATHE_MS/.test(sliceFn(codeOnly, "advanceBoltFighter")) && /holdingCutBolt\(f\)\) return 0/.test(sliceFn(codeOnly, "idleBreath")));
check("throw+bolt recovery settle→walk plant-release destRect leftover chip\/wakeup\/feint\/tele unchanged", /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "idleBreath")) && /if \(telegraphing\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /if \(wakeupFade\(f\) > 0/.test(sliceFn(codeOnly, "idleBreath")) && /if \(f\.stunT > 0\)/.test(sliceFn(codeOnly, "idleBreath")) && /f\.feintT > 0/.test(sliceFn(codeOnly, "idleBreath")));
check("throw+bolt recovery settle→walk plant-release destRect leftover tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("throw+bolt recovery settle→walk plant-release destRect leftover is not a new combat verb", V.THROW_STARTUP===80 && V.THROW_ACTIVE===40 && V.THROW_RECOVERY===220 && V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140 && V.HITSTUN===350 && V.WALK_SETTLE_MS===160);
check("slash frames still 180/140/280 after throw+bolt recovery settle→walk plant-release destRect leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after throw+bolt recovery settle→walk plant-release destRect leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after throw+bolt recovery settle→walk plant-release destRect leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("throw frames still 80/40/220 after throw+bolt recovery settle→walk plant-release destRect leftover", V.THROW_STARTUP===80 && V.THROW_ACTIVE===40 && V.THROW_RECOVERY===220);
check("no 6th button after throw+bolt recovery settle→walk plant-release destRect leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after throw+bolt recovery settle→walk plant-release destRect leftover", /game.js\?v=350/.test(htmlTxt));

// live: throw walkFadeHold-only keep-0 + arms cutRec on end
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.hp=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cut="slash"; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.walkSettleT=0; V.player.walkRiseT=0; V.player.walkFadeHold=0; V.player.telegraph=false;
V.player.boltHoldCut=false; V.player.boltSuper=false;
V.player.thrownT=0; V.player.throwInvulnT=0; V.player.wakeRev=false;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=500; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
let trsw279WfhAmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ trsw279WfhAmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.cut="throw"; V.player.phase="recovery";
V.player.phaseT = Math.max(0, V.THROW_RECOVERY - V.GUARD_RAISE_MS); // near end so plant ease would seat
V.player.clashRec=false; V.player.techRec=false; V.player.boltPhase="";
V.player.cutRecBreathT=0; V.player.gait=0; V.player.walkRiseT=0; V.player.walkFadeHold=1; V.player.guarding=false; V.player.guardPoseK=0;
const trsw279Wfh0=V.player.walkFadeHold;
const trsw279WfhBr0=V.idleBreath(V.player);
const trsw279WfhTf0=V.throwPlantFade(V.player);
let trsw279WfhMaxSeat=0, trsw279WfhArmed=false, trsw279WfhCbt=0, trsw279WfhSaw=false;
for(let i=0;i<30;i++){
  holdRivalStill();
  V.keys.clear();
  V.player.gait=0;
  if(V.player.walkFadeHold<0.05 && V.player.phase==="recovery" && V.player.cut==="throw") V.player.walkFadeHold=1;
  const ph0=V.player.phase;
  const cut0=V.player.cut;
  const br=V.idleBreath(V.player);
  if(ph0==="recovery" && cut0==="throw"){
    trsw279WfhSaw=true;
    trsw279WfhMaxSeat=Math.max(trsw279WfhMaxSeat, Math.abs(br));
  }
  V.update(STEP);
  if(ph0==="recovery" && cut0==="throw" && V.player.phase==="idle"){
    trsw279WfhArmed = V.player.cutRecBreathT>0;
    trsw279WfhCbt = V.player.cutRecBreathT;
  }
  if(V.player.phase==="idle" && i>4) break;
}
check("throw recovery settle→walk plant-release destRect leftover live walkFadeHold keep-0", trsw279WfhAmpReady && trsw279Wfh0>0.9 && Math.abs(trsw279WfhBr0)<0.05 && trsw279WfhMaxSeat<0.35 && trsw279WfhSaw, `ready=${trsw279WfhAmpReady} wfh0=${trsw279Wfh0.toFixed(3)} br0=${trsw279WfhBr0.toFixed(3)} tf0=${trsw279WfhTf0.toFixed(3)} maxSeat=${trsw279WfhMaxSeat.toFixed(3)} saw=${trsw279WfhSaw}`);
check("throw recovery settle→walk plant-release destRect leftover live walkFadeHold arms cutRec", trsw279WfhArmed && trsw279WfhCbt>0, `armed=${trsw279WfhArmed} cbt=${trsw279WfhCbt}`);

// live: idle throw plant still eases (no cutRec arm)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.hp=100; V.player.stamina=100;
V.player.clashRec=false; V.player.techRec=false; V.player.thrownT=0; V.player.throwInvulnT=0;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=500; V.rival.x=1100;
let trsw279IdleAmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ trsw279IdleAmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.cut="throw"; V.player.phase="recovery";
V.player.phaseT = 0;
V.player.clashRec=false; V.player.techRec=false; V.player.cutRecBreathT=0; V.player.gait=0; V.player.walkFadeHold=0;
let trsw279IdleBr0=V.idleBreath(V.player), trsw279IdleBr1=trsw279IdleBr0, trsw279IdleUp=false, trsw279IdleArmed=false;
for(let i=0;i<24;i++){
  holdRivalStill(); V.update(STEP);
  if(V.player.phase==="idle" && V.player.cutRecBreathT>0) trsw279IdleArmed=true;
  const br=V.idleBreath(V.player);
  if(V.player.phase==="recovery" && V.player.cut==="throw" && Math.abs(br)>Math.abs(trsw279IdleBr1)+0.05) trsw279IdleUp=true;
  if(V.player.phase==="idle" && V.player.cutRecBreathT===0 && Math.abs(br)>Math.abs(trsw279IdleBr1)+0.05) trsw279IdleUp=true;
  trsw279IdleBr1=br;
  if(V.player.phase==="idle" && i>8) break;
}
check("throw recovery settle→walk plant-release destRect leftover live idle throw plant still eases", trsw279IdleAmpReady && !trsw279IdleArmed && trsw279IdleUp && Math.abs(trsw279IdleBr0)<0.05 && Math.abs(trsw279IdleBr1)>0.4 && V.player.gait===0, `ready=${trsw279IdleAmpReady} up=${trsw279IdleUp} armed=${trsw279IdleArmed} br0=${trsw279IdleBr0.toFixed(3)} br1=${trsw279IdleBr1.toFixed(3)} cbt=${V.player.cutRecBreathT}`);

// live: bolt walkFadeHold-only keep-0 + arms cutRec on end
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.hp=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cut="slash"; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.walkSettleT=0; V.player.walkRiseT=0; V.player.walkFadeHold=0;
V.player.boltHoldCut=false; V.player.boltSuper=false;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=500; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
let brsw279WfhAmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ brsw279WfhAmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.boltPhase="recovery";
V.player.boltT = Math.max(0, V.BOLT_RECOVERY - V.GUARD_RAISE_MS);
V.player.boltHoldCut=false; V.player.boltSuper=false;
V.player.cutRecBreathT=0; V.player.gait=0; V.player.walkRiseT=0; V.player.walkFadeHold=1; V.player.guarding=false; V.player.guardPoseK=0;
const brsw279Wfh0=V.player.walkFadeHold;
const brsw279WfhBr0=V.idleBreath(V.player);
const brsw279WfhBf0=V.boltPlantFade(V.player);
let brsw279WfhMaxSeat=0, brsw279WfhArmed=false, brsw279WfhCbt=0, brsw279WfhSaw=false;
for(let i=0;i<30;i++){
  holdRivalStill();
  V.keys.clear();
  V.player.gait=0;
  if(V.player.walkFadeHold<0.05 && V.player.boltPhase==="recovery") V.player.walkFadeHold=1;
  const bph0=V.player.boltPhase;
  const br=V.idleBreath(V.player);
  if(bph0==="recovery"){
    brsw279WfhSaw=true;
    brsw279WfhMaxSeat=Math.max(brsw279WfhMaxSeat, Math.abs(br));
  }
  V.update(STEP);
  if(bph0==="recovery" && !V.player.boltPhase){
    brsw279WfhArmed = V.player.cutRecBreathT>0;
    brsw279WfhCbt = V.player.cutRecBreathT;
  }
  if(!V.player.boltPhase && i>4) break;
}
check("bolt recovery settle→walk plant-release destRect leftover live walkFadeHold keep-0", brsw279WfhAmpReady && brsw279Wfh0>0.9 && Math.abs(brsw279WfhBr0)<0.05 && brsw279WfhMaxSeat<0.35 && brsw279WfhSaw, `ready=${brsw279WfhAmpReady} wfh0=${brsw279Wfh0.toFixed(3)} br0=${brsw279WfhBr0.toFixed(3)} bf0=${brsw279WfhBf0.toFixed(3)} maxSeat=${brsw279WfhMaxSeat.toFixed(3)} saw=${brsw279WfhSaw}`);
check("bolt recovery settle→walk plant-release destRect leftover live walkFadeHold arms cutRec", brsw279WfhArmed && brsw279WfhCbt>0, `armed=${brsw279WfhArmed} cbt=${brsw279WfhCbt}`);

// live: idle knife plant still eases (no cutRec arm)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.hp=100; V.player.stamina=100;
V.player.boltHoldCut=false; V.player.boltSuper=false;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=500; V.rival.x=1100;
let brsw279IdleAmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ brsw279IdleAmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.boltPhase="recovery";
V.player.boltT = 0;
V.player.boltHoldCut=false; V.player.cutRecBreathT=0; V.player.gait=0; V.player.walkFadeHold=0;
let brsw279IdleBr0=V.idleBreath(V.player), brsw279IdleBr1=brsw279IdleBr0, brsw279IdleUp=false, brsw279IdleArmed=false;
for(let i=0;i<24;i++){
  holdRivalStill(); V.update(STEP);
  if(!V.player.boltPhase && V.player.cutRecBreathT>0) brsw279IdleArmed=true;
  const br=V.idleBreath(V.player);
  if(V.player.boltPhase==="recovery" && Math.abs(br)>Math.abs(brsw279IdleBr1)+0.05) brsw279IdleUp=true;
  if(!V.player.boltPhase && V.player.cutRecBreathT===0 && Math.abs(br)>Math.abs(brsw279IdleBr1)+0.05) brsw279IdleUp=true;
  brsw279IdleBr1=br;
  if(!V.player.boltPhase && i>8) break;
}
check("bolt recovery settle→walk plant-release destRect leftover live idle knife plant still eases", brsw279IdleAmpReady && !brsw279IdleArmed && brsw279IdleUp && Math.abs(brsw279IdleBr0)<0.05 && Math.abs(brsw279IdleBr1)>0.4 && V.player.gait===0, `ready=${brsw279IdleAmpReady} up=${brsw279IdleUp} armed=${brsw279IdleArmed} br0=${brsw279IdleBr0.toFixed(3)} br1=${brsw279IdleBr1.toFixed(3)} cbt=${V.player.cutRecBreathT}`);

// live: A/D through throw recovery still keep-0 (regression of v273)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.hp=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cut="slash"; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.walkSettleT=0; V.player.walkRiseT=0; V.player.walkFadeHold=0;
V.player.boltHoldCut=false; V.player.boltSuper=false;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=200; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
let trsw279AdAmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ trsw279AdAmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.cut="throw"; V.player.phase="recovery"; V.player.phaseT=0;
V.player.clashRec=false; V.player.techRec=false; V.player.cutRecBreathT=0; V.player.gait=0; V.player.walkRiseT=0; V.player.guarding=false; V.player.guardPoseK=0;
V.keys.add("KeyD");
const trsw279AdBr0=V.idleBreath(V.player);
const trsw279AdRwo0=V.recoveryWalkOut(V.player);
let trsw279AdMaxSeat=0, trsw279AdArmed=false, trsw279AdSaw=false;
for(let i=0;i<40;i++){
  holdRivalStill();
  V.keys.add("KeyD");
  const ph0=V.player.phase;
  const cut0=V.player.cut;
  const br=V.idleBreath(V.player);
  if(ph0==="recovery" && cut0==="throw"){
    trsw279AdSaw=true;
    trsw279AdMaxSeat=Math.max(trsw279AdMaxSeat, Math.abs(br));
  }
  if(V.player.gait===1) trsw279AdMaxSeat=Math.max(trsw279AdMaxSeat, Math.abs(br));
  V.update(STEP);
  if(ph0==="recovery" && cut0==="throw" && V.player.phase==="idle"){
    trsw279AdArmed = V.player.cutRecBreathT>0;
  }
  if(trsw279AdArmed && V.player.cutRecBreathT===0 && i>8) break;
}
check("throw recovery settle→walk plant-release destRect leftover live A/D keep-0 arms", trsw279AdAmpReady && Math.abs(trsw279AdBr0)<0.05 && trsw279AdRwo0 && trsw279AdSaw && trsw279AdArmed && trsw279AdMaxSeat<0.35, `ready=${trsw279AdAmpReady} br0=${trsw279AdBr0.toFixed(3)} rwo=${trsw279AdRwo0} saw=${trsw279AdSaw} armed=${trsw279AdArmed} maxSeat=${trsw279AdMaxSeat.toFixed(3)} cbt=${V.player.cutRecBreathT}`);

check("throw+bolt recovery settle→walk plant-release destRect leftover rival advance shared", /advanceAttack\(rival/.test(srcTxt) && /advanceBoltFighter\(rival/.test(srcTxt) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "idleBreath")) && /else if \(walkOutEnd\) f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "advanceAttack")) && /else if \(walkOutEnd\) f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "advanceBoltFighter")));
check("cache v=296 after throw+bolt recovery settle→walk plant-release destRect leftover live", /game.js\?v=350/.test(htmlTxt));




// --- v280 cut recovery settle→walk plant-release destRect leftover: keep 0 through walkFadeHold / recoveryWalkOut + hold cutRec ---
check("cut recovery settle→walk plant-release destRect leftover seats no breath mid-stride", /Cut recovery settle→walk plant-release destRect leftover/.test(srcTxt) && /leftover cutRecBreathT ease-in used to/.test(srcTxt) && /only gated walking\(f\)→max\(ck, wk\), not walkFadeHold \/ recoveryWalkOut/.test(srcTxt) && /Same[\s\S]{0,80}hole chip stun \/ throw \/ bolt \/ wakeup settle→walk plant-release/.test(srcTxt) && /recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\) return 0/.test(sliceFn(codeOnly, "idleBreath")));
check("cut recovery settle→walk plant-release destRect leftover is destRect-only", /destRect-only \(idleBreath \/ cutRecBreathT\)/.test(srcTxt) && /Keep breath 0 through walk-out \/ leftover walkFadeHold/.test(srcTxt) && /f\.cutRecBreathT > 0/.test(sliceFn(codeOnly, "idleBreath")) && /recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\) return 0/.test(sliceFn(codeOnly, "idleBreath")) && /else if \(recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\)/.test(sliceFn(codeOnly, "tickCutRecBreath")) && !/walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "destRect")) && !/walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "bodyAABB")) && !/walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "poseBitmap")) && !/recoveryWalkOut/.test(sliceFn(codeOnly, "destRect")));
check("cut recovery settle→walk plant-release destRect leftover keeps 0 through early-step hold", /if \(f\.cutRecBreathT > 0\)/.test(sliceFn(codeOnly, "idleBreath")) && /if \(walking\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /1 - Math\.max\(ck, wk\)/.test(sliceFn(codeOnly, "idleBreath")) && /recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\) return 0/.test(sliceFn(codeOnly, "idleBreath")) && /tickCutRecBreath hold cutRec under recoveryWalkOut/.test(srcTxt));
check("cut recovery settle→walk plant-release destRect leftover holds cutRec under walk-out", /else if \(recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\)/.test(sliceFn(codeOnly, "tickCutRecBreath")) && /hold cutRec through early-step hold/.test(srcTxt) && /else if \(walking\(f\)\)/.test(sliceFn(codeOnly, "tickCutRecBreath")) && /walkRiseK\(f\) >= ck/.test(sliceFn(codeOnly, "tickCutRecBreath")));
check("cut recovery settle→walk plant-release destRect leftover idle settle still eases", /Idle cut recovery settle still eases when not walking/.test(srcTxt) && /return amp \* \(1 - ck\)/.test(sliceFn(codeOnly, "idleBreath")) && /f\.cutRecBreathT > 0/.test(sliceFn(codeOnly, "idleBreath")));
check("cut recovery settle→walk plant-release destRect leftover walking max\(ck, wk\) unchanged", /Math\.max\(ck, wk\)/.test(sliceFn(codeOnly, "idleBreath")) && /walkRiseK\(f\) >= ck/.test(sliceFn(codeOnly, "tickCutRecBreath")) && /Keep cutRecBreathT through rise/.test(srcTxt));
check("cut recovery settle→walk plant-release destRect leftover settle→guard unchanged", /Math\.max\(ck, rk\)/.test(sliceFn(codeOnly, "idleBreath")) && /guardRaiseK\(f\) >= ck/.test(sliceFn(codeOnly, "tickCutRecBreath")) && /Cut recovery settle→guard unchanged/.test(srcTxt));
check("cut recovery settle→walk plant-release destRect leftover chip\/throw\/bolt\/wakeup plant-release unchanged", /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "idleBreath")) && /if \(telegraphing\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /if \(wakeupFade\(f\) > 0/.test(sliceFn(codeOnly, "idleBreath")) && /if \(f\.stunT > 0\)/.test(sliceFn(codeOnly, "idleBreath")) && /f\.boltPhase === "recovery"/.test(sliceFn(codeOnly, "idleBreath")) && /f\.cut === "throw" && f\.phase === "recovery"/.test(sliceFn(codeOnly, "idleBreath")));
check("cut recovery settle→walk plant-release destRect leftover tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("cut recovery settle→walk plant-release destRect leftover is not a new combat verb", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.HITSTUN===350 && V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140 && V.WALK_SETTLE_MS===160 && V.FEINT_RECOVERY===100);
check("slash frames still 180/140/280 after cut recovery settle→walk plant-release destRect leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after cut recovery settle→walk plant-release destRect leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after cut recovery settle→walk plant-release destRect leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("throw frames still 80/40/220 after cut recovery settle→walk plant-release destRect leftover", V.THROW_STARTUP===80 && V.THROW_ACTIVE===40 && V.THROW_RECOVERY===220);
check("no 6th button after cut recovery settle→walk plant-release destRect leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after cut recovery settle→walk plant-release destRect leftover", /game.js\?v=350/.test(htmlTxt));

// live: walkFadeHold / recoveryWalkOut keep-0 + hold cutRec (no plant-release hop)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.hp=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cut="slash"; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.walkSettleT=0; V.player.walkRiseT=0; V.player.walkFadeHold=0; V.player.telegraph=false;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=500; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
let crsw280WfhAmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ crsw280WfhAmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.cutRecBreathT = V.GUARD_RAISE_MS;
V.player.gait=0; V.player.walkRiseT=0; V.player.walkFadeHold=1; V.player.guarding=false; V.player.guardPoseK=0;
V.player.phase="idle"; V.player.sheatheT=0; V.player.feintT=0; V.player.boltPhase=""; V.player.stunT=0;
V.keys.add("KeyD");
const crsw280Wfh0=V.player.walkFadeHold;
const crsw280WfhBr0=V.idleBreath(V.player);
const crsw280WfhRwo0=V.recoveryWalkOut(V.player);
const crsw280WfhCbt0=V.player.cutRecBreathT;
let crsw280WfhMaxSeat=0, crsw280WfhHeld=true, crsw280WfhAabbOk=true;
const crsw280WfhAabb0=V.bodyAABB(V.player).y;
for(let i=0;i<10;i++){
  holdRivalStill();
  V.keys.add("KeyD");
  V.player.gait=0; // early-step: pad held, gait not armed yet / forced off
  if(V.player.walkFadeHold<0.05) V.player.walkFadeHold=1;
  const br=V.idleBreath(V.player);
  crsw280WfhMaxSeat=Math.max(crsw280WfhMaxSeat, Math.abs(br));
  if(V.player.cutRecBreathT < crsw280WfhCbt0 - 1) crsw280WfhHeld=false;
  if(Math.abs(V.bodyAABB(V.player).y - crsw280WfhAabb0)>0.05) crsw280WfhAabbOk=false;
  V.update(STEP);
}
check("cut recovery settle→walk plant-release destRect leftover live walkFadeHold keep-0", crsw280WfhAmpReady && crsw280Wfh0>0.9 && Math.abs(crsw280WfhBr0)<0.05 && crsw280WfhRwo0 && crsw280WfhMaxSeat<0.35 && crsw280WfhHeld && V.player.cutRecBreathT===crsw280WfhCbt0, `ready=${crsw280WfhAmpReady} wfh0=${crsw280Wfh0.toFixed(3)} br0=${crsw280WfhBr0.toFixed(3)} rwo=${crsw280WfhRwo0} maxSeat=${crsw280WfhMaxSeat.toFixed(3)} held=${crsw280WfhHeld} cbt=${V.player.cutRecBreathT}`);
check("cut recovery settle→walk plant-release destRect leftover live walkFadeHold AABB planted", crsw280WfhAabbOk, `aabbOk=${crsw280WfhAabbOk}`);

// live: idle cutRec still eases when not walking / walk-out / hold
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.hp=100; V.player.stamina=100;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=500; V.rival.x=1100;
let crsw280IdleAmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ crsw280IdleAmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.player.cutRecBreathT = V.GUARD_RAISE_MS;
let crsw280IdleBr0=V.idleBreath(V.player), crsw280IdleBr1=crsw280IdleBr0, crsw280IdleUp=true;
for(let i=0;i<6;i++){
  holdRivalStill(); V.update(STEP);
  const br=V.idleBreath(V.player);
  if(Math.abs(br)+0.08<Math.abs(crsw280IdleBr1)) crsw280IdleUp=false;
  crsw280IdleBr1=br;
}
check("cut recovery settle→walk plant-release destRect leftover live idle ease still climbs", crsw280IdleAmpReady && crsw280IdleUp && Math.abs(crsw280IdleBr0)<0.2 && Math.abs(crsw280IdleBr1)>0.9 && V.player.cutRecBreathT===0 && V.player.gait===0, `ready=${crsw280IdleAmpReady} up=${crsw280IdleUp} br0=${crsw280IdleBr0.toFixed(3)} br1=${crsw280IdleBr1.toFixed(3)} cbt=${V.player.cutRecBreathT}`);

// live: walking max(ck,wk) still holds / rise clears (regression)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.hp=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cut=""; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.walkSettleT=0; V.player.walkRiseT=0; V.player.walkFadeHold=0;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=200; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
let crsw280WalkAmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ crsw280WalkAmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.keys.add("KeyD");
for(let i=0;i<20;i++){ holdRivalStill(); V.update(STEP); }
const crsw280WalkRiseReady = V.player.gait===1 && Math.abs(V.idleBreath(V.player))<0.05;
V.player.cutRecBreathT = V.GUARD_RAISE_MS;
V.player.guarding=false; V.player.guardPoseK=0; V.player.phase="idle"; V.player.sheatheT=0; V.player.feintT=0; V.player.boltPhase="";
const crsw280WalkBr0=V.idleBreath(V.player);
let crsw280WalkMaxAbsBr=Math.abs(crsw280WalkBr0), crsw280WalkCleared=false, crsw280WalkEndBr=0;
for(let i=0;i<12;i++){
  holdRivalStill();
  const c0=V.player.cutRecBreathT;
  V.update(STEP);
  const br=V.idleBreath(V.player);
  crsw280WalkMaxAbsBr=Math.max(crsw280WalkMaxAbsBr, Math.abs(br));
  if(c0>0 && V.player.cutRecBreathT===0){ crsw280WalkCleared=true; crsw280WalkEndBr=br; break; }
}
check("cut recovery settle→walk plant-release destRect leftover live walking max still holds", crsw280WalkAmpReady && crsw280WalkRiseReady && crsw280WalkMaxAbsBr<0.35 && crsw280WalkCleared && Math.abs(crsw280WalkEndBr)<0.05, `ready=${crsw280WalkAmpReady} rise=${crsw280WalkRiseReady} maxBr=${crsw280WalkMaxAbsBr.toFixed(3)} cleared=${crsw280WalkCleared} endBr=${crsw280WalkEndBr.toFixed(3)}`);

// live: rival closing + walkFadeHold keep-0 / hold cutRec
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.rival.phase="idle"; V.rival.stunT=0; V.rival.guarding=false; V.rival.boltPhase=""; V.rival.falling=false;
V.rival.feintT=0; V.rival.sheatheT=0; V.rival.gait=0; V.rival.stamina=100; V.rival.stamRegenT=99999; V.rival.hp=100;
V.rival.clashRec=false; V.rival.techRec=false; V.rival.cut="slash"; V.rival.cutRecBreathT=0; V.rival.guardPoseK=0; V.rival.walkSettleT=0; V.rival.walkRiseT=0; V.rival.walkFadeHold=0;
V.player.phase="idle"; V.player.wantBlock=false;
V.player.x=200; V.rival.x=1100;
let crsw280RivalAmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT + 380)/2400*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ crsw280RivalAmpReady=true; break; }
  freezeRivalAI(); V.rival.closing=false; V.update(STEP);
}
V.rival.cutRecBreathT = V.GUARD_RAISE_MS;
V.rival.gait=0; V.rival.walkRiseT=0; V.rival.walkFadeHold=1; V.rival.closing=true;
V.rival.guarding=false; V.rival.guardPoseK=0; V.rival.phase="idle"; V.rival.sheatheT=0; V.rival.feintT=0; V.rival.boltPhase=""; V.rival.stunT=0;
const crsw280RivalBr0=V.idleBreath(V.rival);
const crsw280RivalRwo0=V.recoveryWalkOut(V.rival);
const crsw280RivalCbt0=V.rival.cutRecBreathT;
let crsw280RivalMaxSeat=0, crsw280RivalHeld=true;
for(let i=0;i<10;i++){
  freezeRivalAI();
  V.rival.closing=true;
  V.rival.gait=0;
  if(V.rival.walkFadeHold<0.05) V.rival.walkFadeHold=1;
  const br=V.idleBreath(V.rival);
  crsw280RivalMaxSeat=Math.max(crsw280RivalMaxSeat, Math.abs(br));
  if(V.rival.cutRecBreathT < crsw280RivalCbt0 - 1) crsw280RivalHeld=false;
  V.update(STEP);
}
check("cut recovery settle→walk plant-release destRect leftover live rival closing keep-0", crsw280RivalAmpReady && crsw280RivalRwo0 && Math.abs(crsw280RivalBr0)<0.05 && crsw280RivalMaxSeat<0.35 && crsw280RivalHeld && V.rival.cutRecBreathT===crsw280RivalCbt0, `ready=${crsw280RivalAmpReady} rwo0=${crsw280RivalRwo0} br0=${crsw280RivalBr0.toFixed(3)} maxSeat=${crsw280RivalMaxSeat.toFixed(3)} held=${crsw280RivalHeld} cbt=${V.rival.cutRecBreathT}`);

check("cut recovery settle→walk plant-release destRect leftover rival idleBreath shared", /recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\) return 0/.test(sliceFn(codeOnly, "idleBreath")) && /else if \(recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\)/.test(sliceFn(codeOnly, "tickCutRecBreath")) && /tickCutRecBreath\(rival/.test(srcTxt) && !/f\.kind === "you"/.test(sliceFn(codeOnly, "tickCutRecBreath")));
check("cache v=296 after cut recovery settle→walk plant-release destRect leftover live", /game.js\?v=350/.test(htmlTxt));




// --- v281 telegraph keep-0 add walking(f) + guard drop settle→walk plant-release destRect leftover ---
check("telegraph settle→walk keep-0 includes walking(f)", /Telegraph settle→walk keep-0: add walking\(f\)/.test(srcTxt) && /if \(telegraphing\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\) return 0/.test(sliceFn(codeOnly, "idleBreath")) && /idle telegraph still eases with telegraphFade/.test(srcTxt));
check("telegraph settle→walk keep-0 walking parity with siblings", /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "idleBreath")) && /if \(telegraphing\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /if \(f\.feintT > 0\)/.test(sliceFn(codeOnly, "idleBreath")) && /if \(wakeupFade\(f\) > 0/.test(sliceFn(codeOnly, "idleBreath")));

check("guard drop settle→walk plant-release destRect leftover seats no breath mid-stride", /Guard drop settle→walk plant-release destRect leftover/.test(srcTxt) && /leftover raise k \/ max\(rk, wk\) used to/.test(srcTxt) && /walkRise restarted from 0/.test(srcTxt) && /restGuardWalk/.test(srcTxt) && /Same[\s\S]{0,80}hole feint \/ cutRec \/ chip stun settle→walk plant-release/.test(srcTxt) && /f\.guardPoseK > 0 && !f\.guarding && \(walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\)/.test(sliceFn(codeOnly, "idleBreath")));
check("guard drop settle→walk plant-release destRect leftover is destRect-only", /destRect-only \(idleBreath \/ cutRecBreathT\)/.test(srcTxt) && /Keep breath 0 through walking \/ walk-out \/ leftover/.test(srcTxt) && /f\.guardPoseK > 0 && !f\.guarding && \(walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\)\) \{\s*return 0;/.test(sliceFn(codeOnly, "idleBreath")) && /prevGuardK > 0/.test(sliceFn(codeOnly, "tickGuardPose")) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickGuardPose")) && !/prevGuardK/.test(sliceFn(codeOnly, "destRect")) && !/prevGuardK/.test(sliceFn(codeOnly, "bodyAABB")) && !/prevGuardK/.test(sliceFn(codeOnly, "poseBitmap")));
check("guard drop settle→walk plant-release destRect leftover keeps 0 through drop walk", /if \(f\.guardPoseK > 0 && !f\.guarding && \(walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /return 0;/.test(sliceFn(codeOnly, "idleBreath")) && /tickGuardPose arm cutRecBreathT when drop ends/.test(srcTxt));
check("guard drop settle→walk plant-release destRect leftover arms cutRec on drop end", /prevGuardK > 0/.test(sliceFn(codeOnly, "tickGuardPose")) && /f\.guardPoseK === 0/.test(sliceFn(codeOnly, "tickGuardPose")) && /!f\.guarding/.test(sliceFn(codeOnly, "tickGuardPose")) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "tickGuardPose")) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickGuardPose")));
check("guard drop settle→walk plant-release destRect leftover idle drop still eases", /Idle drop still eases with raise k/.test(srcTxt) && /if \(f\.guarding \|\| f\.guardPoseK > 0\) return amp \* \(1 - guardRaiseK\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")));
check("guard drop settle→walk plant-release destRect leftover pure idle→walk still eases", /Pure idle→walk still eases with rise/.test(srcTxt) && /if \(walking\(f\)\) return amp \* \(1 - walkRiseK\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")));
check("guard drop settle→walk plant-release destRect leftover sheet leftover unchanged", /Walk-out sheet leftover unchanged \(guardDropFade\)/.test(srcTxt) && /function guardDropFade/.test(srcTxt));
check("guard drop settle→walk plant-release destRect leftover feint\/cutRec\/chip plant-release unchanged", /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "idleBreath")) && /f\.cutRecBreathT > 0/.test(sliceFn(codeOnly, "idleBreath")) && /f\.feintT > 0/.test(sliceFn(codeOnly, "idleBreath")) && /if \(f\.stunT > 0\)/.test(sliceFn(codeOnly, "idleBreath")));
check("guard drop settle→walk plant-release destRect leftover tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("guard drop settle→walk plant-release destRect leftover is not a new combat verb", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.HITSTUN===350 && V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140 && V.WALK_SETTLE_MS===160 && V.FEINT_RECOVERY===100);
check("slash frames still 180/140/280 after guard drop settle→walk plant-release destRect leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after guard drop settle→walk plant-release destRect leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after guard drop settle→walk plant-release destRect leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("throw frames still 80/40/220 after guard drop settle→walk plant-release destRect leftover", V.THROW_STARTUP===80 && V.THROW_ACTIVE===40 && V.THROW_RECOVERY===220);
check("no 6th button after guard drop settle→walk plant-release destRect leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after guard drop settle→walk plant-release destRect leftover", /game.js\?v=350/.test(htmlTxt));

// live: drop + D keep-0 + arms cutRec
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.hp=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cut="slash"; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.walkSettleT=0; V.player.walkRiseT=0; V.player.walkFadeHold=0; V.player.telegraph=false;
V.player.thrownT=0; V.player.throwInvulnT=0;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=200; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
let gdsw281AmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ gdsw281AmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.keys.add("KeyD");
for(let i=0;i<25;i++){ holdRivalStill(); V.keys.add("KeyD"); V.update(STEP); }
V.keys.add("KeyS");
for(let i=0;i<20;i++){ holdRivalStill(); V.keys.add("KeyD"); V.keys.add("KeyS"); V.update(STEP); }
V.keys.delete("KeyS");
let gdsw281MaxSeat=0, gdsw281SawDrop=false, gdsw281Armed=false, gdsw281AabbOk=true;
const gdsw281Aabb0=V.bodyAABB(V.player).y;
for(let i=0;i<36;i++){
  holdRivalStill(); V.keys.add("KeyD"); V.keys.delete("KeyS");
  const br=V.idleBreath(V.player);
  const gpk=V.player.guardPoseK;
  if(gpk>0){ gdsw281SawDrop=true; gdsw281MaxSeat=Math.max(gdsw281MaxSeat, Math.abs(br)); }
  if(Math.abs(V.bodyAABB(V.player).y-gdsw281Aabb0)>0.05) gdsw281AabbOk=false;
  const prevK=gpk;
  V.update(STEP);
  if(prevK>0 && V.player.guardPoseK===0 && V.player.cutRecBreathT>0) gdsw281Armed=true;
  if(gdsw281Armed && V.player.cutRecBreathT===0 && i>8) break;
}
check("guard drop settle→walk plant-release destRect leftover live A/D keep-0", gdsw281AmpReady && gdsw281SawDrop && gdsw281MaxSeat<0.35, `ready=${gdsw281AmpReady} saw=${gdsw281SawDrop} maxSeat=${gdsw281MaxSeat.toFixed(3)}`);
check("guard drop settle→walk plant-release destRect leftover live A/D arms cutRec", gdsw281Armed, `armed=${gdsw281Armed} cbt=${V.player.cutRecBreathT}`);
check("guard drop settle→walk plant-release destRect leftover live A/D AABB planted", gdsw281AabbOk, `aabbOk=${gdsw281AabbOk}`);

// live: idle drop still eases (no cutRec arm)
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.hp=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.walkSettleT=0; V.player.walkFadeHold=0; V.player.telegraph=false;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=500; V.rival.x=1100; V.keepApart();
let gdsw281IdleAmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ gdsw281IdleAmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.keys.add("KeyS");
for(let i=0;i<20;i++){ holdRivalStill(); V.keys.add("KeyS"); V.update(STEP); }
V.keys.clear();
let gdsw281IdleMax=0, gdsw281IdleArmed=false, gdsw281IdleSaw=false;
for(let i=0;i<24;i++){
  holdRivalStill();
  const br=V.idleBreath(V.player);
  if(V.player.guardPoseK>0){ gdsw281IdleSaw=true; gdsw281IdleMax=Math.max(gdsw281IdleMax, Math.abs(br)); }
  if(V.player.cutRecBreathT>0) gdsw281IdleArmed=true;
  V.update(STEP);
}
check("guard drop settle→walk plant-release destRect leftover live idle drop still eases", gdsw281IdleAmpReady && gdsw281IdleSaw && !gdsw281IdleArmed && gdsw281IdleMax>0.5, `ready=${gdsw281IdleAmpReady} saw=${gdsw281IdleSaw} armed=${gdsw281IdleArmed} max=${gdsw281IdleMax.toFixed(3)}`);

// live: forced wfh drop keep-0 + arms cutRec
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.gait=0; V.player.stamina=100; V.player.stamRegenT=99999; V.player.hp=100;
V.player.clashRec=false; V.player.techRec=false; V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.walkSettleT=0; V.player.walkFadeHold=0; V.player.telegraph=false;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.player.x=500; V.rival.x=1100; V.keepApart();
let gdsw281WfhAmpReady=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ gdsw281WfhAmpReady=true; break; }
  holdRivalStill(); V.update(STEP);
}
V.keys.add("KeyS");
for(let i=0;i<20;i++){ holdRivalStill(); V.keys.add("KeyS"); V.update(STEP); }
V.keys.clear();
V.player.walkFadeHold=1; V.player.gait=0; V.player.walkSettleT=0;
const gdsw281WfhBr0=V.idleBreath(V.player);
let gdsw281WfhMax=0, gdsw281WfhArmed=false;
for(let i=0;i<24;i++){
  holdRivalStill();
  V.player.gait=0; V.player.walkSettleT=0;
  if(V.player.walkFadeHold<0.5) V.player.walkFadeHold=1;
  const prevK=V.player.guardPoseK;
  const br=V.idleBreath(V.player);
  if(prevK>0) gdsw281WfhMax=Math.max(gdsw281WfhMax, Math.abs(br));
  V.update(STEP);
  if(prevK>0 && V.player.guardPoseK===0 && V.player.cutRecBreathT>0) gdsw281WfhArmed=true;
  if(gdsw281WfhArmed && i>4) break;
}
check("guard drop settle→walk plant-release destRect leftover live walkFadeHold keep-0", gdsw281WfhAmpReady && Math.abs(gdsw281WfhBr0)<0.05 && gdsw281WfhMax<0.35, `ready=${gdsw281WfhAmpReady} br0=${gdsw281WfhBr0.toFixed(3)} maxSeat=${gdsw281WfhMax.toFixed(3)}`);
check("guard drop settle→walk plant-release destRect leftover live walkFadeHold arms cutRec", gdsw281WfhArmed, `armed=${gdsw281WfhArmed} cbt=${V.player.cutRecBreathT}`);

check("guard drop settle→walk plant-release destRect leftover rival idleBreath shared", /f\.guardPoseK > 0 && !f\.guarding && \(walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\)/.test(sliceFn(codeOnly, "idleBreath")) && /prevGuardK > 0/.test(sliceFn(codeOnly, "tickGuardPose")) && /tickGuardPose\(/.test(srcTxt) && !/f\.kind === "you"/.test(sliceFn(codeOnly, "tickGuardPose")));
check("cache v=296 after guard drop settle→walk plant-release destRect leftover live", /game.js\?v=350/.test(htmlTxt));


// --- v282 plant walk-in destRect lean leftover: keep settle through plant fades ---
check("plant walk-in destRect lean leftover seats no lean dump mid-stride plant", /Plant walk-in destRect lean leftover/.test(srcTxt) && /mid-stride Space\/L\/K/.test(srcTxt) && /dump walkSettleRot\/Oy/.test(srcTxt) && /zeroed walkSettleT/.test(srcTxt) && /walkSettleK also gated/.test(srcTxt) && /phase!==idle/.test(srcTxt) && /restLinkWalk\/restClashWalk\/restTeleWalk/.test(srcTxt) && /Same hole walk→guard destRect lean/.test(srcTxt));
check("plant walk-in destRect lean leftover is destRect-only ease", /destRect-only ease \(walkSettleK\)/.test(srcTxt) && /drainWalkSettleUnderPlant/.test(sliceFn(codeOnly, "tickGait")) && /walkSettleRot \* sk/.test(sliceFn(codeOnly, "destRect")) && /walkSettleOy \* sk/.test(sliceFn(codeOnly, "destRect")) && !/drainWalkSettleUnderPlant/.test(sliceFn(codeOnly, "bodyAABB")) && !/drainWalkSettleUnderPlant/.test(sliceFn(codeOnly, "poseBitmap")) && !/drainWalkSettleUnderPlant/.test(sliceFn(codeOnly, "slashPose")));
check("plant walk-in destRect lean leftover tickGait drains settle under plants", /drainWalkSettleUnderPlant\(f, dt, was\)/.test(sliceFn(codeOnly, "tickGait")) && /telegraphing\(f\) && \(f\.walkFadeHold > 0 \|\| f\.walkSettleT > 0\)/.test(sliceFn(codeOnly, "tickGait")) && /linkPlanting\(f\) && \(f\.walkFadeHold > 0 \|\| f\.walkSettleT > 0\)/.test(sliceFn(codeOnly, "tickGait")) && /clashPlanting\(f\) && \(f\.walkFadeHold > 0 \|\| f\.walkSettleT > 0\)/.test(sliceFn(codeOnly, "tickGait")) && /holdCutPlanting\(f\) && \(f\.walkFadeHold > 0 \|\| f\.walkSettleT > 0\)/.test(sliceFn(codeOnly, "tickGait")) && !/f\.walkSettleT = 0;\s*return;/.test(sliceFn(codeOnly, "tickGait").split("Combat poses lock")[0]));
check("plant walk-in destRect lean leftover walkSettleK keeps through plant fades", /telegraphing\(f\)/.test(sliceFn(codeOnly, "walkSettleK")) && /linkPlanting\(f\)/.test(sliceFn(codeOnly, "walkSettleK")) && /clashPlanting\(f\)/.test(sliceFn(codeOnly, "walkSettleK")) && /holdCutPlanting\(f\)/.test(sliceFn(codeOnly, "walkSettleK")) && /reversalPlanting\(f\)/.test(sliceFn(codeOnly, "walkSettleK")) && /throwGuardPlanting\(f\)/.test(sliceFn(codeOnly, "walkSettleK")) && /techGuardPlanting\(f\)/.test(sliceFn(codeOnly, "walkSettleK")) && /f\.feintT > 0/.test(sliceFn(codeOnly, "walkSettleK")) && /if \(f\.guarding\)/.test(sliceFn(codeOnly, "walkSettleK")));
check("plant walk-in destRect lean leftover arms settle on was-walk only", /was && f\.walkSettleT <= 0/.test(sliceFn(codeOnly, "drainWalkSettleUnderPlant")) && /f\.walkSettleT = WALK_SETTLE_MS/.test(sliceFn(codeOnly, "drainWalkSettleUnderPlant")) && /do not invent settle from a bare/.test(srcTxt) && /walkFadeHold arm/.test(srcTxt));
check("plant walk-in destRect lean leftover sheatheDip unchanged", /sheatheDip hump unchanged/.test(srcTxt) && /if \(f\.sheatheT <= 0 \|\| f\.falling \|\| f\.stunT > 0\) return 0;/.test(sliceFn(codeOnly, "sheatheDip")) && /return -5 \* Math\.sin\(Math\.PI \* u\)/.test(sliceFn(codeOnly, "sheatheDip")));
check("plant walk-in destRect lean leftover walk→guard path unchanged", /if \(f\.guarding\)/.test(sliceFn(codeOnly, "walkSettleK")) && /if \(f\.guarding\)/.test(sliceFn(codeOnly, "tickGait")) && /Keep leftover settle through leftover walkSettleT if they raise/.test(srcTxt));
check("plant walk-in destRect lean leftover tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("plant walk-in destRect lean leftover is not a new combat verb", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.HITSTUN===350 && V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140 && V.WALK_SETTLE_MS===160 && V.WALK_STEP_MS===320 && V.FEINT_RECOVERY===100);
check("slash frames still 180/140/280 after plant walk-in destRect lean leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after plant walk-in destRect lean leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after plant walk-in destRect lean leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after plant walk-in destRect lean leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after plant walk-in destRect lean leftover", /game.js\?v=350/.test(htmlTxt));

// live: mid-stride telegraph Space keeps destRect lean (no dump)
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x = 400; V.rival.x = 1100;
V.player.guarding = false; V.player.stunT = 0; V.player.phase = "idle";
V.player.boltPhase = ""; V.player.falling = false; V.player.guardPoseK = 0; V.player.hp = 100;
V.player.sheatheT = 0; V.player.linkPlant = false; V.player.telegraph = false;
V.player.gait = 0; V.player.walkT = 0; V.player.walkSettleT = 0; V.player.walkFadeHold = 0; V.player.walkRiseT = 0;
V.keys.add("KeyD");
let plwReady=false, plwRot0=0, plwAabb0=null, plwSettleRot=0;
for(let i=0;i<50;i++){
  freezeRivalAI();
  V.player.phase="idle"; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase="";
  V.keys.add("KeyD");
  V.update(STEP);
  if(V.player.walkT>=150 && V.player.walkRiseT>=V.WALK_SETTLE_MS && Math.abs(V.destRect(V.player).rot)>0.03){
    plwReady=true;
    plwRot0=V.destRect(V.player).rot;
    plwAabb0=V.bodyAABB(V.player);
    plwSettleRot=V.player.walkSettleRot;
    break;
  }
}
check("plant walk-in destRect lean leftover live stride ready", plwReady && V.player.gait!==0 && Math.abs(plwRot0)>0.03, `ready=${plwReady} gait=${V.player.gait} rot0=${plwRot0.toFixed(4)}`);
V.attackEdge = true;
V.update(STEP);
freezeRivalAI();
const plwRot1=V.destRect(V.player).rot;
const plwSk1=V.walkSettleK(V.player);
check("plant walk-in destRect lean leftover live telegraph keeps lean", V.player.phase==="startup" && V.player.telegraph===true && V.player.walkFadeHold>0.02 && V.player.walkSettleT>0 && Math.abs(plwRot1-plwRot0)<0.03 && plwSk1>0.9, `ph=${V.player.phase} tel=${V.player.telegraph} hold=${V.player.walkFadeHold} settle=${V.player.walkSettleT} rot0=${plwRot0.toFixed(4)} rot1=${plwRot1.toFixed(4)} sk=${plwSk1.toFixed(3)}`);
check("plant walk-in destRect lean leftover live telegraph does not dump lean", Math.abs(plwRot1-plwRot0)<0.03 && !(Math.abs(plwRot1) < Math.abs(plwRot0) - 0.04), `drot=${(plwRot1-plwRot0).toFixed(4)} rot0=${plwRot0.toFixed(4)} rot1=${plwRot1.toFixed(4)}`);
check("plant walk-in destRect lean leftover live telegraph AABB planted", plwAabb0 && Math.abs(V.bodyAABB(V.player).y-plwAabb0.y)<0.05, `dy=${plwAabb0?(V.bodyAABB(V.player).y-plwAabb0.y).toFixed(2):"na"}`);
let plwRotMid=0, plwSettleMid=0;
for(let i=0;i<4;i++){
  freezeRivalAI();
  V.player.stunT=0; V.player.falling=false; V.hitstopLeft=0;
  V.update(STEP);
  plwRotMid=V.destRect(V.player).rot;
  plwSettleMid=V.player.walkSettleT;
}
const plwSkMid=(plwSettleMid/V.WALK_SETTLE_MS)*(plwSettleMid/V.WALK_SETTLE_MS);
check("plant walk-in destRect lean leftover live lean eases under plant", V.player.phase==="startup" && plwSettleMid>0 && plwSettleMid<V.WALK_SETTLE_MS && Math.abs(plwRotMid - plwSettleRot*plwSkMid)<0.02, `midRot=${plwRotMid.toFixed(4)} expect=${(plwSettleRot*plwSkMid).toFixed(4)} settle=${plwSettleMid} sk=${plwSkMid.toFixed(3)}`);

// live: mid-stride sheathe Space (link plant) keeps lean
bootPlay(); wait(1700);
armMidStrideSheatheRaise();
{
  // refresh walk settle rot via one walking tick
  freezeRivalAI();
  V.update(STEP);
  freezeRivalAI();
  const lr0=V.destRect(V.player).rot;
  const hold0=V.player.walkFadeHold;
  const aabb0=V.bodyAABB(V.player);
  V.attackEdge = true;
  V.update(STEP);
  freezeRivalAI();
  const lr1=V.destRect(V.player).rot;
  check("plant walk-in destRect lean leftover live link keeps lean", V.player.phase==="startup" && V.player.linkPlant===true && V.player.walkFadeHold>0.02 && V.player.walkSettleT>0 && Math.abs(lr1-lr0)<0.03 && hold0>0.5, `ph=${V.player.phase} lp=${V.player.linkPlant} hold=${V.player.walkFadeHold} settle=${V.player.walkSettleT} rot0=${lr0.toFixed(4)} rot1=${lr1.toFixed(4)}`);
  check("plant walk-in destRect lean leftover live link AABB planted", Math.abs(V.bodyAABB(V.player).y-aabb0.y)<0.05, `dy=${(V.bodyAABB(V.player).y-aabb0.y).toFixed(2)}`);
}

// live: idle telegraph still walkSettleT 0 (no invented lean)
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.gait=0; V.player.walkT=0; V.player.walkSettleT=0; V.player.walkFadeHold=0;
V.player.sheatheT=0; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase="";
V.attackEdge = true;
V.update(STEP);
freezeRivalAI();
check("plant walk-in destRect lean leftover live idle plant still no settle", V.player.phase==="startup" && V.player.telegraph===true && V.player.walkSettleT===0 && V.player.walkFadeHold===0 && Math.abs(V.destRect(V.player).rot)<0.02, `ph=${V.player.phase} tel=${V.player.telegraph} settle=${V.player.walkSettleT} hold=${V.player.walkFadeHold} rot=${V.destRect(V.player).rot.toFixed(4)}`);

check("plant walk-in destRect lean leftover rival tickGait shared", /tickGait\(rival/.test(srcTxt) && /drainWalkSettleUnderPlant/.test(sliceFn(codeOnly, "tickGait")) && /linkPlanting\(f\)/.test(sliceFn(codeOnly, "walkSettleK")) && !/f\.kind === "you"/.test(sliceFn(codeOnly, "drainWalkSettleUnderPlant")) && !/f\.kind === "you"/.test(sliceFn(codeOnly, "walkSettleK")));

check("cache v=296 after plant walk-in destRect lean leftover live", /game.js\?v=350/.test(htmlTxt));

// --- v283 sheathe settle lean under walkFadeHold: keep settle through sheathe ---
check("sheathe settle lean under walkFadeHold seats no lean dump mid-hump release", /Sheathe settle lean under walkFadeHold/.test(srcTxt) && /A\/D release mid-clash-sheathe/.test(srcTxt) && /gated usingDedicatedPose while sheathing\(\) held slash/.test(srcTxt) && /tickGait already armed walkSettleT/.test(srcTxt) && /sheatheDip still rode the hump/.test(srcTxt) && /Same hole plant walk-in \/ walk→guard/.test(srcTxt));
check("sheathe settle lean under walkFadeHold is destRect-only ease", /destRect-only ease \(walkSettleK\)/.test(srcTxt) && /Keep leftover settle through leftover walkSettleT under sheathe/.test(srcTxt) && /walkSettleRot \* sk/.test(sliceFn(codeOnly, "destRect")) && /walkSettleOy \* sk/.test(sliceFn(codeOnly, "destRect")) && !/f\.sheatheT > 0/.test(sliceFn(codeOnly, "bodyAABB")) && /if \(f\.sheatheT > 0\)/.test(sliceFn(codeOnly, "walkSettleK")));
check("sheathe settle lean under walkFadeHold walkSettleK keeps through sheathe", /if \(f\.sheatheT > 0\)/.test(sliceFn(codeOnly, "walkSettleK")) && /usingDedicatedPose\(f\)\) return 0/.test(sliceFn(codeOnly, "walkSettleK")) && sliceFn(codeOnly, "walkSettleK").indexOf("f.sheatheT > 0") < sliceFn(codeOnly, "walkSettleK").indexOf("usingDedicatedPose(f)") && /mirror plant fades \/ guarding/.test(srcTxt));
check("sheathe settle lean under walkFadeHold sheatheDip unchanged", /sheatheDip hump unchanged/.test(srcTxt) && /if \(f\.sheatheT <= 0 \|\| f\.falling \|\| f\.stunT > 0\) return 0;/.test(sliceFn(codeOnly, "sheatheDip")) && /return -5 \* Math\.sin\(Math\.PI \* u\)/.test(sliceFn(codeOnly, "sheatheDip")));
check("sheathe settle lean under walkFadeHold plant\/guard paths unchanged", /telegraphing\(f\)/.test(sliceFn(codeOnly, "walkSettleK")) && /linkPlanting\(f\)/.test(sliceFn(codeOnly, "walkSettleK")) && /clashPlanting\(f\)/.test(sliceFn(codeOnly, "walkSettleK")) && /if \(f\.guarding\)/.test(sliceFn(codeOnly, "walkSettleK")) && /drainWalkSettleUnderPlant/.test(sliceFn(codeOnly, "tickGait")));
check("sheathe settle lean under walkFadeHold tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("sheathe settle lean under walkFadeHold is not a new combat verb", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.HITSTUN===350 && V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140 && V.WALK_SETTLE_MS===160 && V.WALK_STEP_MS===320 && V.FEINT_RECOVERY===100);
check("slash frames still 180/140/280 after sheathe settle lean under walkFadeHold", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after sheathe settle lean under walkFadeHold", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after sheathe settle lean under walkFadeHold", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after sheathe settle lean under walkFadeHold", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after sheathe settle lean under walkFadeHold", /game.js\?v=350/.test(htmlTxt));

// live: release mid clash-sheathe hump keeps destRect lean (no dump while dip rides)
bootPlay(); wait(1700);
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.openLeft=0; V.hitstopLeft=0;
V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0; V.player.guarding=false; V.player.boltPhase=""; V.player.falling=false;
V.player.feintT=0; V.player.sheatheT=0; V.player.cutRecBreathT=0; V.player.guardPoseK=0;
V.player.walkSettleT=0; V.player.walkFadeHold=0; V.player.gait=0; V.player.walkT=0; V.player.walkRiseT=0; V.player.walkLeanGait=0;
V.player.walkSettleRot=0; V.player.walkSettleOy=0;
V.player.hp=100; V.player.stamina=100; V.player.stamRegenT=99999;
V.player.clashRec=false; V.player.techRec=false; V.player.telegraph=false; V.player.linkPlant=false; V.player.clashPlant=false; V.player.holdCutPlant=false;
V.rival.phase="idle"; V.rival.wantBlock=false; V.rival.gait=0; V.rival.sheatheT=0; V.rival.revArmed=-1;
V.player.x=200; V.rival.x=1100; V.player.facing=1; V.rival.facing=-1;
V.keepApart();
// force mid-hump clash sheathe (same arm pattern as clash sheathe settle→walk live)
V.player.sheatheT = V.SHEATHE_MS * 0.9;
V.player.clashRec=true; V.player.cut="slash"; V.player.phase="idle"; V.player.cutRecBreathT=0; V.player.feintT=0;
check("sheathe settle lean under walkFadeHold live clash sheathe ready", V.player.sheatheT>0 && V.player.sheatheT>=V.SHEATHE_MS*0.8 && V.player.clashRec===true, `ready=true sh=${V.player.sheatheT|0} clash=${V.player.clashRec}`);
// walk mid-hump a few frames (rise partial lean; sheathing() false while walking)
V.keys.add("KeyD");
let sslWalkReady=false, sslRotWalk=0, sslWfh=0, sslAabb0=null, sslShWalk=0;
for(let i=0;i<4;i++){
  holdRivalStill();
  V.keys.add("KeyD");
  V.hitstopLeft=0;
  V.update(STEP);
  if(V.player.sheatheT>0 && V.player.walkT>0 && V.player.gait===1 && Math.abs(V.destRect(V.player).rot)>0.003 && V.player.walkFadeHold>0.5){
    sslWalkReady=true;
    sslRotWalk=V.destRect(V.player).rot;
    sslWfh=V.player.walkFadeHold;
    sslAabb0=V.bodyAABB(V.player);
    sslShWalk=V.player.sheatheT;
    break;
  }
}
check("sheathe settle lean under walkFadeHold live mid-hump walk ready", sslWalkReady && Math.abs(sslRotWalk)>0.003 && sslShWalk>0, `ready=${sslWalkReady} rot=${sslRotWalk.toFixed(4)} sh=${sslShWalk|0} wfh=${sslWfh.toFixed(3)}`);
// release — settle arms; lean must keep through sheathe (not dump while dip rides)
V.keys.clear();
holdRivalStill();
V.hitstopLeft=0;
V.update(STEP);
const sslRot1=V.destRect(V.player).rot;
const sslSk1=V.walkSettleK(V.player);
const sslSettle1=V.player.walkSettleT;
const sslSh1=V.player.sheatheT;
const sslWfh1=V.player.walkFadeHold;
check("sheathe settle lean under walkFadeHold live release keeps lean", sslSh1>0 && sslSettle1>0 && sslSk1>0.9 && Math.abs(sslRot1-sslRotWalk)<0.03 && !(Math.abs(sslRot1)<Math.abs(sslRotWalk)-0.004), `sh=${sslSh1|0} settle=${sslSettle1} sk=${sslSk1.toFixed(3)} rot0=${sslRotWalk.toFixed(4)} rot1=${sslRot1.toFixed(4)} wfh=${sslWfh1.toFixed(3)}`);
check("sheathe settle lean under walkFadeHold live release does not dump lean", Math.abs(sslRot1) >= Math.abs(sslRotWalk)*0.85 - 0.001 && sslSk1>0.9, `rot0=${sslRotWalk.toFixed(4)} rot1=${sslRot1.toFixed(4)} sk=${sslSk1.toFixed(3)}`);
check("sheathe settle lean under walkFadeHold live release AABB planted", sslAabb0 && Math.abs(V.bodyAABB(V.player).y-sslAabb0.y)<0.05, `dy=${sslAabb0?(V.bodyAABB(V.player).y-sslAabb0.y).toFixed(2):"na"}`);
// ease under remaining sheathe
let sslRotMid=0, sslSkMid=0, sslShMid=0, sslSettleMid=0;
for(let i=0;i<3;i++){
  holdRivalStill();
  V.hitstopLeft=0;
  V.update(STEP);
  sslRotMid=V.destRect(V.player).rot;
  sslSkMid=V.walkSettleK(V.player);
  sslShMid=V.player.sheatheT;
  sslSettleMid=V.player.walkSettleT;
}
check("sheathe settle lean under walkFadeHold live lean eases under sheathe", sslSettleMid>0 && sslSettleMid<V.WALK_SETTLE_MS && sslSkMid>0 && sslSkMid<sslSk1+0.001 && Math.abs(sslRotMid)<=Math.abs(sslRot1)+0.01, `sh=${sslShMid|0} settle=${sslSettleMid} sk=${sslSkMid.toFixed(3)} sk0=${sslSk1.toFixed(3)} rot=${sslRotMid.toFixed(4)}`);

// live: idle clash sheathe (no walk) still no invented settle lean
bootPlay(); wait(1700);
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.player.x=500; V.rival.x=1100; V.keepApart();
V.player.phase="idle"; V.player.gait=0; V.player.walkT=0; V.player.walkSettleT=0; V.player.walkFadeHold=0;
V.player.walkRiseT=0; V.player.walkLeanGait=0; V.player.walkSettleRot=0; V.player.walkSettleOy=0;
V.player.guarding=false; V.player.stunT=0; V.player.boltPhase=""; V.player.falling=false; V.player.feintT=0;
V.player.cutRecBreathT=0; V.player.guardPoseK=0; V.player.hp=100; V.player.stamina=100;
V.rival.phase="idle"; V.rival.wantBlock=false; V.rival.gait=0;
V.player.sheatheT = V.SHEATHE_MS * 0.55;
V.player.clashRec=true; V.player.cut="slash"; V.player.phase="idle";
holdRivalStill(); V.update(STEP);
const sslIdleRot=V.destRect(V.player).rot;
const sslIdleSettle=V.player.walkSettleT;
const sslIdleSk=V.walkSettleK(V.player);
check("sheathe settle lean under walkFadeHold live idle sheathe still no settle", V.player.sheatheT>0 && V.player.gait===0 && V.player.walkT===0 && sslIdleSettle===0 && sslIdleSk===0 && Math.abs(sslIdleRot)<0.02, `ready=true settle=${sslIdleSettle} sk=${sslIdleSk} rot=${sslIdleRot.toFixed(4)} sh=${V.player.sheatheT|0}`);

check("sheathe settle lean under walkFadeHold rival walkSettleK shared", /tickGait\(rival/.test(srcTxt) && /if \(f\.sheatheT > 0\)/.test(sliceFn(codeOnly, "walkSettleK")) && !/f\.kind === "you"/.test(sliceFn(codeOnly, "walkSettleK")));
check("cache v=296 after sheathe settle lean under walkFadeHold live", /game.js\?v=350/.test(htmlTxt));



// --- v284 settle→plant destRect breath leftover: max(settle k, telegraphFade) ---
check("settle→plant destRect breath leftover seats no breath dump mid-settle tele", /Settle→plant destRect breath leftover/.test(srcTxt) && /mid-settle Space\/L\/throw\/K/.test(srcTxt) && /dump leftover settle breath/.test(srcTxt) && /keep-0 on/.test(srcTxt) && /walkFadeHold while walkSettleK still kept lean under restTeleWalk/.test(srcTxt) && /Same hole idle settle→guard/.test(srcTxt));
check("settle→plant destRect breath leftover is destRect-only", /destRect-only \(idleBreath\)/.test(srcTxt) && /Math\.max\(settle k, telegraphFade\)/.test(srcTxt) && /Math\.max\(sk, tk\)/.test(sliceFn(codeOnly, "idleBreath")) && /f\.walkSettleT > 0/.test(sliceFn(codeOnly, "idleBreath")) && !/Math\.max\(sk, tk\)/.test(sliceFn(codeOnly, "destRect")) && !/Math\.max\(sk, tk\)/.test(sliceFn(codeOnly, "bodyAABB")) && !/Math\.max\(sk, tk\)/.test(sliceFn(codeOnly, "poseBitmap")) && !/Math\.max\(sk, tk\)/.test(sliceFn(codeOnly, "walkSettleK")));
check("settle→plant destRect breath leftover telegraph max settle\/fade", /if \(telegraphing\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /const sk = walkSettleK\(f\)/.test(sliceFn(codeOnly, "idleBreath")) && /const tk = telegraphFade\(f\)/.test(sliceFn(codeOnly, "idleBreath")) && /1 - Math\.max\(sk, tk\)/.test(sliceFn(codeOnly, "idleBreath")) && /f\.walkSettleT > 0 && f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "idleBreath")));
check("settle→plant destRect breath leftover fresh walk-in still keep 0", /A\/D-held walk-in \/ rival closing still keep 0/.test(srcTxt) && /if \(walking\(f\) \|\| recoveryWalkOut\(f\)\) return 0;/.test(sliceFn(codeOnly, "idleBreath")) && /telegraph settle→walk/.test(srcTxt));
check("settle→plant destRect breath leftover clash\/link\/holdCut still hard-0", /Clash\/link\/holdCut still hard-0/.test(srcTxt) && /if \(clashPlanting\(f\)\) return 0;/.test(sliceFn(codeOnly, "idleBreath")) && /if \(linkPlanting\(f\)\) return 0;/.test(sliceFn(codeOnly, "idleBreath")) && /if \(holdCutPlanting\(f\)\) return 0;/.test(sliceFn(codeOnly, "idleBreath")));
check("settle→plant destRect breath leftover idle telegraph path unchanged", /idle telegraph still eases with telegraphFade/.test(srcTxt) && /return amp \* \(1 - telegraphFade\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")));
check("settle→plant destRect breath leftover tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("settle→plant destRect breath leftover is not a new combat verb", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.HITSTUN===350 && V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140 && V.WALK_SETTLE_MS===160 && V.WALK_STEP_MS===320 && V.FEINT_RECOVERY===100);
check("slash frames still 180/140/280 after settle→plant destRect breath leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after settle→plant destRect breath leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after settle→plant destRect breath leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after settle→plant destRect breath leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after settle→plant destRect breath leftover", /game.js\?v=350/.test(htmlTxt));

// live: mid-settle Space keeps settle breath (no dump under restTeleWalk lean)
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x = 400; V.rival.x = 1100;
V.player.guarding = false; V.player.stunT = 0; V.player.phase = "idle";
V.player.boltPhase = ""; V.player.falling = false; V.player.guardPoseK = 0; V.player.hp = 100;
V.player.sheatheT = 0; V.player.linkPlant = false; V.player.telegraph = false; V.player.cutRecBreathT = 0;
V.player.gait = 0; V.player.walkT = 0; V.player.walkSettleT = 0; V.player.walkFadeHold = 0; V.player.walkRiseT = 0;
V.player.walkSettleRot = 0; V.player.walkSettleOy = 0; V.player.walkLeanGait = 0;
V.rival.phase="idle"; V.rival.wantBlock=false;
// walk then release into mid-settle with live breath amp
V.keys.add("KeyD");
let spbReady=false;
for(let i=0;i<60;i++){
  freezeRivalAI();
  V.player.phase="idle"; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase=""; V.player.sheatheT=0;
  V.keys.add("KeyD");
  V.update(STEP);
  if(V.player.walkT>=150 && V.player.walkRiseT>=V.WALK_SETTLE_MS && Math.abs(V.destRect(V.player).rot)>0.03){
    spbReady=true;
    break;
  }
}
check("settle→plant destRect breath leftover live stride ready", spbReady && V.player.gait!==0, `ready=${spbReady} gait=${V.player.gait} rot=${V.destRect(V.player).rot.toFixed(4)}`);
V.keys.clear();
// drain settle into mid (sk < 0.98) while amp is live
let spbMid=false, spbBr0=0, spbSk0=0, spbSettle0=0, spbAabb0=null, spbWfh0=0;
for(let i=0;i<12;i++){
  freezeRivalAI();
  V.hitstopLeft=0;
  V.update(STEP);
  const sk=V.walkSettleK(V.player);
  const br=V.idleBreath(V.player);
  const amp=Math.abs(Math.sin((V.modeT/2400)*Math.PI*2)*1.6);
  if(V.player.walkSettleT>0 && sk>0.15 && sk<0.85 && Math.abs(br)>0.25 && amp>0.9){
    spbMid=true;
    spbBr0=br; spbSk0=sk; spbSettle0=V.player.walkSettleT; spbAabb0=V.bodyAABB(V.player); spbWfh0=V.player.walkFadeHold;
    break;
  }
}
check("settle→plant destRect breath leftover live mid-settle breath ready", spbMid && Math.abs(spbBr0)>0.25 && spbSk0<0.98, `mid=${spbMid} br0=${spbBr0.toFixed(3)} sk=${spbSk0.toFixed(3)} settle=${spbSettle0} wfh=${spbWfh0.toFixed(3)}`);
V.attackEdge = true;
V.update(STEP);
freezeRivalAI();
const spbBr1=V.idleBreath(V.player);
const spbSk1=V.walkSettleK(V.player);
const spbTk1=V.telegraphFade(V.player);
check("settle→plant destRect breath leftover live tele arms mid-settle", V.player.phase==="startup" && V.player.telegraph===true && V.player.walkSettleT>0 && spbSk1<0.98, `ph=${V.player.phase} tel=${V.player.telegraph} settle=${V.player.walkSettleT} sk=${spbSk1.toFixed(3)} tk=${spbTk1.toFixed(3)}`);
check("settle→plant destRect breath leftover live tele does not dump breath", Math.abs(spbBr1) >= Math.abs(spbBr0)*0.55 - 0.05 && !(Math.abs(spbBr1) < 0.08 && Math.abs(spbBr0) > 0.3), `br0=${spbBr0.toFixed(3)} br1=${spbBr1.toFixed(3)} sk0=${spbSk0.toFixed(3)} sk1=${spbSk1.toFixed(3)} tk=${spbTk1.toFixed(3)}`);
check("settle→plant destRect breath leftover live tele AABB planted", spbAabb0 && Math.abs(V.bodyAABB(V.player).y-spbAabb0.y)<0.05, `dy=${spbAabb0?(V.bodyAABB(V.player).y-spbAabb0.y).toFixed(2):"na"}`);

// live: fresh walk-in (sk≈1) still keep breath 0
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x = 400; V.rival.x = 1100;
V.player.guarding = false; V.player.stunT = 0; V.player.phase = "idle";
V.player.boltPhase = ""; V.player.falling = false; V.player.guardPoseK = 0; V.player.hp = 100;
V.player.sheatheT = 0; V.player.linkPlant = false; V.player.telegraph = false; V.player.cutRecBreathT = 0;
V.player.gait = 0; V.player.walkT = 0; V.player.walkSettleT = 0; V.player.walkFadeHold = 0; V.player.walkRiseT = 0;
V.keys.add("KeyD");
let spbWalkReady=false, spbWalkBr0=0;
for(let i=0;i<50;i++){
  freezeRivalAI();
  V.player.phase="idle"; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase=""; V.player.sheatheT=0;
  V.keys.add("KeyD");
  V.update(STEP);
  if(V.player.walkT>=150 && V.player.walkRiseT>=V.WALK_SETTLE_MS && Math.abs(V.destRect(V.player).rot)>0.03){
    spbWalkReady=true;
    spbWalkBr0=V.idleBreath(V.player);
    break;
  }
}
check("settle→plant destRect breath leftover live walk-in ready", spbWalkReady && Math.abs(spbWalkBr0)<0.08, `ready=${spbWalkReady} br0=${spbWalkBr0.toFixed(3)}`);
V.attackEdge = true;
V.update(STEP);
freezeRivalAI();
const spbWalkBr1=V.idleBreath(V.player);
const spbWalkSk1=V.walkSettleK(V.player);
check("settle→plant destRect breath leftover live walk-in keeps breath 0", V.player.phase==="startup" && V.player.telegraph===true && V.player.walkFadeHold>0.02 && spbWalkSk1>0.9 && Math.abs(spbWalkBr1)<0.12, `ph=${V.player.phase} tel=${V.player.telegraph} hold=${V.player.walkFadeHold} sk=${spbWalkSk1.toFixed(3)} br1=${spbWalkBr1.toFixed(3)}`);

// live: idle telegraph still eases (no settle)
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.gait=0; V.player.walkT=0; V.player.walkSettleT=0; V.player.walkFadeHold=0;
V.player.sheatheT=0; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase=""; V.player.cutRecBreathT=0;
// seek live amp
let spbIdleAmp=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ spbIdleAmp=true; break; }
  freezeRivalAI(); V.update(STEP);
}
const spbIdleBr0=V.idleBreath(V.player);
V.attackEdge = true;
V.update(STEP);
freezeRivalAI();
const spbIdleBr1=V.idleBreath(V.player);
const spbIdleTk=V.telegraphFade(V.player);
check("settle→plant destRect breath leftover live idle tele still eases", spbIdleAmp && V.player.phase==="startup" && V.player.telegraph===true && V.player.walkSettleT===0 && Math.abs(spbIdleBr1) <= Math.abs(spbIdleBr0)+0.05 && spbIdleTk>=0, `ready=${spbIdleAmp} br0=${spbIdleBr0.toFixed(3)} br1=${spbIdleBr1.toFixed(3)} tk=${spbIdleTk.toFixed(3)} settle=${V.player.walkSettleT}`);

check("settle→plant destRect breath leftover rival idleBreath shared", /if \(telegraphing\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /Math\.max\(sk, tk\)/.test(sliceFn(codeOnly, "idleBreath")) && !/f\.kind === "you"/.test(sliceFn(codeOnly, "idleBreath").slice(sliceFn(codeOnly, "idleBreath").indexOf("if (telegraphing(f))"), sliceFn(codeOnly, "idleBreath").indexOf("if (telegraphing(f))")+480)));

check("cache v=296 after settle→plant destRect breath leftover live", /game.js\?v=350/.test(htmlTxt));



// --- v285 feint settle→plant destRect breath leftover: max(settle k, feintFade) ---
check("feint settle→plant destRect breath leftover seats no breath dump mid-settle feint", /Feint settle→plant destRect breath leftover/.test(srcTxt) && /mid-settle Space cancel/.test(srcTxt) && /dump leftover settle breath the tick feint armed/.test(srcTxt) && /keep-0 on/.test(srcTxt) && /walkFadeHold while walkSettleK still kept lean under restFeintWalk/.test(srcTxt) && /Same hole tele settle→plant/.test(srcTxt));
check("feint settle→plant destRect breath leftover is destRect-only", /destRect-only \(idleBreath\)/.test(srcTxt) && /Math\.max\(settle k, feintFade\)/.test(srcTxt) && /Math\.max\(sk, fk\)/.test(sliceFn(codeOnly, "idleBreath")) && /f\.walkSettleT > 0 && f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "idleBreath")) && !/Math\.max\(sk, fk\)/.test(sliceFn(codeOnly, "destRect")) && !/Math\.max\(sk, fk\)/.test(sliceFn(codeOnly, "bodyAABB")) && !/Math\.max\(sk, fk\)/.test(sliceFn(codeOnly, "poseBitmap")) && !/Math\.max\(sk, fk\)/.test(sliceFn(codeOnly, "walkSettleK")) && !/Math\.max\(sk, fk\)/.test(sliceFn(codeOnly, "feintFade")));
check("feint settle→plant destRect breath leftover feint max settle\/fade", /if \(f\.feintT > 0\)/.test(sliceFn(codeOnly, "idleBreath")) && /const sk = walkSettleK\(f\)/.test(sliceFn(codeOnly, "idleBreath")) && /const fk = feintFade\(f\)/.test(sliceFn(codeOnly, "idleBreath")) && /1 - Math\.max\(sk, fk\)/.test(sliceFn(codeOnly, "idleBreath")) && /f\.walkSettleT > 0 && f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "idleBreath")));
check("feint settle→plant destRect breath leftover fresh walk-in still keep 0", /A\/D-held walk-in \/ rival closing still keep 0/.test(srcTxt) && /feint settle→walk[\s\S]{0,40}plant-release/.test(srcTxt) && /if \(walking\(f\) \|\| recoveryWalkOut\(f\)\) return 0;/.test(sliceFn(codeOnly, "idleBreath")) && /if \(f\.walkFadeHold > 0\.02\) return 0;/.test(sliceFn(codeOnly, "idleBreath")));
check("feint settle→plant destRect breath leftover tele settle→plant unchanged", /Math\.max\(sk, tk\)/.test(sliceFn(codeOnly, "idleBreath")) && /1 - Math\.max\(sk, tk\)/.test(sliceFn(codeOnly, "idleBreath")) && /if \(telegraphing\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /Settle→plant destRect breath leftover/.test(srcTxt));
check("feint settle→plant destRect breath leftover clash\/link\/holdCut still hard-0", /if \(clashPlanting\(f\)\) return 0;/.test(sliceFn(codeOnly, "idleBreath")) && /if \(linkPlanting\(f\)\) return 0;/.test(sliceFn(codeOnly, "idleBreath")) && /if \(holdCutPlanting\(f\)\) return 0;/.test(sliceFn(codeOnly, "idleBreath")));
check("feint settle→plant destRect breath leftover idle feint path unchanged", /Idle feint still eases with feintFade/.test(srcTxt) && /return amp \* \(1 - feintFade\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")));
check("feint settle→plant destRect breath leftover settle→guard unchanged", /Math\.max\(fk, rk\)/.test(sliceFn(codeOnly, "idleBreath")) && /1 - Math\.max\(fk, rk\)/.test(sliceFn(codeOnly, "idleBreath")) && /Feint settle→guard unchanged/.test(srcTxt));
check("feint settle→plant destRect breath leftover tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("feint settle→plant destRect breath leftover is not a new combat verb", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.HITSTUN===350 && V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140 && V.WALK_SETTLE_MS===160 && V.WALK_STEP_MS===320 && V.FEINT_RECOVERY===100);
check("slash frames still 180/140/280 after feint settle→plant destRect breath leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after feint settle→plant destRect breath leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after feint settle→plant destRect breath leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after feint settle→plant destRect breath leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after feint settle→plant destRect breath leftover", /game.js\?v=350/.test(htmlTxt));

// live: mid-settle Space→feint keeps settle breath (no dump under restFeintWalk lean)
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x = 400; V.rival.x = 1100;
V.player.guarding = false; V.player.stunT = 0; V.player.phase = "idle";
V.player.boltPhase = ""; V.player.falling = false; V.player.guardPoseK = 0; V.player.hp = 100;
V.player.sheatheT = 0; V.player.linkPlant = false; V.player.telegraph = false; V.player.cutRecBreathT = 0;
V.player.feintT = 0; V.player.gait = 0; V.player.walkT = 0; V.player.walkSettleT = 0; V.player.walkFadeHold = 0; V.player.walkRiseT = 0;
V.player.walkSettleRot = 0; V.player.walkSettleOy = 0; V.player.walkLeanGait = 0;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.keys.add("KeyD");
let fspReady=false;
for(let i=0;i<60;i++){
  freezeRivalAI();
  V.player.phase="idle"; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase=""; V.player.sheatheT=0; V.player.feintT=0;
  V.keys.add("KeyD");
  V.update(STEP);
  if(V.player.walkT>=150 && V.player.walkRiseT>=V.WALK_SETTLE_MS && Math.abs(V.destRect(V.player).rot)>0.03){
    fspReady=true;
    break;
  }
}
check("feint settle→plant destRect breath leftover live stride ready", fspReady && V.player.gait!==0, `ready=${fspReady} gait=${V.player.gait} rot=${V.destRect(V.player).rot.toFixed(4)}`);
V.keys.clear();
let fspMid=false, fspBr0=0, fspSk0=0, fspSettle0=0, fspAabb0=null, fspWfh0=0;
for(let i=0;i<12;i++){
  freezeRivalAI();
  V.hitstopLeft=0;
  V.update(STEP);
  const sk=V.walkSettleK(V.player);
  const br=V.idleBreath(V.player);
  const amp=Math.abs(Math.sin((V.modeT/2400)*Math.PI*2)*1.6);
  if(V.player.walkSettleT>0 && sk>0.15 && sk<0.85 && Math.abs(br)>0.25 && amp>0.9){
    fspMid=true;
    fspBr0=br; fspSk0=sk; fspSettle0=V.player.walkSettleT; fspAabb0=V.bodyAABB(V.player); fspWfh0=V.player.walkFadeHold;
    break;
  }
}
check("feint settle→plant destRect breath leftover live mid-settle breath ready", fspMid && Math.abs(fspBr0)>0.25 && fspSk0<0.98, `mid=${fspMid} br0=${fspBr0.toFixed(3)} sk=${fspSk0.toFixed(3)} settle=${fspSettle0} wfh=${fspWfh0.toFixed(3)}`);
// arm telegraph mid-settle first (tele path already keeps breath)
V.attackEdge = true;
V.update(STEP);
freezeRivalAI();
const fspBrTele=V.idleBreath(V.player);
const fspSkTele=V.walkSettleK(V.player);
check("feint settle→plant destRect breath leftover live tele mid-settle kept", V.player.phase==="startup" && V.player.telegraph===true && V.player.walkSettleT>0 && Math.abs(fspBrTele) >= Math.abs(fspBr0)*0.55 - 0.05, `ph=${V.player.phase} tel=${V.player.telegraph} br0=${fspBr0.toFixed(3)} brTele=${fspBrTele.toFixed(3)} sk=${fspSkTele.toFixed(3)}`);
// cancel into feint while settle/wfh still live (direct startFeint — Space edge drains settle)
let fspDid=false;
if(V.canFeint(V.player)) fspDid = !!V.startFeint(V.player);
freezeRivalAI();
V.hitstopLeft=0;
V.update(STEP);
if(V.player.feintT>0) fspDid=true;
const fspBr1=V.idleBreath(V.player);
const fspSk1=V.walkSettleK(V.player);
const fspFk1=V.feintFade(V.player);
const fspExp1=Math.sin((V.modeT/2400)*Math.PI*2)*1.6*(1-Math.max(fspSk1,fspFk1));
check("feint settle→plant destRect breath leftover live feint arms mid-settle", fspDid && V.player.feintT>0 && V.player.walkSettleT>0 && V.player.walkFadeHold>0.02 && fspSk1>0.05, `did=${fspDid} ft=${V.player.feintT|0} settle=${V.player.walkSettleT|0} wfh=${V.player.walkFadeHold.toFixed(3)} sk=${fspSk1.toFixed(3)} fk=${fspFk1.toFixed(3)}`);
check("feint settle→plant destRect breath leftover live feint does not dump breath", Math.abs(fspBr1-fspExp1)<0.08 && !(Math.abs(fspBr1)<0.01 && fspFk1<0.95 && fspSk1>0.2 && Math.abs(fspBrTele)>0.3), `brTele=${fspBrTele.toFixed(3)} br1=${fspBr1.toFixed(3)} exp=${fspExp1.toFixed(3)} sk1=${fspSk1.toFixed(3)} fk=${fspFk1.toFixed(3)}`);
check("feint settle→plant destRect breath leftover live feint AABB planted", fspAabb0 && Math.abs(V.bodyAABB(V.player).y-fspAabb0.y)<0.05, `dy=${fspAabb0?(V.bodyAABB(V.player).y-fspAabb0.y).toFixed(2):"na"}`);

// live: fresh walk-in (sk≈1 / D held) still keep breath 0 through feint
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x = 400; V.rival.x = 1100;
V.player.guarding = false; V.player.stunT = 0; V.player.phase = "idle";
V.player.boltPhase = ""; V.player.falling = false; V.player.guardPoseK = 0; V.player.hp = 100;
V.player.sheatheT = 0; V.player.linkPlant = false; V.player.telegraph = false; V.player.cutRecBreathT = 0; V.player.feintT = 0;
V.player.gait = 0; V.player.walkT = 0; V.player.walkSettleT = 0; V.player.walkFadeHold = 0; V.player.walkRiseT = 0;
V.keys.add("KeyD");
let fspWalkReady=false, fspWalkBr0=0;
for(let i=0;i<50;i++){
  freezeRivalAI();
  V.player.phase="idle"; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase=""; V.player.sheatheT=0; V.player.feintT=0;
  V.keys.add("KeyD");
  V.update(STEP);
  if(V.player.walkT>=150 && V.player.walkRiseT>=V.WALK_SETTLE_MS && Math.abs(V.destRect(V.player).rot)>0.03){
    fspWalkReady=true;
    fspWalkBr0=V.idleBreath(V.player);
    break;
  }
}
check("feint settle→plant destRect breath leftover live walk-in ready", fspWalkReady && Math.abs(fspWalkBr0)<0.08, `ready=${fspWalkReady} br0=${fspWalkBr0.toFixed(3)}`);
V.attackEdge = true;
V.keys.add("KeyD");
V.update(STEP);
freezeRivalAI();
let fspWalkDid=false;
V.keys.add("KeyD");
if(V.canFeint(V.player)) fspWalkDid = !!V.startFeint(V.player);
V.keys.add("KeyD");
freezeRivalAI();
V.hitstopLeft=0;
V.update(STEP);
if(V.player.feintT>0) fspWalkDid=true;
const fspWalkBr1=V.idleBreath(V.player);
check("feint settle→plant destRect breath leftover live walk-in keeps breath 0", fspWalkDid && V.player.feintT>0 && (V.player.walkFadeHold>0.02 || V.recoveryWalkOut(V.player)) && Math.abs(fspWalkBr1)<0.12, `did=${fspWalkDid} ft=${V.player.feintT|0} hold=${V.player.walkFadeHold.toFixed(3)} rwo=${V.recoveryWalkOut(V.player)} br1=${fspWalkBr1.toFixed(3)}`);

// live: idle feint still eases (no settle)
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.gait=0; V.player.walkT=0; V.player.walkSettleT=0; V.player.walkFadeHold=0;
V.player.sheatheT=0; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase=""; V.player.cutRecBreathT=0; V.player.feintT=0;
let fspIdleAmp=false;
for(let i=0;i<500;i++){
  const a=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
  if(Math.abs(a)>1.2){ fspIdleAmp=true; break; }
  freezeRivalAI(); V.update(STEP);
}
const fspIdleBr0=V.idleBreath(V.player);
V.attackEdge = true;
V.update(STEP);
freezeRivalAI();
const fspIdleOk=V.startFeint(V.player);
V.update(STEP);
freezeRivalAI();
const fspIdleBr1=V.idleBreath(V.player);
const fspIdleFk=V.feintFade(V.player);
check("feint settle→plant destRect breath leftover live idle feint still eases", fspIdleAmp && fspIdleOk && V.player.feintT>0 && V.player.walkSettleT===0 && fspIdleFk>0.8 && Math.abs(fspIdleBr1) <= Math.abs(fspIdleBr0)+0.05 + Math.abs(Math.sin((V.modeT/2400)*Math.PI*2)*1.6)*(1-fspIdleFk)+0.05, `ready=${fspIdleAmp} ok=${fspIdleOk} br0=${fspIdleBr0.toFixed(3)} br1=${fspIdleBr1.toFixed(3)} fk=${fspIdleFk.toFixed(3)} settle=${V.player.walkSettleT}`);

check("feint settle→plant destRect breath leftover rival idleBreath shared", /if \(f\.feintT > 0\)/.test(sliceFn(codeOnly, "idleBreath")) && /Math\.max\(sk, fk\)/.test(sliceFn(codeOnly, "idleBreath")) && !/f\.kind === "you"/.test(sliceFn(codeOnly, "idleBreath").slice(sliceFn(codeOnly, "idleBreath").indexOf("if (f.feintT > 0)"), sliceFn(codeOnly, "idleBreath").indexOf("if (f.feintT > 0)")+680)));
check("cache v=296 after feint settle→plant destRect breath leftover live", /game.js\?v=350/.test(htmlTxt));





// --- tipX under plant fades leftover (v286) ---
check("tipX under plant fades leftover seats no tip dump mid-fade link", /TipX under plant fades leftover/.test(srcTxt) && /connected slash-L \/ clash-Space\/L used to dump/.test(srcTxt) && /bladeTip onto windup the same tick poseBitmap flipped/.test(srcTxt) && /linkPlantFade \/ clashPlantFade/.test(srcTxt) && /tip juice hopped/.test(srcTxt));
check("tipX under plant fades leftover is draw-only", /Draw-only \(bladeTipX \/ bladeTipY\)/.test(srcTxt) && /Ease slash→pose tip with that leftover fade/.test(srcTxt) && /clashPlantFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /linkPlantFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /poseFamily\(f\)\.slash/.test(sliceFn(codeOnly, "bladeTipX")) && !/bladeTipX/.test(sliceFn(codeOnly, "destRect")) && !/linkPlantFade/.test(sliceFn(codeOnly, "bodyAABB")) && !/clashPlantFade/.test(sliceFn(codeOnly, "poseBitmap")));
check("tipX under plant fades leftover eases slash→pose tip", (/sx \+ \(tip - sx\) \* \(1 - k\)/.test(sliceFn(codeOnly, "bladeTipX")) || /from \+ \(tip - from\) \* \(1 - k\)/.test(sliceFn(codeOnly, "bladeTipX"))) && /Math\.max\(clashPlantFade\(f\), linkPlantFade\(f\)\)/.test(sliceFn(codeOnly, "bladeTipX")) && /sy \+ \(tip - sy\) \* \(1 - k\)/.test(sliceFn(codeOnly, "bladeTipY")));
check("tipX under plant fades leftover holdCut still slash tip", /HoldCut still slash tip/.test(srcTxt) && /holdingCutBolt/.test(srcTxt) && /holdCutFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /holdCutFade\(f\)/.test(sliceFn(codeOnly, "bladeTipY")) && /after fade tip stays slash|HoldCut still slash tip after holdCutFade/.test(srcTxt));
check("tipX under plant fades leftover castPlantXY K path unchanged", /clashPlantFade\(f\)/.test(sliceFn(codeOnly, "castPlantXY")) && /linkPlantFade\(f\)/.test(sliceFn(codeOnly, "castPlantXY")) && /poseFamily\(f\)\.slash/.test(sliceFn(codeOnly, "castPlantXY")) && /holdingCutBolt\(f\)/.test(sliceFn(codeOnly, "castPlantXY")));
check("tipX under plant fades leftover tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("tipX under plant fades leftover is not a new combat verb", /No new combat verb/.test(srcTxt) && (/Active hitbox still phase===active only/.test(srcTxt) || /active hitbox unchanged/.test(srcTxt)));
check("slash frames still 180\/140\/280 after tipX under plant fades leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280);
check("golpe frames still 120\/80\/180 after tipX under plant fades leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180);
check("K frames still 200\/280 after tipX under plant fades leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280);
check("no 6th button after tipX under plant fades leftover", !/KeyM/.test(codeOnly));
check("cache v=296 after tipX under plant fades leftover", /game.js\?v=350/.test(htmlTxt));

// live: connected slash-L tip eases under linkPlantFade (mirror landCloseSlash path)
landCloseSlash();
const tipLinkWin = waitUntilWindow();
const tipSlashLive = V.bladeTipX(V.player);
const tipLinkLeft = V.player.phase==="recovery" ? (V.RECOVERY - V.player.phaseT) : -1;
check("tipX under plant fades leftover live slash tip ready", tipLinkWin && V.player.phase === "recovery" && V.player.cutHit === true && V.poseSheet(V.player).tipX === 991, `win=${tipLinkWin} ph=${V.player.phase} sheetTip=${V.poseSheet(V.player).tipX} tip=${tipSlashLive.toFixed(2)} left=${tipLinkLeft}`);
V.golpeEdge = true;
holdRival();
V.update(STEP);
const tipLink0 = V.bladeTipX(V.player);
const lf0 = V.linkPlantFade(V.player);
const sheetTip0 = V.poseSheet(V.player).tipX;
check("tipX under plant fades leftover live link arms", V.player.linkPlant === true && V.player.phase === "startup" && V.player.cut === "golpe" && lf0 > 0.8 && sheetTip0 === 359, `lp=${V.player.linkPlant} ph=${V.player.phase} cut=${V.player.cut} lf=${lf0} sheetTip=${sheetTip0}`);
// Dump would put tip on windup immediately (~240px from slash). Ease keeps tip near slash while lf high.
check("tipX under plant fades leftover live tip eases not dumps", Math.abs(tipLink0 - tipSlashLive) < 80 && Math.abs(tipLink0 - tipSlashLive) > 0.5, `tip=${tipLink0.toFixed(2)} slash=${tipSlashLive.toFixed(2)} d=${(tipLink0-tipSlashLive).toFixed(2)} lf=${lf0}`);
check("tipX under plant fades leftover live AABB planted", Math.abs(V.bodyAABB(V.player).y - 316.693) < 2, `ay=${V.bodyAABB(V.player).y.toFixed(2)}`);
let tipTrackOk = true, tipPrev = tipLink0, tipEnd = tipLink0;
for (let i = 0; i < 10; i++) {
  if (!V.player.linkPlant || V.linkPlantFade(V.player) <= 0) break;
  const tip = V.bladeTipX(V.player);
  // tip should move from slash toward windup (decreasing for you facing +1)
  if (tip > tipPrev + 2) tipTrackOk = false;
  tipPrev = tip;
  tipEnd = tip;
  holdRival();
  V.update(STEP);
}
check("tipX under plant fades leftover live tip tracks fade", tipTrackOk && Math.abs(tipEnd - tipSlashLive) > 150, `track=${tipTrackOk} end=${tipEnd.toFixed(2)} slash=${tipSlashLive.toFixed(2)} d=${(tipEnd-tipSlashLive).toFixed(2)}`);

// holdCut tip still slash (no ease dump off cut)
landCloseSlash();
const tipHcWin = waitUntilBoltWindow("slash");
const tipHcPre = V.bladeTipX(V.player);
V.boltEdge = true;
holdRival();
V.update(STEP);
const tipHc0 = V.bladeTipX(V.player);
const hf0 = V.holdCutFade(V.player);
check("tipX under plant fades leftover live holdCut still slash tip", tipHcWin && V.player.boltPhase === "startup" && V.holdCutPlanting(V.player) && hf0 > 0.8 && Math.abs(tipHc0 - tipHcPre) < 12, `win=${tipHcWin} bph=${V.player.boltPhase} hc=${V.holdCutPlanting(V.player)} hf=${hf0} tip0=${tipHcPre.toFixed(2)} tip1=${tipHc0.toFixed(2)}`);

check("tipX under plant fades leftover rival bladeTip shared", /function bladeTipX/.test(srcTxt) && /clashPlantFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /linkPlantFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")));
check("cache v=296 after tipX under plant fades leftover live", /game.js\?v=350/.test(htmlTxt));

// --- v287 reversal/throw-guard settle→plant destRect breath leftover ---
check("reversal/throw-guard settle→plant destRect breath leftover seats no breath dump mid-settle reverse", /Reversal\/throw-guard settle→plant destRect breath leftover/.test(srcTxt) && /mid-settle L \/ Space\+S/.test(srcTxt) && /dump leftover settle breath/.test(srcTxt) && /phase!==idle hard-0 while walkSettleK still kept lean/.test(srcTxt) && /restRevWalk \/ restThrowGuardWalk/.test(srcTxt) && /Same hole tele\/feint[\s\S]{0,40}settle→plant/.test(srcTxt));
check("reversal/throw-guard settle→plant destRect breath leftover is destRect-only", /destRect-only \(idleBreath\)/.test(srcTxt) && /Math\.max\(settle k,[\s\S]{0,80}reversalPlantFade \/ throwGuardPlantFade\)/.test(srcTxt) && /1 - Math\.max\(sk, rf\)/.test(sliceFn(codeOnly, "idleBreath")) && /1 - Math\.max\(sk, tgf\)/.test(sliceFn(codeOnly, "idleBreath")) && /f\.walkSettleT > 0 && f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "idleBreath")) && !/Math\.max\(sk, rf\)/.test(sliceFn(codeOnly, "destRect")) && !/Math\.max\(sk, rf\)/.test(sliceFn(codeOnly, "bodyAABB")) && !/Math\.max\(sk, rf\)/.test(sliceFn(codeOnly, "poseBitmap")) && !/Math\.max\(sk, tgf\)/.test(sliceFn(codeOnly, "walkSettleK")));
check("reversal/throw-guard settle→plant destRect breath leftover reverse max settle\/fade", /if \(reversalPlanting\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /const sk = walkSettleK\(f\)/.test(sliceFn(codeOnly, "idleBreath")) && /const rf = reversalPlantFade\(f\)/.test(sliceFn(codeOnly, "idleBreath")) && /1 - Math\.max\(sk, rf\)/.test(sliceFn(codeOnly, "idleBreath")) && /return amp \* \(1 - reversalPlantFade\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")));
check("reversal/throw-guard settle→plant destRect breath leftover throw max settle\/fade", /if \(throwGuardPlanting\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /const tgf = throwGuardPlantFade\(f\)/.test(sliceFn(codeOnly, "idleBreath")) && /1 - Math\.max\(sk, tgf\)/.test(sliceFn(codeOnly, "idleBreath")) && /return amp \* \(1 - throwGuardPlantFade\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")));
check("reversal/throw-guard settle→plant destRect breath leftover fresh walk-in still keep 0", /A\/D-held walk-in \/[\s\S]{0,40}rival closing still keep 0/.test(srcTxt) && /if \(walking\(f\) \|\| recoveryWalkOut\(f\)\) return 0;/.test(sliceFn(codeOnly, "idleBreath")) && /if \(f\.walkFadeHold > 0\.02\) return 0;/.test(sliceFn(codeOnly, "idleBreath")));
check("reversal/throw-guard settle→plant destRect breath leftover tele\/feint settle→plant unchanged", /Math\.max\(sk, tk\)/.test(sliceFn(codeOnly, "idleBreath")) && /1 - Math\.max\(sk, tk\)/.test(sliceFn(codeOnly, "idleBreath")) && /Math\.max\(sk, fk\)/.test(sliceFn(codeOnly, "idleBreath")) && /1 - Math\.max\(sk, fk\)/.test(sliceFn(codeOnly, "idleBreath")) && /Settle→plant destRect breath leftover/.test(srcTxt) && /Feint settle→plant destRect breath leftover/.test(srcTxt));
check("reversal/throw-guard settle→plant destRect breath leftover clash\/link\/holdCut still hard-0", /if \(clashPlanting\(f\)\) return 0;/.test(sliceFn(codeOnly, "idleBreath")) && /if \(linkPlanting\(f\)\) return 0;/.test(sliceFn(codeOnly, "idleBreath")) && /if \(holdCutPlanting\(f\)\) return 0;/.test(sliceFn(codeOnly, "idleBreath")));
check("reversal/throw-guard settle→plant destRect breath leftover eases tip block→pose", /Ease block→pose tip with that leftover fade/.test(srcTxt) || /ease block→pose tip with max\(reversalPlantFade, throwGuardPlantFade\)/.test(srcTxt) || /Reversal\/throw-guard settle→plant tip leftover/.test(srcTxt));
check("reversal/throw-guard settle→plant destRect breath leftover tip uses plant fades", /Math\.max\(reversalPlantFade\(f\), throwGuardPlantFade\(f\), techGuardPlantFade\(f\)\)/.test(sliceFn(codeOnly, "bladeTipX")) && /Math\.max\(reversalPlantFade\(f\), throwGuardPlantFade\(f\), techGuardPlantFade\(f\)\)/.test(sliceFn(codeOnly, "bladeTipY")) && /poseFamily\(f\)\.block/.test(sliceFn(codeOnly, "bladeTipX")) && /bx \+ \(tip - bx\) \* \(1 - tipPlantK\)/.test(sliceFn(codeOnly, "bladeTipX")) && /by \+ \(tip - by\) \* \(1 - tipPlantK\)/.test(sliceFn(codeOnly, "bladeTipY")));
check("reversal/throw-guard settle→plant destRect breath leftover tipX under plant fades unchanged", /Math\.max\(clashPlantFade\(f\), linkPlantFade\(f\)\)/.test(sliceFn(codeOnly, "bladeTipX")) && (/sx \+ \(tip - sx\) \* \(1 - k\)/.test(sliceFn(codeOnly, "bladeTipX")) || /from \+ \(tip - from\) \* \(1 - k\)/.test(sliceFn(codeOnly, "bladeTipX"))) && /HoldCut still slash tip/.test(srcTxt));
check("reversal/throw-guard settle→plant destRect breath leftover tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("reversal/throw-guard settle→plant destRect breath leftover is not a new combat verb", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.HITSTUN===350 && V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140 && V.WALK_SETTLE_MS===160 && V.WALK_STEP_MS===320 && V.REVERSAL_STAM===30 && V.REVERSAL_INVULN===120);
check("slash frames still 180/140/280 after reversal/throw-guard settle→plant destRect breath leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after reversal/throw-guard settle→plant destRect breath leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after reversal/throw-guard settle→plant destRect breath leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after reversal/throw-guard settle→plant destRect breath leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=296 after reversal/throw-guard settle→plant destRect breath leftover", /game.js\?v=350/.test(htmlTxt));

// live: mid-settle → guard → reverse keeps settle breath (no dump) + tip eases
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x = 400; V.rival.x = 1100;
V.player.guarding = false; V.player.stunT = 0; V.player.phase = "idle";
V.player.boltPhase = ""; V.player.falling = false; V.player.guardPoseK = 0; V.player.hp = 100;
V.player.sheatheT = 0; V.player.linkPlant = false; V.player.telegraph = false; V.player.cutRecBreathT = 0;
V.player.feintT = 0; V.player.reversal = false; V.player.gait = 0; V.player.walkT = 0; V.player.walkSettleT = 0; V.player.walkFadeHold = 0; V.player.walkRiseT = 0;
V.player.walkSettleRot = 0; V.player.walkSettleOy = 0; V.player.walkLeanGait = 0;
V.rival.phase="idle"; V.rival.wantBlock=false;
V.keys.add("KeyD");
let rspReady=false;
for(let i=0;i<60;i++){
  freezeRivalAI();
  V.player.phase="idle"; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase=""; V.player.sheatheT=0; V.player.feintT=0; V.player.reversal=false;
  V.keys.add("KeyD");
  V.update(STEP);
  if(V.player.walkT>=150 && V.player.walkRiseT>=V.WALK_SETTLE_MS && Math.abs(V.destRect(V.player).rot)>0.03){
    rspReady=true;
    break;
  }
}
check("reversal/throw-guard settle→plant destRect breath leftover live stride ready", rspReady && V.player.gait!==0, `ready=${rspReady} gait=${V.player.gait} rot=${V.destRect(V.player).rot.toFixed(4)}`);
V.keys.clear();
let rspMid=false, rspBr0=0, rspSk0=0, rspSettle0=0, rspAabb0=null, rspWfh0=0;
for(let i=0;i<12;i++){
  freezeRivalAI();
  V.hitstopLeft=0;
  V.update(STEP);
  const sk=V.walkSettleK(V.player);
  const br=V.idleBreath(V.player);
  const amp=Math.abs(Math.sin((V.modeT/2400)*Math.PI*2)*1.6);
  if(V.player.walkSettleT>0 && sk>0.15 && sk<0.85 && Math.abs(br)>0.25 && amp>0.9){
    rspMid=true;
    rspBr0=br; rspSk0=sk; rspSettle0=V.player.walkSettleT; rspAabb0=V.bodyAABB(V.player); rspWfh0=V.player.walkFadeHold;
    break;
  }
}
check("reversal/throw-guard settle→plant destRect breath leftover live mid-settle breath ready", rspMid && Math.abs(rspBr0)>0.25 && rspSk0<0.98, `mid=${rspMid} br0=${rspBr0.toFixed(3)} sk=${rspSk0.toFixed(3)} settle=${rspSettle0} wfh=${rspWfh0.toFixed(3)}`);
// raise mid-settle then reverse
V.keys.add("KeyS");
V.update(STEP);
freezeRivalAI();
const rspBrGuard=V.idleBreath(V.player);
const rspSkGuard=V.walkSettleK(V.player);
const rspTipGuard=V.bladeTipX(V.player);
const rspGpkGuard=V.player.guardPoseK;
check("reversal/throw-guard settle→plant destRect breath leftover live guard mid-settle", V.player.guarding===true && V.player.walkSettleT>0 && V.player.walkFadeHold>0.02 && rspSkGuard>0.05, `g=${V.player.guarding} settle=${V.player.walkSettleT|0} wfh=${V.player.walkFadeHold.toFixed(3)} sk=${rspSkGuard.toFixed(3)} br=${rspBrGuard.toFixed(3)} gpk=${rspGpkGuard.toFixed(3)}`);
V.golpeEdge=true;
V.keys.add("KeyL");
V.update(STEP);
V.keys.delete("KeyL");
freezeRivalAI();
const rspBr1=V.idleBreath(V.player);
const rspSk1=V.walkSettleK(V.player);
const rspRf1=V.reversalPlantFade(V.player);
const rspTip1=V.bladeTipX(V.player);
const amp1=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
const rspExp1=amp1*(1-Math.max(rspSk1, rspRf1));
const rspRect=V.destRect(V.player);
const rspSheet=V.poseSheet(V.player);
const rspSc=rspRect.dw/rspSheet.w; // poseScale — windup owns sheet on reverse
const rspBx=V.player.facing>0 ? rspRect.dx+rspRect.dw-8 : rspRect.dx+8;
const rspWx=rspSheet.tipX!=null ? rspRect.dx+rspSheet.tipX*rspSc : rspBx;
const rspRaiseMs=V.GUARD_RAISE_MS||80;
const rspU=Math.max(0,Math.min(1,(V.player.phaseT||0)/rspRaiseMs));
const rspRaiseT=rspU*rspU*(3-2*rspU);
const rspTipPlantK=Math.max(rspRf1, 1-rspRaiseT);
const tipExp=rspBx+(rspWx-rspBx)*(1-rspTipPlantK);
check("reversal/throw-guard settle→plant destRect breath leftover live reverse arms mid-settle", V.player.reversal===true && V.player.cut==="golpe" && V.player.phase==="startup" && rspRf1>0.05 && V.player.walkSettleT>0 && V.player.walkFadeHold>0.02 && rspSk1>0.05, `rev=${V.player.reversal} cut=${V.player.cut} ph=${V.player.phase} settle=${V.player.walkSettleT|0} wfh=${V.player.walkFadeHold.toFixed(3)} sk=${rspSk1.toFixed(3)} rf=${rspRf1.toFixed(3)}`);
check("reversal/throw-guard settle→plant destRect breath leftover live reverse does not dump breath", Math.abs(rspBr1-rspExp1)<0.08 && !(Math.abs(rspBr1)<0.01 && rspRf1<0.95 && rspSk1>0.2 && Math.abs(rspBrGuard)>0.15), `brG=${rspBrGuard.toFixed(3)} br1=${rspBr1.toFixed(3)} exp=${rspExp1.toFixed(3)} sk1=${rspSk1.toFixed(3)} rf=${rspRf1.toFixed(3)}`);
check("reversal/throw-guard settle→plant destRect breath leftover live tip eases not dumps", rspRf1>0.05 && Math.abs(rspTip1-rspTipGuard)>5 && Math.abs(rspTip1-rspWx)>15 && Math.abs(rspTip1-tipExp)<8, `tipG=${rspTipGuard.toFixed(2)} tip1=${rspTip1.toFixed(2)} wx=${rspWx.toFixed(2)} exp=${tipExp.toFixed(2)} dG=${(rspTip1-rspTipGuard).toFixed(2)} rf=${rspRf1.toFixed(3)} tipK=${rspTipPlantK.toFixed(3)}`);
check("reversal/throw-guard settle→plant destRect breath leftover live AABB planted", rspAabb0 && Math.abs(V.bodyAABB(V.player).y-rspAabb0.y)<0.05, `dy=${rspAabb0?(V.bodyAABB(V.player).y-rspAabb0.y).toFixed(2):"na"}`);

// idle guard reverse still eases with plant fade (breath ~0 at full raise)
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=1000;
Object.assign(V.player,{phase:"idle",phaseT:0,boltPhase:"",stunT:0,guarding:false,falling:false,feintT:0,sheatheT:0,cutRecBreathT:0,guardPoseK:0,walkSettleT:0,walkFadeHold:0,walkRiseT:0,gait:0,walkT:0,walkSettleRot:0,walkSettleOy:0,reversal:false,telegraph:false,linkPlant:false,hp:100,stamina:100});
V.keys.add("KeyS");
for(let i=0;i<20;i++){
  freezeRivalAI();
  V.player.stamina=100; V.player.phase="idle"; V.player.boltPhase=""; V.player.stunT=0; V.player.falling=false;
  V.keys.add("KeyS");
  V.update(STEP);
  if(V.player.guarding && V.player.guardPoseK>0.95) break;
}
let rspIdleAmp=false;
for(let i=0;i<120;i++){
  freezeRivalAI();
  V.player.stamina=100; V.player.phase="idle"; V.player.boltPhase=""; V.player.stunT=0; V.player.falling=false; V.player.reversal=false;
  V.keys.add("KeyS");
  V.update(STEP);
  if(V.player.guarding && V.player.guardPoseK>0.95 && Math.abs(Math.sin((V.modeT/2400)*Math.PI*2)*1.6)>0.9){
    rspIdleAmp=true; break;
  }
}
const rspIdleBr0=V.idleBreath(V.player);
const rspIdleTip0=V.bladeTipX(V.player);
const rspIdleK0=V.player.guardPoseK;
V.player.stamina=100;
const rspIdleOk=V.startReversal(V.player);
freezeRivalAI();
const rspIdleBr1=V.idleBreath(V.player);
const rspIdleRf=V.reversalPlantFade(V.player);
const rspIdleTip1=V.bladeTipX(V.player);
const rspIdleExp=Math.sin((V.modeT/2400)*Math.PI*2)*1.6*(1-rspIdleRf);
check("reversal/throw-guard settle→plant destRect breath leftover live idle reverse still eases", rspIdleAmp && rspIdleOk && V.player.reversal===true && V.player.cut==="golpe" && V.player.phase==="startup" && V.player.walkSettleT===0 && rspIdleRf>0.8 && Math.abs(rspIdleBr1-rspIdleExp)<0.08, `ready=${rspIdleAmp} ok=${rspIdleOk} rev=${V.player.reversal} rf=${rspIdleRf.toFixed(3)} br0=${rspIdleBr0.toFixed(3)} br1=${rspIdleBr1.toFixed(3)} exp=${rspIdleExp.toFixed(3)} settle=${V.player.walkSettleT} k0=${rspIdleK0.toFixed(3)}`);
check("reversal/throw-guard settle→plant destRect breath leftover live idle reverse tip stays planted", rspIdleOk && rspIdleRf>0.8 && Math.abs(rspIdleTip1-rspIdleTip0)<25, `ok=${rspIdleOk} tip0=${rspIdleTip0.toFixed(2)} tip1=${rspIdleTip1.toFixed(2)} rf=${rspIdleRf.toFixed(3)}`);

check("reversal/throw-guard settle→plant destRect breath leftover rival idleBreath shared", /if \(reversalPlanting\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /Math\.max\(sk, rf\)/.test(sliceFn(codeOnly, "idleBreath")) && /if \(throwGuardPlanting\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && !/f\.kind === "you"/.test(sliceFn(codeOnly, "idleBreath").slice(sliceFn(codeOnly, "idleBreath").indexOf("if (reversalPlanting(f))"), sliceFn(codeOnly, "idleBreath").indexOf("if (reversalPlanting(f))")+720)));
check("cache v=296 after reversal/throw-guard settle→plant destRect breath leftover live", /game.js\?v=350/.test(htmlTxt));



// --- tip under telegraphFade leftover (v288) ---
check("tip under telegraphFade leftover seats no tip dump mid-tele", /Tip under telegraphFade leftover/.test(srcTxt) && /idle\/walk Space\/L\/throw\/K used to dump/.test(srcTxt) && /bladeTip onto windup/.test(srcTxt) && /leftover idle still owned the sheet \(telegraphFade\)/.test(srcTxt) && /tip juice hopped/.test(srcTxt));
check("tip under telegraphFade leftover is draw-only", /Draw-only \(bladeTipX \/ bladeTipY\)/.test(srcTxt) && /Ease idle-edge→pose tip with telegraphFade/.test(srcTxt) && /telegraphing\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /telegraphFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /ex \+ \(tip - ex\) \* tf/.test(sliceFn(codeOnly, "bladeTipX")) && /ey \+ \(tip - ey\) \* tf/.test(sliceFn(codeOnly, "bladeTipY")) && !/telegraphFade/.test(sliceFn(codeOnly, "destRect")) && !/telegraphFade/.test(sliceFn(codeOnly, "bodyAABB")) && !/telegraphFade/.test(sliceFn(codeOnly, "poseBitmap")));
check("tip under telegraphFade leftover eases idle-edge→pose tip", /if \(telegraphing\(f\)\)/.test(sliceFn(codeOnly, "bladeTipX")) && /const tf = telegraphFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /tf < 0\.98/.test(sliceFn(codeOnly, "bladeTipX")) && /ex \+ \(tip - ex\) \* tf/.test(sliceFn(codeOnly, "bladeTipX")) && /ey \+ \(tip - ey\) \* tf/.test(sliceFn(codeOnly, "bladeTipY")));
check("tip under telegraphFade leftover tipX under plant fades unchanged", /Math\.max\(clashPlantFade\(f\), linkPlantFade\(f\)\)/.test(sliceFn(codeOnly, "bladeTipX")) && (/sx \+ \(tip - sx\) \* \(1 - k\)/.test(sliceFn(codeOnly, "bladeTipX")) || /from \+ \(tip - from\) \* \(1 - k\)/.test(sliceFn(codeOnly, "bladeTipX"))) && /HoldCut still slash tip/.test(srcTxt));
check("tip under telegraphFade leftover reversal tip unchanged", /Math\.max\(reversalPlantFade\(f\), throwGuardPlantFade\(f\), techGuardPlantFade\(f\)\)/.test(sliceFn(codeOnly, "bladeTipX")) && /bx \+ \(tip - bx\) \* \(1 - tipPlantK\)/.test(sliceFn(codeOnly, "bladeTipX")) && /by \+ \(tip - by\) \* \(1 - tipPlantK\)/.test(sliceFn(codeOnly, "bladeTipY")));
check("tip under telegraphFade leftover castPlantXY K path unchanged", /clashPlantFade\(f\)/.test(sliceFn(codeOnly, "castPlantXY")) && /linkPlantFade\(f\)/.test(sliceFn(codeOnly, "castPlantXY")) && /poseFamily\(f\)\.slash/.test(sliceFn(codeOnly, "castPlantXY")) && /holdingCutBolt\(f\)/.test(sliceFn(codeOnly, "castPlantXY")) && !/telegraphFade/.test(sliceFn(codeOnly, "castPlantXY")));
check("tip under telegraphFade leftover tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("tip under telegraphFade leftover is not a new combat verb", /No new combat verb/.test(srcTxt) && (/Active hitbox still phase===active only/.test(srcTxt) || /active hitbox unchanged/.test(srcTxt)));
check("slash frames still 180\/140\/280 after tip under telegraphFade leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280);
check("golpe frames still 120\/80\/180 after tip under telegraphFade leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180);
check("K frames still 200\/280 after tip under telegraphFade leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280);
check("no 6th button after tip under telegraphFade leftover", !/KeyM/.test(codeOnly));
check("cache v=296 after tip under telegraphFade leftover", /game.js\?v=350/.test(htmlTxt));

// live: idle Space tele tip eases not dumps
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkT=0; V.player.walkSettleT=0; V.player.walkFadeHold=0;
V.player.sheatheT=0; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.cutRecBreathT=0; V.player.telegraph=false; V.player.linkPlant=false; V.player.clashPlant=false;
V.player.reversal=false; V.player.facing=1; V.openLeft=0; V.hitstopLeft=0;
V.player.guardPoseK=0; V.player.leftoverPlantTip=false;
freezeRivalAI(); V.update(STEP);
const teleTip0 = V.bladeTipX(V.player);
const teleR0 = V.destRect(V.player);
const teleEdge0 = V.player.facing>0 ? teleR0.dx+teleR0.dw-8 : teleR0.dx+8;
const teleWu0 = teleR0.dx + 359 * (teleR0.dw/1186);
V.attackEdge = true;
V.update(STEP);
freezeRivalAI();
const teleArmTip = V.bladeTipX(V.player);
const teleArmTf = V.telegraphFade(V.player);
const teleArmTel = V.telegraphing(V.player);
check("tip under telegraphFade leftover live idle tip ready", Math.abs(teleTip0-teleEdge0)<1.5 && V.player.phase==="startup", `tip0=${teleTip0.toFixed(2)} edge=${teleEdge0.toFixed(2)} ph=${V.player.phase}`);
check("tip under telegraphFade leftover live tele arms", teleArmTel===true && V.player.phase==="startup" && V.player.telegraph===true && V.poseSheet(V.player).tipX===359, `tel=${teleArmTel} ph=${V.player.phase} sheetTip=${V.poseSheet(V.player).tipX} tip=${teleArmTip.toFixed(2)} tf=${teleArmTf.toFixed(3)}`);
const teleArmExp = teleEdge0 + (teleWu0 - teleEdge0) * teleArmTf;
check("tip under telegraphFade leftover live arm tip stays near idle", Math.abs(teleArmTip-teleArmExp)<8 && Math.abs(teleArmTip-teleWu0)>20, `tip0=${teleTip0.toFixed(2)} tipArm=${teleArmTip.toFixed(2)} exp=${teleArmExp.toFixed(2)} wu=${teleWu0.toFixed(2)} d=${(teleArmTip-teleTip0).toFixed(2)} tf=${teleArmTf.toFixed(3)}`);
let teleMid=null, teleEnd=null, teleTrackOk=true, prevTip=teleArmTip;
for (let i=0;i<12;i++){
  freezeRivalAI(); V.update(STEP);
  const tip=V.bladeTipX(V.player); const tf=V.telegraphFade(V.player);
  const r=V.destRect(V.player); const wu=r.dx+359*(r.dw/1186);
  if (teleMid==null && tf>0.3 && tf<0.8) teleMid={tip, tf, wu};
  if (V.telegraphing(V.player) && tf>=0.98) teleEnd={tip, tf, wu};
  if (Math.abs(tip-prevTip)>160) teleTrackOk=false;
  prevTip=tip;
  if (!V.telegraphing(V.player)) break;
}
check("tip under telegraphFade leftover live tip eases mid-tele", teleMid && Math.abs(teleMid.tip-teleTip0)>15 && Math.abs(teleMid.tip-teleMid.wu)>15, `mid=${!!teleMid} tip=${teleMid?teleMid.tip.toFixed(2):"na"} tip0=${teleTip0.toFixed(2)} wu=${teleMid?teleMid.wu.toFixed(2):teleWu0.toFixed(2)} tf=${teleMid?teleMid.tf.toFixed(3):"na"}`);
check("tip under telegraphFade leftover live tip tracks fade", teleEnd && Math.abs(teleEnd.tip-teleEnd.wu)<8 && Math.abs(teleEnd.tip-teleTip0)>150, `track=${teleTrackOk} end=${teleEnd?teleEnd.tip.toFixed(2):"na"} wu=${teleEnd?teleEnd.wu.toFixed(2):"na"} tip0=${teleTip0.toFixed(2)} d=${teleEnd?(teleEnd.tip-teleEnd.wu).toFixed(2):"na"}`);
check("tip under telegraphFade leftover live AABB planted", Math.abs(V.bodyAABB(V.player).y - 316.693)<2, `ay=${V.bodyAABB(V.player).y.toFixed(2)}`);

// live: tipX under plant fades still eases (regression) — connected slash-L
landCloseSlash();
const tipLinkWin2 = waitUntilWindow();
const tipSlashReg = V.bladeTipX(V.player);
V.golpeEdge = true;
holdRival();
V.update(STEP);
const tipLinkReg = V.bladeTipX(V.player);
const lfReg = V.linkPlantFade(V.player);
check("tip under telegraphFade leftover live link tip still eases", tipLinkWin2 && V.player.linkPlant===true && lfReg>0.8 && Math.abs(tipLinkReg-tipSlashReg)<80 && Math.abs(tipLinkReg-tipSlashReg)>0.5, `win=${tipLinkWin2} lp=${V.player.linkPlant} lf=${lfReg} tip=${tipLinkReg.toFixed(2)} slash=${tipSlashReg.toFixed(2)} d=${(tipLinkReg-tipSlashReg).toFixed(2)}`);

// live: rival tele tip eases
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=200; V.rival.x=700;
V.rival.phase="idle"; V.rival.phaseT=0; V.rival.gait=0; V.rival.walkFadeHold=0; V.rival.walkSettleT=0;
V.rival.sheatheT=0; V.rival.guarding=false; V.rival.stunT=0; V.rival.boltPhase=""; V.rival.telegraph=false;
V.rival.facing=-1; V.rival.closing=false; V.rival.linkPlant=false; V.rival.clashPlant=false;
V.player.phase="idle"; V.player.guarding=false; V.player.stunT=0; V.player.gait=0; V.openLeft=0;
freezeRivalAI(); V.update(STEP);
const rTip0 = V.bladeTipX(V.rival);
V.startAttack(V.rival, "slash");
const rTipArm = V.bladeTipX(V.rival);
const rTel = V.telegraphing(V.rival);
// Advance tele clock without rival AI eating the raise.
V.rival.phaseT = 40;
const rTip1 = V.bladeTipX(V.rival); const rTf1 = V.telegraphFade(V.rival);
check("tip under telegraphFade leftover rival tip eases not dumps", rTel && V.telegraphing(V.rival) && Math.abs(rTipArm-rTip0)<25 && Math.abs(rTip1-rTip0)>15 && Math.abs(rTip1-rTipArm)>10, `tip0=${rTip0.toFixed(2)} tipArm=${rTipArm.toFixed(2)} tip1=${rTip1.toFixed(2)} tf1=${rTf1.toFixed(3)} dArm=${(rTipArm-rTip0).toFixed(2)} d1=${(rTip1-rTip0).toFixed(2)}`);
check("tip under telegraphFade leftover rival bladeTip shared", /function bladeTipX/.test(srcTxt) && /telegraphing\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /telegraphFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")));
check("cache v=296 after tip under telegraphFade leftover live", /game.js\?v=350/.test(htmlTxt));

// --- tip under feintFade leftover (v289) ---
check("tip under feintFade leftover seats no tip dump mid-pull", /Tip under feintFade leftover/.test(srcTxt) && /windup→idle \(Space feint\) used to dump/.test(srcTxt) && /bladeTip onto idle-edge/.test(srcTxt) && /leftover windup still owned the sheet \(feintFade\)/.test(srcTxt) && /tip juice hopped ~304px while/.test(srcTxt));
check("tip under feintFade leftover is draw-only", /Draw-only \(bladeTipX \/ bladeTipY\)/.test(srcTxt) && /Ease windup→pose tip with feintFade/.test(srcTxt) && /feintFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /wx \+ \(tip - wx\) \* \(1 - fk\)/.test(sliceFn(codeOnly, "bladeTipX")) && /wy \+ \(tip - wy\) \* \(1 - fk\)/.test(sliceFn(codeOnly, "bladeTipY")) && !/feintFade/.test(sliceFn(codeOnly, "destRect")) && !/feintFade/.test(sliceFn(codeOnly, "bodyAABB")) && !/feintFade/.test(sliceFn(codeOnly, "poseBitmap")));
check("tip under feintFade leftover eases windup→pose tip", /const fk = feintFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /fk > 0\.02/.test(sliceFn(codeOnly, "bladeTipX")) && /poseFamily\(f\)\.windup/.test(sliceFn(codeOnly, "bladeTipX")) && /wx \+ \(tip - wx\) \* \(1 - fk\)/.test(sliceFn(codeOnly, "bladeTipX")) && /wy \+ \(tip - wy\) \* \(1 - fk\)/.test(sliceFn(codeOnly, "bladeTipY")));
check("tip under feintFade leftover tip under telegraphFade unchanged", /if \(telegraphing\(f\)\)/.test(sliceFn(codeOnly, "bladeTipX")) && /ex \+ \(tip - ex\) \* tf/.test(sliceFn(codeOnly, "bladeTipX")) && /ey \+ \(tip - ey\) \* tf/.test(sliceFn(codeOnly, "bladeTipY")));
check("tip under feintFade leftover tipX under plant fades unchanged", /Math\.max\(clashPlantFade\(f\), linkPlantFade\(f\)\)/.test(sliceFn(codeOnly, "bladeTipX")) && (/sx \+ \(tip - sx\) \* \(1 - k\)/.test(sliceFn(codeOnly, "bladeTipX")) || /from \+ \(tip - from\) \* \(1 - k\)/.test(sliceFn(codeOnly, "bladeTipX"))));
check("tip under feintFade leftover reversal tip unchanged", /Math\.max\(reversalPlantFade\(f\), throwGuardPlantFade\(f\), techGuardPlantFade\(f\)\)/.test(sliceFn(codeOnly, "bladeTipX")) && /bx \+ \(tip - bx\) \* \(1 - tipPlantK\)/.test(sliceFn(codeOnly, "bladeTipX")) && /by \+ \(tip - by\) \* \(1 - tipPlantK\)/.test(sliceFn(codeOnly, "bladeTipY")));
check("tip under feintFade leftover castPlantXY K path unchanged", /clashPlantFade\(f\)/.test(sliceFn(codeOnly, "castPlantXY")) && /linkPlantFade\(f\)/.test(sliceFn(codeOnly, "castPlantXY")) && !/feintFade/.test(sliceFn(codeOnly, "castPlantXY")) && !/telegraphFade/.test(sliceFn(codeOnly, "castPlantXY")));
check("tip under feintFade leftover tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("tip under feintFade leftover tipX locks hold", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt));
check("tip under feintFade leftover is not a new combat verb", /No new combat verb/.test(srcTxt) && (/Active hitbox still phase===active only/.test(srcTxt) || /active hitbox unchanged/.test(srcTxt)));
check("slash frames still 180\/140\/280 after tip under feintFade leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280);
check("golpe frames still 120\/80\/180 after tip under feintFade leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180);
check("K frames still 200\/280 after tip under feintFade leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280);
check("no 6th button after tip under feintFade leftover", !/KeyM/.test(codeOnly));
check("cache v=296 after tip under feintFade leftover", /game.js\?v=350/.test(htmlTxt));

// live: windup→idle feint tip eases not dumps
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkT=0; V.player.walkSettleT=0; V.player.walkFadeHold=0;
V.player.sheatheT=0; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.cutRecBreathT=0; V.player.telegraph=false; V.player.linkPlant=false; V.player.clashPlant=false;
V.player.reversal=false; V.player.facing=1; V.openLeft=0; V.hitstopLeft=0;
freezeRivalAI(); V.update(STEP);
V.startAttack(V.player, "slash");
// Seat tip on windup after telegraph raise, still slash startup for feint.
// telegraphing() stays true through startup — wait on telegraphFade, not !tele.
for (let i=0;i<20;i++){
  freezeRivalAI(); V.update(STEP);
  if (V.canFeint(V.player) && V.telegraphFade(V.player) >= 0.98) break;
}
const ffR0 = V.destRect(V.player);
const ffWu0 = ffR0.dx + 359 * (ffR0.dw/1186);
const ffEdge0 = V.player.facing>0 ? ffR0.dx+ffR0.dw-8 : ffR0.dx+8;
const ffTip0 = V.bladeTipX(V.player);
const ffCan = V.canFeint(V.player);
check("tip under feintFade leftover live windup tip ready", ffCan && V.player.phase==="startup" && V.player.cut==="slash" && Math.abs(ffTip0-ffWu0)<12 && Math.abs(ffTip0-ffEdge0)>150, `tip0=${ffTip0.toFixed(2)} wu=${ffWu0.toFixed(2)} edge=${ffEdge0.toFixed(2)} can=${ffCan} ph=${V.player.phase} tel=${V.telegraphing(V.player)} pt=${V.player.phaseT}`);
const ffDid = V.startFeint(V.player);
const ffArmTip = V.bladeTipX(V.player);
const ffArmFk = V.feintFade(V.player);
check("tip under feintFade leftover live feint arms", ffDid===true && V.player.feintT>0 && ffArmFk>0.9 && V.player.phase==="idle", `did=${ffDid} ft=${V.player.feintT} fk=${ffArmFk.toFixed(3)} tip=${ffArmTip.toFixed(2)}`);
const ffArmExp = ffWu0 + (ffEdge0 - ffWu0) * (1 - ffArmFk);
check("tip under feintFade leftover live arm tip stays near windup", Math.abs(ffArmTip-ffWu0)<12 && Math.abs(ffArmTip-ffEdge0)>150 && Math.abs(ffArmTip-ffArmExp)<8, `tip0=${ffTip0.toFixed(2)} tipArm=${ffArmTip.toFixed(2)} exp=${ffArmExp.toFixed(2)} wu=${ffWu0.toFixed(2)} edge=${ffEdge0.toFixed(2)} d=${(ffArmTip-ffTip0).toFixed(2)} fk=${ffArmFk.toFixed(3)}`);
let ffMid=null, ffEnd=null, ffTrackOk=true, ffPrev=ffArmTip;
for (let i=0;i<10;i++){
  freezeRivalAI(); V.update(STEP);
  const tip=V.bladeTipX(V.player); const fk=V.feintFade(V.player);
  const r=V.destRect(V.player); const wu=r.dx+359*(r.dw/1186); const edge=V.player.facing>0?r.dx+r.dw-8:r.dx+8;
  if (ffMid==null && fk>0.2 && fk<0.8) ffMid={tip, fk, wu, edge};
  if (V.player.feintT<=0 || fk<=0.02) ffEnd={tip, fk, wu, edge};
  if (Math.abs(tip-ffPrev)>160) ffTrackOk=false;
  ffPrev=tip;
  if (V.player.feintT<=0) break;
}
check("tip under feintFade leftover live tip eases mid-pull", ffMid && Math.abs(ffMid.tip-ffWu0)>15 && Math.abs(ffMid.tip-ffEdge0)>15, `mid=${!!ffMid} tip=${ffMid?ffMid.tip.toFixed(2):"na"} wu=${ffWu0.toFixed(2)} edge=${ffEdge0.toFixed(2)} fk=${ffMid?ffMid.fk.toFixed(3):"na"}`);
check("tip under feintFade leftover live tip tracks fade", ffEnd && Math.abs(ffEnd.tip-ffEdge0)<8 && Math.abs(ffEnd.tip-ffWu0)>150 && ffTrackOk, `track=${ffTrackOk} end=${ffEnd?ffEnd.tip.toFixed(2):"na"} edge=${ffEnd?ffEnd.edge.toFixed(2):"na"} wu=${ffWu0.toFixed(2)} d=${ffEnd?(ffEnd.tip-ffEdge0).toFixed(2):"na"}`);
check("tip under feintFade leftover live AABB planted", Math.abs(V.bodyAABB(V.player).y - 316.693)<2, `ay=${V.bodyAABB(V.player).y.toFixed(2)}`);

// live: tele tip still eases (regression)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
V.player.sheatheT=0; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.telegraph=false; V.player.facing=1; V.openLeft=0; V.player.linkPlant=false; V.player.clashPlant=false;
freezeRivalAI(); V.update(STEP);
const ffTele0 = V.bladeTipX(V.player);
const ffTeleR = V.destRect(V.player);
const ffTeleEdge = V.player.facing>0 ? ffTeleR.dx+ffTeleR.dw-8 : ffTeleR.dx+8;
const ffTeleWu = ffTeleR.dx + 359 * (ffTeleR.dw/1186);
V.startAttack(V.player, "slash");
holdRival(); V.update(STEP);
const ffTeleArm = V.bladeTipX(V.player);
const ffTeleTf = V.telegraphFade(V.player);
check("tip under feintFade leftover live tele tip still eases", V.telegraphing(V.player) && V.player.phase==="startup" && Math.abs(ffTeleArm-ffTele0)<40 && Math.abs(ffTeleArm-ffTeleWu)>20 && ffTeleTf>0 && ffTeleTf<0.3, `tip0=${ffTele0.toFixed(2)} tipArm=${ffTeleArm.toFixed(2)} edge=${ffTeleEdge.toFixed(2)} wu=${ffTeleWu.toFixed(2)} tf=${ffTeleTf.toFixed(3)} tel=${V.telegraphing(V.player)}`);

// live: rival feint tip eases
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=200; V.rival.x=700;
V.rival.phase="idle"; V.rival.phaseT=0; V.rival.gait=0; V.rival.walkFadeHold=0; V.rival.walkSettleT=0;
V.rival.sheatheT=0; V.rival.guarding=false; V.rival.stunT=0; V.rival.boltPhase=""; V.rival.telegraph=false;
V.rival.facing=-1; V.rival.closing=false; V.rival.linkPlant=false; V.rival.clashPlant=false; V.rival.feintT=0;
V.player.phase="idle"; V.player.guarding=false; V.player.stunT=0; V.player.gait=0; V.openLeft=0;
freezeRivalAI(); V.update(STEP);
V.startAttack(V.rival, "slash");
// Advance tele clock without freezeRivalAI (it resets phase to idle).
for (let i=0;i<20;i++){
  holdRivalStill();
  V.rival.phaseT = Math.min((V.rival.phaseT|0) + 20, 160);
  if (V.canFeint(V.rival) && V.telegraphFade(V.rival) >= 0.98) break;
}
const rFfTip0 = V.bladeTipX(V.rival);
const rFfCan = V.canFeint(V.rival);
const rFfDid = rFfCan ? V.startFeint(V.rival) : false;
const rFfArm = V.bladeTipX(V.rival);
const rFfFk = V.feintFade(V.rival);
// Force mid-pull fk without AI: drop feintT into mid range.
if (V.rival.feintT>0) V.rival.feintT = Math.max(1, Math.floor(V.FEINT_RECOVERY*0.5));
const rFf1 = V.bladeTipX(V.rival); const rFfFk1 = V.feintFade(V.rival);
check("tip under feintFade leftover rival tip eases not dumps", rFfDid && rFfFk>0.9 && Math.abs(rFfArm-rFfTip0)<40 && Math.abs(rFf1-rFfArm)>10 && rFfFk1<rFfFk, `tip0=${rFfTip0.toFixed(2)} tipArm=${rFfArm.toFixed(2)} tip1=${rFf1.toFixed(2)} fk=${rFfFk.toFixed(3)} fk1=${rFfFk1.toFixed(3)} dArm=${(rFfArm-rFfTip0).toFixed(2)} d1=${(rFf1-rFfArm).toFixed(2)} can=${rFfCan} ph=${V.rival.phase}`);
check("cache v=296 after tip under feintFade leftover live", /game.js\?v=350/.test(htmlTxt));



// --- tip under throw-guard short raise leftover (v291) ---
check("tip under throw-guard short raise leftover seats no tip dump", /Tip under throw-guard short raise leftover/.test(srcTxt) && /Space\+S from short guarda used to/.test(srcTxt) && /dump bladeTip onto windup/.test(srcTxt) && /leftover gpk \/ tgf died/.test(srcTxt) && (/~304px/.test(srcTxt) || /tip dump while raise short/.test(srcTxt)));
check("tip under throw-guard short raise leftover is draw-only", /Draw-only \(bladeTipX \/ bladeTipY\)/.test(srcTxt) && /Ease block→pose tip with max\(throwGuardPlantFade, 1−raiseT\)/.test(srcTxt) && /throwGuardPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /tipPlantK/.test(sliceFn(codeOnly, "bladeTipX")) && /tipPlantK/.test(sliceFn(codeOnly, "bladeTipY")) && !/tipPlantK/.test(sliceFn(codeOnly, "destRect")) && !/tipPlantK/.test(sliceFn(codeOnly, "bodyAABB")) && !/tipPlantK/.test(sliceFn(codeOnly, "poseBitmap")) && !/tipPlantK/.test(sliceFn(codeOnly, "throwGuardPlantFade")) && !/tipPlantK/.test(sliceFn(codeOnly, "tickGuardPose")));
check("tip under throw-guard short raise leftover eases over GUARD_RAISE_MS", /\(throwGuardPlanting\(f\) \|\| reversalPlanting\(f\) \|\| techGuardPlanting\(f\) \|\| f\.techGuardTip \|\| \(\(slashLeftoverPlanting\(f\) \|\| golpeLeftoverPlanting\(f\) \|\| boltLeftoverPlanting\(f\)\) && f\.leftoverPlantTip && !telegraphing\(f\)\)\) && GUARD_RAISE_MS > 0/.test(sliceFn(codeOnly, "bladeTipX")) && /phaseT/.test(sliceFn(codeOnly, "bladeTipX")) && /Math\.max\(pk, 1 - raiseT\)/.test(sliceFn(codeOnly, "bladeTipX")) && /Math\.max\(pk, 1 - raiseT\)/.test(sliceFn(codeOnly, "bladeTipY")) && /bx \+ \(tip - bx\) \* \(1 - tipPlantK\)/.test(sliceFn(codeOnly, "bladeTipX")) && /by \+ \(tip - by\) \* \(1 - tipPlantK\)/.test(sliceFn(codeOnly, "bladeTipY")));
check("tip under throw-guard short raise leftover tip under feintFade unchanged", /const fk = feintFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /wx \+ \(tip - wx\) \* \(1 - fk\)/.test(sliceFn(codeOnly, "bladeTipX")) && /wy \+ \(tip - wy\) \* \(1 - fk\)/.test(sliceFn(codeOnly, "bladeTipY")));
check("tip under throw-guard short raise leftover tip under telegraphFade unchanged", /if \(telegraphing\(f\)\)/.test(sliceFn(codeOnly, "bladeTipX")) && /ex \+ \(tip - ex\) \* tf/.test(sliceFn(codeOnly, "bladeTipX")) && /ey \+ \(tip - ey\) \* tf/.test(sliceFn(codeOnly, "bladeTipY")));
check("tip under throw-guard short raise leftover tipX under plant fades unchanged", /Math\.max\(clashPlantFade\(f\), linkPlantFade\(f\)\)/.test(sliceFn(codeOnly, "bladeTipX")) && (/sx \+ \(tip - sx\) \* \(1 - k\)/.test(sliceFn(codeOnly, "bladeTipX")) || /from \+ \(tip - from\) \* \(1 - k\)/.test(sliceFn(codeOnly, "bladeTipX"))));
check("tip under throw-guard short raise leftover reverse short-raise tip mirrors", /Reverse short-raise tip mirrors/.test(srcTxt) && /\(throwGuardPlanting\(f\) \|\| reversalPlanting\(f\) \|\| techGuardPlanting\(f\) \|\| f\.techGuardTip \|\| \(\(slashLeftoverPlanting\(f\) \|\| golpeLeftoverPlanting\(f\) \|\| boltLeftoverPlanting\(f\)\) && f\.leftoverPlantTip && !telegraphing\(f\)\)\) && GUARD_RAISE_MS > 0/.test(sliceFn(codeOnly, "bladeTipX")) && /Math\.max\(pk, 1 - raiseT\)/.test(sliceFn(codeOnly, "bladeTipX")) && /let tipPlantK = pk/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under throw-guard short raise leftover sheet fade drain unchanged", /return guardRaiseK\(f\)/.test(sliceFn(codeOnly, "throwGuardPlantFade")) && /throwGuardPlanting\(f\)/.test(sliceFn(codeOnly, "tickGuardPose")) && /f\.guardPoseK <= step/.test(sliceFn(codeOnly, "tickGuardPose")));
check("tip under throw-guard short raise leftover castPlantXY K path unchanged", /clashPlantFade\(f\)/.test(sliceFn(codeOnly, "castPlantXY")) && /linkPlantFade\(f\)/.test(sliceFn(codeOnly, "castPlantXY")) && !/tipPlantK/.test(sliceFn(codeOnly, "castPlantXY")) && !/telegraphFade/.test(sliceFn(codeOnly, "castPlantXY")) && !/feintFade/.test(sliceFn(codeOnly, "castPlantXY")));
check("tip under throw-guard short raise leftover tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("tip under throw-guard short raise leftover tipX locks hold", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt));
check("tip under throw-guard short raise leftover is not a new combat verb", /No new combat verb/.test(srcTxt) && (/Active hitbox still phase===active only/.test(srcTxt) || /active hitbox unchanged/.test(srcTxt)));
check("slash frames still 180\/140\/280 after tip under throw-guard short raise leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280);
check("golpe frames still 120\/80\/180 after tip under throw-guard short raise leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180);
check("K frames still 200\/280 after tip under throw-guard short raise leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280);
check("no 6th button after tip under throw-guard short raise leftover", !/KeyM/.test(codeOnly));
check("cache v=296 after tip under throw-guard short raise leftover", /game.js\?v=350/.test(htmlTxt));

// live: short raise Space+S tip eases over GUARD_RAISE_MS, no ~304px dump
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkT=0; V.player.walkSettleT=0; V.player.walkFadeHold=0;
V.player.sheatheT=0; V.player.guarding=true; V.player.guardPoseK=1; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.cutRecBreathT=0; V.player.telegraph=false; V.player.linkPlant=false; V.player.clashPlant=false;
V.player.reversal=false; V.player.facing=1; V.openLeft=0; V.hitstopLeft=0;
V.keys.add("KeyS");
freezeRivalAI(); V.update(STEP);
V.player.guardPoseK = 0.2;
const tgTip0 = V.bladeTipX(V.player);
const tgR0 = V.destRect(V.player);
const tgEdge0 = V.player.facing>0 ? tgR0.dx+tgR0.dw-8 : tgR0.dx+8;
const tgWu0 = tgR0.dx + 359 * (tgR0.dw/1186);
const tgDid = V.startThrow(V.player);
const tgArmTip = V.bladeTipX(V.player);
const tgArmTgf = V.throwGuardPlantFade(V.player);
const tgArmTgp = V.throwGuardPlanting(V.player);
const tgArmPt = V.player.phaseT|0;
check("tip under throw-guard short raise leftover live arms", tgDid===true && tgArmTgp===true && V.player.cut==="throw" && V.player.phase==="startup", `did=${tgDid} tgp=${tgArmTgp} cut=${V.player.cut} ph=${V.player.phase} tgf=${tgArmTgf.toFixed(3)} tip=${tgArmTip.toFixed(2)}`);
check("tip under throw-guard short raise leftover live arm tip stays near block", Math.abs(tgArmTip-tgTip0)<12 && Math.abs(tgArmTip-tgWu0)>150 && Math.abs(tgArmTip-tgEdge0)<12, `tip0=${tgTip0.toFixed(2)} tipArm=${tgArmTip.toFixed(2)} edge=${tgEdge0.toFixed(2)} wu=${tgWu0.toFixed(2)} tgf=${tgArmTgf.toFixed(3)} pt=${tgArmPt}`);
let tgMid=null, tgEnd=null, tgTrackOk=true, tgPrev=tgArmTip, tgSawTgf0=false;
for (let i=0;i<10;i++){
  freezeRivalAI(); V.update(STEP);
  const tip=V.bladeTipX(V.player);
  const tgf=V.throwGuardPlantFade(V.player);
  const r=V.destRect(V.player);
  const wu=r.dx+359*(r.dw/1186);
  const edge=V.player.facing>0?r.dx+r.dw-8:r.dx+8;
  const pt=V.player.phaseT|0;
  const raiseMs=V.GUARD_RAISE_MS||80;
  const u=Math.max(0,Math.min(1,pt/raiseMs));
  const raiseT=u*u*(3-2*u);
  const tipPlantK=V.throwGuardPlanting(V.player) ? Math.max(tgf, 1-raiseT) : 0;
  if (tgf<=0.02) tgSawTgf0=true;
  if (tgMid==null && tipPlantK>0.2 && tipPlantK<0.8) tgMid={tip, tgf, tipPlantK, wu, edge, pt};
  // THROW_STARTUP === GUARD_RAISE_MS: startup flips active as raise ends; tip seats on windup.
  if (tgEnd==null && (tipPlantK<=0.02 || Math.abs(tip-wu)<12 || V.player.phase!=="startup")) tgEnd={tip, tgf, tipPlantK, wu, edge, pt, ph:V.player.phase};
  if (Math.abs(tip-tgPrev)>160) tgTrackOk=false;
  tgPrev=tip;
  if (tgEnd) break;
}
check("tip under throw-guard short raise leftover live tip eases mid-plant", tgMid && Math.abs(tgMid.tip-tgTip0)>15 && Math.abs(tgMid.tip-tgWu0)>15, `mid=${!!tgMid} tip=${tgMid?tgMid.tip.toFixed(2):"na"} tip0=${tgTip0.toFixed(2)} wu=${tgWu0.toFixed(2)} k=${tgMid?tgMid.tipPlantK.toFixed(3):"na"} tgf0=${tgSawTgf0}`);
check("tip under throw-guard short raise leftover live tip tracks raise not dump", tgEnd && Math.abs(tgEnd.tip-tgWu0)<12 && Math.abs(tgEnd.tip-tgTip0)>150 && tgTrackOk, `track=${tgTrackOk} end=${tgEnd?tgEnd.tip.toFixed(2):"na"} wu=${tgEnd?tgEnd.wu.toFixed(2):"na"} tip0=${tgTip0.toFixed(2)} d=${tgEnd?(tgEnd.tip-tgTip0).toFixed(2):"na"} tgf0=${tgSawTgf0}`);
check("tip under throw-guard short raise leftover live AABB planted", Math.abs(V.bodyAABB(V.player).y - 316.693)<2, `ay=${V.bodyAABB(V.player).y.toFixed(2)}`);

// live: full raise throw-guard tip still eases (regression)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
V.player.sheatheT=0; V.player.guarding=true; V.player.guardPoseK=1; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.telegraph=false; V.player.facing=1; V.openLeft=0; V.player.linkPlant=false; V.player.clashPlant=false;
V.keys.add("KeyS");
for (let i=0;i<10;i++){ freezeRivalAI(); V.update(STEP); }
const tgFull0 = V.bladeTipX(V.player);
const tgFullDid = V.startThrow(V.player);
const tgFullArm = V.bladeTipX(V.player);
const tgFullTgf = V.throwGuardPlantFade(V.player);
freezeRivalAI(); V.update(STEP);
const tgFull1 = V.bladeTipX(V.player);
const tgFullTgf1 = V.throwGuardPlantFade(V.player);
check("tip under throw-guard short raise leftover live full raise still eases", tgFullDid && tgFullTgf>0.8 && Math.abs(tgFullArm-tgFull0)<12 && Math.abs(tgFull1-tgFullArm)>10 && tgFullTgf1<tgFullTgf, `tip0=${tgFull0.toFixed(2)} tipArm=${tgFullArm.toFixed(2)} tip1=${tgFull1.toFixed(2)} tgf=${tgFullTgf.toFixed(3)} tgf1=${tgFullTgf1.toFixed(3)} d1=${(tgFull1-tgFullArm).toFixed(2)}`);

// live: tele tip still eases (regression)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
V.player.sheatheT=0; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.telegraph=false; V.player.facing=1; V.openLeft=0; V.player.linkPlant=false; V.player.clashPlant=false;
freezeRivalAI(); V.update(STEP);
const tgTele0 = V.bladeTipX(V.player);
const tgTeleR = V.destRect(V.player);
const tgTeleWu = tgTeleR.dx + 359 * (tgTeleR.dw/1186);
V.startAttack(V.player, "slash");
holdRival(); V.update(STEP);
const tgTeleArm = V.bladeTipX(V.player);
const tgTeleTf = V.telegraphFade(V.player);
check("tip under throw-guard short raise leftover live tele tip still eases", V.telegraphing(V.player) && V.player.phase==="startup" && Math.abs(tgTeleArm-tgTele0)<40 && Math.abs(tgTeleArm-tgTeleWu)>20 && tgTeleTf>0 && tgTeleTf<0.3, `tip0=${tgTele0.toFixed(2)} tipArm=${tgTeleArm.toFixed(2)} wu=${tgTeleWu.toFixed(2)} tf=${tgTeleTf.toFixed(3)}`);

// live: castPlantXY K path still snaps intentional (unchanged)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkFadeHold=0;
V.player.sheatheT=0; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.telegraph=false; V.player.facing=1; V.openLeft=0;
freezeRivalAI(); V.update(STEP);
const tgK0 = V.castPlantXY(V.player).x;
V.startBolt(V.player);
const tgKArm = V.castPlantXY(V.player).x;
check("tip under throw-guard short raise leftover live castPlantXY K snap intentional", V.player.boltPhase==="startup" && Math.abs(tgKArm-tgK0)>1, `plant0=${tgK0.toFixed(2)} plantArm=${tgKArm.toFixed(2)} bph=${V.player.boltPhase}`);

check("cache v=296 after tip under throw-guard short raise leftover live", /game.js\?v=350/.test(htmlTxt));


// --- tip under reverse short raise leftover (v291) ---
check("tip under reverse short raise leftover seats no tip dump", /Tip under reverse short raise leftover/.test(srcTxt) && /L\+S from short guarda used to/.test(srcTxt) && /dump bladeTip onto windup/.test(srcTxt) && /leftover gpk \/ rpf died/.test(srcTxt) && (/~304px/.test(srcTxt) || /gpk one-tick hole/.test(srcTxt)));
check("tip under reverse short raise leftover is draw-only", /Draw-only[\s\S]{0,40}bladeTipX \/ bladeTipY/.test(srcTxt) && /Ease block→pose tip with max\(reversalPlantFade,[\s\S]{0,20}1−raiseT\)/.test(srcTxt) && /reversalPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /tipPlantK/.test(sliceFn(codeOnly, "bladeTipX")) && /tipPlantK/.test(sliceFn(codeOnly, "bladeTipY")) && !/tipPlantK/.test(sliceFn(codeOnly, "destRect")) && !/tipPlantK/.test(sliceFn(codeOnly, "bodyAABB")) && !/tipPlantK/.test(sliceFn(codeOnly, "poseBitmap")) && !/tipPlantK/.test(sliceFn(codeOnly, "reversalPlantFade")) && !/tipPlantK/.test(sliceFn(codeOnly, "tickGuardPose")));
check("tip under reverse short raise leftover mirrors throw-guard tipPlantK", /\(throwGuardPlanting\(f\) \|\| reversalPlanting\(f\) \|\| techGuardPlanting\(f\) \|\| f\.techGuardTip \|\| \(\(slashLeftoverPlanting\(f\) \|\| golpeLeftoverPlanting\(f\) \|\| boltLeftoverPlanting\(f\)\) && f\.leftoverPlantTip && !telegraphing\(f\)\)\) && GUARD_RAISE_MS > 0/.test(sliceFn(codeOnly, "bladeTipX")) && /\(throwGuardPlanting\(f\) \|\| reversalPlanting\(f\) \|\| techGuardPlanting\(f\) \|\| f\.techGuardTip \|\| \(\(slashLeftoverPlanting\(f\) \|\| golpeLeftoverPlanting\(f\) \|\| boltLeftoverPlanting\(f\)\) && f\.leftoverPlantTip && !telegraphing\(f\)\)\) && GUARD_RAISE_MS > 0/.test(sliceFn(codeOnly, "bladeTipY")) && /Math\.max\(pk, 1 - raiseT\)/.test(sliceFn(codeOnly, "bladeTipX")) && /Math\.max\(pk, 1 - raiseT\)/.test(sliceFn(codeOnly, "bladeTipY")) && /bx \+ \(tip - bx\) \* \(1 - tipPlantK\)/.test(sliceFn(codeOnly, "bladeTipX")) && /by \+ \(tip - by\) \* \(1 - tipPlantK\)/.test(sliceFn(codeOnly, "bladeTipY")));
check("tip under reverse short raise leftover tip under throw-guard short raise unchanged", /Tip under throw-guard short raise leftover/.test(srcTxt) && /throwGuardPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /Math\.max\(pk, 1 - raiseT\)/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under reverse short raise leftover tip under feintFade unchanged", /const fk = feintFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /wx \+ \(tip - wx\) \* \(1 - fk\)/.test(sliceFn(codeOnly, "bladeTipX")) && /wy \+ \(tip - wy\) \* \(1 - fk\)/.test(sliceFn(codeOnly, "bladeTipY")));
check("tip under reverse short raise leftover tip under telegraphFade unchanged", /telegraphing\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /telegraphFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /ex \+ \(tip - ex\) \* tf/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under reverse short raise leftover tipX under plant fades unchanged", /Math\.max\(clashPlantFade\(f\), linkPlantFade\(f\)\)/.test(sliceFn(codeOnly, "bladeTipX")) && (/sx \+ \(tip - sx\) \* \(1 - k\)/.test(sliceFn(codeOnly, "bladeTipX")) || /from \+ \(tip - from\) \* \(1 - k\)/.test(sliceFn(codeOnly, "bladeTipX"))));
check("tip under reverse short raise leftover sheet fade drain unchanged", /return guardRaiseK\(f\)/.test(sliceFn(codeOnly, "reversalPlantFade")) && /reversalPlanting\(f\)/.test(sliceFn(codeOnly, "tickGuardPose")) && /f\.guardPoseK <= step/.test(sliceFn(codeOnly, "tickGuardPose")));
check("tip under reverse short raise leftover castPlantXY K path unchanged", /clashPlantFade\(f\)/.test(sliceFn(codeOnly, "castPlantXY")) && /linkPlantFade\(f\)/.test(sliceFn(codeOnly, "castPlantXY")) && !/tipPlantK/.test(sliceFn(codeOnly, "castPlantXY")) && !/telegraphFade/.test(sliceFn(codeOnly, "castPlantXY")) && !/feintFade/.test(sliceFn(codeOnly, "castPlantXY")));
check("tip under reverse short raise leftover tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("tip under reverse short raise leftover tipX locks hold", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt));
check("tip under reverse short raise leftover is not a new combat verb", /No new combat verb/.test(srcTxt) && (/Active hitbox still phase===active only/.test(srcTxt) || /active hitbox unchanged/.test(srcTxt)));
check("slash frames still 180\/140\/280 after tip under reverse short raise leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280);
check("golpe frames still 120\/80\/180 after tip under reverse short raise leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180);
check("K frames still 200\/280 after tip under reverse short raise leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280);
check("no 6th button after tip under reverse short raise leftover", !/KeyM/.test(codeOnly));
check("cache v=296 after tip under reverse short raise leftover", /game.js\?v=350/.test(htmlTxt));

// live: short raise L+S tip eases over GUARD_RAISE_MS, no ~304px dump
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkT=0; V.player.walkSettleT=0; V.player.walkFadeHold=0;
V.player.sheatheT=0; V.player.guarding=true; V.player.guardPoseK=1; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.cutRecBreathT=0; V.player.telegraph=false; V.player.linkPlant=false; V.player.clashPlant=false;
V.player.reversal=false; V.player.facing=1; V.openLeft=0; V.hitstopLeft=0; V.player.stamina=100;
V.keys.add("KeyS");
freezeRivalAI(); V.update(STEP);
V.player.guardPoseK = 0.2;
const rvTip0 = V.bladeTipX(V.player);
const rvR0 = V.destRect(V.player);
const rvEdge0 = V.player.facing>0 ? rvR0.dx+rvR0.dw-8 : rvR0.dx+8;
const rvWu0 = rvR0.dx + 359 * (rvR0.dw/1186);
const rvDid = V.startReversal(V.player);
const rvArmTip = V.bladeTipX(V.player);
const rvArmRpf = V.reversalPlantFade(V.player);
const rvArmRp = V.reversalPlanting(V.player);
const rvArmPt = V.player.phaseT|0;
check("tip under reverse short raise leftover live arms", rvDid===true && rvArmRp===true && V.player.cut==="golpe" && V.player.phase==="startup" && V.player.reversal===true, `did=${rvDid} rp=${rvArmRp} cut=${V.player.cut} ph=${V.player.phase} rev=${V.player.reversal} rpf=${rvArmRpf.toFixed(3)} tip=${rvArmTip.toFixed(2)}`);
check("tip under reverse short raise leftover live arm tip stays near block", Math.abs(rvArmTip-rvTip0)<12 && Math.abs(rvArmTip-rvWu0)>150 && Math.abs(rvArmTip-rvEdge0)<12, `tip0=${rvTip0.toFixed(2)} tipArm=${rvArmTip.toFixed(2)} edge=${rvEdge0.toFixed(2)} wu=${rvWu0.toFixed(2)} rpf=${rvArmRpf.toFixed(3)} pt=${rvArmPt}`);
let rvMid=null, rvEnd=null, rvTrackOk=true, rvPrev=rvArmTip, rvSawRpf0=false;
for (let i=0;i<10;i++){
  freezeRivalAI(); V.update(STEP);
  const tip=V.bladeTipX(V.player);
  const rpf=V.reversalPlantFade(V.player);
  const r=V.destRect(V.player);
  const wu=r.dx+359*(r.dw/1186);
  const edge=V.player.facing>0?r.dx+r.dw-8:r.dx+8;
  const pt=V.player.phaseT|0;
  const raiseMs=V.GUARD_RAISE_MS||80;
  const u=Math.max(0,Math.min(1,pt/raiseMs));
  const raiseT=u*u*(3-2*u);
  const tipPlantK=V.reversalPlanting(V.player) ? Math.max(rpf, 1-raiseT) : 0;
  if (rpf<=0.02) rvSawRpf0=true;
  if (rvMid==null && tipPlantK>0.2 && tipPlantK<0.8) rvMid={tip, rpf, tipPlantK, wu, edge, pt};
  if (rvEnd==null && (tipPlantK<=0.02 || Math.abs(tip-wu)<12 || V.player.phase!=="startup")) rvEnd={tip, rpf, tipPlantK, wu, edge, pt, ph:V.player.phase};
  if (Math.abs(tip-rvPrev)>160) rvTrackOk=false;
  rvPrev=tip;
  if (rvEnd) break;
}
check("tip under reverse short raise leftover live tip eases mid-plant", rvMid && Math.abs(rvMid.tip-rvTip0)>15 && Math.abs(rvMid.tip-rvWu0)>15, `mid=${!!rvMid} tip=${rvMid?rvMid.tip.toFixed(2):"na"} tip0=${rvTip0.toFixed(2)} wu=${rvWu0.toFixed(2)} k=${rvMid?rvMid.tipPlantK.toFixed(3):"na"} rpf0=${rvSawRpf0}`);
check("tip under reverse short raise leftover live tip tracks raise not dump", rvEnd && Math.abs(rvEnd.tip-rvWu0)<12 && Math.abs(rvEnd.tip-rvTip0)>150 && rvTrackOk, `track=${rvTrackOk} end=${rvEnd?rvEnd.tip.toFixed(2):"na"} wu=${rvEnd?rvEnd.wu.toFixed(2):"na"} tip0=${rvTip0.toFixed(2)} d=${rvEnd?(rvEnd.tip-rvTip0).toFixed(2):"na"} rpf0=${rvSawRpf0}`);
check("tip under reverse short raise leftover live AABB planted", Math.abs(V.bodyAABB(V.player).y - 316.693)<2, `ay=${V.bodyAABB(V.player).y.toFixed(2)}`);

// live: full raise reverse tip still eases (regression)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
V.player.sheatheT=0; V.player.guarding=true; V.player.guardPoseK=1; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.telegraph=false; V.player.facing=1; V.openLeft=0; V.player.linkPlant=false; V.player.clashPlant=false;
V.player.reversal=false; V.player.stamina=100;
V.keys.add("KeyS");
for (let i=0;i<10;i++){ freezeRivalAI(); V.update(STEP); }
const rvFull0 = V.bladeTipX(V.player);
const rvFullDid = V.startReversal(V.player);
const rvFullArm = V.bladeTipX(V.player);
const rvFullRpf = V.reversalPlantFade(V.player);
freezeRivalAI(); V.update(STEP);
const rvFull1 = V.bladeTipX(V.player);
const rvFullRpf1 = V.reversalPlantFade(V.player);
check("tip under reverse short raise leftover live full raise still eases", rvFullDid && rvFullRpf>0.8 && Math.abs(rvFullArm-rvFull0)<12 && Math.abs(rvFull1-rvFullArm)>10 && rvFullRpf1<rvFullRpf, `tip0=${rvFull0.toFixed(2)} tipArm=${rvFullArm.toFixed(2)} tip1=${rvFull1.toFixed(2)} rpf=${rvFullRpf.toFixed(3)} rpf1=${rvFullRpf1.toFixed(3)} d1=${(rvFull1-rvFullArm).toFixed(2)}`);

// live: throw-guard short raise tip still eases (regression)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
V.player.sheatheT=0; V.player.guarding=true; V.player.guardPoseK=1; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.telegraph=false; V.player.facing=1; V.openLeft=0; V.player.linkPlant=false; V.player.clashPlant=false; V.player.reversal=false;
V.keys.add("KeyS");
freezeRivalAI(); V.update(STEP);
V.player.guardPoseK = 0.2;
const rvTg0 = V.bladeTipX(V.player);
const rvTgDid = V.startThrow(V.player);
const rvTgArm = V.bladeTipX(V.player);
const rvTgTgp = V.throwGuardPlanting(V.player);
check("tip under reverse short raise leftover live throw-guard short still holds tip", rvTgDid && rvTgTgp && Math.abs(rvTgArm-rvTg0)<12, `did=${rvTgDid} tgp=${rvTgTgp} tip0=${rvTg0.toFixed(2)} tipArm=${rvTgArm.toFixed(2)}`);

// live: tele tip still eases (regression)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
V.player.sheatheT=0; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.telegraph=false; V.player.facing=1; V.openLeft=0; V.player.linkPlant=false; V.player.clashPlant=false;
freezeRivalAI(); V.update(STEP);
const rvTele0 = V.bladeTipX(V.player);
const rvTeleR = V.destRect(V.player);
const rvTeleWu = rvTeleR.dx + 359 * (rvTeleR.dw/1186);
V.startAttack(V.player, "slash");
holdRival(); V.update(STEP);
const rvTeleArm = V.bladeTipX(V.player);
const rvTeleTf = V.telegraphFade(V.player);
check("tip under reverse short raise leftover live tele tip still eases", V.telegraphing(V.player) && V.player.phase==="startup" && Math.abs(rvTeleArm-rvTele0)<40 && Math.abs(rvTeleArm-rvTeleWu)>20 && rvTeleTf>0 && rvTeleTf<0.3, `tip0=${rvTele0.toFixed(2)} tipArm=${rvTeleArm.toFixed(2)} wu=${rvTeleWu.toFixed(2)} tf=${rvTeleTf.toFixed(3)}`);

check("cache v=296 after tip under reverse short raise leftover live", /game.js\?v=350/.test(htmlTxt));




// --- tip under tech-guard short raise leftover (v292) ---
check("tip under tech-guard short raise leftover seats no tip dump", /Tip under tech-guard short raise leftover/.test(srcTxt) && /Space\+S tech from short guarda used to/.test(srcTxt) && /dump bladeTip onto windup/.test(srcTxt) && /leftover gpk \/ tecf died/.test(srcTxt) && (/~304px/.test(srcTxt) || /gpk one-tick hole/.test(srcTxt)));
check("tip under tech-guard short raise leftover is draw-only", /Draw-only \(bladeTipX \/ bladeTipY\)/.test(srcTxt) && /Ease block→pose tip with[\s\S]{0,40}max\(techGuardPlantFade,[\s\S]{0,20}1−raiseT\)/.test(srcTxt) && /techGuardPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /techGuardTip/.test(sliceFn(codeOnly, "bladeTipX")) && /tipPlantK/.test(sliceFn(codeOnly, "bladeTipX")) && /tipPlantK/.test(sliceFn(codeOnly, "bladeTipY")) && !/tipPlantK/.test(sliceFn(codeOnly, "destRect")) && !/tipPlantK/.test(sliceFn(codeOnly, "bodyAABB")) && !/tipPlantK/.test(sliceFn(codeOnly, "poseBitmap")) && !/tipPlantK/.test(sliceFn(codeOnly, "techGuardPlantFade")) && !/tipPlantK/.test(sliceFn(codeOnly, "tickGuardPose")));
check("tip under tech-guard short raise leftover mirrors throw-guard tipPlantK", /\(throwGuardPlanting\(f\) \|\| reversalPlanting\(f\) \|\| techGuardPlanting\(f\) \|\| f\.techGuardTip \|\| \(\(slashLeftoverPlanting\(f\) \|\| golpeLeftoverPlanting\(f\) \|\| boltLeftoverPlanting\(f\)\) && f\.leftoverPlantTip && !telegraphing\(f\)\)\) && GUARD_RAISE_MS > 0/.test(sliceFn(codeOnly, "bladeTipX")) && /\(throwGuardPlanting\(f\) \|\| reversalPlanting\(f\) \|\| techGuardPlanting\(f\) \|\| f\.techGuardTip \|\| \(\(slashLeftoverPlanting\(f\) \|\| golpeLeftoverPlanting\(f\) \|\| boltLeftoverPlanting\(f\)\) && f\.leftoverPlantTip && !telegraphing\(f\)\)\) && GUARD_RAISE_MS > 0/.test(sliceFn(codeOnly, "bladeTipY")) && /Math\.max\(pk, 1 - raiseT\)/.test(sliceFn(codeOnly, "bladeTipX")) && /Math\.max\(pk, 1 - raiseT\)/.test(sliceFn(codeOnly, "bladeTipY")) && /bx \+ \(tip - bx\) \* \(1 - tipPlantK\)/.test(sliceFn(codeOnly, "bladeTipX")) && /by \+ \(tip - by\) \* \(1 - tipPlantK\)/.test(sliceFn(codeOnly, "bladeTipY")));
check("tip under tech-guard short raise leftover pk includes techGuardPlantFade", /Math\.max\(reversalPlantFade\(f\), throwGuardPlantFade\(f\), techGuardPlantFade\(f\)\)/.test(sliceFn(codeOnly, "bladeTipX")) && /Math\.max\(reversalPlantFade\(f\), throwGuardPlantFade\(f\), techGuardPlantFade\(f\)\)/.test(sliceFn(codeOnly, "bladeTipY")));
check("tip under tech-guard short raise leftover latches techGuardTip", /f\.techGuardTip = \(f\.guardPoseK \|\| 0\) > 0/.test(sliceFn(codeOnly, "landThrowTech")) && /techGuardTip: false/.test(srcTxt) && /Idle tech still snaps \(no techGuardTip\)/.test(srcTxt));
check("tip under tech-guard short raise leftover tip under throw-guard short raise unchanged", /Tip under throw-guard short raise leftover/.test(srcTxt) && /throwGuardPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /Math\.max\(pk, 1 - raiseT\)/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under tech-guard short raise leftover tip under reverse short raise unchanged", /Tip under reverse short raise leftover/.test(srcTxt) && /reversalPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /Math\.max\(pk, 1 - raiseT\)/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under tech-guard short raise leftover tip under feintFade unchanged", /const fk = feintFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /wx \+ \(tip - wx\) \* \(1 - fk\)/.test(sliceFn(codeOnly, "bladeTipX")) && /wy \+ \(tip - wy\) \* \(1 - fk\)/.test(sliceFn(codeOnly, "bladeTipY")));
check("tip under tech-guard short raise leftover tip under telegraphFade unchanged", /telegraphing\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /telegraphFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /ex \+ \(tip - ex\) \* tf/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under tech-guard short raise leftover tipX under plant fades unchanged", /Math\.max\(clashPlantFade\(f\), linkPlantFade\(f\)\)/.test(sliceFn(codeOnly, "bladeTipX")) && (/sx \+ \(tip - sx\) \* \(1 - k\)/.test(sliceFn(codeOnly, "bladeTipX")) || /from \+ \(tip - from\) \* \(1 - k\)/.test(sliceFn(codeOnly, "bladeTipX"))));
check("tip under tech-guard short raise leftover sheet fade drain unchanged", /return guardRaiseK\(f\)/.test(sliceFn(codeOnly, "techGuardPlantFade")) && /techGuardPlanting\(f\)/.test(sliceFn(codeOnly, "tickGuardPose")) && /f\.guardPoseK <= step/.test(sliceFn(codeOnly, "tickGuardPose")) && /\(f\.guardPoseK \|\| 0\) > 0/.test(sliceFn(codeOnly, "techGuardPlanting")));
check("tip under tech-guard short raise leftover castPlantXY K path unchanged", /clashPlantFade\(f\)/.test(sliceFn(codeOnly, "castPlantXY")) && /linkPlantFade\(f\)/.test(sliceFn(codeOnly, "castPlantXY")) && !/tipPlantK/.test(sliceFn(codeOnly, "castPlantXY")) && !/telegraphFade/.test(sliceFn(codeOnly, "castPlantXY")) && !/feintFade/.test(sliceFn(codeOnly, "castPlantXY")));
check("tip under tech-guard short raise leftover tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("tip under tech-guard short raise leftover tipX locks hold", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt));
check("tip under tech-guard short raise leftover is not a new combat verb", /No new combat verb/.test(srcTxt) && (/Active hitbox still phase===active only/.test(srcTxt) || /active hitbox unchanged/.test(srcTxt)));
check("slash frames still 180\/140\/280 after tip under tech-guard short raise leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280);
check("golpe frames still 120\/80\/180 after tip under tech-guard short raise leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180);
check("K frames still 200\/280 after tip under tech-guard short raise leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280);
check("no 6th button after tip under tech-guard short raise leftover", !/KeyM/.test(codeOnly));
check("cache v=296 after tip under tech-guard short raise leftover", /game.js\?v=350/.test(htmlTxt));

// live: short raise Space+S tech tip eases over GUARD_RAISE_MS, no ~304px dump
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=520;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkT=0; V.player.walkSettleT=0; V.player.walkFadeHold=0;
V.player.sheatheT=0; V.player.guarding=true; V.player.guardPoseK=1; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.cutRecBreathT=0; V.player.telegraph=false; V.player.linkPlant=false; V.player.clashPlant=false;
V.player.reversal=false; V.player.techRec=false; V.player.techGuardTip=false; V.player.facing=1; V.openLeft=0; V.hitstopLeft=0; V.player.stamina=100;
V.rival.phase="idle"; V.rival.phaseT=0; V.rival.guarding=false; V.rival.stunT=0; V.rival.falling=false; V.rival.boltPhase=""; V.rival.feintT=0; V.rival.facing=-1;
V.keys.add("KeyS");
freezeRivalAI(); V.update(STEP);
V.player.guardPoseK = 0.2;
const tecGTip0 = V.bladeTipX(V.player);
const tecGTipR0 = V.destRect(V.player);
const tecGTipEdge0 = V.player.facing>0 ? tecGTipR0.dx+tecGTipR0.dw-8 : tecGTipR0.dx+8;
const tecGTipWu0 = tecGTipR0.dx + 359 * (tecGTipR0.dw/1186);
const tecGTipThrowOk = V.startThrow(V.rival);
const tecGTipDid = tecGTipThrowOk && V.tryThrowTech(V.player);
const tecGTipArmTip = V.bladeTipX(V.player);
const tecGTipArmTecf = V.techGuardPlantFade(V.player);
const tecGTipArmTgp = V.techGuardPlanting(V.player);
const tecGTipArmLatch = !!V.player.techGuardTip;
const tecGTipArmPt = V.player.phaseT|0;
check("tip under tech-guard short raise leftover live arms", tecGTipDid===true && tecGTipArmTgp===true && tecGTipArmLatch===true && V.player.cut==="throw" && V.player.phase==="recovery" && V.player.techRec===true, `did=${tecGTipDid} tgp=${tecGTipArmTgp} tipLatch=${tecGTipArmLatch} cut=${V.player.cut} ph=${V.player.phase} tecf=${tecGTipArmTecf.toFixed(3)} tip=${tecGTipArmTip.toFixed(2)} throwOk=${tecGTipThrowOk}`);
check("tip under tech-guard short raise leftover live arm tip stays near block", Math.abs(tecGTipArmTip-tecGTip0)<12 && Math.abs(tecGTipArmTip-tecGTipWu0)>150 && Math.abs(tecGTipArmTip-tecGTipEdge0)<12, `tip0=${tecGTip0.toFixed(2)} tipArm=${tecGTipArmTip.toFixed(2)} edge=${tecGTipEdge0.toFixed(2)} wu=${tecGTipWu0.toFixed(2)} tecf=${tecGTipArmTecf.toFixed(3)} pt=${tecGTipArmPt}`);
let tecGTipMid=null, tecGTipEnd=null, tecGTipTrackOk=true, tecGTipPrev=tecGTipArmTip, tecGTipSawTecf0=false;
function pinTecGTipYou(){
  freezeRivalAI();
  V.player.stunT = 0;
  V.player.falling = false;
  V.player.hp = 100;
  V.player.boltPhase = "";
  V.player.gait = 0;
  V.player.walkT = 0;
  V.player.walkSettleT = 0;
  V.player.sheatheT = 0;
  V.player.guarding = false;
  V.hitstopLeft = 0;
  V.keys.clear();
}
for (let i=0;i<10;i++){
  pinTecGTipYou(); V.update(STEP);
  const tip=V.bladeTipX(V.player);
  const tecf=V.techGuardPlantFade(V.player);
  const r=V.destRect(V.player);
  const wu=r.dx+359*(r.dw/1186);
  const edge=V.player.facing>0?r.dx+r.dw-8:r.dx+8;
  const pt=V.player.phaseT|0;
  const raiseMs=V.GUARD_RAISE_MS||80;
  const u=Math.max(0,Math.min(1,pt/raiseMs));
  const raiseT=u*u*(3-2*u);
  const tipPlantK=(V.techGuardPlanting(V.player) || V.player.techGuardTip) ? Math.max(tecf, 1-raiseT) : 0;
  if (tecf<=0.02) tecGTipSawTecf0=true;
  if (tecGTipMid==null && tipPlantK>0.2 && tipPlantK<0.8) tecGTipMid={tip, tecf, tipPlantK, wu, edge, pt};
  if (tecGTipEnd==null && (tipPlantK<=0.02 || Math.abs(tip-wu)<12 || V.player.phase!=="recovery")) tecGTipEnd={tip, tecf, tipPlantK, wu, edge, pt, ph:V.player.phase};
  if (Math.abs(tip-tecGTipPrev)>160) tecGTipTrackOk=false;
  tecGTipPrev=tip;
  if (tecGTipEnd) break;
}
check("tip under tech-guard short raise leftover live tip eases mid-plant", tecGTipMid && Math.abs(tecGTipMid.tip-tecGTip0)>15 && Math.abs(tecGTipMid.tip-tecGTipWu0)>15, `mid=${!!tecGTipMid} tip=${tecGTipMid?tecGTipMid.tip.toFixed(2):"na"} tip0=${tecGTip0.toFixed(2)} wu=${tecGTipWu0.toFixed(2)} k=${tecGTipMid?tecGTipMid.tipPlantK.toFixed(3):"na"} tecf0=${tecGTipSawTecf0}`);
check("tip under tech-guard short raise leftover live tip tracks raise not dump", tecGTipEnd && Math.abs(tecGTipEnd.tip-tecGTipEnd.wu)<12 && Math.abs(tecGTipEnd.tip-tecGTip0)>150 && tecGTipTrackOk, `track=${tecGTipTrackOk} end=${tecGTipEnd?tecGTipEnd.tip.toFixed(2):"na"} wu=${tecGTipEnd?tecGTipEnd.wu.toFixed(2):"na"} tip0=${tecGTip0.toFixed(2)} d=${tecGTipEnd?(tecGTipEnd.tip-tecGTip0).toFixed(2):"na"} tecf0=${tecGTipSawTecf0}`);
check("tip under tech-guard short raise leftover live AABB planted", Math.abs(V.bodyAABB(V.player).y - 316.693)<2, `ay=${V.bodyAABB(V.player).y.toFixed(2)}`);

// live: full raise tech tip still eases (regression)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=520;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
V.player.sheatheT=0; V.player.guarding=true; V.player.guardPoseK=1; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.telegraph=false; V.player.facing=1; V.openLeft=0; V.player.linkPlant=false; V.player.clashPlant=false;
V.player.reversal=false; V.player.techRec=false; V.player.techGuardTip=false; V.player.stamina=100;
V.rival.phase="idle"; V.rival.guarding=false; V.rival.stunT=0; V.rival.falling=false; V.rival.boltPhase=""; V.rival.facing=-1;
V.keys.add("KeyS");
for (let i=0;i<10;i++){ freezeRivalAI(); V.update(STEP); }
const tecGTipFull0 = V.bladeTipX(V.player);
const tecGTipFullThrow = V.startThrow(V.rival);
const tecGTipFullDid = tecGTipFullThrow && V.tryThrowTech(V.player);
const tecGTipFullArm = V.bladeTipX(V.player);
const tecGTipFullTecf = V.techGuardPlantFade(V.player);
pinTecGTipYou(); V.update(STEP);
const tecGTipFull1 = V.bladeTipX(V.player);
const tecGTipFullTecf1 = V.techGuardPlantFade(V.player);
check("tip under tech-guard short raise leftover live full raise still eases", tecGTipFullDid && tecGTipFullTecf>0.8 && Math.abs(tecGTipFullArm-tecGTipFull0)<12 && Math.abs(tecGTipFull1-tecGTipFullArm)>10 && tecGTipFullTecf1<tecGTipFullTecf, `tip0=${tecGTipFull0.toFixed(2)} tipArm=${tecGTipFullArm.toFixed(2)} tip1=${tecGTipFull1.toFixed(2)} tecf=${tecGTipFullTecf.toFixed(3)} tecf1=${tecGTipFullTecf1.toFixed(3)} d1=${(tecGTipFull1-tecGTipFullArm).toFixed(2)}`);

// live: throw-guard short raise tip still eases (regression)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
V.player.sheatheT=0; V.player.guarding=true; V.player.guardPoseK=1; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.telegraph=false; V.player.facing=1; V.openLeft=0; V.player.linkPlant=false; V.player.clashPlant=false; V.player.reversal=false;
V.keys.add("KeyS");
freezeRivalAI(); V.update(STEP);
V.player.guardPoseK = 0.2;
const tecGTipTg0 = V.bladeTipX(V.player);
const tecGTipTgDid = V.startThrow(V.player);
const tecGTipTgArm = V.bladeTipX(V.player);
const tecGTipTgTgp = V.throwGuardPlanting(V.player);
check("tip under tech-guard short raise leftover live throw-guard short still holds tip", tecGTipTgDid && tecGTipTgTgp && Math.abs(tecGTipTgArm-tecGTipTg0)<12, `did=${tecGTipTgDid} tgp=${tecGTipTgTgp} tip0=${tecGTipTg0.toFixed(2)} tipArm=${tecGTipTgArm.toFixed(2)}`);

// live: reverse short raise tip still eases (regression)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
V.player.sheatheT=0; V.player.guarding=true; V.player.guardPoseK=1; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.telegraph=false; V.player.facing=1; V.openLeft=0; V.player.linkPlant=false; V.player.clashPlant=false; V.player.reversal=false; V.player.stamina=100;
V.keys.add("KeyS");
freezeRivalAI(); V.update(STEP);
V.player.guardPoseK = 0.2;
const tecGTipRv0 = V.bladeTipX(V.player);
const tecGTipRvDid = V.startReversal(V.player);
const tecGTipRvArm = V.bladeTipX(V.player);
const tecGTipRvRp = V.reversalPlanting(V.player);
check("tip under tech-guard short raise leftover live reverse short still holds tip", tecGTipRvDid && tecGTipRvRp && Math.abs(tecGTipRvArm-tecGTipRv0)<12, `did=${tecGTipRvDid} rp=${tecGTipRvRp} tip0=${tecGTipRv0.toFixed(2)} tipArm=${tecGTipRvArm.toFixed(2)}`);

// live: idle tech still snaps tip (no techGuardTip / no tipPlantK dump to block)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=520;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
V.player.sheatheT=0; V.player.guarding=false; V.player.guardPoseK=0; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.telegraph=false; V.player.facing=1; V.openLeft=0; V.player.linkPlant=false; V.player.clashPlant=false;
V.player.reversal=false; V.player.techRec=false; V.player.techGuardTip=false;
V.rival.phase="idle"; V.rival.guarding=false; V.rival.stunT=0; V.rival.falling=false; V.rival.boltPhase=""; V.rival.facing=-1;
freezeRivalAI(); V.update(STEP);
const tecGTipIdleR = V.destRect(V.player);
const tecGTipIdleWu = tecGTipIdleR.dx + 359 * (tecGTipIdleR.dw/1186);
const tecGTipIdle0 = V.bladeTipX(V.player);
const tecGTipIdleThrow = V.startThrow(V.rival);
const tecGTipIdleDid = tecGTipIdleThrow && V.tryThrowTech(V.player);
const tecGTipIdleArm = V.bladeTipX(V.player);
check("tip under tech-guard short raise leftover live idle tech snaps tip", tecGTipIdleDid && V.player.techRec && V.techGuardPlanting(V.player)===false && V.player.techGuardTip===false && V.techGuardPlantFade(V.player)===0 && Math.abs(tecGTipIdleArm-tecGTipIdleWu)<12, `did=${tecGTipIdleDid} tip0=${tecGTipIdle0.toFixed(2)} tipArm=${tecGTipIdleArm.toFixed(2)} wu=${tecGTipIdleWu.toFixed(2)} latch=${V.player.techGuardTip} tgp=${V.techGuardPlanting(V.player)}`);

// live: tele tip still eases (regression)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
V.player.sheatheT=0; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.telegraph=false; V.player.facing=1; V.openLeft=0; V.player.linkPlant=false; V.player.clashPlant=false;
freezeRivalAI(); V.update(STEP);
const tecGTipTele0 = V.bladeTipX(V.player);
const tecGTipTeleR = V.destRect(V.player);
const tecGTipTeleWu = tecGTipTeleR.dx + 359 * (tecGTipTeleR.dw/1186);
V.startAttack(V.player, "slash");
holdRival(); V.update(STEP);
const tecGTipTeleArm = V.bladeTipX(V.player);
const tecGTipTeleTf = V.telegraphFade(V.player);
check("tip under tech-guard short raise leftover live tele tip still eases", V.telegraphing(V.player) && V.player.phase==="startup" && Math.abs(tecGTipTeleArm-tecGTipTele0)<40 && Math.abs(tecGTipTeleArm-tecGTipTeleWu)>20 && tecGTipTeleTf>0 && tecGTipTeleTf<0.3, `tip0=${tecGTipTele0.toFixed(2)} tipArm=${tecGTipTeleArm.toFixed(2)} wu=${tecGTipTeleWu.toFixed(2)} tf=${tecGTipTeleTf.toFixed(3)}`);

check("cache v=296 after tip under tech-guard short raise leftover live", /game.js\?v=350/.test(htmlTxt));




// --- tip under slash/golpe leftover short raise leftover (v293) ---
check("tip under slash/golpe leftover short raise leftover seats no tip dump", /Tip under slash\/golpe leftover short raise leftover/.test(srcTxt) && /Space\/L from short/.test(srcTxt) && /dump bladeTip onto windup/.test(srcTxt) && /slashLeftoverPlanting/.test(srcTxt) && /golpeLeftoverPlanting/.test(srcTxt) && (/~304px/.test(srcTxt) || /gpk one-tick hole/.test(srcTxt)));
check("tip under slash/golpe leftover short raise leftover is draw-only", /Draw-only \(bladeTipX \/ bladeTipY\)/.test(srcTxt) && /leftoverPlantTip/.test(sliceFn(codeOnly, "bladeTipX")) && /slashLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /golpeLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /tipPlantK/.test(sliceFn(codeOnly, "bladeTipX")) && /tipPlantK/.test(sliceFn(codeOnly, "bladeTipY")) && !/tipPlantK/.test(sliceFn(codeOnly, "destRect")) && !/tipPlantK/.test(sliceFn(codeOnly, "bodyAABB")) && !/tipPlantK/.test(sliceFn(codeOnly, "poseBitmap")) && !/tipPlantK/.test(sliceFn(codeOnly, "tickGuardPose")));
check("tip under slash/golpe leftover short raise leftover mirrors tech-guard tipPlantK", /\(throwGuardPlanting\(f\) \|\| reversalPlanting\(f\) \|\| techGuardPlanting\(f\) \|\| f\.techGuardTip \|\| \(\(slashLeftoverPlanting\(f\) \|\| golpeLeftoverPlanting\(f\) \|\| boltLeftoverPlanting\(f\)\) && f\.leftoverPlantTip && !telegraphing\(f\)\)\) && GUARD_RAISE_MS > 0/.test(sliceFn(codeOnly, "bladeTipX")) && /\(throwGuardPlanting\(f\) \|\| reversalPlanting\(f\) \|\| techGuardPlanting\(f\) \|\| f\.techGuardTip \|\| \(\(slashLeftoverPlanting\(f\) \|\| golpeLeftoverPlanting\(f\) \|\| boltLeftoverPlanting\(f\)\) && f\.leftoverPlantTip && !telegraphing\(f\)\)\) && GUARD_RAISE_MS > 0/.test(sliceFn(codeOnly, "bladeTipY")) && /Math\.max\(pk, 1 - raiseT\)/.test(sliceFn(codeOnly, "bladeTipX")) && /Math\.max\(pk, 1 - raiseT\)/.test(sliceFn(codeOnly, "bladeTipY")) && /bx \+ \(tip - bx\) \* \(1 - tipPlantK\)/.test(sliceFn(codeOnly, "bladeTipX")) && /by \+ \(tip - by\) \* \(1 - tipPlantK\)/.test(sliceFn(codeOnly, "bladeTipY")));
check("tip under slash/golpe leftover short raise leftover latches leftoverPlantTip", /f\.leftoverPlantTip = \(f\.guardPoseK \|\| 0\) > 0/.test(sliceFn(codeOnly, "startAttack")) && /leftoverPlantTip: false/.test(srcTxt) && /idle Space\/L/.test(srcTxt) && /still snaps \(no latch\)/.test(srcTxt));
check("tip under slash/golpe leftover short raise leftover tip under tech-guard short raise unchanged", /Tip under tech-guard short raise leftover/.test(srcTxt) && /techGuardPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /techGuardTip/.test(sliceFn(codeOnly, "bladeTipX")) && /Math\.max\(pk, 1 - raiseT\)/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under slash/golpe leftover short raise leftover tip under throw-guard short raise unchanged", /Tip under throw-guard short raise leftover/.test(srcTxt) && /throwGuardPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /Math\.max\(pk, 1 - raiseT\)/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under slash/golpe leftover short raise leftover tip under reverse short raise unchanged", /Tip under reverse short raise leftover/.test(srcTxt) && /reversalPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /Math\.max\(pk, 1 - raiseT\)/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under slash/golpe leftover short raise leftover tip under feintFade unchanged", /const fk = feintFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /wx \+ \(tip - wx\) \* \(1 - fk\)/.test(sliceFn(codeOnly, "bladeTipX")) && /wy \+ \(tip - wy\) \* \(1 - fk\)/.test(sliceFn(codeOnly, "bladeTipY")));
check("tip under slash/golpe leftover short raise leftover tip under telegraphFade unchanged", /telegraphing\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /telegraphFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /ex \+ \(tip - ex\) \* tf/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under slash/golpe leftover short raise leftover tipX under plant fades unchanged", /Math\.max\(clashPlantFade\(f\), linkPlantFade\(f\)\)/.test(sliceFn(codeOnly, "bladeTipX")) && (/sx \+ \(tip - sx\) \* \(1 - k\)/.test(sliceFn(codeOnly, "bladeTipX")) || /from \+ \(tip - from\) \* \(1 - k\)/.test(sliceFn(codeOnly, "bladeTipX"))));
check("tip under slash/golpe leftover short raise leftover sheet leftover-k drain unchanged", /slashLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "tickGuardPose")) && /golpeLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "tickGuardPose")) && /f\.guardPoseK <= step/.test(sliceFn(codeOnly, "tickGuardPose")) && !/\(f\.guardPoseK \|\| 0\) > 0/.test(sliceFn(codeOnly, "slashLeftoverPlanting")));
check("tip under slash/golpe leftover short raise leftover castPlantXY K path unchanged", /clashPlantFade\(f\)/.test(sliceFn(codeOnly, "castPlantXY")) && /linkPlantFade\(f\)/.test(sliceFn(codeOnly, "castPlantXY")) && !/tipPlantK/.test(sliceFn(codeOnly, "castPlantXY")) && !/telegraphFade/.test(sliceFn(codeOnly, "castPlantXY")) && !/feintFade/.test(sliceFn(codeOnly, "castPlantXY")));
check("tip under slash/golpe leftover short raise leftover tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("tip under slash/golpe leftover short raise leftover tipX locks hold", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt));
check("tip under slash/golpe leftover short raise leftover is not a new combat verb", /No new combat verb/.test(srcTxt) && (/Active hitbox still phase===active only/.test(srcTxt) || /active hitbox unchanged/.test(srcTxt)));
check("slash frames still 180\/140\/280 after tip under slash/golpe leftover short raise leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280);
check("golpe frames still 120\/80\/180 after tip under slash/golpe leftover short raise leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180);
check("K frames still 200\/280 after tip under slash/golpe leftover short raise leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280);
check("no 6th button after tip under slash/golpe leftover short raise leftover", !/KeyM/.test(codeOnly));
check("cache v=296 after tip under slash/golpe leftover short raise leftover", /game.js\?v=350/.test(htmlTxt));

// live: short raise leftover-drop slash tip eases over GUARD_RAISE_MS with tele off (no ~304px dump)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkT=0; V.player.walkSettleT=0; V.player.walkFadeHold=0;
V.player.sheatheT=0; V.player.guarding=true; V.player.guardPoseK=1; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.cutRecBreathT=0; V.player.telegraph=false; V.player.linkPlant=false; V.player.clashPlant=false;
V.player.reversal=false; V.player.leftoverPlantTip=false; V.player.facing=1; V.openLeft=0; V.hitstopLeft=0;
V.keys.add("KeyS");
freezeRivalAI(); V.update(STEP);
V.player.guardPoseK = 0.2;
const sgTip0 = V.bladeTipX(V.player);
const sgR0 = V.destRect(V.player);
const sgEdge0 = V.player.facing>0 ? sgR0.dx+sgR0.dw-8 : sgR0.dx+8;
const sgWu0 = sgR0.dx + 359 * (sgR0.dw/1186);
V.keys.clear();
V.player.guarding = false;
V.startAttack(V.player, "slash");
// Force tele off so tipPlantK latch is the sole tip ease (tele would also hold).
V.player.telegraph = false; V.player.linkPlant = false; V.player.linkSheathe = 0;
const sgArmTip = V.bladeTipX(V.player);
const sgArmSlp = V.slashLeftoverPlanting(V.player);
const sgArmLatch = !!V.player.leftoverPlantTip;
const sgArmGk = V.guardRaiseK(V.player);
const sgArmPt = V.player.phaseT|0;
check("tip under slash/golpe leftover short raise leftover live slash arms", V.player.cut==="slash" && V.player.phase==="startup" && sgArmSlp===true && sgArmLatch===true, `cut=${V.player.cut} ph=${V.player.phase} slp=${sgArmSlp} latch=${sgArmLatch} gk=${sgArmGk.toFixed(3)} tip=${sgArmTip.toFixed(2)}`);
check("tip under slash/golpe leftover short raise leftover live slash arm tip stays near block", Math.abs(sgArmTip-sgTip0)<12 && Math.abs(sgArmTip-sgWu0)>150 && Math.abs(sgArmTip-sgEdge0)<12, `tip0=${sgTip0.toFixed(2)} tipArm=${sgArmTip.toFixed(2)} edge=${sgEdge0.toFixed(2)} wu=${sgWu0.toFixed(2)} gk=${sgArmGk.toFixed(3)} pt=${sgArmPt}`);
let sgMid=null, sgEnd=null, sgTrackOk=true, sgPrev=sgArmTip, sgSawGk0=false;
function pinSgYou(){
  freezeRivalAI();
  V.player.stunT = 0;
  V.player.falling = false;
  V.player.hp = 100;
  V.player.boltPhase = "";
  V.player.gait = 0;
  V.player.walkT = 0;
  V.player.walkSettleT = 0;
  V.player.sheatheT = 0;
  V.player.guarding = false;
  V.player.telegraph = false;
  V.player.linkPlant = false;
  V.player.linkSheathe = 0;
  V.hitstopLeft = 0;
  V.keys.clear();
}
for (let i=0;i<10;i++){
  pinSgYou(); V.update(STEP);
  const tip=V.bladeTipX(V.player);
  const gk=V.guardRaiseK(V.player);
  const r=V.destRect(V.player);
  const wu=r.dx+359*(r.dw/1186);
  const edge=V.player.facing>0?r.dx+r.dw-8:r.dx+8;
  const pt=V.player.phaseT|0;
  const raiseMs=V.GUARD_RAISE_MS||80;
  const u=Math.max(0,Math.min(1,pt/raiseMs));
  const raiseT=u*u*(3-2*u);
  const tipPlantK=(V.slashLeftoverPlanting(V.player) && V.player.leftoverPlantTip) ? Math.max(gk, 1-raiseT) : 0;
  if (gk<=0.02) sgSawGk0=true;
  if (sgMid==null && tipPlantK>0.2 && tipPlantK<0.8) sgMid={tip, gk, tipPlantK, wu, edge, pt};
  if (sgEnd==null && (tipPlantK<=0.02 || Math.abs(tip-wu)<12 || V.player.phase!=="startup")) sgEnd={tip, gk, tipPlantK, wu, edge, pt, ph:V.player.phase};
  if (Math.abs(tip-sgPrev)>160) sgTrackOk=false;
  sgPrev=tip;
  if (sgEnd) break;
}
check("tip under slash/golpe leftover short raise leftover live slash tip eases mid-plant", sgMid && Math.abs(sgMid.tip-sgTip0)>15 && Math.abs(sgMid.tip-sgWu0)>15, `mid=${!!sgMid} tip=${sgMid?sgMid.tip.toFixed(2):"na"} tip0=${sgTip0.toFixed(2)} wu=${sgWu0.toFixed(2)} k=${sgMid?sgMid.tipPlantK.toFixed(3):"na"} gk0=${sgSawGk0}`);
check("tip under slash/golpe leftover short raise leftover live slash tip tracks raise not dump", sgEnd && Math.abs(sgEnd.tip-sgEnd.wu)<12 && Math.abs(sgEnd.tip-sgTip0)>150 && sgTrackOk, `track=${sgTrackOk} end=${sgEnd?sgEnd.tip.toFixed(2):"na"} wu=${sgEnd?sgEnd.wu.toFixed(2):"na"} tip0=${sgTip0.toFixed(2)} d=${sgEnd?(sgEnd.tip-sgTip0).toFixed(2):"na"} gk0=${sgSawGk0}`);
check("tip under slash/golpe leftover short raise leftover live AABB planted", Math.abs(V.bodyAABB(V.player).y - 316.693)<2, `ay=${V.bodyAABB(V.player).y.toFixed(2)}`);

// live: golpe sibling short raise leftover tip holds (same latch hole)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
V.player.sheatheT=0; V.player.guarding=true; V.player.guardPoseK=1; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.telegraph=false; V.player.facing=1; V.openLeft=0; V.player.linkPlant=false; V.player.clashPlant=false;
V.player.reversal=false; V.player.leftoverPlantTip=false;
V.keys.add("KeyS");
freezeRivalAI(); V.update(STEP);
V.player.guardPoseK = 0.2;
const sgG0 = V.bladeTipX(V.player);
const sgGR0 = V.destRect(V.player);
const sgGWu0 = sgGR0.dx + 359 * (sgGR0.dw/1186);
V.keys.clear();
V.player.guarding = false;
V.startAttack(V.player, "golpe");
V.player.telegraph = false; V.player.linkPlant = false; V.player.linkSheathe = 0;
const sgGArm = V.bladeTipX(V.player);
const sgGLatch = !!V.player.leftoverPlantTip;
const sgGGlp = V.golpeLeftoverPlanting(V.player);
check("tip under slash/golpe leftover short raise leftover live golpe arms", V.player.cut==="golpe" && V.player.phase==="startup" && sgGGlp===true && sgGLatch===true && Math.abs(sgGArm-sgG0)<12 && Math.abs(sgGArm-sgGWu0)>150, `cut=${V.player.cut} glp=${sgGGlp} latch=${sgGLatch} tip0=${sgG0.toFixed(2)} tipArm=${sgGArm.toFixed(2)} wu=${sgGWu0.toFixed(2)}`);

// live: full raise leftover-drop slash tip still eases (regression)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
V.player.sheatheT=0; V.player.guarding=true; V.player.guardPoseK=1; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.telegraph=false; V.player.facing=1; V.openLeft=0; V.player.linkPlant=false; V.player.clashPlant=false;
V.player.leftoverPlantTip=false;
V.keys.add("KeyS");
for (let i=0;i<10;i++){ freezeRivalAI(); V.update(STEP); }
const sgFull0 = V.bladeTipX(V.player);
V.keys.clear();
V.player.guarding = false;
V.startAttack(V.player, "slash");
V.player.telegraph = false; V.player.linkPlant = false; V.player.linkSheathe = 0;
const sgFullArm = V.bladeTipX(V.player);
const sgFullGk = V.guardRaiseK(V.player);
const sgFullLatch = !!V.player.leftoverPlantTip;
pinSgYou(); V.update(STEP);
const sgFull1 = V.bladeTipX(V.player);
const sgFullGk1 = V.guardRaiseK(V.player);
check("tip under slash/golpe leftover short raise leftover live full raise still eases", sgFullLatch && sgFullGk>0.8 && Math.abs(sgFullArm-sgFull0)<12 && Math.abs(sgFull1-sgFullArm)>10 && sgFullGk1<sgFullGk, `tip0=${sgFull0.toFixed(2)} tipArm=${sgFullArm.toFixed(2)} tip1=${sgFull1.toFixed(2)} gk=${sgFullGk.toFixed(3)} gk1=${sgFullGk1.toFixed(3)} d1=${(sgFull1-sgFullArm).toFixed(2)}`);

// live: tech-guard short still holds tip (regression)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=520;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
V.player.sheatheT=0; V.player.guarding=true; V.player.guardPoseK=1; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.telegraph=false; V.player.facing=1; V.openLeft=0; V.player.linkPlant=false; V.player.clashPlant=false;
V.player.reversal=false; V.player.techRec=false; V.player.techGuardTip=false; V.player.leftoverPlantTip=false; V.player.stamina=100;
V.rival.phase="idle"; V.rival.guarding=false; V.rival.stunT=0; V.rival.falling=false; V.rival.boltPhase=""; V.rival.facing=-1;
V.keys.add("KeyS");
freezeRivalAI(); V.update(STEP);
V.player.guardPoseK = 0.2;
const sgTec0 = V.bladeTipX(V.player);
const sgTecThrow = V.startThrow(V.rival);
const sgTecDid = sgTecThrow && V.tryThrowTech(V.player);
const sgTecArm = V.bladeTipX(V.player);
const sgTecTgp = V.techGuardPlanting(V.player);
check("tip under slash/golpe leftover short raise leftover live tech-guard short still holds tip", sgTecDid && sgTecTgp && Math.abs(sgTecArm-sgTec0)<12, `did=${sgTecDid} tgp=${sgTecTgp} tip0=${sgTec0.toFixed(2)} tipArm=${sgTecArm.toFixed(2)}`);

// live: throw-guard short still holds tip (regression)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
V.player.sheatheT=0; V.player.guarding=true; V.player.guardPoseK=1; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.telegraph=false; V.player.facing=1; V.openLeft=0; V.player.linkPlant=false; V.player.clashPlant=false; V.player.reversal=false; V.player.leftoverPlantTip=false;
V.keys.add("KeyS");
freezeRivalAI(); V.update(STEP);
V.player.guardPoseK = 0.2;
const sgTg0 = V.bladeTipX(V.player);
const sgTgDid = V.startThrow(V.player);
const sgTgArm = V.bladeTipX(V.player);
const sgTgTgp = V.throwGuardPlanting(V.player);
check("tip under slash/golpe leftover short raise leftover live throw-guard short still holds tip", sgTgDid && sgTgTgp && Math.abs(sgTgArm-sgTg0)<12, `did=${sgTgDid} tgp=${sgTgTgp} tip0=${sgTg0.toFixed(2)} tipArm=${sgTgArm.toFixed(2)}`);

// live: idle slash still snaps tip with tele off (no leftoverPlantTip)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
V.player.sheatheT=0; V.player.guarding=false; V.player.guardPoseK=0; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.telegraph=false; V.player.facing=1; V.openLeft=0; V.player.linkPlant=false; V.player.clashPlant=false;
V.player.leftoverPlantTip=false;
freezeRivalAI(); V.update(STEP);
const sgIdleR = V.destRect(V.player);
const sgIdleWu = sgIdleR.dx + 359 * (sgIdleR.dw/1186);
const sgIdle0 = V.bladeTipX(V.player);
V.startAttack(V.player, "slash");
V.player.telegraph = false; V.player.linkPlant = false; V.player.linkSheathe = 0;
const sgIdleArm = V.bladeTipX(V.player);
check("tip under slash/golpe leftover short raise leftover live idle slash snaps tip", V.player.cut==="slash" && V.player.phase==="startup" && V.player.leftoverPlantTip===false && V.slashLeftoverPlanting(V.player)===true && Math.abs(sgIdleArm-sgIdleWu)<12, `tip0=${sgIdle0.toFixed(2)} tipArm=${sgIdleArm.toFixed(2)} wu=${sgIdleWu.toFixed(2)} latch=${V.player.leftoverPlantTip}`);

// live: tele tip still eases (regression; idle slash with tele on, no latch)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
V.player.sheatheT=0; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.telegraph=false; V.player.facing=1; V.openLeft=0; V.player.linkPlant=false; V.player.clashPlant=false;
V.player.leftoverPlantTip=false;
freezeRivalAI(); V.update(STEP);
const sgTele0 = V.bladeTipX(V.player);
const sgTeleR = V.destRect(V.player);
const sgTeleWu = sgTeleR.dx + 359 * (sgTeleR.dw/1186);
V.startAttack(V.player, "slash");
holdRival(); V.update(STEP);
const sgTeleArm = V.bladeTipX(V.player);
const sgTeleTf = V.telegraphFade(V.player);
check("tip under slash/golpe leftover short raise leftover live tele tip still eases", V.telegraphing(V.player) && V.player.phase==="startup" && V.player.leftoverPlantTip===false && Math.abs(sgTeleArm-sgTele0)<40 && Math.abs(sgTeleArm-sgTeleWu)>20 && sgTeleTf>0 && sgTeleTf<0.3, `tip0=${sgTele0.toFixed(2)} tipArm=${sgTeleArm.toFixed(2)} wu=${sgTeleWu.toFixed(2)} tf=${sgTeleTf.toFixed(3)} latch=${V.player.leftoverPlantTip}`);

check("cache v=296 after tip under slash/golpe leftover short raise leftover live", /game.js\?v=350/.test(htmlTxt));




// --- tip under bolt leftover short raise leftover (v294) ---
check("tip under bolt leftover short raise leftover seats no tip dump", /Tip under bolt leftover short raise leftover/.test(srcTxt) && /K from short leftover drop/.test(srcTxt) && /dump bladeTip onto windup/.test(srcTxt) && /boltLeftoverPlanting/.test(srcTxt) && (/~304px/.test(srcTxt) || /gpk one-tick hole/.test(srcTxt) || /sheathe-plant sets tele off/.test(srcTxt)));
check("tip under bolt leftover short raise leftover is draw-only", /Draw-only \(bladeTipX \/ bladeTipY\)/.test(srcTxt) && /leftoverPlantTip/.test(sliceFn(codeOnly, "bladeTipX")) && /boltLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /tipPlantK/.test(sliceFn(codeOnly, "bladeTipX")) && /tipPlantK/.test(sliceFn(codeOnly, "bladeTipY")) && !/tipPlantK/.test(sliceFn(codeOnly, "destRect")) && !/tipPlantK/.test(sliceFn(codeOnly, "bodyAABB")) && !/tipPlantK/.test(sliceFn(codeOnly, "poseBitmap")) && !/tipPlantK/.test(sliceFn(codeOnly, "tickGuardPose")));
check("tip under bolt leftover short raise leftover mirrors slash/golpe tipPlantK", /\(throwGuardPlanting\(f\) \|\| reversalPlanting\(f\) \|\| techGuardPlanting\(f\) \|\| f\.techGuardTip \|\| \(\(slashLeftoverPlanting\(f\) \|\| golpeLeftoverPlanting\(f\) \|\| boltLeftoverPlanting\(f\)\) && f\.leftoverPlantTip && !telegraphing\(f\)\)\) && GUARD_RAISE_MS > 0/.test(sliceFn(codeOnly, "bladeTipX")) && /\(throwGuardPlanting\(f\) \|\| reversalPlanting\(f\) \|\| techGuardPlanting\(f\) \|\| f\.techGuardTip \|\| \(\(slashLeftoverPlanting\(f\) \|\| golpeLeftoverPlanting\(f\) \|\| boltLeftoverPlanting\(f\)\) && f\.leftoverPlantTip && !telegraphing\(f\)\)\) && GUARD_RAISE_MS > 0/.test(sliceFn(codeOnly, "bladeTipY")) && /Math\.max\(pk, 1 - raiseT\)/.test(sliceFn(codeOnly, "bladeTipX")) && /Math\.max\(pk, 1 - raiseT\)/.test(sliceFn(codeOnly, "bladeTipY")) && /bx \+ \(tip - bx\) \* \(1 - tipPlantK\)/.test(sliceFn(codeOnly, "bladeTipX")) && /by \+ \(tip - by\) \* \(1 - tipPlantK\)/.test(sliceFn(codeOnly, "bladeTipY")));
check("tip under bolt leftover short raise leftover uses boltT on bolt startup", /tRaise/.test(sliceFn(codeOnly, "bladeTipX")) && /boltPhase === \"startup\" \? f\.boltT : \(f\.phaseT \|\| 0\)/.test(sliceFn(codeOnly, "bladeTipX")) && /boltPhase === \"startup\" \? f\.boltT : \(f\.phaseT \|\| 0\)/.test(sliceFn(codeOnly, "bladeTipY")));
check("tip under bolt leftover short raise leftover latches leftoverPlantTip", /u\.leftoverPlantTip = \(u\.guardPoseK \|\| 0\) > 0/.test(sliceFn(codeOnly, "startBolt")) && /leftoverPlantTip: false/.test(srcTxt) && /idle K still snaps/.test(srcTxt) && /special-cancel still snaps/.test(srcTxt));
check("tip under bolt leftover short raise leftover tip under slash/golpe leftover short raise unchanged", /Tip under slash\/golpe leftover short raise leftover/.test(srcTxt) && /slashLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /golpeLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /Math\.max\(pk, 1 - raiseT\)/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under bolt leftover short raise leftover tip under tech-guard short raise unchanged", /Tip under tech-guard short raise leftover/.test(srcTxt) && /techGuardPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /techGuardTip/.test(sliceFn(codeOnly, "bladeTipX")) && /Math\.max\(pk, 1 - raiseT\)/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under bolt leftover short raise leftover tip under throw-guard short raise unchanged", /Tip under throw-guard short raise leftover/.test(srcTxt) && /throwGuardPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /Math\.max\(pk, 1 - raiseT\)/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under bolt leftover short raise leftover tip under reverse short raise unchanged", /Tip under reverse short raise leftover/.test(srcTxt) && /reversalPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /Math\.max\(pk, 1 - raiseT\)/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under bolt leftover short raise leftover tip under feintFade unchanged", /const fk = feintFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /wx \+ \(tip - wx\) \* \(1 - fk\)/.test(sliceFn(codeOnly, "bladeTipX")) && /wy \+ \(tip - wy\) \* \(1 - fk\)/.test(sliceFn(codeOnly, "bladeTipY")));
check("tip under bolt leftover short raise leftover tip under telegraphFade unchanged", /telegraphing\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /telegraphFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /ex \+ \(tip - ex\) \* tf/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under bolt leftover short raise leftover tipX under plant fades unchanged", /Math\.max\(clashPlantFade\(f\), linkPlantFade\(f\)\)/.test(sliceFn(codeOnly, "bladeTipX")) && (/sx \+ \(tip - sx\) \* \(1 - k\)/.test(sliceFn(codeOnly, "bladeTipX")) || /from \+ \(tip - from\) \* \(1 - k\)/.test(sliceFn(codeOnly, "bladeTipX"))));
check("tip under bolt leftover short raise leftover sheet leftover-k drain unchanged", /boltLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "tickGuardPose")) && /slashLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "tickGuardPose")) && /golpeLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "tickGuardPose")) && /f\.guardPoseK <= step/.test(sliceFn(codeOnly, "tickGuardPose")));
check("tip under bolt leftover short raise leftover castPlantXY K path unchanged", /clashPlantFade\(f\)/.test(sliceFn(codeOnly, "castPlantXY")) && /linkPlantFade\(f\)/.test(sliceFn(codeOnly, "castPlantXY")) && !/tipPlantK/.test(sliceFn(codeOnly, "castPlantXY")) && !/telegraphFade/.test(sliceFn(codeOnly, "castPlantXY")) && !/feintFade/.test(sliceFn(codeOnly, "castPlantXY")));
check("tip under bolt leftover short raise leftover tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("tip under bolt leftover short raise leftover tipX locks hold", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt));
check("tip under bolt leftover short raise leftover is not a new combat verb", /No new combat verb/.test(srcTxt) && (/Active hitbox still phase===active only/.test(srcTxt) || /active hitbox unchanged/.test(srcTxt)));
check("slash frames still 180\/140\/280 after tip under bolt leftover short raise leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280);
check("golpe frames still 120\/80\/180 after tip under bolt leftover short raise leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180);
check("K frames still 200\/280 after tip under bolt leftover short raise leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280);
check("no 6th button after tip under bolt leftover short raise leftover", !/KeyM/.test(codeOnly));
check("cache v=296 after tip under bolt leftover short raise leftover", /game.js\?v=350/.test(htmlTxt));

// live: short raise leftover-drop empty-K tip eases over GUARD_RAISE_MS with tele off (no ~304px dump)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkT=0; V.player.walkSettleT=0; V.player.walkFadeHold=0;
V.player.sheatheT=0; V.player.guarding=true; V.player.guardPoseK=1; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.cutRecBreathT=0; V.player.telegraph=false; V.player.linkPlant=false; V.player.clashPlant=false;
V.player.reversal=false; V.player.leftoverPlantTip=false; V.player.facing=1; V.openLeft=0; V.hitstopLeft=0;
V.player.stamina=100; V.player.boltHoldCut=false; V.player.boltSuper=false;
V.keys.add("KeyS");
freezeRivalAI(); V.update(STEP);
V.player.guardPoseK = 0.2;
const bltTip0 = V.bladeTipX(V.player);
const bltR0 = V.destRect(V.player);
const bltEdge0 = V.player.facing>0 ? bltR0.dx+bltR0.dw-8 : bltR0.dx+8;
const bltWu0 = bltR0.dx + 268 * (bltR0.dw/1186);
V.keys.clear();
V.player.guarding = false;
const bltOk = V.startBolt(V.player, false);
// Force tele off so tipPlantK latch is the sole tip ease (tele would also hold).
V.player.telegraph = false; V.player.linkPlant = false; V.player.linkSheathe = 0;
const bltArmTip = V.bladeTipX(V.player);
const bltArmBlp = V.boltLeftoverPlanting(V.player);
const bltArmLatch = !!V.player.leftoverPlantTip;
const bltArmGk = V.guardRaiseK(V.player);
const bltArmBt = V.player.boltT|0;
check("tip under bolt leftover short raise leftover live bolt arms", bltOk && V.player.boltPhase==="startup" && bltArmBlp===true && bltArmLatch===true, `ok=${bltOk} bph=${V.player.boltPhase} blp=${bltArmBlp} latch=${bltArmLatch} gk=${bltArmGk.toFixed(3)} tip=${bltArmTip.toFixed(2)}`);
check("tip under bolt leftover short raise leftover live bolt arm tip stays near block", Math.abs(bltArmTip-bltTip0)<12 && Math.abs(bltArmTip-bltWu0)>150 && Math.abs(bltArmTip-bltEdge0)<12, `tip0=${bltTip0.toFixed(2)} tipArm=${bltArmTip.toFixed(2)} edge=${bltEdge0.toFixed(2)} wu=${bltWu0.toFixed(2)} gk=${bltArmGk.toFixed(3)} bt=${bltArmBt}`);
let bltMid=null, bltEnd=null, bltTrackOk=true, bltPrev=bltArmTip, bltSawGk0=false;
function pinBltYou(){
  freezeRivalAI();
  V.player.stunT = 0;
  V.player.falling = false;
  V.player.hp = 100;
  V.player.gait = 0;
  V.player.walkT = 0;
  V.player.walkSettleT = 0;
  V.player.sheatheT = 0;
  V.player.guarding = false;
  V.player.telegraph = false;
  V.player.linkPlant = false;
  V.player.linkSheathe = 0;
  V.player.boltHoldCut = false;
  V.hitstopLeft = 0;
  V.keys.clear();
}
for (let i=0;i<10;i++){
  pinBltYou(); V.update(STEP);
  const tip=V.bladeTipX(V.player);
  const gk=V.guardRaiseK(V.player);
  const r=V.destRect(V.player);
  const wu=r.dx+268*(r.dw/1186);
  const edge=V.player.facing>0?r.dx+r.dw-8:r.dx+8;
  const bt=V.player.boltT|0;
  const raiseMs=V.GUARD_RAISE_MS||80;
  const u=Math.max(0,Math.min(1,bt/raiseMs));
  const raiseT=u*u*(3-2*u);
  const tipPlantK=(V.boltLeftoverPlanting(V.player) && V.player.leftoverPlantTip) ? Math.max(gk, 1-raiseT) : 0;
  if (gk<=0.02) bltSawGk0=true;
  if (bltMid==null && tipPlantK>0.2 && tipPlantK<0.8) bltMid={tip, gk, tipPlantK, wu, edge, bt};
  if (bltEnd==null && (tipPlantK<=0.02 || Math.abs(tip-wu)<12 || V.player.boltPhase!=="startup")) bltEnd={tip, gk, tipPlantK, wu, edge, bt, bph:V.player.boltPhase};
  if (Math.abs(tip-bltPrev)>160) bltTrackOk=false;
  bltPrev=tip;
  if (bltEnd) break;
}
check("tip under bolt leftover short raise leftover live bolt tip eases mid-plant", bltMid && Math.abs(bltMid.tip-bltTip0)>15 && Math.abs(bltMid.tip-bltWu0)>15, `mid=${!!bltMid} tip=${bltMid?bltMid.tip.toFixed(2):"na"} tip0=${bltTip0.toFixed(2)} wu=${bltWu0.toFixed(2)} k=${bltMid?bltMid.tipPlantK.toFixed(3):"na"} gk0=${bltSawGk0}`);
check("tip under bolt leftover short raise leftover live bolt tip tracks raise not dump", bltEnd && Math.abs(bltEnd.tip-bltEnd.wu)<12 && Math.abs(bltEnd.tip-bltTip0)>150 && bltTrackOk, `track=${bltTrackOk} end=${bltEnd?bltEnd.tip.toFixed(2):"na"} wu=${bltEnd?bltEnd.wu.toFixed(2):"na"} tip0=${bltTip0.toFixed(2)} d=${bltEnd?(bltEnd.tip-bltTip0).toFixed(2):"na"} gk0=${bltSawGk0}`);
check("tip under bolt leftover short raise leftover live AABB planted", Math.abs(V.bodyAABB(V.player).y - 316.693)<2, `ay=${V.bodyAABB(V.player).y.toFixed(2)}`);

// live: sheathe/tele-off path still owned by tipPlantK (source + latch under forced tele off already above)
check("tip under bolt leftover short raise leftover sheathe tele-off path still in startBolt", /u\.telegraph = !holdingCutBolt\(u\) && !u\.linkPlant/.test(sliceFn(codeOnly, "startBolt")) && /leftoverSheathe = sheatheFade\(u\)/.test(sliceFn(codeOnly, "startBolt")) && /u\.leftoverPlantTip = \(u\.guardPoseK \|\| 0\) > 0/.test(sliceFn(codeOnly, "startBolt")) && /tipPlantK covers tele-off \/ sheathe-plant leftover drop/.test(srcTxt));

// live: full raise leftover-drop empty-K tip still eases (regression)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
V.player.sheatheT=0; V.player.guarding=true; V.player.guardPoseK=1; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.telegraph=false; V.player.facing=1; V.openLeft=0; V.player.linkPlant=false; V.player.clashPlant=false;
V.player.leftoverPlantTip=false; V.player.stamina=100; V.player.boltHoldCut=false;
V.keys.add("KeyS");
for (let i=0;i<10;i++){ freezeRivalAI(); V.update(STEP); }
const bltFull0 = V.bladeTipX(V.player);
V.keys.clear();
V.player.guarding = false;
const bltFullOk = V.startBolt(V.player, false);
V.player.telegraph = false; V.player.linkPlant = false; V.player.linkSheathe = 0;
const bltFullArm = V.bladeTipX(V.player);
const bltFullGk = V.guardRaiseK(V.player);
const bltFullLatch = !!V.player.leftoverPlantTip;
pinBltYou(); V.update(STEP);
const bltFull1 = V.bladeTipX(V.player);
const bltFullGk1 = V.guardRaiseK(V.player);
check("tip under bolt leftover short raise leftover live full raise still eases", bltFullOk && bltFullLatch && bltFullGk>0.8 && Math.abs(bltFullArm-bltFull0)<12 && Math.abs(bltFull1-bltFullArm)>10 && bltFullGk1<bltFullGk, `tip0=${bltFull0.toFixed(2)} tipArm=${bltFullArm.toFixed(2)} tip1=${bltFull1.toFixed(2)} gk=${bltFullGk.toFixed(3)} gk1=${bltFullGk1.toFixed(3)} d1=${(bltFull1-bltFullArm).toFixed(2)}`);

// live: slash leftover short still holds tip (regression)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
V.player.sheatheT=0; V.player.guarding=true; V.player.guardPoseK=1; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.telegraph=false; V.player.facing=1; V.openLeft=0; V.player.linkPlant=false; V.player.clashPlant=false;
V.player.reversal=false; V.player.leftoverPlantTip=false;
V.keys.add("KeyS");
freezeRivalAI(); V.update(STEP);
V.player.guardPoseK = 0.2;
const bltSg0 = V.bladeTipX(V.player);
V.keys.clear();
V.player.guarding = false;
V.startAttack(V.player, "slash");
V.player.telegraph = false; V.player.linkPlant = false; V.player.linkSheathe = 0;
const bltSgArm = V.bladeTipX(V.player);
const bltSgLatch = !!V.player.leftoverPlantTip;
const bltSgSlp = V.slashLeftoverPlanting(V.player);
check("tip under bolt leftover short raise leftover live slash leftover short still holds tip", V.player.cut==="slash" && bltSgSlp && bltSgLatch && Math.abs(bltSgArm-bltSg0)<12, `slp=${bltSgSlp} latch=${bltSgLatch} tip0=${bltSg0.toFixed(2)} tipArm=${bltSgArm.toFixed(2)}`);

// live: idle empty-K still snaps tip with tele off (no leftoverPlantTip)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
V.player.sheatheT=0; V.player.guarding=false; V.player.guardPoseK=0; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.telegraph=false; V.player.facing=1; V.openLeft=0; V.player.linkPlant=false; V.player.clashPlant=false;
V.player.leftoverPlantTip=false; V.player.stamina=100; V.player.boltHoldCut=false;
freezeRivalAI(); V.update(STEP);
const bltIdleR = V.destRect(V.player);
const bltIdleWu = bltIdleR.dx + 268 * (bltIdleR.dw/1186);
const bltIdle0 = V.bladeTipX(V.player);
const bltIdleOk = V.startBolt(V.player, false);
V.player.telegraph = false; V.player.linkPlant = false; V.player.linkSheathe = 0;
const bltIdleArm = V.bladeTipX(V.player);
check("tip under bolt leftover short raise leftover live idle bolt snaps tip", bltIdleOk && V.player.boltPhase==="startup" && V.player.leftoverPlantTip===false && V.boltLeftoverPlanting(V.player)===true && Math.abs(bltIdleArm-bltIdleWu)<12, `tip0=${bltIdle0.toFixed(2)} tipArm=${bltIdleArm.toFixed(2)} wu=${bltIdleWu.toFixed(2)} latch=${V.player.leftoverPlantTip}`);

// live: tele tip still eases (regression; idle empty-K with tele on, no latch)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
V.player.sheatheT=0; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.telegraph=false; V.player.facing=1; V.openLeft=0; V.player.linkPlant=false; V.player.clashPlant=false;
V.player.leftoverPlantTip=false; V.player.stamina=100; V.player.boltHoldCut=false;
freezeRivalAI(); V.update(STEP);
const bltTele0 = V.bladeTipX(V.player);
const bltTeleR = V.destRect(V.player);
const bltTeleWu = bltTeleR.dx + 268 * (bltTeleR.dw/1186);
const bltTeleOk = V.startBolt(V.player, false);
holdRival(); V.update(STEP);
const bltTeleArm = V.bladeTipX(V.player);
const bltTeleTf = V.telegraphFade(V.player);
check("tip under bolt leftover short raise leftover live tele tip still eases", bltTeleOk && V.telegraphing(V.player) && V.player.boltPhase==="startup" && V.player.leftoverPlantTip===false && Math.abs(bltTeleArm-bltTele0)<40 && Math.abs(bltTeleArm-bltTeleWu)>20 && bltTeleTf>0 && bltTeleTf<0.3, `tip0=${bltTele0.toFixed(2)} tipArm=${bltTeleArm.toFixed(2)} wu=${bltTeleWu.toFixed(2)} tf=${bltTeleTf.toFixed(3)} latch=${V.player.leftoverPlantTip}`);

check("cache v=296 after tip under bolt leftover short raise leftover live", /game.js\?v=350/.test(htmlTxt));





// --- tip under stun / throwKd leftover short raise leftover (v295) ---
check("tip under stun leftover short raise leftover seats no tip dump", /Tip under stun leftover short raise leftover/.test(srcTxt) && /chip stun from short leftover/.test(srcTxt) && /dump bladeTip onto idle/.test(srcTxt) && /stunLeftoverPlanting sat outside tipPlantK/.test(srcTxt) && (/landHit set stunT/.test(srcTxt) || /poseBitmap still idle immediately/.test(srcTxt)));
check("tip under throwKd leftover short raise leftover seats no tip dump", /Tip under throwKd leftover short raise leftover/.test(srcTxt) && /throw knockdown from/.test(srcTxt) && /dump bladeTip onto hurt/.test(srcTxt) && /throwKdLeftoverPlanting/.test(srcTxt) && /sat outside tipPlantK/.test(srcTxt) && (/landThrow set thrownT/.test(srcTxt) || /poseBitmap still hurt immediately/.test(srcTxt)));
check("tip under stun/throwKd leftover short raise leftover is draw-only", /Draw-only \(bladeTipX \/ bladeTipY\)/.test(srcTxt) && /stunLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /throwKdLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /stunLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipY")) && /throwKdLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipY")) && /tipPlantK/.test(sliceFn(codeOnly, "bladeTipX")) && /tipPlantK/.test(sliceFn(codeOnly, "bladeTipY")) && !/tipPlantK/.test(sliceFn(codeOnly, "destRect")) && !/tipPlantK/.test(sliceFn(codeOnly, "bodyAABB")) && !/tipPlantK/.test(sliceFn(codeOnly, "poseBitmap")) && !/tipPlantK/.test(sliceFn(codeOnly, "tickGuardPose")) && !/tipPlantK/.test(sliceFn(codeOnly, "landHit")) && !/tipPlantK/.test(sliceFn(codeOnly, "landThrow")));
check("tip under stun/throwKd leftover short raise leftover uses guardRaiseK drain", /stunLeftoverPlanting\(f\) \|\| throwKdLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /stunLeftoverPlanting\(f\) \|\| throwKdLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipY")) && /tipPlantK = Math\.max\(pk, guardRaiseK\(f\)\)/.test(sliceFn(codeOnly, "bladeTipX")) && /tipPlantK = Math\.max\(pk, guardRaiseK\(f\)\)/.test(sliceFn(codeOnly, "bladeTipY")) && /Math\.max\(pk, 1 - raiseT\)/.test(sliceFn(codeOnly, "bladeTipX")) && /Math\.max\(pk, 1 - raiseT\)/.test(sliceFn(codeOnly, "bladeTipY")));
check("tip under stun/throwKd leftover short raise leftover keeps attack raiseT path", /\(throwGuardPlanting\(f\) \|\| reversalPlanting\(f\) \|\| techGuardPlanting\(f\) \|\| f\.techGuardTip \|\| \(\(slashLeftoverPlanting\(f\) \|\| golpeLeftoverPlanting\(f\) \|\| boltLeftoverPlanting\(f\)\) && f\.leftoverPlantTip && !telegraphing\(f\)\)\) && GUARD_RAISE_MS > 0/.test(sliceFn(codeOnly, "bladeTipX")) && /\(throwGuardPlanting\(f\) \|\| reversalPlanting\(f\) \|\| techGuardPlanting\(f\) \|\| f\.techGuardTip \|\| \(\(slashLeftoverPlanting\(f\) \|\| golpeLeftoverPlanting\(f\) \|\| boltLeftoverPlanting\(f\)\) && f\.leftoverPlantTip && !telegraphing\(f\)\)\) && GUARD_RAISE_MS > 0/.test(sliceFn(codeOnly, "bladeTipY")) && /boltPhase === \"startup\" \? f\.boltT : \(f\.phaseT \|\| 0\)/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under stun/throwKd leftover short raise leftover no leftoverPlantTip latch", /No leftoverPlantTip latch/.test(srcTxt) && /stunLeftoverPlanting already requires gpk>0/.test(srcTxt) && /throwKdLeftoverPlanting already requires gpk>0/.test(srcTxt) && !/leftoverPlantTip/.test(sliceFn(codeOnly, "landHit")) && !/leftoverPlantTip/.test(sliceFn(codeOnly, "landBoltHit")) && !/def\.leftoverPlantTip = \(def\.guardPoseK/.test(sliceFn(codeOnly, "landThrow")));
check("tip under stun/throwKd leftover short raise leftover tip under bolt leftover short raise unchanged", /Tip under bolt leftover short raise leftover/.test(srcTxt) && /boltLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /leftoverPlantTip/.test(sliceFn(codeOnly, "bladeTipX")) && /Math\.max\(pk, 1 - raiseT\)/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under stun leftover short raise leftover tip under slash\/golpe leftover short raise unchanged", /Tip under slash\/golpe leftover short raise leftover/.test(srcTxt) && /slashLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /golpeLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under stun leftover short raise leftover tip under tech-guard short raise unchanged", /Tip under tech-guard short raise leftover/.test(srcTxt) && /techGuardPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /techGuardTip/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under stun leftover short raise leftover tip under throw-guard short raise unchanged", /Tip under throw-guard short raise leftover/.test(srcTxt) && /throwGuardPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under stun leftover short raise leftover tip under reverse short raise unchanged", /Tip under reverse short raise leftover/.test(srcTxt) && /reversalPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under stun leftover short raise leftover tip under feintFade unchanged", /const fk = feintFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /wx \+ \(tip - wx\) \* \(1 - fk\)/.test(sliceFn(codeOnly, "bladeTipX")) && /wy \+ \(tip - wy\) \* \(1 - fk\)/.test(sliceFn(codeOnly, "bladeTipY")));
check("tip under stun leftover short raise leftover tip under telegraphFade unchanged", /telegraphing\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /telegraphFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /ex \+ \(tip - ex\) \* tf/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under stun leftover short raise leftover tipX under plant fades unchanged", /Math\.max\(clashPlantFade\(f\), linkPlantFade\(f\)\)/.test(sliceFn(codeOnly, "bladeTipX")) && (/sx \+ \(tip - sx\) \* \(1 - k\)/.test(sliceFn(codeOnly, "bladeTipX")) || /from \+ \(tip - from\) \* \(1 - k\)/.test(sliceFn(codeOnly, "bladeTipX"))));
check("tip under stun leftover short raise leftover sheet leftover-k drain unchanged", /stunLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "tickGuardPose")) && /throwKdLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "tickGuardPose")) && /f\.guardPoseK <= step/.test(sliceFn(codeOnly, "tickGuardPose")) && /stunLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "guardRaiseK")) && /throwKdLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "guardRaiseK")));
check("tip under stun leftover short raise leftover castPlantXY K path unchanged", /clashPlantFade\(f\)/.test(sliceFn(codeOnly, "castPlantXY")) && /linkPlantFade\(f\)/.test(sliceFn(codeOnly, "castPlantXY")) && !/tipPlantK/.test(sliceFn(codeOnly, "castPlantXY")) && !/stunLeftoverPlanting/.test(sliceFn(codeOnly, "castPlantXY")) && !/throwKdLeftoverPlanting/.test(sliceFn(codeOnly, "castPlantXY")));
check("tip under stun leftover short raise leftover tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("tip under stun leftover short raise leftover tipX locks hold", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt));
check("tip under stun leftover short raise leftover is not a new combat verb", /No new combat verb/.test(srcTxt) && (/Active hitbox still phase===active only/.test(srcTxt) || /active hitbox unchanged/.test(srcTxt)));
check("slash frames still 180\/140\/280 after tip under stun leftover short raise leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280);
check("golpe frames still 120\/80\/180 after tip under stun leftover short raise leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180);
check("K frames still 200\/280 after tip under stun leftover short raise leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280);
check("no 6th button after tip under stun leftover short raise leftover", !/KeyM/.test(codeOnly));
check("cache v=296 after tip under stun leftover short raise leftover", /game.js\?v=350/.test(htmlTxt));

// live: stun/throwKd leftover tipY plant (harness may lack block bitmap ready;
// tipPlantK still seats POSE.block tipY; compare plant vs dump, not guarding tipY0)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
V.player.sheatheT=0; V.player.guarding=false; V.player.guardPoseK=1; V.player.stunT=V.HITSTUN; V.player.boltPhase=""; V.player.feintT=0;
V.player.telegraph=false; V.player.facing=1; V.openLeft=0; V.player.linkPlant=false; V.player.clashPlant=false;
V.player.leftoverPlantTip=false; V.player.thrownT=0; V.player.hp=100; V.hitstopLeft=0;
freezeRivalAI();
const sttArmSlp = V.stunLeftoverPlanting(V.player);
const sttArmGk = V.guardRaiseK(V.player);
const sttPlantY = V.bladeTipY(V.player);
const sttPlantX = V.bladeTipX(V.player);
V.player.guardPoseK=0;
const sttDumpY = V.bladeTipY(V.player);
V.player.guardPoseK=1;
check("tip under stun leftover short raise leftover live stun arms", V.player.stunT>0 && sttArmSlp===true && sttArmGk>0.5, `st=${V.player.stunT} slp=${sttArmSlp} gk=${sttArmGk.toFixed(3)} plantY=${sttPlantY.toFixed(2)}`);
check("tip under stun leftover short raise leftover live stun arm tipY seats block plant", Math.abs(sttPlantY-sttDumpY)>20, `plantY=${sttPlantY.toFixed(2)} dumpY=${sttDumpY.toFixed(2)} gk=${sttArmGk.toFixed(3)}`);
check("tip under stun leftover short raise leftover live stun arm tipX sheet-edge ok", Number.isFinite(sttPlantX), `tipX=${sttPlantX.toFixed(2)}`);
let sttMid=null, sttEnd=null, sttTrackOk=true, sttPrev=sttPlantY, sttSawGk0=sttArmGk>0.8;
for (let i=0;i<40;i++){
  V.update(STEP);
  if (!(V.player.stunT>0)) break;
  const tipY=V.bladeTipY(V.player);
  const gk=V.guardRaiseK(V.player);
  const tipPlantK = V.stunLeftoverPlanting(V.player) ? Math.max(0, gk) : 0;
  if (sttMid==null && tipPlantK>0.15 && tipPlantK<0.85 && Math.abs(tipY-sttPlantY)>8) sttMid={tipY,gk,tipPlantK};
  if (Math.abs(tipY-sttPrev)>80) sttTrackOk=false;
  sttPrev=tipY;
  if (sttEnd==null && (tipPlantK<=0.02 || !V.stunLeftoverPlanting(V.player))) sttEnd={tipY,gk,tipPlantK};
}
check("tip under stun leftover short raise leftover live stun tipY eases mid-plant", sttMid && Math.abs(sttMid.tipY-sttPlantY)>8 && Math.abs(sttMid.tipY-sttDumpY)>8, `mid=${!!sttMid} tipY=${sttMid?sttMid.tipY.toFixed(2):"na"} plant=${sttPlantY.toFixed(2)} dump=${sttDumpY.toFixed(2)} k=${sttMid?sttMid.tipPlantK.toFixed(3):"na"}`);
check("tip under stun leftover short raise leftover live stun tipY tracks gpk not dump", sttTrackOk && sttEnd && Math.abs(sttEnd.tipY-sttDumpY)<12 && Math.abs(sttEnd.tipY-sttPlantY)>20 && sttSawGk0, `track=${sttTrackOk} end=${sttEnd?sttEnd.tipY.toFixed(2):"na"} plant=${sttPlantY.toFixed(2)} dump=${sttDumpY.toFixed(2)} gk0=${sttSawGk0}`);
check("tip under stun leftover short raise leftover live AABB planted", Math.abs(V.bodyAABB(V.player).y - 316.693)<2, `ay=${V.bodyAABB(V.player).y.toFixed(2)}`);

// live: short leftover stun tipY not full dump
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.guarding=false; V.player.guardPoseK=0.55; V.player.stunT=V.HITSTUN;
V.player.facing=1; V.player.leftoverPlantTip=false; V.player.thrownT=0; V.player.hp=100; V.hitstopLeft=0;
V.player.boltPhase=""; V.player.telegraph=false; V.openLeft=0;
freezeRivalAI(); V.update(STEP);
const sttShortGk = V.guardRaiseK(V.player);
const sttShortY = V.bladeTipY(V.player);
V.player.guardPoseK=0;
const sttShortDump = V.bladeTipY(V.player);
V.player.guardPoseK=0.55;
check("tip under stun leftover short raise leftover live short raise tipY not full dump", V.stunLeftoverPlanting(V.player) && sttShortGk>0.1 && Math.abs(sttShortY-sttShortDump)>4, `gk=${sttShortGk.toFixed(3)} tipY=${sttShortY.toFixed(2)} dump=${sttShortDump.toFixed(2)}`);

// live: full raise stun leftover tipY eases with gpk drain
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.guarding=false; V.player.guardPoseK=1; V.player.stunT=V.HITSTUN;
V.player.facing=1; V.player.leftoverPlantTip=false; V.player.thrownT=0; V.player.hp=100; V.hitstopLeft=0;
V.player.boltPhase=""; V.player.telegraph=false; V.openLeft=0;
freezeRivalAI();
// no update yet — measure plant at gpk=1, then drain one tick
const sttFullGk = V.guardRaiseK(V.player);
const sttFullArm = V.bladeTipY(V.player);
V.player.guardPoseK=0;
const sttFullDump = V.bladeTipY(V.player);
V.player.guardPoseK=1;
V.update(STEP);
const sttFullGk1 = V.guardRaiseK(V.player);
const sttFull1 = V.bladeTipY(V.player);
check("tip under stun leftover short raise leftover live full raise still eases", sttFullGk>0.8 && Math.abs(sttFullArm-sttFullDump)>20 && Math.abs(sttFull1-sttFullArm)>3 && sttFullGk1<sttFullGk, `tipArm=${sttFullArm.toFixed(2)} tip1=${sttFull1.toFixed(2)} dump=${sttFullDump.toFixed(2)} gk=${sttFullGk.toFixed(3)} gk1=${sttFullGk1.toFixed(3)} d1=${(sttFull1-sttFullArm).toFixed(2)}`);

// live: bolt leftover short still holds tip (regression)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.guarding=true; V.player.guardPoseK=0.22; V.player.stunT=0;
V.player.facing=1; V.player.leftoverPlantTip=false; V.player.thrownT=0; V.player.hp=100; V.player.stamina=100;
V.player.boltPhase=""; V.player.boltHoldCut=false; V.player.telegraph=false; V.openLeft=0; V.hitstopLeft=0;
freezeRivalAI(); V.update(STEP);
const sttBlt0 = V.bladeTipX(V.player);
V.player.guarding=false; V.player.boltPhase="startup"; V.player.boltT=0; V.player.guardPoseK=0.22; V.player.leftoverPlantTip=true; V.player.telegraph=false;
const sttBltArm = V.bladeTipX(V.player);
const sttBltLatch = !!V.player.leftoverPlantTip;
const sttBltBlp = V.boltLeftoverPlanting(V.player);
check("tip under stun leftover short raise leftover live bolt leftover short still holds tip", sttBltBlp && sttBltLatch && Math.abs(sttBltArm-sttBlt0)<12, `blp=${sttBltBlp} latch=${sttBltLatch} tip0=${sttBlt0.toFixed(2)} tipArm=${sttBltArm.toFixed(2)}`);

// live: standing chip stun still snaps tip (no leftover k)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.guarding=false; V.player.guardPoseK=0; V.player.stunT=0;
V.player.facing=1; V.player.leftoverPlantTip=false; V.player.thrownT=0; V.player.hp=100;
V.player.boltPhase=""; V.player.telegraph=false; V.openLeft=0;
freezeRivalAI(); V.update(STEP);
const sttIdleY0 = V.bladeTipY(V.player);
V.player.stunT = V.HITSTUN;
V.player.leftoverPlantTip = false;
const sttIdleArmY = V.bladeTipY(V.player);
check("tip under stun leftover short raise leftover live standing chip snaps tip", V.player.stunT===V.HITSTUN && V.player.leftoverPlantTip===false && V.stunLeftoverPlanting(V.player)===false && Math.abs(sttIdleArmY-sttIdleY0)<12, `tipY0=${sttIdleY0.toFixed(2)} tipArm=${sttIdleArmY.toFixed(2)} latch=${V.player.leftoverPlantTip} slp=${V.stunLeftoverPlanting(V.player)}`);

// live: throwKd leftover tipY plant vs dump
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.guarding=false; V.player.guardPoseK=1; V.player.stunT=0;
V.player.facing=1; V.player.leftoverPlantTip=false; V.player.thrownT=V.THROW_KD_MS; V.player.hp=100; V.hitstopLeft=0;
V.player.boltPhase=""; V.player.telegraph=false; V.openLeft=0;
freezeRivalAI();
const tkdArm = V.throwKdLeftoverPlanting(V.player);
const tkdGk = V.guardRaiseK(V.player);
const tkdPlantY = V.bladeTipY(V.player);
V.player.guardPoseK=0;
const tkdDumpY = V.bladeTipY(V.player);
V.player.guardPoseK=1;
check("tip under throwKd leftover short raise leftover live arms", V.player.thrownT>0 && tkdArm===true && tkdGk>0.5, `th=${V.player.thrownT} tkd=${tkdArm} gk=${tkdGk.toFixed(3)} plantY=${tkdPlantY.toFixed(2)}`);
check("tip under throwKd leftover short raise leftover live arm tipY seats block plant", Math.abs(tkdPlantY-tkdDumpY)>20, `plantY=${tkdPlantY.toFixed(2)} dumpY=${tkdDumpY.toFixed(2)} gk=${tkdGk.toFixed(3)}`);
let tkdMid=null, tkdEnd=null, tkdTrackOk=true, tkdPrev=tkdPlantY;
for (let i=0;i<40;i++){
  V.update(STEP);
  if (!(V.player.thrownT>0)) break;
  const tipY=V.bladeTipY(V.player);
  const gk=V.guardRaiseK(V.player);
  const tipPlantK = V.throwKdLeftoverPlanting(V.player) ? Math.max(0, gk) : 0;
  if (tkdMid==null && tipPlantK>0.15 && tipPlantK<0.85 && Math.abs(tipY-tkdPlantY)>8) tkdMid={tipY,gk,tipPlantK};
  if (Math.abs(tipY-tkdPrev)>80) tkdTrackOk=false;
  tkdPrev=tipY;
  if (tkdEnd==null && (tipPlantK<=0.02 || !V.throwKdLeftoverPlanting(V.player))) tkdEnd={tipY,gk,tipPlantK};
}
check("tip under throwKd leftover short raise leftover live tipY eases mid-plant", tkdMid && Math.abs(tkdMid.tipY-tkdPlantY)>8 && Math.abs(tkdMid.tipY-tkdDumpY)>8, `mid=${!!tkdMid} tipY=${tkdMid?tkdMid.tipY.toFixed(2):"na"} plant=${tkdPlantY.toFixed(2)} dump=${tkdDumpY.toFixed(2)} k=${tkdMid?tkdMid.tipPlantK.toFixed(3):"na"}`);
check("tip under throwKd leftover short raise leftover live tipY tracks gpk not dump", tkdTrackOk && tkdEnd && Math.abs(tkdEnd.tipY-tkdDumpY)<12 && Math.abs(tkdEnd.tipY-tkdPlantY)>20, `track=${tkdTrackOk} end=${tkdEnd?tkdEnd.tipY.toFixed(2):"na"} plant=${tkdPlantY.toFixed(2)} dump=${tkdDumpY.toFixed(2)}`);
check("tip under throwKd leftover short raise leftover live AABB planted", Math.abs(V.bodyAABB(V.player).y - 316.693)<2, `ay=${V.bodyAABB(V.player).y.toFixed(2)}`);

// live: throwKd without leftover k still snaps
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.guarding=false; V.player.guardPoseK=0; V.player.stunT=0;
V.player.thrownT=0; V.player.leftoverPlantTip=false; V.player.facing=1; V.player.hp=100;
freezeRivalAI(); V.update(STEP);
const tkdSnap0 = V.bladeTipY(V.player);
V.player.thrownT=V.THROW_KD_MS; V.player.guardPoseK=0;
const tkdSnapArm = V.bladeTipY(V.player);
check("tip under throwKd leftover short raise leftover live no-leftover snaps tip", V.throwKdLeftoverPlanting(V.player)===false && V.guardRaiseK(V.player)===0 && Math.abs(tkdSnapArm-tkdSnap0)<12, `tkd=${V.throwKdLeftoverPlanting(V.player)} tipY0=${tkdSnap0.toFixed(2)} tipArm=${tkdSnapArm.toFixed(2)}`);

check("cache v=296 after tip under stun/throwKd leftover short raise leftover live", /game.js\?v=350/.test(htmlTxt));


// --- tip under guard drop leftover (v296) ---
check("tip under guard drop leftover seats no tip dump", /Tip under guard drop leftover/.test(srcTxt) && /S-release \/ guard-break/.test(srcTxt) && /dump bladeTip onto idle/.test(srcTxt) && /guardDropFade sat outside tipPlantK/.test(srcTxt) && (/guarding cleared/.test(srcTxt) || /poseBitmap still idle immediately/.test(srcTxt)));
check("tip under guard drop leftover is draw-only", /Draw-only \(bladeTipX \/ bladeTipY\)/.test(srcTxt) && /guardDropFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /guardDropFade\(f\)/.test(sliceFn(codeOnly, "bladeTipY")) && /tipPlantK/.test(sliceFn(codeOnly, "bladeTipX")) && /tipPlantK/.test(sliceFn(codeOnly, "bladeTipY")) && !/tipPlantK/.test(sliceFn(codeOnly, "destRect")) && !/tipPlantK/.test(sliceFn(codeOnly, "bodyAABB")) && !/tipPlantK/.test(sliceFn(codeOnly, "poseBitmap")) && !/tipPlantK/.test(sliceFn(codeOnly, "tickGuardPose")) && !/tipPlantK/.test(sliceFn(codeOnly, "guardDropFade")) && !/tipPlantK/.test(sliceFn(codeOnly, "tripGuardBreak")));
check("tip under guard drop leftover mirrors stun/throwKd tipPlantK", /stunLeftoverPlanting\(f\) \|\| throwKdLeftoverPlanting\(f\) \|\| guardDropFade\(f\) > 0\.02/.test(sliceFn(codeOnly, "bladeTipX")) && /stunLeftoverPlanting\(f\) \|\| throwKdLeftoverPlanting\(f\) \|\| guardDropFade\(f\) > 0\.02/.test(sliceFn(codeOnly, "bladeTipY")) && /tipPlantK = Math\.max\(pk, guardRaiseK\(f\)\)/.test(sliceFn(codeOnly, "bladeTipX")) && /tipPlantK = Math\.max\(pk, guardRaiseK\(f\)\)/.test(sliceFn(codeOnly, "bladeTipY")));
check("tip under guard drop leftover keeps attack raiseT path", /\(throwGuardPlanting\(f\) \|\| reversalPlanting\(f\) \|\| techGuardPlanting\(f\) \|\| f\.techGuardTip \|\| \(\(slashLeftoverPlanting\(f\) \|\| golpeLeftoverPlanting\(f\) \|\| boltLeftoverPlanting\(f\)\) && f\.leftoverPlantTip && !telegraphing\(f\)\)\) && GUARD_RAISE_MS > 0/.test(sliceFn(codeOnly, "bladeTipX")) && /boltPhase === \"startup\" \? f\.boltT : \(f\.phaseT \|\| 0\)/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under guard drop leftover no leftoverPlantTip latch", /No leftoverPlantTip latch/.test(srcTxt) && /guardDropFade already requires gpk>0/.test(srcTxt) && !/leftoverPlantTip/.test(sliceFn(codeOnly, "tripGuardBreak")) && !/leftoverPlantTip/.test(sliceFn(codeOnly, "guardDropFade")));
check("tip under guard drop leftover tip under stun\/throwKd leftover short raise unchanged", /Tip under stun leftover short raise leftover/.test(srcTxt) && /Tip under throwKd leftover short raise leftover/.test(srcTxt) && /stunLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /throwKdLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under guard drop leftover tip under bolt leftover short raise unchanged", /Tip under bolt leftover short raise leftover/.test(srcTxt) && /boltLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /leftoverPlantTip/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under guard drop leftover tip under slash\/golpe leftover short raise unchanged", /Tip under slash\/golpe leftover short raise leftover/.test(srcTxt) && /slashLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /golpeLeftoverPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under guard drop leftover tip under tech-guard short raise unchanged", /Tip under tech-guard short raise leftover/.test(srcTxt) && /techGuardPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /techGuardTip/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under guard drop leftover tip under throw-guard short raise unchanged", /Tip under throw-guard short raise leftover/.test(srcTxt) && /throwGuardPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under guard drop leftover tip under reverse short raise unchanged", /Tip under reverse short raise leftover/.test(srcTxt) && /reversalPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under guard drop leftover tip under feintFade unchanged", /const fk = feintFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /wx \+ \(tip - wx\) \* \(1 - fk\)/.test(sliceFn(codeOnly, "bladeTipX")) && /wy \+ \(tip - wy\) \* \(1 - fk\)/.test(sliceFn(codeOnly, "bladeTipY")));
check("tip under guard drop leftover tip under telegraphFade unchanged", /telegraphing\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /telegraphFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /ex \+ \(tip - ex\) \* tf/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under guard drop leftover tipX under plant fades unchanged", /Math\.max\(clashPlantFade\(f\), linkPlantFade\(f\)\)/.test(sliceFn(codeOnly, "bladeTipX")) && (/sx \+ \(tip - sx\) \* \(1 - k\)/.test(sliceFn(codeOnly, "bladeTipX")) || /from \+ \(tip - from\) \* \(1 - k\)/.test(sliceFn(codeOnly, "bladeTipX"))));
check("tip under guard drop leftover sheet guardDropFade drain unchanged", /function guardDropFade/.test(srcTxt) && /return guardRaiseK\(f\)/.test(sliceFn(codeOnly, "guardDropFade")) && /f\.guardPoseK <= step/.test(sliceFn(codeOnly, "tickGuardPose")) && !/tipPlantK/.test(sliceFn(codeOnly, "guardDropFade")));
check("tip under guard drop leftover castPlantXY K path unchanged", /clashPlantFade\(f\)/.test(sliceFn(codeOnly, "castPlantXY")) && /linkPlantFade\(f\)/.test(sliceFn(codeOnly, "castPlantXY")) && !/tipPlantK/.test(sliceFn(codeOnly, "castPlantXY")) && !/guardDropFade/.test(sliceFn(codeOnly, "castPlantXY")));
check("tip under guard drop leftover tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("tip under guard drop leftover tipX locks hold", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt));
check("tip under guard drop leftover is not a new combat verb", /No new combat verb/.test(srcTxt) && (/Active hitbox still phase===active only/.test(srcTxt) || /active hitbox unchanged/.test(srcTxt)));
check("slash frames still 180\/140\/280 after tip under guard drop leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280);
check("golpe frames still 120\/80\/180 after tip under guard drop leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180);
check("K frames still 200\/280 after tip under guard drop leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280);
check("no 6th button after tip under guard drop leftover", !/KeyM/.test(codeOnly));
check("cache v=296 after tip under guard drop leftover", /game.js\?v=350/.test(htmlTxt));

// live: tip under guard drop (S-release leftover) tipY plant vs dump
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
V.player.sheatheT=0; V.player.guarding=false; V.player.guardPoseK=1; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.telegraph=false; V.player.facing=1; V.openLeft=0; V.player.linkPlant=false; V.player.clashPlant=false;
V.player.leftoverPlantTip=false; V.player.thrownT=0; V.player.hp=100; V.player.guardBreakT=0; V.hitstopLeft=0;
freezeRivalAI();
const gdArmGdf = V.guardDropFade(V.player);
const gdArmGk = V.guardRaiseK(V.player);
const gdPlantY = V.bladeTipY(V.player);
const gdPlantX = V.bladeTipX(V.player);
V.player.guardPoseK=0;
const gdDumpY = V.bladeTipY(V.player);
V.player.guardPoseK=1;
check("tip under guard drop leftover live drop arms", gdArmGdf>0.5 && gdArmGk>0.5 && V.player.guarding===false && V.player.phase==="idle", `gdf=${gdArmGdf.toFixed(3)} gk=${gdArmGk.toFixed(3)} plantY=${gdPlantY.toFixed(2)}`);
check("tip under guard drop leftover live drop arm tipY seats block plant", Math.abs(gdPlantY-gdDumpY)>20, `plantY=${gdPlantY.toFixed(2)} dumpY=${gdDumpY.toFixed(2)} gdf=${gdArmGdf.toFixed(3)}`);
check("tip under guard drop leftover live drop arm tipX sheet-edge ok", Number.isFinite(gdPlantX), `tipX=${gdPlantX.toFixed(2)}`);
let gdMid=null, gdEnd=null, gdTrackOk=true, gdPrev=gdPlantY, gdSawGk0=gdArmGk>0.8;
for (let i=0;i<40;i++){
  V.update(STEP);
  const tipY=V.bladeTipY(V.player);
  const gdf=V.guardDropFade(V.player);
  const tipPlantK = gdf>0.02 ? Math.max(0, gdf) : 0;
  if (gdMid==null && tipPlantK>0.15 && tipPlantK<0.85 && Math.abs(tipY-gdPlantY)>8) gdMid={tipY,gdf,tipPlantK};
  if (Math.abs(tipY-gdPrev)>80) gdTrackOk=false;
  gdPrev=tipY;
  if (gdEnd==null && tipPlantK<=0.02) gdEnd={tipY,gdf,tipPlantK};
}
check("tip under guard drop leftover live tipY eases mid-plant", gdMid && Math.abs(gdMid.tipY-gdPlantY)>8 && Math.abs(gdMid.tipY-gdDumpY)>8, `mid=${!!gdMid} tipY=${gdMid?gdMid.tipY.toFixed(2):"na"} plant=${gdPlantY.toFixed(2)} dump=${gdDumpY.toFixed(2)} k=${gdMid?gdMid.tipPlantK.toFixed(3):"na"}`);
check("tip under guard drop leftover live tipY tracks gdf not dump", gdTrackOk && gdEnd && Math.abs(gdEnd.tipY-gdDumpY)<12 && Math.abs(gdEnd.tipY-gdPlantY)>20 && gdSawGk0, `track=${gdTrackOk} end=${gdEnd?gdEnd.tipY.toFixed(2):"na"} plant=${gdPlantY.toFixed(2)} dump=${gdDumpY.toFixed(2)} gk0=${gdSawGk0}`);
check("tip under guard drop leftover live AABB planted", Math.abs(V.bodyAABB(V.player).y - 316.693)<2, `ay=${V.bodyAABB(V.player).y.toFixed(2)}`);

// live: short leftover guard drop tipY not full dump
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.guarding=false; V.player.guardPoseK=0.55; V.player.stunT=0;
V.player.facing=1; V.player.leftoverPlantTip=false; V.player.thrownT=0; V.player.hp=100; V.player.guardBreakT=0; V.hitstopLeft=0;
V.player.boltPhase=""; V.player.telegraph=false; V.player.feintT=0; V.openLeft=0;
freezeRivalAI(); V.update(STEP);
const gdShortGdf = V.guardDropFade(V.player);
const gdShortY = V.bladeTipY(V.player);
V.player.guardPoseK=0;
const gdShortDump = V.bladeTipY(V.player);
V.player.guardPoseK=0.55;
check("tip under guard drop leftover live short raise tipY not full dump", gdShortGdf>0.1 && Math.abs(gdShortY-gdShortDump)>4, `gdf=${gdShortGdf.toFixed(3)} tipY=${gdShortY.toFixed(2)} dump=${gdShortDump.toFixed(2)}`);

// live: full raise still eases (gpk from 1)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.guarding=false; V.player.guardPoseK=1; V.player.stunT=0;
V.player.facing=1; V.player.thrownT=0; V.player.hp=100; V.player.guardBreakT=0; V.hitstopLeft=0;
V.player.boltPhase=""; V.player.telegraph=false; V.player.feintT=0; V.openLeft=0;
freezeRivalAI();
const gdFullArm = V.bladeTipY(V.player);
const gdFullGk = V.guardRaiseK(V.player);
V.player.guardPoseK=0;
const gdFullDump = V.bladeTipY(V.player);
V.player.guardPoseK=1;
V.update(STEP);
const gdFull1 = V.bladeTipY(V.player);
const gdFullGk1 = V.guardRaiseK(V.player);
check("tip under guard drop leftover live full raise still eases", gdFullGk>0.8 && Math.abs(gdFullArm-gdFullDump)>20 && Math.abs(gdFull1-gdFullArm)>3 && gdFullGk1<gdFullGk, `tipArm=${gdFullArm.toFixed(2)} tip1=${gdFull1.toFixed(2)} dump=${gdFullDump.toFixed(2)} gk=${gdFullGk.toFixed(3)} gk1=${gdFullGk1.toFixed(3)} d1=${(gdFull1-gdFullArm).toFixed(2)}`);

// live: guard break leftover same tip plant
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.guarding=true; V.player.guardPoseK=1; V.player.stunT=0;
V.player.facing=1; V.player.stamina=100; V.player.hp=100; V.hitstopLeft=0; V.openLeft=0;
freezeRivalAI();
V.tripGuardBreak(V.player);
const gdBrGdf = V.guardDropFade(V.player);
const gdBrPlant = V.bladeTipY(V.player);
V.player.guardPoseK=0;
const gdBrDump = V.bladeTipY(V.player);
V.player.guardPoseK=1;
check("tip under guard drop leftover live guard break tipY seats plant", V.player.guardBreakT>0 && V.player.guarding===false && gdBrGdf>0.5 && Math.abs(gdBrPlant-gdBrDump)>20, `gb=${V.player.guardBreakT} gdf=${gdBrGdf.toFixed(3)} plant=${gdBrPlant.toFixed(2)} dump=${gdBrDump.toFixed(2)}`);

// live: no-leftover idle snap (guarding false, gpk 0)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.guarding=false; V.player.guardPoseK=0; V.player.stunT=0;
V.player.facing=1; V.player.hp=100; V.player.guardBreakT=0; V.hitstopLeft=0; V.openLeft=0;
freezeRivalAI();
const gdSnap0 = V.bladeTipY(V.player);
const gdSnapGdf = V.guardDropFade(V.player);
check("tip under guard drop leftover live no-leftover snaps tip", gdSnapGdf===0 && V.guardRaiseK(V.player)===0 && Number.isFinite(gdSnap0), `gdf=${gdSnapGdf} tipY=${gdSnap0.toFixed(2)}`);

// live: stun leftover tip still holds (regression)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.guarding=false; V.player.guardPoseK=1; V.player.stunT=V.HITSTUN;
V.player.facing=1; V.player.thrownT=0; V.player.hp=100; V.hitstopLeft=0; V.openLeft=0;
freezeRivalAI();
const gdStPlant = V.bladeTipY(V.player);
V.player.guardPoseK=0;
const gdStDump = V.bladeTipY(V.player);
V.player.guardPoseK=1;
check("tip under guard drop leftover live stun leftover tip still holds", V.stunLeftoverPlanting(V.player) && Math.abs(gdStPlant-gdStDump)>20, `slp=${V.stunLeftoverPlanting(V.player)} plant=${gdStPlant.toFixed(2)} dump=${gdStDump.toFixed(2)}`);

check("cache v=296 after tip under guard drop leftover live", /game.js\?v=350/.test(htmlTxt));



// --- v297 tech-guard settle→plant destRect breath leftover ---
check("tech-guard settle→plant destRect breath leftover seats no breath dump mid-settle tech", /Tech-guard settle→plant destRect breath leftover/.test(srcTxt) && /mid-settle Space\+S tech/.test(srcTxt) && /dump leftover settle breath/.test(srcTxt) && /fell through throw recovery/.test(srcTxt) && /restTechGuardWalk/.test(srcTxt) && /Same hole reverse\/throw-guard settle→plant/.test(srcTxt));
check("tech-guard settle→plant destRect breath leftover is destRect-only", /destRect-only \(idleBreath\)/.test(srcTxt) && /Math\.max\(settle k, techGuardPlantFade\)/.test(srcTxt) && /1 - Math\.max\(sk, tef\)/.test(sliceFn(codeOnly, "idleBreath")) && /f\.walkSettleT > 0 && f\.walkFadeHold > 0\.02/.test(sliceFn(codeOnly, "idleBreath")) && !/Math\.max\(sk, tef\)/.test(sliceFn(codeOnly, "destRect")) && !/Math\.max\(sk, tef\)/.test(sliceFn(codeOnly, "bodyAABB")) && !/Math\.max\(sk, tef\)/.test(sliceFn(codeOnly, "poseBitmap")) && !/Math\.max\(sk, tef\)/.test(sliceFn(codeOnly, "walkSettleK")) && !/Math\.max\(sk, tef\)/.test(sliceFn(codeOnly, "techGuardPlantFade")));
check("tech-guard settle→plant destRect breath leftover tech max settle\/fade", /if \(techGuardPlanting\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /const tef = techGuardPlantFade\(f\)/.test(sliceFn(codeOnly, "idleBreath")) && /1 - Math\.max\(sk, tef\)/.test(sliceFn(codeOnly, "idleBreath")) && /return amp \* \(1 - techGuardPlantFade\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")));
check("tech-guard settle→plant destRect breath leftover reverse\/throw settle→plant unchanged", /1 - Math\.max\(sk, rf\)/.test(sliceFn(codeOnly, "idleBreath")) && /1 - Math\.max\(sk, tgf\)/.test(sliceFn(codeOnly, "idleBreath")) && /if \(reversalPlanting\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /if \(throwGuardPlanting\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /Reversal\/throw-guard settle→plant destRect breath leftover/.test(srcTxt));
check("tech-guard settle→plant destRect breath leftover fresh walk-in still keep 0", /A\/D-held walk-in \/ rival closing still keep 0/.test(srcTxt) && /if \(walking\(f\) \|\| recoveryWalkOut\(f\)\) return 0;/.test(sliceFn(codeOnly, "idleBreath")) && /if \(f\.walkFadeHold > 0\.02\) return 0;/.test(sliceFn(codeOnly, "idleBreath")));
check("tech-guard settle→plant destRect breath leftover tele\/feint settle→plant unchanged", /Math\.max\(sk, tk\)/.test(sliceFn(codeOnly, "idleBreath")) && /1 - Math\.max\(sk, tk\)/.test(sliceFn(codeOnly, "idleBreath")) && /Math\.max\(sk, fk\)/.test(sliceFn(codeOnly, "idleBreath")) && /1 - Math\.max\(sk, fk\)/.test(sliceFn(codeOnly, "idleBreath")));
check("tech-guard settle→plant destRect breath leftover clash\/link\/holdCut still hard-0", /if \(clashPlanting\(f\)\) return 0;/.test(sliceFn(codeOnly, "idleBreath")) && /if \(linkPlanting\(f\)\) return 0;/.test(sliceFn(codeOnly, "idleBreath")) && /if \(holdCutPlanting\(f\)\) return 0;/.test(sliceFn(codeOnly, "idleBreath")));
check("tech-guard settle→plant destRect breath leftover tipPlantK \/ tip under guard drop unchanged", /Math\.max\(reversalPlantFade\(f\), throwGuardPlantFade\(f\), techGuardPlantFade\(f\)\)/.test(sliceFn(codeOnly, "bladeTipX")) && /guardDropFade\(f\) > 0\.02/.test(sliceFn(codeOnly, "bladeTipX")) && /tipPlantK = Math\.max\(pk, guardRaiseK\(f\)\)/.test(sliceFn(codeOnly, "bladeTipX")) && /Tip under guard drop leftover/.test(srcTxt));
check("tech-guard settle→plant destRect breath leftover throw recovery path still eases", /f\.cut === "throw" && f\.phase === "recovery"/.test(sliceFn(codeOnly, "idleBreath")) && /1 - throwPlantFade\(f\)/.test(sliceFn(codeOnly, "idleBreath")));
check("tech-guard settle→plant destRect breath leftover tip\/steel families unchanged", /tipX: 359/.test(srcTxt) && /tipY: 105/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipY: 270/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt) && /tipY: 223, steelX: 544/.test(srcTxt) && /tipY: 230, steelX: 335/.test(srcTxt) && /steelTipRide/.test(srcTxt));
check("tech-guard settle→plant destRect breath leftover tipX locks hold", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt));
check("tech-guard settle→plant destRect breath leftover is not a new combat verb", V.THROW_TECH_REC===160 && V.THROW_STARTUP===80 && V.THROW_ACTIVE===40 && V.THROW_RECOVERY===220 && V.THROW_RANGE===120 && V.GUARD_RAISE_MS===80 && V.WALK_SETTLE_MS===160 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280);
check("slash frames still 180/140/280 after tech-guard settle→plant destRect breath leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after tech-guard settle→plant destRect breath leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after tech-guard settle→plant destRect breath leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after tech-guard settle→plant destRect breath leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=297 after tech-guard settle→plant destRect breath leftover", /game.js\?v=350/.test(htmlTxt));

// live: mid-settle → guard → tech keeps settle breath (no dump through throw recovery)
// Walk far first (GAP would zero gait if rival already in throw range), then snap
// rival close for throw/tech while settle+walkFadeHold still live.
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x = 400; V.rival.x = 1100;
V.player.guarding = false; V.player.stunT = 0; V.player.phase = "idle";
V.player.boltPhase = ""; V.player.falling = false; V.player.guardPoseK = 0; V.player.hp = 100;
V.player.sheatheT = 0; V.player.linkPlant = false; V.player.telegraph = false; V.player.cutRecBreathT = 0;
V.player.feintT = 0; V.player.reversal = false; V.player.techRec = false; V.player.techGuardTip = false;
V.player.gait = 0; V.player.walkT = 0; V.player.walkSettleT = 0; V.player.walkFadeHold = 0; V.player.walkRiseT = 0;
V.player.walkSettleRot = 0; V.player.walkSettleOy = 0; V.player.walkLeanGait = 0;
V.rival.phase="idle"; V.rival.wantBlock=false; V.rival.techRec=false; V.rival.guarding=false;
V.keys.add("KeyD");
let tspReady=false;
for(let i=0;i<60;i++){
  freezeRivalAI();
  V.player.phase="idle"; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase=""; V.player.sheatheT=0; V.player.feintT=0; V.player.reversal=false; V.player.techRec=false;
  V.keys.add("KeyD");
  V.update(STEP);
  if(V.player.walkT>=150 && V.player.walkRiseT>=V.WALK_SETTLE_MS && Math.abs(V.destRect(V.player).rot)>0.03){
    tspReady=true;
    break;
  }
}
check("tech-guard settle→plant destRect breath leftover live stride ready", tspReady && V.player.gait!==0, `ready=${tspReady} gait=${V.player.gait} rot=${V.destRect(V.player).rot.toFixed(4)}`);
V.keys.clear();
let tspMid=false, tspBr0=0, tspSk0=0, tspSettle0=0, tspAabb0=null, tspWfh0=0;
for(let i=0;i<12;i++){
  freezeRivalAI();
  V.hitstopLeft=0;
  V.update(STEP);
  const sk=V.walkSettleK(V.player);
  const br=V.idleBreath(V.player);
  const amp=Math.abs(Math.sin((V.modeT/2400)*Math.PI*2)*1.6);
  if(V.player.walkSettleT>0 && sk>0.15 && sk<0.85 && Math.abs(br)>0.25 && amp>0.9){
    tspMid=true;
    tspBr0=br; tspSk0=sk; tspSettle0=V.player.walkSettleT; tspAabb0=V.bodyAABB(V.player); tspWfh0=V.player.walkFadeHold;
    break;
  }
}
check("tech-guard settle→plant destRect breath leftover live mid-settle breath ready", tspMid && Math.abs(tspBr0)>0.25 && tspSk0<0.98, `mid=${tspMid} br0=${tspBr0.toFixed(3)} sk=${tspSk0.toFixed(3)} settle=${tspSettle0} wfh=${tspWfh0.toFixed(3)}`);
// raise mid-settle then snap rival into throw range + tech
V.keys.add("KeyS");
V.update(STEP);
freezeRivalAI();
const tspBrGuard=V.idleBreath(V.player);
const tspSkGuard=V.walkSettleK(V.player);
const tspGpkGuard=V.player.guardPoseK;
check("tech-guard settle→plant destRect breath leftover live guard mid-settle", V.player.guarding===true && V.player.walkSettleT>0 && V.player.walkFadeHold>0.02 && tspSkGuard>0.05, `g=${V.player.guarding} settle=${V.player.walkSettleT|0} wfh=${V.player.walkFadeHold.toFixed(3)} sk=${tspSkGuard.toFixed(3)} br=${tspBrGuard.toFixed(3)} gpk=${tspGpkGuard.toFixed(3)}`);
V.rival.x = V.player.x + 80;
V.rival.guarding = false; V.rival.phase = "idle"; V.rival.stunT = 0; V.rival.falling = false; V.rival.boltPhase = ""; V.rival.feintT = 0; V.rival.techRec = false;
const tspThrowOk = V.startThrow(V.rival);
V.keys.clear();
const tspOk = V.tryThrowTech(V.player);
freezeRivalAI();
const tspBr1=V.idleBreath(V.player);
const tspSk1=V.walkSettleK(V.player);
const tspTef1=V.techGuardPlantFade(V.player);
const ampT=Math.sin((V.modeT/2400)*Math.PI*2)*1.6;
const tspExp1=ampT*(1-Math.max(tspSk1, tspTef1));
check("tech-guard settle→plant destRect breath leftover live tech arms mid-settle", tspThrowOk && tspOk && V.player.techRec && V.player.cut==="throw" && V.player.phase==="recovery" && V.techGuardPlanting(V.player) && tspTef1>0.05 && V.player.walkSettleT>0 && V.player.walkFadeHold>0.02 && tspSk1>0.05, `tok=${tspThrowOk} ok=${tspOk} rec=${V.player.techRec} cut=${V.player.cut} ph=${V.player.phase} plant=${V.techGuardPlanting(V.player)} settle=${V.player.walkSettleT|0} wfh=${V.player.walkFadeHold.toFixed(3)} sk=${tspSk1.toFixed(3)} tef=${tspTef1.toFixed(3)}`);
check("tech-guard settle→plant destRect breath leftover live tech does not dump breath", Math.abs(tspBr1-tspExp1)<0.08 && !(Math.abs(tspBr1)<0.01 && tspTef1<0.95 && tspSk1>0.2 && Math.abs(tspBrGuard)>0.15), `brG=${tspBrGuard.toFixed(3)} br1=${tspBr1.toFixed(3)} exp=${tspExp1.toFixed(3)} sk1=${tspSk1.toFixed(3)} tef=${tspTef1.toFixed(3)}`);
check("tech-guard settle→plant destRect breath leftover live AABB planted", tspAabb0 && Math.abs(V.bodyAABB(V.player).y-tspAabb0.y)<0.05, `dy=${tspAabb0?(V.bodyAABB(V.player).y-tspAabb0.y).toFixed(2):"na"}`);

// idle tech-from-guard still eases with plant fade
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=500; V.rival.x=580;
Object.assign(V.player,{phase:"idle",phaseT:0,boltPhase:"",stunT:0,guarding:false,falling:false,feintT:0,sheatheT:0,cutRecBreathT:0,guardPoseK:0,walkSettleT:0,walkFadeHold:0,walkRiseT:0,gait:0,walkT:0,walkSettleRot:0,walkSettleOy:0,reversal:false,telegraph:false,linkPlant:false,techRec:false,techGuardTip:false,hp:100,stamina:100});
V.keys.add("KeyS");
for(let i=0;i<20;i++){
  freezeRivalAI();
  V.player.stamina=100; V.player.phase="idle"; V.player.boltPhase=""; V.player.stunT=0; V.player.falling=false;
  V.keys.add("KeyS");
  V.update(STEP);
  if(V.player.guarding && V.player.guardPoseK>0.95) break;
}
let tspIdleAmp=false;
for(let i=0;i<120;i++){
  freezeRivalAI();
  V.player.stamina=100; V.player.phase="idle"; V.player.boltPhase=""; V.player.stunT=0; V.player.falling=false; V.player.reversal=false; V.player.techRec=false;
  V.keys.add("KeyS");
  V.update(STEP);
  if(V.player.guarding && V.player.guardPoseK>0.95 && Math.abs(Math.sin((V.modeT/2400)*Math.PI*2)*1.6)>0.9){
    tspIdleAmp=true; break;
  }
}
const tspIdleBr0=V.idleBreath(V.player);
const tspIdleK0=V.player.guardPoseK;
V.rival.guarding=false; V.rival.phase="idle"; V.rival.stunT=0; V.rival.falling=false; V.rival.boltPhase=""; V.rival.feintT=0; V.rival.techRec=false;
const tspIdleThrowOk=V.startThrow(V.rival);
V.keys.clear();
const tspIdleOk=V.tryThrowTech(V.player);
freezeRivalAI();
const tspIdleBr1=V.idleBreath(V.player);
const tspIdleTef=V.techGuardPlantFade(V.player);
const tspIdleExp=Math.sin((V.modeT/2400)*Math.PI*2)*1.6*(1-tspIdleTef);
check("tech-guard settle→plant destRect breath leftover live idle tech still eases", tspIdleAmp && tspIdleThrowOk && tspIdleOk && V.player.techRec && V.player.cut==="throw" && V.player.phase==="recovery" && V.techGuardPlanting(V.player) && V.player.walkSettleT===0 && tspIdleTef>0.8 && Math.abs(tspIdleBr1-tspIdleExp)<0.08, `ready=${tspIdleAmp} tok=${tspIdleThrowOk} ok=${tspIdleOk} rec=${V.player.techRec} tef=${tspIdleTef.toFixed(3)} br0=${tspIdleBr0.toFixed(3)} br1=${tspIdleBr1.toFixed(3)} exp=${tspIdleExp.toFixed(3)} settle=${V.player.walkSettleT} k0=${tspIdleK0.toFixed(3)}`);

// reverse settle→plant still max(sk, rf) (regression)
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=1100;
Object.assign(V.player,{phase:"idle",phaseT:0,boltPhase:"",stunT:0,guarding:false,falling:false,feintT:0,sheatheT:0,cutRecBreathT:0,guardPoseK:0,walkSettleT:0,walkFadeHold:0,walkRiseT:0,gait:0,walkT:0,walkSettleRot:0,walkSettleOy:0,reversal:false,telegraph:false,linkPlant:false,techRec:false,hp:100,stamina:100});
V.keys.add("KeyD");
let tspRevReady=false;
for(let i=0;i<60;i++){
  freezeRivalAI();
  V.player.phase="idle"; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase=""; V.player.sheatheT=0; V.player.feintT=0; V.player.reversal=false;
  V.keys.add("KeyD");
  V.update(STEP);
  if(V.player.walkT>=150 && V.player.walkRiseT>=V.WALK_SETTLE_MS && Math.abs(V.destRect(V.player).rot)>0.03){ tspRevReady=true; break; }
}
V.keys.clear();
let tspRevMid=false, tspRevBr0=0;
for(let i=0;i<12;i++){
  freezeRivalAI(); V.hitstopLeft=0; V.update(STEP);
  const sk=V.walkSettleK(V.player); const br=V.idleBreath(V.player);
  const amp=Math.abs(Math.sin((V.modeT/2400)*Math.PI*2)*1.6);
  if(V.player.walkSettleT>0 && sk>0.15 && sk<0.85 && Math.abs(br)>0.25 && amp>0.9){ tspRevMid=true; tspRevBr0=br; break; }
}
V.keys.add("KeyS"); V.update(STEP); freezeRivalAI();
const tspRevBrG=V.idleBreath(V.player);
V.golpeEdge=true; V.keys.add("KeyL"); V.update(STEP); V.keys.delete("KeyL"); freezeRivalAI();
const tspRevBr1=V.idleBreath(V.player);
const tspRevSk1=V.walkSettleK(V.player);
const tspRevRf1=V.reversalPlantFade(V.player);
const tspRevExp=Math.sin((V.modeT/2400)*Math.PI*2)*1.6*(1-Math.max(tspRevSk1, tspRevRf1));
check("tech-guard settle→plant destRect breath leftover live reverse still max settle\/fade", tspRevReady && tspRevMid && V.player.reversal===true && Math.abs(tspRevBr1-tspRevExp)<0.08 && !(Math.abs(tspRevBr1)<0.01 && tspRevRf1<0.95 && tspRevSk1>0.2 && Math.abs(tspRevBrG)>0.15), `ready=${tspRevReady} mid=${tspRevMid} rev=${V.player.reversal} brG=${tspRevBrG.toFixed(3)} br1=${tspRevBr1.toFixed(3)} exp=${tspRevExp.toFixed(3)} rf=${tspRevRf1.toFixed(3)}`);

check("tech-guard settle→plant destRect breath leftover rival idleBreath shared", /if \(techGuardPlanting\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /Math\.max\(sk, tef\)/.test(sliceFn(codeOnly, "idleBreath")) && !/f\.kind === "you"/.test(sliceFn(codeOnly, "idleBreath").slice(sliceFn(codeOnly, "idleBreath").indexOf("if (techGuardPlanting(f))"), sliceFn(codeOnly, "idleBreath").indexOf("if (techGuardPlanting(f))")+480)));
check("cache v=297 after tech-guard settle→plant destRect breath leftover live", /game.js\?v=350/.test(htmlTxt));



// --- v298 KO caida audio sync leftover: caida sting on land grit / punch (juice-only) ---
check("KO caida audio sync leftover seats no early caida", /KO caida audio sync leftover/.test(srcTxt) && /sfx_ko_caida used to fire the same tick/.test(srcTxt) && /beginFall armed crumple/.test(srcTxt) && /FALL_MS\*0\.72 stayed silent/.test(srcTxt) && /Arm caida on koLanded/.test(srcTxt) && /land dust \/ bumpShake/.test(srcTxt));
check("KO caida audio sync leftover is juice-only", /Juice-only \(playSfx\)/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && /FALL_MS \/ crumpleFade \/ KO flash/.test(srcTxt) && !/lastKoSfx/.test(sliceFn(codeOnly, "destRect")) && !/lastKoSfx/.test(sliceFn(codeOnly, "bodyAABB")) && !/lastKoSfx/.test(sliceFn(codeOnly, "slashPose")) && !/lastKoSfx/.test(sliceFn(codeOnly, "idleBreath")) && !/playSfx\(SFX\.ko\)/.test(sliceFn(codeOnly, "landHit")));
check("KO caida audio sync leftover arms on land not beginFall", /playSfx\(SFX\.ko\)/.test(srcTxt) && !/playSfx\(SFX\.ko\)/.test(sliceFn(codeOnly, "beginFall")) && /koLanded = true/.test(srcTxt) && /playSfx\(SFX\.ko\)/.test(srcTxt.slice(srcTxt.indexOf("koLanded = true"), srcTxt.indexOf("koLanded = true")+280)) && /lastKoSfx = "caida"/.test(srcTxt) && /spawnPlantDust\(koTarget, 2\.4\)/.test(srcTxt) && /bumpShake\(5/.test(srcTxt));
check("KO caida audio sync leftover beginFall still soft grit", /spawnPlantDust\(koTarget, 1\.6\)/.test(sliceFn(codeOnly, "beginFall")) && /koLanded = false/.test(sliceFn(codeOnly, "beginFall")) && /lastKoSfx = ""/.test(sliceFn(codeOnly, "beginFall")));
check("KO caida audio sync leftover land still FALL_MS\*0\.72", /fallT >= FALL_MS \* 0\.72/.test(srcTxt) && V.FALL_MS===720);
check("KO caida audio sync leftover tipX locks hold", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt));
check("KO caida audio sync leftover is not a new combat verb", V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.FALL_MS===720 && V.HIT_FLASH_MS===120 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("slash frames still 180/140/280 after KO caida audio sync leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after KO caida audio sync leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after KO caida audio sync leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after KO caida audio sync leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=298 after KO caida audio sync leftover", /game.js\?v=350/.test(htmlTxt));

bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.player.guarding=false; V.player.stunT=0; V.player.falling=false; V.player.hp=100;
V.rival.guarding=false; V.rival.wantBlock=false; V.rival.guardChoice=-1; V.rival.hp=10; V.rival.falling=false;
V.player.x = V.rival.x - 160; V.keepApart();
const koCaidaAabb0 = V.bodyAABB(V.rival);
const koCaidaDest0 = V.destRect(V.rival);
V.attackEdge = true;
let koCaidaHit=false;
for(let i=0;i<80;i++){
  freezeRivalAI();
  V.update(STEP);
  if(V.rival.hp<=0 || V.hitstopLeft>0){ koCaidaHit=true; break; }
}
check("KO caida audio sync leftover live kill arms", koCaidaHit && V.rival.hp<=0, `hit=${koCaidaHit} hp=${V.rival.hp} hs=${V.hitstopLeft}`);
for(let i=0;i<40;i++){ freezeRivalAI(); V.update(STEP); if(V.mode==="falling") break; }
check("KO caida audio sync leftover live beginFall quiet", V.mode==="falling" && V.rival.falling && V.koLanded===false && V.lastKoSfx==="" && V.rival.fallT<V.FALL_MS*0.72, `mode=${V.mode} land=${V.koLanded} sfx=${V.lastKoSfx} fallT=${V.rival.fallT}`);
check("KO caida audio sync leftover live AABB planted", Math.abs(V.bodyAABB(V.rival).y-koCaidaAabb0.y)<0.05 && Math.abs(V.bodyAABB(V.rival).h-koCaidaAabb0.h)<0.05, `dy=${(V.bodyAABB(V.rival).y-koCaidaAabb0.y).toFixed(2)}`);
check("KO caida audio sync leftover live destRect planted", Math.abs(V.destRect(V.rival).dy-koCaidaDest0.dy)<12, `ddy=${(V.destRect(V.rival).dy-koCaidaDest0.dy).toFixed(2)}`);
let koCaidaLanded=false;
for(let i=0;i<Math.ceil(V.FALL_MS/STEP)+4;i++){
  freezeRivalAI();
  V.update(STEP);
  if(V.koLanded){ koCaidaLanded=true; break; }
  if(V.mode==="over") break;
}
check("KO caida audio sync leftover live land arms caida", koCaidaLanded && V.koLanded===true && V.lastKoSfx==="caida" && V.rival.fallT>=V.FALL_MS*0.72-STEP, `land=${koCaidaLanded} sfx=${V.lastKoSfx} fallT=${V.rival&&V.rival.fallT} mode=${V.mode}`);
check("KO caida audio sync leftover live land dust\/punch path", /spawnPlantDust\(koTarget, 2\.4\)/.test(srcTxt) && /bumpShake\(5/.test(srcTxt) && V.FALL_MS===720);
check("cache v=298 after KO caida audio sync leftover live", /game.js\?v=350/.test(htmlTxt));

// --- v299 Stick Esc→JUGAR leftover: resetRound re-feeds stick A/D after keys.clear ---
check("stick Esc→JUGAR leftover seats no stickHeld without KeyA after Esc→JUGAR", /Stick Esc→JUGAR leftover/.test(srcTxt) && /goTitleFromPlay keys\.clear used to leave/.test(srcTxt) && /stickHeldA\/D true while KeyA\/D were gone/.test(srcTxt) && /JUGAR with the thumb still on/.test(srcTxt) && /did not walk until release\+re-nudge/.test(srcTxt) && /stickApply only edges/.test(srcTxt));
check("stick Esc→JUGAR leftover is pad juice", /Pad juice: resetRound re-feeds A\/D from stickHeld while stickPtr is live/.test(srcTxt) && /Title still drops walk/.test(srcTxt) && /actionHeld requires keys/.test(srcTxt) && /Harness[\s\S]{0,40}stickApply without stickPtr does not re-arm/.test(srcTxt) && /stickRelease \/ deadzone/.test(srcTxt) && /portrait follow unchanged/.test(srcTxt) && /Pad zones keep padHoldN/.test(srcTxt));
check("stick Esc→JUGAR leftover resetRound refeeds", /stickPtr != null/.test(sliceFn(codeOnly, "resetRound")) && /stickHeldA && !keys\.has\("KeyA"\)/.test(sliceFn(codeOnly, "resetRound")) && /stickHeldD && !keys\.has\("KeyD"\)/.test(sliceFn(codeOnly, "resetRound")) && /feedKey\("KeyA", true\)/.test(sliceFn(codeOnly, "resetRound")) && /feedKey\("KeyD", true\)/.test(sliceFn(codeOnly, "resetRound")));
check("stick Esc→JUGAR leftover title still drops walk", /keys\.clear\(\)/.test(sliceFn(codeOnly, "goTitleFromPlay")) && /stickHeldA && keys\.has\("KeyA"\)/.test(sliceFn(codeOnly, "actionHeld")) && /stickHeldD && keys\.has\("KeyD"\)/.test(sliceFn(codeOnly, "actionHeld")) && /Esc keys\.clear\(\) drops walk/.test(srcTxt));
check("stick Esc→JUGAR leftover stickApply edge unchanged", /function stickApply/.test(srcTxt) && /if \(!stickHeldA\)/.test(sliceFn(codeOnly, "stickApply")) && /if \(!stickHeldD\)/.test(sliceFn(codeOnly, "stickApply")) && /STICK_DEAD = 0\.28/.test(srcTxt));
check("stick Esc→JUGAR leftover tipPlantK / KO caida unchanged", /Tip under guard drop leftover/.test(srcTxt) && /KO caida audio sync leftover/.test(srcTxt) && /Tech-guard settle→plant destRect breath leftover/.test(srcTxt) && /guardDropFade\(f\) > 0\.02/.test(sliceFn(codeOnly, "bladeTipX")));
check("stick Esc→JUGAR leftover tipX locks hold", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt));
check("stick Esc→JUGAR leftover is not a new combat verb", V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80 && V.GUARD_COMMIT_MS===140 && V.WALK_SETTLE_MS===160 && V.GUARD_RAISE_MS===80 && /STICK_DEAD = 0\.28/.test(srcTxt));
check("slash frames still 180/140/280 after stick Esc→JUGAR leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after stick Esc→JUGAR leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after stick Esc→JUGAR leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after stick Esc→JUGAR leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=299 after stick Esc→JUGAR leftover", /game.js\?v=350/.test(htmlTxt));

// live: Esc→title drops walk; JUGAR refeeds stick A/D without release+re-nudge
bootPlay(); wait(50);
V.openLeft = 0; V.hitstopLeft = 0;
freezeYouAI(); freezeRivalAI();
V.stickPtr = 7; // live thumb
V.stickApply(-0.9);
check("stick Esc→JUGAR leftover live stick arms KeyA", V.keys.has("KeyA") && !V.keys.has("KeyD") && V.actionHeld("left"), `a=${V.keys.has("KeyA")} held=${V.actionHeld("left")}`);
const stickAabb0 = V.bodyAABB(V.player);
V.goTitleFromPlay();
check("stick Esc→JUGAR leftover live Esc drops walk", V.mode==="title" && !V.keys.has("KeyA") && !V.actionHeld("left"), `mode=${V.mode} a=${V.keys.has("KeyA")} held=${V.actionHeld("left")}`);
// thumb still held (stickPtr + stickHeld) — no stickRelease
V.resetRound();
check("stick Esc→JUGAR leftover live JUGAR refeeds KeyA", V.mode==="play" && V.keys.has("KeyA") && !V.keys.has("KeyD") && V.actionHeld("left"), `mode=${V.mode} a=${V.keys.has("KeyA")} d=${V.keys.has("KeyD")} held=${V.actionHeld("left")}`);
check("stick Esc→JUGAR leftover live AABB planted", Math.abs(V.bodyAABB(V.player).y-stickAabb0.y)<0.05, `dy=${(V.bodyAABB(V.player).y-stickAabb0.y).toFixed(2)}`);
// deadzone / release still clears
V.stickApply(0);
check("stick Esc→JUGAR leftover live deadzone still releases", !V.keys.has("KeyA") && !V.keys.has("KeyD") && !V.actionHeld("left"));
V.stickApply(0.9);
check("stick Esc→JUGAR leftover live right still KeyD", V.keys.has("KeyD") && !V.keys.has("KeyA") && V.actionHeld("right"));
V.stickRelease();
check("stick Esc→JUGAR leftover live release clears", !V.keys.has("KeyA") && !V.keys.has("KeyD") && (V.stickPtr==null), `a=${V.keys.has("KeyA")} ptr=${V.stickPtr}`);
// freezeYouAI keys.clear still drops walk; harness stickApply without stickPtr does not re-arm on resetRound
V.stickApply(-0.9);
freezeYouAI();
check("stick Esc→JUGAR leftover live freezeYouAI still drops", !V.keys.has("KeyA") && !V.actionHeld("left"), `a=${V.keys.has("KeyA")} held=${V.actionHeld("left")}`);
V.resetRound();
check("stick Esc→JUGAR leftover live harness stickApply no rearm", !V.keys.has("KeyA") && !V.actionHeld("left"), `a=${V.keys.has("KeyA")} held=${V.actionHeld("left")}`);
V.stickApply(0);
check("cache v=299 after stick Esc→JUGAR leftover live", /game.js\?v=350/.test(htmlTxt));



// --- v300 Portrait pad horizontal pack leftover: mins+cluster fit ~390px right budget ---
check("portrait pad horizontal pack leftover seats no overflow beside big stick", /Portrait pad horizontal pack leftover/.test(srcTxt) && /bigger mins \(guarda 64/.test(srcTxt) && /cut-cluster 130\/42%/.test(srcTxt) && /still overflowed the right budget/.test(srcTxt) && /120px stick on ~390px/.test(srcTxt) && /buttons spilled past the strip/.test(srcTxt) && /Same hole portrait pad leftover already closed/.test(srcTxt));
check("portrait pad horizontal pack leftover is draw-only css", /Draw-only \(style\.css\)/.test(srcTxt) && /Portrait pad horizontal pack leftover/.test(cssTxt) && /body\.touch-on \.pad-right/.test(cssTxt) && /body\.touch-on \.zone-guard/.test(cssTxt) && /body\.touch-on \.cut-cluster/.test(cssTxt));
check("portrait pad horizontal pack leftover packed mins+gaps", /body\.touch-on \.zone-guard\s*\{[^}]*min-width:\s*56px/.test(cssTxt) && /body\.touch-on \.zone-slash\s*\{[^}]*min-width:\s*56px/.test(cssTxt) && /body\.touch-on \.zone-golpe\s*\{[^}]*min-width:\s*50px/.test(cssTxt) && /body\.touch-on \.zone-dart\s*\{[^}]*min-width:\s*56px/.test(cssTxt) && /body\.touch-on \.cut-cluster\s*\{[^}]*min-width:\s*112px/.test(cssTxt) && /body\.touch-on \.cut-cluster\s*\{[^}]*width:\s*40%/.test(cssTxt) && /body\.touch-on \.cut-cluster\s*\{[^}]*gap:\s*4px/.test(cssTxt) && /body\.touch-on \.pad-right\s*\{[^}]*gap:\s*4px/.test(cssTxt));
check("portrait pad horizontal pack leftover pad chrome packed", /body\.touch-on #pad\s*\{[^}]*padding:\s*6px 6px/.test(cssTxt) && /body\.touch-on #pad\s*\{[^}]*gap:\s*6px/.test(cssTxt));
check("portrait pad horizontal pack leftover ~390 right budget fits", (()=>{ const W=390, padX=12, stick=120, leftPad=4, sideGap=6, rightGap=4*2, g=56, c=112, d=56; const rb=W-padX-stick-leftPad-sideGap; const need=g+c+d+rightGap; return need<=rb && rb-need>=8; })(), "pack must fit ~390 with spare");
check("portrait pad horizontal pack leftover landscape mins unchanged", /\.zone-guard\s*\{[^}]*min-width:\s*52px/.test(cssTxt) && /\.zone-slash\s*\{[^}]*min-width:\s*52px/.test(cssTxt) && /\.zone-golpe\s*\{[^}]*min-width:\s*48px/.test(cssTxt) && /\.zone-dart\s*\{[^}]*min-width:\s*56px/.test(cssTxt) && /\.cut-cluster\s*\{[^}]*min-width:\s*110px/.test(cssTxt) && /\.cut-cluster\s*\{[^}]*width:\s*38%/.test(cssTxt));
check("portrait pad horizontal pack leftover stick size unchanged", /Portrait stick leftover/.test(cssTxt) && /body\.touch-on \.stick/.test(cssTxt) && /--stick-size:\s*min\(120px/.test(cssTxt) && /38vw/.test(cssTxt) && /min\(84px, calc\(var\(--pad-h\) - 14px\), 26vw\)/.test(cssTxt));
check("portrait pad horizontal pack leftover still letterbox no canvas resize", /@media \(orientation: portrait\)/.test(cssTxt) && /body\.touch-on #pad/.test(cssTxt) && /bottom:\s*0/.test(cssTxt) && /position:\s*absolute/.test(cssTxt) && !/@media \(orientation: portrait\)[\s\S]*body\.touch-on #game/.test(cssTxt));
check("giro hidden in portrait after portrait pad horizontal pack leftover", /body\.touch-on #giro\s*\{[^}]*display:\s*none/.test(cssTxt) && !/body\.touch-on #giro\s*\{[^}]*display:\s*flex/.test(cssTxt));
check("portrait pad horizontal pack leftover tipPlantK / stick Esc unchanged", /Tip under guard drop leftover/.test(srcTxt) && /Stick Esc→JUGAR leftover/.test(srcTxt) && /KO caida audio sync leftover/.test(srcTxt) && /stickPtr != null/.test(sliceFn(codeOnly, "resetRound")));
check("portrait pad horizontal pack leftover tipX locks hold", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt));
check("portrait pad horizontal pack leftover is not a new combat verb", V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80 && V.GUARD_COMMIT_MS===140 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("slash frames still 180/140/280 after portrait pad horizontal pack leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after portrait pad horizontal pack leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after portrait pad horizontal pack leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after portrait pad horizontal pack leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=300 after portrait pad horizontal pack leftover", /game.js\?v=350/.test(htmlTxt));

// live: pad path + AABB still planted (draw-only css; stick wiring unchanged)
bootPlay(); wait(50);
V.openLeft = 0; V.hitstopLeft = 0;
freezeYouAI(); freezeRivalAI();
const packAabb0 = V.bodyAABB(V.player);
const packDest0 = V.destRect(V.player);
V.stickPtr = 7;
V.stickApply(-0.9);
check("portrait pad horizontal pack leftover live stick still KeyA", V.keys.has("KeyA") && !V.keys.has("KeyD") && V.actionHeld("left"), `a=${V.keys.has("KeyA")} d=${V.keys.has("KeyD")}`);
V.stickApply(0.9);
check("portrait pad horizontal pack leftover live stick still KeyD", V.keys.has("KeyD") && !V.keys.has("KeyA") && V.actionHeld("right"), `a=${V.keys.has("KeyA")} d=${V.keys.has("KeyD")}`);
V.stickApply(0);
check("portrait pad horizontal pack leftover live stick deadzone still releases", !V.keys.has("KeyA") && !V.keys.has("KeyD") && !V.actionHeld("left"));
V.stickRelease();
check("portrait pad horizontal pack leftover live AABB planted", Math.abs(V.bodyAABB(V.player).y-packAabb0.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-packAabb0.h)<0.05, `dy=${(V.bodyAABB(V.player).y-packAabb0.y).toFixed(2)}`);
check("portrait pad horizontal pack leftover live destRect planted", Math.abs(V.destRect(V.player).dy-packDest0.dy)<2.2, `ddy=${(V.destRect(V.player).dy-packDest0.dy).toFixed(2)}`);
check("portrait pad horizontal pack leftover zone hierarchy unchanged", /\.zone-golpe[\s\S]*?height:\s*80%/.test(cssTxt) && /\.zone-slash[\s\S]*?height:\s*92%/.test(cssTxt) && /\.zone-guard[\s\S]*?height:\s*88%/.test(cssTxt) && /\.zone-dart[\s\S]*?height:\s*88%/.test(cssTxt));
check("cache v=300 after portrait pad horizontal pack leftover live", /game.js\?v=350/.test(htmlTxt));



// --- v301 Hit flash vs knock resume leftover: white dies with punchCover ---
check("hit flash vs knock resume leftover seats", /Hit flash vs knock resume leftover/.test(srcTxt) && /leftover white used to stay at full/.test(srcTxt) && /HIT_FLASH_MS 120 the tick freeze ended/.test(srcTxt) && /punchCover held full through the slam/.test(srcTxt) && /die with cover/.test(srcTxt));
check("hit flash vs knock resume leftover is draw-only", /Draw-only \(hurtFlashK\)/.test(srcTxt) && /Hit flash vs knock resume leftover/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && !/Hit flash vs knock resume leftover/.test(sliceFn(codeOnly, "destRect")) && !/Hit flash vs knock resume leftover/.test(sliceFn(codeOnly, "bodyAABB")) && !/Hit flash vs knock resume leftover/.test(sliceFn(codeOnly, "landHit")) && !/Hit flash vs knock resume leftover/.test(sliceFn(codeOnly, "bumpShake")));
check("hit flash vs knock resume leftover hurtFlashK cover envelope", /Hold peak while cover is full/.test(srcTxt) && /same smoothstep as punchCover/.test(srcTxt) && /k > 0\.25/.test(sliceFn(codeOnly, "hurtFlashK")) && /u \* u \* \(3 - 2 \* u\)/.test(sliceFn(codeOnly, "hurtFlashK")) && /shake > 0 && shakeDur > 0/.test(sliceFn(codeOnly, "hurtFlashK")));
check("hit flash vs knock resume leftover clock holds through live punch", /Hold the armed clock/.test(srcTxt) && /through the live punch/.test(srcTxt) && /clear when cover dies/.test(srcTxt) && /shake > 0 && shakeDur > 0/.test(sliceFn(codeOnly, "update")));
check("hit flash vs knock resume leftover HIT_FLASH_MS arm unchanged", V.HIT_FLASH_MS===120 && V.HITSTOP_HIT===140 && V.PUNCH_PX===12);
check("hit flash vs knock resume leftover tipX locks hold", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt));
check("hit flash vs knock resume leftover is not a new combat verb", V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.HIT_FLASH_MS===120 && V.HIT_FLASH_MS<V.HITSTOP_HIT && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("slash frames still 180/140/280 after hit flash vs knock resume leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after hit flash vs knock resume leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after hit flash vs knock resume leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after hit flash vs knock resume leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=301 after hit flash vs knock resume leftover", /game.js\?v=350/.test(htmlTxt));

// live: chip hit — freeze holds flash; resume holds peak with cover; dies with cover
toMeasure();
freezeYouAI(); freezeRivalAI();
V.openLeft = 0; V.hitstopLeft = 0;
V.rival.guarding = false;
V.rival.wantBlock = false;
V.rival.phase = "idle";
V.player.phase = "idle";
V.player.phaseT = 0;
V.startAttack(V.player, "slash");
let hfHit=false, hfFreezeFlash=0, hfFreezeCover=0, hfFreezeK=0;
for(let i=0;i<120;i++){
  V.update(STEP);
  if(V.hitstopLeft>0 && V.rival.hp<100 && V.hitFlashT>0){
    hfHit=true; hfFreezeFlash=V.hitFlashT; hfFreezeCover=V.punchCover(); hfFreezeK=V.hurtFlashK(V.rival);
    break;
  }
}
check("hit flash vs knock resume leftover live freeze still full flash", hfHit && hfFreezeFlash===V.HIT_FLASH_MS && hfFreezeCover===V.PUNCH_PX && hfFreezeK>0.9, `hit=${hfHit} flash=${hfFreezeFlash} cover=${hfFreezeCover} k=${hfFreezeK}`);
for(let i=0;i<40;i++){ if(V.hitstopLeft<=0) break; V.update(STEP); }
for(let i=0;i<3;i++){ if(V.hitstopLeft<=0 && V.shake<V.HITSTOP_HIT) break; V.update(STEP); }
const hfResumeFlash=V.hitFlashT, hfResumeCover=V.punchCover(), hfResumeK=V.hurtFlashK(V.rival), hfResumeShake=V.shake;
check("hit flash vs knock resume leftover live resume still peak with cover", V.hitstopLeft<=0 && hfResumeFlash===V.HIT_FLASH_MS && hfResumeCover===V.PUNCH_PX && hfResumeK>0.9 && hfResumeShake>0, `hs=${V.hitstopLeft} flash=${hfResumeFlash} cover=${hfResumeCover} k=${hfResumeK} shake=${hfResumeShake}`);
let hfEaseK=-1, hfEaseCover=-1, hfEaseFlash=-1;
for(let i=0;i<30;i++){
  V.update(STEP);
  if(V.shake>0 && V.shake/V.shakeDur<=0.25){
    hfEaseK=V.hurtFlashK(V.rival); hfEaseCover=V.punchCover(); hfEaseFlash=V.hitFlashT;
    break;
  }
}
check("hit flash vs knock resume leftover live ease with cover last quarter", hfEaseFlash===V.HIT_FLASH_MS && hfEaseCover<V.PUNCH_PX && hfEaseCover>0 && hfEaseK>0 && hfEaseK<1, `flash=${hfEaseFlash} cover=${hfEaseCover} k=${hfEaseK}`);
for(let i=0;i<40;i++){ if(V.shake<=0) break; V.update(STEP); }
check("hit flash vs knock resume leftover live dies with cover", V.shake===0 && V.punchCover()===0 && V.hitFlashT===0 && V.hurtFlashK(V.rival)===0, `shake=${V.shake} cover=${V.punchCover()} flash=${V.hitFlashT} k=${V.hurtFlashK(V.rival)}`);
check("cache v=301 after hit flash vs knock resume leftover live", /game.js\?v=350/.test(htmlTxt));


// --- v302 Portrait pad 2x2 clarity leftover: equal thumb cells on ~390px ---
check("portrait pad 2x2 clarity leftover seats no lost golpe+dardo", /Portrait pad 2x2 clarity leftover/.test(srcTxt) && /lost golpe\+dardo/.test(srcTxt) && /cut-cluster display:contents/.test(srcTxt) && /TL guarda/.test(srcTxt) && /TR tajo/.test(srcTxt) && /BL golpe/.test(srcTxt) && /BR dardo/.test(srcTxt) && /hold-guarda\+tap-tajo/.test(srcTxt) && /hold-guarda\+tap-golpe/.test(srcTxt));
check("portrait pad 2x2 clarity leftover is draw-only css", /Draw-only \(style\.css\)/.test(srcTxt) && /Portrait pad 2x2 clarity leftover/.test(cssTxt) && /display:\s*contents/.test(cssTxt) && /grid-template-columns:\s*1fr 1fr/.test(cssTxt) && /grid-template-rows:\s*1fr 1fr/.test(cssTxt));
check("portrait pad 2x2 clarity leftover equal mins ≥56", /body\.touch-on \.zone-guard,\s*body\.touch-on \.zone-slash,\s*body\.touch-on \.zone-golpe,\s*body\.touch-on \.zone-dart\s*\{[^}]*min-width:\s*56px/.test(cssTxt) && /body\.touch-on \.zone-guard,\s*body\.touch-on \.zone-slash,\s*body\.touch-on \.zone-golpe,\s*body\.touch-on \.zone-dart\s*\{[^}]*min-height:\s*56px/.test(cssTxt));
check("portrait pad 2x2 clarity leftover cut-cluster contents", /body\.touch-on \.cut-cluster\s*\{[^}]*display:\s*contents/.test(cssTxt));
check("portrait pad 2x2 clarity leftover pad-right grid", /body\.touch-on \.pad-right\s*\{[^}]*display:\s*grid/.test(cssTxt) && /body\.touch-on \.pad-right\s*\{[^}]*gap:\s*4px/.test(cssTxt));
check("portrait pad 2x2 clarity leftover taller pad-h", /--pad-h:\s*calc\(clamp\(132px, 24vh, 168px\)/.test(cssTxt) && /--pad-h:\s*calc\(clamp\(132px, 24dvh, 168px\)/.test(cssTxt));
check("portrait pad 2x2 clarity leftover ~390 right budget fits", (()=>{ const W=390, padX=12, stick=120, leftPad=4, sideGap=6, gap=4, cell=56; const rb=W-padX-stick-leftPad-sideGap; const need=cell*2+gap; const padH=132, padY=12; const contentH=padH-padY; const needH=cell*2+gap; return need<=rb && rb-need>=8 && needH<=contentH && cell>=56; })(), "2x2 must fit ~390 with spare");
check("portrait pad 2x2 clarity leftover landscape row unchanged", /\.cut-cluster\s*\{[^}]*display:\s*flex/.test(cssTxt) && /\.cut-cluster\s*\{[^}]*min-width:\s*110px/.test(cssTxt) && /\.cut-cluster\s*\{[^}]*width:\s*38%/.test(cssTxt) && /\.zone-guard\s*\{[^}]*min-width:\s*52px/.test(cssTxt) && /\.zone-slash\s*\{[^}]*min-width:\s*52px/.test(cssTxt) && /\.zone-golpe\s*\{[^}]*min-width:\s*48px/.test(cssTxt) && /\.zone-dart\s*\{[^}]*min-width:\s*56px/.test(cssTxt));
check("portrait pad 2x2 clarity leftover stick size formula unchanged", /Portrait stick leftover/.test(cssTxt) && /body\.touch-on \.stick/.test(cssTxt) && /--stick-size:\s*min\(120px/.test(cssTxt) && /38vw/.test(cssTxt) && /min\(84px, calc\(var\(--pad-h\) - 14px\), 26vw\)/.test(cssTxt));
check("portrait pad 2x2 clarity leftover still letterbox no canvas resize", /@media \(orientation: portrait\)/.test(cssTxt) && /body\.touch-on #pad/.test(cssTxt) && /bottom:\s*0/.test(cssTxt) && /position:\s*absolute/.test(cssTxt) && !/@media \(orientation: portrait\)[\s\S]*body\.touch-on #game/.test(cssTxt));
check("giro hidden in portrait after portrait pad 2x2 clarity leftover", /body\.touch-on #giro\s*\{[^}]*display:\s*none/.test(cssTxt) && !/body\.touch-on #giro\s*\{[^}]*display:\s*flex/.test(cssTxt));
check("portrait pad 2x2 clarity leftover HTML still nested cluster", /cut-cluster/.test(htmlTxt) && htmlTxt.indexOf("cut-cluster") < htmlTxt.indexOf('data-tap="KeyL"') && htmlTxt.indexOf('data-tap="KeyL"') < htmlTxt.indexOf('zone-dart') && /data-hold="KeyS"/.test(htmlTxt) && /data-tap="Space"/.test(htmlTxt) && /data-tap="KeyK"/.test(htmlTxt));
check("portrait pad 2x2 clarity leftover tipX locks hold", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt));
check("portrait pad 2x2 clarity leftover is not a new combat verb", V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.THROW_WAKE_INVULN===80 && V.GUARD_COMMIT_MS===140 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("slash frames still 180/140/280 after portrait pad 2x2 clarity leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after portrait pad 2x2 clarity leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after portrait pad 2x2 clarity leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("no 6th button after portrait pad 2x2 clarity leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=302 after portrait pad 2x2 clarity leftover", /game.js\?v=350/.test(htmlTxt));

// live: pad path + AABB still planted (draw-only css; stick wiring unchanged)
bootPlay(); wait(50);
V.openLeft = 0; V.hitstopLeft = 0;
freezeYouAI(); freezeRivalAI();
const pad2x2Aabb0 = V.bodyAABB(V.player);
const pad2x2Dest0 = V.destRect(V.player);
V.stickPtr = 7;
V.stickApply(-0.9);
check("portrait pad 2x2 clarity leftover live stick still KeyA", V.keys.has("KeyA") && !V.keys.has("KeyD") && V.actionHeld("left"), `a=${V.keys.has("KeyA")} d=${V.keys.has("KeyD")}`);
V.stickApply(0.9);
check("portrait pad 2x2 clarity leftover live stick still KeyD", V.keys.has("KeyD") && !V.keys.has("KeyA") && V.actionHeld("right"), `a=${V.keys.has("KeyA")} d=${V.keys.has("KeyD")}`);
V.stickApply(0);
check("portrait pad 2x2 clarity leftover live stick deadzone still releases", !V.keys.has("KeyA") && !V.keys.has("KeyD") && !V.actionHeld("left"));
V.stickRelease();
check("portrait pad 2x2 clarity leftover live AABB planted", Math.abs(V.bodyAABB(V.player).y-pad2x2Aabb0.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-pad2x2Aabb0.h)<0.05, `dy=${(V.bodyAABB(V.player).y-pad2x2Aabb0.y).toFixed(2)}`);
check("portrait pad 2x2 clarity leftover live destRect planted", Math.abs(V.destRect(V.player).dy-pad2x2Dest0.dy)<2.2, `ddy=${(V.destRect(V.player).dy-pad2x2Dest0.dy).toFixed(2)}`);
check("portrait pad 2x2 clarity leftover landscape zone hierarchy unchanged", /\.zone-golpe[\s\S]*?height:\s*80%/.test(cssTxt) && /\.zone-slash[\s\S]*?height:\s*92%/.test(cssTxt) && /\.zone-guard[\s\S]*?height:\s*88%/.test(cssTxt) && /\.zone-dart[\s\S]*?height:\s*88%/.test(cssTxt));
check("cache v=302 after portrait pad 2x2 clarity leftover live", /game.js\?v=350/.test(htmlTxt));



// --- v304 Riposte: faster slash after perfect parry (player only; hold no longer arms) ---
check("riposte seats after perfect parry", /Perfect parry:/.test(srcTxt) && /RIPOSTE_WIN 280ms/.test(srcTxt) && /RIPOSTE_STARTUP 130/.test(srcTxt) && /same clean/.test(srcTxt) && /no \+dmg/.test(srcTxt) && /Player only/.test(srcTxt) && /rival AI does not/.test(srcTxt));
check("riposte constants 280/130/140/280", V.RIPOSTE_WIN_MS===280 && V.RIPOSTE_STARTUP===130 && V.RIPOSTE_ACTIVE===140 && V.RIPOSTE_RECOVERY===280 && V.SLASH_DMG===10 && V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.HIT_FLASH_MS===120 && V.HITSTOP_BLOCK===60);
check("riposte cut helpers honor flag", /if \(f\.riposte\) return RIPOSTE_STARTUP/.test(sliceFn(codeOnly, "cutStartup")) && /if \(f\.riposte\) return RIPOSTE_ACTIVE/.test(sliceFn(codeOnly, "cutActive")) && /if \(f\.riposte\) return RIPOSTE_RECOVERY/.test(sliceFn(codeOnly, "cutRecovery")));
check("riposte landParry arms player only", /function landParry/.test(srcTxt) && /riposteWindowT = RIPOSTE_WIN_MS/.test(sliceFn(codeOnly, "landParry")) && !/riposteWindowT = RIPOSTE_WIN_MS/.test(sliceFn(codeOnly, "landBlock")));
check("riposte spend Space or L", /function trySpendRiposteInput/.test(srcTxt) && /attackEdge/.test(sliceFn(codeOnly, "trySpendRiposteInput")) && /golpeEdge/.test(sliceFn(codeOnly, "trySpendRiposteInput")) && /riposteArmed = true/.test(sliceFn(codeOnly, "trySpendRiposteInput")));
check("riposte does not steal throw or reversal", /throwInRange\(\)/.test(sliceFn(codeOnly, "trySpendRiposteInput")) && /actionHeld\("guard"\)/.test(sliceFn(codeOnly, "trySpendRiposteInput")));
check("riposte startAttack forces slash frames", /doRiposte/.test(sliceFn(codeOnly, "startAttack")) && /riposteArmed/.test(sliceFn(codeOnly, "startAttack")) && /f\.riposte = doRiposte/.test(sliceFn(codeOnly, "startAttack")));
check("riposte hint pad + canvas", /\.zone-guard\.riposte/.test(cssTxt) && /function syncRipostePad/.test(srcTxt) && /function drawRiposteHint/.test(srcTxt) && /fillRect\(x - 2, y - 14/.test(srcTxt));
check("riposte ticks through hitstop", /tickRiposte\(player, dt\)/.test(srcTxt) && /trySpendRiposteInput\(\)/.test(srcTxt));
check("riposte tipX locks hold", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt));
check("riposte is not a 6th button", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("slash frames still 180/140/280 after riposte", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after riposte", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after riposte", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("cache v=304 after riposte", /game.js\?v=350/.test(htmlTxt));

// live: perfect parry (raise-edge) → window → Space fires 130 startup riposte, same −10
toMeasure();
freezeYouAI(); freezeRivalAI();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100; V.rival.stamina = 100;
V.player.phase = "idle"; V.player.phaseT = 0; V.player.guarding = false;
V.rival.phase = "idle"; V.rival.phaseT = 0; V.rival.guarding = false;
V.player.riposteWindowT = 0; V.player.riposteArmed = false; V.player.riposte = false;
V.player.guardRaiseElapsed = 0;
// Rival slash first; raise S late in startup so elapsed_guard < PARRY_WIN
V.player.x = 400; V.rival.x = 520;
V.player.facing = 1; V.rival.facing = -1;
V.keys.clear();
V.startAttack(V.rival, "slash");
for (let i = 0; i < 80; i++) {
  V.update(STEP);
  if (V.rival.phase === "startup" && V.rival.phaseT >= V.STARTUP - 50) break;
}
V.keys.add("KeyS");
let blocked = false;
for (let i = 0; i < 80; i++) {
  V.rival.revCd = 99999; V.rival.revArmed = -1; V.rival.pbCd = 99999; V.rival.pbArmed = -1;
  V.update(STEP);
  if (V.player.riposteWindowT > 0) { blocked = true; break; }
}
check("riposte live arms window on perfect parry", blocked && V.player.riposteWindowT > 0 && V.player.riposteWindowT <= V.RIPOSTE_WIN_MS, `arm=${blocked} win=${V.player.riposteWindowT} el=${V.player.guardRaiseElapsed}`);
const win0 = V.player.riposteWindowT;
// Drain hitstop if any, keep window
for (let i = 0; i < 10; i++) { if (V.hitstopLeft <= 0) break; V.update(STEP); }
check("riposte live window survives hitstop", V.player.riposteWindowT > 0 && V.player.riposteWindowT < win0 + 1, `win=${V.player.riposteWindowT} was=${win0}`);
// Spend with Space (drop S first so throw chord doesn't steal; wantGuard suppressed when armed anyway)
V.keys.delete("KeyS");
V.player.guarding = false;
V.attackEdge = true;
V.update(STEP);
check("riposte live Space spends and starts slash", (V.player.riposteArmed || V.player.riposte) && (V.player.phase === "startup" || V.player.riposteArmed), `armed=${V.player.riposteArmed} rip=${V.player.riposte} ph=${V.player.phase} cut=${V.player.cut}`);
// Fire if still armed
for (let i = 0; i < 5; i++) {
  if (V.player.phase === "startup" && V.player.riposte) break;
  V.update(STEP);
}
check("riposte live is riposte slash startup", V.player.phase === "startup" && V.player.cut === "slash" && V.player.riposte === true, `ph=${V.player.phase} cut=${V.player.cut} rip=${V.player.riposte}`);
check("riposte live startup is 130 not 180", V.cutStartup(V.player) === 130 && V.cutActive(V.player) === 140 && V.cutRecovery(V.player) === 280, `st=${V.cutStartup(V.player)} act=${V.cutActive(V.player)} rec=${V.cutRecovery(V.player)}`);
// Connect clean — same −10
V.rival.guarding = false; V.rival.wantBlock = false; V.rival.phase = "idle";
const hp0 = V.rival.hp;
for (let i = 0; i < 80; i++) {
  V.update(STEP);
  if (V.rival.hp < hp0) break;
}
check("riposte live clean damage still −10", hp0 - V.rival.hp === V.SLASH_DMG && V.SLASH_DMG === 10, `dHp=${hp0 - V.rival.hp} slash=${V.SLASH_DMG}`);

// live: one-shot — second block needed for new window; press spends even on whiff path
toMeasure();
freezeYouAI(); freezeRivalAI();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.riposteWindowT = V.RIPOSTE_WIN_MS;
V.player.riposteArmed = false;
V.player.riposte = false;
V.player.phase = "idle"; V.player.guarding = false; V.player.stunT = 0;
V.keys.clear();
V.golpeEdge = true;
V.update(STEP);
for (let i = 0; i < 5; i++) {
  if (V.player.riposte || V.player.phase === "startup") break;
  V.update(STEP);
}
check("riposte live L also starts riposte slash", V.player.riposte === true && V.player.cut === "slash" && V.player.phase === "startup" && V.player.riposteWindowT === 0, `rip=${V.player.riposte} cut=${V.player.cut} ph=${V.player.phase} win=${V.player.riposteWindowT}`);
check("riposte live window spent on press", V.player.riposteWindowT === 0 && V.player.riposteArmed === false, `win=${V.player.riposteWindowT} armed=${V.player.riposteArmed}`);

// dart block does not arm (bolt chip path — landBlock not used). Simulate: no window from idle.
toMeasure();
freezeYouAI(); freezeRivalAI();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.riposteWindowT = 0;
V.player.guarding = true;
V.player.stamina = 100;
// Ensure landBlock only arms on slash/golpe — static already checks. Live: call landBlock with throw cut should not arm.
V.rival.cut = "throw";
V.landBlock(V.rival, V.player, -1);
check("riposte live throw-cut landBlock does not arm", V.player.riposteWindowT === 0, `win=${V.player.riposteWindowT}`);

check("cache v=304 after riposte live", /game.js\?v=350/.test(htmlTxt));

// --- v304 Arte regen fighter cache-bust ?v=304 (courtyard untouched) ---
check("arte v4 fighter loadImg cache-bust ?v=304", /you\.png\?v=304/.test(srcTxt) && /rival\.png\?v=304/.test(srcTxt) && /rival_flip\.png\?v=304/.test(srcTxt) && /you_windup\.png\?v=304/.test(srcTxt) && /you_slash\.png\?v=304/.test(srcTxt) && /you_block\.png\?v=304/.test(srcTxt) && /you_hurt\.png\?v=304/.test(srcTxt) && /you_walk\.png\?v=304/.test(srcTxt) && /you_throw_knife\.png\?v=304/.test(srcTxt));
check("arte v4 rival sheets cache-bust no leftover v=56", /rival_hurt\.png\?v=304/.test(srcTxt) && /rival_flip_hurt\.png\?v=304/.test(srcTxt) && /rival_walk\.png\?v=304/.test(srcTxt) && /rival_flip_walk\.png\?v=304/.test(srcTxt) && !/\?v=56/.test(srcTxt));
check("arte v4 flip + rival combat sheets ?v=304", /rival_windup\.png\?v=304/.test(srcTxt) && /rival_slash\.png\?v=304/.test(srcTxt) && /rival_block\.png\?v=304/.test(srcTxt) && /rival_throw_knife\.png\?v=304/.test(srcTxt) && /rival_flip_windup\.png\?v=304/.test(srcTxt) && /rival_flip_slash\.png\?v=304/.test(srcTxt) && /rival_flip_block\.png\?v=304/.test(srcTxt) && /rival_flip_throw_knife\.png\?v=304/.test(srcTxt));
check("arte v4 courtyard loads untouched", /courtyard\.png"\)/.test(srcTxt) && /courtyard2\.png"\)/.test(srcTxt) && !/courtyard\.png\?v=/.test(srcTxt));
check("arte v4 tipX / plant locks hold", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt));
check("plant you idle still footX 75", V.poseSheet({kind:"you",facing:1,phase:"idle",stunT:0,guarding:false,falling:false,img:null}).footX===75);
check("cache v=304 after arte fighter cache-bust", /game.js\?v=350/.test(htmlTxt));



// --- v305 Teach HUD: full combat verbs + verb/time fade ---
check("teach HUD header leftover", /Teach HUD \(first fight\)/.test(srcTxt) && /remap-aware BIND_ACTIONS/.test(srcTxt) && /HINT_HOLD_MS/.test(srcTxt) && /3\+ distinct verbs/.test(srcTxt) && /~12s/.test(srcTxt));
check("teach HUD controlsHintLines remap-aware", /function controlsHintLines/.test(srcTxt) && /bindPrimary\("left"\)/.test(srcTxt) && /bindPrimary\("right"\)/.test(srcTxt) && /bindPrimary\("guard"\)/.test(srcTxt) && /bindPrimary\("slash"\)/.test(srcTxt) && /bindPrimary\("golpe"\)/.test(srcTxt) && /bindPrimary\("dart"\)/.test(srcTxt) && /codeLabel\(bindPrimary/.test(srcTxt));
check("teach HUD lines cover verbs", (()=>{ const L=V.controlsHintLines(); return L.length===2 && /ANDAR/.test(L[0]) && /GUARDA/.test(L[0]) && /TAJO/.test(L[0]) && /GOLPE/.test(L[0]) && /DARDO/.test(L[0]) && /AGARRE/.test(L[1]) && /REV/.test(L[1]) && /S AL FILO/.test(L[1]) && /RIPOSTE/.test(L[1]) && /PARRY>/.test(L[1]); })(), V.controlsHintLines().join(" | "));
check("teach HUD default labels A/D S ESPACIO L K", (()=>{ const L=V.controlsHintLines(); return L[0].indexOf("A/D")===0 && / S GUARDA /.test(L[0]) && / ESPACIO TAJO /.test(L[0]) && / L GOLPE /.test(L[0]) && / K DARDO/.test(L[0]) && L[1].indexOf("S AL FILO PARRY>")===0 && /PARRY>ESPACIO\/L RIPOSTE/.test(L[1]) && / ESPACIO\+S AGARRE /.test(L[1]) && / S\+L REV/.test(L[1]); })(), V.controlsHintLines().join(" | "));
check("teach HUD remap swaps labels", (()=>{
  const prev=JSON.parse(JSON.stringify(V.binds));
  V.setBindCode("slash","KeyU");
  const L=V.controlsHintLines();
  const ok=/ U TAJO /.test(L[0]) && L[1].indexOf("S AL FILO PARRY>")===0 && /PARRY>U\/L RIPOSTE/.test(L[1]) && / U\+S AGARRE /.test(L[1]);
  V.binds=prev;
  return ok;
})(), V.controlsHintLines().join(" | "));
check("teach HUD two lines no fillRect card", /function drawControlsHint/.test(srcTxt) && /H - 52/.test(srcTxt) && /H - 32/.test(srcTxt) && !/function drawControlsHint[\s\S]{0,900}fillRect/.test(srcTxt) && /0\.68 \* inn \* out/.test(srcTxt));
check("teach HUD fade contract hold+verbs", V.HINT_IN_MS===520 && V.HINT_FADE_MS===640 && V.HINT_HOLD_MS===12000 && V.HINT_VERB_N===3);
check("teach HUD noteHintVerb + armHintFade source", /function noteHintVerb/.test(srcTxt) && /function armHintFade/.test(srcTxt) && /noteHintVerb\("walk"\)/.test(srcTxt) && /noteHintVerb\("guard"\)/.test(srcTxt) && /noteHintVerb\("slash"\)|noteHintVerb\(doRiposte/.test(srcTxt) && /noteHintVerb\("throw"\)/.test(srcTxt) && /noteHintVerb\("reversal"\)/.test(srcTxt) && /noteHintVerb\("dart"\)/.test(srcTxt) && /noteHintVerb\("feint"\)/.test(srcTxt) && /noteHintVerb\("parry"\)/.test(srcTxt) && /noteHintVerb\("riposte"\)|noteHintVerb\(doRiposte \? "riposte"/.test(srcTxt));
check("teach HUD tipX / plant locks hold", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt));
check("plant you idle still footX 75 after teach HUD", V.poseSheet({kind:"you",facing:1,phase:"idle",stunT:0,guarding:false,falling:false,img:null}).footX===75);
check("slash frames still 180/140/280 after teach HUD", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after teach HUD", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after teach HUD", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("riposte frames still 130/140/280 after teach HUD", V.RIPOSTE_STARTUP===130 && V.RIPOSTE_ACTIVE===140 && V.RIPOSTE_RECOVERY===280 && V.RIPOSTE_WIN_MS===280);
check("no 6th button after teach HUD", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=305 after teach HUD", /game.js\?v=350/.test(htmlTxt));
check("arte fighters still ?v=304 after teach HUD", /you\.png\?v=304/.test(srcTxt) && /rival\.png\?v=304/.test(srcTxt) && /courtyard\.png"\)/.test(srcTxt) && !/courtyard\.png\?v=/.test(srcTxt));

// CONTROLES title page teaches chords (remap-aware) + riposte; VOLVER still fits
check("CONTROLES page teach chords source", /titlePage === "controls"/.test(srcTxt) && /FEINT/.test(srcTxt) && /AGARRE/.test(srcTxt) && /REVERSAL/.test(srcTxt) && /EMPUJON/.test(srcTxt) && /RIPOSTE/.test(srcTxt) && /CAMINAR/.test(srcTxt) && /GUARDA/.test(srcTxt) && /TAJO/.test(srcTxt) && /GOLPE/.test(srcTxt) && /DARDO/.test(srcTxt));
check("CONTROLES page remap-aware binds", /titlePage === "controls"[\s\S]{0,900}bindPrimary\("left"\)/.test(srcTxt) && /titlePage === "controls"[\s\S]{0,900}bindPrimary\("guard"\)/.test(srcTxt) && /titlePage === "controls"[\s\S]{0,900}bindPrimary\("slash"\)/.test(srcTxt) && /titlePage === "controls"[\s\S]{0,900}bindPrimary\("golpe"\)/.test(srcTxt) && /titlePage === "controls"[\s\S]{0,900}bindPrimary\("dart"\)/.test(srcTxt));
check("CONTROLES page tight spacing VOLVER", /titlePage === "controls"[\s\S]{0,1200}const step = 22/.test(srcTxt) && /titlePage === "controls"[\s\S]{0,1400}H \* 0\.88/.test(srcTxt) && /VOLVER/.test(srcTxt));
check("CONTROLES page no 6th button invent", !/zone-pushblock/.test(htmlTxt) && (htmlTxt.match(/data-tap=/g)||[]).length===3);

// live: 3 distinct verbs fade hint without connect
toClose();
V.hintDone=false; V.hintFade=0; V.hintIn=V.HINT_IN_MS; V.hintAge=0; V.hintVerbN=0;
for (const k of Object.keys(V.hintVerbs)) delete V.hintVerbs[k];
freezeYouAI(); freezeRivalAI();
V.openLeft=0; V.hitstopLeft=0;
V.keys.clear();
V.keys.add("KeyA");
V.update(STEP);
check("teach HUD live walk verb arms", V.hintVerbN>=1 && !!V.hintVerbs.walk && !V.hintDone, `n=${V.hintVerbN} done=${V.hintDone}`);
V.keys.clear();
V.keys.add("KeyS");
V.update(STEP);
check("teach HUD live guard verb arms", V.hintVerbN>=2 && !!V.hintVerbs.guard && !V.hintDone, `n=${V.hintVerbN} done=${V.hintDone}`);
V.keys.clear();
V.player.guarding=false; V.player.phase="idle"; V.player.phaseT=0; V.player.stunT=0;
V.attackEdge=true;
V.update(STEP);
check("teach HUD live 3rd verb fades", V.hintDone && V.hintFade>=V.HINT_FADE_MS-STEP*2 && V.hintVerbN>=3 && !!V.hintVerbs.slash, `done=${V.hintDone} fade=${V.hintFade} n=${V.hintVerbN} verbs=${Object.keys(V.hintVerbs).join(",")}`);

// live: hold timeout fades without connect
toClose();
V.hintDone=false; V.hintFade=0; V.hintIn=V.HINT_IN_MS; V.hintAge=0; V.hintVerbN=0;
for (const k of Object.keys(V.hintVerbs)) delete V.hintVerbs[k];
freezeYouAI(); freezeRivalAI();
V.openLeft=0; V.hitstopLeft=0;
V.keys.clear();
V.hintAge = V.HINT_HOLD_MS - STEP;
V.update(STEP);
check("teach HUD live hold timeout fades", V.hintDone && V.hintFade>=V.HINT_FADE_MS-STEP*2 && V.hintAge>=V.HINT_HOLD_MS, `done=${V.hintDone} fade=${V.hintFade} age=${V.hintAge}`);

// connect fade still works
toClose();
V.hintDone=false; V.hintFade=0; V.hintIn=V.HINT_IN_MS; V.hintAge=0; V.hintVerbN=0;
for (const k of Object.keys(V.hintVerbs)) delete V.hintVerbs[k];
freezeRivalAI();
V.openLeft=0; V.hitstopLeft=0;
V.attackEdge=true;
let teachHit=false;
for(let i=0;i<80;i++){ V.update(STEP); if(V.hitstopLeft>0 || V.rival.hp<100){ teachHit=true; break; } }
check("teach HUD live connect still fades", teachHit && V.hintDone && V.hintFade>0, `hit=${teachHit} done=${V.hintDone} fade=${V.hintFade}`);
V.resetRound();
check("teach HUD rematch stays gone", V.hintDone && V.hintFade===0, `done=${V.hintDone} fade=${V.hintFade}`);
check("cache v=305 after teach HUD live", /game.js\?v=350/.test(htmlTxt));



// --- v306 Meter / combo feedback: fill pulse, stock sting, spend bite, sparse combo ---
check("meter combo feedback seats header", /Meter \/ combo feedback: brasa pip fill pulse/.test(srcTxt) && /meterGainT scale\/glow/.test(srcTxt) && /meterFlashKind/.test(srcTxt) && /Sparse[\s\S]*?pixel combo count/.test(srcTxt) && /Does not obscure HP\/stam or teach HUD/.test(srcTxt));
check("meter combo feedback constants", V.METER_GAIN_MS===180 && V.COMBO_SHOW_MS===640 && V.METER_FLASH_MS===220 && V.METER_MAX===100 && V.METER_HIT===20 && V.METER_BLOCK_SPECIAL===10);
check("meter combo feedback gainMeter arms gain pulse", /f\.meterGainT = METER_GAIN_MS/.test(sliceFn(codeOnly, "gainMeter")) && /was < METER_MAX && f\.meter >= METER_MAX/.test(sliceFn(codeOnly, "gainMeter")) && /meterFlashKind = "full"/.test(sliceFn(codeOnly, "gainMeter")) && /f\.meterFlashT = METER_FLASH_MS/.test(sliceFn(codeOnly, "gainMeter")));
check("meter combo feedback spend kind bite", /meterFlashKind = "spend"/.test(sliceFn(codeOnly, "startBolt")) && /u\.meterFlashT = METER_FLASH_MS/.test(sliceFn(codeOnly, "startBolt")) && /u\.meter = 0/.test(sliceFn(codeOnly, "startBolt")));
check("meter combo feedback meterGainK draw-only", /function meterGainK/.test(srcTxt) && /f\.meterGainT \/ METER_GAIN_MS/.test(sliceFn(codeOnly, "meterGainK")) && /Partial fill pulse envelope/.test(srcTxt) && !/meterGainT/.test(sliceFn(codeOnly, "destRect")) && !/meterGainT/.test(sliceFn(codeOnly, "bodyAABB")) && !/meterGainT/.test(sliceFn(codeOnly, "slashPose")) && !/meterGainK/.test(sliceFn(codeOnly, "landHit")));
check("meter combo feedback drawLifeBar juice", /const mg = meterGainK\(f\)/.test(sliceFn(codeOnly, "drawLifeBar")) && /mKind === "full"/.test(sliceFn(codeOnly, "drawLifeBar")) && /mKind === "spend"/.test(sliceFn(codeOnly, "drawLifeBar")) && /0\.55 \* mf/.test(sliceFn(codeOnly, "drawLifeBar")) && /const pk = k >= 1 \? 1 : mf/.test(sliceFn(codeOnly, "drawLifeBar")) && /fillFrom\(left, mx, my - grow/.test(sliceFn(codeOnly, "drawLifeBar")));
check("meter combo feedback noteCombo sparse", /function noteCombo/.test(srcTxt) && /atk\.comboN >= 2/.test(sliceFn(codeOnly, "noteCombo")) && /COMBO_SHOW_MS/.test(sliceFn(codeOnly, "noteCombo")) && /noteCombo\(atk, def\)/.test(sliceFn(codeOnly, "landHit")) && /noteCombo\(atk, def\)/.test(sliceFn(codeOnly, "landThrow")) && /noteCombo\(atk, def\)/.test(sliceFn(codeOnly, "landBoltHit")) && !/noteCombo/.test(sliceFn(codeOnly, "landBoltBlock")) && !/noteCombo/.test(sliceFn(codeOnly, "landBlock")));
check("meter combo feedback drawComboCount", /function drawComboCount/.test(srcTxt) && /f\.comboN < 2/.test(sliceFn(codeOnly, "drawComboCount")) && /drawPixelText\(String\(f\.comboN/.test(sliceFn(codeOnly, "drawComboCount")) && /drawComboCount\(player\)/.test(sliceFn(codeOnly, "drawHud")) && /drawComboCount\(rival\)/.test(sliceFn(codeOnly, "drawHud")) && /drawControlsHint\(\)/.test(sliceFn(codeOnly, "drawHud")));
check("meter combo feedback tipX / plant locks hold", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt));
check("plant you idle still footX 75 after meter combo feedback", V.poseSheet({kind:"you",facing:1,phase:"idle",stunT:0,guarding:false,falling:false,img:null}).footX===75);
check("slash frames still 180/140/280 after meter combo feedback", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after meter combo feedback", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after meter combo feedback", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("riposte frames still 130/140/280 after meter combo feedback", V.RIPOSTE_STARTUP===130 && V.RIPOSTE_ACTIVE===140 && V.RIPOSTE_RECOVERY===280 && V.RIPOSTE_WIN_MS===280);
check("no 6th button after meter combo feedback", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("arte fighters still ?v=304 after meter combo feedback", /you\.png\?v=304/.test(srcTxt) && /rival\.png\?v=304/.test(srcTxt) && /courtyard\.png"\)/.test(srcTxt) && !/courtyard\.png\?v=/.test(srcTxt));
check("cache v=306 after meter combo feedback", /game.js\?v=350/.test(htmlTxt));

// live: partial gain pulses meterGainT, meterFlashT stays quiet
toMeasure();
freezeYouAI(); freezeRivalAI();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.meter = 0; V.player.meterFlashT = 0; V.player.meterGainT = 0; V.player.meterFlashKind = "";
V.player.comboN = 0; V.player.comboT = 0;
V.player.phase = "idle"; V.player.stunT = 0;
V.rival.phase = "idle"; V.rival.stunT = 0; V.rival.guarding = false;
const mcgAabb0 = V.bodyAABB(V.player);
const mcgDest0 = V.destRect(V.player);
V.gainMeter(V.player, V.METER_HIT);
check("meter combo feedback live partial arms gain pulse", V.player.meter===20 && V.player.meterGainT===V.METER_GAIN_MS && V.player.meterFlashT===0 && V.meterGainK(V.player)>0.9 && V.meterFlashK(V.player)===0, `m=${V.player.meter} gt=${V.player.meterGainT} ft=${V.player.meterFlashT} gk=${V.meterGainK(V.player)} fk=${V.meterFlashK(V.player)}`);
check("meter combo feedback live partial quiet flash", V.player.meterFlashT===0 && V.player.meterFlashKind==="" && V.meterFlashK(V.player)===0, `ft=${V.player.meterFlashT} kind=${V.player.meterFlashKind}`);
check("meter combo feedback live AABB planted", Math.abs(V.bodyAABB(V.player).y-mcgAabb0.y)<0.05 && Math.abs(V.bodyAABB(V.player).h-mcgAabb0.h)<0.05, `dy=${(V.bodyAABB(V.player).y-mcgAabb0.y).toFixed(2)}`);
check("meter combo feedback live destRect planted", Math.abs(V.destRect(V.player).dy-mcgDest0.dy)<2.2, `ddy=${(V.destRect(V.player).dy-mcgDest0.dy).toFixed(2)}`);

// stock-complete sting
V.player.meter = 80; V.player.meterFlashT = 0; V.player.meterGainT = 0; V.player.meterFlashKind = "";
V.gainMeter(V.player, V.METER_HIT);
check("meter combo feedback live stock full sting", V.player.meter===V.METER_MAX && V.player.meterFlashT===V.METER_FLASH_MS && V.player.meterFlashKind==="full" && V.player.meterGainT===V.METER_GAIN_MS && V.meterFlashK(V.player)>0.9, `m=${V.player.meter} ft=${V.player.meterFlashT} kind=${V.player.meterFlashKind} gt=${V.player.meterGainT}`);

// spend bite
V.player.meter = 100; V.player.meterFlashT = 0; V.player.meterGainT = 0; V.player.meterFlashKind = "";
V.player.stamina = 100; V.player.stamRegenT = 99999; V.player.phase = "idle"; V.player.boltPhase = "";
V.startBolt(V.player, true);
check("meter combo feedback live spend bite", V.player.boltPhase==="startup" && V.player.boltSuper===true && V.player.meter===0 && V.player.meterFlashT===V.METER_FLASH_MS && V.player.meterFlashKind==="spend" && V.meterFlashK(V.player)>0.9, `bph=${V.player.boltPhase} super=${V.player.boltSuper} m=${V.player.meter} ft=${V.player.meterFlashT} kind=${V.player.meterFlashKind}`);

// combo: first hit silent, second shows
toMeasure();
freezeYouAI(); freezeRivalAI();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100; V.rival.stamina = 100;
V.player.phase = "idle"; V.rival.phase = "idle";
V.player.comboN = 0; V.player.comboT = 0;
V.rival.comboN = 0; V.rival.comboT = 0;
V.rival.stunT = 0; V.rival.falling = false; V.rival.thrownT = 0;
V.rival.guarding = false;
V.player.x = 400; V.rival.x = 520;
V.player.facing = 1; V.rival.facing = -1;
V.noteCombo(V.player, V.rival);
check("meter combo feedback live first connect silent", V.player.comboN===1 && V.player.comboT===0, `n=${V.player.comboN} t=${V.player.comboT}`);
V.rival.stunT = V.HITSTUN;
V.noteCombo(V.player, V.rival);
check("meter combo feedback live second connect shows", V.player.comboN===2 && V.player.comboT===V.COMBO_SHOW_MS, `n=${V.player.comboN} t=${V.player.comboT}`);
V.rival.stunT = V.HITSTUN;
V.noteCombo(V.player, V.rival);
check("meter combo feedback live third connect climbs", V.player.comboN===3 && V.player.comboT===V.COMBO_SHOW_MS, `n=${V.player.comboN} t=${V.player.comboT}`);

// rival gain pulse
V.rival.meter = 40; V.rival.meterFlashT = 0; V.rival.meterGainT = 0; V.rival.meterFlashKind = "";
V.gainMeter(V.rival, V.METER_BLOCK_SPECIAL);
check("meter combo feedback rival partial gain pulse", V.rival.meter===50 && V.rival.meterGainT===V.METER_GAIN_MS && V.rival.meterFlashT===0, `m=${V.rival.meter} gt=${V.rival.meterGainT} ft=${V.rival.meterFlashT}`);

check("cache v=306 after meter combo feedback live", /game.js\?v=350/.test(htmlTxt));


// --- v307 Riposte SFX + connect juice: sfx_riposte on spend, punchier hit ---
check("riposte SFX seats header", /Riposte SFX \+ connect juice: spend Space\/L/.test(srcTxt) && /sfx_riposte/.test(srcTxt) && /No arm-window tick/.test(srcTxt) && /RIPOSTE_HIT_SHAKE 12/.test(srcTxt) && /130\/140\/280 locked/.test(srcTxt));
check("riposte SFX asset wired", /riposte:\s*new Audio\("sfx\/sfx_riposte\.wav"\)/.test(srcTxt) && /function playRiposteSpendSting/.test(srcTxt) && /function playRiposteHitSting/.test(srcTxt));
check("riposte spend sting plays file", /lastRiposteSfx = "spend"/.test(sliceFn(codeOnly, "playRiposteSpendSting")) && /playSfx\(SFX\.riposte/.test(sliceFn(codeOnly, "playRiposteSpendSting")) && /playRiposteSpendSting\(\)/.test(sliceFn(codeOnly, "trySpendRiposteInput")));
check("riposte hit sting pitches impacto", /lastRiposteSfx = "hit"/.test(sliceFn(codeOnly, "playRiposteHitSting")) && (sliceFn(codeOnly, "playRiposteHitSting").match(/playSfx\(SFX\.impacto/g)||[]).length>=2);
check("riposte active skips tajo whoosh", /!f\.riposte\) playSfx\(SFX\.whoosh\)/.test(sliceFn(codeOnly, "advanceAttack")) || /cut !== "throw" && !f\.riposte/.test(sliceFn(codeOnly, "advanceAttack")));
check("riposte landHit connect juice", /atk\.riposte/.test(sliceFn(codeOnly, "landHit")) && /playRiposteHitSting/.test(sliceFn(codeOnly, "landHit")) && /RIPOSTE_HIT_SHAKE/.test(sliceFn(codeOnly, "landHit")) && /bumpShake\(10, dir, HITSTOP_HIT\)/.test(sliceFn(codeOnly, "landHit")));
check("riposte hitstop numbers still locked", V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.HIT_FLASH_MS===120 && V.RIPOSTE_HIT_SHAKE===12 && V.SLASH_DMG===10);
check("riposte frames still 130/140/280 after SFX", V.RIPOSTE_STARTUP===130 && V.RIPOSTE_ACTIVE===140 && V.RIPOSTE_RECOVERY===280 && V.RIPOSTE_WIN_MS===280);
check("slash frames still 180/140/280 after riposte SFX", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after riposte SFX", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after riposte SFX", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("riposte tipX / plant locks hold after SFX", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt));
check("plant you idle still footX 75 after riposte SFX", V.poseSheet({kind:"you",facing:1,phase:"idle",stunT:0,guarding:false,falling:false,img:null}).footX===75);
check("no 6th button after riposte SFX", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("arte fighters still ?v=304 after riposte SFX", /you\.png\?v=304/.test(srcTxt) && /rival\.png\?v=304/.test(srcTxt) && /courtyard\.png"\)/.test(srcTxt) && !/courtyard\.png\?v=/.test(srcTxt));
check("riposte credits not regress", /sfx_riposte\.wav/.test(fs.readFileSync("/workspace/estudio/vispera/AUDIO_CREDITS.md","utf8")) && /sfx_riposte\.wav/.test(fs.readFileSync("/workspace/estudio/vispera/sfx/README.md","utf8")));
check("cache v=307 after riposte SFX", /game.js\?v=350/.test(htmlTxt));

// live: perfect parry → spend fires riposte SFX, connect punches shake 12 + hit sting
toClose();
freezeYouAI(); freezeRivalAI();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100; V.rival.stamina = 100;
V.player.phase = "idle"; V.rival.phase = "idle";
V.player.stunT = 0; V.rival.stunT = 0;
V.player.guarding = false; V.rival.guarding = false;
V.player.riposteWindowT = 0; V.player.riposteArmed = false; V.player.riposte = false;
V.player.guardRaiseElapsed = 0;
V.player.x = 400; V.rival.x = 520;
V.player.facing = 1; V.rival.facing = -1;
V.keys.clear();
V.startAttack(V.rival, "slash");
for (let i = 0; i < 80; i++) {
  V.update(STEP);
  if (V.rival.phase === "startup" && V.rival.phaseT >= V.STARTUP - 50) break;
}
V.keys.add("KeyS");
let rArm = false;
for (let i = 0; i < 80; i++) {
  V.rival.revCd = 99999; V.rival.revArmed = -1; V.rival.pbCd = 99999; V.rival.pbArmed = -1;
  V.update(STEP);
  if (V.player.riposteWindowT > 0) { rArm = true; break; }
}
check("riposte SFX live arms window on perfect parry", rArm && V.player.riposteWindowT > 0, `arm=${rArm} win=${V.player.riposteWindowT} el=${V.player.guardRaiseElapsed}`);
drainHitstop();
V.keys.clear();
V.attackEdge = true;
let spentOk = false;
for (let i = 0; i < 8; i++) {
  V.update(STEP);
  if (V.lastRiposteSfx === "spend" || V.player.riposte || V.player.riposteArmed) { spentOk = true; break; }
}
check("riposte SFX live spend fires sfx_riposte", spentOk && V.lastRiposteSfx === "spend", `ok=${spentOk} sfx=${V.lastRiposteSfx} armed=${V.player.riposteArmed} rip=${V.player.riposte} ph=${V.player.phase}`);
for (let i = 0; i < 40; i++) {
  V.update(STEP);
  if (V.player.phase === "startup" && V.player.riposte) break;
}
check("riposte SFX live still 130 startup", V.player.riposte === true && V.cutStartup(V.player) === 130, `rip=${V.player.riposte} st=${V.cutStartup(V.player)} ph=${V.player.phase}`);
const hpR0 = V.rival.hp;
let hitOk = false;
for (let i = 0; i < 80; i++) {
  V.rival.revCd = 99999; V.rival.revArmed = -1; V.rival.pbCd = 99999; V.rival.pbArmed = -1;
  V.rival.guarding = false;
  V.update(STEP);
  if (V.hitstopLeft > 0 && V.rival.hp < hpR0) { hitOk = true; break; }
}
check("riposte SFX live connect hit sting", hitOk && V.lastRiposteSfx === "hit" && (hpR0 - V.rival.hp) === V.SLASH_DMG, `ok=${hitOk} sfx=${V.lastRiposteSfx} dHp=${hpR0 - V.rival.hp}`);
check("riposte SFX live punchier shake mag 12", V.shakeMag === V.RIPOSTE_HIT_SHAKE && V.hitstopLeft > 0 && V.hitstopLeft <= V.HITSTOP_HIT, `mag=${V.shakeMag} lock=${V.RIPOSTE_HIT_SHAKE} hs=${V.hitstopLeft}`);
check("riposte SFX live hitstop still 140 design", V.HITSTOP_HIT === 140 && V.HITSTUN === 350 && V.HIT_FLASH_MS === 120, `hs=${V.HITSTOP_HIT} stun=${V.HITSTUN} flash=${V.HIT_FLASH_MS}`);

// normal slash still whoosh path / mag 10
toMeasure();
freezeYouAI(); freezeRivalAI();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.hp = 100; V.rival.hp = 100;
V.player.phase = "idle"; V.rival.phase = "idle";
V.rival.guarding = false; V.rival.stunT = 0;
V.player.riposte = false; V.player.riposteArmed = false; V.player.riposteWindowT = 0;
V.player.x = 400; V.rival.x = 520;
V.player.facing = 1; V.rival.facing = -1;
V.keys.clear();
V.startAttack(V.player, "slash");
let nHit = false;
for (let i = 0; i < 80; i++) {
  V.rival.revCd = 99999; V.rival.revArmed = -1; V.rival.pbCd = 99999; V.rival.pbArmed = -1;
  V.update(STEP);
  if (V.hitstopLeft > 0 && V.rival.hp < 100) { nHit = true; break; }
}
check("normal slash still mag 10 not riposte sting", nHit && V.shakeMag === 10 && V.player.riposte === false && V.HITSTOP_HIT === 140, `ok=${nHit} mag=${V.shakeMag} rip=${V.player.riposte} sfx=${V.lastRiposteSfx}`);

check("cache v=307 after riposte SFX live", /game.js\?v=350/.test(htmlTxt));



// --- v308 Parry timing window vs hold-guard (Combate final) ---
check("parry Combate seats", /Parry \(Combate final\)/.test(srcTxt) && /PARRY_WIN 140ms/.test(srcTxt) && /PARRY_STAGGER 180ms/.test(srcTxt) && /PARRY_GLEAM 80ms/.test(srcTxt) && /Does NOT arm RIPOSTE_WIN/.test(srcTxt) && /rival AI does not parry/.test(srcTxt));
check("parry constants 140/180/80", V.PARRY_WIN_MS===140 && V.PARRY_STAGGER_MS===180 && V.PARRY_GLEAM_MS===80 && V.RIPOSTE_WIN_MS===280 && V.HITSTOP_BLOCK===60 && V.GUARD_PUSH_MS===150 && V.STEEL_FLASH_MS===60);
check("parry raise-edge clock", /guardRaiseElapsed/.test(srcTxt) && /rising\) f\.guardRaiseElapsed = 0/.test(codeOnly) && /guardRaiseElapsed = \(f\.guardRaiseElapsed \|\| 0\) \+ dt/.test(codeOnly));
check("parry melee only gate", /function isPerfectParry/.test(srcTxt) && /def !== player/.test(sliceFn(codeOnly, "isPerfectParry")) && /atk\.cut === "slash" \|\| atk\.cut === "golpe"/.test(sliceFn(codeOnly, "isPerfectParry")) && /PARRY_WIN_MS/.test(sliceFn(codeOnly, "isPerfectParry")));
check("parry landBlock branches", /isPerfectParry\(atk, def\)/.test(sliceFn(codeOnly, "landBlock")) && /landParry\(atk, def, dir\)/.test(sliceFn(codeOnly, "landBlock")) && !/riposteWindowT = RIPOSTE_WIN_MS/.test(sliceFn(codeOnly, "landBlock")));
check("parry landParry arms riposte + stagger", /atk\.stunT = PARRY_STAGGER_MS/.test(sliceFn(codeOnly, "landParry")) && /riposteWindowT = RIPOSTE_WIN_MS/.test(sliceFn(codeOnly, "landParry")) && /playParrySting/.test(sliceFn(codeOnly, "landParry")) && /spawnParryGleam/.test(sliceFn(codeOnly, "landParry")));
check("parry SFX pitched steel", /function playParrySting/.test(srcTxt) && /SFX\.bloqueo/.test(sliceFn(codeOnly, "playParrySting")) && /rate: 1\.48/.test(sliceFn(codeOnly, "playParrySting")) && /SFX\.choque/.test(sliceFn(codeOnly, "playParrySting")));
check("parry gleam distinct", /function spawnParryGleam/.test(srcTxt) && /function drawParryGleam/.test(srcTxt) && /PARRY_GLEAM_MS/.test(srcTxt) && /drawParryGleam\(\)/.test(srcTxt) && /COL_BRASA/.test(sliceFn(codeOnly, "drawParryGleam")));
check("parry does not steal throw/dart/reversal", /THROW_STARTUP/.test(srcTxt) && V.THROW_STARTUP===80 && V.BOLT_CHIP===2 && V.REVERSAL_STAM===30 && V.PUSHBLOCK_STAM===25);
check("parry tipX / frames locks", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt) && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.RIPOSTE_STARTUP===130 && V.RIPOSTE_ACTIVE===140 && V.RIPOSTE_RECOVERY===280);
check("parry no 6th button", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("parry teach HUD PARRY>", (()=>{ const L=V.controlsHintLines(); return /PARRY>/.test(L[1]) && /RIPOSTE/.test(L[1]) && !/BLOQUEO>/.test(L[1]); })(), V.controlsHintLines().join(" | "));
check("cache v=308 after parry", /game.js\?v=350/.test(htmlTxt));
check("arte fighters still ?v=304 after parry", /you\.png\?v=304/.test(srcTxt) && /rival\.png\?v=304/.test(srcTxt) && /courtyard\.png"\)/.test(srcTxt) && !/courtyard\.png\?v=/.test(srcTxt));

// live: raise-edge vs melee → perfect parry arms riposte + stagger + gleam
toClose();
freezeYouAI(); freezeRivalAI();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100; V.rival.stamina = 100;
V.player.phase = "idle"; V.rival.phase = "idle";
V.player.stunT = 0; V.rival.stunT = 0;
V.player.guarding = false; V.rival.guarding = false;
V.player.riposteWindowT = 0; V.player.riposteArmed = false; V.player.riposte = false;
V.player.guardRaiseElapsed = 0;
V.player.x = 400; V.rival.x = 520;
V.player.facing = 1; V.rival.facing = -1;
V.keys.clear();
V.lastParrySfx = "";
V.startAttack(V.rival, "slash");
for (let i = 0; i < 80; i++) {
  V.update(STEP);
  if (V.rival.phase === "startup" && V.rival.phaseT >= V.STARTUP - 50) break;
}
V.keys.add("KeyS");
let pArm = false;
let pStun = 0, pGleam = 0, pSfx = "";
for (let i = 0; i < 80; i++) {
  V.rival.revCd = 99999; V.rival.revArmed = -1; V.rival.pbCd = 99999; V.rival.pbArmed = -1;
  V.update(STEP);
  if (V.player.riposteWindowT > 0) {
    pArm = true;
    pStun = V.rival.stunT;
    pGleam = V.parryGleamT;
    pSfx = V.lastParrySfx;
    break;
  }
}
check("parry live arms riposte on raise-edge", pArm && V.player.riposteWindowT > 0 && V.player.hp === 100, `arm=${pArm} win=${V.player.riposteWindowT} hp=${V.player.hp} sfx=${V.lastParrySfx}`);
check("parry live rival stagger 180", pStun > 0 && pStun <= V.PARRY_STAGGER_MS, `stun=${pStun} now=${V.rival.stunT} ph=${V.rival.phase}`);
check("parry live gleam armed", pGleam > 0 && pGleam <= V.PARRY_GLEAM_MS, `gleam=${pGleam} now=${V.parryGleamT}`);
check("parry live SFX pitched", V.lastParrySfx === "parry", `sfx=${V.lastParrySfx}`);
check("parry live zero chip", V.player.hp === 100 && V.rival.hp === 100, `php=${V.player.hp} rhp=${V.rival.hp}`);

// live: hold-guard late (≥140) → normal block, NO riposte
toClose();
freezeYouAI(); freezeRivalAI();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100; V.rival.stamina = 100;
V.player.phase = "idle"; V.rival.phase = "idle";
V.player.stunT = 0; V.rival.stunT = 0;
V.player.riposteWindowT = 0; V.player.riposteArmed = false; V.player.riposte = false;
V.player.x = 400; V.rival.x = 520;
V.player.facing = 1; V.rival.facing = -1;
V.keys.clear();
V.keys.add("KeyS");
for (let i = 0; i < 20; i++) V.update(STEP); // ~333ms hold > 140
check("parry live hold elapsed past window", V.player.guarding && V.player.guardRaiseElapsed >= V.PARRY_WIN_MS, `g=${V.player.guarding} el=${V.player.guardRaiseElapsed}`);
V.startAttack(V.rival, "slash");
let holdBlocked = false;
for (let i = 0; i < 100; i++) {
  V.rival.revCd = 99999; V.rival.revArmed = -1; V.rival.pbCd = 99999; V.rival.pbArmed = -1;
  V.update(STEP);
  if (V.hitstopLeft > 0 && V.player.hp === 100 && V.rival.phase === "recovery") { holdBlocked = true; break; }
}
check("parry live hold-block no riposte arm", holdBlocked && V.player.riposteWindowT === 0 && V.player.hp === 100, `blk=${holdBlocked} win=${V.player.riposteWindowT} hp=${V.player.hp} sfx=${V.lastParrySfx}`);
check("parry live hold still steel not gleam-only", V.steelFlashT > 0 || V.lastPushblockSfx === "block", `steel=${V.steelFlashT} pbs=${V.lastPushblockSfx}`);

// live: throw vs raise-edge does not perfect-parry (throw still grabs hold-guard)
toClose();
freezeYouAI(); freezeRivalAI();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100; V.rival.stamina = 100;
V.player.phase = "idle"; V.rival.phase = "idle";
V.player.stunT = 0; V.rival.stunT = 0;
V.player.riposteWindowT = 0;
V.player.x = 460; V.rival.x = 520;
V.player.facing = 1; V.rival.facing = -1;
V.keys.clear();
V.keys.add("KeyS");
V.update(STEP); // raise edge
V.startThrow(V.rival);
let threw = false;
for (let i = 0; i < 60; i++) {
  V.rival.revCd = 99999; V.rival.revArmed = -1;
  V.update(STEP);
  if (V.player.thrownT > 0 || V.player.hp < 100) { threw = true; break; }
}
check("parry live throw still beats raise-edge guard", threw && V.player.riposteWindowT === 0, `threw=${threw} win=${V.player.riposteWindowT} th=${V.player.thrownT} hp=${V.player.hp}`);

// live: dart chip on hold/raise is not parry (bolt path separate)
toClose();
freezeYouAI(); freezeRivalAI();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100; V.rival.stamina = 100;
V.player.phase = "idle"; V.rival.phase = "idle";
V.player.riposteWindowT = 0;
V.player.x = 400; V.rival.x = 700;
V.player.facing = 1; V.rival.facing = -1;
V.keys.clear();
V.keys.add("KeyS");
V.update(STEP);
V.startBolt(V.rival, false);
let chip = false;
for (let i = 0; i < 200; i++) {
  V.rival.revCd = 99999; V.rival.boltCd = 99999;
  V.update(STEP);
  if (V.player.hp < 100) { chip = true; break; }
}
check("parry live dart chip not parry", chip && V.player.riposteWindowT === 0 && V.player.hp === 100 - V.BOLT_CHIP, `chip=${chip} hp=${V.player.hp} win=${V.player.riposteWindowT}`);

check("cache v=310 after parry live", /game.js\?v=350/.test(htmlTxt));

// --- v310 rival AI variety (no rival parry) + mid dart/pause ---
check("AI variety seats no rival parry", /Rival AI variety \(v310\)/.test(srcTxt) && /no rival parry/.test(srcTxt) && /rival AI does not parry/.test(srcTxt) && V.GUARD_COMMIT_MS===140 && V.PARRY_WIN_MS===140);
check("AI link weights 0.4 / 0.55", V.AI_LINK_CHANCE===0.4 && V.AI_LINK_RANGE_CHANCE===0.55 && V.AI_LINK_RANGE_CHANCE>V.AI_LINK_CHANCE);
check("AI close mix weights", V.AI_CLOSE_GOLPE===0.32 && V.AI_CLOSE_SLASH===0.28 && V.AI_CLOSE_BAIT===0.18 && V.AI_CLOSE_RESET===0.14 && Math.abs(V.AI_CLOSE_GOLPE+V.AI_CLOSE_SLASH+V.AI_CLOSE_BAIT+V.AI_CLOSE_RESET-0.92)<1e-9);
check("AI mid dart/pause weights", V.AI_MID_DART===0.72 && V.AI_MID_WALK_PAUSE===0.22 && Math.abs(V.AI_MID_DART+V.AI_MID_WALK_PAUSE-0.94)<1e-9);
check("AI mid path uses mid weights", /AI_MID_DART/.test(sliceFn(codeOnly, "tickAI")) && /AI_MID_WALK_PAUSE/.test(sliceFn(codeOnly, "tickAI")) && /1 - AI_MID_DART/.test(sliceFn(codeOnly, "tickAI")));
check("AI post-reset / bait timers", V.AI_POST_RESET===0.55 && V.AI_RESET_MS===900 && V.AI_BAIT_MS_MIN===380 && V.AI_BAIT_MS_MAX===720 && V.AI_RETREAT_MS_MIN===220 && V.AI_RETREAT_MS_MAX===480);
check("AI variety helpers exist", /function rivalIdleCommit/.test(codeOnly) && /function rivalLinkRoll/.test(codeOnly) && /function rivalDoPostReset/.test(codeOnly) && /aiBaitT/.test(codeOnly) && /aiResetT/.test(codeOnly) && /aiRetreatT/.test(codeOnly) && /aiSawBlock/.test(codeOnly));
check("AI idle commit not pure Space", /rivalIdleCommit\(/.test(sliceFn(codeOnly, "tickAI")) && /startAttack\(rival, "golpe"\)/.test(sliceFn(codeOnly, "rivalIdleCommit")) && /aiBaitT/.test(sliceFn(codeOnly, "rivalIdleCommit")) && /startAttack\(rival\)/.test(sliceFn(codeOnly, "rivalIdleCommit")));
check("AI post block/whiff reset path", /aiSawBlock = true/.test(sliceFn(codeOnly, "landBlock")) && /aiResetT = AI_RESET_MS/.test(codeOnly) && /rivalDoPostReset/.test(sliceFn(codeOnly, "rivalIdleCommit")));
check("AI link in-range bump", /AI_LINK_RANGE_CHANCE/.test(sliceFn(codeOnly, "rivalLinkRoll")) && /AI_LINK_CHANCE/.test(sliceFn(codeOnly, "rivalLinkRoll")) && /rivalLinkRoll\(absGap\(\) <= gReach\)/.test(sliceFn(codeOnly, "armRivalSlashLink")));
check("AI clash doors stay 0.4", /Math\.random\(\) < AI_LINK_CHANCE/.test(sliceFn(codeOnly, "armRivalClashBolt")) && /Math\.random\(\) < AI_LINK_CHANCE/.test(sliceFn(codeOnly, "armRivalClashCut")));
check("AI CDs / frames locked", V.BOLT_AI_CD===1800 && V.THROW_AI_CD===1800 && V.REVERSAL_AI_CD===1800 && V.PUSHBLOCK_AI_CD===1800 && V.FEINT_AI_CD===1800 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.RIPOSTE_STARTUP===130 && V.BOLT_STARTUP===200);
check("AI no rival parry/riposte", !/isPerfectParry/.test(sliceFn(codeOnly, "tickAI")) && !/riposteWindowT/.test(sliceFn(codeOnly, "tickAI")) && /guardChoice/.test(sliceFn(codeOnly, "tickAI")) && V.PARRY_WIN_MS===140);

// live: measure commit with random 0.99 still Space (readable, not bait/reset)
toMeasure();
Math.random = () => 0.99;
freezeYouAI();
V.openLeft = 0; V.hitstopLeft = 0;
V.rival.boltCd = 99999; V.rival.boltPhase = "";
V.rival.stamina = 100; V.rival.guarding = false; V.rival.wantBlock = false;
V.rival.guardChoice = -1; V.rival.aiBaitT = 0; V.rival.aiResetT = 0; V.rival.aiRetreatT = 0;
V.rival.aiSawBlock = false;
const holdV = V.bladeReach(V.rival)+V.LUNGE_PX;
if (V.bodyGap() > holdV - 8) V.rival.x -= (V.bodyGap() - (holdV - 8));
V.rival.standWait = 0; V.rival.standGoal = 1; V.rival.closing = false; V.rival.phase = "idle";
V.rival.cut = "slash";
for (let i = 0; i < 24; i++) { V.update(STEP); if (V.rival.phase === "startup") break; }
check("AI variety live high-roll still slash", V.rival.phase==="startup" && V.rival.cut==="slash", `ph=${V.rival.phase} cut=${V.rival.cut} bait=${V.rival.aiBaitT} gap=${V.absGap().toFixed(1)}`);

// live: low roll prefers golpe when still in golpe reach (idle pocket, not too-close walk-out).
// Place by absGap (what rivalIdleCommit uses). keepApart() floors idle body gap at 120,
// so target >=120 and <= gReach-2 — blade+10 (~116) left only ~3px margin after keepApart.
toMeasure();
Math.random = () => 0.0;
freezeYouAI();
V.openLeft = 0; V.hitstopLeft = 0;
V.rival.boltCd = 99999; V.rival.throwCd = 99999; V.rival.revCd = 99999;
V.rival.stamina = 100; V.rival.guarding = false; V.rival.wantBlock = false;
V.rival.guardChoice = -1; V.rival.aiBaitT = 0; V.rival.aiResetT = 0; V.rival.aiRetreatT = 0;
V.rival.aiSawBlock = false; V.rival.boltPhase = ""; V.rival.boltArmed = false;
V.rival.pushT = 0; V.rival.pushVel = 0; V.player.pushT = 0; V.player.pushVel = 0;
const bladeV = V.bladeReach(V.rival);
const gReachV = bladeV + V.GOLPE_LUNGE_PX;
const holdGapV = bladeV + V.LUNGE_PX;
const tooCloseV = Math.max(20, holdGapV - 48);
const wantGolpeGap = Math.min(gReachV - 2, Math.max(120, tooCloseV + 20));
function nudgeAbsGap(want) {
  const g = V.absGap();
  if (g > want) V.rival.x -= (g - want);
  else if (g < want) V.rival.x += (want - g);
}
nudgeAbsGap(wantGolpeGap);
V.keepApart();
nudgeAbsGap(wantGolpeGap); // re-nudge if keepApart shifted plants
V.rival.standWait = 0; V.rival.standGoal = 1; V.rival.closing = false; V.rival.phase = "idle"; V.rival.cut = "slash"; V.rival.gait = 0;
V.player.phase = "idle"; V.player.stunT = 0; V.player.falling = false; V.player.guarding = false; V.player.gait = 0;
for (let i = 0; i < 30; i++) {
  Math.random = () => 0.0;
  // Hold the golpe pocket if keepApart(dt) / walk jitter creeps out of gReach.
  if (V.rival.phase === "idle" && V.absGap() > gReachV - 1) nudgeAbsGap(wantGolpeGap);
  V.update(STEP);
  if (V.rival.phase === "startup") break;
}
check("AI variety live low-roll golpe", V.rival.phase==="startup" && V.rival.cut==="golpe", `ph=${V.rival.phase} cut=${V.rival.cut} gap=${V.absGap().toFixed(1)} body=${V.bodyGap().toFixed(1)} gReach=${gReachV.toFixed(1)} want=${wantGolpeGap.toFixed(1)}`);

// live: bait band arms hold-guard without player swing
toMeasure();
Math.random = () => 0.70; // in bait band (0.60..0.78)
freezeYouAI();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase = "idle"; V.player.stunT = 0; V.player.falling = false;
V.rival.boltCd = 99999; V.rival.throwCd = 99999;
V.rival.stamina = 100; V.rival.guarding = false; V.rival.wantBlock = false;
V.rival.guardChoice = 0; V.rival.aiBaitT = 0; V.rival.aiResetT = 0; V.rival.aiRetreatT = 0;
if (V.bodyGap() > holdV - 8) V.rival.x -= (V.bodyGap() - (holdV - 8));
V.rival.standWait = 0; V.rival.standGoal = 1; V.rival.closing = false; V.rival.phase = "idle";
for (let i = 0; i < 8; i++) { V.update(STEP); if (V.rival.aiBaitT > 0 || V.rival.wantBlock) break; }
check("AI variety live bait arms hold-guard", V.rival.aiBaitT>0 && V.rival.wantBlock && V.rival.guardChoice===1 && V.rival.phase==="idle", `bait=${V.rival.aiBaitT.toFixed(0)} want=${V.rival.wantBlock} choice=${V.rival.guardChoice} ph=${V.rival.phase} cut=${V.rival.cut}`);

// live: post-whiff reset prefers retreat/dart over instant slash
toMeasure();
Math.random = () => 0.0;
freezeYouAI();
V.openLeft = 0; V.hitstopLeft = 0;
V.rival.boltCd = 99999; V.rival.throwCd = 99999;
V.rival.stamina = 100; V.rival.guarding = false; V.rival.wantBlock = false;
V.rival.guardChoice = -1; V.rival.aiBaitT = 0; V.rival.aiRetreatT = 0; V.rival.aiSawBlock = false;
V.rival.aiResetT = V.AI_RESET_MS;
if (V.bodyGap() > holdV - 8) V.rival.x -= (V.bodyGap() - (holdV - 8));
const xAiReset = V.rival.x;
V.rival.standWait = 0; V.rival.standGoal = 1; V.rival.closing = false; V.rival.phase = "idle";
for (let i = 0; i < 12; i++) { V.update(STEP); if (V.rival.aiRetreatT > 0 || V.rival.boltPhase || V.rival.phase==="startup") break; }
check("AI variety live post-reset retreat", V.rival.aiRetreatT>0 || V.rival.x>xAiReset+1 || V.rival.gait!==0, `ret=${V.rival.aiRetreatT} dx=${(V.rival.x-xAiReset).toFixed(1)} gait=${V.rival.gait} ph=${V.rival.phase} cut=${V.rival.cut} bph=${V.rival.boltPhase}`);

check("cache v=310 after AI variety", /game.js\?v=350/.test(htmlTxt));
check("arte fighters still ?v=304 after AI variety", /you\.png\?v=304/.test(srcTxt) && /rival\.png\?v=304/.test(srcTxt) && /courtyard\.png"\)/.test(srcTxt) && !/courtyard\.png\?v=/.test(srcTxt));

// --- v311 tutorial nudge + escenarios contrast ---
check("tutorial contrast seats header", /Tutorial \/ escenarios contrast \(v311\)/.test(srcTxt) && /teach copy leads with PARRY/.test(srcTxt) && /Rematch \(KO→REVANCHA\) rotates yard/.test(srcTxt) && /Escenarios labels carry a brief light tag/.test(srcTxt) && /sparse one-line tip under REVANCHA/.test(srcTxt));
check("teach HUD parry-first copy", (()=>{ const L=V.controlsHintLines(); return L[1].indexOf("S AL FILO PARRY>")===0 && /PARRY>ESPACIO\/L RIPOSTE/.test(L[1]) && / ESPACIO\+S AGARRE /.test(L[1]) && / S\+L REV/.test(L[1]) && !/\(S FEINT\)/.test(L[1]); })(), V.controlsHintLines().join(" | "));
check("controles page S AL FILO PARRY", /S AL FILO PARRY>/.test(srcTxt) && /titlePage === "controls"/.test(srcTxt));
check("escenarios contrast labels", V.STAGE_LABELS.yard==="PATIO · SOL" && V.STAGE_LABELS.yard2==="PATIO 2 · SOMBRA" && V.STAGE_LABELS.yard3==="PUENTE · OCASO" && V.STAGE_LABELS.yard4==="ARMERÍA · BRASA" && V.STAGE_LABELS.yard5==="MURALLA · LUNA" && /PATIO 2/.test(V.STAGE_LABELS.yard2) && /PUENTE/.test(V.STAGE_LABELS.yard3) && /ARMERÍA/.test(V.STAGE_LABELS.yard4) && /MURALLA/.test(V.STAGE_LABELS.yard5));
check("rotateYardForContrast seats +1 no repeat", /function rotateYardForContrast/.test(srcTxt) && /setYardIndex\(\(yardIndex \+ 1\) % n\)/.test(srcTxt) && /if \(mode === "over"\) rotateYardForContrast/.test(sliceFn(codeOnly, "resetRound")));
check("OVER_TIPS rotating verbs", Array.isArray(V.OVER_TIPS) && V.OVER_TIPS.length===3 && /PARRY/.test(V.OVER_TIPS[0]) && /DARDO/.test(V.OVER_TIPS[1]) && /GOLPE/.test(V.OVER_TIPS[2]) && /drawOverPrompt[\s\S]{0,500}OVER_TIPS/.test(srcTxt));
check("landParry notes teach verb", /noteHintVerb\("parry"\)/.test(sliceFn(codeOnly, "landParry")));
check("tipX / plant locks hold after tutorial contrast", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt));
check("plant you idle still footX 75 after tutorial contrast", V.poseSheet({kind:"you",facing:1,phase:"idle",stunT:0,guarding:false,falling:false,img:null}).footX===75);
check("slash frames still 180/140/280 after tutorial contrast", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after tutorial contrast", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after tutorial contrast", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("riposte frames still 130/140/280 after tutorial contrast", V.RIPOSTE_STARTUP===130 && V.RIPOSTE_ACTIVE===140 && V.RIPOSTE_RECOVERY===280 && V.RIPOSTE_WIN_MS===280);
check("parry constants still 140/180/80 after tutorial contrast", V.PARRY_WIN_MS===140 && V.PARRY_STAGGER_MS===180 && V.PARRY_GLEAM_MS===80);
check("no 6th button after tutorial contrast", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=311 after tutorial contrast", /game.js\?v=350/.test(htmlTxt));
check("arte fighters still ?v=304 after tutorial contrast", /you\.png\?v=304/.test(srcTxt) && /rival\.png\?v=304/.test(srcTxt) && /courtyard\.png"\)/.test(srcTxt) && !/courtyard\.png\?v=/.test(srcTxt));

// live: rematch from over rotates yard; title JUGAR keeps yard
bootPlay();
const y311a = V.yardIndex;
V.mode = "over"; V.yardSwitchT = 0; V.yardPrevIndex = -1;
V.resetRound();
check("tutorial contrast live rematch rotates yard", V.yardIndex===(y311a+1)%V.YARD_KEYS.length && V.yardSwitchT===V.YARD_SWITCH_MS, `y0=${y311a} y1=${V.yardIndex} t=${V.yardSwitchT}`);
const y311b = V.yardIndex;
V.yardSwitchT = 0; V.yardPrevIndex = -1;
V.mode = "title"; V.titlePage = "root";
V.resetRound();
check("tutorial contrast live JUGAR keeps yard", V.yardIndex===y311b && V.yardSwitchT===0, `y=${V.yardIndex} keep=${y311b} t=${V.yardSwitchT}`);
// consecutive rematches never repeat
V.mode = "over"; V.resetRound();
const y311c = V.yardIndex;
V.mode = "over"; V.resetRound();
check("tutorial contrast live consecutive rematch no repeat", V.yardIndex!==y311c && V.yardIndex===(y311c+1)%V.YARD_KEYS.length, `prev=${y311c} next=${V.yardIndex}`);
// over tip arms on KO→over (source) + writable cycle
check("tutorial contrast live over tip advance source", /overTipI = \(overTipI \+ 1\) % OVER_TIPS\.length/.test(srcTxt));
V.overTipI = 0;
check("tutorial contrast live OVER_TIPS[0] parry", /PARRY/.test(V.OVER_TIPS[V.overTipI]));
V.overTipI = 1;
check("tutorial contrast live OVER_TIPS[1] dardo", /DARDO/.test(V.OVER_TIPS[V.overTipI]));
V.overTipI = 2;
check("tutorial contrast live OVER_TIPS[2] golpe", /GOLPE/.test(V.OVER_TIPS[V.overTipI]));
// play-mode resetRound still quiet (no rotate) — leftover lock intent
const y311keep = V.yardIndex;
V.yardSwitchT = 0; V.yardPrevIndex = -1;
V.mode = "play";
V.resetRound();
check("tutorial contrast live play resetRound quiet", V.yardIndex===y311keep && V.yardSwitchT===0, `y=${V.yardIndex} keep=${y311keep} t=${V.yardSwitchT}`);
check("cache v=312 after tutorial contrast live", /game.js\?v=350/.test(htmlTxt));

// --- v312 pushblock / clash readability juice ---
check("pushblock shake stronger than hold-block", V.PUSHBLOCK_SHAKE===8 && V.PUSHBLOCK_SHAKE>4 && V.PUSHBLOCK_SHAKE<V.RIPOSTE_HIT_SHAKE, `pb=${V.PUSHBLOCK_SHAKE}`);
check("clash shake punches harder than old 10", V.CLASH_SHAKE===14 && V.CLASH_SHAKE>10 && V.CLASH_SHAKE>=V.PUNCH_PX, `cl=${V.CLASH_SHAKE} punch=${V.PUNCH_PX}`);
check("clash extra grit above locked scrape 1.2", V.CLASH_FX===1.7 && V.CLASH_FX>1.2 && V.CLASH_FX<V.PUSHBLOCK_FX, `fx=${V.CLASH_FX}`);
const tryPb312 = sliceFn(codeOnly, "tryPushblock");
const pbSting312 = sliceFn(codeOnly, "playPushblockSting");
const clashSting312 = sliceFn(codeOnly, "playClashSting");
const doClash312 = sliceFn(codeOnly, "doClash");
const drawSteel312 = sliceFn(codeOnly, "drawSteelFlash");
const drawClash312 = sliceFn(codeOnly, "drawClashSpark");
const landBlock312 = sliceFn(codeOnly, "landBlock");
check("pushblock connect arms shake + oxido steel",
  /bumpShake\(PUSHBLOCK_SHAKE, away, HITSTOP_BLOCK\)/.test(tryPb312)
  && /spawnSteelFlash\(/.test(tryPb312)
  && /"push"/.test(tryPb312)
  && tryPb312.indexOf("bumpShake(PUSHBLOCK_SHAKE") < tryPb312.indexOf("spawnPlantDust(f, PUSHBLOCK_FX")
  && tryPb312.indexOf("bumpShake(PUSHBLOCK_SHAKE") < tryPb312.indexOf("playPushblockSting()")
  && tryPb312.indexOf("playPushblockSting()") < tryPb312.indexOf("return true")
);
check("pushblock sting still pitched bloqueo+choque",
  /SFX\.bloqueo/.test(pbSting312) && /SFX\.choque/.test(pbSting312) && /rate:/.test(pbSting312)
  && (pbSting312.match(/playSfx\(SFX\.bloqueo/g)||[]).length>=2
  && /lastPushblockSfx = "pushblock"/.test(pbSting312)
);
check("normal block still unpitched bloqueo",
  /playSfx\(SFX\.bloqueo\);/.test(landBlock312) && !/rate:/.test(landBlock312)
  && /lastPushblockSfx = "block"/.test(landBlock312)
  && !/PUSHBLOCK_SHAKE/.test(landBlock312)
  && !/playPushblockSting/.test(landBlock312)
);
check("pushblock steel draw uses oxido palette",
  /steelKind === "push"/.test(drawSteel312)
  && /COL_OXIDO/.test(drawSteel312)
  && /COL_BRASA/.test(drawSteel312)
);
check("clash keeps locked 1.2 scrape + unpitched choque",
  /spawnPlantDust\(player, 1\.2\)/.test(doClash312)
  && /spawnPlantDust\(rival, 1\.2\)/.test(doClash312)
  && /playSfx\(SFX\.choque\);/.test(doClash312)
);
check("clash adds CLASH_FX shove grit + CLASH_SHAKE + layered sting",
  /spawnPlantDust\(player, CLASH_FX, -1\)/.test(doClash312)
  && /spawnPlantDust\(rival, CLASH_FX, 1\)/.test(doClash312)
  && /bumpShake\(CLASH_SHAKE, 1, HITSTOP_BLOCK\)/.test(doClash312)
  && /playClashSting\(\)/.test(doClash312)
  && doClash312.indexOf("playSfx(SFX.choque)") < doClash312.indexOf("playClashSting()")
);
check("clash sting pitches layered choque",
  /function playClashSting/.test(srcTxt)
  && /SFX\.choque/.test(clashSting312)
  && /rate:/.test(clashSting312)
  && (clashSting312.match(/playSfx\(SFX\.choque/g)||[]).length>=2
  && !/new Audio/.test(clashSting312)
);
check("clash spark draw uses pizarra/oxido/brasa/hueso",
  /COL_PIZARRA/.test(drawClash312)
  && /COL_OXIDO/.test(drawClash312)
  && /COL_BRASA/.test(drawClash312)
  && /COL_HUESO/.test(drawClash312)
);
check("v312 juice does not retune frames/damage/pushblock economy",
  V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280
  && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180
  && V.HITSTOP_HIT===140 && V.HITSTOP_BLOCK===60 && V.HITSTUN===350
  && V.SLASH_DMG===10 && V.CLASH_RECOVERY===200 && V.CLASH_SPARK_MS===110
  && V.PUSHBLOCK_STAM===25 && V.PUSHBLOCK_PX===240 && V.PUSHBLOCK_FX===1.85
  && V.PUNCH_PX===12 && V.STEEL_FLASH_MS===60
);
check("v312 no new combat verb / no 6th button",
  (htmlTxt.match(/data-tap=/g)||[]).length===3
  && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt)
  && !/zone-crush|zone-pushblock/.test(htmlTxt)
);

// live: pushblock arms stronger shake + push steel kind
bootPlay();
toClose();
raiseGuardInRoom();
V.keys.add("KeyA");
V.update(STEP);
check("pushblock live shake mag", V.lastPushblockSfx==="pushblock" && V.shakeMag===V.PUSHBLOCK_SHAKE && V.player.pushT>0, `sfx=${V.lastPushblockSfx} mag=${V.shakeMag} pushT=${V.player.pushT}`);
check("pushblock live oxido steel kind", V.steelKind==="push" && V.steelFlashT>0, `kind=${V.steelKind} t=${V.steelFlashT}`);
V.keys.clear();

// live: normal block keeps block steel kind
toMeasure();
Math.random = () => 0;
V.rival.pbArmed = -1;
V.rival.pbCd = 99999;
V.rival.revArmed = -1;
raiseGuardInRoom();
V.player.stamina = 100;
V.rival.stamina = 100;
V.rival.x = V.player.x + 200;
V.startAttack(V.rival, "slash");
let blockLive312 = false;
for (let i = 0; i < 80; i++) {
  V.update(STEP);
  if (V.lastPushblockSfx==="block" && V.steelFlashT>0) { blockLive312 = true; break; }
}
check("normal block live steel kind block", blockLive312 && V.steelKind==="block" && V.lastPushblockSfx==="block", `live=${blockLive312} kind=${V.steelKind} sfx=${V.lastPushblockSfx} mag=${V.shakeMag}`);

// synthetic clash: locked 1.2 still present + CLASH_FX shove trails + shake
bootPlay();
V.plantDust.splice(0);
V.doClash(640, 360);
const cStamps312 = (V.plantDust||[]).filter(p=>!p.speck && !p.shove);
const cScrapes312 = cStamps312.filter(p=>Math.abs((p.power||0)-1.2)<0.01);
const cShoves312 = (V.plantDust||[]).filter(p=>p.shove && !p.speck);
check("clash juice keeps 1.2 scrape both boots", cScrapes312.length===2, `scrape=${cScrapes312.length}`);
check("clash juice adds CLASH_FX shove stamps", cShoves312.filter(p=>Math.abs((p.power||0)-V.CLASH_FX)<0.01).length>=2, `shoves=${cShoves312.map(p=>p.power).join(",")}`);
check("clash juice shake mag", V.shakeMag===V.CLASH_SHAKE && V.hitstopLeft===V.HITSTOP_BLOCK, `mag=${V.shakeMag} stop=${V.hitstopLeft}`);
check("clash juice layered sting flag", V.lastClashSfx==="clash", `sfx=${V.lastClashSfx}`);
check("cache v=312 after pushblock/clash juice", /game.js\?v=350/.test(htmlTxt));


// --- v313 throw-tech / tech-clash readability juice ---
check("tech shake between pushblock and clash", V.TECH_SHAKE===9 && V.TECH_SHAKE>V.PUSHBLOCK_SHAKE && V.TECH_SHAKE<V.CLASH_SHAKE, `tech=${V.TECH_SHAKE} pb=${V.PUSHBLOCK_SHAKE} cl=${V.CLASH_SHAKE}`);
check("tech grit above locked scrape 1.1 under clash", V.TECH_FX===1.5 && V.TECH_FX>1.1 && V.TECH_FX<V.CLASH_FX && V.TECH_FX<V.PUSHBLOCK_FX, `fx=${V.TECH_FX}`);
const landTech313 = sliceFn(codeOnly, "landThrowTech");
const techSting313 = sliceFn(codeOnly, "playThrowTechSting");
const drawSteel313 = sliceFn(codeOnly, "drawSteelFlash");
const landThrow313 = sliceFn(codeOnly, "landThrow");
const landBlock313b = sliceFn(codeOnly, "landBlock");
check("throw tech keeps locked 1.1 scrape + unpitched choque",
  /spawnPlantDust\(atk, 1\.1\)/.test(landTech313)
  && /spawnPlantDust\(def, 1\.1\)/.test(landTech313)
  && /playSfx\(SFX\.choque\);/.test(landTech313)
);
check("throw tech adds TECH_FX shove grit + TECH_SHAKE + tech steel + sting",
  /spawnPlantDust\(atk, TECH_FX, -dir\)/.test(landTech313)
  && /spawnPlantDust\(def, TECH_FX, dir\)/.test(landTech313)
  && /bumpShake\(TECH_SHAKE, dir, HITSTOP_BLOCK\)/.test(landTech313)
  && /"tech"/.test(landTech313)
  && /spawnSteelFlash\(/.test(landTech313)
  && /playThrowTechSting\(\)/.test(landTech313)
  && landTech313.indexOf("playSfx(SFX.choque)") < landTech313.indexOf("playThrowTechSting()")
);
check("throw tech sting pitches layered choque+bloqueo",
  /function playThrowTechSting/.test(srcTxt)
  && /SFX\.choque/.test(techSting313)
  && /SFX\.bloqueo/.test(techSting313)
  && /rate:/.test(techSting313)
  && (techSting313.match(/playSfx\(SFX\.choque/g)||[]).length>=2
  && /lastThrowTechSfx = "tech"/.test(techSting313)
  && !/new Audio/.test(techSting313)
);
check("grab connect keeps unpitched impacto + not tech steel",
  /playSfx\(SFX\.impacto\);/.test(landThrow313)
  && /bumpShake\(GRAB_SHAKE,/.test(landThrow313)
  && /playGrabConnectSting\(\)/.test(landThrow313)
  && /spawnBrasaFx\("grab"/.test(landThrow313)
  && !/TECH_SHAKE/.test(landThrow313)
  && !/playThrowTechSting/.test(landThrow313)
  && !/"tech"/.test(landThrow313)
);
check("normal block still unpitched bloqueo after tech juice",
  /playSfx\(SFX\.bloqueo\);/.test(landBlock313b) && !/rate:/.test(landBlock313b)
  && /lastPushblockSfx = "block"/.test(landBlock313b)
  && !/TECH_SHAKE/.test(landBlock313b)
  && !/playThrowTechSting/.test(landBlock313b)
);
check("tech steel draw uses hueso/brasa/pizarra palette",
  /steelKind === "tech"/.test(drawSteel313)
  && /COL_HUESO/.test(drawSteel313)
  && /COL_BRASA/.test(drawSteel313)
  && /COL_PIZARRA/.test(drawSteel313)
);
check("v313 juice does not retune frames/damage/tech window",
  V.THROW_STARTUP===80 && V.THROW_ACTIVE===40 && V.THROW_RECOVERY===220
  && V.THROW_DMG===20 && V.THROW_TECH_MS===80 && V.THROW_TECH_REC===160
  && V.THROW_TECH_CHANCE===0.45 && V.HITSTOP_BLOCK===60
  && V.PUSHBLOCK_SHAKE===8 && V.CLASH_SHAKE===14 && V.CLASH_FX===1.7
  && V.PUSHBLOCK_STAM===25 && V.PUSHBLOCK_PX===240 && V.STEEL_FLASH_MS===60
);
check("v313 no new combat verb / no 6th button",
  (htmlTxt.match(/data-tap=/g)||[]).length===3
  && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt)
  && !/zone-tech|zone-crush/.test(htmlTxt)
);

// live: tech arms TECH_SHAKE + tech steel + sting (tryThrowTech path)
toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100; V.rival.stamina = 100;
V.player.thrownT = 0; V.rival.thrownT = 0;
V.plantDust.splice(0);
fireThrow();
let sawThrowStart313=false;
for(let i=0;i<20;i++){
  holdRivalStill();
  V.update(STEP);
  if(V.player.cut==="throw" && V.player.phase==="startup"){ sawThrowStart313=true; break; }
}
const techLive313 = sawThrowStart313 && V.tryThrowTech(V.rival);
check("throw tech live shake mag", techLive313 && V.lastThrowTechSfx==="tech" && V.shakeMag===V.TECH_SHAKE && V.player.techRec && V.rival.techRec, `ok=${techLive313} sfx=${V.lastThrowTechSfx} mag=${V.shakeMag} ptech=${V.player.techRec}`);
check("throw tech live hueso/brasa steel kind", techLive313 && V.steelKind==="tech" && V.steelFlashT>0, `ok=${techLive313} kind=${V.steelKind} t=${V.steelFlashT}`);

// synthetic landThrowTech: locked 1.1 + TECH_FX shove + shake
bootPlay();
V.plantDust.splice(0);
V.landThrowTech(V.player, V.rival);
const tStamps313 = (V.plantDust||[]).filter(p=>!p.speck && !p.shove);
const tScrapes313 = tStamps313.filter(p=>Math.abs((p.power||0)-1.1)<0.01);
const tShoves313 = (V.plantDust||[]).filter(p=>p.shove && !p.speck);
check("tech juice keeps 1.1 scrape both boots", tScrapes313.length===2, `scrape=${tScrapes313.length}`);
check("tech juice adds TECH_FX shove stamps", tShoves313.filter(p=>Math.abs((p.power||0)-V.TECH_FX)<0.01).length>=2, `shoves=${tShoves313.map(p=>p.power).join(",")}`);
check("tech juice shake mag", V.shakeMag===V.TECH_SHAKE, `mag=${V.shakeMag}`);
check("tech juice layered sting flag", V.lastThrowTechSfx==="tech", `sfx=${V.lastThrowTechSfx}`);
check("cache v=314 after throw-tech juice", /game.js\?v=350/.test(htmlTxt));

// --- v314 grab-connect readability juice ---
check("grab shake between hit 10 and riposte", V.GRAB_SHAKE===11 && V.GRAB_SHAKE>10 && V.GRAB_SHAKE<V.RIPOSTE_HIT_SHAKE && V.GRAB_SHAKE>V.TECH_SHAKE, `grab=${V.GRAB_SHAKE} tech=${V.TECH_SHAKE} rip=${V.RIPOSTE_HIT_SHAKE}`);
check("grab puff brief under hit spark", V.GRAB_FX_MS===90 && V.GRAB_FX_MS<V.HIT_SPARK_MS && V.GRAB_FX_MS>V.STEEL_FLASH_MS, `ms=${V.GRAB_FX_MS}`);
const landThrow314 = sliceFn(codeOnly, "landThrow");
const grabSting314 = sliceFn(codeOnly, "playGrabConnectSting");
const spawnBrasa314 = sliceFn(codeOnly, "spawnBrasaFx");
const drawBrasa314 = sliceFn(codeOnly, "drawBrasaFx");
const landTech314 = sliceFn(codeOnly, "landThrowTech");
const landBlock314 = sliceFn(codeOnly, "landBlock");
const landHit314 = sliceFn(codeOnly, "landHit");
check("grab connect keeps unpitched impacto first + sting + GRAB_SHAKE + grab puff",
  /playSfx\(SFX\.impacto\);/.test(landThrow314)
  && /playGrabConnectSting\(\)/.test(landThrow314)
  && landThrow314.indexOf("playSfx(SFX.impacto)") < landThrow314.indexOf("playGrabConnectSting()")
  && /bumpShake\(GRAB_SHAKE, atk\.facing, HITSTOP_HIT\)/.test(landThrow314)
  && /spawnBrasaFx\("grab"/.test(landThrow314)
  && /HITSTOP_HIT/.test(landThrow314)
  && /THROW_DMG/.test(landThrow314)
);
check("grab sting pitches layered impacto",
  /function playGrabConnectSting/.test(srcTxt)
  && /SFX\.impacto/.test(grabSting314)
  && /rate:/.test(grabSting314)
  && (grabSting314.match(/playSfx\(SFX\.impacto/g)||[]).length>=2
  && /lastGrabSfx = "grab"/.test(grabSting314)
  && !/new Audio/.test(grabSting314)
  && !/SFX\.choque/.test(grabSting314)
);
check("grab puff spawn/draw uses brasa/hueso/pizarra",
  /kind === "grab"/.test(spawnBrasa314)
  && /GRAB_FX_MS/.test(spawnBrasa314)
  && /brasaFxKind === "grab"/.test(drawBrasa314)
  && /COL_BRASA/.test(drawBrasa314)
  && /COL_HUESO/.test(drawBrasa314)
  && /COL_PIZARRA/.test(drawBrasa314)
);
check("tech path still distinct after grab juice",
  /bumpShake\(TECH_SHAKE, dir, HITSTOP_BLOCK\)/.test(landTech314)
  && /playThrowTechSting\(\)/.test(landTech314)
  && /"tech"/.test(landTech314)
  && !/GRAB_SHAKE/.test(landTech314)
  && !/playGrabConnectSting/.test(landTech314)
  && !/spawnBrasaFx\("grab"/.test(landTech314)
);
check("normal block still unpitched bloqueo after grab juice",
  /playSfx\(SFX\.bloqueo\);/.test(landBlock314) && !/rate:/.test(landBlock314)
  && !/GRAB_SHAKE/.test(landBlock314)
  && !/playGrabConnectSting/.test(landBlock314)
);
check("normal hit still shake 10 after grab juice",
  /bumpShake\(10, dir, HITSTOP_HIT\)/.test(landHit314)
  && !/GRAB_SHAKE/.test(landHit314)
  && !/playGrabConnectSting/.test(landHit314)
  && !/spawnBrasaFx\("grab"/.test(landHit314)
);
check("v314 juice does not retune frames/damage/tech window",
  V.THROW_STARTUP===80 && V.THROW_ACTIVE===40 && V.THROW_RECOVERY===220
  && V.THROW_DMG===20 && V.THROW_TECH_MS===80 && V.THROW_TECH_REC===160
  && V.THROW_TECH_CHANCE===0.45 && V.HITSTOP_HIT===140 && V.HITSTOP_BLOCK===60
  && V.TECH_SHAKE===9 && V.PUSHBLOCK_SHAKE===8 && V.CLASH_SHAKE===14
  && V.PUSHBLOCK_STAM===25 && V.PUSHBLOCK_PX===240 && V.STEEL_FLASH_MS===60
);
check("v314 no new combat verb / no 6th button",
  (htmlTxt.match(/data-tap=/g)||[]).length===3
  && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt)
  && !/zone-grab|zone-tech|zone-crush/.test(htmlTxt)
);

// synthetic landThrow: GRAB_SHAKE + grab puff + sting
bootPlay();
V.openLeft = 0;
V.plantDust.splice(0);
V.landThrow(V.player, V.rival);
check("grab juice shake mag", V.shakeMag===V.GRAB_SHAKE && V.hitstopLeft===V.HITSTOP_HIT, `mag=${V.shakeMag} hs=${V.hitstopLeft}`);
check("grab juice brasa puff kind", V.brasaFxKind==="grab" && V.brasaFxT===V.GRAB_FX_MS, `kind=${V.brasaFxKind} t=${V.brasaFxT}`);
check("grab juice layered sting flag", V.lastGrabSfx==="grab", `sfx=${V.lastGrabSfx}`);
check("grab juice still applies THROW_DMG + KD", V.rival.hp===80 && V.rival.thrownT===V.THROW_KD_MS, `hp=${V.rival.hp} th=${V.rival.thrownT}`);

// live: throw connect arms grab juice (no tech)
toClose();
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100; V.rival.stamina = 100;
V.player.thrownT = 0; V.rival.thrownT = 0;
V.rival.throwTechArmed = -1;
fireThrow();
let grabLive314=false;
for(let i=0;i<40;i++){
  holdRivalStill();
  V.update(STEP);
  if(V.lastGrabSfx==="grab" && V.rival.thrownT>0){ grabLive314=true; break; }
}
check("grab connect live shake mag", grabLive314 && V.shakeMag===V.GRAB_SHAKE && V.rival.hp===80 && !V.player.techRec, `ok=${grabLive314} mag=${V.shakeMag} hp=${V.rival.hp} tech=${V.player.techRec}`);
check("grab connect live brasa puff", grabLive314 && V.brasaFxKind==="grab" && V.brasaFxT>0, `ok=${grabLive314} kind=${V.brasaFxKind} t=${V.brasaFxT}`);
check("cache v=314 after grab-connect juice", /game.js\?v=350/.test(htmlTxt));


// --- v315 damage number distinct leftover: hit / chip / throw floats ---
check("dmg distinct leftover seats hit/chip/throw apart", /Damage number distinct leftover/.test(srcTxt) && /same −N family/.test(srcTxt) && /dart chip looked like a soft clean hit/.test(srcTxt) && /Hit hueso 4/.test(srcTxt) && /ASCII "-"/.test(srcTxt) && /throw\/grab brasa 3/.test(srcTxt));
check("dmg distinct leftover is draw-only", /Draw-only \(drawDmgNums \/ spawnDmgNum kind\)/.test(srcTxt) && /Ride\/rise\/trail\/MS locked/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && !/dmgNums/.test(sliceFn(codeOnly, "destRect")) && !/dmgNums/.test(sliceFn(codeOnly, "bodyAABB")) && !/kind === "throw"/.test(sliceFn(codeOnly, "landHit")) && !/kind === "throw"/.test(sliceFn(codeOnly, "landBoltHit")));
const drawDmg315 = sliceFn(codeOnly, "drawDmgNums");
const spawnDmg315 = sliceFn(codeOnly, "spawnDmgNum");
const landThrow315b = sliceFn(codeOnly, "landThrow");
const landBoltBlock315 = sliceFn(codeOnly, "landBoltBlock");
const landHit315b = sliceFn(codeOnly, "landHit");
check("dmg distinct hit is bright hueso larger", /COL_HUESO/.test(drawDmg315) && /scale = 4/.test(drawDmg315) && /"−" \+ p\.amt/.test(drawDmg315));
check("dmg distinct chip is muted oxido smaller with ASCII hyphen", /kind === "chip" \|\| p\.chip/.test(drawDmg315) && /scale = 2/.test(drawDmg315) && /COL_OXIDO/.test(drawDmg315) && /"-" \+ p\.amt/.test(drawDmg315) && /COL_PIZARRA/.test(drawDmg315));
check("dmg distinct throw is brasa third tint", /kind === "throw"/.test(drawDmg315) && /scale = 3/.test(drawDmg315) && /COL_BRASA/.test(drawDmg315));
check("dmg distinct spawn carries kind", /kind \|\| \(chip \? "chip" : "hit"\)/.test(spawnDmg315) && /kind: k/.test(spawnDmg315) && /function spawnDmgNum\(x, y, amount, chip, def, kind\)/.test(srcTxt));
check("dmg distinct throw hook passes throw kind", /spawnDmgNum\(bb\.x \+ bb\.w \* 0\.5, bb\.y \+ bb\.h \* 0\.28, before - def\.hp, false, def, "throw"\)/.test(landThrow315b));
check("dmg distinct hit/chip hooks still false/true", /spawnDmgNum\(pt\.x, pt\.y, before - def\.hp, false, def\)/.test(landHit315b) && /spawnDmgNum\(pt\.x, pt\.y, before - def\.hp, true, def\)/.test(landBoltBlock315));
check("dmg distinct trail ghosts still ride", /for \(let g = 3; g >= 1; g--\)/.test(drawDmg315) && /u - g \* 0\.08/.test(drawDmg315) && /fade \* \(0\.28 \/ g\)/.test(drawDmg315));
check("dmg distinct ride/rise/MS unchanged", V.DMG_NUM_MS === 600 && V.DMG_NUM_RISE === 50 && /plantDX/.test(spawnDmg315) && /homeYou/.test(spawnDmg315) && /hitWoundAnchor/.test(sliceFn(codeOnly, "syncDmgNums")));
check("dmg distinct does not retune combat math", V.SLASH_DMG===10 && V.BOLT_CHIP===2 && V.BOLT_SUPER_CHIP===6 && V.BOLT_SUPER_DMG===28 && V.THROW_DMG===20 && V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.GRAB_SHAKE===11 && V.TECH_SHAKE===9 && V.PUSHBLOCK_SHAKE===8);
check("dmg distinct frames still locked", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.THROW_STARTUP===80 && V.THROW_ACTIVE===40 && V.THROW_RECOVERY===220);
check("dmg distinct no new combat verb / no 6th button", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt) && !/zone-dmg|zone-chip/.test(htmlTxt));

// synthetic floats: hit / chip / throw kinds
V.dmgNums.length = 0;
V.spawnDmgNum(640, 360, 10, false, V.rival);
V.spawnDmgNum(640, 360, 2, true, V.rival);
V.spawnDmgNum(640, 360, 20, false, V.rival, "throw");
const dnHit = V.dmgNums[V.dmgNums.length - 3];
const dnChip = V.dmgNums[V.dmgNums.length - 2];
const dnThrow = V.dmgNums[V.dmgNums.length - 1];
check("dmg distinct live hit kind/hueso path", dnHit && dnHit.chip === false && dnHit.kind === "hit" && dnHit.amt === 10, `kind=${dnHit&&dnHit.kind} chip=${dnHit&&dnHit.chip} amt=${dnHit&&dnHit.amt}`);
check("dmg distinct live chip kind/hyphen path", dnChip && dnChip.chip === true && dnChip.kind === "chip" && dnChip.amt === 2, `kind=${dnChip&&dnChip.kind} chip=${dnChip&&dnChip.chip} amt=${dnChip&&dnChip.amt}`);
check("dmg distinct live throw kind/brasa path", dnThrow && dnThrow.chip === false && dnThrow.kind === "throw" && dnThrow.amt === 20, `kind=${dnThrow&&dnThrow.kind} chip=${dnThrow&&dnThrow.chip} amt=${dnThrow&&dnThrow.amt}`);

// landThrow still spawns throw-kind float + THROW_DMG
bootPlay();
V.dmgNums.length = 0;
V.openLeft = 0;
V.landThrow(V.player, V.rival);
const throwNum = V.dmgNums[0];
check("dmg distinct landThrow spawns throw float", throwNum && throwNum.kind === "throw" && throwNum.amt === 20 && throwNum.chip === false && V.rival.hp === 80, `kind=${throwNum&&throwNum.kind} amt=${throwNum&&throwNum.amt} hp=${V.rival.hp}`);

// landBoltBlock still chip
bootPlay();
V.dmgNums.length = 0;
V.player.hp = 100; V.rival.hp = 100;
// use spawn path directly for chip already covered; also verify hook string above
check("cache v=317 after dmg distinct leftover", /game.js\?v=350/.test(htmlTxt));


// --- v316 dust / hit spark polish: cleaner flesh sparks + quieter knock grit ---
check("hit spark polish seats fewer sharper streaks", /Dust \/ hit spark polish/.test(srcTxt) && /fewer muddy particles/.test(srcTxt) && /sharper brasa\/hueso streaks/.test(srcTxt) && /shorter post-freeze life/.test(srcTxt) && /9 wide muddy fans/.test(srcTxt));
check("hit spark polish is draw-only", /Draw-only\. Combat math \/ dmg distinct v315 locked/.test(srcTxt) && !/HIT_SPARK_MS/.test(sliceFn(codeOnly, "destRect")) && !/HIT_SPARK_MS/.test(sliceFn(codeOnly, "bodyAABB")) && !/hitShards/.test(sliceFn(codeOnly, "landHit")) && !/HIT_SPARK_MS/.test(sliceFn(codeOnly, "slashPose")));
const spawnHit316 = sliceFn(codeOnly, "spawnHitSpark");
const drawHit316 = sliceFn(codeOnly, "drawHitSpark");
const spawnDust316 = sliceFn(codeOnly, "spawnPlantDust");
const drawSteel316 = sliceFn(codeOnly, "drawSteelFlash");
check("hit spark polish shorter life 100", V.HIT_SPARK_MS===100 && V.HIT_SPARK_MS<140 && V.HIT_SPARK_MS>V.GRAB_FX_MS && V.HIT_SPARK_MS>V.STEEL_FLASH_MS, `ms=${V.HIT_SPARK_MS}`);
check("brasa hit ember life stays 140 after spark polish", V.BRASA_HIT_MS===140 && /brasaFxT = BRASA_HIT_MS/.test(sliceFn(codeOnly, "spawnBrasaFx")) && /BRASA_HIT_MS\)\)\)\)\)\);/.test(sliceFn(codeOnly, "drawBrasaFx")));
check("hit spark polish five tight shards", /i < 5/.test(spawnHit316) && /1\.35/.test(spawnHit316) && /i % 2 === 0/.test(spawnHit316) && !/i < 9/.test(spawnHit316) && !/2\.2/.test(spawnHit316));
check("hit spark polish draw uses palette streaks", /COL_BRASA/.test(drawHit316) && /COL_HUESO/.test(drawHit316) && /fade = k \* k/.test(drawHit316) && /lineCap = "butt"/.test(drawHit316) && /0\.55 \+ 0\.75 \* grow/.test(drawHit316) && !/#ff7040/.test(drawHit316) && !/#fff1e0/.test(drawHit316) && !/lineCap = "round"/.test(drawHit316));
check("hit spark polish no muddy soft wash", !/#c42818/.test(drawHit316) && !/2 \+ 18 \* grow/.test(drawHit316) && !/4 \+ 11 \* grow/.test(drawHit316) && !/arc\(c \* outer, sn \* outer, 2\.6/.test(drawHit316) && /fillRect\(c \* outer - 1/.test(drawHit316));
check("steelKind block/push/tech still distinct after spark polish", /steelKind === "push"/.test(drawSteel316) && /steelKind === "tech"/.test(drawSteel316) && /COL_OXIDO/.test(drawSteel316) && /COL_HUESO/.test(drawSteel316) && /COL_BRASA/.test(drawSteel316) && /#e8e2d2/.test(drawSteel316) && /kind === "push" \? "push"/.test(sliceFn(codeOnly, "spawnSteelFlash")));
check("dust polish quieter heavy grit specks", /Dust polish \(v316\)/.test(srcTxt) && /power > 1\.1 \? 5 : 4/.test(spawnDust316) && /shove \? 190 : 145/.test(spawnDust316) && /\+ i \* 16/.test(spawnDust316) && /shove \? 7 : \(power > 1\.1 \? 5 : 4\)/.test(spawnDust316));
check("hit spark polish ride/sync unchanged", /hitWoundDX/.test(spawnHit316) && /hitWoundAnchor/.test(sliceFn(codeOnly, "syncHitSpark")) && /a\.x \+ hitWoundDX/.test(sliceFn(codeOnly, "syncHitSpark")) && /spawnHitSpark\(pt\.x, pt\.y, atk\.facing, def\)/.test(sliceFn(codeOnly, "landHit")));
check("hit spark polish does not retune combat math", V.SLASH_DMG===10 && V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.KNOCK_PX===80 && V.KNOCK_MS===180 && V.STEEL_FLASH_MS===60 && V.CLASH_SPARK_MS===110 && V.GRAB_FX_MS===90 && V.GRAB_SHAKE===11 && V.TECH_SHAKE===9 && V.PUSHBLOCK_SHAKE===8 && V.CLASH_SHAKE===14);
check("hit spark polish frames still locked", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.THROW_STARTUP===80 && V.THROW_ACTIVE===40 && V.THROW_RECOVERY===220);
check("hit spark polish no new combat verb / no 6th button", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt) && !/zone-spark|zone-dust/.test(htmlTxt));
check("dmg distinct still locked after spark polish", V.DMG_NUM_MS===600 && V.DMG_NUM_RISE===50 && /scale = 4/.test(sliceFn(codeOnly, "drawDmgNums")) && /kind === "throw"/.test(sliceFn(codeOnly, "drawDmgNums")));

// live: spawnHitSpark builds 5 shards + arms 100ms
bootPlay();
V.hitShards && (V.hitShards.length = 0);
V.spawnHitSpark(640, 360, 1, V.rival);
check("hit spark polish live shard count/life", V.hitSparkT===V.HIT_SPARK_MS && V.HIT_SPARK_MS===100 && (V.hitShards||[]).length===5, `t=${V.hitSparkT} n=${(V.hitShards||[]).length}`);
const brasaN = (V.hitShards||[]).filter(s=>s.brasa).length;
const huesoN = (V.hitShards||[]).filter(s=>!s.brasa).length;
check("hit spark polish live brasa/hueso mix", brasaN===3 && huesoN===2, `brasa=${brasaN} hueso=${huesoN}`);

// live: heavy knock grit quieter than old 6-speck; shove still dense
V.plantDust.splice(0);
V.spawnPlantDust(V.rival, 1.15);
const knockSpecks = (V.plantDust||[]).filter(p=>p.speck && !p.shove);
const knockStamp = (V.plantDust||[]).filter(p=>!p.speck && !p.shove);
check("dust polish live knock speck count", knockSpecks.length===5 && knockStamp.length===1, `specks=${knockSpecks.length} stamps=${knockStamp.length}`);
check("dust polish live knock speck life quieter", knockSpecks.every(p=>p.life<=145+4*16) && knockSpecks[0].life===145, `lives=${knockSpecks.map(p=>p.life).join(",")}`);
V.plantDust.splice(0);
V.spawnPlantDust(V.player, V.PUSHBLOCK_FX, -1);
const shoveSpecks = (V.plantDust||[]).filter(p=>p.speck && p.shove);
check("dust polish shove trail still dense 7", shoveSpecks.length===7 && V.PUSHBLOCK_FX===1.85, `n=${shoveSpecks.length}`);

// steel kinds still arm distinctly
bootPlay();
V.spawnSteelFlash(640, 360, V.player, V.rival, true, "push");
check("steel push kind still after spark polish", V.steelKind==="push" && V.steelFlashT===V.STEEL_FLASH_MS, `kind=${V.steelKind} t=${V.steelFlashT}`);
V.spawnSteelFlash(640, 360, V.player, V.rival, true, "tech");
check("steel tech kind still after spark polish", V.steelKind==="tech", `kind=${V.steelKind}`);
V.spawnSteelFlash(640, 360, V.player, V.rival, false);
check("steel block kind still after spark polish", V.steelKind==="block", `kind=${V.steelKind}`);
check("cache v=317 after dust hit spark polish", /game.js\?v=350/.test(htmlTxt));


// --- v317 feint cancel readability juice: whoosh-down sheath sting + tip hueso fleck ---
check("feint juice seats whoosh-down sheath sting", /Feint cancel readability \(v317\)/.test(srcTxt) && /whoosh-down/.test(srcTxt) && /soft sheath sting/.test(srcTxt) && /tiny tip hueso fleck/.test(srcTxt) && /not hit bloom, not parry gleam/.test(srcTxt));
check("feint juice is juice-only", /Juice only — FEINT_RECOVERY \/ frames \/ tipX \/ plants \/ pad \/ sparks v316/.test(srcTxt) && /dmg nums \/ grab-tech-pushblock \/ parry-riposte locked/.test(srcTxt) && !/FEINT_FX_MS/.test(sliceFn(codeOnly, "destRect")) && !/FEINT_FX_MS/.test(sliceFn(codeOnly, "bodyAABB")) && !/playFeintSting/.test(sliceFn(codeOnly, "slashPose")) && !/playFeintSting/.test(sliceFn(codeOnly, "canFeint")));
const feintSting317 = sliceFn(codeOnly, "playFeintSting");
const startFeint317 = sliceFn(codeOnly, "startFeint");
const spawnBrasa317 = sliceFn(codeOnly, "spawnBrasaFx");
const drawBrasa317 = sliceFn(codeOnly, "drawBrasaFx");
const syncBrasa317 = sliceFn(codeOnly, "syncBrasaFx");
check("feint sting pitches whoosh-down + soft bloqueo",
  /function playFeintSting/.test(srcTxt)
  && /lastFeintSfx = "feint"/.test(feintSting317)
  && /SFX\.whoosh/.test(feintSting317) && /SFX\.bloqueo/.test(feintSting317)
  && /rate: 0\.66/.test(feintSting317) && /rate: 1\.08/.test(feintSting317) && /rate: 1\.18/.test(feintSting317)
  && !/SFX\.impacto/.test(feintSting317) && !/SFX\.choque/.test(feintSting317)
  && !/SFX\.riposte/.test(feintSting317)
);
check("feint sting distinct from parry / pushblock / slash whoosh",
  /rate: 1\.48/.test(sliceFn(codeOnly, "playParrySting"))
  && /rate: 0\.66/.test(sliceFn(codeOnly, "playPushblockSting"))
  && /playSfx\(SFX\.whoosh\);/.test(sliceFn(codeOnly, "advanceAttack"))
  && !/rate: 1\.28/.test(startFeint317)
);
check("startFeint arms feint sting + tip fleck + dust",
  /playFeintSting\(\)/.test(startFeint317)
  && /spawnBrasaFx\("feint", bladeTipX\(f\), bladeTipY\(f\), f\.facing, f\)/.test(startFeint317)
  && /spawnPlantDust\(f, 0\.9\)/.test(startFeint317)
  && startFeint317.indexOf("playFeintSting()") < startFeint317.indexOf('spawnBrasaFx("feint"')
  && !/bumpShake/.test(startFeint317)
  && !/hitFlashT/.test(startFeint317)
  && !/spawnSteelFlash/.test(startFeint317)
  && !/spawnParryGleam/.test(startFeint317)
);
check("feint FX brief tip fleck 55ms", V.FEINT_FX_MS===55 && V.FEINT_FX_MS<V.STEEL_FLASH_MS && V.FEINT_FX_MS<V.GRAB_FX_MS && V.FEINT_FX_MS<V.HIT_SPARK_MS, `ms=${V.FEINT_FX_MS}`);
check("feint FX spawn rides tip / hueso bits",
  /kind === "feint"/.test(spawnBrasa317)
  && /brasaFxT = FEINT_FX_MS/.test(spawnBrasa317)
  && /bladeTipX\(home\)/.test(spawnBrasa317)
  && /i < 3/.test(spawnBrasa317)
  && /"hueso"/.test(spawnBrasa317) && /"pizarra"/.test(spawnBrasa317)
);
check("feint FX sync/draw discreet tip fleck",
  /brasaFxKind === "feint"/.test(syncBrasa317)
  && /bladeTipX\(home\)/.test(syncBrasa317)
  && /brasaFxKind === "feint"/.test(drawBrasa317)
  && /COL_HUESO/.test(drawBrasa317)
  && /FEINT_FX_MS/.test(drawBrasa317)
  && /Discreet tip sheath fleck/.test(srcTxt)
  && /not hit bloom \/ parry gleam/.test(srcTxt)
  && /Slight down drift reads as tip dip/.test(srcTxt)
);
check("feint juice does not retune feint rules / frames",
  V.FEINT_RECOVERY===100 && V.FEINT_AI_CD===1800 && V.FEINT_AI_CHANCE===0.4
  && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36
  && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180
  && /f\.feintT = FEINT_RECOVERY/.test(startFeint317)
  && /function canFeint/.test(srcTxt)
);
check("feint juice tipX / plants / pad locked",
  /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt)
  && (htmlTxt.match(/data-tap=/g)||[]).length===3
  && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt)
  && !/zone-feint/.test(htmlTxt)
);
check("sparks v316 / dmg nums / grab-tech-pushblock / parry still locked after feint juice",
  V.HIT_SPARK_MS===100 && V.BRASA_HIT_MS===140 && V.DMG_NUM_MS===600 && V.DMG_NUM_RISE===50
  && V.GRAB_SHAKE===11 && V.GRAB_FX_MS===90 && V.TECH_SHAKE===9 && V.PUSHBLOCK_SHAKE===8 && V.CLASH_SHAKE===14
  && V.PARRY_WIN_MS===140 && V.PARRY_STAGGER_MS===180 && V.PARRY_GLEAM_MS===80
  && V.RIPOSTE_STARTUP===130 && /scale = 4/.test(sliceFn(codeOnly, "drawDmgNums"))
  && /i < 5/.test(sliceFn(codeOnly, "spawnHitSpark"))
);

// synthetic: startFeint arms sting + fleck
bootPlay();
V.openLeft = 0;
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100; V.rival.stamina = 100;
V.player.phase = "idle"; V.player.phaseT = 0; V.player.feintT = 0;
V.player.guarding = false;
V.keys.clear();
V.startAttack(V.player, "slash");
check("feint juice arms slash startup", V.player.phase==="startup" && V.canFeint(V.player), `ph=${V.player.phase} can=${V.canFeint(V.player)}`);
V.plantDust.splice(0);
const fe317Ok = !!V.startFeint(V.player);
check("feint juice live sting flag", fe317Ok && V.lastFeintSfx==="feint" && V.player.feintT===V.FEINT_RECOVERY, `ok=${fe317Ok} sfx=${V.lastFeintSfx} ft=${V.player.feintT}`);
check("feint juice live tip fleck", fe317Ok && V.brasaFxKind==="feint" && V.brasaFxT===V.FEINT_FX_MS && V.FEINT_FX_MS===55, `ok=${fe317Ok} kind=${V.brasaFxKind} t=${V.brasaFxT}`);
check("feint juice live no shake / no hit flash / no steel", fe317Ok && V.shakeMag===0 && V.hitFlashT===0 && V.steelFlashT===0 && !(V.parryGleamT>0), `ok=${fe317Ok} shake=${V.shakeMag} flash=${V.hitFlashT} steel=${V.steelFlashT} gleam=${V.parryGleamT}`);
check("feint juice live still idle short rec no hitbox", fe317Ok && V.player.phase==="idle" && V.hitbox(V.player)===null && V.player.sheatheT>0, `ph=${V.player.phase} hb=${!!V.hitbox(V.player)} sh=${V.player.sheatheT}`);
const fe317Dust = (V.plantDust||[]).length;
check("feint juice live still plants dust", fe317Ok && fe317Dust>0, `ok=${fe317Ok} dust=${fe317Dust}`);

// rival shares startFeint juice
bootPlay();
V.openLeft = 0;
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.startAttack(V.rival, "slash");
const rFe317 = V.canFeint(V.rival) && !!V.startFeint(V.rival);
check("feint juice rival sting + fleck", rFe317 && V.lastFeintSfx==="feint" && V.brasaFxKind==="feint" && V.rival.feintT===V.FEINT_RECOVERY, `ok=${rFe317} sfx=${V.lastFeintSfx} kind=${V.brasaFxKind} ft=${V.rival.feintT}`);

// feint rules regression: active still refuses
bootPlay();
V.openLeft = 0;
freezeRivalAI();
holdRivalStill();
V.startAttack(V.player, "slash");
for(let i=0;i<Math.ceil((V.STARTUP+STEP)/STEP);i++){
  holdRivalStill();
  if(V.player.phase==="active") break;
  V.update(STEP);
}
const lateFe = V.startFeint(V.player);
check("feint juice still refuses after active", !lateFe && V.player.phase==="active" && V.player.feintT===0, `ok=${lateFe} ph=${V.player.phase} ft=${V.player.feintT}`);

check("cache v=318 after feint cancel juice", /game.js\?v=350/.test(htmlTxt));


// --- v319 reversal juice: pitched whoosh-up + soft choque + chest invuln fleck ---
check("reversal juice seats whoosh-up choque sting", /Reversal juice \(v319\)/.test(srcTxt) && /pitched whoosh-up/.test(srcTxt) && /soft choque/.test(srcTxt) && /chest invuln fleck/.test(srcTxt) && /not parry gleam/.test(srcTxt) && /not hit bloom/.test(srcTxt));
check("reversal juice is juice-only", /Juice only — FEINT_RECOVERY \/ frames \/ tipX \/ plants \/ pad \/ sparks v316 \/ feint v317/.test(srcTxt) && /dmg nums \/ grab-tech-pushblock \/ parry-riposte locked/.test(srcTxt) && !/REVERSAL_FX_MS/.test(sliceFn(codeOnly, "destRect")) && !/REVERSAL_FX_MS/.test(sliceFn(codeOnly, "bodyAABB")) && !/playReversalSting/.test(sliceFn(codeOnly, "slashPose")) && !/playReversalSting/.test(sliceFn(codeOnly, "advanceAttack")));
const revSting318 = sliceFn(codeOnly, "playReversalSting");
const startRev318 = sliceFn(codeOnly, "startReversal");
const startWakeRev318 = sliceFn(codeOnly, "startWakeReversal");
const spawnBrasa318 = sliceFn(codeOnly, "spawnBrasaFx");
const drawBrasa318 = sliceFn(codeOnly, "drawBrasaFx");
const syncBrasa318 = sliceFn(codeOnly, "syncBrasaFx");
check("reversal sting pitches whoosh-up + soft choque",
  /function playReversalSting/.test(srcTxt)
  && /lastReversalSfx = "reversal"/.test(revSting318)
  && /SFX\.whoosh/.test(revSting318) && /SFX\.choque/.test(revSting318)
  && /rate: 1\.32/.test(revSting318) && /rate: 0\.94/.test(revSting318)
  && !/SFX\.impacto/.test(revSting318) && !/SFX\.bloqueo/.test(revSting318)
  && !/SFX\.riposte/.test(revSting318)
);
check("reversal sting distinct from parry / feint / golpe whoosh",
  /rate: 1\.48/.test(sliceFn(codeOnly, "playParrySting"))
  && /rate: 0\.66/.test(sliceFn(codeOnly, "playFeintSting"))
  && /playSfx\(SFX\.whoosh\);/.test(sliceFn(codeOnly, "advanceAttack"))
  && !/rate: 0\.66/.test(revSting318)
  && !/rate: 1\.48/.test(revSting318)
);
check("startReversal arms reversal sting + chest fleck + dust",
  /playReversalSting\(\)/.test(startRev318)
  && /spawnBrasaFx\("reversal"/.test(startRev318)
  && /hitWoundAnchor\(f\)/.test(startRev318)
  && /spawnPlantDust\(f, 1\.0\)/.test(startRev318)
  && startRev318.indexOf("playReversalSting()") < startRev318.indexOf('spawnBrasaFx("reversal"')
  && !/bumpShake/.test(startRev318)
  && !/hitFlashT/.test(startRev318)
  && !/spawnSteelFlash/.test(startRev318)
  && !/spawnParryGleam/.test(startRev318)
);
check("startWakeReversal reuses startReversal juice once",
  /startReversal\(f\)/.test(startWakeRev318)
  && !/playReversalSting/.test(startWakeRev318)
  && !/spawnBrasaFx\("reversal"/.test(startWakeRev318)
);
check("reversal FX brief chest fleck 70ms", V.REVERSAL_FX_MS===70 && V.REVERSAL_FX_MS>V.FEINT_FX_MS && V.REVERSAL_FX_MS>V.STEEL_FLASH_MS && V.REVERSAL_FX_MS<V.GRAB_FX_MS && V.REVERSAL_FX_MS<V.HIT_SPARK_MS, `ms=${V.REVERSAL_FX_MS}`);
check("reversal FX spawn rides chest / hueso-brasa-pizarra bits",
  /kind === "reversal"/.test(spawnBrasa318)
  && /brasaFxT = REVERSAL_FX_MS/.test(spawnBrasa318)
  && /hitWoundAnchor\(home\)/.test(spawnBrasa318)
  && /i < 4/.test(spawnBrasa318)
  && /"hueso"/.test(spawnBrasa318) && /"brasa"/.test(spawnBrasa318) && /"pizarra"/.test(spawnBrasa318)
);
check("reversal FX sync/draw discreet chest mote",
  /brasaFxKind === "reversal"/.test(syncBrasa318)
  && /hitWoundAnchor\(home\)/.test(syncBrasa318)
  && /brasaFxKind === "reversal"/.test(drawBrasa318)
  && /COL_HUESO/.test(drawBrasa318)
  && /REVERSAL_FX_MS/.test(drawBrasa318)
  && /Discreet invuln flash mote/.test(srcTxt)
  && /not parry gleam ring, not hit bloom/.test(srcTxt)
);
check("reversal juice does not retune reversal rules / frames",
  V.REVERSAL_STAM===30 && V.REVERSAL_INVULN===V.GOLPE_STARTUP && V.REVERSAL_AI_CD===1800 && V.REVERSAL_AI_CHANCE===0.4
  && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180
  && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280
  && /f\.reversal = true/.test(startRev318)
  && /f\.stamina -= REVERSAL_STAM/.test(startRev318)
);
check("reversal juice tipX / plants / pad locked",
  /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt)
  && (htmlTxt.match(/data-tap=/g)||[]).length===3
  && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt)
  && !/zone-reversal/.test(htmlTxt)
);
check("sparks v316 / feint v317 / dmg nums / grab-tech-pushblock / parry still locked after reversal juice",
  V.HIT_SPARK_MS===100 && V.BRASA_HIT_MS===140 && V.DMG_NUM_MS===600 && V.DMG_NUM_RISE===50
  && V.FEINT_FX_MS===55 && V.FEINT_RECOVERY===100
  && V.GRAB_SHAKE===11 && V.GRAB_FX_MS===90 && V.TECH_SHAKE===9 && V.PUSHBLOCK_SHAKE===8 && V.CLASH_SHAKE===14
  && V.PARRY_WIN_MS===140 && V.PARRY_STAGGER_MS===180 && V.PARRY_GLEAM_MS===80
  && V.RIPOSTE_STARTUP===130 && /scale = 4/.test(sliceFn(codeOnly, "drawDmgNums"))
  && /i < 5/.test(sliceFn(codeOnly, "spawnHitSpark"))
);

// synthetic: startReversal arms sting + fleck
bootPlay();
V.openLeft = 0;
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100; V.rival.stamina = 100;
V.player.phase = "idle"; V.player.phaseT = 0;
V.player.guarding = true;
V.player.guardBreakT = 0;
V.player.feintT = 0;
V.keys.clear();
V.plantDust.splice(0);
const rev318Ok = !!V.startReversal(V.player);
check("reversal juice live sting flag", rev318Ok && V.lastReversalSfx==="reversal" && V.player.reversal===true && V.player.phase==="startup" && V.player.cut==="golpe", `ok=${rev318Ok} sfx=${V.lastReversalSfx} rev=${V.player.reversal} ph=${V.player.phase}`);
check("reversal juice live chest fleck", rev318Ok && V.brasaFxKind==="reversal" && V.brasaFxT===V.REVERSAL_FX_MS && V.REVERSAL_FX_MS===70, `ok=${rev318Ok} kind=${V.brasaFxKind} t=${V.brasaFxT}`);
check("reversal juice live no shake / no hit flash / no steel / no parry gleam", rev318Ok && V.shakeMag===0 && V.hitFlashT===0 && V.steelFlashT===0 && !(V.parryGleamT>0), `ok=${rev318Ok} shake=${V.shakeMag} flash=${V.hitFlashT} steel=${V.steelFlashT} gleam=${V.parryGleamT}`);
check("reversal juice live stam / invuln locked", rev318Ok && V.player.stamina===70 && V.REVERSAL_STAM===30 && V.REVERSAL_INVULN===V.GOLPE_STARTUP && V.reversalInvuln(V.player)===true, `ok=${rev318Ok} stam=${V.player.stamina} inv=${V.reversalInvuln(V.player)}`);
const rev318Dust = (V.plantDust||[]).length;
check("reversal juice live plants dust", rev318Ok && rev318Dust>0, `ok=${rev318Ok} dust=${rev318Dust}`);

// wakeup reuses startReversal juice
bootPlay();
V.openLeft = 0;
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100;
V.player.phase = "idle"; V.player.phaseT = 0;
V.player.falling = false; V.player.thrownT = 0; V.player.stunT = 0;
V.player.throwInvulnT = V.THROW_WAKE_INVULN;
V.player.wakeRev = true;
V.player.guarding = false;
V.player.boltPhase = "";
V.keys.clear();
V.plantDust.splice(0);
const wake318Ok = !!V.startWakeReversal(V.player);
check("wakeup reversal juice live sting + fleck", wake318Ok && V.lastReversalSfx==="reversal" && V.brasaFxKind==="reversal" && V.brasaFxT===V.REVERSAL_FX_MS && V.player.reversal===true, `ok=${wake318Ok} sfx=${V.lastReversalSfx} kind=${V.brasaFxKind} t=${V.brasaFxT}`);

// rival shares startReversal juice
bootPlay();
V.openLeft = 0;
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.rival.hp = 100; V.player.hp = 100;
V.rival.stamina = 100;
V.rival.phase = "idle"; V.rival.phaseT = 0;
V.rival.guarding = true;
V.rival.guardBreakT = 0;
V.rival.feintT = 0;
V.keys.clear();
const rRev318 = !!V.startReversal(V.rival);
check("rival reversal juice sting + fleck", rRev318 && V.lastReversalSfx==="reversal" && V.brasaFxKind==="reversal" && V.rival.reversal===true && V.rival.stamina===70, `ok=${rRev318} sfx=${V.lastReversalSfx} kind=${V.brasaFxKind} stam=${V.rival.stamina}`);

check("reversal juice no 6th pad button", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="Space"/.test(htmlTxt) && /data-tap="KeyL"/.test(htmlTxt) && /data-tap="KeyK"/.test(htmlTxt));

check("cache v=319 after reversal juice", /game.js\?v=350/.test(htmlTxt));


// --- v320 wakeup / getup readability: grit + soft hueso wash on throw-invuln arm ---
check("wakeup getup juice seats grit + soft hueso wash", /Wakeup \/ getup readability \(v320\)/.test(srcTxt) && /brief getup grit/.test(srcTxt) && /soft hueso wash/.test(srcTxt) && /not reversal fleck/.test(srcTxt) && /not parry gleam/.test(srcTxt));
check("wakeup getup juice is juice-only", /Juice only — THROW_WAKE_INVULN \/ frames \/ tipX \/ plants \/ pad \/ sparks v316 \/ feint v317 \/ reversal v319/.test(srcTxt) && /dmg nums \/ grab-tech-pushblock \/ parry-riposte locked/.test(srcTxt) && !/WAKE_FX_MS/.test(sliceFn(codeOnly, "destRect")) && !/WAKE_FX_MS/.test(sliceFn(codeOnly, "bodyAABB")) && !/spawnBrasaFx\("wakeup"/.test(sliceFn(codeOnly, "startReversal")) && !/spawnBrasaFx\("wakeup"/.test(sliceFn(codeOnly, "slashPose")));
const tickThrow320 = sliceFn(codeOnly, "tickThrowState");
const spawnBrasa320 = sliceFn(codeOnly, "spawnBrasaFx");
const drawBrasa320 = sliceFn(codeOnly, "drawBrasaFx");
const syncBrasa320 = sliceFn(codeOnly, "syncBrasaFx");
check("tickThrowState arms getup grit + soft hueso on invuln start",
  /throwInvulnT = THROW_WAKE_INVULN/.test(tickThrow320)
  && /wakeRev = true/.test(tickThrow320)
  && /spawnPlantDust\(f, 1\.15\)/.test(tickThrow320)
  && /spawnBrasaFx\("wakeup"/.test(tickThrow320)
  && /hitWoundAnchor\(f\)/.test(tickThrow320)
  && tickThrow320.indexOf("spawnPlantDust(f, 1.15)") < tickThrow320.indexOf('spawnBrasaFx("wakeup"')
  && !/playReversalSting/.test(tickThrow320)
  && !/bumpShake/.test(tickThrow320)
  && !/hitFlashT/.test(tickThrow320)
  && !/spawnSteelFlash/.test(tickThrow320)
  && !/spawnParryGleam/.test(tickThrow320)
);
check("wakeup FX brief soft wash 65ms", V.WAKE_FX_MS===65 && V.WAKE_FX_MS>V.FEINT_FX_MS && V.WAKE_FX_MS>V.STEEL_FLASH_MS && V.WAKE_FX_MS<V.REVERSAL_FX_MS && V.WAKE_FX_MS<V.GRAB_FX_MS, `ms=${V.WAKE_FX_MS}`);
check("wakeup FX spawn rides chest / hueso-pizarra only",
  /kind === "wakeup"/.test(spawnBrasa320)
  && /brasaFxT = WAKE_FX_MS/.test(spawnBrasa320)
  && /hitWoundAnchor\(home\)/.test(spawnBrasa320)
  && /i < 3/.test(spawnBrasa320)
  && /"hueso"/.test(spawnBrasa320) && /"pizarra"/.test(spawnBrasa320)
  && !/"brasa"/.test(spawnBrasa320.split('kind === "wakeup"')[1].split('brasaFxT = BRASA_HIT_MS')[0])
);
check("wakeup FX sync/draw soft hueso wash",
  /brasaFxKind === "wakeup"/.test(syncBrasa320)
  && /hitWoundAnchor\(home\)/.test(syncBrasa320)
  && /brasaFxKind === "wakeup"/.test(drawBrasa320)
  && /COL_HUESO/.test(drawBrasa320)
  && /WAKE_FX_MS/.test(drawBrasa320)
  && /Soft hueso wash on getup/.test(srcTxt)
  && /not reversal fleck, not parry gleam/.test(srcTxt)
);
check("wakeup getup does not retune invuln / frames / reversal",
  V.THROW_WAKE_INVULN===80 && V.THROW_WAKE_INVULN===V.THROW_STARTUP
  && V.REVERSAL_STAM===30 && V.REVERSAL_INVULN===V.GOLPE_STARTUP && V.REVERSAL_FX_MS===70
  && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180
  && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280
);
check("wakeup getup tipX / plants / pad locked",
  /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt)
  && (htmlTxt.match(/data-tap=/g)||[]).length===3
  && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt)
  && !/zone-reversal/.test(htmlTxt)
);
check("sparks v316 / feint v317 / reversal v319 / dmg / grab-tech-pushblock / parry still locked after wakeup juice",
  V.HIT_SPARK_MS===100 && V.BRASA_HIT_MS===140 && V.DMG_NUM_MS===600 && V.DMG_NUM_RISE===50
  && V.FEINT_FX_MS===55 && V.FEINT_RECOVERY===100 && V.REVERSAL_FX_MS===70
  && V.GRAB_SHAKE===11 && V.GRAB_FX_MS===90 && V.TECH_SHAKE===9 && V.PUSHBLOCK_SHAKE===8 && V.CLASH_SHAKE===14
  && V.PARRY_WIN_MS===140 && V.PARRY_STAGGER_MS===180 && V.PARRY_GLEAM_MS===80
  && V.RIPOSTE_STARTUP===130 && /scale = 4/.test(sliceFn(codeOnly, "drawDmgNums"))
  && /i < 5/.test(sliceFn(codeOnly, "spawnHitSpark"))
  && /playReversalSting\(\)/.test(sliceFn(codeOnly, "startReversal"))
  && /spawnBrasaFx\("reversal"/.test(sliceFn(codeOnly, "startReversal"))
);

// live: thrownT expiry arms grit + soft hueso wash
bootPlay();
V.openLeft = 0;
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100;
V.player.phase = "idle"; V.player.phaseT = 0;
V.player.falling = false;
V.player.thrownT = STEP * 2;
V.player.stunT = 0;
V.player.throwInvulnT = 0;
V.player.wakeRev = false;
V.player.guarding = false;
V.player.boltPhase = "";
V.keys.clear();
V.plantDust.splice(0);
for(let i=0;i<8;i++){
  V.update(STEP);
  if(V.player.thrownT===0 && V.player.throwInvulnT>0) break;
}
const wake320Ok = V.player.thrownT===0 && V.player.throwInvulnT>0 && V.player.wakeRev===true;
const wake320Dust = (V.plantDust||[]).length;
check("wakeup getup live invuln armed", wake320Ok && V.player.throwInvulnT===V.THROW_WAKE_INVULN, `ok=${wake320Ok} inv=${V.player.throwInvulnT} wr=${V.player.wakeRev} th=${V.player.thrownT}`);
check("wakeup getup live grit stamp", wake320Ok && wake320Dust>0, `ok=${wake320Ok} dust=${wake320Dust}`);
check("wakeup getup live soft hueso wash", wake320Ok && V.brasaFxKind==="wakeup" && V.brasaFxT>0 && V.brasaFxT<=V.WAKE_FX_MS && V.WAKE_FX_MS===65, `ok=${wake320Ok} kind=${V.brasaFxKind} t=${V.brasaFxT}`);
check("wakeup getup live no shake / steel / parry gleam / reversal fleck", wake320Ok && V.shakeMag===0 && V.hitFlashT===0 && V.steelFlashT===0 && !(V.parryGleamT>0) && V.brasaFxKind!=="reversal", `ok=${wake320Ok} kind=${V.brasaFxKind} shake=${V.shakeMag} steel=${V.steelFlashT} gleam=${V.parryGleamT}`);

// rival shares getup juice
bootPlay();
V.openLeft = 0;
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.rival.hp = 100; V.player.hp = 100;
V.rival.phase = "idle"; V.rival.phaseT = 0;
V.rival.falling = false;
V.rival.thrownT = STEP * 2;
V.rival.stunT = 0;
V.rival.throwInvulnT = 0;
V.rival.wakeRev = false;
V.keys.clear();
V.plantDust.splice(0);
for(let i=0;i<8;i++){
  V.update(STEP);
  if(V.rival.thrownT===0 && V.rival.throwInvulnT>0) break;
}
const rWake320 = V.rival.thrownT===0 && V.rival.throwInvulnT===V.THROW_WAKE_INVULN && V.rival.wakeRev===true;
check("rival wakeup getup grit + soft wash", rWake320 && (V.plantDust||[]).length>0 && V.brasaFxKind==="wakeup", `ok=${rWake320} dust=${(V.plantDust||[]).length} kind=${V.brasaFxKind} inv=${V.rival.throwInvulnT}`);

// reversal juice still distinct after getup arm
bootPlay();
V.openLeft = 0;
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100;
V.player.phase = "idle"; V.player.phaseT = 0;
V.player.guarding = true;
V.player.guardBreakT = 0;
V.player.feintT = 0;
V.keys.clear();
V.plantDust.splice(0);
const rev320Still = !!V.startReversal(V.player);
check("reversal juice still fleck after wakeup juice", rev320Still && V.lastReversalSfx==="reversal" && V.brasaFxKind==="reversal" && V.brasaFxT===V.REVERSAL_FX_MS && V.player.reversal===true, `ok=${rev320Still} sfx=${V.lastReversalSfx} kind=${V.brasaFxKind} t=${V.brasaFxT}`);

check("wakeup getup no 6th pad button", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="Space"/.test(htmlTxt) && /data-tap="KeyL"/.test(htmlTxt) && /data-tap="KeyK"/.test(htmlTxt));

check("cache v=320 after wakeup getup juice", /game.js\?v=350/.test(htmlTxt));


// --- v321 soft getup foot-scrape SFX on same grit/fleck arm ---
check("getup scrape seats soft whoosh-down plant", /Soft getup foot-scrape SFX \(v321\)/.test(srcTxt) && /pitched whoosh-down/.test(srcTxt) && /very quiet/.test(srcTxt) && /bloqueo plant scrape/.test(srcTxt) && /matching the grit stamp/.test(srcTxt));
check("getup scrape is juice-only", /Soft getup foot-scrape SFX \(v321\)/.test(srcTxt) && /wakeup grit v320/.test(srcTxt) && /dmg nums \/ grab-tech-pushblock \/ parry-riposte locked/.test(srcTxt) && !/WAKE_FX_MS/.test(sliceFn(codeOnly, "destRect")) && !/WAKE_FX_MS/.test(sliceFn(codeOnly, "bodyAABB")) && !/playGetupScrape/.test(sliceFn(codeOnly, "startReversal")) && !/playGetupScrape/.test(sliceFn(codeOnly, "slashPose")) && !/playGetupScrape/.test(sliceFn(codeOnly, "startFeint")));
const getupScrape321 = sliceFn(codeOnly, "playGetupScrape");
const tickThrow321 = sliceFn(codeOnly, "tickThrowState");
check("getup scrape pitches whoosh-down + soft bloqueo",
  /function playGetupScrape/.test(srcTxt)
  && /lastGetupSfx = "getup"/.test(getupScrape321)
  && /SFX\.whoosh/.test(getupScrape321) && /SFX\.bloqueo/.test(getupScrape321)
  && /rate: 0\.48/.test(getupScrape321) && /rate: 0\.82/.test(getupScrape321)
  && /volume: 0\.46/.test(getupScrape321) && /volume: 0\.14/.test(getupScrape321)
  && !/SFX\.impacto/.test(getupScrape321) && !/SFX\.choque/.test(getupScrape321)
  && !/SFX\.riposte/.test(getupScrape321) && !/SFX\.cast/.test(getupScrape321)
);
check("getup scrape distinct from feint / reversal / parry / grab",
  /rate: 0\.66/.test(sliceFn(codeOnly, "playFeintSting"))
  && /rate: 1\.32/.test(sliceFn(codeOnly, "playReversalSting"))
  && /rate: 1\.48/.test(sliceFn(codeOnly, "playParrySting"))
  && /rate: 0\.72/.test(sliceFn(codeOnly, "playGrabConnectSting"))
  && !/rate: 0\.66/.test(getupScrape321)
  && !/rate: 1\.32/.test(getupScrape321)
  && !/SFX\.choque/.test(getupScrape321)
  && !/SFX\.impacto/.test(getupScrape321)
);
check("tickThrowState arms getup scrape with grit + soft hueso",
  /playGetupScrape\(\)/.test(tickThrow321)
  && /spawnPlantDust\(f, 1\.15\)/.test(tickThrow321)
  && /spawnBrasaFx\("wakeup"/.test(tickThrow321)
  && tickThrow321.indexOf("playGetupScrape()") < tickThrow321.indexOf("spawnPlantDust(f, 1.15)")
  && tickThrow321.indexOf("spawnPlantDust(f, 1.15)") < tickThrow321.indexOf('spawnBrasaFx("wakeup"')
  && !/playReversalSting/.test(tickThrow321)
  && !/playFeintSting/.test(tickThrow321)
  && !/playParrySting/.test(tickThrow321)
  && !/bumpShake/.test(tickThrow321)
  && !/hitFlashT/.test(tickThrow321)
  && !/spawnSteelFlash/.test(tickThrow321)
  && !/spawnParryGleam/.test(tickThrow321)
);
check("getup scrape does not retune invuln / frames / reversal",
  V.THROW_WAKE_INVULN===80 && V.THROW_WAKE_INVULN===V.THROW_STARTUP
  && V.WAKE_FX_MS===65 && V.REVERSAL_STAM===30 && V.REVERSAL_INVULN===V.GOLPE_STARTUP && V.REVERSAL_FX_MS===70
  && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180
  && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280
  && V.FEINT_RECOVERY===100 && V.FEINT_FX_MS===55
);
check("getup scrape tipX / plants / pad locked",
  /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt)
  && (htmlTxt.match(/data-tap=/g)||[]).length===3
  && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt)
  && !/zone-reversal/.test(htmlTxt)
);
check("sparks v316 / feint v317 / reversal v319 / wakeup grit v320 / dmg / grab-tech-pushblock / parry still locked after getup scrape",
  V.HIT_SPARK_MS===100 && V.BRASA_HIT_MS===140 && V.DMG_NUM_MS===600 && V.DMG_NUM_RISE===50
  && V.FEINT_FX_MS===55 && V.FEINT_RECOVERY===100 && V.REVERSAL_FX_MS===70 && V.WAKE_FX_MS===65
  && V.GRAB_SHAKE===11 && V.GRAB_FX_MS===90 && V.TECH_SHAKE===9 && V.PUSHBLOCK_SHAKE===8 && V.CLASH_SHAKE===14
  && V.PARRY_WIN_MS===140 && V.PARRY_STAGGER_MS===180 && V.PARRY_GLEAM_MS===80
  && V.RIPOSTE_STARTUP===130 && /scale = 4/.test(sliceFn(codeOnly, "drawDmgNums"))
  && /i < 5/.test(sliceFn(codeOnly, "spawnHitSpark"))
  && /playReversalSting\(\)/.test(sliceFn(codeOnly, "startReversal"))
  && /spawnBrasaFx\("reversal"/.test(sliceFn(codeOnly, "startReversal"))
  && /spawnBrasaFx\("wakeup"/.test(sliceFn(codeOnly, "tickThrowState"))
  && /spawnPlantDust\(f, 1\.15\)/.test(sliceFn(codeOnly, "tickThrowState"))
);

// live: thrownT expiry arms scrape + grit + soft hueso
bootPlay();
V.openLeft = 0;
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100;
V.player.phase = "idle"; V.player.phaseT = 0;
V.player.falling = false;
V.player.thrownT = STEP * 2;
V.player.stunT = 0;
V.player.throwInvulnT = 0;
V.player.wakeRev = false;
V.player.guarding = false;
V.player.boltPhase = "";
V.keys.clear();
V.plantDust.splice(0);
for(let i=0;i<8;i++){
  V.update(STEP);
  if(V.player.thrownT===0 && V.player.throwInvulnT>0) break;
}
const wake321Ok = V.player.thrownT===0 && V.player.throwInvulnT>0 && V.player.wakeRev===true;
const wake321Dust = (V.plantDust||[]).length;
check("getup scrape live invuln armed", wake321Ok && V.player.throwInvulnT===V.THROW_WAKE_INVULN, `ok=${wake321Ok} inv=${V.player.throwInvulnT} wr=${V.player.wakeRev} th=${V.player.thrownT}`);
check("getup scrape live scrape flag", wake321Ok && V.lastGetupSfx==="getup", `ok=${wake321Ok} sfx=${V.lastGetupSfx}`);
check("getup scrape live grit + soft hueso still", wake321Ok && wake321Dust>0 && V.brasaFxKind==="wakeup" && V.brasaFxT>0 && V.brasaFxT<=V.WAKE_FX_MS, `ok=${wake321Ok} dust=${wake321Dust} kind=${V.brasaFxKind} t=${V.brasaFxT}`);
check("getup scrape live no shake / steel / parry gleam / reversal fleck", wake321Ok && V.shakeMag===0 && V.hitFlashT===0 && V.steelFlashT===0 && !(V.parryGleamT>0) && V.brasaFxKind!=="reversal" && V.lastGetupSfx==="getup", `ok=${wake321Ok} kind=${V.brasaFxKind} shake=${V.shakeMag} sfx=${V.lastGetupSfx}`);

// rival shares getup scrape
bootPlay();
V.openLeft = 0;
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.rival.hp = 100; V.player.hp = 100;
V.rival.phase = "idle"; V.rival.phaseT = 0;
V.rival.falling = false;
V.rival.thrownT = STEP * 2;
V.rival.stunT = 0;
V.rival.throwInvulnT = 0;
V.rival.wakeRev = false;
V.keys.clear();
V.plantDust.splice(0);
for(let i=0;i<8;i++){
  V.update(STEP);
  if(V.rival.thrownT===0 && V.rival.throwInvulnT>0) break;
}
const rWake321 = V.rival.thrownT===0 && V.rival.throwInvulnT===V.THROW_WAKE_INVULN && V.rival.wakeRev===true;
check("rival getup scrape + grit + soft wash", rWake321 && V.lastGetupSfx==="getup" && (V.plantDust||[]).length>0 && V.brasaFxKind==="wakeup", `ok=${rWake321} sfx=${V.lastGetupSfx} dust=${(V.plantDust||[]).length} kind=${V.brasaFxKind} inv=${V.rival.throwInvulnT}`);

// feint / reversal still distinct after getup scrape
bootPlay();
V.openLeft = 0;
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100;
V.player.phase = "startup"; V.player.phaseT = 10;
V.player.cut = "slash";
V.player.guarding = false;
V.player.feintT = 0;
V.keys.clear();
const fe321Still = !!V.startFeint(V.player);
check("feint sting still after getup scrape", fe321Still && V.lastFeintSfx==="feint" && V.brasaFxKind==="feint", `ok=${fe321Still} sfx=${V.lastFeintSfx} kind=${V.brasaFxKind}`);

bootPlay();
V.openLeft = 0;
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100;
V.player.phase = "idle"; V.player.phaseT = 0;
V.player.guarding = true;
V.player.guardBreakT = 0;
V.player.feintT = 0;
V.keys.clear();
const rev321Still = !!V.startReversal(V.player);
check("reversal sting still after getup scrape", rev321Still && V.lastReversalSfx==="reversal" && V.brasaFxKind==="reversal" && V.brasaFxT===V.REVERSAL_FX_MS && V.player.reversal===true, `ok=${rev321Still} sfx=${V.lastReversalSfx} kind=${V.brasaFxKind} t=${V.brasaFxT}`);

check("getup scrape no 6th pad button", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="Space"/.test(htmlTxt) && /data-tap="KeyL"/.test(htmlTxt) && /data-tap="KeyK"/.test(htmlTxt));

check("cache v=321 after getup scrape", /game.js\?v=350/.test(htmlTxt));




// --- v322 special-cancel K clarity (holdCut sting + soft plant dust) ---
check("special-cancel K clarity seats pitched cast cancel", /Special-cancel K clarity \(v322\)/.test(srcTxt) && /pitched cast\/whoosh cancel sting/.test(srcTxt) && /Soft plant dust on cancel/.test(srcTxt) && /Keeps existing cast puff on slash tip/.test(srcTxt) && /do not replace brasaFxKind cast/.test(srcTxt));
check("special-cancel K clarity is juice-only", /Special-cancel K clarity \(v322\)/.test(srcTxt) && /getup scrape v321/.test(srcTxt) && /dmg nums \/ grab-tech-pushblock \/ parry-riposte locked/.test(srcTxt) && !/playHoldCutSting/.test(sliceFn(codeOnly, "destRect")) && !/playHoldCutSting/.test(sliceFn(codeOnly, "bodyAABB")) && !/playHoldCutSting/.test(sliceFn(codeOnly, "startFeint")) && !/playHoldCutSting/.test(sliceFn(codeOnly, "startReversal")) && !/playHoldCutSting/.test(sliceFn(codeOnly, "slashPose")));
const holdCutSting322 = sliceFn(codeOnly, "playHoldCutSting");
const cancelBolt322 = sliceFn(codeOnly, "cancelIntoBolt");
check("playHoldCutSting pitches cast + whoosh",
  /function playHoldCutSting/.test(srcTxt)
  && /lastHoldCutSfx = "holdCut"/.test(holdCutSting322)
  && /SFX\.cast/.test(holdCutSting322) && /SFX\.whoosh/.test(holdCutSting322)
  && /rate: 1\.28/.test(holdCutSting322) && /rate: 0\.88/.test(holdCutSting322)
  && /volume: 0\.72/.test(holdCutSting322) && /volume: 0\.34/.test(holdCutSting322)
  && !/SFX\.impacto/.test(holdCutSting322) && !/SFX\.riposte/.test(holdCutSting322)
  && !/SFX\.choque/.test(holdCutSting322) && !/SFX\.bloqueo/.test(holdCutSting322)
);
check("holdCut sting distinct from feint / reversal / getup / super",
  /rate: 0\.66/.test(sliceFn(codeOnly, "playFeintSting"))
  && /rate: 1\.32/.test(sliceFn(codeOnly, "playReversalSting"))
  && /rate: 0\.48/.test(sliceFn(codeOnly, "playGetupScrape"))
  && /rate: 0\.66/.test(srcTxt) && /rate: 1\.38/.test(srcTxt)
  && !/rate: 0\.66/.test(holdCutSting322)
  && !/rate: 1\.32/.test(holdCutSting322)
  && !/rate: 0\.48/.test(holdCutSting322)
  && !/rate: 1\.38/.test(holdCutSting322)
  && !/SFX\.choque/.test(holdCutSting322)
  && !/SFX\.impacto/.test(holdCutSting322)
);
check("cancelIntoBolt arms holdCut sting + soft dust only on holdCut branch",
  /playHoldCutSting\(\)/.test(cancelBolt322)
  && /spawnPlantDust\(f, 0\.95\)/.test(cancelBolt322)
  && cancelBolt322.indexOf("playHoldCutSting()") > cancelBolt322.indexOf("holdCutPlant = true")
  && cancelBolt322.indexOf("playHoldCutSting()") > cancelBolt322.indexOf("const ok = startBolt")
  && /f\.clashPlant = true/.test(cancelBolt322)
  && cancelBolt322.indexOf("playHoldCutSting()") > cancelBolt322.indexOf("f.clashPlant = true")
);
{
  const fromIdx = cancelBolt322.indexOf("else if (fromClash)");
  const elseIdx = cancelBolt322.indexOf("} else {", fromIdx);
  const clashArm = cancelBolt322.slice(fromIdx, elseIdx);
  const holdArm = cancelBolt322.slice(elseIdx);
  check("clash-K branch does not arm holdCut sting", !/playHoldCutSting/.test(clashArm) && !/spawnPlantDust\(f, 0\.95\)/.test(clashArm));
  check("holdCut branch arms sting after startBolt ok", /playHoldCutSting\(\)/.test(holdArm) && /spawnPlantDust\(f, 0\.95\)/.test(holdArm) && holdArm.indexOf("playHoldCutSting()") < holdArm.indexOf("spawnPlantDust(f, 0.95)"));
}
check("startBolt still spawns cast puff; idle cast unpitched",
  /spawnBrasaFx\("cast"/.test(sliceFn(codeOnly, "startBolt"))
  && /lastBoltSfx = "cast"/.test(sliceFn(codeOnly, "startBolt"))
  && /playSfx\(SFX\.cast\)/.test(sliceFn(codeOnly, "startBolt"))
  && !/playHoldCutSting/.test(sliceFn(codeOnly, "startBolt"))
);
check("special-cancel K clarity does not retune BOLT / Space / L / tipX / pad",
  V.BOLT_CANCEL_MS===100 && V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_STAM===30
  && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280
  && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180
  && /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt)
  && (htmlTxt.match(/data-tap=/g)||[]).length===3
);
check("sparks v316 / feint v317 / reversal v319 / wakeup grit v320 / getup scrape v321 locked after holdCut sting",
  V.HIT_SPARK_MS===100 && V.BRASA_HIT_MS===140 && V.DMG_NUM_MS===600 && V.DMG_NUM_RISE===50
  && V.FEINT_FX_MS===55 && V.FEINT_RECOVERY===100 && V.REVERSAL_FX_MS===70 && V.WAKE_FX_MS===65
  && V.GRAB_SHAKE===11 && V.GRAB_FX_MS===90 && V.TECH_SHAKE===9 && V.PUSHBLOCK_SHAKE===8 && V.CLASH_SHAKE===14
  && V.PARRY_WIN_MS===140 && V.PARRY_STAGGER_MS===180 && V.PARRY_GLEAM_MS===80
  && V.RIPOSTE_STARTUP===130
  && /playFeintSting\(\)/.test(sliceFn(codeOnly, "startFeint"))
  && /playReversalSting\(\)/.test(sliceFn(codeOnly, "startReversal"))
  && /playGetupScrape\(\)/.test(sliceFn(codeOnly, "tickThrowState"))
  && /spawnBrasaFx\("wakeup"/.test(sliceFn(codeOnly, "tickThrowState"))
);

// LIVE: connected slash → cancelIntoBolt holdCut
bootPlay();
V.openLeft = 0;
freezeYouAI();
freezeRivalAI();
holdRivalStill();
landCloseSlash();
V.player.stamRegenT = 99999;
V.player.stamina = 100;
const st322 = V.player.stamina;
const win322 = waitUntilBoltWindow("slash");
V.plantDust.splice(0);
V.lastHoldCutSfx = "";
// Hit leftover punch may still be decaying from landCloseSlash — juice must not ADD shake/steel/gleam.
const shakePre322 = V.shakeMag|0;
const steelPre322 = V.steelFlashT|0;
const gleamPre322 = V.parryGleamT|0;
const ok322 = win322 && !!V.cancelIntoBolt(V.player);
const dust322 = (V.plantDust||[]).length;
check("holdCut sting live cancel arms", ok322 && V.player.boltPhase==="startup" && V.player.boltHoldCut===true && V.player.holdCutPlant===true && V.player.clashPlant===false, `ok=${ok322} win=${win322} bph=${V.player.boltPhase} hold=${V.player.boltHoldCut} plant=${V.player.holdCutPlant} clash=${V.player.clashPlant}`);
check("holdCut sting live sfx flag", ok322 && V.lastHoldCutSfx==="holdCut", `ok=${ok322} sfx=${V.lastHoldCutSfx}`);
check("holdCut sting live cast puff preserved", ok322 && V.brasaFxKind==="cast" && V.brasaFxT>0, `ok=${ok322} kind=${V.brasaFxKind} t=${V.brasaFxT}`);
check("holdCut sting live plant dust", ok322 && dust322>0, `ok=${ok322} dust=${dust322}`);
check("holdCut sting live stam −30", ok322 && Math.abs(V.player.stamina - (st322 - V.BOLT_STAM))<0.2, `ok=${ok322} st=${V.player.stamina} s0=${st322}`);
check("holdCut sting live no shake / steel / parry gleam", ok322 && (V.shakeMag|0)<=shakePre322 && (V.steelFlashT|0)<=steelPre322 && !(V.parryGleamT>gleamPre322) && !/bumpShake/.test(sliceFn(codeOnly, "playHoldCutSting")) && !/bumpShake/.test(cancelBolt322) && !/spawnSteelFlash/.test(cancelBolt322) && !/spawnParryGleam/.test(cancelBolt322), `ok=${ok322} shake=${V.shakeMag}/${shakePre322} steel=${V.steelFlashT}/${steelPre322} gleam=${V.parryGleamT}/${gleamPre322}`);

// LIVE: idle startBolt does NOT set holdCut sting
bootPlay();
V.openLeft = 0;
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100;
V.player.stamRegenT = 99999;
V.player.phase = "idle"; V.player.phaseT = 0;
V.player.guarding = false; V.player.boltPhase = "";
V.player.boltHoldCut = false; V.player.holdCutPlant = false; V.player.clashPlant = false;
V.player.x = 180; V.rival.x = 1100;
V.lastHoldCutSfx = "";
V.plantDust.splice(0);
const idle322 = !!V.startBolt(V.player, true);
check("idle K does not arm holdCut sting", idle322 && V.player.boltPhase==="startup" && V.player.boltHoldCut===false && V.lastHoldCutSfx==="" && V.lastBoltSfx==="cast" && V.brasaFxKind==="cast", `ok=${idle322} hold=${V.player.boltHoldCut} hc=${V.lastHoldCutSfx} bolt=${V.lastBoltSfx} kind=${V.brasaFxKind}`);

// LIVE: clash-K does NOT fire holdCut sting
landClash();
V.player.stamRegenT = 99999;
V.player.stamina = 100;
const clashWin322 = waitUntilBoltWindow("slash");
V.lastHoldCutSfx = "";
V.plantDust.splice(0);
const clashOk322 = clashWin322 && !!V.cancelIntoBolt(V.player);
check("clash-K does not arm holdCut sting", clashOk322 && V.player.boltPhase==="startup" && V.player.boltHoldCut===false && V.player.clashPlant===true && V.lastHoldCutSfx==="" && V.brasaFxKind==="cast", `ok=${clashOk322} win=${clashWin322} hold=${V.player.boltHoldCut} clash=${V.player.clashPlant} sfx=${V.lastHoldCutSfx} kind=${V.brasaFxKind}`);

// LIVE: rival holdCut cancel also sets flag
bootPlay();
V.openLeft = 0;
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.rival.hp = 100; V.player.hp = 100;
V.rival.stamina = 100;
V.rival.stamRegenT = 99999;
V.rival.phase = "recovery"; V.rival.phaseT = V.RECOVERY - 50;
V.rival.cut = "slash"; V.rival.cutHit = true;
V.rival.clashRec = false; V.rival.guarding = false; V.rival.falling = false;
V.rival.stunT = 0; V.rival.boltPhase = ""; V.rival.boltHoldCut = false;
V.rival.x = 520; V.player.x = 180;
V.player.phase = "idle"; V.player.stunT = 0; V.player.guarding = false; V.player.boltPhase = "";
V.lastHoldCutSfx = "";
const r322 = !!V.cancelIntoBolt(V.rival);
check("rival holdCut cancel arms holdCut sting", r322 && V.rival.boltPhase==="startup" && V.rival.boltHoldCut===true && V.lastHoldCutSfx==="holdCut" && V.brasaFxKind==="cast", `ok=${r322} bph=${V.rival.boltPhase} hold=${V.rival.boltHoldCut} sfx=${V.lastHoldCutSfx} kind=${V.brasaFxKind}`);

// feint / reversal / getup still distinct
bootPlay();
V.openLeft = 0;
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100;
V.player.phase = "startup"; V.player.phaseT = 10;
V.player.cut = "slash";
V.player.guarding = false;
V.player.feintT = 0;
V.keys.clear();
const fe322 = !!V.startFeint(V.player);
check("feint sting still after holdCut clarity", fe322 && V.lastFeintSfx==="feint" && V.brasaFxKind==="feint", `ok=${fe322} sfx=${V.lastFeintSfx} kind=${V.brasaFxKind}`);

bootPlay();
V.openLeft = 0;
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100;
V.player.phase = "idle"; V.player.phaseT = 0;
V.player.guarding = true;
V.player.guardBreakT = 0;
V.player.feintT = 0;
V.keys.clear();
const rev322 = !!V.startReversal(V.player);
check("reversal sting still after holdCut clarity", rev322 && V.lastReversalSfx==="reversal" && V.brasaFxKind==="reversal" && V.brasaFxT===V.REVERSAL_FX_MS && V.player.reversal===true, `ok=${rev322} sfx=${V.lastReversalSfx} kind=${V.brasaFxKind} t=${V.brasaFxT}`);

bootPlay();
V.openLeft = 0;
freezeYouAI();
freezeRivalAI();
holdRivalStill();
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100;
V.player.phase = "idle"; V.player.phaseT = 0;
V.player.falling = false;
V.player.thrownT = STEP * 2;
V.player.stunT = 0;
V.player.throwInvulnT = 0;
V.player.wakeRev = false;
V.player.guarding = false;
V.player.boltPhase = "";
V.keys.clear();
V.plantDust.splice(0);
for(let i=0;i<8;i++){
  V.update(STEP);
  if(V.player.thrownT===0 && V.player.throwInvulnT>0) break;
}
const wake322 = V.player.thrownT===0 && V.player.throwInvulnT>0 && V.player.wakeRev===true;
check("getup scrape still after holdCut clarity", wake322 && V.lastGetupSfx==="getup" && V.brasaFxKind==="wakeup", `ok=${wake322} sfx=${V.lastGetupSfx} kind=${V.brasaFxKind}`);

check("holdCut clarity no 6th pad button", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="Space"/.test(htmlTxt) && /data-tap="KeyL"/.test(htmlTxt) && /data-tap="KeyK"/.test(htmlTxt));

check("cache v=324 after special-cancel K clarity", /game.js\?v=350/.test(htmlTxt));


// --- v323 steel flash ↔ punchCover + KO land grit (no orphan yard punch) ---
check("steel flash cover leftover seats mid-slam die", /Steel flash ↔ punchCover \(v323\)/.test(srcTxt) && /block steel asterisk used to fade/.test(srcTxt) && /STEEL_FLASH_MS/.test(srcTxt) && /asterisk died mid-cover/.test(srcTxt) && /steelFlashK/.test(srcTxt));
check("steel flash cover leftover is draw-only", /Draw-only[\s\S]*steelFlashK/.test(srcTxt) && /Steel flash ↔ punchCover \(v323\)/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && !/steelFlashK/.test(sliceFn(codeOnly, "destRect")) && !/steelFlashK/.test(sliceFn(codeOnly, "bodyAABB")) && !/steelFlashK/.test(sliceFn(codeOnly, "landBlock")) && !/steelFlashK/.test(sliceFn(codeOnly, "bumpShake")) && !/steelFlashK/.test(sliceFn(codeOnly, "spawnSteelFlash")));
check("steel flash cover leftover steelFlashK envelope", /function steelFlashK/.test(srcTxt) && /Hold peak while cover is full/.test(srcTxt) && /same smoothstep as punchCover/.test(srcTxt) && /k > 0\.25/.test(sliceFn(codeOnly, "steelFlashK")) && /u \* u \* \(3 - 2 \* u\)/.test(sliceFn(codeOnly, "steelFlashK")) && /shake > 0 && shakeDur > 0/.test(sliceFn(codeOnly, "steelFlashK")));
check("steel flash cover leftover draw uses steelFlashK", /const k = steelFlashK\(\)/.test(sliceFn(codeOnly, "drawSteelFlash")) && !/steelFlashT \/ STEEL_FLASH_MS/.test(sliceFn(codeOnly, "drawSteelFlash")));
check("steel flash cover leftover STEEL_FLASH_MS arm unchanged", V.STEEL_FLASH_MS===60 && V.HITSTOP_BLOCK===60 && V.PUNCH_PX===12);
check("KO land grit leftover seats orphan punch", /KO land grit \(v323\)/.test(srcTxt) && /caida bumpShake used to re-arm punchCover/.test(srcTxt) && /yard/.test(srcTxt) && /no white/.test(srcTxt) && /Prefer grit\+shake only on land/.test(srcTxt));
check("KO land grit leftover punchCover skips once landed", /KO land grit \(v323\)/.test(srcTxt) && /mode === "falling" && koLanded/.test(sliceFn(codeOnly, "punchCover")) && /skip overscan punch once land/.test(srcTxt));
check("KO land grit leftover still arms grit + bumpShake + caida", /spawnPlantDust\(koTarget, 2\.4\)/.test(srcTxt) && /bumpShake\(5/.test(srcTxt) && /lastKoSfx = "caida"/.test(srcTxt) && /playSfx\(SFX\.ko\)/.test(srcTxt) && !/hitFlashT = HIT_FLASH_MS/.test(srcTxt.slice(srcTxt.indexOf("koLanded = true"), srcTxt.indexOf("koLanded = true")+420)));
check("steel / KO land leftover tipX locks hold", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt));
check("steel / KO land leftover is not a new combat verb", V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.HIT_FLASH_MS===120 && V.HITSTOP_BLOCK===60 && V.STEEL_FLASH_MS===60 && V.FALL_MS===720 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("slash frames still 180/140/280 after steel / KO land leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after steel / KO land leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after steel / KO land leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("hitFlash v301 / hudFlash / pushblock / clash / wakeup / special-cancel K locked after steel / KO land", V.HIT_FLASH_MS===120 && V.PUNCH_PX===12 && /k > 0\.25/.test(sliceFn(codeOnly, "hurtFlashK")) && V.PUSHBLOCK_SHAKE===8 && V.CLASH_SHAKE===14 && V.WAKE_FX_MS===65 && /playHoldCutSting\(\)/.test(sliceFn(codeOnly, "cancelIntoBolt")) && /playGetupScrape\(\)/.test(sliceFn(codeOnly, "tickThrowState")));
check("no 6th button after steel / KO land leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=324 after steel / KO land leftover", /game.js\?v=350/.test(htmlTxt));

// LIVE: block steel — freeze peak; resume peak with cover; ease last quarter; dies with cover
toMeasure();
freezeYouAI(); freezeRivalAI();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase = "idle"; V.player.phaseT = 0; V.player.guarding = false;
V.rival.phase = "idle"; V.rival.guarding = true; V.rival.wantBlock = true; V.rival.guardChoice = 1;
V.rival.stamina = 100; V.rival.hp = 100;
V.player.x = V.rival.x - 160; V.keepApart();
V.attackEdge = true;
let sfHit=false, sfFreezeK=0, sfFreezeCover=0, sfFreezeT=0, sfKind="";
for(let i=0;i<80;i++){
  freezeRivalAI();
  V.rival.guarding = true; V.rival.wantBlock = true; V.rival.guardChoice = 1;
  V.update(STEP);
  if(V.hitstopLeft>0 && V.steelFlashT>0){
    sfHit=true; sfFreezeK=V.steelFlashK(); sfFreezeCover=V.punchCover(); sfFreezeT=V.steelFlashT; sfKind=V.steelKind;
    break;
  }
}
check("steel flash cover leftover live freeze still peak", sfHit && sfFreezeT===V.STEEL_FLASH_MS && sfFreezeCover===V.PUNCH_PX && sfFreezeK>0.9 && sfKind==="block" && V.rival.hp===100, `hit=${sfHit} t=${sfFreezeT} cover=${sfFreezeCover} k=${sfFreezeK} kind=${sfKind}`);
for(let i=0;i<20;i++){ if(V.hitstopLeft<=0) break; V.update(STEP); }
for(let i=0;i<3;i++){ if(V.hitstopLeft<=0 && V.shake<V.HITSTOP_BLOCK) break; V.update(STEP); }
const sfResumeK=V.steelFlashK(), sfResumeCover=V.punchCover(), sfResumeShake=V.shake;
check("steel flash cover leftover live resume still peak with cover", V.hitstopLeft<=0 && sfResumeCover===V.PUNCH_PX && sfResumeK>0.9 && sfResumeShake>0 && V.steelFlashT===V.STEEL_FLASH_MS, `hs=${V.hitstopLeft} cover=${sfResumeCover} k=${sfResumeK} shake=${sfResumeShake} t=${V.steelFlashT}`);
let sfEaseK=-1, sfEaseCover=-1, sfEaseT=-1;
for(let i=0;i<30;i++){
  V.update(STEP);
  if(V.shake>0 && V.shakeDur>0 && V.shake/V.shakeDur<=0.25 && V.steelFlashT>0){
    sfEaseK=V.steelFlashK(); sfEaseCover=V.punchCover(); sfEaseT=V.steelFlashT;
    break;
  }
}
check("steel flash cover leftover live ease with cover last quarter", sfEaseCover<V.PUNCH_PX && sfEaseCover>0 && sfEaseK>0 && sfEaseK<1 && sfEaseT===V.STEEL_FLASH_MS, `cover=${sfEaseCover} k=${sfEaseK} t=${sfEaseT}`);
for(let i=0;i<40;i++){ if(V.shake<=0) break; V.update(STEP); }
check("steel flash cover leftover live dies with cover", V.shake===0 && V.punchCover()===0 && V.steelFlashT===0 && V.steelFlashK()===0, `shake=${V.shake} cover=${V.punchCover()} t=${V.steelFlashT} k=${V.steelFlashK()}`);

// LIVE: push / tech kinds still distinct under envelope (linear no-punch fallback at arm)
bootPlay();
V.openLeft = 0;
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.player.guarding = true; V.player.stamina = 100;
V.player.x = 400; V.rival.x = 520; V.keepApart();
V.spawnSteelFlash(500, 300, V.rival, V.player, true, "push");
check("steel flash cover leftover live push kind peak", V.steelKind==="push" && V.steelFlashT===V.STEEL_FLASH_MS && V.steelFlashK()>0.9, `kind=${V.steelKind} t=${V.steelFlashT} k=${V.steelFlashK()}`);
V.spawnSteelFlash(500, 300, V.rival, V.player, true, "tech");
check("steel flash cover leftover live tech kind peak", V.steelKind==="tech" && V.steelFlashT===V.STEEL_FLASH_MS && V.steelFlashK()>0.9, `kind=${V.steelKind} t=${V.steelFlashT} k=${V.steelFlashK()}`);
V.spawnSteelFlash(500, 300, V.rival, V.player, false, "block");
check("steel flash cover leftover live block kind peak", V.steelKind==="block" && V.steelFlashK()>0.9, `kind=${V.steelKind} k=${V.steelFlashK()}`);

// LIVE: KO land — grit + shake, no punchCover, no HIT_FLASH re-arm
bootPlay();
V.openLeft = 0;
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.player.hp = 100; V.rival.hp = 0;
V.koTarget = V.rival;
// hitFlashT / shake are get-only — beginFall from a clean boot keeps flash 0;
// land bumpShake re-arms shake without HIT_FLASH.
V.beginFall();
check("KO land grit leftover live beginFall quiet", V.mode==="falling" && V.koLanded===false && V.lastKoSfx==="" && V.punchCover()===0, `mode=${V.mode} land=${V.koLanded} sfx=${V.lastKoSfx} cover=${V.punchCover()}`);
let koLandOk=false;
for(let i=0;i<80;i++){
  V.update(STEP);
  if(V.koLanded){ koLandOk=true; break; }
}
const koLandDust=(V.plantDust||[]).length;
const koLandCover=V.punchCover();
const koLandShake=V.shake;
const koLandFlash=V.hitFlashT;
check("KO land grit leftover live land arms caida + grit", koLandOk && V.koLanded===true && V.lastKoSfx==="caida" && koLandDust>0, `land=${koLandOk} sfx=${V.lastKoSfx} dust=${koLandDust}`);
check("KO land grit leftover live shake without punchCover", koLandOk && koLandShake>0 && koLandCover===0 && koLandFlash===0, `shake=${koLandShake} cover=${koLandCover} flash=${koLandFlash}`);
check("cache v=324 after steel / KO land leftover live", /game.js\?v=350/.test(htmlTxt));

// --- v324 Meter fill ↔ punchCover: fill/gain pip dies with slam cover ---
check("meter fill cover leftover seats mid-slam die", /Meter fill ↔ punchCover \(v324\)/.test(srcTxt) && /fill-to-full \/ gain pulse used to fade/.test(srcTxt) && /METER_FLASH_MS \/ METER_GAIN_MS linear clocks/.test(srcTxt) && /pip flash died mid-cover/.test(srcTxt) && /meterFlashK \/ meterGainK/.test(srcTxt));
check("meter fill cover leftover is draw-only", /Draw-only \(meterFlashK \/ meterGainK\)/.test(srcTxt) && /Meter fill ↔ punchCover \(v324\)/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && !/meterFlashCover|meterGainCover/.test(sliceFn(codeOnly, "destRect")) && !/Meter fill ↔ punchCover/.test(sliceFn(codeOnly, "landHit")) && !/Meter fill ↔ punchCover/.test(sliceFn(codeOnly, "bumpShake")));
check("meter fill cover leftover meterFlashK envelope", /function meterFlashK/.test(srcTxt) && /Hold peak while cover is full/.test(srcTxt) && /same smoothstep as punchCover/.test(srcTxt) && /meterFlashKind === "full"/.test(sliceFn(codeOnly, "meterFlashK")) && /k > 0\.25/.test(sliceFn(codeOnly, "meterFlashK")) && /u \* u \* \(3 - 2 \* u\)/.test(sliceFn(codeOnly, "meterFlashK")) && /f\.meterFlashT \/ METER_FLASH_MS/.test(sliceFn(codeOnly, "meterFlashK")));
check("meter fill cover leftover meterGainK envelope", /function meterGainK/.test(srcTxt) && /Hold peak while cover is full/.test(srcTxt) && /same smoothstep as punchCover/.test(srcTxt) && /k > 0\.25/.test(sliceFn(codeOnly, "meterGainK")) && /u \* u \* \(3 - 2 \* u\)/.test(sliceFn(codeOnly, "meterGainK")) && /f\.meterGainT \/ METER_GAIN_MS/.test(sliceFn(codeOnly, "meterGainK")));
check("meter fill cover leftover clocks hold through live punch", /Hold fill-to-full/.test(srcTxt) && /gain clocks through the live punch/.test(srcTxt) && /clear when cover dies/.test(srcTxt) && /meterFlashKind === "full" && shake > 0 && shakeDur > 0/.test(sliceFn(codeOnly, "tickHudBar")) && /meterFlashT >= METER_FLASH_MS - 0\.5/.test(sliceFn(codeOnly, "tickHudBar")) && /meterGainT >= METER_GAIN_MS - 0\.5/.test(sliceFn(codeOnly, "tickHudBar")));
check("meter fill cover leftover spend stays linear", /Spend flash stays linear/.test(srcTxt) && /own freeze/.test(srcTxt) && /meterFlashKind = "spend"/.test(srcTxt) && /mKind === "spend"/.test(srcTxt) && !/meterFlashKind === "spend"/.test(sliceFn(codeOnly, "meterFlashK")) && !/kind === "spend"/.test(sliceFn(codeOnly, "meterFlashK")));
check("meter fill cover leftover METER arm unchanged", V.METER_FLASH_MS===220 && V.METER_GAIN_MS===180 && V.METER_MAX===100 && V.METER_HIT===20 && V.PUNCH_PX===12 && V.HITSTOP_HIT===140);
check("meter fill cover leftover tipX locks hold", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt));
check("meter fill cover leftover is not a new combat verb", V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.HIT_FLASH_MS===120 && V.HITSTOP_BLOCK===60 && V.STEEL_FLASH_MS===60 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("slash frames still 180/140/280 after meter fill cover leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after meter fill cover leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after meter fill cover leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("hitFlash v301 / steel v323 / hudFlash / pushblock / clash / wakeup / special-cancel K locked after meter fill cover", V.HIT_FLASH_MS===120 && V.PUNCH_PX===12 && /k > 0\.25/.test(sliceFn(codeOnly, "hurtFlashK")) && /k > 0\.25/.test(sliceFn(codeOnly, "steelFlashK")) && V.PUSHBLOCK_SHAKE===8 && V.CLASH_SHAKE===14 && V.WAKE_FX_MS===65 && /playHoldCutSting\(\)/.test(sliceFn(codeOnly, "cancelIntoBolt")));
check("no 6th button after meter fill cover leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=324 after meter fill cover leftover", /game.js\?v=350/.test(htmlTxt));

// LIVE: fill-to-full on connect — freeze peak; resume peak with cover; ease last quarter; dies with cover
toMeasure();
freezeYouAI(); freezeRivalAI();
V.openLeft = 0; V.hitstopLeft = 0;
V.rival.guarding = false;
V.rival.wantBlock = false;
V.rival.phase = "idle";
V.player.phase = "idle";
V.player.phaseT = 0;
V.player.meter = 80;
V.player.meterFlashT = 0;
V.player.meterGainT = 0;
V.player.meterFlashKind = "";
V.startAttack(V.player, "slash");
let mfcHit=false, mfcFreezeFt=0, mfcFreezeGt=0, mfcFreezeCover=0, mfcFreezeK=0, mfcFreezeGk=0, mfcKind="";
for(let i=0;i<120;i++){
  V.update(STEP);
  if(V.hitstopLeft>0 && V.rival.hp<100 && V.player.meter===V.METER_MAX && V.player.meterFlashT>0){
    mfcHit=true;
    mfcFreezeFt=V.player.meterFlashT;
    mfcFreezeGt=V.player.meterGainT;
    mfcFreezeCover=V.punchCover();
    mfcFreezeK=V.meterFlashK(V.player);
    mfcFreezeGk=V.meterGainK(V.player);
    mfcKind=V.player.meterFlashKind;
    break;
  }
}
check("meter fill cover leftover live freeze still peak", mfcHit && mfcFreezeFt===V.METER_FLASH_MS && mfcFreezeGt===V.METER_GAIN_MS && mfcFreezeCover===V.PUNCH_PX && mfcFreezeK>0.9 && mfcFreezeGk>0.9 && mfcKind==="full", `hit=${mfcHit} ft=${mfcFreezeFt} gt=${mfcFreezeGt} cover=${mfcFreezeCover} k=${mfcFreezeK} gk=${mfcFreezeGk} kind=${mfcKind}`);
for(let i=0;i<40;i++){ if(V.hitstopLeft<=0) break; V.update(STEP); }
for(let i=0;i<3;i++){ if(V.hitstopLeft<=0 && V.shake<V.HITSTOP_HIT) break; V.update(STEP); }
const mfcResumeFt=V.player.meterFlashT, mfcResumeGt=V.player.meterGainT, mfcResumeCover=V.punchCover(), mfcResumeK=V.meterFlashK(V.player), mfcResumeGk=V.meterGainK(V.player), mfcResumeShake=V.shake;
check("meter fill cover leftover live resume still peak with cover", V.hitstopLeft<=0 && mfcResumeFt===V.METER_FLASH_MS && mfcResumeGt===V.METER_GAIN_MS && mfcResumeCover===V.PUNCH_PX && mfcResumeK>0.9 && mfcResumeGk>0.9 && mfcResumeShake>0, `hs=${V.hitstopLeft} ft=${mfcResumeFt} gt=${mfcResumeGt} cover=${mfcResumeCover} k=${mfcResumeK} gk=${mfcResumeGk} shake=${mfcResumeShake}`);
let mfcEaseK=-1, mfcEaseGk=-1, mfcEaseCover=-1, mfcEaseFt=-1;
for(let i=0;i<30;i++){
  V.update(STEP);
  if(V.shake>0 && V.shake/V.shakeDur<=0.25){
    mfcEaseK=V.meterFlashK(V.player); mfcEaseGk=V.meterGainK(V.player); mfcEaseCover=V.punchCover(); mfcEaseFt=V.player.meterFlashT;
    break;
  }
}
check("meter fill cover leftover live ease with cover last quarter", mfcEaseFt===V.METER_FLASH_MS && mfcEaseCover<V.PUNCH_PX && mfcEaseCover>0 && mfcEaseK>0 && mfcEaseK<1 && mfcEaseGk>0 && mfcEaseGk<1, `ft=${mfcEaseFt} cover=${mfcEaseCover} k=${mfcEaseK} gk=${mfcEaseGk}`);
for(let i=0;i<40;i++){ if(V.shake<=0) break; V.update(STEP); }
check("meter fill cover leftover live dies with cover", V.shake===0 && V.punchCover()===0 && V.player.meterFlashT===0 && V.meterFlashK(V.player)===0 && V.player.meterGainT===0 && V.meterGainK(V.player)===0, `shake=${V.shake} cover=${V.punchCover()} ft=${V.player.meterFlashT} k=${V.meterFlashK(V.player)} gt=${V.player.meterGainT} gk=${V.meterGainK(V.player)}`);

// LIVE: spend flash still linear (not cover-tied)
bootPlay();
V.openLeft = 0;
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.player.meter = 100; V.player.meterFlashT = 0; V.player.meterGainT = 0; V.player.meterFlashKind = "";
V.player.stamina = 100; V.player.stamRegenT = 99999; V.player.phase = "idle"; V.player.boltPhase = "";
V.boltEdge = true;
V.update(STEP);
check("meter fill cover leftover live spend still arms linear", V.player.boltPhase==="startup" && V.player.boltSuper===true && V.player.meter===0 && V.player.meterFlashKind==="spend" && V.player.meterFlashT===V.METER_FLASH_MS && V.meterFlashK(V.player)>0.9, `bph=${V.player.boltPhase} super=${V.player.boltSuper} kind=${V.player.meterFlashKind} ft=${V.player.meterFlashT} k=${V.meterFlashK(V.player)}`);
for(let i=0;i<20;i++){ if(V.hitstopLeft<=0) break; V.update(STEP); }
const spendFt0 = V.player.meterFlashT;
const spendK0 = V.meterFlashK(V.player);
for(let i=0;i<5;i++){ V.update(STEP); }
check("meter fill cover leftover live spend fades linear after freeze", V.player.meterFlashKind==="spend" && V.player.meterFlashT<spendFt0-STEP*2 && V.meterFlashK(V.player)>0 && V.meterFlashK(V.player)<spendK0+0.01, `ft=${V.player.meterFlashT} t0=${spendFt0} k=${V.meterFlashK(V.player)} k0=${spendK0}`);

// LIVE: no-punch synthetic fill still linear (existing path)
bootPlay();
V.openLeft = 0;
freezeYouAI(); freezeRivalAI();
V.player.meter = 80; V.player.meterFlashT = 0; V.player.meterGainT = 0; V.player.meterFlashKind = "";
V.gainMeter(V.player, V.METER_HIT);
check("meter fill cover leftover live no-punch arms", V.player.meter===V.METER_MAX && V.player.meterFlashT===V.METER_FLASH_MS && V.player.meterFlashKind==="full" && V.meterFlashK(V.player)>0.9 && V.shake===0, `m=${V.player.meter} ft=${V.player.meterFlashT} k=${V.meterFlashK(V.player)} shake=${V.shake}`);
const npFt0 = V.player.meterFlashT;
for(let i=0;i<8;i++){ V.update(STEP); }
check("meter fill cover leftover live no-punch fades linear", V.player.meterFlashT<npFt0-STEP*4 && V.meterFlashK(V.player)>0 && V.player.meterFlashKind==="full", `ft=${V.player.meterFlashT} t0=${npFt0} k=${V.meterFlashK(V.player)}`);
check("cache v=324 after meter fill cover leftover live", /game.js\?v=350/.test(htmlTxt));


// --- v325 Parry interrupt leftover: slash→idle fade on perfect parry ---
check("parry interrupt leftover seats snap", /Parry interrupt leftover \(v325\)/.test(srcTxt) && /perfect-parry used to pop the attacker slash\/golpe/.test(srcTxt) && /sheatheFade dies on stun/.test(srcTxt) && /freeze frame was standing idle while gleam/.test(srcTxt) && /parryFade/.test(srcTxt));
check("parry interrupt leftover is draw-only", /Draw-only \(parryFade\)/.test(srcTxt) && /Parry interrupt leftover \(v325\)/.test(srcTxt) && /poseBitmap still idle/.test(srcTxt) && /AABB planted/.test(srcTxt) && !/parryFade/.test(sliceFn(codeOnly, "bodyAABB")) && !/parryFadeT/.test(sliceFn(codeOnly, "isPerfectParry")));
check("parry interrupt leftover parryFade envelope", /function parryFade/.test(srcTxt) && /SHEATHE_MS/.test(sliceFn(codeOnly, "parryFade")) && /parryFadeT/.test(sliceFn(codeOnly, "parryFade")) && /u \* u \* \(3 - 2 \* u\)/.test(sliceFn(codeOnly, "parryFade")));
check("parry interrupt leftover landParry arms", /atk\.parryFadeT = SHEATHE_MS/.test(sliceFn(codeOnly, "landParry")) && /atk\.stunT = PARRY_STAGGER_MS/.test(sliceFn(codeOnly, "landParry")));
check("parry interrupt leftover draw rests cut", /restParry/.test(sliceFn(codeOnly, "drawKnight")) && /parryFade\(f\)/.test(sliceFn(codeOnly, "drawKnight")) && /octx\.globalAlpha = pf/.test(sliceFn(codeOnly, "drawKnight")));
check("parry interrupt leftover destRect eases ox", /leftover active ox\/rot used to dump/.test(srcTxt) && /parryFade\(f\)/.test(sliceFn(codeOnly, "slashPose")) && /fromOx/.test(sliceFn(codeOnly, "slashPose")));
check("parry interrupt leftover tip ease", /Tip under parryFade leftover/.test(srcTxt) && /parryFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /parryFade\(f\)/.test(sliceFn(codeOnly, "bladeTipY")));
check("parry interrupt leftover locks hold", V.PARRY_STAGGER_MS===180 && V.PARRY_GLEAM_MS===80 && V.PARRY_WIN_MS===140 && V.SHEATHE_MS===140 && V.HITSTOP_BLOCK===60 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280);
check("no 6th button after parry interrupt leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=325 after parry interrupt leftover", /game.js\?v=350/.test(htmlTxt));

// LIVE: perfect parry freeze holds leftover cut fade; poseBitmap idle; gleam live
toClose();
freezeYouAI(); freezeRivalAI();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100; V.rival.stamina = 100;
V.player.phase = "idle"; V.rival.phase = "idle";
V.player.stunT = 0; V.rival.stunT = 0;
V.player.riposteWindowT = 0; V.player.riposteArmed = false; V.player.riposte = false;
V.player.parryFadeT = 0; V.rival.parryFadeT = 0;
V.player.x = 400; V.rival.x = 520;
V.player.facing = 1; V.rival.facing = -1;
V.keys.clear();
V.lastParrySfx = "";
V.startAttack(V.rival, "slash");
for (let i = 0; i < 80; i++) {
  V.update(STEP);
  if (V.rival.phase === "startup" && V.rival.phaseT >= V.STARTUP - 50) break;
}
V.keys.add("KeyS");
let piHit=false, piFreezeFade=0, piFreezeStun=0, piFreezeGleam=0, piFreezePh="", piSrc="";
for (let i = 0; i < 80; i++) {
  V.rival.revCd = 99999; V.rival.revArmed = -1; V.rival.pbCd = 99999; V.rival.pbArmed = -1;
  V.update(STEP);
  if (V.hitstopLeft > 0 && V.rival.stunT > 0 && V.parryGleamT > 0 && V.player.riposteWindowT > 0) {
    piHit=true;
    piFreezeFade=V.parryFade(V.rival);
    piFreezeStun=V.rival.stunT;
    piFreezeGleam=V.parryGleamT;
    piFreezePh=V.rival.phase;
    piSrc=(V.poseBitmap(V.rival)&&V.poseBitmap(V.rival)._src)||"";
    break;
  }
}
check("parry interrupt leftover live freeze arms fade", piHit && piFreezeFade>0.85 && piFreezeStun===V.PARRY_STAGGER_MS && piFreezeGleam===V.PARRY_GLEAM_MS && piFreezePh==="idle" && V.rival.parryFadeT===V.SHEATHE_MS, `hit=${piHit} fade=${piFreezeFade} stun=${piFreezeStun} gleam=${piFreezeGleam} ph=${piFreezePh} t=${V.rival.parryFadeT}`);
check("parry interrupt leftover live poseBitmap idle", piHit && !/slash|windup|hurt/.test(piSrc), `src=${piSrc}`);
for(let i=0;i<40;i++){ if(V.hitstopLeft<=0) break; V.update(STEP); }
const piResumeFade=V.parryFade(V.rival), piResumeT=V.rival.parryFadeT;
check("parry interrupt leftover live resume still high", V.hitstopLeft<=0 && piResumeFade>0.7 && piResumeT>0 && V.rival.stunT>0 && V.rival.phase==="idle", `hs=${V.hitstopLeft} fade=${piResumeFade} t=${piResumeT} stun=${V.rival.stunT}`);
const piX0 = V.rival.x;
const piDest0 = V.destRect(V.rival);
const piOff0 = piDest0.dx - piX0;
for(let i=0;i<4;i++) V.update(STEP);
const piDest1 = V.destRect(V.rival);
const piOff1 = piDest1.dx - V.rival.x;
const piFadeMid = V.parryFade(V.rival);
check("parry interrupt leftover live fade drops", piFadeMid>=0 && piFadeMid<piResumeFade-0.05 && piFadeMid>0.15 && V.rival.parryFadeT>0, `mid=${piFadeMid} t0=${piResumeFade} t=${V.rival.parryFadeT}`);
// ox eases toward 0 with fade; compare plant offset vs x (push moves x, not ox dump).
check("parry interrupt leftover live destRect eases ox no teleport", Math.abs(piOff1)<Math.abs(piOff0)+2 && Math.abs(piOff1-piOff0)<50, `off0=${piOff0.toFixed(2)} off1=${piOff1.toFixed(2)} fade=${piFadeMid.toFixed(3)}`);
for(let i=0;i<40;i++){ if(V.rival.parryFadeT<=0) break; V.update(STEP); }
check("parry interrupt leftover live fade gone", V.rival.parryFadeT===0 && V.parryFade(V.rival)===0 && V.rival.phase==="idle", `t=${V.rival.parryFadeT} fade=${V.parryFade(V.rival)} stun=${V.rival.stunT}`);

// LIVE: chip stun still no parryFade
toClose();
freezeYouAI(); freezeRivalAI();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100; V.rival.stamina = 100;
V.player.phase = "idle"; V.rival.phase = "idle";
V.player.stunT = 0; V.rival.stunT = 0;
V.player.parryFadeT = 0; V.rival.parryFadeT = 0;
V.player.x = 400; V.rival.x = 520;
V.player.facing = 1; V.rival.facing = -1;
V.keys.clear();
V.rival.guarding = false; V.rival.wantBlock = false;
V.startAttack(V.player, "slash");
let chipOk=false;
for(let i=0;i<120;i++){
  V.rival.revCd = 99999; V.rival.revArmed = -1; V.rival.pbCd = 99999; V.rival.pbArmed = -1;
  V.update(STEP);
  if(V.rival.stunT>0 && V.rival.hp<100){ chipOk=true; break; }
}
check("parry interrupt leftover live chip stun no fade", chipOk && V.rival.parryFadeT===0 && V.parryFade(V.rival)===0 && V.rival.stunT>0, `ok=${chipOk} t=${V.rival.parryFadeT} fade=${V.parryFade(V.rival)} stun=${V.rival.stunT}`);
check("cache v=325 after parry interrupt leftover live", /game.js\?v=350/.test(htmlTxt));


// --- v326 Parry gleam ↔ punchCover (gleam orphan mid-slam) ---
check("parry gleam cover leftover seats mid-slam die", /Parry gleam ↔ punchCover \(v326\)/.test(srcTxt) && /perfect-parry brasa gleam used to fade/.test(srcTxt) && /PARRY_GLEAM_MS linear clock/.test(srcTxt) && /gleam died mid-cover/.test(srcTxt) && /parryGleamK/.test(srcTxt));
check("parry gleam cover leftover is draw-only", /Draw-only \(parryGleamK\)/.test(srcTxt) && /Parry gleam ↔ punchCover \(v326\)/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && !/parryGleamK/.test(sliceFn(codeOnly, "destRect")) && !/parryGleamK/.test(sliceFn(codeOnly, "bodyAABB")) && !/parryGleamK/.test(sliceFn(codeOnly, "landParry")) && !/parryGleamK/.test(sliceFn(codeOnly, "bumpShake")) && !/parryGleamK/.test(sliceFn(codeOnly, "spawnParryGleam")));
check("parry gleam cover leftover parryGleamK envelope", /function parryGleamK/.test(srcTxt) && /Hold peak while cover is full/.test(srcTxt) && /same smoothstep as punchCover/.test(srcTxt) && /k > 0\.25/.test(sliceFn(codeOnly, "parryGleamK")) && /u \* u \* \(3 - 2 \* u\)/.test(sliceFn(codeOnly, "parryGleamK")) && /shake > 0 && shakeDur > 0/.test(sliceFn(codeOnly, "parryGleamK")) && /parryGleamT \/ PARRY_GLEAM_MS/.test(sliceFn(codeOnly, "parryGleamK")));
check("parry gleam cover leftover draw uses parryGleamK", /const k = parryGleamK\(\)/.test(sliceFn(codeOnly, "drawParryGleam")) && !/parryGleamT \/ PARRY_GLEAM_MS/.test(sliceFn(codeOnly, "drawParryGleam")));
check("parry gleam cover leftover PARRY_GLEAM_MS arm unchanged", V.PARRY_GLEAM_MS===80 && V.PARRY_STAGGER_MS===180 && V.PARRY_WIN_MS===140 && V.HITSTOP_BLOCK===60 && V.PUNCH_PX===12 && V.STEEL_FLASH_MS===60);
check("parry gleam cover leftover tipX locks hold", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt));
check("parry gleam cover leftover is not a new combat verb", V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.HIT_FLASH_MS===120 && V.HITSTOP_BLOCK===60 && V.STEEL_FLASH_MS===60 && V.PARRY_GLEAM_MS===80 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("slash frames still 180/140/280 after parry gleam cover leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after parry gleam cover leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after parry gleam cover leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("parryFade v325 / steel v323 / meter v324 / hitFlash / pushblock / clash / wakeup locked after parry gleam cover", V.HIT_FLASH_MS===120 && V.PUNCH_PX===12 && /k > 0\.25/.test(sliceFn(codeOnly, "hurtFlashK")) && /k > 0\.25/.test(sliceFn(codeOnly, "steelFlashK")) && /k > 0\.25/.test(sliceFn(codeOnly, "meterFlashK")) && /function parryFade/.test(srcTxt) && V.PUSHBLOCK_SHAKE===8 && V.CLASH_SHAKE===14 && V.WAKE_FX_MS===65 && V.SHEATHE_MS===140);
check("no 6th button after parry gleam cover leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=326 after parry gleam cover leftover", /game.js\?v=350/.test(htmlTxt));

// LIVE: perfect parry gleam — freeze peak; resume peak with cover; ease last quarter; dies with cover
toClose();
freezeYouAI(); freezeRivalAI();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100; V.rival.stamina = 100;
V.player.phase = "idle"; V.rival.phase = "idle";
V.player.stunT = 0; V.rival.stunT = 0;
V.player.riposteWindowT = 0; V.player.riposteArmed = false; V.player.riposte = false;
V.player.parryFadeT = 0; V.rival.parryFadeT = 0;
V.player.x = 400; V.rival.x = 520;
V.player.facing = 1; V.rival.facing = -1;
V.keys.clear();
V.lastParrySfx = "";
V.startAttack(V.rival, "slash");
for (let i = 0; i < 80; i++) {
  V.update(STEP);
  if (V.rival.phase === "startup" && V.rival.phaseT >= V.STARTUP - 50) break;
}
V.keys.add("KeyS");
let pgHit=false, pgFreezeK=0, pgFreezeCover=0, pgFreezeT=0, pgFreezeShake=0;
for (let i = 0; i < 80; i++) {
  V.rival.revCd = 99999; V.rival.revArmed = -1; V.rival.pbCd = 99999; V.rival.pbArmed = -1;
  V.update(STEP);
  if (V.hitstopLeft > 0 && V.parryGleamT > 0 && V.player.riposteWindowT > 0) {
    pgHit=true;
    pgFreezeK=V.parryGleamK();
    pgFreezeCover=V.punchCover();
    pgFreezeT=V.parryGleamT;
    pgFreezeShake=V.shake;
    break;
  }
}
check("parry gleam cover leftover live freeze still peak", pgHit && pgFreezeT===V.PARRY_GLEAM_MS && pgFreezeCover===V.PUNCH_PX && pgFreezeK>0.9 && pgFreezeShake>0 && V.lastParrySfx==="parry", `hit=${pgHit} t=${pgFreezeT} cover=${pgFreezeCover} k=${pgFreezeK} shake=${pgFreezeShake} sfx=${V.lastParrySfx}`);
for(let i=0;i<20;i++){ if(V.hitstopLeft<=0) break; V.update(STEP); }
for(let i=0;i<3;i++){ if(V.hitstopLeft<=0 && V.shake<V.HITSTOP_BLOCK) break; V.update(STEP); }
const pgResumeK=V.parryGleamK(), pgResumeCover=V.punchCover(), pgResumeShake=V.shake, pgResumeT=V.parryGleamT;
check("parry gleam cover leftover live resume still peak with cover", V.hitstopLeft<=0 && pgResumeCover===V.PUNCH_PX && pgResumeK>0.9 && pgResumeShake>0 && pgResumeT===V.PARRY_GLEAM_MS, `hs=${V.hitstopLeft} cover=${pgResumeCover} k=${pgResumeK} shake=${pgResumeShake} t=${pgResumeT}`);
// Mid-cover: armed clock held at peak through live punch (v332); envelope still peak while k>0.25
let pgMidK=-1, pgMidCover=-1, pgMidT=-1, pgMidRatio=-1;
for(let i=0;i<20;i++){
  V.update(STEP);
  if(V.shake>0 && V.shakeDur>0 && V.shake/V.shakeDur>0.25 && V.parryGleamT===V.PARRY_GLEAM_MS && V.punchCover()===V.PUNCH_PX){
    pgMidK=V.parryGleamK();
    pgMidT=V.parryGleamT;
    pgMidCover=V.punchCover();
    pgMidRatio=V.shake/V.shakeDur;
    break;
  }
}
check("parry gleam cover leftover live mid-cover holds peak", pgMidCover===V.PUNCH_PX && pgMidK>0.9 && pgMidT===V.PARRY_GLEAM_MS && pgMidRatio>0.25, `cover=${pgMidCover} k=${pgMidK} t=${pgMidT} ratio=${pgMidRatio}`);
let pgEaseK=-1, pgEaseCover=-1;
for(let i=0;i<30;i++){
  V.update(STEP);
  if(V.shake>0 && V.shakeDur>0 && V.shake/V.shakeDur<=0.25 && V.parryGleamT>0){
    pgEaseK=V.parryGleamK(); pgEaseCover=V.punchCover();
    break;
  }
}
check("parry gleam cover leftover live ease with cover last quarter", pgEaseCover<V.PUNCH_PX && pgEaseCover>0 && pgEaseK>0 && pgEaseK<1, `cover=${pgEaseCover} k=${pgEaseK}`);
for(let i=0;i<40;i++){ if(V.shake<=0) break; V.update(STEP); }
check("parry gleam cover leftover live dies with cover", V.shake===0 && V.punchCover()===0 && V.parryGleamT===0 && V.parryGleamK()===0, `shake=${V.shake} cover=${V.punchCover()} t=${V.parryGleamT} k=${V.parryGleamK()}`);

// LIVE: no-punch (synthetic gleam) still fades linear
bootPlay();
V.openLeft = 0; V.hitstopLeft = 0;
freezeYouAI(); freezeRivalAI();
V.spawnParryGleam(640, 360, V.player);
const pgNpT0 = V.parryGleamT;
const pgNpK0 = V.parryGleamK();
check("parry gleam cover leftover live no-punch arms linear", pgNpT0===V.PARRY_GLEAM_MS && pgNpK0>0.9 && V.shake===0 && V.punchCover()===0, `t=${pgNpT0} k=${pgNpK0} shake=${V.shake}`);
for(let i=0;i<3;i++) V.update(STEP);
check("parry gleam cover leftover live no-punch fades linear", V.parryGleamT<pgNpT0-STEP*2 && V.parryGleamT>0 && V.parryGleamK()>0 && V.parryGleamK()<pgNpK0-0.05 && V.shake===0, `t=${V.parryGleamT} t0=${pgNpT0} k=${V.parryGleamK()} k0=${pgNpK0}`);
check("cache v=326 after parry gleam cover leftover live", /game.js\?v=350/.test(htmlTxt));



// --- v327 Pad zone slide-off leftover: hold-guarda keeps through lostpointercapture drift ---
check("pad zone slide-off leftover seats dump", /Pad zone slide-off leftover \(v327\)/.test(srcTxt) && /hold-guarda \(and other zone holds\) used to dump on/.test(srcTxt) && /lostpointercapture when the thumb drifted/.test(srcTxt) && /Capture loss does not dump zone holds/.test(srcTxt) && /padByPtr/.test(srcTxt));
check("pad zone slide-off leftover ignores capture loss for holds", /padByPtr\.has\(e\.pointerId\)/.test(srcTxt) && /if \(padByPtr\.has\(e\.pointerId\)\) return;/.test(srcTxt) && /Window pointerup \/ pointercancel still release/.test(srcTxt) && /hold-guarda dumps on lostpointercapture/.test(srcTxt));
check("pad zone slide-off leftover stick path unchanged", /stickPtr != null && e\.pointerId === stickPtr/.test(srcTxt) && /Capture loss does not dump the stick/.test(srcTxt) && /Window pointerup still/.test(srcTxt) && /lostpointercapture/.test(srcTxt));
check("pad zone slide-off leftover still releases on real lift", /window\.addEventListener\("pointerup", up\)/.test(srcTxt) && /window\.addEventListener\("pointercancel", up\)/.test(srcTxt) && /pad\.addEventListener\("pointerup", up\)/.test(srcTxt) && /pad\.addEventListener\("pointercancel", up\)/.test(srcTxt) && /const up = \(e\) => \{ padUp\(e\.pointerId\); \};/.test(srcTxt));
check("pad zone slide-off leftover hold via pointerup not toggle", /function padDown/.test(srcTxt) && /function padUp/.test(srcTxt) && /lostpointercapture/.test(srcTxt) && !/toggleGuard|guardToggle/.test(srcTxt));
check("pad zone slide-off leftover chords stay", /Hold-guarda \+ tap-tajo/.test(srcTxt) && /padChordHeld/.test(srcTxt) && /data-hold="KeyS"/.test(htmlTxt) && /data-tap="Space"/.test(htmlTxt) && /data-tap="KeyL"/.test(htmlTxt) && /data-tap="KeyK"/.test(htmlTxt));
check("pad zone slide-off leftover 2x2 pad locked", /Portrait pad 2x2 clarity leftover/.test(srcTxt) && /TL guarda/.test(srcTxt) && /TR tajo/.test(srcTxt) && /BL golpe/.test(srcTxt) && /BR dardo/.test(srcTxt) && /hold-guarda\+tap-tajo/.test(srcTxt) && /hold-guarda\+tap-golpe/.test(srcTxt));
check("pad zone slide-off leftover tipX / frames locked", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt) && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280);
check("pad zone slide-off leftover is not a new combat verb", V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.HIT_FLASH_MS===120 && V.HITSTOP_BLOCK===60 && V.PARRY_STAGGER_MS===180 && V.PARRY_GLEAM_MS===80 && V.PUSHBLOCK_STAM===25 && V.REVERSAL_STAM===30);
check("no 6th button after pad zone slide-off leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=328 after pad zone slide-off leftover", /game.js\?v=350/.test(htmlTxt));

// LIVE: hold path + handler shape (DOM pad not in harness; source + key feed)
bootPlay();
V.openLeft = 0;
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.keys.clear();
V.player.phase = "idle"; V.player.phaseT = 0; V.player.guarding = false; V.player.stunT = 0;
V.keys.add("KeyS");
V.update(STEP);
check("pad zone slide-off leftover live hold KeyS feeds guard", V.keys.has("KeyS"), `keys=${V.keys.has("KeyS")} g=${V.player.guarding}`);
const lpcSlice = (srcTxt.match(/addEventListener\("lostpointercapture"[\s\S]{0,700}/) || [""])[0];
check("pad zone slide-off leftover live handler skips padByPtr", /padByPtr\.has\(e\.pointerId\)/.test(lpcSlice) && /if \(padByPtr\.has\(e\.pointerId\)\) return;/.test(lpcSlice) && !/Zones still padUp/.test(lpcSlice), `slice=${lpcSlice.slice(0,180).replace(/\n/g," ")}`);
check("pad zone slide-off leftover live throw chord seats", /padChordHeld/.test(srcTxt) && /actionHeld\("guard"\) && actionHeld\("slash"\)/.test(srcTxt));
check("pad zone slide-off leftover live reversal seats", /hold-guarda\+tap-golpe/.test(srcTxt));
V.keys.delete("KeyS");
V.update(STEP);
check("pad zone slide-off leftover live lift releases", !V.keys.has("KeyS"), `keys=${V.keys.has("KeyS")}`);
check("cache v=328 after pad zone slide-off leftover live", /game.js\?v=350/.test(htmlTxt));


// --- v328 Clash spark ↔ punchCover (shards orphan mid-slam) ---
check("clash spark cover leftover seats mid-slam die", /Clash spark ↔ punchCover \(v328\)/.test(srcTxt) && /choque spark used to fade/.test(srcTxt) && /CLASH_SPARK_MS linear clock/.test(srcTxt) && /shards died mid-cover/.test(srcTxt) && /clashSparkK/.test(srcTxt));
check("clash spark cover leftover is draw-only", /Draw-only \(clashSparkK\)/.test(srcTxt) && /Clash spark ↔ punchCover \(v328\)/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && !/clashSparkK/.test(sliceFn(codeOnly, "destRect")) && !/clashSparkK/.test(sliceFn(codeOnly, "bodyAABB")) && !/clashSparkK/.test(sliceFn(codeOnly, "doClash")) && !/clashSparkK/.test(sliceFn(codeOnly, "bumpShake")) && !/clashSparkK/.test(sliceFn(codeOnly, "spawnClashSpark")));
check("clash spark cover leftover clashSparkK envelope", /function clashSparkK/.test(srcTxt) && /Hold peak while cover is full/.test(srcTxt) && /same smoothstep as punchCover/.test(srcTxt) && /k > 0\.25/.test(sliceFn(codeOnly, "clashSparkK")) && /u \* u \* \(3 - 2 \* u\)/.test(sliceFn(codeOnly, "clashSparkK")) && /shake > 0 && shakeDur > 0/.test(sliceFn(codeOnly, "clashSparkK")) && /clashSparkT \/ CLASH_SPARK_MS/.test(sliceFn(codeOnly, "clashSparkK")));
check("clash spark cover leftover draw uses clashSparkK", /const k = clashSparkK\(\)/.test(sliceFn(codeOnly, "drawClashSpark")) && !/clashSparkT \/ CLASH_SPARK_MS/.test(sliceFn(codeOnly, "drawClashSpark")));
check("clash spark cover leftover CLASH_SPARK_MS arm unchanged", V.CLASH_SPARK_MS===110 && V.HITSTOP_BLOCK===60 && V.CLASH_SHAKE===14 && V.PUNCH_PX===12 && V.CLASH_RECOVERY===200);
check("clash spark cover leftover tipX locks hold", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt));
check("clash spark cover leftover is not a new combat verb", V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.HIT_FLASH_MS===120 && V.HITSTOP_BLOCK===60 && V.STEEL_FLASH_MS===60 && V.CLASH_SPARK_MS===110 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("slash frames still 180/140/280 after clash spark cover leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after clash spark cover leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after clash spark cover leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("pad v327 / parry gleam v326 / steel v323 / meter v324 / hitFlash / pushblock / clash / wakeup locked after clash spark cover", V.HIT_FLASH_MS===120 && V.PUNCH_PX===12 && /k > 0\.25/.test(sliceFn(codeOnly, "hurtFlashK")) && /k > 0\.25/.test(sliceFn(codeOnly, "steelFlashK")) && /k > 0\.25/.test(sliceFn(codeOnly, "meterFlashK")) && /k > 0\.25/.test(sliceFn(codeOnly, "parryGleamK")) && /Pad zone slide-off leftover \(v327\)/.test(srcTxt) && V.PUSHBLOCK_SHAKE===8 && V.CLASH_SHAKE===14 && V.WAKE_FX_MS===65 && V.SHEATHE_MS===140);
check("no 6th button after clash spark cover leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=328 after clash spark cover leftover", /game.js\?v=350/.test(htmlTxt));

// LIVE: clash spark — freeze peak; resume peak with cover; mid-cover holds vs linear; ease last quarter; dies with cover
toClose();
freezeYouAI(); freezeRivalAI();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100; V.rival.stamina = 100;
V.player.phase = "idle"; V.rival.phase = "idle";
V.player.stunT = 0; V.rival.stunT = 0;
V.player.x = 400; V.rival.x = 520;
V.player.facing = 1; V.rival.facing = -1;
V.keys.clear();
V.startAttack(V.player, "slash");
V.startAttack(V.rival, "slash");
let csHit=false, csFreezeK=0, csFreezeCover=0, csFreezeT=0, csFreezeShake=0;
for (let i = 0; i < 120; i++) {
  V.rival.revCd = 99999; V.rival.revArmed = -1; V.rival.pbCd = 99999; V.rival.pbArmed = -1;
  V.player.revCd = 99999; V.player.revArmed = -1; V.player.pbCd = 99999; V.player.pbArmed = -1;
  V.update(STEP);
  if (V.hitstopLeft > 0 && V.clashSparkT > 0) {
    csHit=true;
    csFreezeK=V.clashSparkK();
    csFreezeCover=V.punchCover();
    csFreezeT=V.clashSparkT;
    csFreezeShake=V.shake;
    break;
  }
}
check("clash spark cover leftover live freeze still peak", csHit && csFreezeT===V.CLASH_SPARK_MS && csFreezeCover===V.PUNCH_PX && csFreezeK>0.9 && csFreezeShake>0, `hit=${csHit} t=${csFreezeT} cover=${csFreezeCover} k=${csFreezeK} shake=${csFreezeShake}`);
for(let i=0;i<20;i++){ if(V.hitstopLeft<=0) break; V.update(STEP); }
for(let i=0;i<3;i++){ if(V.hitstopLeft<=0 && V.shake<V.HITSTOP_BLOCK) break; V.update(STEP); }
const csResumeK=V.clashSparkK(), csResumeCover=V.punchCover(), csResumeShake=V.shake, csResumeT=V.clashSparkT;
check("clash spark cover leftover live resume still peak with cover", V.hitstopLeft<=0 && csResumeCover===V.PUNCH_PX && csResumeK>0.9 && csResumeShake>0 && csResumeT===V.CLASH_SPARK_MS, `hs=${V.hitstopLeft} cover=${csResumeCover} k=${csResumeK} shake=${csResumeShake} t=${csResumeT}`);
// Mid-cover: armed clock held at peak through live punch (v332)
let csMidK=-1, csMidCover=-1, csMidT=-1, csMidRatio=-1;
for(let i=0;i<20;i++){
  V.update(STEP);
  if(V.shake>0 && V.shakeDur>0 && V.shake/V.shakeDur>0.25 && V.clashSparkT===V.CLASH_SPARK_MS && V.punchCover()===V.PUNCH_PX){
    csMidK=V.clashSparkK();
    csMidT=V.clashSparkT;
    csMidCover=V.punchCover();
    csMidRatio=V.shake/V.shakeDur;
    break;
  }
}
check("clash spark cover leftover live mid-cover holds peak", csMidCover===V.PUNCH_PX && csMidK>0.9 && csMidT===V.CLASH_SPARK_MS && csMidRatio>0.25, `cover=${csMidCover} k=${csMidK} t=${csMidT} ratio=${csMidRatio}`);
let csEaseK=-1, csEaseCover=-1;
for(let i=0;i<30;i++){
  V.update(STEP);
  if(V.shake>0 && V.shakeDur>0 && V.shake/V.shakeDur<=0.25 && V.clashSparkT>0){
    csEaseK=V.clashSparkK(); csEaseCover=V.punchCover();
    break;
  }
}
check("clash spark cover leftover live ease with cover last quarter", csEaseCover<V.PUNCH_PX && csEaseCover>0 && csEaseK>0 && csEaseK<1, `cover=${csEaseCover} k=${csEaseK}`);
for(let i=0;i<40;i++){ if(V.shake<=0) break; V.update(STEP); }
check("clash spark cover leftover live dies with cover", V.shake===0 && V.punchCover()===0 && V.clashSparkT===0 && V.clashSparkK()===0, `shake=${V.shake} cover=${V.punchCover()} t=${V.clashSparkT} k=${V.clashSparkK()}`);

// LIVE: no-punch (synthetic spark) still fades linear
bootPlay();
V.openLeft = 0; V.hitstopLeft = 0;
freezeYouAI(); freezeRivalAI();
V.spawnClashSpark(640, 360);
const csNpT0 = V.clashSparkT;
const csNpK0 = V.clashSparkK();
check("clash spark cover leftover live no-punch arms linear", csNpT0===V.CLASH_SPARK_MS && csNpK0>0.9 && V.shake===0 && V.punchCover()===0, `t=${csNpT0} k=${csNpK0} shake=${V.shake}`);
for(let i=0;i<3;i++) V.update(STEP);
check("clash spark cover leftover live no-punch fades linear", V.clashSparkT<csNpT0-STEP*2 && V.clashSparkT>0 && V.clashSparkK()>0 && V.clashSparkK()<csNpK0-0.05 && V.shake===0, `t=${V.clashSparkT} t0=${csNpT0} k=${V.clashSparkK()} k0=${csNpK0}`);
check("cache v=331 after clash spark cover leftover live", /game.js\?v=350/.test(htmlTxt));


// --- v329 Flesh hit spark ↔ punchCover (shards orphan mid-slam) ---
check("flesh hit spark cover leftover seats mid-slam die", /Flesh hit spark ↔ punchCover \(v329\)/.test(srcTxt) && /clean flesh spark used to fade/.test(srcTxt) && /HIT_SPARK_MS 100 linear clock/.test(srcTxt) && /shards died mid-cover/.test(srcTxt) && /hitSparkK/.test(srcTxt));
check("flesh hit spark cover leftover is draw-only", /Draw-only \(hitSparkK\)/.test(srcTxt) && /Flesh hit spark ↔ punchCover \(v329\)/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && !/hitSparkK/.test(sliceFn(codeOnly, "destRect")) && !/hitSparkK/.test(sliceFn(codeOnly, "bodyAABB")) && !/hitSparkK/.test(sliceFn(codeOnly, "landHit")) && !/hitSparkK/.test(sliceFn(codeOnly, "bumpShake")) && !/hitSparkK/.test(sliceFn(codeOnly, "spawnHitSpark")));
check("flesh hit spark cover leftover hitSparkK envelope", /function hitSparkK/.test(srcTxt) && /Hold peak while cover is full/.test(srcTxt) && /same smoothstep as punchCover/.test(srcTxt) && /k > 0\.25/.test(sliceFn(codeOnly, "hitSparkK")) && /u \* u \* \(3 - 2 \* u\)/.test(sliceFn(codeOnly, "hitSparkK")) && /shake > 0 && shakeDur > 0/.test(sliceFn(codeOnly, "hitSparkK")) && /hitSparkT \/ HIT_SPARK_MS/.test(sliceFn(codeOnly, "hitSparkK")));
check("flesh hit spark cover leftover draw uses hitSparkK", /const k = hitSparkK\(\)/.test(sliceFn(codeOnly, "drawHitSpark")) && !/hitSparkT \/ HIT_SPARK_MS/.test(sliceFn(codeOnly, "drawHitSpark")));
check("flesh hit spark cover leftover clock holds through live punch", /Flesh hit spark ↔ punchCover \(v329\)/.test(srcTxt) && /Hold the armed clock/.test(srcTxt) && /through the live punch/.test(srcTxt) && /clear when cover dies/.test(srcTxt) && /hitSparkT > 0/.test(sliceFn(codeOnly, "update")) && /hitSparkT = 0/.test(sliceFn(codeOnly, "update")) && /brasaFxKind === "hit"/.test(sliceFn(codeOnly, "update")));
check("flesh hit spark cover leftover brasaHitK seats", /function brasaHitK/.test(srcTxt) && /Dart ember ↔ punchCover \(v329\)/.test(srcTxt) && /brasaFxKind !== "hit"/.test(sliceFn(codeOnly, "brasaHitK")) && /brasaFxT \/ BRASA_HIT_MS/.test(sliceFn(codeOnly, "brasaHitK")) && /brasaFxKind === "hit" \? brasaHitK\(\)/.test(sliceFn(codeOnly, "drawBrasaFx")));
check("flesh hit spark cover leftover HIT_SPARK_MS / BRASA_HIT_MS arm unchanged", V.HIT_SPARK_MS===100 && V.BRASA_HIT_MS===140 && V.HITSTOP_HIT===140 && V.HIT_FLASH_MS===120 && V.PUNCH_PX===12 && V.BRASA_HIT_MS>V.HIT_SPARK_MS);
check("flesh hit spark cover leftover tipX locks hold", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt));
check("flesh hit spark cover leftover is not a new combat verb", V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.HIT_FLASH_MS===120 && V.HIT_SPARK_MS===100 && V.BRASA_HIT_MS===140 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("slash frames still 180/140/280 after flesh hit spark cover leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after flesh hit spark cover leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after flesh hit spark cover leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("clashSparkK v328 / pad v327 / parry gleam v326 / steel v323 / meter v324 / hitFlash locked after flesh hit spark cover", V.HIT_FLASH_MS===120 && V.PUNCH_PX===12 && /k > 0\.25/.test(sliceFn(codeOnly, "hurtFlashK")) && /k > 0\.25/.test(sliceFn(codeOnly, "steelFlashK")) && /k > 0\.25/.test(sliceFn(codeOnly, "meterFlashK")) && /k > 0\.25/.test(sliceFn(codeOnly, "parryGleamK")) && /k > 0\.25/.test(sliceFn(codeOnly, "clashSparkK")) && /Clash spark ↔ punchCover \(v328\)/.test(srcTxt) && /Pad zone slide-off leftover \(v327\)/.test(srcTxt) && V.PUSHBLOCK_SHAKE===8 && V.CLASH_SHAKE===14 && V.WAKE_FX_MS===65 && V.SHEATHE_MS===140);
check("no 6th button after flesh hit spark cover leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=331 after flesh hit spark cover leftover", /game.js\?v=350/.test(htmlTxt));

// LIVE: flesh hit spark — freeze peak; resume peak with cover (T held); mid-cover holds; ease last quarter; dies with cover
toClose();
freezeYouAI(); freezeRivalAI();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100; V.rival.stamina = 100;
V.player.phase = "idle"; V.rival.phase = "idle";
V.player.stunT = 0; V.rival.stunT = 0;
V.player.x = 400; V.rival.x = 520;
V.player.facing = 1; V.rival.facing = -1;
V.rival.guarding = false; V.rival.wantBlock = false;
V.keys.clear();
V.startAttack(V.player, "slash");
let hsHit=false, hsFreezeK=0, hsFreezeCover=0, hsFreezeT=0, hsFreezeShake=0, hsFreezeWhite=0;
for (let i = 0; i < 120; i++) {
  V.rival.revCd = 99999; V.rival.revArmed = -1; V.rival.pbCd = 99999; V.rival.pbArmed = -1;
  V.player.revCd = 99999; V.player.revArmed = -1; V.player.pbCd = 99999; V.player.pbArmed = -1;
  V.update(STEP);
  if (V.hitstopLeft > 0 && V.hitSparkT > 0 && V.rival.hp < 100) {
    hsHit=true;
    hsFreezeK=V.hitSparkK();
    hsFreezeCover=V.punchCover();
    hsFreezeT=V.hitSparkT;
    hsFreezeShake=V.shake;
    hsFreezeWhite=V.hurtFlashK(V.rival);
    break;
  }
}
check("flesh hit spark cover leftover live freeze still peak", hsHit && hsFreezeT===V.HIT_SPARK_MS && hsFreezeCover===V.PUNCH_PX && hsFreezeK>0.9 && hsFreezeShake>0 && hsFreezeWhite>0.9, `hit=${hsHit} t=${hsFreezeT} cover=${hsFreezeCover} k=${hsFreezeK} shake=${hsFreezeShake} white=${hsFreezeWhite}`);
for(let i=0;i<40;i++){ if(V.hitstopLeft<=0) break; V.update(STEP); }
for(let i=0;i<3;i++){ if(V.hitstopLeft<=0 && V.shake<V.HITSTOP_HIT) break; V.update(STEP); }
const hsResumeK=V.hitSparkK(), hsResumeCover=V.punchCover(), hsResumeShake=V.shake, hsResumeT=V.hitSparkT, hsResumeWhite=V.hurtFlashK(V.rival);
check("flesh hit spark cover leftover live resume still peak with cover", V.hitstopLeft<=0 && hsResumeCover===V.PUNCH_PX && hsResumeK>0.9 && hsResumeShake>0 && hsResumeT===V.HIT_SPARK_MS && hsResumeWhite>0.9, `hs=${V.hitstopLeft} cover=${hsResumeCover} k=${hsResumeK} shake=${hsResumeShake} t=${hsResumeT} white=${hsResumeWhite}`);
let hsMidK=-1, hsMidCover=-1, hsMidT=-1, hsMidRatio=-1;
for(let i=0;i<20;i++){
  V.update(STEP);
  if(V.shake>0 && V.shakeDur>0 && V.shake/V.shakeDur>0.25 && V.hitSparkT===V.HIT_SPARK_MS && V.punchCover()===V.PUNCH_PX){
    hsMidK=V.hitSparkK();
    hsMidCover=V.punchCover();
    hsMidT=V.hitSparkT;
    hsMidRatio=V.shake/V.shakeDur;
    break;
  }
}
check("flesh hit spark cover leftover live mid-cover holds peak", hsMidCover===V.PUNCH_PX && hsMidK>0.9 && hsMidT===V.HIT_SPARK_MS && hsMidRatio>0.25, `cover=${hsMidCover} k=${hsMidK} t=${hsMidT} ratio=${hsMidRatio}`);
let hsEaseK=-1, hsEaseCover=-1, hsEaseT=-1;
for(let i=0;i<40;i++){
  V.update(STEP);
  if(V.shake>0 && V.shakeDur>0 && V.shake/V.shakeDur<=0.25 && V.hitSparkT>0){
    hsEaseK=V.hitSparkK(); hsEaseCover=V.punchCover(); hsEaseT=V.hitSparkT;
    break;
  }
}
check("flesh hit spark cover leftover live ease with cover last quarter", hsEaseCover<V.PUNCH_PX && hsEaseCover>0 && hsEaseK>0 && hsEaseK<1 && hsEaseT===V.HIT_SPARK_MS, `cover=${hsEaseCover} k=${hsEaseK} t=${hsEaseT}`);
for(let i=0;i<50;i++){ if(V.shake<=0) break; V.update(STEP); }
check("flesh hit spark cover leftover live dies with cover", V.shake===0 && V.punchCover()===0 && V.hitSparkT===0 && V.hitSparkK()===0, `shake=${V.shake} cover=${V.punchCover()} t=${V.hitSparkT} k=${V.hitSparkK()}`);

// LIVE: no-punch (synthetic spark) still fades linear
bootPlay();
V.openLeft = 0; V.hitstopLeft = 0;
freezeYouAI(); freezeRivalAI();
V.spawnHitSpark(640, 360, 1, V.rival);
const hsNpT0 = V.hitSparkT;
const hsNpK0 = V.hitSparkK();
check("flesh hit spark cover leftover live no-punch arms linear", hsNpT0===V.HIT_SPARK_MS && hsNpK0>0.9 && V.shake===0 && V.punchCover()===0, `t=${hsNpT0} k=${hsNpK0} shake=${V.shake}`);
for(let i=0;i<3;i++) V.update(STEP);
check("flesh hit spark cover leftover live no-punch fades linear", V.hitSparkT<hsNpT0-STEP*2 && V.hitSparkT>0 && V.hitSparkK()>0 && V.hitSparkK()<hsNpK0-0.05 && V.shake===0, `t=${V.hitSparkT} t0=${hsNpT0} k=${V.hitSparkK()} k0=${hsNpK0}`);

// LIVE: dart ember brasaHitK — freeze/resume peak; dies with cover; BRASA_HIT_MS longer no-punch
bootPlay();
freezeYouAI(); freezeRivalAI();
V.openLeft = 0; V.hitstopLeft = 0;
V.spawnBrasaFx("hit", 640, 360, 1, V.rival);
V.bumpShake(10, 1, V.HITSTOP_HIT);
V.hitstopLeft = V.HITSTOP_HIT;
check("flesh dart ember cover leftover live freeze peak", V.brasaFxKind==="hit" && V.brasaFxT===V.BRASA_HIT_MS && V.brasaHitK()>0.9 && V.punchCover()===V.PUNCH_PX, `kind=${V.brasaFxKind} t=${V.brasaFxT} k=${V.brasaHitK()} cover=${V.punchCover()}`);
for(let i=0;i<40;i++){ if(V.hitstopLeft<=0) break; V.update(STEP); }
for(let i=0;i<3;i++){ if(V.hitstopLeft<=0 && V.shake<V.HITSTOP_HIT) break; V.update(STEP); }
check("flesh dart ember cover leftover live resume peak held", V.hitstopLeft<=0 && V.brasaFxT===V.BRASA_HIT_MS && V.brasaHitK()>0.9 && V.punchCover()===V.PUNCH_PX && V.shake>0, `hs=${V.hitstopLeft} t=${V.brasaFxT} k=${V.brasaHitK()} cover=${V.punchCover()} shake=${V.shake}`);
for(let i=0;i<50;i++){ if(V.shake<=0) break; V.update(STEP); }
check("flesh dart ember cover leftover live dies with cover", V.shake===0 && V.punchCover()===0 && V.brasaFxT===0 && V.brasaHitK()===0, `shake=${V.shake} cover=${V.punchCover()} t=${V.brasaFxT} k=${V.brasaHitK()}`);
bootPlay();
V.openLeft = 0; V.hitstopLeft = 0;
freezeYouAI(); freezeRivalAI();
V.spawnBrasaFx("hit", 640, 360, 1, V.rival);
const bhNpT0 = V.brasaFxT;
const bhNpK0 = V.brasaHitK();
check("flesh dart ember cover leftover live no-punch arms BRASA_HIT_MS", bhNpT0===V.BRASA_HIT_MS && bhNpK0>0.9 && V.BRASA_HIT_MS>V.HIT_SPARK_MS && V.shake===0, `t=${bhNpT0} k=${bhNpK0}`);
for(let i=0;i<3;i++) V.update(STEP);
check("flesh dart ember cover leftover live no-punch fades linear on BRASA_HIT_MS", V.brasaFxT<bhNpT0-STEP*2 && V.brasaFxT>0 && V.brasaHitK()>0 && V.brasaHitK()<bhNpK0-0.05 && V.shake===0, `t=${V.brasaFxT} t0=${bhNpT0} k=${V.brasaHitK()} k0=${bhNpK0}`);
check("cache v=331 after flesh hit spark cover leftover live", /game.js\?v=350/.test(htmlTxt));






// --- v330 Knock grit / plant dust + floating −N ↔ punchCover ---
check("grit dmg cover leftover seats mid-slam die", /Knock grit \/ plant dust \+ floating −N ↔ punchCover \(v330\)/.test(srcTxt) && /connect grit and dmg nums used/.test(srcTxt) && /dust \/ −N died mid-cover/.test(srcTxt) && /plantDustK/.test(srcTxt) && /dmgNumK/.test(srcTxt));
check("grit dmg cover leftover is draw-only", /Draw-only \(plantDustK \/ dmgNumK\)/.test(srcTxt) && /Knock grit \/ plant dust \+ floating −N ↔ punchCover \(v330\)/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && !/plantDustK/.test(sliceFn(codeOnly, "destRect")) && !/plantDustK/.test(sliceFn(codeOnly, "bodyAABB")) && !/plantDustK/.test(sliceFn(codeOnly, "landHit")) && !/dmgNumK/.test(sliceFn(codeOnly, "landHit")) && !/plantDustK/.test(sliceFn(codeOnly, "bumpShake")) && !/dmgNumK/.test(sliceFn(codeOnly, "spawnDmgNum")));
check("grit dmg cover leftover plantDustK envelope", /function plantDustK/.test(srcTxt) && /Hold peak while cover is full/.test(srcTxt) && /same smoothstep as punchCover/.test(srcTxt) && /k > 0\.25/.test(sliceFn(codeOnly, "plantDustK")) && /u \* u \* \(3 - 2 \* u\)/.test(sliceFn(codeOnly, "plantDustK")) && /Math\.max\(lin, ease\)/.test(sliceFn(codeOnly, "plantDustK")) && /shake > 0 && shakeDur > 0/.test(sliceFn(codeOnly, "plantDustK")) && /!p\.punch/.test(sliceFn(codeOnly, "plantDustK")));
check("grit dmg cover leftover dmgNumK envelope", /function dmgNumK/.test(srcTxt) && /k > 0\.25/.test(sliceFn(codeOnly, "dmgNumK")) && /u \* u \* \(3 - 2 \* u\)/.test(sliceFn(codeOnly, "dmgNumK")) && /Math\.max\(lin, ease\)/.test(sliceFn(codeOnly, "dmgNumK")) && /Chip\/clean\/throw tints stay distinct/.test(srcTxt) && /DMG_NUM_MS 600/.test(srcTxt) && /Rise still ages/.test(srcTxt));
check("grit dmg cover leftover draw uses K fns", /const k = plantDustK\(p\)/.test(sliceFn(codeOnly, "drawPlantDust")) && /const fade = dmgNumK\(p\)/.test(sliceFn(codeOnly, "drawDmgNums")) && !/1 - p\.t \/ p\.life/.test(sliceFn(codeOnly, "drawPlantDust")) && !/const fade = 1 - u/.test(sliceFn(codeOnly, "drawDmgNums")));
check("grit dmg cover leftover punch mark on spawn", /const punch = shake > 0 && shakeDur > 0/.test(sliceFn(codeOnly, "spawnPlantDust")) && /punch: punch/.test(sliceFn(codeOnly, "spawnPlantDust")) && /const punch = true/.test(sliceFn(codeOnly, "spawnDmgNum")) && /punch: punch/.test(sliceFn(codeOnly, "spawnDmgNum")));
check("grit dmg cover leftover DMG_NUM / grit life unchanged", V.DMG_NUM_MS===600 && V.DMG_NUM_RISE===50 && V.HITSTOP_HIT===140 && V.HIT_FLASH_MS===120 && V.PUNCH_PX===12 && V.HIT_SPARK_MS===100);
check("grit dmg cover leftover tipX locks hold", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt));
check("grit dmg cover leftover is not a new combat verb", V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.HIT_FLASH_MS===120 && V.SLASH_DMG===10 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("slash frames still 180/140/280 after grit dmg cover leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after grit dmg cover leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after grit dmg cover leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("hitSparkK v329 / clashSparkK v328 / pad v327 / parry gleam v326 / steel v323 / meter v324 / hitFlash locked after grit dmg cover", V.HIT_FLASH_MS===120 && V.PUNCH_PX===12 && /k > 0\.25/.test(sliceFn(codeOnly, "hurtFlashK")) && /k > 0\.25/.test(sliceFn(codeOnly, "steelFlashK")) && /k > 0\.25/.test(sliceFn(codeOnly, "meterFlashK")) && /k > 0\.25/.test(sliceFn(codeOnly, "parryGleamK")) && /k > 0\.25/.test(sliceFn(codeOnly, "clashSparkK")) && /k > 0\.25/.test(sliceFn(codeOnly, "hitSparkK")) && /Flesh hit spark ↔ punchCover \(v329\)/.test(srcTxt) && /Clash spark ↔ punchCover \(v328\)/.test(srcTxt) && /Pad zone slide-off leftover \(v327\)/.test(srcTxt) && V.PUSHBLOCK_SHAKE===8 && V.CLASH_SHAKE===14 && V.WAKE_FX_MS===65 && V.SHEATHE_MS===140);
check("no 6th button after grit dmg cover leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=331 after grit dmg cover leftover", /game.js\?v=350/.test(htmlTxt));

// LIVE: knock grit + dmg num — freeze/resume/mid peak with cover; walk grit stays linear; rise ages; DMG values locked
toClose();
freezeYouAI(); freezeRivalAI();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100; V.rival.stamina = 100;
V.player.phase = "idle"; V.rival.phase = "idle";
V.player.stunT = 0; V.rival.stunT = 0;
V.player.x = 400; V.rival.x = 520;
V.player.facing = 1; V.rival.facing = -1;
V.rival.guarding = false; V.rival.wantBlock = false;
V.keys.clear();
V.startAttack(V.player, "slash");
let gdHit=false, gdFreezeDustK=0, gdFreezeDmgK=0, gdFreezeCover=0, gdFreezeWhite=0, gdFreezeSpark=0, gdFreezePunch=false, gdFreezeDmgPunch=false;
for (let i = 0; i < 120; i++) {
  V.rival.revCd = 99999; V.rival.revArmed = -1; V.rival.pbCd = 99999; V.rival.pbArmed = -1;
  V.player.revCd = 99999; V.player.revArmed = -1; V.player.pbCd = 99999; V.player.pbArmed = -1;
  V.update(STEP);
  if (V.hitstopLeft > 0 && V.rival.hp < 100 && V.dmgNums.length) {
    const stamp = (V.plantDust||[]).find(p=>!p.speck && p.punch && p.homeYou===false);
    const num = V.dmgNums[0];
    gdHit=true;
    gdFreezeDustK=stamp?V.plantDustK(stamp):0;
    gdFreezeDmgK=V.dmgNumK(num);
    gdFreezeCover=V.punchCover();
    gdFreezeWhite=V.hurtFlashK(V.rival);
    gdFreezeSpark=V.hitSparkK();
    gdFreezePunch=!!(stamp&&stamp.punch);
    gdFreezeDmgPunch=!!num.punch;
    break;
  }
}
check("grit dmg cover leftover live freeze still peak", gdHit && gdFreezePunch && gdFreezeDmgPunch && gdFreezeCover===V.PUNCH_PX && gdFreezeDustK>0.9 && gdFreezeDmgK>0.9 && gdFreezeWhite>0.9 && gdFreezeSpark>0.9, `hit=${gdHit} punch=${gdFreezePunch}/${gdFreezeDmgPunch} cover=${gdFreezeCover} dustK=${gdFreezeDustK} dmgK=${gdFreezeDmgK} white=${gdFreezeWhite} spark=${gdFreezeSpark}`);
for(let i=0;i<40;i++){ if(V.hitstopLeft<=0) break; V.update(STEP); }
for(let i=0;i<3;i++){ if(V.hitstopLeft<=0 && V.shake<V.HITSTOP_HIT) break; V.update(STEP); }
{
  const stamp = (V.plantDust||[]).find(p=>!p.speck && p.punch && p.homeYou===false);
  const num = V.dmgNums[0];
  const dustK=stamp?V.plantDustK(stamp):0;
  const dmgK=num?V.dmgNumK(num):-1;
  check("grit dmg cover leftover live resume still peak with cover", V.hitstopLeft<=0 && V.punchCover()===V.PUNCH_PX && V.shake>0 && dustK>0.9 && dmgK>0.9 && V.hurtFlashK(V.rival)>0.9 && V.hitSparkK()>0.9, `hs=${V.hitstopLeft} cover=${V.punchCover()} dustK=${dustK} dmgK=${dmgK} shake=${V.shake} t=${num&&num.t}`);
}
let gdMidDustK=-1, gdMidDmgK=-1, gdMidCover=-1, gdMidRatio=-1, gdMidDmgT=-1, gdMidLin=-1;
for(let i=0;i<20;i++){
  V.update(STEP);
  if(V.shake>0 && V.shakeDur>0 && V.shake/V.shakeDur>0.25 && V.punchCover()===V.PUNCH_PX && V.dmgNums.length){
    const stamp = (V.plantDust||[]).find(p=>!p.speck && p.punch && p.homeYou===false);
    const num = V.dmgNums[0];
    gdMidDustK=stamp?V.plantDustK(stamp):0;
    gdMidDmgK=V.dmgNumK(num);
    gdMidCover=V.punchCover();
    gdMidRatio=V.shake/V.shakeDur;
    gdMidDmgT=num.t;
    gdMidLin=Math.max(0,1-Math.min(1,num.t/num.life));
    break;
  }
}
check("grit dmg cover leftover live mid-cover holds peak", gdMidCover===V.PUNCH_PX && gdMidDustK>0.9 && gdMidDmgK>0.9 && gdMidRatio>0.25 && gdMidDmgT>0 && gdMidDmgK>=gdMidLin-0.001, `cover=${gdMidCover} dustK=${gdMidDustK} dmgK=${gdMidDmgK} ratio=${gdMidRatio} t=${gdMidDmgT} lin=${gdMidLin}`);
// rise still ages (clock not held)
check("grit dmg cover leftover live rise still ages", gdMidDmgT>STEP && gdMidDmgT<V.DMG_NUM_MS, `t=${gdMidDmgT}`);
let gdEaseDustK=-1, gdEaseDmgK=-1, gdEaseCover=-1;
for(let i=0;i<40;i++){
  V.update(STEP);
  if(V.shake>0 && V.shakeDur>0 && V.shake/V.shakeDur<=0.25 && V.dmgNums.length){
    const stamp = (V.plantDust||[]).find(p=>!p.speck && p.punch && p.homeYou===false);
    const num = V.dmgNums[0];
    gdEaseDustK=stamp?V.plantDustK(stamp):-1;
    gdEaseDmgK=V.dmgNumK(num);
    gdEaseCover=V.punchCover();
    break;
  }
}
check("grit dmg cover leftover live ease with cover last quarter", gdEaseCover<V.PUNCH_PX && gdEaseCover>0 && gdEaseDustK>0 && gdEaseDmgK>0, `cover=${gdEaseCover} dustK=${gdEaseDustK} dmgK=${gdEaseDmgK}`);
for(let i=0;i<50;i++){ if(V.shake<=0) break; V.update(STEP); }
check("grit dmg cover leftover live cover dies hitSpark cleared", V.shake===0 && V.punchCover()===0 && V.hitSparkT===0 && V.hitSparkK()===0, `shake=${V.shake} cover=${V.punchCover()} sparkT=${V.hitSparkT}`);
// dmg nums survive cover (life 600) and resume linear
check("grit dmg cover leftover live dmg survives cover linear", V.dmgNums.length>0 && V.dmgNums[0].t>0 && V.dmgNums[0].t<V.DMG_NUM_MS && Math.abs(V.dmgNumK(V.dmgNums[0]) - Math.max(0,1-Math.min(1,V.dmgNums[0].t/V.dmgNums[0].life)))<0.001, `t=${V.dmgNums[0]&&V.dmgNums[0].t} k=${V.dmgNums[0]&&V.dmgNumK(V.dmgNums[0])}`);

// LIVE: walk grit unmarked — linear, no punch couple
bootPlay();
V.openLeft = 0; V.hitstopLeft = 0;
freezeYouAI(); freezeRivalAI();
V.player.phase="idle"; V.player.stunT=0; V.player.gait=0;
V.keys.clear();
V.keys.add("KeyD");
let gdWalkStamp=null;
for(let i=0;i<90;i++){
  V.update(STEP);
  gdWalkStamp=(V.plantDust||[]).find(p=>!p.speck && p.homeYou===true && !p.shove);
  if(gdWalkStamp) break;
}
check("grit dmg cover leftover live walk grit unmarked", !!gdWalkStamp && gdWalkStamp.punch!==true, `stamp=${!!gdWalkStamp} punch=${gdWalkStamp&&gdWalkStamp.punch}`);
if(gdWalkStamp){
  // age without punch
  const t0=gdWalkStamp.t;
  for(let i=0;i<5;i++) V.update(STEP);
  const s2=(V.plantDust||[]).find(p=>p===gdWalkStamp) || (V.plantDust||[]).find(p=>!p.speck && p.homeYou===true && !p.shove && p.punch!==true);
  const lin = s2 ? Math.max(0,1-s2.t/s2.life) : -1;
  const k = s2 ? V.plantDustK(s2) : -1;
  check("grit dmg cover leftover live walk grit stays linear", s2 && V.shake===0 && Math.abs(k-lin)<0.001 && s2.t>t0, `k=${k} lin=${lin} t=${s2&&s2.t} t0=${t0}`);
}

// LIVE: no-punch synthetic dmg num fades linear
bootPlay();
V.openLeft=0; V.hitstopLeft=0;
freezeYouAI(); freezeRivalAI();
V.spawnDmgNum(640, 360, 10, false, V.rival);
const dn0=V.dmgNums[V.dmgNums.length-1];
check("grit dmg cover leftover live no-punch dmg arms", dn0 && dn0.punch===true && V.shake===0 && V.punchCover()===0 && V.dmgNumK(dn0)>0.9, `punch=${dn0&&dn0.punch} shake=${V.shake} k=${dn0&&V.dmgNumK(dn0)}`);
const dnK0=V.dmgNumK(dn0);
for(let i=0;i<3;i++) V.update(STEP);
const dn1=V.dmgNums.find(p=>p===dn0) || V.dmgNums[0];
check("grit dmg cover leftover live no-punch dmg fades linear", dn1 && dn1.t>STEP && V.dmgNumK(dn1)<dnK0-0.01 && Math.abs(V.dmgNumK(dn1)-(1-Math.min(1,dn1.t/dn1.life)))<0.001 && V.shake===0, `t=${dn1&&dn1.t} k=${dn1&&V.dmgNumK(dn1)} k0=${dnK0}`);

check("cache v=331 after grit dmg cover leftover live", /game.js\?v=350/.test(htmlTxt));

// --- v331 HP drain flash + combo counter ↔ punchCover ---
check("hud combo cover leftover seats mid-slam die", /HP drain flash \+ combo counter ↔ punchCover \(v331\)/.test(srcTxt) && /hudFlashT \/ comboT used to fade/.test(srcTxt) && /HP brasa/.test(srcTxt) && /ghost flash \/ combo count died mid-cover/.test(srcTxt) && /hudFlashK/.test(srcTxt) && /comboK/.test(srcTxt));
check("hud combo cover leftover is draw-only", /Draw-only \(hudFlashK \/ comboK\)/.test(srcTxt) && /HP drain flash \+ combo counter ↔ punchCover \(v331\)/.test(srcTxt) && /destRect\/AABB planted/.test(srcTxt) && !/hudFlashK/.test(sliceFn(codeOnly, "destRect")) && !/hudFlashK/.test(sliceFn(codeOnly, "bodyAABB")) && !/hudFlashK/.test(sliceFn(codeOnly, "landHit")) && !/comboK/.test(sliceFn(codeOnly, "landHit")) && !/hudFlashK/.test(sliceFn(codeOnly, "bumpShake")) && !/comboK/.test(sliceFn(codeOnly, "noteCombo")));
check("hud combo cover leftover hudFlashK envelope", /function hudFlashK/.test(srcTxt) && /Hold peak while cover is full/.test(srcTxt) && /same smoothstep as punchCover/.test(srcTxt) && /k > 0\.25/.test(sliceFn(codeOnly, "hudFlashK")) && /u \* u \* \(3 - 2 \* u\)/.test(sliceFn(codeOnly, "hudFlashK")) && /shake > 0 && shakeDur > 0/.test(sliceFn(codeOnly, "hudFlashK")) && /f\.hudFlashT \/ HUD_FLASH_MS/.test(sliceFn(codeOnly, "hudFlashK")));
check("hud combo cover leftover comboK envelope", /function comboK/.test(srcTxt) && /k > 0\.25/.test(sliceFn(codeOnly, "comboK")) && /u \* u \* \(3 - 2 \* u\)/.test(sliceFn(codeOnly, "comboK")) && /Math\.max\(lin, ease\)/.test(sliceFn(codeOnly, "comboK")) && /!f\.comboPunch/.test(sliceFn(codeOnly, "comboK")) && /Rise still ages/.test(srcTxt) && /COMBO_SHOW_MS 640/.test(srcTxt));
check("hud combo cover leftover draw uses K fns", /const k = hudFlashK\(f\)/.test(sliceFn(codeOnly, "drawLifeBar")) && /const fade = comboK\(f\)/.test(sliceFn(codeOnly, "drawComboCount")) && !/f\.hudFlashT \/ 220/.test(sliceFn(codeOnly, "drawLifeBar")) && !/0\.45 \+ 0\.40 \* u/.test(sliceFn(codeOnly, "drawComboCount")));
check("hud combo cover leftover clock hold + punch mark", /Hold through/.test(srcTxt) && /the live punch/.test(srcTxt) && /clear when cover dies/.test(srcTxt) && /shake > 0 && shakeDur > 0/.test(sliceFn(codeOnly, "tickHudBar")) && /f\.hudFlashT >= HUD_FLASH_MS - 0\.5/.test(sliceFn(codeOnly, "tickHudBar")) && /atk\.comboPunch = atk\.comboT > 0/.test(sliceFn(codeOnly, "noteCombo")) && /f\.comboPunch = false/.test(sliceFn(codeOnly, "tickHudBar")) && V.HUD_FLASH_MS===220 && V.COMBO_SHOW_MS===640);
check("hud combo cover leftover constants / combat locked", V.HUD_FLASH_MS===220 && V.COMBO_SHOW_MS===640 && V.HITSTOP_HIT===140 && V.HIT_FLASH_MS===120 && V.PUNCH_PX===12 && V.DMG_NUM_MS===600 && V.HIT_SPARK_MS===100 && V.METER_FLASH_MS===220);
check("hud combo cover leftover tipX locks hold", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt));
check("hud combo cover leftover is not a new combat verb", V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.HIT_FLASH_MS===120 && V.SLASH_DMG===10 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("slash frames still 180/140/280 after hud combo cover leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after hud combo cover leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after hud combo cover leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("grit dmg v330 / hitSparkK v329 / clashSparkK v328 / pad v327 / parry gleam v326 / steel v323 / meter v324 / hitFlash locked after hud combo cover", V.HIT_FLASH_MS===120 && V.PUNCH_PX===12 && /k > 0\.25/.test(sliceFn(codeOnly, "hurtFlashK")) && /k > 0\.25/.test(sliceFn(codeOnly, "steelFlashK")) && /k > 0\.25/.test(sliceFn(codeOnly, "meterFlashK")) && /k > 0\.25/.test(sliceFn(codeOnly, "parryGleamK")) && /k > 0\.25/.test(sliceFn(codeOnly, "clashSparkK")) && /k > 0\.25/.test(sliceFn(codeOnly, "hitSparkK")) && /k > 0\.25/.test(sliceFn(codeOnly, "plantDustK")) && /k > 0\.25/.test(sliceFn(codeOnly, "dmgNumK")) && /Knock grit \/ plant dust \+ floating −N ↔ punchCover \(v330\)/.test(srcTxt) && /Flesh hit spark ↔ punchCover \(v329\)/.test(srcTxt) && /Clash spark ↔ punchCover \(v328\)/.test(srcTxt) && /Pad zone slide-off leftover \(v327\)/.test(srcTxt) && V.PUSHBLOCK_SHAKE===8 && V.CLASH_SHAKE===14 && V.WAKE_FX_MS===65 && V.SHEATHE_MS===140);
check("no 6th button after hud combo cover leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=331 after hud combo cover leftover", /game.js\?v=350/.test(htmlTxt));

// LIVE: hudFlash — freeze/resume/mid peak with cover; ghost holds; no-punch linear
toClose();
freezeYouAI(); freezeRivalAI();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100; V.rival.stamina = 100;
V.player.phase = "idle"; V.rival.phase = "idle";
V.player.stunT = 0; V.rival.stunT = 0;
V.player.x = 400; V.rival.x = 520;
V.player.facing = 1; V.rival.facing = -1;
V.rival.guarding = false; V.rival.wantBlock = false;
V.player.comboN = 0; V.player.comboT = 0; V.player.comboPunch = false;
V.keys.clear();
V.startAttack(V.player, "slash");
let hcHit=false, hcFreezeHud=0, hcFreezeCover=0, hcFreezeWhite=0, hcFreezeGhost=0, hcFreezeHp=0, hcFreezeHft=0;
for (let i = 0; i < 120; i++) {
  V.rival.revCd = 99999; V.rival.revArmed = -1; V.rival.pbCd = 99999; V.rival.pbArmed = -1;
  V.player.revCd = 99999; V.player.revArmed = -1; V.player.pbCd = 99999; V.player.pbArmed = -1;
  V.update(STEP);
  if (V.hitstopLeft > 0 && V.rival.hp < 100 && V.rival.hudFlashT > 0) {
    hcHit=true;
    hcFreezeHud=V.hudFlashK(V.rival);
    hcFreezeCover=V.punchCover();
    hcFreezeWhite=V.hurtFlashK(V.rival);
    hcFreezeGhost=V.rival.hudGhost;
    hcFreezeHp=V.rival.hp;
    hcFreezeHft=V.rival.hudFlashT;
    break;
  }
}
check("hud combo cover leftover live freeze hud peak", hcHit && hcFreezeCover===V.PUNCH_PX && hcFreezeHud>0.9 && hcFreezeWhite>0.9 && hcFreezeHft===V.HUD_FLASH_MS && hcFreezeGhost>hcFreezeHp, `hit=${hcHit} cover=${hcFreezeCover} hudK=${hcFreezeHud} white=${hcFreezeWhite} hft=${hcFreezeHft} ghost=${hcFreezeGhost} hp=${hcFreezeHp}`);
for(let i=0;i<40;i++){ if(V.hitstopLeft<=0) break; V.update(STEP); }
for(let i=0;i<3;i++){ if(V.hitstopLeft<=0 && V.shake<V.HITSTOP_HIT) break; V.update(STEP); }
check("hud combo cover leftover live resume still peak with cover", V.hitstopLeft<=0 && V.punchCover()===V.PUNCH_PX && V.shake>0 && V.hudFlashK(V.rival)>0.9 && V.rival.hudFlashT===V.HUD_FLASH_MS && V.hurtFlashK(V.rival)>0.9 && V.rival.hudGhost>V.rival.hp, `hs=${V.hitstopLeft} cover=${V.punchCover()} hudK=${V.hudFlashK(V.rival)} hft=${V.rival.hudFlashT} ghost=${V.rival.hudGhost}`);
let hcMidHud=-1, hcMidCover=-1, hcMidRatio=-1, hcMidHft=-1, hcMidGhost=-1;
for(let i=0;i<20;i++){
  V.update(STEP);
  if(V.shake>0 && V.shakeDur>0 && V.shake/V.shakeDur>0.25 && V.punchCover()===V.PUNCH_PX && V.rival.hudFlashT>0){
    hcMidHud=V.hudFlashK(V.rival);
    hcMidCover=V.punchCover();
    hcMidRatio=V.shake/V.shakeDur;
    hcMidHft=V.rival.hudFlashT;
    hcMidGhost=V.rival.hudGhost;
    break;
  }
}
check("hud combo cover leftover live mid-cover holds peak", hcMidCover===V.PUNCH_PX && hcMidHud>0.9 && hcMidRatio>0.25 && hcMidHft===V.HUD_FLASH_MS && hcMidGhost>V.rival.hp, `cover=${hcMidCover} hudK=${hcMidHud} ratio=${hcMidRatio} hft=${hcMidHft} ghost=${hcMidGhost}`);
let hcEaseHud=-1, hcEaseCover=-1;
for(let i=0;i<40;i++){
  V.update(STEP);
  if(V.shake>0 && V.shakeDur>0 && V.shake/V.shakeDur<=0.25 && V.rival.hudFlashT>0){
    hcEaseHud=V.hudFlashK(V.rival);
    hcEaseCover=V.punchCover();
    break;
  }
}
check("hud combo cover leftover live ease with cover last quarter", hcEaseCover<V.PUNCH_PX && hcEaseCover>0 && hcEaseHud>0 && hcEaseHud<=1.001, `cover=${hcEaseCover} hudK=${hcEaseHud}`);
for(let i=0;i<50;i++){ if(V.shake<=0) break; V.update(STEP); }
check("hud combo cover leftover live cover dies hud cleared", V.shake===0 && V.punchCover()===0 && V.rival.hudFlashT===0 && V.hudFlashK(V.rival)===0, `shake=${V.shake} cover=${V.punchCover()} hft=${V.rival.hudFlashT} k=${V.hudFlashK(V.rival)}`);

// LIVE: combo 2+ — peak with cover, rise ages
toClose();
freezeYouAI(); freezeRivalAI();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100; V.rival.stamina = 100;
V.player.phase = "idle"; V.rival.phase = "idle";
V.player.stunT = 0; V.rival.stunT = 0;
V.player.x = 400; V.rival.x = 520;
V.player.facing = 1; V.rival.facing = -1;
V.rival.guarding = false; V.rival.wantBlock = false;
V.player.comboN = 0; V.player.comboT = 0; V.player.comboPunch = false;
V.keys.clear();
V.startAttack(V.player, "slash");
let c1=false;
for (let i = 0; i < 120; i++) {
  V.rival.revCd = 99999; V.rival.revArmed = -1; V.rival.pbCd = 99999; V.rival.pbArmed = -1;
  V.player.revCd = 99999; V.player.revArmed = -1; V.player.pbCd = 99999; V.player.pbArmed = -1;
  V.update(STEP);
  if (V.hitstopLeft > 0 && V.rival.hp < 100) { c1=true; break; }
}
check("hud combo cover leftover live first connect silent", c1 && V.player.comboN===1 && V.player.comboT===0 && !V.player.comboPunch, `c1=${c1} n=${V.player.comboN} t=${V.player.comboT} punch=${V.player.comboPunch}`);
for(let i=0;i<80;i++){ if(V.hitstopLeft<=0 && V.player.phase==="idle") break; V.update(STEP); }
V.rival.stunT = Math.max(V.rival.stunT, 200);
V.player.phase="idle"; V.player.phaseT=0; V.player.cut="";
V.keys.clear();
V.startAttack(V.player, "golpe");
let c2=false, c2FreezeK=0, c2FreezeT=0, c2FreezeCover=0;
for (let i = 0; i < 120; i++) {
  V.rival.revCd = 99999; V.rival.revArmed = -1; V.rival.pbCd = 99999; V.rival.pbArmed = -1;
  V.player.revCd = 99999; V.player.revArmed = -1; V.player.pbCd = 99999; V.player.pbArmed = -1;
  V.update(STEP);
  if (V.hitstopLeft > 0 && V.player.comboN>=2 && V.player.comboT>0) {
    c2=true; c2FreezeK=V.comboK(V.player); c2FreezeT=V.player.comboT; c2FreezeCover=V.punchCover();
    break;
  }
}
check("hud combo cover leftover live second connect shows peak", c2 && V.player.comboN===2 && V.player.comboPunch===true && c2FreezeT===V.COMBO_SHOW_MS && c2FreezeK>0.9 && c2FreezeCover===V.PUNCH_PX, `c2=${c2} n=${V.player.comboN} punch=${V.player.comboPunch} t=${c2FreezeT} k=${c2FreezeK} cover=${c2FreezeCover}`);
for(let i=0;i<40;i++){ if(V.hitstopLeft<=0) break; V.update(STEP); }
for(let i=0;i<3;i++){ if(V.hitstopLeft<=0 && V.shake<V.HITSTOP_HIT) break; V.update(STEP); }
const c2ResumeT=V.player.comboT, c2ResumeK=V.comboK(V.player);
check("hud combo cover leftover live combo resume peak with cover", V.hitstopLeft<=0 && V.punchCover()===V.PUNCH_PX && V.shake>0 && c2ResumeK>0.9 && c2ResumeT<V.COMBO_SHOW_MS && c2ResumeT>V.COMBO_SHOW_MS-80, `cover=${V.punchCover()} k=${c2ResumeK} t=${c2ResumeT}`);
let c2MidK=-1, c2MidT=-1, c2MidCover=-1, c2MidLin=-1;
for(let i=0;i<20;i++){
  V.update(STEP);
  if(V.shake>0 && V.shakeDur>0 && V.shake/V.shakeDur>0.25 && V.punchCover()===V.PUNCH_PX && V.player.comboT>0){
    c2MidK=V.comboK(V.player); c2MidT=V.player.comboT; c2MidCover=V.punchCover();
    c2MidLin=Math.min(1, V.player.comboT/V.COMBO_SHOW_MS);
    break;
  }
}
check("hud combo cover leftover live combo mid-cover holds peak", c2MidCover===V.PUNCH_PX && c2MidK>0.9 && c2MidT>0 && c2MidT<V.COMBO_SHOW_MS && c2MidK>=c2MidLin-0.001, `cover=${c2MidCover} k=${c2MidK} t=${c2MidT} lin=${c2MidLin}`);
check("hud combo cover leftover live combo rise still ages", c2MidT>0 && c2MidT<V.COMBO_SHOW_MS-STEP, `t=${c2MidT}`);
for(let i=0;i<50;i++){ if(V.shake<=0) break; V.update(STEP); }
check("hud combo cover leftover live combo survives cover linear", V.player.comboT>0 && V.player.comboPunch===true && V.shake===0 && Math.abs(V.comboK(V.player)-Math.min(1,V.player.comboT/V.COMBO_SHOW_MS))<0.001, `t=${V.player.comboT} k=${V.comboK(V.player)} punch=${V.player.comboPunch}`);

// LIVE: no-punch synthetic hudFlash fades linear
bootPlay();
V.openLeft=0; V.hitstopLeft=0;
freezeYouAI(); freezeRivalAI();
V.rival.hp=80; V.rival.hudGhost=100; V.rival.hudFlashT=V.HUD_FLASH_MS;
check("hud combo cover leftover live no-punch hud arms", V.rival.hudFlashT===V.HUD_FLASH_MS && V.shake===0 && V.punchCover()===0 && V.hudFlashK(V.rival)>0.9, `hft=${V.rival.hudFlashT} k=${V.hudFlashK(V.rival)}`);
const npK0=V.hudFlashK(V.rival);
for(let i=0;i<3;i++) V.update(STEP);
check("hud combo cover leftover live no-punch hud fades linear", V.rival.hudFlashT>0 && V.rival.hudFlashT<V.HUD_FLASH_MS && V.hudFlashK(V.rival)<npK0-0.01 && Math.abs(V.hudFlashK(V.rival)-(V.rival.hudFlashT/V.HUD_FLASH_MS))<0.001 && V.shake===0, `hft=${V.rival.hudFlashT} k=${V.hudFlashK(V.rival)} k0=${npK0}`);

// LIVE: no-punch combo ages linear
V.player.comboN=3; V.player.comboT=V.COMBO_SHOW_MS; V.player.comboPunch=false;
const ncK0=V.comboK(V.player);
for(let i=0;i<3;i++) V.update(STEP);
check("hud combo cover leftover live no-punch combo fades linear", V.player.comboT>0 && V.player.comboT<V.COMBO_SHOW_MS && V.comboK(V.player)<ncK0-0.01 && Math.abs(V.comboK(V.player)-(V.player.comboT/V.COMBO_SHOW_MS))<0.001 && V.shake===0 && V.player.comboPunch===false, `t=${V.player.comboT} k=${V.comboK(V.player)} punch=${V.player.comboPunch}`);

check("cache v=331 after hud combo cover leftover live", /game.js\?v=350/.test(htmlTxt));





// --- v332 Steel / parry gleam / clash spark clocks ↔ punchCover (hold+clear) ---
check("steel parry clash clock cover leftover seats mid-slam die", /Steel \/ parry gleam \/ clash spark clocks ↔ punchCover \(v332\)/.test(srcTxt) && /steelFlashT/.test(srcTxt) && /parryGleamT/.test(srcTxt) && /clashSparkT/.test(srcTxt) && /short T could zero mid-cover/.test(srcTxt) && /draw-K peaked \(v323\/v326\/v328\)/.test(srcTxt));
check("steel parry clash clock cover leftover hold+clear seats", /Hold armed clocks through live punch/.test(srcTxt) && /clear when cover dies/.test(srcTxt) && /same as/.test(srcTxt) && /hitSparkT \/ hudFlashT/.test(srcTxt) && /steelFlashT > 0/.test(sliceFn(codeOnly, "update")) && /parryGleamT > 0/.test(sliceFn(codeOnly, "update")) && /clashSparkT > 0/.test(sliceFn(codeOnly, "update")) && /steelFlashT = 0/.test(sliceFn(codeOnly, "update")) && /parryGleamT = 0/.test(sliceFn(codeOnly, "update")) && /clashSparkT = 0/.test(sliceFn(codeOnly, "update")));
check("steel parry clash clock cover leftover draw-K envelopes hold", /function steelFlashK/.test(srcTxt) && /function parryGleamK/.test(srcTxt) && /function clashSparkK/.test(srcTxt) && /Armed clock holds through live punch/.test(srcTxt) && (srcTxt.match(/Armed clock holds through live punch/g)||[]).length>=3 && /k > 0\.25/.test(sliceFn(codeOnly, "steelFlashK")) && /k > 0\.25/.test(sliceFn(codeOnly, "parryGleamK")) && /k > 0\.25/.test(sliceFn(codeOnly, "clashSparkK")) && /steelFlashT \/ STEEL_FLASH_MS/.test(sliceFn(codeOnly, "steelFlashK")) && /parryGleamT \/ PARRY_GLEAM_MS/.test(sliceFn(codeOnly, "parryGleamK")) && /clashSparkT \/ CLASH_SPARK_MS/.test(sliceFn(codeOnly, "clashSparkK")));
check("steel parry clash clock cover leftover constants unchanged", V.STEEL_FLASH_MS===60 && V.PARRY_GLEAM_MS===80 && V.CLASH_SPARK_MS===110 && V.HITSTOP_BLOCK===60 && V.HIT_SPARK_MS===100 && V.HUD_FLASH_MS===220 && V.PUNCH_PX===12);
check("steel parry clash clock cover leftover tipX locks hold", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt));
check("steel parry clash clock cover leftover is not a new combat verb", V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.HIT_FLASH_MS===120 && V.HITSTOP_BLOCK===60 && V.STEEL_FLASH_MS===60 && V.PARRY_GLEAM_MS===80 && V.CLASH_SPARK_MS===110 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("slash frames still 180/140/280 after steel parry clash clock cover leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after steel parry clash clock cover leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after steel parry clash clock cover leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("hud combo v331 / grit dmg v330 / hitSparkK v329 / clashSparkK v328 / pad v327 / parry gleam v326 / steel v323 / meter v324 / hitFlash locked after steel parry clash clock cover", V.HIT_FLASH_MS===120 && V.PUNCH_PX===12 && /k > 0\.25/.test(sliceFn(codeOnly, "hurtFlashK")) && /k > 0\.25/.test(sliceFn(codeOnly, "steelFlashK")) && /k > 0\.25/.test(sliceFn(codeOnly, "meterFlashK")) && /k > 0\.25/.test(sliceFn(codeOnly, "parryGleamK")) && /k > 0\.25/.test(sliceFn(codeOnly, "clashSparkK")) && /k > 0\.25/.test(sliceFn(codeOnly, "hitSparkK")) && /k > 0\.25/.test(sliceFn(codeOnly, "plantDustK")) && /k > 0\.25/.test(sliceFn(codeOnly, "dmgNumK")) && /k > 0\.25/.test(sliceFn(codeOnly, "hudFlashK")) && /k > 0\.25/.test(sliceFn(codeOnly, "comboK")) && /HP drain flash \+ combo counter ↔ punchCover \(v331\)/.test(srcTxt) && /Knock grit \/ plant dust \+ floating −N ↔ punchCover \(v330\)/.test(srcTxt) && /Flesh hit spark ↔ punchCover \(v329\)/.test(srcTxt) && /Clash spark ↔ punchCover \(v328\)/.test(srcTxt) && /Pad zone slide-off leftover \(v327\)/.test(srcTxt) && V.PUSHBLOCK_SHAKE===8 && V.CLASH_SHAKE===14 && V.WAKE_FX_MS===65 && V.SHEATHE_MS===140);
check("no 6th button after steel parry clash clock cover leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=335 after steel parry clash clock cover leftover", /game.js\?v=350/.test(htmlTxt));

// LIVE: steel clock hold — resume T at peak; mid-cover T held; clear with cover; no-punch linear
toMeasure();
freezeYouAI(); freezeRivalAI();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.phase = "idle"; V.player.phaseT = 0; V.player.guarding = false;
V.rival.phase = "idle"; V.rival.guarding = true; V.rival.wantBlock = true; V.rival.guardChoice = 1;
V.rival.stamina = 100; V.rival.hp = 100;
V.player.x = V.rival.x - 160; V.keepApart();
V.attackEdge = true;
let spcHit=false, spcFreezeT=0, spcFreezeCover=0, spcFreezeK=0;
for(let i=0;i<80;i++){
  freezeRivalAI();
  V.rival.guarding = true; V.rival.wantBlock = true; V.rival.guardChoice = 1;
  V.update(STEP);
  if(V.hitstopLeft>0 && V.steelFlashT>0){
    spcHit=true; spcFreezeT=V.steelFlashT; spcFreezeCover=V.punchCover(); spcFreezeK=V.steelFlashK();
    break;
  }
}
check("steel parry clash clock cover leftover live steel freeze peak", spcHit && spcFreezeT===V.STEEL_FLASH_MS && spcFreezeCover===V.PUNCH_PX && spcFreezeK>0.9, `hit=${spcHit} t=${spcFreezeT} cover=${spcFreezeCover} k=${spcFreezeK}`);
for(let i=0;i<20;i++){ if(V.hitstopLeft<=0) break; V.update(STEP); }
for(let i=0;i<3;i++){ if(V.hitstopLeft<=0 && V.shake<V.HITSTOP_BLOCK) break; V.update(STEP); }
check("steel parry clash clock cover leftover live steel resume T held", V.hitstopLeft<=0 && V.punchCover()===V.PUNCH_PX && V.shake>0 && V.steelFlashT===V.STEEL_FLASH_MS && V.steelFlashK()>0.9, `hs=${V.hitstopLeft} cover=${V.punchCover()} t=${V.steelFlashT} k=${V.steelFlashK()} shake=${V.shake}`);
let spcMidT=-1, spcMidK=-1, spcMidCover=-1;
for(let i=0;i<20;i++){
  V.update(STEP);
  if(V.shake>0 && V.shakeDur>0 && V.shake/V.shakeDur>0.25 && V.punchCover()===V.PUNCH_PX && V.steelFlashT===V.STEEL_FLASH_MS){
    spcMidT=V.steelFlashT; spcMidK=V.steelFlashK(); spcMidCover=V.punchCover();
    break;
  }
}
check("steel parry clash clock cover leftover live steel mid-cover T held", spcMidCover===V.PUNCH_PX && spcMidT===V.STEEL_FLASH_MS && spcMidK>0.9, `cover=${spcMidCover} t=${spcMidT} k=${spcMidK}`);
for(let i=0;i<50;i++){ if(V.shake<=0) break; V.update(STEP); }
check("steel parry clash clock cover leftover live steel clears with cover", V.shake===0 && V.punchCover()===0 && V.steelFlashT===0 && V.steelFlashK()===0, `shake=${V.shake} cover=${V.punchCover()} t=${V.steelFlashT}`);

// LIVE: no-punch steel still linear
bootPlay();
V.openLeft=0; V.hitstopLeft=0;
freezeYouAI(); freezeRivalAI();
V.spawnSteelFlash(500, 300, V.rival, V.player, false, "block");
const spcNpT0=V.steelFlashT, spcNpK0=V.steelFlashK();
check("steel parry clash clock cover leftover live steel no-punch arms", spcNpT0===V.STEEL_FLASH_MS && spcNpK0>0.9 && V.shake===0 && V.punchCover()===0, `t=${spcNpT0} k=${spcNpK0}`);
for(let i=0;i<3;i++) V.update(STEP);
check("steel parry clash clock cover leftover live steel no-punch fades linear", V.steelFlashT<spcNpT0-STEP*2 && V.steelFlashT>0 && V.steelFlashK()<spcNpK0-0.05 && V.shake===0, `t=${V.steelFlashT} t0=${spcNpT0} k=${V.steelFlashK()} k0=${spcNpK0}`);

// LIVE: parry gleam clock hold mid + clear (reuses spawn path via prior live suite; synthetic + bump)
bootPlay();
V.openLeft=0; V.hitstopLeft=0;
freezeYouAI(); freezeRivalAI();
V.spawnParryGleam(640, 360, V.player);
V.bumpShake(6, 1, V.HITSTOP_BLOCK);
V.hitstopLeft=V.HITSTOP_BLOCK;
check("steel parry clash clock cover leftover live gleam freeze peak", V.parryGleamT===V.PARRY_GLEAM_MS && V.parryGleamK()>0.9 && V.punchCover()===V.PUNCH_PX, `t=${V.parryGleamT} k=${V.parryGleamK()} cover=${V.punchCover()}`);
for(let i=0;i<20;i++){ if(V.hitstopLeft<=0) break; V.update(STEP); }
for(let i=0;i<3;i++){ if(V.hitstopLeft<=0 && V.shake<V.HITSTOP_BLOCK) break; V.update(STEP); }
check("steel parry clash clock cover leftover live gleam resume T held", V.hitstopLeft<=0 && V.parryGleamT===V.PARRY_GLEAM_MS && V.parryGleamK()>0.9 && V.punchCover()===V.PUNCH_PX && V.shake>0, `hs=${V.hitstopLeft} t=${V.parryGleamT} k=${V.parryGleamK()} cover=${V.punchCover()}`);
for(let i=0;i<50;i++){ if(V.shake<=0) break; V.update(STEP); }
check("steel parry clash clock cover leftover live gleam clears with cover", V.shake===0 && V.punchCover()===0 && V.parryGleamT===0 && V.parryGleamK()===0, `shake=${V.shake} t=${V.parryGleamT}`);

// LIVE: clash spark clock hold mid + clear
bootPlay();
V.openLeft=0; V.hitstopLeft=0;
freezeYouAI(); freezeRivalAI();
V.spawnClashSpark(640, 360);
V.bumpShake(V.CLASH_SHAKE, 1, V.HITSTOP_BLOCK);
V.hitstopLeft=V.HITSTOP_BLOCK;
check("steel parry clash clock cover leftover live clash freeze peak", V.clashSparkT===V.CLASH_SPARK_MS && V.clashSparkK()>0.9 && V.punchCover()===V.PUNCH_PX, `t=${V.clashSparkT} k=${V.clashSparkK()} cover=${V.punchCover()}`);
for(let i=0;i<20;i++){ if(V.hitstopLeft<=0) break; V.update(STEP); }
for(let i=0;i<3;i++){ if(V.hitstopLeft<=0 && V.shake<V.HITSTOP_BLOCK) break; V.update(STEP); }
check("steel parry clash clock cover leftover live clash resume T held", V.hitstopLeft<=0 && V.clashSparkT===V.CLASH_SPARK_MS && V.clashSparkK()>0.9 && V.punchCover()===V.PUNCH_PX && V.shake>0, `hs=${V.hitstopLeft} t=${V.clashSparkT} k=${V.clashSparkK()} cover=${V.punchCover()}`);
for(let i=0;i<50;i++){ if(V.shake<=0) break; V.update(STEP); }
check("steel parry clash clock cover leftover live clash clears with cover", V.shake===0 && V.punchCover()===0 && V.clashSparkT===0 && V.clashSparkK()===0, `shake=${V.shake} t=${V.clashSparkT}`);

check("cache v=335 after steel parry clash clock cover leftover live", /game.js\?v=350/.test(htmlTxt));

// --- v333 Short brasa flecks ↔ punchCover (grab hold+clear; feint/rev/wake stay linear) ---
check("short brasa fleck cover leftover seats mid-slam die", /Short brasa flecks ↔ punchCover \(v333\)/.test(srcTxt) && /grab clinch puff/.test(srcTxt) && /GRAB_FX_MS 90/.test(srcTxt) && /linear-drain mid-cover/.test(srcTxt) && /Feint \/ reversal \/ wakeup flecks never arm shake/.test(srcTxt));
check("short brasa fleck cover leftover hold+clear seats", /Hold grab \(\+ hit\) brasaFxT through live punch/.test(srcTxt) && /clear when cover/.test(srcTxt) && /brasaCoverHold/.test(sliceFn(codeOnly, "update")) && /brasaFxKind === "hit" \|\| brasaFxKind === "grab"/.test(sliceFn(codeOnly, "update")) && /brasaFxT = 0/.test(sliceFn(codeOnly, "update")));
check("short brasa fleck cover leftover grab spawn notes cover", /Hold\+clear through punchCover \(v333\)/.test(srcTxt) && /landThrow arms GRAB_SHAKE \+ HITSTOP_HIT/.test(srcTxt) && /brasaFxT = GRAB_FX_MS/.test(sliceFn(codeOnly, "spawnBrasaFx")));
check("short brasa fleck cover leftover feint/rev/wake stay linear", /Feint \/ reversal \/ wakeup flecks never arm shake/.test(srcTxt) && /No shake, no hit flash, no steel asterisk \/ parry gleam/.test(srcTxt) && /No shake, no hit bloom, no steel asterisk \/ parry gleam/.test(srcTxt) && /Not reversal fleck, not parry gleam, not feint sting\. No shake/.test(srcTxt) && !/brasaFxKind === "feint"/.test((sliceFn(codeOnly, "update").match(/const brasaCoverHold[\s\S]{0,350}/)||[""])[0]) && !/brasaFxKind === "reversal"/.test((sliceFn(codeOnly, "update").match(/const brasaCoverHold[\s\S]{0,350}/)||[""])[0]) && !/brasaFxKind === "wakeup"/.test((sliceFn(codeOnly, "update").match(/const brasaCoverHold[\s\S]{0,350}/)||[""])[0]));
check("short brasa fleck cover leftover block/clash brasa stay linear", /Cast \/ block \/ clash brasa kinds unchanged/.test(srcTxt) && /brasaFxKind === "cast"/.test((sliceFn(codeOnly, "update").match(/const brasaCoverHold[\s\S]{0,350}/)||[""])[0]) && !/brasaFxKind === "block"/.test((sliceFn(codeOnly, "update").match(/const brasaCoverHold[\s\S]{0,350}/)||[""])[0]) && !/brasaFxKind === "clash"/.test((sliceFn(codeOnly, "update").match(/const brasaCoverHold[\s\S]{0,350}/)||[""])[0]) && !/brasaFxKind === "feint"/.test((sliceFn(codeOnly, "update").match(/const brasaCoverHold[\s\S]{0,350}/)||[""])[0]));
check("short brasa fleck cover leftover is juice-only", /Juice only — steel\/gleam\/clash clocks v332/.test(srcTxt) && /GRAB_FX_MS 90 \/ FEINT_FX_MS 55 \/ REVERSAL_FX_MS 70/.test(srcTxt) && !/brasaCoverHold/.test(sliceFn(codeOnly, "destRect")) && !/brasaCoverHold/.test(sliceFn(codeOnly, "bodyAABB")) && !/brasaCoverHold/.test(sliceFn(codeOnly, "landThrow")) && !/brasaCoverHold/.test(sliceFn(codeOnly, "bumpShake")) && !/brasaCoverHold/.test(sliceFn(codeOnly, "spawnBrasaFx")));
check("short brasa fleck cover leftover constants locked", V.GRAB_FX_MS===90 && V.FEINT_FX_MS===55 && V.REVERSAL_FX_MS===70 && V.WAKE_FX_MS===65 && V.GRAB_SHAKE===11 && V.HITSTOP_HIT===140 && V.BRASA_HIT_MS===140 && V.HIT_SPARK_MS===100 && V.PUNCH_PX===12);
check("steel/gleam/clash clocks v332 / hud combo v331 / grit dmg v330 / hitSparkK v329 locked after short brasa fleck cover", V.HIT_FLASH_MS===120 && V.PUNCH_PX===12 && /k > 0\.25/.test(sliceFn(codeOnly, "hurtFlashK")) && /k > 0\.25/.test(sliceFn(codeOnly, "steelFlashK")) && /k > 0\.25/.test(sliceFn(codeOnly, "meterFlashK")) && /k > 0\.25/.test(sliceFn(codeOnly, "parryGleamK")) && /k > 0\.25/.test(sliceFn(codeOnly, "clashSparkK")) && /k > 0\.25/.test(sliceFn(codeOnly, "hitSparkK")) && /k > 0\.25/.test(sliceFn(codeOnly, "plantDustK")) && /k > 0\.25/.test(sliceFn(codeOnly, "dmgNumK")) && /k > 0\.25/.test(sliceFn(codeOnly, "hudFlashK")) && /k > 0\.25/.test(sliceFn(codeOnly, "comboK")) && /Steel \/ parry gleam \/ clash spark clocks ↔ punchCover \(v332\)/.test(srcTxt) && /HP drain flash \+ combo counter ↔ punchCover \(v331\)/.test(srcTxt) && /Knock grit \/ plant dust \+ floating −N ↔ punchCover \(v330\)/.test(srcTxt) && /Flesh hit spark ↔ punchCover \(v329\)/.test(srcTxt) && V.PUSHBLOCK_SHAKE===8 && V.CLASH_SHAKE===14 && V.WAKE_FX_MS===65 && V.SHEATHE_MS===140);

// LIVE: grab fleck hold through punchCover — freeze peak; resume T held; mid-cover held; clear with cover
toClose();
freezeYouAI(); freezeRivalAI();
holdRivalStill();
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100; V.rival.stamina = 100;
V.player.thrownT = 0; V.rival.thrownT = 0;
V.rival.throwTechArmed = -1;
V.openLeft = 0; V.hitstopLeft = 0;
fireThrow();
let gfHit=false, gfFreezeT=0, gfFreezeCover=0, gfFreezeKind="";
for(let i=0;i<40;i++){
  holdRivalStill();
  V.update(STEP);
  if(V.hitstopLeft>0 && V.brasaFxKind==="grab" && V.brasaFxT>0){
    gfHit=true; gfFreezeT=V.brasaFxT; gfFreezeCover=V.punchCover(); gfFreezeKind=V.brasaFxKind;
    break;
  }
}
check("short brasa fleck cover leftover live grab freeze peak", gfHit && gfFreezeKind==="grab" && gfFreezeT===V.GRAB_FX_MS && gfFreezeCover===V.PUNCH_PX, `hit=${gfHit} kind=${gfFreezeKind} t=${gfFreezeT} cover=${gfFreezeCover}`);
for(let i=0;i<20;i++){ if(V.hitstopLeft<=0) break; V.update(STEP); }
for(let i=0;i<3;i++){ if(V.hitstopLeft<=0 && V.shake<V.HITSTOP_HIT) break; V.update(STEP); }
check("short brasa fleck cover leftover live grab resume T held", V.hitstopLeft<=0 && V.punchCover()===V.PUNCH_PX && V.shake>0 && V.brasaFxKind==="grab" && V.brasaFxT===V.GRAB_FX_MS, `hs=${V.hitstopLeft} cover=${V.punchCover()} kind=${V.brasaFxKind} t=${V.brasaFxT} shake=${V.shake}`);
let gfMidT=-1, gfMidCover=-1;
for(let i=0;i<20;i++){
  V.update(STEP);
  if(V.shake>0 && V.shakeDur>0 && V.shake/V.shakeDur>0.25 && V.punchCover()===V.PUNCH_PX && V.brasaFxKind==="grab" && V.brasaFxT===V.GRAB_FX_MS){
    gfMidT=V.brasaFxT; gfMidCover=V.punchCover();
    break;
  }
}
check("short brasa fleck cover leftover live grab mid-cover T held", gfMidCover===V.PUNCH_PX && gfMidT===V.GRAB_FX_MS, `cover=${gfMidCover} t=${gfMidT}`);
for(let i=0;i<50;i++){ if(V.shake<=0) break; V.update(STEP); }
check("short brasa fleck cover leftover live grab clears with cover", V.shake===0 && V.punchCover()===0 && V.brasaFxT===0, `shake=${V.shake} cover=${V.punchCover()} t=${V.brasaFxT} kind=${V.brasaFxKind}`);

// LIVE: feint fleck still linear (no punch) — arms and fades on FEINT_FX_MS
bootPlay();
V.openLeft=0; V.hitstopLeft=0;
freezeYouAI(); freezeRivalAI();
V.player.phase="startup"; V.player.cut="slash"; V.player.phaseT=0; V.player.facing=1;
const feOk = V.startFeint(V.player);
const feT0 = V.brasaFxT;
check("short brasa fleck cover leftover live feint arms linear", feOk && V.brasaFxKind==="feint" && feT0===V.FEINT_FX_MS && V.shake===0 && V.punchCover()===0, `ok=${feOk} kind=${V.brasaFxKind} t=${feT0}`);
for(let i=0;i<3;i++) V.update(STEP);
check("short brasa fleck cover leftover live feint fades linear", V.brasaFxKind==="feint" && V.brasaFxT<feT0-STEP*2 && V.brasaFxT>0 && V.shake===0, `t=${V.brasaFxT} t0=${feT0}`);

// LIVE: grab no-punch synthetic still linear if armed without shakeDur
bootPlay();
V.openLeft=0; V.hitstopLeft=0;
freezeYouAI(); freezeRivalAI();
V.bumpShake(0,1,0);
V.spawnBrasaFx("grab", 640, 360, 1, V.rival);
const gfNpT0=V.brasaFxT;
check("short brasa fleck cover leftover live grab no-punch arms", gfNpT0===V.GRAB_FX_MS && V.brasaFxKind==="grab" && V.shake===0 && V.punchCover()===0, `t=${gfNpT0} kind=${V.brasaFxKind}`);
for(let i=0;i<3;i++) V.update(STEP);
check("short brasa fleck cover leftover live grab no-punch fades linear", V.brasaFxT<gfNpT0-STEP*2 && V.brasaFxT>0 && V.shake===0, `t=${V.brasaFxT} t0=${gfNpT0}`);

check("cache v=335 after short brasa fleck cover leftover live", /game.js\?v=350/.test(htmlTxt));


// --- v334 Tip under boltPlantFade leftover (dart recovery tip juice) ---
check("tip under boltPlantFade leftover seats no tip dump on birth", /Tip under boltPlantFade leftover/.test(srcTxt) && /idle\/walk K recovery used to dump/.test(srcTxt) && /bladeTip onto idle-edge/.test(srcTxt) && /leftover knife still owned the sheet/.test(srcTxt) && /boltPlantFade; poseBitmap already idle/.test(srcTxt) && /tip juice hopped ~339px|~339px while bf high/.test(srcTxt));
check("tip under boltPlantFade leftover is draw-only", /Draw-only \(bladeTipX \/ bladeTipY\)/.test(srcTxt) && /Ease throwKnife→pose tip with boltPlantFade/.test(srcTxt) && /boltPlantFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /kx \+ \(tip - kx\) \* \(1 - bfTip\)/.test(sliceFn(codeOnly, "bladeTipX")) && /ky \+ \(tip - ky\) \* \(1 - bfTipY\)/.test(sliceFn(codeOnly, "bladeTipY")) && !/bfTip/.test(sliceFn(codeOnly, "destRect")) && !/bfTip/.test(sliceFn(codeOnly, "bodyAABB")) && !/bfTip/.test(sliceFn(codeOnly, "poseBitmap")) && !/bfTip/.test(sliceFn(codeOnly, "castPlantXY")));
check("tip under boltPlantFade leftover eases knife→pose tip", /const bfTip = boltPlantFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /bfTip > 0\.02/.test(sliceFn(codeOnly, "bladeTipX")) && /poseFamily\(f\)\.throwKnife/.test(sliceFn(codeOnly, "bladeTipX")) && /kx \+ \(tip - kx\) \* \(1 - bfTip\)/.test(sliceFn(codeOnly, "bladeTipX")) && /const bfTipY = boltPlantFade\(f\)/.test(sliceFn(codeOnly, "bladeTipY")) && /ky \+ \(tip - ky\) \* \(1 - bfTipY\)/.test(sliceFn(codeOnly, "bladeTipY")));
check("tip under boltPlantFade leftover tip under feintFade unchanged", /const fk = feintFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /wx \+ \(tip - wx\) \* \(1 - fk\)/.test(sliceFn(codeOnly, "bladeTipX")) && /wy \+ \(tip - wy\) \* \(1 - fk\)/.test(sliceFn(codeOnly, "bladeTipY")));
check("tip under boltPlantFade leftover tip under telegraphFade unchanged", /if \(telegraphing\(f\)\)/.test(sliceFn(codeOnly, "bladeTipX")) && /ex \+ \(tip - ex\) \* tf/.test(sliceFn(codeOnly, "bladeTipX")) && /ey \+ \(tip - ey\) \* tf/.test(sliceFn(codeOnly, "bladeTipY")));
check("tip under boltPlantFade leftover tipX under plant fades unchanged", /Math\.max\(clashPlantFade\(f\), linkPlantFade\(f\)\)/.test(sliceFn(codeOnly, "bladeTipX")) && (/sx \+ \(tip - sx\) \* \(1 - k\)/.test(sliceFn(codeOnly, "bladeTipX")) || /from \+ \(tip - from\) \* \(1 - k\)/.test(sliceFn(codeOnly, "bladeTipX"))));
check("tip under boltPlantFade leftover castPlantXY K path unchanged", /boltPlantFade\(f\) <= 0/.test(sliceFn(codeOnly, "castPlantXY")) && /poseFamily\(f\)\.throwKnife/.test(sliceFn(codeOnly, "castPlantXY")) && /clashPlantFade\(f\)/.test(sliceFn(codeOnly, "castPlantXY")) && !/bfTip/.test(sliceFn(codeOnly, "castPlantXY")));
check("tip under boltPlantFade leftover tip\/steel families unchanged", /tipX: 268/.test(srcTxt) && /tipY: 109/.test(srcTxt) && /tipX: 6/.test(srcTxt) && /tipX: 873/.test(srcTxt) && /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt) && /tipY: 105, steelX: 424/.test(srcTxt));
check("tip under boltPlantFade leftover special-cancel still 0", /holdingCutBolt/.test(sliceFn(codeOnly, "boltPlantFade")) && /Special-cancel still 0 \(holdingCutBolt\)/.test(srcTxt));
check("tip under boltPlantFade leftover is not a new combat verb", /No new combat verb/.test(srcTxt) && /active hitbox unchanged/.test(srcTxt));
check("slash frames still 180\/140\/280 after tip under boltPlantFade leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280);
check("golpe frames still 120\/80\/180 after tip under boltPlantFade leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180);
check("K frames still 200\/280 after tip under boltPlantFade leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280);
check("riposte frames still 130\/140\/280 after tip under boltPlantFade leftover", V.RIPOSTE_STARTUP===130 && V.RIPOSTE_ACTIVE===140 && V.RIPOSTE_RECOVERY===280);
check("no 6th button after tip under boltPlantFade leftover", !/KeyM/.test(codeOnly));
check("cache v=335 after tip under boltPlantFade leftover", /game.js\?v=350/.test(htmlTxt));

// live: knife→idle tip eases not dumps on dart birth
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkT=0; V.player.walkSettleT=0; V.player.walkFadeHold=0;
V.player.sheatheT=0; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.cutRecBreathT=0; V.player.telegraph=false; V.player.linkPlant=false; V.player.clashPlant=false;
V.player.boltHoldCut=false; V.player.reversal=false; V.player.facing=1; V.player.stamina=100; V.player.meter=0;
V.openLeft=0; V.hitstopLeft=0;
freezeRivalAI(); V.update(STEP);
const bfDid = V.startBolt(V.player, false);
check("tip under boltPlantFade leftover live bolt arms", bfDid===true && V.player.boltPhase==="startup", `did=${bfDid} bph=${V.player.boltPhase}`);
// advance through startup to birth
let bfBirth=null, bfPrevTip=null, bfDump=false, bfSeries=[];
for (let i=0;i<30;i++){
  freezeRivalAI(); V.update(STEP);
  const tip=V.bladeTipX(V.player); const cast=V.castPlantXY(V.player); const bf=V.boltPlantFade(V.player);
  const row={i, bph:V.player.boltPhase, tip, castX:cast.x, bf, hasBolt:!!V.bolt};
  bfSeries.push(row);
  if (bfPrevTip!=null && V.player.boltPhase==="recovery" && Math.abs(tip-bfPrevTip)>120) bfDump=true;
  if (V.player.boltPhase==="recovery" && bfBirth==null){
    bfBirth={tip, castX:cast.x, castY:cast.y, bf, tipY:V.bladeTipY(V.player), prev:bfPrevTip};
  }
  bfPrevTip=tip;
  if (V.player.boltPhase==="recovery" && V.player.boltT>80) break;
}
check("tip under boltPlantFade leftover live birth reached", !!bfBirth && bfBirth.bf>0.9, `birth=${!!bfBirth} bf=${bfBirth?bfBirth.bf.toFixed(3):"na"} bph=${V.player.boltPhase}`);
check("tip under boltPlantFade leftover live tip stays on knife at birth", bfBirth && Math.abs(bfBirth.tip-bfBirth.castX)<12 && !bfDump, `tip=${bfBirth?bfBirth.tip.toFixed(2):"na"} cast=${bfBirth?bfBirth.castX.toFixed(2):"na"} dump=${bfDump} prev=${bfBirth&&bfBirth.prev!=null?bfBirth.prev.toFixed(2):"na"}`);
// ease mid-recovery then settle to idle edge
let bfMid=null, bfEnd=null, bfTrackOk=true, bfPrev=bfBirth?bfBirth.tip:0;
const bfR0 = V.destRect(V.player);
const bfKnife0 = bfR0.dx + 268 * (bfR0.dw/1186);
const bfEdge0 = V.player.facing>0 ? bfR0.dx+bfR0.dw-8 : bfR0.dx+8;
for (let i=0;i<24;i++){
  freezeRivalAI(); V.update(STEP);
  if (!V.player.boltPhase) break;
  const tip=V.bladeTipX(V.player); const bf=V.boltPlantFade(V.player);
  const r=V.destRect(V.player); const knife=r.dx+268*(r.dw/1186); const edge=V.player.facing>0?r.dx+r.dw-8:r.dx+8;
  if (bfMid==null && bf>0.2 && bf<0.8) bfMid={tip, bf, knife, edge};
  if (bf<=0.02) bfEnd={tip, bf, knife, edge};
  if (Math.abs(tip-bfPrev)>160) bfTrackOk=false;
  bfPrev=tip;
}
check("tip under boltPlantFade leftover live tip eases mid-recovery", bfMid && Math.abs(bfMid.tip-bfKnife0)>10 && Math.abs(bfMid.tip-bfEdge0)>10, `mid=${!!bfMid} tip=${bfMid?bfMid.tip.toFixed(2):"na"} knife=${bfKnife0.toFixed(2)} edge=${bfEdge0.toFixed(2)} bf=${bfMid?bfMid.bf.toFixed(3):"na"}`);
// if recovery still running, drain it
for (let i=0;i<30;i++){ if(!V.player.boltPhase) break; freezeRivalAI(); V.update(STEP); }
const bfEndTip = V.bladeTipX(V.player);
const bfEndR = V.destRect(V.player);
const bfEndEdge = V.player.facing>0 ? bfEndR.dx+bfEndR.dw-8 : bfEndR.dx+8;
check("tip under boltPlantFade leftover live tip settles idle edge", !V.player.boltPhase && Math.abs(bfEndTip-bfEndEdge)<10 && Math.abs(bfEndTip-bfKnife0)>150 && bfTrackOk, `track=${bfTrackOk} tip=${bfEndTip.toFixed(2)} edge=${bfEndEdge.toFixed(2)} knife=${bfKnife0.toFixed(2)} bph=${V.player.boltPhase}`);
check("tip under boltPlantFade leftover live cast still knife through fade", bfBirth && Math.abs(bfBirth.castX-bfKnife0)<12, `cast=${bfBirth?bfBirth.castX.toFixed(2):"na"} knife=${bfKnife0.toFixed(2)}`);
check("tip under boltPlantFade leftover live AABB planted", Math.abs(V.bodyAABB(V.player).y - 316.693)<2, `ay=${V.bodyAABB(V.player).y.toFixed(2)}`);

// live: special-cancel K still snaps tip (boltPlantFade 0 / holdingCutBolt)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=520;
V.player.phase="recovery"; V.player.phaseT=200; V.player.cut="slash"; V.player.cutHit=true;
V.player.boltPhase=""; V.player.boltHoldCut=false; V.player.stamina=100; V.player.facing=1;
V.player.guarding=false; V.player.stunT=0; V.player.linkBolt=true; V.openLeft=0; V.hitstopLeft=0;
const bfScTip0 = V.bladeTipX(V.player);
const bfScOk = V.cancelIntoBolt(V.player);
const bfScHold = !!V.player.boltHoldCut;
const bfScBf = V.boltPlantFade(V.player);
const bfScTip1 = V.bladeTipX(V.player);
check("tip under boltPlantFade leftover live special-cancel bf 0", bfScOk ? (bfScHold && bfScBf===0) : true, `ok=${bfScOk} hold=${bfScHold} bf=${bfScBf} bph=${V.player.boltPhase}`);

check("cache v=335 after tip under boltPlantFade leftover live", /game.js\?v=350/.test(htmlTxt));

// --- v335 Hurt pose snap leftover (hurtFade SHEATHE_MS) ---
check("hurt pose snap leftover seats crumple pop on invuln death", /Hurt pose snap leftover/.test(srcTxt) && /leftover crumple used to pop hurt→idle/.test(srcTxt) && /wakeupFade dies with THROW_WAKE_INVULN 80/.test(srcTxt) && /meaty stun/.test(srcTxt) && /parryFade closed for slash→idle/.test(srcTxt));
check("hurt pose snap leftover is draw-only hurtFade", /Draw-only \(hurtFade\)/.test(srcTxt) && /function hurtFade/.test(srcTxt) && /Fade leftover hurt→idle over SHEATHE_MS/.test(srcTxt) && /hurtFadeT = SHEATHE_MS/.test(sliceFn(codeOnly, "tickThrowState")) && !/hurtFade/.test(sliceFn(codeOnly, "destRect")) && !/hurtFade/.test(sliceFn(codeOnly, "bodyAABB")) && !/hurtFade/.test(sliceFn(codeOnly, "poseBitmap")) && !/hurtFade/.test(sliceFn(codeOnly, "landHit")) && !/HITSTUN\s*=/.test(sliceFn(codeOnly, "hurtFade")));
check("hurt pose snap leftover keeps through stun\/guard\/wakeRev", /Keep through stun \/ guard \/ wakeRev like wakeupFade/.test(srcTxt) && /wakeRevFadeHold > 0 && reversalPlanting/.test(sliceFn(codeOnly, "hurtFade")) && !/if \(f\.guarding\) return 0/.test(sliceFn(codeOnly, "hurtFade")) && !/if \(f\.stunT > 0\) return 0/.test(sliceFn(codeOnly, "hurtFade")));
check("hurt pose snap leftover draw uses max wake\/hurt", /Math\.max\(wakeupFade\(f\), hurtFade\(f\)\)/.test(sliceFn(codeOnly, "drawKnight")) && /Math\.max\(wakeupFade\(f\), hurtFade\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /hurtFade\(f\) > 0/.test(sliceFn(codeOnly, "idleBreath")));
check("hurt pose snap leftover tip under hurt did not hop", /Tip under hurt did not hop/.test(srcTxt) && /shared sheet-edge/.test(srcTxt));
check("hurt pose snap leftover wakeupFade still owns invuln", /wakeupFade still owns invuln-window \/ wakeRevFadeHold/.test(srcTxt) && /THROW_WAKE_INVULN/.test(sliceFn(codeOnly, "wakeupFade")) && V.THROW_WAKE_INVULN===80);
check("hurt pose snap leftover stun\/dmg\/invuln locked", V.HITSTUN===350 && V.THROW_KD_MS===520 && V.THROW_WAKE_INVULN===80 && V.HITSTOP_HIT===140 && V.SHEATHE_MS===140 && V.THROW_DMG===20);
check("hurt pose snap leftover tip under boltPlantFade v334 locked", /Tip under boltPlantFade leftover/.test(srcTxt) && /const bfTip = boltPlantFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /kx \+ \(tip - kx\) \* \(1 - bfTip\)/.test(sliceFn(codeOnly, "bladeTipX")));
check("hurt pose snap leftover tipX locks hold", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt));
check("hurt pose snap leftover is not a new combat verb", /No new combat verb/.test(srcTxt) && !/KeyM/.test(codeOnly));
check("slash frames still 180\/140\/280 after hurt pose snap leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280);
check("golpe frames still 120\/80\/180 after hurt pose snap leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180);
check("K frames still 200\/280 after hurt pose snap leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280);
check("riposte frames still 130\/140\/280 after hurt pose snap leftover", V.RIPOSTE_STARTUP===130 && V.RIPOSTE_ACTIVE===140 && V.RIPOSTE_RECOVERY===280);
check("cache v=335 after hurt pose snap leftover", /game.js\?v=350/.test(htmlTxt));

// live: getup arms hurtFade; wake dies at invuln, hurtFade holds
bootPlay(); wait(50);
freezeYouAI();
holdRivalStill();
V.keys.clear();
V.player.x=400; V.rival.x=560; V.player.facing=1; V.rival.facing=-1;
V.rival.hp=80; V.rival.phase="idle"; V.rival.boltPhase=""; V.rival.gait=0; V.rival.closing=false;
V.rival.wantBlock=false; V.rival.guarding=false; V.rival.stunT=0; V.rival.thrownT=0; V.rival.throwInvulnT=0; V.rival.hurtFadeT=0;
V.rival.falling=false; V.rival.wakeRev=false; V.rival.wakeRevArmed=-1; V.openLeft=0; V.hitstopLeft=0;
V.landThrow(V.player, V.rival);
V.hitstopLeft=0; V.openLeft=0;
check("hurt pose snap leftover live landThrow KD", V.rival.thrownT===V.THROW_KD_MS && V.rival.hurtFadeT===0 && /hurt/.test((V.poseBitmap(V.rival)&&V.poseBitmap(V.rival)._src)||""), `th=${V.rival.thrownT} hfT=${V.rival.hurtFadeT} src=${(V.poseBitmap(V.rival)&&V.poseBitmap(V.rival)._src)||""}`);
// natural clear via ticks (do not freezeRivalAI — it zeros throwInvuln/thrownT)
let hfSaw=false, hfArm=0, hfWake0=0, hfHurt0=0, hfTip0=0, hfAabb0=null;
for(let i=0;i<90;i++){
  holdRivalStill();
  V.player.phase="idle"; V.player.boltPhase=""; V.player.gait=0; V.player.guarding=false;
  V.rival.wantBlock=false; V.rival.guardChoice=-1; V.rival.gait=0; V.rival.closing=false; V.rival.guarding=false;
  V.hitstopLeft=0;
  V.update(STEP);
  if(V.rival.thrownT<=0 && V.rival.throwInvulnT>0 && V.rival.hurtFadeT>0 && V.rival.hp>0){
    hfSaw=true;
    hfArm=V.rival.hurtFadeT;
    hfWake0=V.wakeupFade(V.rival);
    hfHurt0=V.hurtFade(V.rival);
    V.rival.x=560;
    hfTip0=V.bladeTipX(V.rival);
    hfAabb0=V.bodyAABB(V.rival);
    break;
  }
}
check("hurt pose snap leftover live arms on clear", hfSaw && V.rival.thrownT===0 && V.rival.throwInvulnT>0 && hfArm===V.SHEATHE_MS && hfHurt0>0.95 && hfWake0>0.95 && !/hurt/.test((V.poseBitmap(V.rival)&&V.poseBitmap(V.rival)._src)||""), `saw=${hfSaw} th=${V.rival.thrownT} inv=${V.rival.throwInvulnT} arm=${hfArm} hurt=${hfHurt0} wake=${hfWake0} src=${(V.poseBitmap(V.rival)&&V.poseBitmap(V.rival)._src)||""}`);
let hfAfterInv=null;
for (let i=0;i<12;i++){
  holdRivalStill();
  V.player.phase="idle"; V.player.boltPhase=""; V.player.gait=0;
  V.rival.x=560; V.rival.gait=0; V.rival.closing=false; V.rival.wantBlock=false; V.rival.guarding=false;
  if (V.rival.phase!=="startup") { V.rival.phase="idle"; V.rival.boltPhase=""; }
  V.hitstopLeft=0;
  V.update(STEP);
  if (V.rival.throwInvulnT===0 && hfAfterInv==null){
    V.rival.stunT=V.HITSTUN;
    V.rival.phase="idle"; V.rival.boltPhase=""; V.rival.guarding=false;
    hfAfterInv={wake:V.wakeupFade(V.rival), hurt:V.hurtFade(V.rival), hfT:V.rival.hurtFadeT, tip:V.bladeTipX(V.rival), st:V.rival.stunT};
  }
}
check("hurt pose snap leftover live holds after invuln dies", hfAfterInv && hfAfterInv.wake===0 && hfAfterInv.hurt>0.2 && hfAfterInv.hfT>0, `after=${JSON.stringify(hfAfterInv)}`);
check("hurt pose snap leftover live tip stable", Math.abs(V.bladeTipX(V.rival)-hfTip0)<8, `tip0=${hfTip0.toFixed(2)} tip=${V.bladeTipX(V.rival).toFixed(2)}`);
check("hurt pose snap leftover live AABB planted", hfAabb0 && Math.abs(V.bodyAABB(V.rival).y-hfAabb0.y)<0.05, `dy=${hfAabb0? (V.bodyAABB(V.rival).y-hfAabb0.y).toFixed(2):"na"}`);
check("hurt pose snap leftover live meaty keeps hurtFade", hfAfterInv && hfAfterInv.st===V.HITSTUN && V.rival.throwInvulnT===0 && hfAfterInv.hurt>0 && hfAfterInv.wake===0, `after=${JSON.stringify(hfAfterInv)} inv=${V.rival.throwInvulnT}`);
for (let i=0;i<20;i++){
  holdRivalStill();
  V.rival.x=560; V.rival.gait=0; V.rival.closing=false; V.rival.phase="idle"; V.rival.boltPhase=""; V.rival.guarding=false;
  V.hitstopLeft=0; V.update(STEP);
  if(V.hurtFade(V.rival)===0 && V.rival.hurtFadeT===0) break;
}
check("hurt pose snap leftover live fade drains", V.hurtFade(V.rival)===0 && V.rival.hurtFadeT===0, `hurt=${V.hurtFade(V.rival)} t=${V.rival.hurtFadeT}`);
bootPlay(); wait(50); freezeYouAI(); holdRivalStill();
V.player.stunT=V.HITSTUN; V.player.thrownT=0; V.player.throwInvulnT=0; V.player.hurtFadeT=0; V.player.hp=100;
check("hurt pose snap leftover live chip stun still 0", V.hurtFade(V.player)===0 && V.wakeupFade(V.player)===0 && V.player.stunT>0, `hurt=${V.hurtFade(V.player)} wake=${V.wakeupFade(V.player)} st=${V.player.stunT}`);
check("cache v=335 after hurt pose snap leftover live", /game.js\?v=350/.test(htmlTxt));









// --- v336 Hit interrupt leftover (parryFade on defender landHit) ---
check("hit interrupt leftover seats mid-cut snap", /Hit interrupt leftover \(v336\)/.test(srcTxt) && /landHit \/ landBoltHit used to pop the defender/.test(srcTxt) && /sheatheFade dies on/.test(srcTxt) && /~66px tip hop/.test(srcTxt) && /Sibling of attacker parryFade/.test(srcTxt));
check("hit interrupt leftover is draw-only parryFade", /Draw-only \(parryFade\)/.test(srcTxt) && /Hit interrupt leftover \(v336\)/.test(srcTxt) && /Arm SHEATHE_MS on def when interrupting a cut/.test(srcTxt) && /function armHitInterruptFade/.test(srcTxt) && /armHitInterruptFade\(def\)/.test(sliceFn(codeOnly, "landHit")) && /armHitInterruptFade\(def\)/.test(sliceFn(codeOnly, "landBoltHit")) && !/armHitInterruptFade/.test(sliceFn(codeOnly, "bodyAABB")) && !/armHitInterruptFade/.test(sliceFn(codeOnly, "poseBitmap")) && !/HITSTUN\s*=/.test(sliceFn(codeOnly, "armHitInterruptFade")));
check("hit interrupt leftover latches sheet", /parryFadeSheet/.test(srcTxt) && /parryFadeSheet = sheet/.test(sliceFn(codeOnly, "armHitInterruptFade")) && /parryFadeSheet === "windup"/.test(sliceFn(codeOnly, "drawKnight")) && /parryFadeSheet === "knife"/.test(sliceFn(codeOnly, "drawKnight")));
check("hit interrupt leftover clears bolt plant", /def\.boltPhase = ""/.test(sliceFn(codeOnly, "armHitInterruptFade")) && /def\.sheatheT = 0/.test(sliceFn(codeOnly, "armHitInterruptFade")) && /poseBitmap stays idle/.test(srcTxt));
check("hit interrupt leftover tip ease latched", /Hit interrupt leftover \(v336\): same ease for defender/.test(srcTxt) && /f\.parryFadeSheet \|\| "slash"/.test(sliceFn(codeOnly, "bladeTipX")) && /f\.parryFadeSheet \|\| "slash"/.test(sliceFn(codeOnly, "bladeTipY")));
check("hit interrupt leftover destRect eases ox", /ease leftover windup\/knife\/slash ox into flinch/.test(srcTxt) && /parryFadeSheet \|\| "slash"/.test(sliceFn(codeOnly, "slashPose")));
check("hit interrupt leftover attacker parry still slash", /atk\.parryFadeSheet = "slash"/.test(sliceFn(codeOnly, "landParry")) && /atk\.parryFadeT = SHEATHE_MS/.test(sliceFn(codeOnly, "landParry")));
check("hit interrupt leftover stun\/flash\/dmg locked", V.HITSTUN===350 && V.HITSTOP_HIT===140 && V.HIT_FLASH_MS===120 && V.SLASH_DMG===10 && V.SHEATHE_MS===140 && V.PARRY_STAGGER_MS===180);
check("hit interrupt leftover frames locked", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.RIPOSTE_STARTUP===130 && V.RIPOSTE_ACTIVE===140 && V.RIPOSTE_RECOVERY===280);
check("hit interrupt leftover tipX \/ pad locked", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt) && (htmlTxt.match(/data-tap=/g)||[]).length===3);
check("hit interrupt leftover does not regress dart tip v334", /Tip under boltPlantFade leftover/.test(srcTxt) && /const bfTip = boltPlantFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")));
check("hit interrupt leftover does not regress hurtFade v335", /function hurtFade/.test(srcTxt) && /hurtFadeT = SHEATHE_MS/.test(sliceFn(codeOnly, "tickThrowState")));
check("hit interrupt leftover is not a new combat verb", /No new combat verb/.test(srcTxt) && !/KeyM/.test(codeOnly));
check("cache v=336 after hit interrupt leftover", /game.js\?v=350/.test(htmlTxt));

// live: mid-cut landHit arms parryFade on defender; tip eases; idle chip still 0
bootPlay(); wait(50);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100; V.rival.stamina = 100;
V.player.phase = "idle"; V.rival.phase = "idle";
V.player.stunT = 0; V.rival.stunT = 0;
V.player.parryFadeT = 0; V.rival.parryFadeT = 0;
V.player.parryFadeSheet = ""; V.rival.parryFadeSheet = "";
V.player.x = 400; V.rival.x = 700; // keep out of blade so natural resolveCuts cannot steal the cut
V.player.facing = 1; V.rival.facing = -1;
V.rival.guarding = false; V.rival.wantBlock = false; V.rival.closing = false;
V.rival.boltPhase = ""; V.rival.sheatheT = 0; V.rival.gait = 0;
V.startAttack(V.rival, "slash");
// Force active cut sheet (natural advance can finish or clash before the probe).
V.rival.phase = "active"; V.rival.phaseT = 20; V.rival.cut = "slash";
V.rival.telegraph = false; V.rival.cutHit = false;
V.openLeft = 0; V.hitstopLeft = 0;
const hiWasActive = V.rival.phase === "active" && /slash/.test((V.poseBitmap(V.rival)&&V.poseBitmap(V.rival)._src)||"");
const hiTip0 = V.bladeTipX(V.rival);
const hiSrc0 = (V.poseBitmap(V.rival)&&V.poseBitmap(V.rival)._src)||"";
V.landHit(V.player, V.rival, 1);
const hiFade0 = V.parryFade(V.rival);
const hiT0 = V.rival.parryFadeT;
const hiSheet = V.rival.parryFadeSheet;
const hiSrc1 = (V.poseBitmap(V.rival)&&V.poseBitmap(V.rival)._src)||"";
const hiTip1 = V.bladeTipX(V.rival);
const hiPh = V.rival.phase;
check("hit interrupt leftover live arms on mid-cut", hiWasActive && hiT0===V.SHEATHE_MS && hiFade0>0.85 && hiSheet==="slash" && hiPh==="idle" && V.rival.stunT===V.HITSTUN, `was=${hiWasActive} t=${hiT0} fade=${hiFade0} sheet=${hiSheet} ph=${hiPh} stun=${V.rival.stunT}`);
check("hit interrupt leftover live poseBitmap idle", hiWasActive && !/slash|windup|hurt|knife/.test(hiSrc1), `src0=${hiSrc0} src1=${hiSrc1}`);
// Tip under fade: dedicated active ox is 0; parryFade eases leftover active ox into flinch
// (mirror attacker v325), so absolute tip may shift ~ox on the arm tick — not a dump to idle edge.
{
  const hiIdleR = V.destRect(V.rival);
  // Force a no-fade reference idle tip by reading pose tip with fade zeroed briefly.
  const _t = V.rival.parryFadeT, _s = V.rival.parryFadeSheet;
  V.rival.parryFadeT = 0; V.rival.parryFadeSheet = "";
  const hiIdleTip = V.bladeTipX(V.rival);
  V.rival.parryFadeT = _t; V.rival.parryFadeSheet = _s;
  check("hit interrupt leftover live tip not dumped to idle", hiWasActive && Math.abs(hiTip1-hiIdleTip)>20, `tip0=${hiTip0.toFixed(2)} tip1=${hiTip1.toFixed(2)} idle=${hiIdleTip.toFixed(2)}`);
}
// hold through freeze
const hiFreezeT = V.rival.parryFadeT;
for (let i=0;i<20;i++){ if(V.hitstopLeft<=0) break; V.update(STEP); }
check("hit interrupt leftover live holds through freeze", V.hitstopLeft<=0 && V.rival.parryFadeT===hiFreezeT && V.parryFade(V.rival)>0.7, `hs=${V.hitstopLeft} t=${V.rival.parryFadeT} freezeT=${hiFreezeT} fade=${V.parryFade(V.rival)}`);
const hiTipMid0 = V.bladeTipX(V.rival);
const hiRel0 = hiTipMid0 - V.rival.x;
const hiDest0 = V.destRect(V.rival);
const hiOff0 = hiDest0.dx - V.rival.x;
for (let i=0;i<4;i++) V.update(STEP);
const hiTipMid1 = V.bladeTipX(V.rival);
const hiRel1 = hiTipMid1 - V.rival.x;
const hiFadeMid = V.parryFade(V.rival);
const hiDest1 = V.destRect(V.rival);
const hiOff1 = hiDest1.dx - V.rival.x;
check("hit interrupt leftover live fade drops", hiFadeMid>=0 && hiFadeMid<0.95 && hiFadeMid>0.15 && V.rival.parryFadeT>0, `mid=${hiFadeMid} t=${V.rival.parryFadeT}`);
// Relative tip (minus knock push on x). Absolute tip moves with pushT.
check("hit interrupt leftover live tip eases no hop", Math.abs(hiRel1-hiRel0)<45, `rel0=${hiRel0.toFixed(2)} rel1=${hiRel1.toFixed(2)} t0=${hiTipMid0.toFixed(2)} t1=${hiTipMid1.toFixed(2)}`);
check("hit interrupt leftover live destRect eases ox no teleport", Math.abs(hiOff1)<Math.abs(hiOff0)+2 && Math.abs(hiOff1-hiOff0)<50, `off0=${hiOff0.toFixed(2)} off1=${hiOff1.toFixed(2)}`);
for (let i=0;i<40;i++){ if(V.rival.parryFadeT<=0) break; V.update(STEP); }
check("hit interrupt leftover live fade gone", V.rival.parryFadeT===0 && V.parryFade(V.rival)===0 && V.rival.phase==="idle", `t=${V.rival.parryFadeT} fade=${V.parryFade(V.rival)}`);

// live: idle chip stun still no fade
bootPlay(); wait(50);
freezeYouAI(); freezeRivalAI();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.hp = 100; V.rival.hp = 100;
V.player.phase = "idle"; V.rival.phase = "idle";
V.player.stunT = 0; V.rival.stunT = 0;
V.player.parryFadeT = 0; V.rival.parryFadeT = 0;
V.player.parryFadeSheet = ""; V.rival.parryFadeSheet = "";
V.player.x = 400; V.rival.x = 520;
V.player.facing = 1; V.rival.facing = -1;
V.keys.clear();
V.rival.guarding = false; V.rival.wantBlock = false; V.rival.boltPhase = ""; V.rival.sheatheT = 0;
V.startAttack(V.player, "slash");
let hiChipOk=false;
for(let i=0;i<120;i++){
  V.rival.revCd = 99999; V.rival.revArmed = -1; V.rival.pbCd = 99999; V.rival.pbArmed = -1;
  V.update(STEP);
  if(V.rival.stunT>0 && V.rival.hp<100){ hiChipOk=true; break; }
}
check("hit interrupt leftover live idle chip stun no fade", hiChipOk && V.rival.parryFadeT===0 && V.parryFade(V.rival)===0 && V.rival.parryFadeSheet==="" && V.rival.stunT>0, `ok=${hiChipOk} t=${V.rival.parryFadeT} sheet=${V.rival.parryFadeSheet} stun=${V.rival.stunT}`);

// live: windup interrupt latches windup sheet
bootPlay(); wait(50);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.hp = 100; V.rival.hp = 100;
V.player.x = 400; V.rival.x = 520;
V.player.facing = 1; V.rival.facing = -1;
V.rival.guarding = false; V.rival.stunT = 0; V.rival.parryFadeT = 0; V.rival.parryFadeSheet = "";
V.startAttack(V.rival, "slash");
for (let i=0;i<10;i++){ V.update(STEP); if(V.rival.phase==="startup") break; }
const hiWu = V.rival.phase==="startup";
V.landHit(V.player, V.rival, 1);
check("hit interrupt leftover live windup latches", hiWu && V.rival.parryFadeSheet==="windup" && V.rival.parryFadeT===V.SHEATHE_MS && V.parryFade(V.rival)>0.85, `wu=${hiWu} sheet=${V.rival.parryFadeSheet} t=${V.rival.parryFadeT} fade=${V.parryFade(V.rival)}`);

check("cache v=336 after hit interrupt leftover live", /game.js\?v=350/.test(htmlTxt));


// --- v337 Clash/tech/pushblock dust ↔ punchCover (spawn after bumpShake) ---
check("clash tech pb dust cover leftover seats pre-shake orphan", /Clash\/tech\/pushblock dust ↔ punchCover \(v337\)/.test(srcTxt) && /spawnPlantDust before bumpShake/.test(srcTxt) && /punch mark never armed/.test(srcTxt) && /Sibling/.test(srcTxt) && /knock grit v330/.test(srcTxt));
check("clash tech pb dust cover leftover is draw-only plantDustK", /Draw-only \(plantDustK\)/.test(srcTxt) && /Clash\/tech\/pushblock dust ↔ punchCover \(v337\)/.test(srcTxt) && /Arm[\s\S]{0,40}shake before dust/.test(srcTxt) && /bumpShake\(CLASH_SHAKE/.test(sliceFn(codeOnly, "doClash")) && /bumpShake\(TECH_SHAKE/.test(sliceFn(codeOnly, "landThrowTech")) && /bumpShake\(PUSHBLOCK_SHAKE/.test(sliceFn(codeOnly, "tryPushblock")) && !/HITSTUN\s*=/.test(sliceFn(codeOnly, "plantDustK")) && !/plantDustK/.test(sliceFn(codeOnly, "destRect")) && !/plantDustK/.test(sliceFn(codeOnly, "bodyAABB")));
check("clash tech pb dust cover leftover clash arms shake before dust", (()=>{ const fn=sliceFn(codeOnly,"doClash"); const iB=fn.indexOf("bumpShake(CLASH_SHAKE"); const iD=fn.indexOf("spawnPlantDust(player, 1.2)"); const iS=fn.indexOf("spawnPlantDust(player, CLASH_FX"); return iB>=0 && iD>=0 && iS>=0 && iB<iD && iB<iS; })());
check("clash tech pb dust cover leftover tech arms shake before dust", (()=>{ const fn=sliceFn(codeOnly,"landThrowTech"); const iB=fn.indexOf("bumpShake(TECH_SHAKE"); const iD=fn.indexOf("spawnPlantDust(atk, 1.1)"); const iS=fn.indexOf("spawnPlantDust(atk, TECH_FX"); return iB>=0 && iD>=0 && iS>=0 && iB<iD && iB<iS; })());
check("clash tech pb dust cover leftover pushblock arms shake before dust", (()=>{ const fn=sliceFn(codeOnly,"tryPushblock"); const iB=fn.indexOf("bumpShake(PUSHBLOCK_SHAKE"); const iD=fn.indexOf("spawnPlantDust(f, PUSHBLOCK_FX"); return iB>=0 && iD>=0 && iB<iD; })());
check("clash tech pb dust cover leftover plantDustK envelope unchanged", /function plantDustK/.test(srcTxt) && /!p\.punch/.test(sliceFn(codeOnly, "plantDustK")) && /k > 0\.25/.test(sliceFn(codeOnly, "plantDustK")) && /Math\.max\(lin, ease\)/.test(sliceFn(codeOnly, "plantDustK")) && /Clash\/tech\/pushblock dust ↔ punchCover \(v337\)/.test(srcTxt));
check("clash tech pb dust cover leftover cull\/skip\/fx locked", /dropPlantUnderClash/.test(sliceFn(codeOnly, "doClash")) && /dropBlockPlantUnderShove/.test(sliceFn(codeOnly, "tryPushblock")) && V.CLASH_FX===1.7 && V.TECH_FX===1.5 && V.PUSHBLOCK_FX===1.85 && V.HITSTOP_BLOCK===60 && V.CLASH_SHAKE===14 && V.TECH_SHAKE===9 && V.PUSHBLOCK_SHAKE===8);
check("clash tech pb dust cover leftover landHit still shake-before-dust", (()=>{ const fn=sliceFn(codeOnly,"landHit"); const iB=fn.indexOf("bumpShake"); const iD=fn.indexOf("spawnPlantDust"); return iB>=0 && iD>=0 && iB<iD; })());
check("clash tech pb dust cover leftover walk grit unmarked path unchanged", /spawnPlantDust\(f, 1\)/.test(srcTxt) && /spawnPlantDust\(f, 0\.8\)/.test(srcTxt));
check("clash tech pb dust cover leftover does not regress hit interrupt v336", /Hit interrupt leftover \(v336\)/.test(srcTxt) && /armHitInterruptFade\(def\)/.test(sliceFn(codeOnly, "landHit")));
check("clash tech pb dust cover leftover does not regress hurtFade v335", /function hurtFade/.test(srcTxt) && /hurtFadeT = SHEATHE_MS/.test(sliceFn(codeOnly, "tickThrowState")));
check("clash tech pb dust cover leftover does not regress dart tip v334", /Tip under boltPlantFade leftover/.test(srcTxt) && /const bfTip = boltPlantFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")));
check("clash tech pb dust cover leftover tipX \/ pad locked", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt) && (htmlTxt.match(/data-tap=/g)||[]).length===3);
check("clash tech pb dust cover leftover is not a new combat verb", V.HITSTUN===350 && V.HITSTOP_HIT===140 && V.HITSTOP_BLOCK===60 && V.SLASH_DMG===10 && V.PUSHBLOCK_STAM===25 && V.PUSHBLOCK_PX===240 && !/KeyM/.test(codeOnly));
check("slash frames still 180\/140\/280 after clash tech pb dust cover leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120\/80\/180 after clash tech pb dust cover leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200\/280 after clash tech pb dust cover leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("riposte frames still 130\/140\/280 after clash tech pb dust cover leftover", V.RIPOSTE_STARTUP===130 && V.RIPOSTE_ACTIVE===140 && V.RIPOSTE_RECOVERY===280);
check("cache v=337 after clash tech pb dust cover leftover", /game.js\?v=350/.test(htmlTxt));

// LIVE: clash dust punch-marks and holds mid-cover
bootPlay(); wait(50);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.hp = 100; V.rival.hp = 100;
V.player.x = 480; V.rival.x = 560;
V.player.facing = 1; V.rival.facing = -1;
V.doClash(520, 400);
const cld0 = V.plantDust.filter(p => !p.speck);
const cldPunch0 = cld0.filter(p => p.punch);
const cldShove0 = cld0.filter(p => p.shove);
check("clash tech pb dust cover leftover live clash punch-marks", cldPunch0.length>=4 && cldShove0.every(p=>p.punch) && V.shake>0 && V.punchCover()>0, `n=${cld0.length} punch=${cldPunch0.length} shovePunch=${cldShove0.filter(p=>p.punch).length} shake=${V.shake} cover=${V.punchCover()}`);
let cldFreezeK = cldPunch0.length ? Math.min(...cldPunch0.map(p=>V.plantDustK(p))) : -1;
check("clash tech pb dust cover leftover live clash freeze peak", V.hitstopLeft>0 && cldFreezeK>=0.99 && V.punchCover()>=10, `hs=${V.hitstopLeft} k=${cldFreezeK} cover=${V.punchCover()}`);
let cldMid=null;
for(let i=0;i<60;i++){
  V.update(STEP);
  const coverK = V.shake>0 && V.shakeDur>0 ? V.shake/V.shakeDur : 0;
  const stamps = V.plantDust.filter(p=>p.punch && !p.speck);
  if(V.hitstopLeft<=0 && V.shake>0 && coverK>0.25 && coverK<0.9 && stamps.length && !cldMid){
    cldMid={i, coverK:+coverK.toFixed(3), cover:V.punchCover(), k:Math.min(...stamps.map(p=>V.plantDustK(p))), lin:Math.min(...stamps.map(p=>Math.max(0,1-p.t/p.life))), n:stamps.length};
  }
  if(V.shake===0 && cldMid) break;
}
check("clash tech pb dust cover leftover live clash mid-cover holds", cldMid && cldMid.k>=0.99 && cldMid.k>=cldMid.lin-0.001, `mid=${JSON.stringify(cldMid)}`);

// LIVE: tech dust punch-marks (no hitstop — ages through cover)
bootPlay(); wait(50);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.hp = 100; V.rival.hp = 100;
V.player.x = 480; V.rival.x = 560;
V.player.facing = 1; V.rival.facing = -1;
V.startAttack(V.player, "throw");
for(let i=0;i<20;i++){ V.update(STEP); if(V.player.phase==="startup") break; }
V.plantDust.length = 0;
V.landThrowTech(V.player, V.rival);
const td0 = V.plantDust.filter(p=>!p.speck);
check("clash tech pb dust cover leftover live tech punch-marks", td0.length>=4 && td0.every(p=>p.punch) && V.shake>0, `n=${td0.length} punch=${td0.filter(p=>p.punch).length} shake=${V.shake}`);
let tdMid=null;
for(let i=0;i<40;i++){
  V.update(STEP);
  const coverK = V.shake>0 && V.shakeDur>0 ? V.shake/V.shakeDur : 0;
  const stamps = V.plantDust.filter(p=>p.punch && !p.speck);
  if(V.shake>0 && coverK>0.25 && coverK<0.9 && stamps.length && !tdMid){
    tdMid={i, coverK:+coverK.toFixed(3), k:Math.min(...stamps.map(p=>V.plantDustK(p))), lin:Math.min(...stamps.map(p=>Math.max(0,1-p.t/p.life)))};
  }
  if(V.shake===0 && tdMid) break;
}
check("clash tech pb dust cover leftover live tech mid-cover holds", tdMid && tdMid.k>=0.99 && tdMid.k>tdMid.lin+0.01, `mid=${JSON.stringify(tdMid)}`);

// LIVE: pushblock dust punch-marks
bootPlay(); wait(50);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.hp = 100; V.rival.hp = 100;
V.player.x = 480; V.rival.x = 560;
V.player.facing = 1; V.rival.facing = -1;
V.player.guarding = true; V.player.guardPoseK = 1; V.player.stamina = 100;
V.plantDust.length = 0;
const pbOk = V.tryPushblock(V.player, -1);
const pbd = V.plantDust.filter(p=>!p.speck);
check("clash tech pb dust cover leftover live pb punch-marks", pbOk && pbd.length>=1 && pbd.every(p=>p.punch) && V.shake>0, `ok=${pbOk} n=${pbd.length} punch=${pbd.filter(p=>p.punch).length} shake=${V.shake}`);
let pbMid=null;
for(let i=0;i<40;i++){
  V.update(STEP);
  const coverK = V.shake>0 && V.shakeDur>0 ? V.shake/V.shakeDur : 0;
  const stamps = V.plantDust.filter(p=>p.punch && !p.speck);
  if(V.shake>0 && coverK>0.25 && coverK<0.9 && stamps.length && !pbMid){
    pbMid={i, coverK:+coverK.toFixed(3), k:Math.min(...stamps.map(p=>V.plantDustK(p))), lin:Math.min(...stamps.map(p=>Math.max(0,1-p.t/p.life)))};
  }
  if(V.shake===0 && pbMid) break;
}
check("clash tech pb dust cover leftover live pb mid-cover holds", pbMid && pbMid.k>=0.99 && pbMid.k>pbMid.lin+0.01, `mid=${JSON.stringify(pbMid)}`);

// LIVE: walk grit still unmarked (no shake)
bootPlay(); wait(50);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
// drain any leftover punch cover
for(let i=0;i<90;i++){ if(V.shake<=0 && V.hitstopLeft<=0) break; V.update(STEP); }
V.plantDust.length = 0;
V.spawnPlantDust(V.player, 1);
const wg = V.plantDust.filter(p=>!p.speck);
check("clash tech pb dust cover leftover live walk grit unmarked", V.shake<=0 && wg.length>=1 && wg.every(p=>!p.punch) && V.plantDustK(wg[0])===1, `shake=${V.shake} n=${wg.length} punch=${wg.filter(p=>p.punch).length}`);

check("cache v=337 after clash tech pb dust cover leftover live", /game.js\?v=350/.test(htmlTxt));


// --- v338 Hurt settle→walk plant-release (tickHurtFade arms cutRec on hurtFadeT clear) ---
check("hurt settle→walk plant-release destRect leftover seats breath hop on hurtFade clear", /Hurt settle→walk plant-release destRect leftover/.test(srcTxt) && /leftover hurtFade breath used to/.test(srcTxt) && /seat mid-stride \/ mid-raise/.test(srcTxt) && /throw-invuln already ended idle/.test(srcTxt) && /no cutRec arm/.test(srcTxt) && /Same[\s\S]{0,80}hole wakeup \/ feint \/ chip stun/.test(srcTxt));
check("hurt settle→walk plant-release destRect leftover is destRect-only", /destRect-only \(idleBreath \/ cutRecBreathT\)/.test(srcTxt) && /function tickHurtFade/.test(srcTxt) && /tickHurtFade arms cutRecBreathT/.test(srcTxt) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickHurtFade")) && /prevHurt > 0/.test(sliceFn(codeOnly, "tickHurtFade")) && !/hurtFadeT/.test(sliceFn(codeOnly, "destRect")) && !/tickHurtFade/.test(sliceFn(codeOnly, "destRect")) && !/tickHurtFade/.test(sliceFn(codeOnly, "bodyAABB")) && !/tickHurtFade/.test(sliceFn(codeOnly, "poseBitmap")) && !/HITSTUN\s*=/.test(sliceFn(codeOnly, "tickHurtFade")));
check("hurt settle→walk plant-release destRect leftover keeps 0 through walk during hurtFade", /wakeupFade\(f\) > 0 \|\| hurtFade\(f\) > 0/.test(sliceFn(codeOnly, "idleBreath")) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02\) return 0/.test(sliceFn(codeOnly, "idleBreath")) && /Keep breath 0 through walking \/ walk-out/.test(srcTxt));
check("hurt settle→walk plant-release destRect leftover arms cutRec on clear", /prevHurt > 0/.test(sliceFn(codeOnly, "tickHurtFade")) && /f\.hurtFadeT === 0/.test(sliceFn(codeOnly, "tickHurtFade")) && /walking\(f\) \|\| recoveryWalkOut\(f\) \|\| f\.walkFadeHold > 0\.02 \|\| f\.guarding \|\| f\.guardPoseK > 0/.test(sliceFn(codeOnly, "tickHurtFade")) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickHurtFade")) && /Mirror tickThrowState invuln-end \/ tickStun chip-end/.test(srcTxt));
check("hurt settle→walk plant-release destRect leftover idle hurt still eases", /Idle hurt→idle still eases with hurtFade/.test(srcTxt) && /1 - Math\.max\(wakeupFade\(f\), hurtFade\(f\)\)/.test(sliceFn(codeOnly, "idleBreath")) && /hurtFade\(f\) > 0/.test(sliceFn(codeOnly, "idleBreath")));
check("hurt settle→walk plant-release destRect leftover wired in update", (()=>{ const iH=codeOnly.indexOf("tickHurtFade(player, dt)"); const iT=codeOnly.indexOf("tickThrowState(player, dt)"); return iH>=0 && iT>=0 && iH<iT && /tickHurtFade\(rival, dt\)/.test(codeOnly) && !/player\.hurtFadeT = Math\.max\(0, player\.hurtFadeT - dt\)/.test(codeOnly); })());
check("hurt settle→walk plant-release destRect leftover does not regress hurtFade v335", /function hurtFade/.test(srcTxt) && /hurtFadeT = SHEATHE_MS/.test(sliceFn(codeOnly, "tickThrowState")) && /Math\.max\(wakeupFade\(f\), hurtFade\(f\)\)/.test(sliceFn(codeOnly, "drawKnight")));
check("hurt settle→walk plant-release destRect leftover does not regress dust cover v337", /Clash\/tech\/pushblock dust ↔ punchCover \(v337\)/.test(srcTxt) && /bumpShake\(CLASH_SHAKE/.test(sliceFn(codeOnly, "doClash")));
check("hurt settle→walk plant-release destRect leftover does not regress hit interrupt v336", /Hit interrupt leftover \(v336\)/.test(srcTxt) && /armHitInterruptFade\(def\)/.test(sliceFn(codeOnly, "landHit")));
check("hurt settle→walk plant-release destRect leftover tipX \/ pad \/ sheathe locked", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt) && V.SHEATHE_MS===140 && V.THROW_WAKE_INVULN===80 && V.GUARD_RAISE_MS===80 && (htmlTxt.match(/data-tap=/g)||[]).length===3);
check("hurt settle→walk plant-release destRect leftover is not a new combat verb", V.HITSTUN===350 && V.HITSTOP_HIT===140 && V.SLASH_DMG===10 && !/KeyM/.test(codeOnly));
check("slash frames still 180\/140\/280 after hurt settle→walk plant-release", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120\/80\/180 after hurt settle→walk plant-release", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200\/280 after hurt settle→walk plant-release", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("riposte frames still 130\/140\/280 after hurt settle→walk plant-release", V.RIPOSTE_STARTUP===130 && V.RIPOSTE_ACTIVE===140 && V.RIPOSTE_RECOVERY===280);
check("cache v=338 after hurt settle→walk plant-release", /game.js\?v=350/.test(htmlTxt));

// LIVE: invuln ends idle, walk through hurtFade clear — no breath hop; cutRec arms
bootPlay(); wait(50);
freezeYouAI();
V.keys.clear(); V.openLeft=0; V.hitstopLeft=0;
V.player.x=400; V.rival.x=700; V.player.facing=1; V.rival.facing=-1;
V.player.hp=100; V.rival.hp=100;
V.rival.closing=false; V.rival.gait=0; V.rival.wantBlock=false; V.rival.guarding=false;
V.rival.revArmed=-1; V.rival.pbArmed=-1; V.rival.revCd=99999; V.rival.pbCd=99999;
V.landThrow(V.rival, V.player);
V.hitstopLeft=0; V.openLeft=0;
for(let i=0;i<300;i++){
  V.rival.closing=false; V.rival.gait=0; V.rival.wantBlock=false; V.rival.phase="idle"; V.rival.boltPhase="";
  V.keys.clear(); V.update(STEP);
  if(V.player.thrownT<=0 && V.player.throwInvulnT>0) break;
}
for(let i=0;i<80;i++){
  V.rival.closing=false; V.rival.gait=0; V.rival.phase="idle"; V.rival.boltPhase="";
  V.keys.clear(); V.player.gait=0; V.player.walkFadeHold=0; V.player.guarding=false; V.player.guardPoseK=0;
  V.update(STEP);
  if(V.player.throwInvulnT<=0) break;
}
check("hurt settle→walk plant-release live invuln dead idle no cutRec", V.player.throwInvulnT===0 && V.player.hurtFadeT>0 && V.player.cutRecBreathT===0 && V.hurtFade(V.player)>0, `inv=${V.player.throwInvulnT} hfT=${V.player.hurtFadeT} cbt=${V.player.cutRecBreathT} hurt=${V.hurtFade(V.player)}`);
// wait near end idle
for(let i=0;i<40;i++){
  V.rival.closing=false; V.rival.gait=0; V.rival.phase="idle"; V.rival.boltPhase="";
  V.keys.clear(); V.player.gait=0; V.player.walkFadeHold=0; V.player.guarding=false; V.player.guardPoseK=0;
  V.update(STEP);
  if(V.player.hurtFadeT>0 && V.player.hurtFadeT<=STEP*4) break;
}
let hf338MaxSeat=0, hf338Armed=false, hf338Cbt=0, hf338Hop=null, hf338SawClear=false;
V.player.walkRiseT=0; V.player.walkFadeHold=0;
for(let i=0;i<30;i++){
  V.rival.closing=false; V.rival.gait=0; V.rival.phase="idle"; V.rival.boltPhase="";
  V.keys.clear(); V.keys.add("KeyD"); V.openLeft=0; V.hitstopLeft=0;
  const br0=V.idleBreath(V.player); const hf0=V.player.hurtFadeT; const c0=V.player.cutRecBreathT;
  V.update(STEP);
  const br1=V.idleBreath(V.player);
  if(Math.abs(br1)>hf338MaxSeat) hf338MaxSeat=Math.abs(br1);
  if(hf0>0 && V.player.hurtFadeT===0){
    hf338SawClear=true;
    hf338Armed = V.player.cutRecBreathT>0;
    hf338Cbt = V.player.cutRecBreathT;
    hf338Hop = {br0, br1, cbt:V.player.cutRecBreathT, rwo:V.recoveryWalkOut(V.player), gait:V.player.gait, hold:V.player.walkFadeHold};
  }
  if(V.player.hurtFadeT===0 && i>8) break;
}
check("hurt settle→walk plant-release live walk clear saw", hf338SawClear, `saw=${hf338SawClear} hfT=${V.player.hurtFadeT}`);
check("hurt settle→walk plant-release live walk arms cutRec", hf338Armed && hf338Cbt>0, `armed=${hf338Armed} cbt=${hf338Cbt} hop=${JSON.stringify(hf338Hop)}`);
check("hurt settle→walk plant-release live walk no breath hop", hf338MaxSeat<0.35, `maxSeat=${hf338MaxSeat.toFixed(3)} hop=${JSON.stringify(hf338Hop)}`);

// LIVE: raise mid-hurtFade clear
bootPlay(); wait(50);
freezeYouAI();
V.keys.clear(); V.openLeft=0; V.hitstopLeft=0;
V.player.x=400; V.rival.x=700; V.player.facing=1; V.rival.facing=-1;
V.rival.closing=false; V.rival.gait=0; V.rival.wantBlock=false; V.rival.revCd=99999; V.rival.pbCd=99999;
V.landThrow(V.rival, V.player); V.hitstopLeft=0; V.openLeft=0;
for(let i=0;i<300;i++){ V.rival.closing=false; V.rival.gait=0; V.rival.phase="idle"; V.keys.clear(); V.update(STEP); if(V.player.thrownT<=0 && V.player.throwInvulnT>0) break; }
for(let i=0;i<80;i++){ V.rival.closing=false; V.rival.gait=0; V.rival.phase="idle"; V.keys.clear(); V.player.guarding=false; V.player.guardPoseK=0; V.update(STEP); if(V.player.throwInvulnT<=0) break; }
for(let i=0;i<40;i++){ V.rival.closing=false; V.rival.gait=0; V.rival.phase="idle"; V.keys.clear(); V.player.guarding=false; V.player.guardPoseK=0; V.update(STEP); if(V.player.hurtFadeT>0 && V.player.hurtFadeT<=STEP*4) break; }
let hf338RMax=0, hf338RArmed=false, hf338RClear=false, hf338RHop=null;
for(let i=0;i<30;i++){
  V.rival.closing=false; V.rival.gait=0; V.rival.phase="idle";
  V.keys.clear(); V.keys.add("KeyS"); V.openLeft=0; V.hitstopLeft=0;
  const br0=V.idleBreath(V.player); const hf0=V.player.hurtFadeT;
  V.update(STEP);
  const br1=V.idleBreath(V.player);
  if(Math.abs(br1)>hf338RMax) hf338RMax=Math.abs(br1);
  if(hf0>0 && V.player.hurtFadeT===0){
    hf338RClear=true; hf338RArmed=V.player.cutRecBreathT>0;
    hf338RHop={br0,br1,cbt:V.player.cutRecBreathT,raise:V.guardRaiseK(V.player),gpk:V.player.guardPoseK,g:V.player.guarding};
  }
  if(V.player.hurtFadeT===0 && i>8) break;
}
check("hurt settle→walk plant-release live raise clear saw", hf338RClear, `saw=${hf338RClear}`);
check("hurt settle→walk plant-release live raise arms cutRec", hf338RArmed, `armed=${hf338RArmed} hop=${JSON.stringify(hf338RHop)}`);
check("hurt settle→walk plant-release live raise no breath hop", hf338RMax<0.35, `maxSeat=${hf338RMax.toFixed(3)} hop=${JSON.stringify(hf338RHop)}`);

// LIVE: idle hurt→idle still eases (no cutRec arm)
bootPlay(); wait(50);
freezeYouAI();
V.keys.clear(); V.openLeft=0; V.hitstopLeft=0;
V.player.x=400; V.rival.x=700;
V.rival.closing=false; V.rival.gait=0; V.rival.revCd=99999; V.rival.pbCd=99999;
V.landThrow(V.rival, V.player); V.hitstopLeft=0;
for(let i=0;i<300;i++){ V.rival.closing=false; V.rival.gait=0; V.rival.phase="idle"; V.keys.clear(); V.update(STEP); if(V.player.thrownT<=0 && V.player.throwInvulnT>0) break; }
for(let i=0;i<200;i++){
  V.rival.closing=false; V.rival.gait=0; V.rival.phase="idle";
  V.keys.clear(); V.player.gait=0; V.player.walkFadeHold=0; V.player.guarding=false; V.player.guardPoseK=0;
  V.update(STEP);
  if(V.player.throwInvulnT<=0 && V.player.hurtFadeT<=0) break;
}
const hf338IdleBr = V.idleBreath(V.player);
check("hurt settle→walk plant-release live idle still eases / no arm", V.player.hurtFadeT===0 && V.player.cutRecBreathT===0 && V.player.throwInvulnT===0 && Math.abs(hf338IdleBr)>0.2, `hfT=${V.player.hurtFadeT} cbt=${V.player.cutRecBreathT} br=${hf338IdleBr.toFixed(3)}`);

check("cache v=338 after hurt settle→walk plant-release live", /game.js\?v=350/.test(htmlTxt));


// --- v339 Throw interrupt leftover (landThrow mid-cut → hurt via parryFade) ---
check("throw interrupt leftover seats mid-cut sheet snap", /Throw interrupt leftover \(v339\)/.test(srcTxt) && /landThrow used to pop the defender mid-cut/.test(srcTxt) && /startup tip hopped ~270px/.test(srcTxt) && /Sibling of hit interrupt v336/.test(srcTxt) && /Rest leftover cut on hurt/.test(srcTxt));
check("throw interrupt leftover is draw-only parryFade", /Draw-only[\s\S]{0,40}\(parryFade\)/.test(srcTxt) && /Throw interrupt leftover \(v339\)/.test(srcTxt) && /armHitInterruptFade\(def\)/.test(sliceFn(codeOnly, "landThrow")) && !/HITSTUN\s*=/.test(sliceFn(codeOnly, "landThrow")) && !/THROW_KD_MS\s*=/.test(sliceFn(codeOnly, "armHitInterruptFade")) && !/armHitInterruptFade/.test(sliceFn(codeOnly, "destRect")) && !/armHitInterruptFade/.test(sliceFn(codeOnly, "bodyAABB")) && !/armHitInterruptFade/.test(sliceFn(codeOnly, "poseBitmap")));
check("throw interrupt leftover latches sheet before thrownT", (()=>{ const fn=sliceFn(codeOnly,"landThrow"); const iA=fn.indexOf("armHitInterruptFade(def)"); const iT=fn.indexOf("def.thrownT = THROW_KD_MS"); const iP=fn.indexOf('def.phase = "idle"'); return iA>=0 && iT>=0 && iP>=0 && iA<iT && iA<iP; })());
check("throw interrupt leftover rests cut on hurt", /parryBase = \(\(f\.thrownT > 0 \|\| f\.falling \|\| f\.hp <= 0\) && ready\(hurt\)\) \? hurt/.test(sliceFn(codeOnly, "drawKnight")) && /octx\.drawImage\(parryBase/.test(sliceFn(codeOnly, "drawKnight")) && /Rest leftover cut on hurt while pf live/.test(srcTxt));
check("throw interrupt leftover destRect eases ox into plant", /Throw interrupt leftover \(v339\): ease leftover mid-cut/.test(srcTxt) && /parryFade\(f\)/.test(sliceFn(codeOnly, "slashPose")) && /f\.thrownT > 0 && f\.hp > 0/.test(sliceFn(codeOnly, "slashPose")) && /fromRot \* pk/.test(sliceFn(codeOnly, "slashPose")));
check("throw interrupt leftover tip ease latched", /Throw interrupt leftover \(v339\): same ease mid-cut → hurt/.test(srcTxt) && /parryFadeSheet/.test(sliceFn(codeOnly, "bladeTipX")) && /Throw interrupt leftover \(v339\): landThrow mid-cut → hurt tipY/.test(srcTxt));
check("throw interrupt leftover clears bolt plant", /def\.boltPhase = ""/.test(sliceFn(codeOnly, "armHitInterruptFade")) && /def\.boltHoldCut = false/.test(sliceFn(codeOnly, "armHitInterruptFade")));
check("throw interrupt leftover idle throw still snaps", /Idle throw still snaps/.test(srcTxt) && /Idle throw still 0/.test(srcTxt));
check("throw interrupt leftover does not regress hit interrupt v336", /Hit interrupt leftover \(v336\)/.test(srcTxt) && /armHitInterruptFade\(def\)/.test(sliceFn(codeOnly, "landHit")) && /armHitInterruptFade\(def\)/.test(sliceFn(codeOnly, "landBoltHit")));
check("throw interrupt leftover does not regress hurt settle→walk v338", /Hurt settle→walk plant-release destRect leftover \(v338\)/.test(srcTxt) && /f\.cutRecBreathT = GUARD_RAISE_MS/.test(sliceFn(codeOnly, "tickHurtFade")));
check("throw interrupt leftover does not regress dust cover v337", /Clash\/tech\/pushblock dust ↔ punchCover \(v337\)/.test(srcTxt) && /bumpShake\(CLASH_SHAKE/.test(sliceFn(codeOnly, "doClash")));
check("throw interrupt leftover tipX \/ pad \/ KD locked", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt) && V.THROW_KD_MS===520 && V.THROW_DMG===20 && V.SHEATHE_MS===140 && V.HITSTOP_HIT===140 && (htmlTxt.match(/data-tap=/g)||[]).length===3);
check("throw interrupt leftover is not a new combat verb", V.HITSTUN===350 && V.HITSTOP_HIT===140 && V.SLASH_DMG===10 && V.THROW_STARTUP===80 && V.THROW_ACTIVE===40 && V.THROW_RECOVERY===220 && V.THROW_RANGE===120 && !/KeyM/.test(codeOnly));
check("slash frames still 180\/140\/280 after throw interrupt leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120\/80\/180 after throw interrupt leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200\/280 after throw interrupt leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("riposte frames still 130\/140\/280 after throw interrupt leftover", V.RIPOSTE_STARTUP===130 && V.RIPOSTE_ACTIVE===140 && V.RIPOSTE_RECOVERY===280);
check("cache v=341 after throw interrupt leftover", /game.js\?v=350/.test(htmlTxt));

// LIVE: mid-startup windup → landThrow arms fade, hurt sheet, tip not dumped
bootPlay(); wait(50);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.hp = 100; V.rival.hp = 100;
V.player.x = 400; V.rival.x = 520;
V.player.facing = 1; V.rival.facing = -1;
V.rival.guarding = false; V.rival.wantBlock = false; V.rival.stunT = 0;
V.rival.parryFadeT = 0; V.rival.parryFadeSheet = "";
V.rival.thrownT = 0; V.rival.boltPhase = ""; V.rival.sheatheT = 0;
V.startAttack(V.rival, "slash");
for (let i=0;i<12;i++){ V.update(STEP); if(V.rival.phase==="startup") break; }
const tiWasWu = V.rival.phase==="startup";
const tiSrc0 = sheetSrc(V.rival);
const tiTip0 = V.bladeTipX(V.rival);
const tiDest0 = V.destRect(V.rival);
V.landThrow(V.player, V.rival);
const tiT0 = V.rival.parryFadeT;
const tiFade0 = V.parryFade(V.rival);
const tiSheet = V.rival.parryFadeSheet;
const tiSrc1 = sheetSrc(V.rival);
const tiTip1 = V.bladeTipX(V.rival);
const tiDest1 = V.destRect(V.rival);
const tiAabb0 = V.bodyAABB(V.rival);
check("throw interrupt leftover live arms on mid-windup", tiWasWu && tiT0===V.SHEATHE_MS && tiFade0>0.85 && tiSheet==="windup" && V.rival.thrownT===V.THROW_KD_MS && V.rival.phase==="idle", `was=${tiWasWu} t=${tiT0} fade=${tiFade0} sheet=${tiSheet} th=${V.rival.thrownT}`);
check("throw interrupt leftover live poseBitmap hurt", tiWasWu && /hurt/.test(tiSrc1) && /windup/.test(tiSrc0), `src0=${tiSrc0} src1=${tiSrc1}`);
{
  const _t = V.rival.parryFadeT, _s = V.rival.parryFadeSheet;
  V.rival.parryFadeT = 0; V.rival.parryFadeSheet = "";
  const tiHurtTip = V.bladeTipX(V.rival);
  V.rival.parryFadeT = _t; V.rival.parryFadeSheet = _s;
  check("throw interrupt leftover live tip not dumped to hurt edge", tiWasWu && Math.abs(tiTip1-tiHurtTip)>40, `tip0=${tiTip0.toFixed(2)} tip1=${tiTip1.toFixed(2)} hurt=${tiHurtTip.toFixed(2)}`);
}
check("throw interrupt leftover live destRect eases ox", tiWasWu && Math.abs(tiDest1.rot)>0.05 && Math.abs(tiDest1.rot)<Math.abs(tiDest0.rot||tiDest1.rot)+0.5, `rot0=${tiDest0.rot.toFixed(3)} rot1=${tiDest1.rot.toFixed(3)}`);
check("throw interrupt leftover live AABB planted", Math.abs(V.bodyAABB(V.rival).y-tiAabb0.y)<0.05 && Math.abs(V.bodyAABB(V.rival).h-tiAabb0.h)<0.05, `dy=${(V.bodyAABB(V.rival).y-tiAabb0.y).toFixed(2)}`);
// hold through freeze
const tiFreezeT = V.rival.parryFadeT;
for (let i=0;i<20;i++){ if(V.hitstopLeft<=0) break; V.update(STEP); }
check("throw interrupt leftover live holds through freeze", V.hitstopLeft<=0 && V.rival.parryFadeT===tiFreezeT && V.parryFade(V.rival)>0.7, `hs=${V.hitstopLeft} t=${V.rival.parryFadeT} freezeT=${tiFreezeT} fade=${V.parryFade(V.rival)}`);
const tiTipMid0 = V.bladeTipX(V.rival);
const tiRel0 = tiTipMid0 - V.rival.x;
const tiOff0 = V.destRect(V.rival).dx - V.rival.x;
for (let i=0;i<4;i++) V.update(STEP);
const tiTipMid1 = V.bladeTipX(V.rival);
const tiRel1 = tiTipMid1 - V.rival.x;
const tiFadeMid = V.parryFade(V.rival);
const tiOff1 = V.destRect(V.rival).dx - V.rival.x;
check("throw interrupt leftover live fade drops", tiFadeMid>=0 && tiFadeMid<0.95 && tiFadeMid>0.15 && V.rival.parryFadeT>0, `mid=${tiFadeMid} t=${V.rival.parryFadeT}`);
check("throw interrupt leftover live tip eases no hop", Math.abs(tiRel1-tiRel0)<160, `rel0=${tiRel0.toFixed(2)} rel1=${tiRel1.toFixed(2)} t0=${tiTipMid0.toFixed(2)} t1=${tiTipMid1.toFixed(2)}`);
check("throw interrupt leftover live destRect eases ox no teleport", Math.abs(tiOff1-tiOff0)<55, `off0=${tiOff0.toFixed(2)} off1=${tiOff1.toFixed(2)}`);
for (let i=0;i<40;i++){ if(V.rival.parryFadeT<=0) break; V.update(STEP); }
check("throw interrupt leftover live fade gone hurt remains", V.rival.parryFadeT===0 && V.parryFade(V.rival)===0 && V.rival.thrownT>0 && /hurt/.test(sheetSrc(V.rival)), `t=${V.rival.parryFadeT} th=${V.rival.thrownT} src=${sheetSrc(V.rival)}`);

// LIVE: idle throw still no fade
bootPlay(); wait(50);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.hp = 100; V.rival.hp = 100;
V.player.x = 400; V.rival.x = 520;
V.player.facing = 1; V.rival.facing = -1;
V.rival.phase = "idle"; V.rival.boltPhase = ""; V.rival.sheatheT = 0;
V.rival.parryFadeT = 0; V.rival.parryFadeSheet = "";
V.landThrow(V.player, V.rival);
check("throw interrupt leftover live idle throw no fade", V.rival.thrownT===V.THROW_KD_MS && V.rival.parryFadeT===0 && V.parryFade(V.rival)===0 && V.rival.parryFadeSheet==="" && /hurt/.test(sheetSrc(V.rival)), `th=${V.rival.thrownT} t=${V.rival.parryFadeT} sheet=${V.rival.parryFadeSheet} src=${sheetSrc(V.rival)}`);

// LIVE: mid-active slash latch (direct landThrow; resolveThrow loses to active mash)
bootPlay(); wait(50);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.x = 400; V.rival.x = 520;
V.player.facing = 1; V.rival.facing = -1;
V.rival.phase = "active"; V.rival.phaseT = 40; V.rival.cut = "slash"; V.rival.cutHit = false;
V.rival.parryFadeT = 0; V.rival.parryFadeSheet = "";
V.landThrow(V.player, V.rival);
check("throw interrupt leftover live active slash latches", V.rival.parryFadeSheet==="slash" && V.rival.parryFadeT===V.SHEATHE_MS && V.parryFade(V.rival)>0.85 && V.rival.thrownT===V.THROW_KD_MS, `sheet=${V.rival.parryFadeSheet} t=${V.rival.parryFadeT} fade=${V.parryFade(V.rival)} th=${V.rival.thrownT}`);

check("cache v=341 after throw interrupt leftover live", /game.js\?v=350/.test(htmlTxt));



// --- v340: tech / KO interrupt leftover (mid-cut sheet snaps) ---
check("tech KO interrupt leftover seats mid-cut snaps", /Tech \/ KO interrupt leftover \(v340\)/.test(srcTxt) && /landThrowTech used to dump slash \/ sheathe/.test(srcTxt) && /parryFade died on hp<=0/.test(srcTxt) && /tip hopped ~270px/.test(srcTxt) && /Sibling of throw interrupt v339/.test(srcTxt));
check("tech KO interrupt leftover is draw-only parryFade", /Draw-only \(parryFade\)/.test(srcTxt) && /Tech \/ KO interrupt leftover \(v340\)/.test(srcTxt) && /armHitInterruptFade\(atk\)/.test(sliceFn(codeOnly, "landThrowTech")) && /armHitInterruptFade\(def\)/.test(sliceFn(codeOnly, "landThrowTech")) && !/HITSTUN\s*=/.test(sliceFn(codeOnly, "landThrowTech")) && !/armHitInterruptFade/.test(sliceFn(codeOnly, "bodyAABB")) && !/armHitInterruptFade/.test(sliceFn(codeOnly, "poseBitmap")));
check("tech KO interrupt leftover latches before phase dump", (()=>{ const fn=sliceFn(codeOnly,"landThrowTech"); const iA=fn.indexOf("armHitInterruptFade(atk)"); const iD=fn.indexOf("armHitInterruptFade(def)"); const iP=fn.indexOf('f.phase = "recovery"'); return iA>=0 && iD>=0 && iP>=0 && iA<iP && iD<iP; })());
check("tech KO interrupt leftover allows pf on techRec", /techCut = !!\(f\.techRec && f\.cut === "throw" && f\.phase === "recovery"\)/.test(sliceFn(codeOnly, "parryFade")) && /f\.phase !== "idle" && !techCut/.test(sliceFn(codeOnly, "parryFade")) && !/f\.falling \|\| f\.hp <= 0/.test(sliceFn(codeOnly, "parryFade")));
check("tech KO interrupt leftover rests on windup or hurt", /f\.techRec && f\.cut === "throw" && ready\(wind\)/.test(sliceFn(codeOnly, "drawKnight")) && /f\.thrownT > 0 \|\| f\.falling \|\| f\.hp <= 0/.test(sliceFn(codeOnly, "drawKnight")) && /Rest leftover cut on windup \(tech\) or hurt \(KO\)/.test(srcTxt));
check("tech KO interrupt leftover skips throwPlant while pf", /!restParry && ready\(wind\) && recoveryWalkOut/.test(sliceFn(codeOnly, "drawKnight")) && /!restThrowWalk && !restParry && ready\(wind\)/.test(sliceFn(codeOnly, "drawKnight")));
check("tech KO interrupt leftover idle tech still snaps", /Idle tech \/ tech-from-guard still own their snaps/.test(srcTxt) && /Idle tech still 0/.test(srcTxt));
check("tech KO interrupt leftover does not regress throw interrupt v339", /Throw interrupt leftover \(v339\)/.test(srcTxt) && /armHitInterruptFade\(def\)/.test(sliceFn(codeOnly, "landThrow")));
check("tech KO interrupt leftover does not regress hit interrupt v336", /Hit interrupt leftover \(v336\)/.test(srcTxt) && /armHitInterruptFade\(def\)/.test(sliceFn(codeOnly, "landHit")));
check("tech KO interrupt leftover tipX \/ pad \/ frames locked", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt) && V.THROW_RECOVERY===220 && V.HITSTOP_BLOCK===60 && V.SHEATHE_MS===140 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && (htmlTxt.match(/data-tap=/g)||[]).length===3);
check("tech KO interrupt leftover is not a new combat verb", V.HITSTUN===350 && V.HITSTOP_HIT===140 && V.SLASH_DMG===10 && V.THROW_STARTUP===80 && V.THROW_RANGE===120 && !/KeyM/.test(codeOnly));
check("slash frames still 180\/140\/280 after tech KO interrupt leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120\/80\/180 after tech KO interrupt leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200\/280 after tech KO interrupt leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("riposte frames still 130\/140\/280 after tech KO interrupt leftover", V.RIPOSTE_STARTUP===130 && V.RIPOSTE_ACTIVE===140 && V.RIPOSTE_RECOVERY===280);
check("cache v=341 after tech KO interrupt leftover", /game.js\?v=350/.test(htmlTxt));

// LIVE: landThrowTech from slash recovery arms fade, tip not dumped to windup
bootPlay(); wait(50);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.openLeft = 0; V.hitstopLeft = 0;
V.player.hp = 100; V.rival.hp = 100;
V.player.x = 400; V.rival.x = 520;
V.player.facing = 1; V.rival.facing = -1;
V.rival.guarding = false; V.rival.wantBlock = false; V.rival.stunT = 0;
V.rival.parryFadeT = 0; V.rival.parryFadeSheet = "";
V.rival.thrownT = 0; V.rival.boltPhase = ""; V.rival.sheatheT = 0;
V.player.phase = "startup"; V.player.phaseT = 40; V.player.cut = "throw";
V.rival.phase = "recovery"; V.rival.phaseT = 40; V.rival.cut = "slash"; V.rival.cutHit = true;
const v340TkSrc0 = sheetSrc(V.rival);
const v340TkTip0 = V.bladeTipX(V.rival);
const v340TkWasSlash = /slash/.test(v340TkSrc0);
V.landThrowTech(V.player, V.rival);
const v340TkT0 = V.rival.parryFadeT;
const v340TkFade0 = V.parryFade(V.rival);
const v340TkSheet = V.rival.parryFadeSheet;
const v340TkSrc1 = sheetSrc(V.rival);
const v340TkTip1 = V.bladeTipX(V.rival);
const v340TkAabb = V.bodyAABB(V.rival);
const v340TkDy0 = V.destRect(V.rival).dy;
check("tech KO interrupt leftover live tech arms on slash recovery", v340TkWasSlash && v340TkT0===V.SHEATHE_MS && v340TkFade0>0.85 && v340TkSheet==="slash" && V.rival.techRec && V.rival.phase==="recovery" && V.rival.cut==="throw", `was=${v340TkWasSlash} t=${v340TkT0} fade=${v340TkFade0} sheet=${v340TkSheet} tech=${V.rival.techRec}`);
check("tech KO interrupt leftover live tech poseBitmap windup", v340TkWasSlash && /windup/.test(v340TkSrc1) && /slash/.test(v340TkSrc0), `src0=${v340TkSrc0} src1=${v340TkSrc1}`);
check("tech KO interrupt leftover live tech tip not dumped to windup edge", v340TkWasSlash && Math.abs(v340TkTip1 - v340TkTip0) < 40, `tip0=${v340TkTip0.toFixed(2)} tip1=${v340TkTip1.toFixed(2)}`);
check("tech KO interrupt leftover live tech AABB planted", Math.abs(V.bodyAABB(V.rival).y - v340TkAabb.y) < 0.5, `dy=${(V.bodyAABB(V.rival).y - v340TkAabb.y).toFixed(2)}`);

// LIVE: idle tech still snaps (no fade)
bootPlay(); wait(50);
freezeYouAI(); freezeRivalAI();
V.keys.clear(); V.openLeft=0; V.hitstopLeft=0;
V.player.x=400; V.rival.x=520; V.player.facing=1; V.rival.facing=-1;
V.player.phase="startup"; V.player.phaseT=40; V.player.cut="throw";
V.rival.phase="idle"; V.rival.parryFadeT=0; V.rival.parryFadeSheet="";
V.landThrowTech(V.player, V.rival);
check("tech KO interrupt leftover live idle tech no fade", V.rival.techRec && V.rival.parryFadeT===0 && V.parryFade(V.rival)===0 && V.rival.parryFadeSheet==="", `tech=${V.rival.techRec} t=${V.rival.parryFadeT} sheet=${V.rival.parryFadeSheet}`);

// LIVE: KO mid-windup landHit keeps pf through hp<=0
bootPlay(); wait(50);
freezeYouAI(); freezeRivalAI();
V.keys.clear(); V.openLeft=0; V.hitstopLeft=0;
V.player.x=400; V.rival.x=520; V.player.facing=1; V.rival.facing=-1;
V.rival.guarding=false; V.rival.wantBlock=false; V.rival.stunT=0;
V.rival.parryFadeT=0; V.rival.parryFadeSheet=""; V.rival.hp=10;
V.rival.phase="startup"; V.rival.phaseT=40; V.rival.cut="slash";
const v340KoSrc0 = sheetSrc(V.rival);
const v340KoTip0 = V.bladeTipX(V.rival);
const v340KoWasWu = V.rival.phase==="startup";
V.landHit(V.player, V.rival, 1);
const v340KoT0 = V.rival.parryFadeT;
const v340KoFade0 = V.parryFade(V.rival);
const v340KoSheet = V.rival.parryFadeSheet;
const v340KoSrc1 = sheetSrc(V.rival);
const v340KoTip1 = V.bladeTipX(V.rival);
check("tech KO interrupt leftover live KO arms on mid-windup", v340KoWasWu && V.rival.hp===0 && v340KoT0===V.SHEATHE_MS && v340KoFade0>0.85 && v340KoSheet==="windup", `was=${v340KoWasWu} hp=${V.rival.hp} t=${v340KoT0} fade=${v340KoFade0} sheet=${v340KoSheet}`);
check("tech KO interrupt leftover live KO poseBitmap hurt", v340KoWasWu && /hurt/.test(v340KoSrc1) && /windup/.test(v340KoSrc0), `src0=${v340KoSrc0} src1=${v340KoSrc1}`);
check("tech KO interrupt leftover live KO tip not dumped to hurt edge", v340KoWasWu && Math.abs(v340KoTip1 - v340KoTip0) < 80, `tip0=${v340KoTip0.toFixed(2)} tip1=${v340KoTip1.toFixed(2)}`);
V.koTarget = V.rival;
V.beginFall();
const v340KoFadeFall = V.parryFade(V.rival);
const v340KoTipFall = V.bladeTipX(V.rival);
check("tech KO interrupt leftover live KO pf through beginFall", V.rival.falling && v340KoFadeFall>0.85 && V.rival.parryFadeT===V.SHEATHE_MS, `fall=${V.rival.falling} fade=${v340KoFadeFall} t=${V.rival.parryFadeT}`);
check("tech KO interrupt leftover live KO tip holds through beginFall", Math.abs(v340KoTipFall - v340KoTip1) < 20, `tip1=${v340KoTip1.toFixed(2)} tipFall=${v340KoTipFall.toFixed(2)}`);

// LIVE: chip hit interrupt still fades (no regress v336)
bootPlay(); wait(50);
freezeYouAI(); freezeRivalAI();
V.keys.clear(); V.openLeft=0; V.hitstopLeft=0;
V.player.x=400; V.rival.x=520; V.player.facing=1; V.rival.facing=-1;
V.rival.hp=100; V.rival.phase="active"; V.rival.phaseT=40; V.rival.cut="slash"; V.rival.cutHit=false;
V.rival.parryFadeT=0; V.rival.parryFadeSheet="";
V.landHit(V.player, V.rival, 1);
check("tech KO interrupt leftover live chip hit still fades", V.rival.hp===90 && V.rival.parryFadeT===V.SHEATHE_MS && V.parryFade(V.rival)>0.85 && V.rival.parryFadeSheet==="slash", `hp=${V.rival.hp} t=${V.rival.parryFadeT} fade=${V.parryFade(V.rival)} sheet=${V.rival.parryFadeSheet}`);

check("cache v=341 after tech KO interrupt leftover live", /game.js\?v=350/.test(htmlTxt));


// --- v341 tip under throw-startup→tech leftover ---
check("tip under throw-startup→tech leftover seats tipPlantK dump", /Tip under throw-startup→tech leftover \(v341\)/.test(srcTxt) && /dump tipX ~152px onto windup tip/.test(srcTxt) && /throwGuardPlanting \/ tipPlantK while the sheet stayed windup/.test(srcTxt) && /Latch tipPlantK raise fraction/.test(srcTxt) && /ease tipX\/tipY from that blend through/.test(srcTxt));
check("tip under throw-startup→tech leftover is draw-only parryFade", /Draw-only \(parryFade\)/.test(srcTxt) && /Tip under throw-startup→tech leftover \(v341\)/.test(srcTxt) && /parryFadeTipK/.test(sliceFn(codeOnly, "armHitInterruptFade")) && /parryFadeTipK/.test(sliceFn(codeOnly, "bladeTipX")) && /parryFadeTipK/.test(sliceFn(codeOnly, "bladeTipY")) && !/THROW_STARTUP\s*=/.test(sliceFn(codeOnly, "armHitInterruptFade")) && !/parryFadeTipK/.test(sliceFn(codeOnly, "bodyAABB")) && !/parryFadeTipK/.test(sliceFn(codeOnly, "poseBitmap")));
check("tip under throw-startup→tech leftover latches tipK on throw startup", /def\.cut === "throw" && GUARD_RAISE_MS > 0/.test(sliceFn(codeOnly, "armHitInterruptFade")) && /tipK = 1 - blend/.test(sliceFn(codeOnly, "armHitInterruptFade")) && /raiseT \* tf/.test(sliceFn(codeOnly, "armHitInterruptFade")) && /def\.parryFadeTipK = tipK/.test(sliceFn(codeOnly, "armHitInterruptFade")));
check("tip under throw-startup→tech leftover keeps pf in tickStun techCut", /techCut = !!\(f\.techRec && f\.cut === "throw" && f\.phase === "recovery"\)/.test(sliceFn(codeOnly, "tickStun")) && /Keep latched pf while techCut/.test(srcTxt));
check("tip under throw-startup→tech leftover eases blend under pf", /const tipK = f\.parryFadeTipK \|\| 0/.test(sliceFn(codeOnly, "bladeTipX")) && /ex \+ \(sx - ex\) \* \(1 - tipK\)/.test(sliceFn(codeOnly, "bladeTipX")) && /by \+ \(sy - by\) \* \(1 - tipK\)/.test(sliceFn(codeOnly, "bladeTipY")));
check("tip under throw-startup→tech leftover does not regress tech KO v340", /Tech \/ KO interrupt leftover \(v340\)/.test(srcTxt) && /armHitInterruptFade\(atk\)/.test(sliceFn(codeOnly, "landThrowTech")) && /armHitInterruptFade\(def\)/.test(sliceFn(codeOnly, "landThrowTech")));
check("tip under throw-startup→tech leftover does not regress throw interrupt v339", /Throw interrupt leftover \(v339\)/.test(srcTxt) && /armHitInterruptFade\(def\)/.test(sliceFn(codeOnly, "landThrow")));
check("tip under throw-startup→tech leftover does not regress hit interrupt v336", /Hit interrupt leftover \(v336\)/.test(srcTxt) && /armHitInterruptFade\(def\)/.test(sliceFn(codeOnly, "landHit")));
check("tip under throw-startup→tech leftover tipX / pad / frames locked", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt) && V.THROW_STARTUP===80 && V.THROW_RECOVERY===220 && V.SHEATHE_MS===140 && V.GUARD_RAISE_MS===80 && V.HITSTOP_BLOCK===60 && (htmlTxt.match(/data-tap=/g)||[]).length===3);
check("tip under throw-startup→tech leftover is not a new combat verb", V.HITSTUN===350 && V.HITSTOP_HIT===140 && V.SLASH_DMG===10 && V.THROW_STARTUP===80 && V.THROW_RANGE===120 && !/KeyM/.test(codeOnly));
check("slash frames still 180/140/280 after tip under throw-startup→tech leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after tip under throw-startup→tech leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after tip under throw-startup→tech leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("riposte frames still 130/140/280 after tip under throw-startup→tech leftover", V.RIPOSTE_STARTUP===130 && V.RIPOSTE_ACTIVE===140 && V.RIPOSTE_RECOVERY===280);
check("cache v=341 after tip under throw-startup→tech leftover", /game.js\?v=350/.test(htmlTxt));

// live: attacker throw startup mid-raise → tech holds tip through pf
freezeYouAI(); freezeRivalAI();
V.keys.clear(); V.openLeft=0; V.hitstopLeft=0;
V.player.x=400; V.rival.x=520; V.player.facing=1; V.rival.facing=-1;
V.player.phase="startup"; V.player.phaseT=40; V.player.cut="throw"; V.player.telegraph=false;
V.player.parryFadeT=0; V.player.parryFadeSheet=""; V.player.parryFadeTipK=0;
V.rival.phase="idle"; V.rival.guarding=false;
const v341Tip0 = V.bladeTipX(V.player);
const v341TipY0 = V.bladeTipY(V.player);
const v341Aabb0 = V.bodyAABB(V.player);
const v341WuTip = (()=>{ const r=V.destRect(V.player); const s=V.poseScale(V.player); const wu=V.poseFamily(V.player).windup; return r.dx + wu.tipX * s; })();
const v341Edge = (()=>{ const r=V.destRect(V.player); return V.player.facing>0 ? r.dx + r.dw - 8 : r.dx + 8; })();
const v341Dump = Math.abs(v341Tip0 - v341WuTip);
V.landThrowTech(V.player, V.rival);
const v341Tip1 = V.bladeTipX(V.player);
const v341TipY1 = V.bladeTipY(V.player);
const v341Hop = Math.abs(v341Tip1 - v341Tip0);
check("tip under throw-startup→tech leftover live arms tipK", V.player.techRec && V.player.parryFadeT===V.SHEATHE_MS && V.parryFade(V.player)>0.85 && V.player.parryFadeSheet==="windup" && V.player.parryFadeTipK>0.4 && V.player.parryFadeTipK<0.6, `tech=${V.player.techRec} t=${V.player.parryFadeT} fade=${V.parryFade(V.player)} sheet=${V.player.parryFadeSheet} tipK=${V.player.parryFadeTipK}`);
check("tip under throw-startup→tech leftover live pre-tip was mid-raise blend", v341Dump>100 && Math.abs(v341Tip0-v341Edge)>100, `tip0=${v341Tip0.toFixed(2)} wu=${v341WuTip.toFixed(2)} edge=${v341Edge.toFixed(2)} dump=${v341Dump.toFixed(2)}`);
check("tip under throw-startup→tech leftover live tip not dumped", v341Hop<20 && Math.abs(v341Tip1-v341WuTip)>100, `tip0=${v341Tip0.toFixed(2)} tip1=${v341Tip1.toFixed(2)} hop=${v341Hop.toFixed(2)} wu=${v341WuTip.toFixed(2)}`);
check("tip under throw-startup→tech leftover live tipY holds", Math.abs(v341TipY1-v341TipY0)<20, `y0=${v341TipY0.toFixed(2)} y1=${v341TipY1.toFixed(2)}`);
check("tip under throw-startup→tech leftover live sheet stable windup", /windup/.test((V.poseBitmap(V.player)._src||"")), `src=${V.poseBitmap(V.player)._src}`);
check("tip under throw-startup→tech leftover live AABB planted", Math.abs(V.bodyAABB(V.player).y-v341Aabb0.y)<0.5 && Math.abs(V.bodyAABB(V.player).h-v341Aabb0.h)<0.5, `dy=${(V.bodyAABB(V.player).y-v341Aabb0.y).toFixed(2)}`);

// ease through fade (natural hitstop hold, then drain; tipK blend → windup)
let v341Mid=null, v341End=null, v341SeenPf=false;
const v341TipAtArm = v341Tip1;
for(let i=0;i<40;i++){
  V.update(STEP);
  const tip=V.bladeTipX(V.player);
  const pf=V.parryFade(V.player);
  if(pf>0.02) v341SeenPf=true;
  if(!v341Mid && pf>0.35 && pf<0.75) v341Mid={tip, pf, x:V.player.x};
  if(V.player.parryFadeT===0 && v341SeenPf && !v341End) {
    const r=V.destRect(V.player); const s=V.poseScale(V.player);
    const wu=r.dx + V.poseFamily(V.player).windup.tipX * s;
    v341End={tip, pf, wu, x:V.player.x};
  }
}
check("tip under throw-startup→tech leftover live tip eases mid-fade", v341Mid && Math.abs(v341Mid.tip-v341TipAtArm)>10 && v341Mid.pf<0.95, `mid=${!!v341Mid} tip=${v341Mid?v341Mid.tip.toFixed(2):"na"} tipArm=${v341TipAtArm.toFixed(2)} pf=${v341Mid?v341Mid.pf.toFixed(3):"na"}`);
check("tip under throw-startup→tech leftover live tip settles windup", v341End && Math.abs(v341End.tip-v341End.wu)<12, `end=${v341End?v341End.tip.toFixed(2):"na"} wu=${v341End?v341End.wu.toFixed(2):"na"} t=${V.player.parryFadeT} seen=${v341SeenPf}`);

// idle tech still snaps (no tipK / no pf)
freezeYouAI(); freezeRivalAI();
V.keys.clear(); V.openLeft=0; V.hitstopLeft=0;
V.player.x=400; V.rival.x=520; V.player.facing=1; V.rival.facing=-1;
V.player.phase="idle"; V.player.cut="slash"; V.player.parryFadeT=0; V.player.parryFadeSheet=""; V.player.parryFadeTipK=0;
V.rival.phase="startup"; V.rival.phaseT=40; V.rival.cut="throw"; V.rival.telegraph=false;
V.landThrowTech(V.rival, V.player);
check("tip under throw-startup→tech leftover live idle tech no tipK on def", V.player.techRec && V.player.parryFadeT===0 && V.player.parryFadeTipK===0 && V.parryFade(V.player)===0, `tech=${V.player.techRec} t=${V.player.parryFadeT} tipK=${V.player.parryFadeTipK}`);
check("tip under throw-startup→tech leftover live idle tech atk still latches tipK", V.rival.techRec && V.rival.parryFadeSheet==="windup" && V.rival.parryFadeTipK>0.4, `tech=${V.rival.techRec} sheet=${V.rival.parryFadeSheet} tipK=${V.rival.parryFadeTipK}`);

// tech-from-guard still intentional (def uses techGuardPlantFade, no tipK on def)
freezeYouAI(); freezeRivalAI();
V.keys.clear(); V.openLeft=0; V.hitstopLeft=0;
V.player.x=400; V.rival.x=520; V.player.facing=1; V.rival.facing=-1;
V.player.phase="startup"; V.player.phaseT=40; V.player.cut="throw"; V.player.telegraph=false;
V.rival.phase="idle"; V.rival.guarding=true; V.rival.guardPoseK=1; V.rival.guardRaiseElapsed=V.GUARD_RAISE_MS;
V.landThrowTech(V.player, V.rival);
check("tip under throw-startup→tech leftover live tech-from-guard def no tipK", V.rival.techRec && V.rival.parryFadeTipK===0 && V.rival.techGuardTip===true && V.techGuardPlantFade(V.rival)>0.9, `tech=${V.rival.techRec} tipK=${V.rival.parryFadeTipK} tgt=${V.rival.techGuardTip} tecf=${V.techGuardPlantFade(V.rival)}`);
check("tip under throw-startup→tech leftover live tech-from-guard atk tip held", V.player.techRec && V.player.parryFadeTipK>0.4 && Math.abs(V.bladeTipX(V.player)-(()=>{ const r=V.destRect(V.player); const s=V.poseScale(V.player); const wu=V.poseFamily(V.player).windup; const ex=V.player.facing>0?r.dx+r.dw-8:r.dx+8; const sx=r.dx+wu.tipX*s; return ex+(sx-ex)*(1-V.player.parryFadeTipK); })())<12, `tipK=${V.player.parryFadeTipK} tip=${V.bladeTipX(V.player).toFixed(2)}`);

// defender mid-cut tech still holds (v340 regression)
freezeYouAI(); freezeRivalAI();
V.keys.clear(); V.openLeft=0; V.hitstopLeft=0;
V.player.x=400; V.rival.x=520; V.player.facing=1; V.rival.facing=-1;
V.player.phase="startup"; V.player.phaseT=40; V.player.cut="throw";
V.rival.phase="recovery"; V.rival.phaseT=40; V.rival.cut="slash"; V.rival.cutHit=true;
const v341DefTip0 = V.bladeTipX(V.rival);
V.landThrowTech(V.player, V.rival);
const v341DefTip1 = V.bladeTipX(V.rival);
check("tip under throw-startup→tech leftover live def mid-cut tipK 0", V.rival.parryFadeSheet==="slash" && V.rival.parryFadeTipK===0 && V.rival.parryFadeT===V.SHEATHE_MS, `sheet=${V.rival.parryFadeSheet} tipK=${V.rival.parryFadeTipK} t=${V.rival.parryFadeT}`);
check("tip under throw-startup→tech leftover live def mid-cut tip not dumped", Math.abs(v341DefTip1-v341DefTip0)<40, `tip0=${v341DefTip0.toFixed(2)} tip1=${v341DefTip1.toFixed(2)}`);

check("cache v=341 after tip under throw-startup→tech leftover live", /game.js\?v=350/.test(htmlTxt));

// --- v342 tip under guard raise leftover ---
check("tip under guard raise leftover seats no tip dump", /Tip under guard raise leftover \(v342\)/.test(srcTxt) && /S-press \/ guard raise used to dump bladeTip/.test(srcTxt) && /onto block tipY/.test(srcTxt) && /guardRaiseK sat outside tip ease/.test(srcTxt) && (/poseBitmap still block immediately/.test(srcTxt) || /poseBitmap flipped to block/.test(srcTxt)));
check("tip under guard raise leftover is draw-only", /Draw-only \(bladeTipX \/ bladeTipY\)/.test(srcTxt) && /Tip under guard raise leftover \(v342\)/.test(srcTxt) && /f\.guarding && f\.phase === \"idle\" && !f\.boltPhase/.test(sliceFn(codeOnly, "bladeTipX")) && /f\.guarding && f\.phase === \"idle\" && !f\.boltPhase/.test(sliceFn(codeOnly, "bladeTipY")) && /guardRaiseK\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /guardRaiseK\(f\)/.test(sliceFn(codeOnly, "bladeTipY")) && !/guardRaiseK\(f\)/.test(sliceFn(codeOnly, "poseBitmap")) && !/Tip under guard raise/.test(sliceFn(codeOnly, "bodyAABB")) && !/Tip under guard raise/.test(sliceFn(codeOnly, "tickGuardPose")));
check("tip under guard raise leftover eases idle→block with rk", /const rk = guardRaiseK\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /rk < 0\.98/.test(sliceFn(codeOnly, "bladeTipX")) && /ex \+ \(tip - ex\) \* rk/.test(sliceFn(codeOnly, "bladeTipX")) && /ey \+ \(tip - ey\) \* rk/.test(sliceFn(codeOnly, "bladeTipY")) && /bodyAABB\(f\)/.test(sliceFn(codeOnly, "bladeTipY")));
check("tip under guard raise leftover mirrors telegraphFade rise", /mirror telegraphFade/.test(srcTxt) && /rk rises 0→1/.test(srcTxt) && /telegraphing\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /ex \+ \(tip - ex\) \* tf/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under guard raise leftover does not regress guard drop tip", /Tip under guard drop leftover/.test(srcTxt) && /stunLeftoverPlanting\(f\) \|\| throwKdLeftoverPlanting\(f\) \|\| guardDropFade\(f\) > 0\.02/.test(sliceFn(codeOnly, "bladeTipX")) && /tipPlantK = Math\.max\(pk, guardRaiseK\(f\)\)/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under guard raise leftover does not regress tipPlantK plants", /throwGuardPlanting\(f\) \|\| reversalPlanting\(f\) \|\| techGuardPlanting\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /bx \+ \(tip - bx\) \* \(1 - tipPlantK\)/.test(sliceFn(codeOnly, "bladeTipX")) && /by \+ \(tip - by\) \* \(1 - tipPlantK\)/.test(sliceFn(codeOnly, "bladeTipY")));
check("tip under guard raise leftover does not regress throw-startup→tech v341", /Tip under throw-startup→tech leftover \(v341\)/.test(srcTxt) && /parryFadeTipK/.test(sliceFn(codeOnly, "bladeTipX")) && /parryFadeTipK/.test(sliceFn(codeOnly, "armHitInterruptFade")));
check("tip under guard raise leftover tip under telegraphFade unchanged", /Tip under telegraphFade leftover/.test(srcTxt) && /telegraphFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /ex \+ \(tip - ex\) \* tf/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under guard raise leftover tip under feintFade unchanged", /const fk = feintFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /wx \+ \(tip - wx\) \* \(1 - fk\)/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under guard raise leftover tipX under plant fades unchanged", /Math\.max\(clashPlantFade\(f\), linkPlantFade\(f\)\)/.test(sliceFn(codeOnly, "bladeTipX")) && (/sx \+ \(tip - sx\) \* \(1 - k\)/.test(sliceFn(codeOnly, "bladeTipX")) || /from \+ \(tip - from\) \* \(1 - k\)/.test(sliceFn(codeOnly, "bladeTipX"))));
check("tip under guard raise leftover castPlantXY K path unchanged", /clashPlantFade\(f\)/.test(sliceFn(codeOnly, "castPlantXY")) && /linkPlantFade\(f\)/.test(sliceFn(codeOnly, "castPlantXY")) && !/Tip under guard raise/.test(sliceFn(codeOnly, "castPlantXY")));
check("tip under guard raise leftover tipX / pad / frames locked", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt) && V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140 && V.HITSTOP_BLOCK===60 && (htmlTxt.match(/data-tap=/g)||[]).length===3);
check("tip under guard raise leftover is not a new combat verb", V.HITSTUN===350 && V.HITSTOP_HIT===140 && V.SLASH_DMG===10 && V.THROW_STARTUP===80 && !/KeyM/.test(codeOnly));
check("slash frames still 180/140/280 after tip under guard raise leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after tip under guard raise leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after tip under guard raise leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("riposte frames still 130/140/280 after tip under guard raise leftover", V.RIPOSTE_STARTUP===130 && V.RIPOSTE_ACTIVE===140 && V.RIPOSTE_RECOVERY===280);
check("cache v=345 after tip under guard raise leftover", /game.js\?v=350/.test(htmlTxt));

// live: tip under guard raise — S-press tipY eases with rk, no sheet-flip dump
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
V.player.sheatheT=0; V.player.guarding=false; V.player.guardPoseK=0; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.telegraph=false; V.player.facing=1; V.openLeft=0; V.player.linkPlant=false; V.player.clashPlant=false;
V.player.leftoverPlantTip=false; V.player.thrownT=0; V.player.hp=100; V.player.guardBreakT=0; V.hitstopLeft=0;
freezeRivalAI();
const grIdleY = V.bladeTipY(V.player);
const grIdleX = V.bladeTipX(V.player);
const grIdleAabb = V.bodyAABB(V.player);
V.keys.add("KeyS");
let grMid=null, grEnd=null, grTrackOk=true, grPrevY=grIdleY, grMaxHop=0, grSawBlock=false, grFirstHop=0;
for (let i=0;i<30;i++){
  V.update(STEP);
  const tipY=V.bladeTipY(V.player);
  const tipX=V.bladeTipX(V.player);
  const rk=V.guardRaiseK(V.player);
  const hop=Math.abs(tipY-grPrevY);
  if (i===0) grFirstHop=hop;
  if (hop>grMaxHop) grMaxHop=hop;
  if (Math.abs(tipY-grPrevY)>35) grTrackOk=false;
  const src=(V.poseBitmap(V.player)._src||"");
  if (/block/.test(src)) grSawBlock=true;
  if (grMid==null && rk>0.15 && rk<0.85 && Math.abs(tipY-grIdleY)>6) grMid={tipY,tipX,rk,i};
  if (grEnd==null && rk>=0.98 && V.player.guardPoseK>=1) grEnd={tipY,tipX,rk};
  grPrevY=tipY;
  if (grEnd && i>8) break;
}
V.keys.clear();
check("tip under guard raise leftover live arms block sheet", grSawBlock && V.player.guarding, `block=${grSawBlock} g=${V.player.guarding} end=${!!grEnd}`);
check("tip under guard raise leftover live tipY eases mid-raise", grMid && Math.abs(grMid.tipY-grIdleY)>6 && grEnd && Math.abs(grMid.tipY-grEnd.tipY)>6, `mid=${!!grMid} tipY=${grMid?grMid.tipY.toFixed(2):"na"} idle=${grIdleY.toFixed(2)} end=${grEnd?grEnd.tipY.toFixed(2):"na"} rk=${grMid?grMid.rk.toFixed(3):"na"}`);
check("tip under guard raise leftover live no sheet-flip tipY dump", grTrackOk && grMaxHop<30 && grFirstHop<25, `track=${grTrackOk} maxHop=${grMaxHop.toFixed(2)} first=${grFirstHop.toFixed(2)} idle=${grIdleY.toFixed(2)}`);
check("tip under guard raise leftover live settles block tipY", grEnd && Math.abs(grEnd.tipY-353.58)<8, `end=${grEnd?grEnd.tipY.toFixed(2):"na"}`);
check("tip under guard raise leftover live tipX sheet-edge stable", grEnd && Math.abs(grEnd.tipX-grIdleX)<40, `idleX=${grIdleX.toFixed(2)} endX=${grEnd?grEnd.tipX.toFixed(2):"na"}`);
check("tip under guard raise leftover live AABB planted", Math.abs(V.bodyAABB(V.player).y-grIdleAabb.y)<8, `dy=${(V.bodyAABB(V.player).y-grIdleAabb.y).toFixed(2)}`);

// live: mid-raise snap seats between idle and block (not dumped)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.guarding=true; V.player.guardPoseK=0.45; V.player.stunT=0;
V.player.facing=1; V.player.hp=100; V.hitstopLeft=0; V.openLeft=0; V.player.boltPhase=""; V.player.feintT=0;
freezeRivalAI();
const grSnapRk = V.guardRaiseK(V.player);
const grSnapY = V.bladeTipY(V.player);
V.player.guardPoseK=1;
const grFullY = V.bladeTipY(V.player);
V.player.guarding=false; V.player.guardPoseK=0;
const grDumpY = V.bladeTipY(V.player);
V.player.guarding=true; V.player.guardPoseK=0.45;
check("tip under guard raise leftover live mid-raise tipY not full dump", grSnapRk>0.2 && grSnapRk<0.8 && Math.abs(grSnapY-grFullY)>6 && Math.abs(grSnapY-grDumpY)>6, `rk=${grSnapRk.toFixed(3)} mid=${grSnapY.toFixed(2)} full=${grFullY.toFixed(2)} idle=${grDumpY.toFixed(2)}`);

// live: full raise tip matches block (rk=1 no-op)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.guarding=true; V.player.guardPoseK=1; V.player.stunT=0;
V.player.facing=1; V.player.hp=100; V.hitstopLeft=0; V.openLeft=0;
freezeRivalAI();
const grFullRk = V.guardRaiseK(V.player);
const grFullTip = V.bladeTipY(V.player);
check("tip under guard raise leftover live full raise at block tip", grFullRk>=0.98 && Math.abs(grFullTip-353.58)<8, `rk=${grFullRk.toFixed(3)} tip=${grFullTip.toFixed(2)}`);

// live: drop path still eases (regression)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.guarding=false; V.player.guardPoseK=1; V.player.stunT=0;
V.player.facing=1; V.player.hp=100; V.hitstopLeft=0; V.openLeft=0;
freezeRivalAI();
const grDropPlant = V.bladeTipY(V.player);
const grDropGdf = V.guardDropFade(V.player);
V.player.guardPoseK=0;
const grDropDump = V.bladeTipY(V.player);
V.player.guardPoseK=1;
check("tip under guard raise leftover live drop still plants", grDropGdf>0.5 && Math.abs(grDropPlant-grDropDump)>20, `gdf=${grDropGdf.toFixed(3)} plant=${grDropPlant.toFixed(2)} dump=${grDropDump.toFixed(2)}`);

// live: tele tip still eases (regression)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
V.player.sheatheT=0; V.player.guarding=false; V.player.guardPoseK=0; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.telegraph=false; V.player.facing=1; V.openLeft=0; V.player.linkPlant=false; V.player.clashPlant=false;
freezeRivalAI(); V.update(STEP);
const grTele0 = V.bladeTipX(V.player);
const grTeleR = V.destRect(V.player);
const grTeleWu = grTeleR.dx + 359 * (grTeleR.dw/1186);
V.startAttack(V.player, "slash");
holdRival(); V.update(STEP);
const grTeleArm = V.bladeTipX(V.player);
const grTeleTf = V.telegraphFade(V.player);
check("tip under guard raise leftover live tele tip still eases", V.telegraphing(V.player) && V.player.phase==="startup" && Math.abs(grTeleArm-grTele0)<40 && Math.abs(grTeleArm-grTeleWu)>20 && grTeleTf>0 && grTeleTf<0.3, `tip0=${grTele0.toFixed(2)} tipArm=${grTeleArm.toFixed(2)} wu=${grTeleWu.toFixed(2)} tf=${grTeleTf.toFixed(3)}`);

check("cache v=345 after tip under guard raise leftover live", /game.js\?v=350/.test(htmlTxt));

// --- tip under tipPlantK+tele leftover stack (v343) ---
check("tip under tipPlantK+tele leftover stack seats no tip dump", /Tip under tipPlantK\+tele leftover stack \(v343\)/.test(srcTxt) && /Space\/L\/K from short leftover drop/.test(srcTxt) && /with telegraph on used to dump/.test(srcTxt) && /bladeTip onto idle-edge/.test(srcTxt) && /leftoverPlantTip latched but tele tip/.test(srcTxt) && /still eased idle-edge→pose/.test(srcTxt));
check("tip under tipPlantK+tele leftover stack is draw-only", /Draw-only \(bladeTipX \/ bladeTipY\)/.test(srcTxt) && /Latch leftoverPlantTipK = guardRaiseK/.test(srcTxt) && /leftoverPlantTipK/.test(sliceFn(codeOnly, "bladeTipX")) && /leftoverPlantTipK/.test(sliceFn(codeOnly, "bladeTipY")) && /bias idle-edge\/chest toward block by tipK/.test(srcTxt) && !/leftoverPlantTipK/.test(sliceFn(codeOnly, "destRect")) && !/leftoverPlantTipK/.test(sliceFn(codeOnly, "bodyAABB")) && !/leftoverPlantTipK/.test(sliceFn(codeOnly, "poseBitmap")) && !/leftoverPlantTipK/.test(sliceFn(codeOnly, "landHit")));
check("tip under tipPlantK+tele leftover stack latches tipK on startAttack", /leftoverPlantTipK = f\.leftoverPlantTip \? guardRaiseK\(f\) : 0/.test(sliceFn(codeOnly, "startAttack")) && /leftoverPlantTip = \(f\.guardPoseK \|\| 0\) > 0/.test(sliceFn(codeOnly, "startAttack")));
check("tip under tipPlantK+tele leftover stack latches tipK on startBolt", /leftoverPlantTipK = u\.leftoverPlantTip \? guardRaiseK\(u\) : 0/.test(sliceFn(codeOnly, "startBolt")) && /leftoverPlantTip = \(u\.guardPoseK \|\| 0\) > 0/.test(sliceFn(codeOnly, "startBolt")));
check("tip under tipPlantK+tele leftover stack biases tele tipX base", /leftoverPlantTipK/.test(sliceFn(codeOnly, "bladeTipX")) && /ex = ex \+ \(bx - ex\) \* tipK/.test(sliceFn(codeOnly, "bladeTipX")) && /telegraphing\(f\)/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under tipPlantK+tele leftover stack biases tele tipY base", /leftoverPlantTipK/.test(sliceFn(codeOnly, "bladeTipY")) && /ey = ey \+ \(by - ey\) \* tipK/.test(sliceFn(codeOnly, "bladeTipY")) && /telegraphing\(f\)/.test(sliceFn(codeOnly, "bladeTipY")));
check("tip under tipPlantK+tele leftover stack tele-off tipPlantK unchanged", /leftoverPlantTip && !telegraphing\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /leftoverPlantTip && !telegraphing\(f\)/.test(sliceFn(codeOnly, "bladeTipY")) && /1 - raiseT/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under tipPlantK+tele leftover stack does not regress guard raise tip", /Tip under guard raise leftover \(v342\)/.test(srcTxt) && /f\.guarding && f\.phase === "idle"/.test(sliceFn(codeOnly, "bladeTipX")) && /guardRaiseK\(f\)/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under tipPlantK+tele leftover stack does not regress throw-startup→tech v341", /Tip under throw-startup→tech leftover \(v341\)/.test(srcTxt) && /parryFadeTipK/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under tipPlantK+tele leftover stack clash\/holdCut\/link tip unchanged", /Math\.max\(clashPlantFade\(f\), linkPlantFade\(f\)\)/.test(sliceFn(codeOnly, "bladeTipX")) && /HoldCut still slash tip/.test(srcTxt) && /holdCutFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under tipPlantK+tele leftover stack tipX \/ pad \/ frames locked", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt) && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280);
check("tip under tipPlantK+tele leftover stack is not a new combat verb", /No new combat verb/.test(srcTxt) && /active hitbox unchanged/.test(srcTxt));
check("slash frames still 180\/140\/280 after tip under tipPlantK+tele leftover stack", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280);
check("golpe frames still 120\/80\/180 after tip under tipPlantK+tele leftover stack", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180);
check("K frames still 200\/280 after tip under tipPlantK+tele leftover stack", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280);
check("riposte frames still 130\/140\/280 after tip under tipPlantK+tele leftover stack", V.RIPOSTE_STARTUP===130 && V.RIPOSTE_ACTIVE===140 && V.RIPOSTE_RECOVERY===280);
check("cache v=345 after tip under tipPlantK+tele leftover stack", /game.js\?v=350/.test(htmlTxt));

// LIVE: leftover gpk + Space tele — tipY not dumped to idle on arm
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
V.player.sheatheT=0; V.player.guarding=false; V.player.guardPoseK=0.5; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.telegraph=false; V.player.facing=1; V.openLeft=0; V.player.linkPlant=false; V.player.clashPlant=false;
V.player.leftoverPlantTip=false; V.player.leftoverPlantTipK=0;
freezeRivalAI(); V.update(STEP);
const tpTelePre = { x: V.bladeTipX(V.player), y: V.bladeTipY(V.player) };
const tpTeleIdleY = 394.4;
const tpTeleGpk = V.player.guardPoseK;
V.startAttack(V.player, "slash");
holdRival();
const tpTelePost = { x: V.bladeTipX(V.player), y: V.bladeTipY(V.player) };
const tpTeleHop = Math.hypot(tpTelePost.x-tpTelePre.x, tpTelePost.y-tpTelePre.y);
const tpTeleLatch = !!V.player.leftoverPlantTip;
const tpTeleTipK = V.player.leftoverPlantTipK || 0;
const tpTeleTf = V.telegraphFade(V.player);
check("tip under tipPlantK+tele leftover stack live arms tele+latch", V.telegraphing(V.player) && V.player.phase==="startup" && tpTeleLatch && tpTeleTipK>0.2 && tpTeleTf===0, `tele=${V.telegraphing(V.player)} ph=${V.player.phase} latch=${tpTeleLatch} tipK=${tpTeleTipK.toFixed(3)} tf=${tpTeleTf}`);
check("tip under tipPlantK+tele leftover stack live tip not dumped to idle", tpTeleHop<8 && Math.abs(tpTelePost.y-tpTeleIdleY)>8 && Math.abs(tpTelePost.y-tpTelePre.y)<8, `hop=${tpTeleHop.toFixed(2)} preY=${tpTelePre.y.toFixed(2)} postY=${tpTelePost.y.toFixed(2)} idleY=${tpTeleIdleY} gpk=${tpTeleGpk}`);
check("tip under tipPlantK+tele leftover stack live tipY holds leftover blend", Math.abs(tpTelePost.y-tpTelePre.y)<3, `preY=${tpTelePre.y.toFixed(2)} postY=${tpTelePost.y.toFixed(2)}`);

// LIVE: trail eases leftover→windup without idle dump
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
V.player.sheatheT=0; V.player.guarding=false; V.player.guardPoseK=0.5; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.telegraph=false; V.player.facing=1; V.openLeft=0; V.player.linkPlant=false; V.player.clashPlant=false;
freezeRivalAI(); V.update(STEP);
const tpTrailPreY = V.bladeTipY(V.player);
const tpTrailAabb0 = V.bodyAABB(V.player);
V.startAttack(V.player, "slash");
holdRival(); freezeRivalAI();
let tpTrailSawIdleDump=false, tpTrailMaxDy=0, tpTrailPrevY=V.bladeTipY(V.player), tpTrailEnd=null, tpTrailMid=null;
for (let i=0;i<12;i++){
  holdRival(); freezeRivalAI();
  V.rival.x=900; V.rival.closing=false; V.rival.phase="idle"; V.rival.boltPhase="";
  V.update(STEP);
  const ty=V.bladeTipY(V.player);
  const dy=Math.abs(ty-tpTrailPrevY);
  if(dy>tpTrailMaxDy) tpTrailMaxDy=dy;
  const tf=V.telegraphFade(V.player);
  if(tf>0.05 && tf<0.35 && Math.abs(ty-tpTeleIdleY)<4) tpTrailSawIdleDump=true;
  if(tf>0.3 && tf<0.7 && !tpTrailMid) tpTrailMid={y:ty,tf};
  if(tf>=0.98 && !tpTrailEnd) tpTrailEnd={y:ty,tf};
  tpTrailPrevY=ty;
}
check("tip under tipPlantK+tele leftover stack live no mid-tele idle dump", !tpTrailSawIdleDump && tpTrailMaxDy<80, `dump=${tpTrailSawIdleDump} maxDy=${tpTrailMaxDy.toFixed(2)} preY=${tpTrailPreY.toFixed(2)}`);
check("tip under tipPlantK+tele leftover stack live settles windup tipY", tpTrailEnd && Math.abs(tpTrailEnd.y-349.58)<8, `end=${tpTrailEnd?tpTrailEnd.y.toFixed(2):"na"}`);
check("tip under tipPlantK+tele leftover stack live AABB planted", Math.abs(V.bodyAABB(V.player).y-tpTrailAabb0.y)<8 && Math.abs(V.bodyAABB(V.player).h-tpTrailAabb0.h)<8, `dy=${(V.bodyAABB(V.player).y-tpTrailAabb0.y).toFixed(2)}`);

// LIVE: idle tele (no leftover) still eases from idle — regression
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
V.player.sheatheT=0; V.player.guarding=false; V.player.guardPoseK=0; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.telegraph=false; V.player.facing=1; V.openLeft=0; V.player.linkPlant=false; V.player.clashPlant=false;
V.player.leftoverPlantTip=false; V.player.leftoverPlantTipK=0;
freezeRivalAI(); V.update(STEP);
const tpIdle0 = V.bladeTipX(V.player);
const tpIdleY0 = V.bladeTipY(V.player);
V.startAttack(V.player, "slash");
holdRival(); V.update(STEP);
const tpIdleArm = V.bladeTipX(V.player);
const tpIdleArmY = V.bladeTipY(V.player);
const tpIdleTf = V.telegraphFade(V.player);
const tpIdleR = V.destRect(V.player);
const tpIdleWu = tpIdleR.dx + 359 * (tpIdleR.dw/1186);
check("tip under tipPlantK+tele leftover stack live idle tele still eases", V.telegraphing(V.player) && !V.player.leftoverPlantTip && Math.abs(tpIdleArm-tpIdle0)<40 && Math.abs(tpIdleArm-tpIdleWu)>20 && tpIdleTf>0 && tpIdleTf<0.3 && Math.abs(tpIdleArmY-tpIdleY0)<30, `tip0=${tpIdle0.toFixed(2)} tipArm=${tpIdleArm.toFixed(2)} wu=${tpIdleWu.toFixed(2)} tf=${tpIdleTf.toFixed(3)} latch=${!!V.player.leftoverPlantTip}`);

// LIVE: tele-off tipPlantK path still holds leftover tip (force tele off after latch)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
V.player.sheatheT=0; V.player.guarding=false; V.player.guardPoseK=0.5; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.telegraph=false; V.player.facing=1; V.openLeft=0; V.player.linkPlant=false; V.player.clashPlant=false;
freezeRivalAI(); V.update(STEP);
const tpOffPre = {x:V.bladeTipX(V.player), y:V.bladeTipY(V.player)};
V.startAttack(V.player, "slash");
holdRival(); freezeRivalAI();
// Force tele-off path (sheathe plant / cancel): tipPlantK owns ease, not tele idle-edge
V.player.telegraph = false;
const tpOffPost = {x:V.bladeTipX(V.player), y:V.bladeTipY(V.player)};
const tpOffHop = Math.hypot(tpOffPost.x-tpOffPre.x, tpOffPost.y-tpOffPre.y);
const tpOffIdleY = 394.4;
check("tip under tipPlantK+tele leftover stack live tele-off tipPlantK still plants", V.player.phase==="startup" && V.player.leftoverPlantTip && !V.telegraphing(V.player) && Math.abs(tpOffPost.y-tpOffIdleY)>20 && Math.abs(tpOffPost.y-353.58)<20, `ph=${V.player.phase} latch=${V.player.leftoverPlantTip} tele=${V.telegraphing(V.player)} hop=${tpOffHop.toFixed(2)} preY=${tpOffPre.y.toFixed(2)} postY=${tpOffPost.y.toFixed(2)}`);

// LIVE: guard raise tip still eases (v342 regression)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.guarding=true; V.player.guardPoseK=0.45; V.player.stunT=0;
V.player.facing=1; V.player.hp=100; V.hitstopLeft=0; V.openLeft=0; V.player.boltPhase=""; V.player.feintT=0;
freezeRivalAI();
const tpGrY = V.bladeTipY(V.player);
const tpGrRk = V.guardRaiseK(V.player);
V.player.guardPoseK=1;
const tpGrFull = V.bladeTipY(V.player);
check("tip under tipPlantK+tele leftover stack live guard raise still eases", tpGrRk>0.2 && tpGrRk<0.8 && Math.abs(tpGrY-tpGrFull)>6 && Math.abs(tpGrY-394.4)>6, `rk=${tpGrRk.toFixed(3)} mid=${tpGrY.toFixed(2)} full=${tpGrFull.toFixed(2)}`);

check("cache v=345 after tip under tipPlantK+tele leftover stack live", /game.js\?v=350/.test(htmlTxt));




// --- tip under mid-tele feint leftover (v344) ---
check("tip under mid-tele feint leftover seats no tip dump", /Tip under mid-tele feint leftover \(v344\)/.test(srcTxt) && /Space feint mid-telegraph used to dump/.test(srcTxt) && /bladeTip onto full windup/.test(srcTxt) && (/~122px/.test(srcTxt) || /tf≈0\.5/.test(srcTxt)) && /startFeint cleared telegraph while feintFade/.test(srcTxt));
check("tip under mid-tele feint leftover is draw-only", /Draw-only \(bladeTipX \/ bladeTipY\)/.test(srcTxt) && /Latch feintTipK = telegraphFade at startFeint/.test(srcTxt) && /feintTipK/.test(sliceFn(codeOnly, "bladeTipX")) && /feintTipK/.test(sliceFn(codeOnly, "bladeTipY")) && /feintTipLatch/.test(sliceFn(codeOnly, "startFeint")) && !/feintTipK/.test(sliceFn(codeOnly, "destRect")) && !/feintTipK/.test(sliceFn(codeOnly, "bodyAABB")) && !/feintTipK/.test(sliceFn(codeOnly, "poseBitmap")) && !/feintTipK/.test(sliceFn(codeOnly, "landHit")));
check("tip under mid-tele feint leftover latches tipK before phase clear", /feintTipLatch = telegraphing\(f\) \? telegraphFade\(f\) : 1/.test(sliceFn(codeOnly, "startFeint")) && /f\.feintTipK = feintTipLatch/.test(sliceFn(codeOnly, "startFeint")) && /BEFORE phase\/tele/.test(srcTxt));
check("tip under mid-tele feint leftover biases windup tipX", /feintTipK/.test(sliceFn(codeOnly, "bladeTipX")) && /wx = ex \+ \(wx - ex\) \* tipK/.test(sliceFn(codeOnly, "bladeTipX")) && /const fk = feintFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under mid-tele feint leftover biases windup tipY", /feintTipK/.test(sliceFn(codeOnly, "bladeTipY")) && /wy = ey \+ \(wy - ey\) \* tipK/.test(sliceFn(codeOnly, "bladeTipY")) && /const fk = feintFade\(f\)/.test(sliceFn(codeOnly, "bladeTipY")));
check("tip under mid-tele feint leftover late tele tipK=1 no-op", /tipK < 0\.98/.test(sliceFn(codeOnly, "bladeTipX")) && /Late tele tipK=1 is a no-op/.test(srcTxt));
check("tip under mid-tele feint leftover does not regress tip under feintFade", /Tip under feintFade leftover/.test(srcTxt) && /wx \+ \(tip - wx\) \* \(1 - fk\)/.test(sliceFn(codeOnly, "bladeTipX")) && /wy \+ \(tip - wy\) \* \(1 - fk\)/.test(sliceFn(codeOnly, "bladeTipY")));
check("tip under mid-tele feint leftover does not regress tipPlantK\+tele v343", /Tip under tipPlantK\+tele leftover stack \(v343\)/.test(srcTxt) && /leftoverPlantTipK/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under mid-tele feint leftover tipX \/ pad \/ frames locked", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt) && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.FEINT_RECOVERY===100);
check("tip under mid-tele feint leftover is not a new combat verb", /No new combat verb/.test(srcTxt) && /active hitbox unchanged/.test(srcTxt));
check("slash frames still 180\/140\/280 after tip under mid-tele feint leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280);
check("golpe frames still 120\/80\/180 after tip under mid-tele feint leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180);
check("K frames still 200\/280 after tip under mid-tele feint leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280);
check("riposte frames still 130\/140\/280 after tip under mid-tele feint leftover", V.RIPOSTE_STARTUP===130 && V.RIPOSTE_ACTIVE===140 && V.RIPOSTE_RECOVERY===280);
check("cache v=345 after tip under mid-tele feint leftover", /game.js\?v=350/.test(htmlTxt));

// LIVE: mid-tele feint — tip holds mid blend on arm (no windup dump)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
V.player.sheatheT=0; V.player.guarding=false; V.player.guardPoseK=0; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.telegraph=false; V.player.facing=1; V.openLeft=0; V.player.linkPlant=false; V.player.clashPlant=false;
V.player.feintTipK=1;
freezeRivalAI(); V.update(STEP);
V.startAttack(V.player, "slash");
// Seat mid-tele (~tf 0.5–0.8)
for (let i=0;i<8;i++){
  freezeRivalAI(); V.update(STEP);
  if (V.canFeint(V.player) && V.telegraphFade(V.player)>0.45 && V.telegraphFade(V.player)<0.85) break;
}
const mtPre = {x:V.bladeTipX(V.player), y:V.bladeTipY(V.player)};
const mtTf = V.telegraphFade(V.player);
const mtR = V.destRect(V.player);
const mtWu = mtR.dx + 359 * (mtR.dw/1186);
const mtEdge = V.player.facing>0 ? mtR.dx+mtR.dw-8 : mtR.dx+8;
const mtCan = V.canFeint(V.player);
check("tip under mid-tele feint leftover live mid-tele ready", mtCan && V.player.phase==="startup" && mtTf>0.45 && mtTf<0.85 && Math.abs(mtPre.x-mtWu)>40 && Math.abs(mtPre.x-mtEdge)>40, `can=${mtCan} ph=${V.player.phase} tf=${mtTf.toFixed(3)} tip=${mtPre.x.toFixed(2)} wu=${mtWu.toFixed(2)} edge=${mtEdge.toFixed(2)}`);
const mtDid = V.startFeint(V.player);
const mtPost = {x:V.bladeTipX(V.player), y:V.bladeTipY(V.player)};
const mtHop = Math.hypot(mtPost.x-mtPre.x, mtPost.y-mtPre.y);
const mtFk = V.feintFade(V.player);
const mtTipK = V.player.feintTipK;
check("tip under mid-tele feint leftover live arms latch", mtDid===true && V.player.feintT>0 && mtFk>0.9 && mtTipK>0.45 && mtTipK<0.85, `did=${mtDid} ft=${V.player.feintT} fk=${mtFk.toFixed(3)} tipK=${mtTipK.toFixed(3)}`);
check("tip under mid-tele feint leftover live no windup dump on arm", mtHop<8 && Math.abs(mtPost.x-mtWu)>40, `hop=${mtHop.toFixed(2)} pre=${mtPre.x.toFixed(2)} post=${mtPost.x.toFixed(2)} wu=${mtWu.toFixed(2)} tipK=${mtTipK.toFixed(3)}`);

// LIVE: trail eases mid→idle without windup snap
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
V.player.sheatheT=0; V.player.guarding=false; V.player.guardPoseK=0; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.telegraph=false; V.player.facing=1; V.openLeft=0;
freezeRivalAI(); V.update(STEP);
V.startAttack(V.player, "slash");
for (let i=0;i<8;i++){
  freezeRivalAI(); V.update(STEP);
  if (V.canFeint(V.player) && V.telegraphFade(V.player)>0.45 && V.telegraphFade(V.player)<0.85) break;
}
const mtTrailPre = V.bladeTipX(V.player);
const mtTrailAabb0 = V.bodyAABB(V.player);
V.startFeint(V.player);
let mtTrailMaxHop=0, mtTrailPrev=V.bladeTipX(V.player), mtTrailSawWu=false, mtTrailEnd=null;
const mtTrailWu = V.destRect(V.player).dx + 359 * (V.destRect(V.player).dw/1186);
const mtTrailEdge = V.player.facing>0 ? V.destRect(V.player).dx+V.destRect(V.player).dw-8 : V.destRect(V.player).dx+8;
for (let i=0;i<12;i++){
  freezeRivalAI(); V.update(STEP);
  const tip=V.bladeTipX(V.player); const fk=V.feintFade(V.player);
  const h=Math.abs(tip-mtTrailPrev); if(h>mtTrailMaxHop) mtTrailMaxHop=h;
  if(fk>0.85 && Math.abs(tip-mtTrailWu)<12) mtTrailSawWu=true;
  if(V.player.feintT<=0 || fk<=0.02) mtTrailEnd={tip,fk};
  mtTrailPrev=tip;
  if(V.player.feintT<=0) break;
}
check("tip under mid-tele feint leftover live trail no windup snap", !mtTrailSawWu && mtTrailMaxHop<90, `sawWu=${mtTrailSawWu} maxHop=${mtTrailMaxHop.toFixed(2)} pre=${mtTrailPre.toFixed(2)}`);
check("tip under mid-tele feint leftover live settles idle tip", mtTrailEnd && Math.abs(mtTrailEnd.tip-mtTrailEdge)<8, `end=${mtTrailEnd?mtTrailEnd.tip.toFixed(2):"na"} edge=${mtTrailEdge.toFixed(2)}`);
check("tip under mid-tele feint leftover live AABB planted", Math.abs(V.bodyAABB(V.player).y-mtTrailAabb0.y)<8 && Math.abs(V.bodyAABB(V.player).h-mtTrailAabb0.h)<8, `dy=${(V.bodyAABB(V.player).y-mtTrailAabb0.y).toFixed(2)}`);

// LIVE: late-tele feint still seats near windup (tip under feintFade regression)
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
V.player.sheatheT=0; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.telegraph=false; V.player.facing=1; V.openLeft=0;
freezeRivalAI(); V.update(STEP);
V.startAttack(V.player, "slash");
for (let i=0;i<20;i++){
  freezeRivalAI(); V.update(STEP);
  if (V.canFeint(V.player) && V.telegraphFade(V.player) >= 0.98) break;
}
const ltR = V.destRect(V.player);
const ltWu = ltR.dx + 359 * (ltR.dw/1186);
const ltEdge = V.player.facing>0 ? ltR.dx+ltR.dw-8 : ltR.dx+8;
const ltTip0 = V.bladeTipX(V.player);
const ltDid = V.startFeint(V.player);
const ltArm = V.bladeTipX(V.player);
const ltTipK = V.player.feintTipK;
check("tip under mid-tele feint leftover live late tele still near windup", ltDid===true && ltTipK>=0.98 && Math.abs(ltArm-ltWu)<12 && Math.abs(ltArm-ltEdge)>150 && Math.abs(ltArm-ltTip0)<8, `did=${ltDid} tipK=${ltTipK.toFixed(3)} tip0=${ltTip0.toFixed(2)} arm=${ltArm.toFixed(2)} wu=${ltWu.toFixed(2)}`);

// LIVE: early-tele feint stays near idle-edge (no windup flash)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkFadeHold=0;
V.player.sheatheT=0; V.player.guarding=false; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.telegraph=false; V.player.facing=1; V.openLeft=0;
freezeRivalAI(); V.update(STEP);
V.startAttack(V.player, "slash");
const etPre = V.bladeTipX(V.player);
const etTf = V.telegraphFade(V.player);
const etR = V.destRect(V.player);
const etWu = etR.dx + 359 * (etR.dw/1186);
const etEdge = V.player.facing>0 ? etR.dx+etR.dw-8 : etR.dx+8;
const etDid = V.startFeint(V.player);
const etPost = V.bladeTipX(V.player);
const etHop = Math.abs(etPost-etPre);
const etTipK = V.player.feintTipK;
check("tip under mid-tele feint leftover live early tele no windup flash", etDid===true && etTipK<0.15 && etHop<8 && Math.abs(etPost-etWu)>150 && Math.abs(etPost-etEdge)<12, `did=${etDid} tipK=${etTipK.toFixed(3)} hop=${etHop.toFixed(2)} pre=${etPre.toFixed(2)} post=${etPost.toFixed(2)} wu=${etWu.toFixed(2)} tf0=${etTf.toFixed(3)}`);

check("cache v=345 after tip under mid-tele feint leftover live", /game.js\?v=350/.test(htmlTxt));


// --- tip under sheathe↔guardDrop tip gate (v345) ---
check("tip under sheathe↔guardDrop tip gate seats no tip dump", /Tip under sheathe↔guardDrop tip gate \(v345\)/.test(srcTxt) && /sheatheFade and guardDrop tip used to/.test(srcTxt) && (/~70px/.test(srcTxt) || /idle-edge/.test(srcTxt)) && /sheathing\(\) still held slash/.test(srcTxt));
check("tip under sheathe↔guardDrop tip gate is draw-only", /Draw-only \(bladeTipX \/ bladeTipY\)/.test(srcTxt) && /Tip under sheatheFade leftover \(v345\)/.test(srcTxt) && /sheatheFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /sheatheFade\(f\)/.test(sliceFn(codeOnly, "bladeTipY")) && /guardDropFade\(f\) > 0\.02 && f\.sheatheT > 0/.test(sliceFn(codeOnly, "bladeTipX")) && /guardDropFade\(f\) > 0\.02 && f\.sheatheT > 0/.test(sliceFn(codeOnly, "bladeTipY")) && !/Tip under sheathe↔guardDrop/.test(sliceFn(codeOnly, "destRect")) && !/Tip under sheathe↔guardDrop/.test(sliceFn(codeOnly, "bodyAABB")) && !/Tip under sheathe↔guardDrop/.test(sliceFn(codeOnly, "poseBitmap")) && !/Tip under sheathe↔guardDrop/.test(sliceFn(codeOnly, "landHit")));
check("tip under sheathe↔guardDrop tip gate eases slash→pose with sf", /const sfTip = sheatheFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /sx \+ \(poseTip - sx\) \* \(1 - sfTip\)/.test(sliceFn(codeOnly, "bladeTipX")) && /const sfTipY = sheatheFade\(f\)/.test(sliceFn(codeOnly, "bladeTipY")) && /sy \+ \(poseTip - sy\) \* \(1 - sfTipY\)/.test(sliceFn(codeOnly, "bladeTipY")));
check("tip under sheathe↔guardDrop tip gate seats idle when sheathing", /sheathing\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /sheathing\(f\)/.test(sliceFn(codeOnly, "bladeTipY")) && /sfTip > 0/.test(sliceFn(codeOnly, "bladeTipX")) && /sfTipY > 0/.test(sliceFn(codeOnly, "bladeTipY")) && /sheathing\(f\) \|\| \(f\.phase === "recovery"/.test(sliceFn(codeOnly, "bladeTipX")) && /restSheathe/.test(srcTxt));
check("tip under sheathe↔guardDrop tip gate drop blends slash-biased block→idle", /slash-biased block/.test(srcTxt) && /mid \+ \(ex - mid\) \* \(1 - gdf\)/.test(sliceFn(codeOnly, "bladeTipX")) && /mid \+ \(ey - mid\) \* \(1 - gdf\)/.test(sliceFn(codeOnly, "bladeTipY")) && /f\.sheatheT \/ SHEATHE_MS/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under sheathe↔guardDrop tip gate keeps tiny sf", /sfTip > 0/.test(sliceFn(codeOnly, "bladeTipX")) && /sfTipY > 0/.test(sliceFn(codeOnly, "bladeTipY")) && /not 0\.02/.test(srcTxt));
check("tip under sheathe↔guardDrop tip gate does not regress guard raise v342", /Tip under guard raise leftover \(v342\)/.test(srcTxt) && /f\.guarding && f\.phase === \"idle\" && !f\.boltPhase/.test(sliceFn(codeOnly, "bladeTipX")) && /ex \+ \(tip - ex\) \* rk/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under sheathe↔guardDrop tip gate does not regress guard drop tip", /Tip under guard drop leftover/.test(srcTxt) && /stunLeftoverPlanting\(f\) \|\| throwKdLeftoverPlanting\(f\) \|\| guardDropFade\(f\) > 0\.02/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under sheathe↔guardDrop tip gate does not regress tipPlantK\+tele v343", /Tip under tipPlantK\+tele leftover stack \(v343\)/.test(srcTxt) && /leftoverPlantTipK/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under sheathe↔guardDrop tip gate does not regress mid-tele feint v344", /Tip under mid-tele feint leftover \(v344\)/.test(srcTxt) && /feintTipK/.test(sliceFn(codeOnly, "bladeTipX")) && /feintTipLatch/.test(sliceFn(codeOnly, "startFeint")));
check("tip under sheathe↔guardDrop tip gate tipX \/ pad \/ frames locked", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt) && V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140 && V.FEINT_RECOVERY===100 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280);
check("tip under sheathe↔guardDrop tip gate is not a new combat verb", /No new combat verb/.test(srcTxt) && /active hitbox unchanged/.test(srcTxt) && !/KeyM/.test(codeOnly));
check("slash frames still 180\/140\/280 after tip under sheathe↔guardDrop tip gate", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280);
check("golpe frames still 120\/80\/180 after tip under sheathe↔guardDrop tip gate", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180);
check("K frames still 200\/280 after tip under sheathe↔guardDrop tip gate", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280);
check("riposte frames still 130\/140\/280 after tip under sheathe↔guardDrop tip gate", V.RIPOSTE_STARTUP===130 && V.RIPOSTE_ACTIVE===140 && V.RIPOSTE_RECOVERY===280);
check("cache v=345 after tip under sheathe↔guardDrop tip gate", /game.js\?v=350/.test(htmlTxt));

// LIVE: pure idle sheathe tip eases slash→idle (no end dump)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0; V.player.walkFadeHold=0; V.player.walkSettleT=0;
V.player.sheatheT=V.SHEATHE_MS; V.player.cut="slash"; V.player.cutHit=true;
V.player.guarding=false; V.player.guardPoseK=0; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.facing=1; V.openLeft=0; V.hitstopLeft=0;
freezeRivalAI();
const shPre = {x:V.bladeTipX(V.player), y:V.bladeTipY(V.player)};
const shR0 = V.destRect(V.player);
const shSlashX = shR0.dx + 991 * (shR0.dw/1186);
const shEdgeX = V.player.facing>0 ? shR0.dx+shR0.dw-8 : shR0.dx+8;
check("tip under sheathe↔guardDrop tip gate live sheathe starts on slash", Math.abs(shPre.x-shSlashX)<8 && V.sheatheFade(V.player)>0.9, `tip=${shPre.x.toFixed(2)} slash=${shSlashX.toFixed(2)} sf=${V.sheatheFade(V.player).toFixed(3)}`);
let shPrev=shPre.x, shMaxHop=0, shEnd=null, shSawSlashFlash=false;
const shAabb0 = V.bodyAABB(V.player);
for (let i=0;i<16;i++){
  freezeRivalAI(); V.update(STEP); V.hitstopLeft=0; V.openLeft=0;
  freezeRivalAI();
  const tip=V.bladeTipX(V.player); const sf=V.sheatheFade(V.player);
  const h=Math.abs(tip-shPrev); if(h>shMaxHop) shMaxHop=h;
  if(sf>0 && sf<0.05 && Math.abs(tip-shSlashX)<12) shSawSlashFlash=true;
  if(V.player.sheatheT<=0) { shEnd={tip,sf}; break; }
  shPrev=tip;
}
check("tip under sheathe↔guardDrop tip gate live sheathe no end dump", !shSawSlashFlash && shMaxHop<40, `flash=${shSawSlashFlash} maxHop=${shMaxHop.toFixed(2)}`);
check("tip under sheathe↔guardDrop tip gate live sheathe settles idle", shEnd && Math.abs(shEnd.tip-shEdgeX)<8, `end=${shEnd?shEnd.tip.toFixed(2):"na"} edge=${shEdgeX.toFixed(2)}`);
check("tip under sheathe↔guardDrop tip gate live sheathe AABB planted", Math.abs(V.bodyAABB(V.player).y-shAabb0.y)<8, `dy=${(V.bodyAABB(V.player).y-shAabb0.y).toFixed(2)}`);

// LIVE: raise mid-sheathe — tip holds slash blend (no idle-edge dump)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0;
V.player.sheatheT=V.SHEATHE_MS; V.player.cut="slash"; V.player.cutHit=true;
V.player.guarding=false; V.player.guardPoseK=0; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.facing=1; V.openLeft=0; V.hitstopLeft=0;
freezeRivalAI();
const sgdPre = {x:V.bladeTipX(V.player), y:V.bladeTipY(V.player)};
const sgdSlashX = V.destRect(V.player).dx + 991 * (V.destRect(V.player).dw/1186);
const sgdEdgeX = V.player.facing>0 ? V.destRect(V.player).dx+V.destRect(V.player).dw-8 : V.destRect(V.player).dx+8;
V.keys.add("KeyS");
freezeRivalAI(); V.update(STEP); V.hitstopLeft=0; freezeRivalAI();
const sgdRaise1 = {x:V.bladeTipX(V.player), y:V.bladeTipY(V.player)};
const sgdHop = Math.hypot(sgdRaise1.x-sgdPre.x, sgdRaise1.y-sgdPre.y);
const sgdSf = V.sheatheFade(V.player);
check("tip under sheathe↔guardDrop tip gate live raise mid-sheathe no idle dump", V.player.guarding && sgdSf>0.8 && sgdHop<25 && Math.abs(sgdRaise1.x-sgdEdgeX)>20, `g=${V.player.guarding} sf=${sgdSf.toFixed(3)} hop=${sgdHop.toFixed(2)} tip=${sgdRaise1.x.toFixed(2)} edge=${sgdEdgeX.toFixed(2)} slash=${sgdSlashX.toFixed(2)}`);
let sgdPrev=sgdRaise1, sgdMaxHop=sgdHop;
for (let i=0;i<10;i++){
  freezeRivalAI(); V.update(STEP); V.hitstopLeft=0; freezeRivalAI();
  const t={x:V.bladeTipX(V.player), y:V.bladeTipY(V.player)};
  const h=Math.hypot(t.x-sgdPrev.x, t.y-sgdPrev.y); if(h>sgdMaxHop) sgdMaxHop=h;
  sgdPrev=t;
}
check("tip under sheathe↔guardDrop tip gate live raise trail continuous", sgdMaxHop<35, `maxHop=${sgdMaxHop.toFixed(2)}`);

// LIVE: release mid-sheathe — tip eases block/slash→idle (no slash pull)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.phaseT=0; V.player.gait=0;
V.player.sheatheT=V.SHEATHE_MS; V.player.cut="slash"; V.player.cutHit=true;
V.player.guarding=false; V.player.guardPoseK=0; V.player.stunT=0; V.player.boltPhase=""; V.player.feintT=0;
V.player.facing=1; V.openLeft=0; V.hitstopLeft=0;
V.keys.add("KeyS");
for (let i=0;i<5;i++){ freezeRivalAI(); V.update(STEP); V.hitstopLeft=0; freezeRivalAI(); }
const sdPre = {x:V.bladeTipX(V.player), y:V.bladeTipY(V.player)};
const sdSlashX = V.destRect(V.player).dx + 991 * (V.destRect(V.player).dw/1186);
const sdEdgeX = V.player.facing>0 ? V.destRect(V.player).dx+V.destRect(V.player).dw-8 : V.destRect(V.player).dx+8;
const sdSh = V.player.sheatheT;
V.keys.delete("KeyS");
freezeRivalAI(); V.update(STEP); V.hitstopLeft=0; freezeRivalAI();
const sdDrop1 = {x:V.bladeTipX(V.player), y:V.bladeTipY(V.player)};
const sdHop = Math.hypot(sdDrop1.x-sdPre.x, sdDrop1.y-sdPre.y);
const sdGdf = V.guardDropFade(V.player);
check("tip under sheathe↔guardDrop tip gate live drop mid-sheathe armed", sdGdf>0.5 && V.player.sheatheT>0 && !V.player.guarding, `gdf=${sdGdf.toFixed(3)} sh=${V.player.sheatheT} g=${V.player.guarding} preSh=${sdSh}`);
check("tip under sheathe↔guardDrop tip gate live drop no slash pull", sdHop<30 && Math.abs(sdDrop1.x-sdSlashX)>30, `hop=${sdHop.toFixed(2)} tip=${sdDrop1.x.toFixed(2)} slash=${sdSlashX.toFixed(2)} edge=${sdEdgeX.toFixed(2)}`);
let sdPrev=sdDrop1, sdMaxHop=sdHop, sdSawSlash=false;
for (let i=0;i<14;i++){
  freezeRivalAI(); V.update(STEP); V.hitstopLeft=0; freezeRivalAI();
  const t={x:V.bladeTipX(V.player), y:V.bladeTipY(V.player)};
  const h=Math.hypot(t.x-sdPrev.x, t.y-sdPrev.y); if(h>sdMaxHop) sdMaxHop=h;
  if(Math.abs(t.x-sdSlashX)<20) sdSawSlash=true;
  sdPrev=t;
  if(V.guardDropFade(V.player)<=0 && V.player.sheatheT<=0) break;
}
check("tip under sheathe↔guardDrop tip gate live drop trail no slash", !sdSawSlash && sdMaxHop<35, `sawSlash=${sdSawSlash} maxHop=${sdMaxHop.toFixed(2)}`);

// LIVE: pure guard drop still plants (regression)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.sheatheT=0; V.player.feintT=0; V.player.gait=0; V.player.stunT=0; V.player.boltPhase="";
V.player.facing=1; V.openLeft=0;
V.keys.add("KeyS");
for (let i=0;i<20;i++){ freezeRivalAI(); V.update(STEP); V.hitstopLeft=0; freezeRivalAI(); }
const pdPreY = V.bladeTipY(V.player);
V.keys.delete("KeyS");
freezeRivalAI(); V.update(STEP); V.hitstopLeft=0; freezeRivalAI();
const pdGdf = V.guardDropFade(V.player);
const pdPlantY = V.bladeTipY(V.player);
const pdDumpY = 394.4;
check("tip under sheathe↔guardDrop tip gate live pure drop still plants", pdGdf>0.5 && Math.abs(pdPlantY-pdDumpY)>20, `gdf=${pdGdf.toFixed(3)} plant=${pdPlantY.toFixed(2)} dump=${pdDumpY} pre=${pdPreY.toFixed(2)}`);

// LIVE: guard raise still eases (v342 regression)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.sheatheT=0; V.player.guarding=false; V.player.guardPoseK=0;
V.player.feintT=0; V.player.gait=0; V.player.facing=1;
const sgdGrIdleY = V.bladeTipY(V.player);
V.keys.add("KeyS");
freezeRivalAI(); V.update(STEP); V.hitstopLeft=0; freezeRivalAI();
let sgdGrMid=null;
for (let i=0;i<6;i++){
  freezeRivalAI(); V.update(STEP); V.hitstopLeft=0; freezeRivalAI();
  const rk=V.guardRaiseK(V.player);
  if(rk>0.2 && rk<0.8 && !sgdGrMid) sgdGrMid={tipY:V.bladeTipY(V.player), rk};
}
V.player.guardPoseK=1;
const sgdGrFullY = V.bladeTipY(V.player);
check("tip under sheathe↔guardDrop tip gate live raise still eases", sgdGrMid && Math.abs(sgdGrMid.tipY-sgdGrIdleY)>6 && Math.abs(sgdGrMid.tipY-sgdGrFullY)>6, `mid=${sgdGrMid?sgdGrMid.tipY.toFixed(2):"na"} idle=${sgdGrIdleY.toFixed(2)} full=${sgdGrFullY.toFixed(2)} rk=${sgdGrMid?sgdGrMid.rk.toFixed(3):"na"}`);

check("cache v=345 after tip under sheathe↔guardDrop tip gate live", /game.js\?v=350/.test(htmlTxt));




// --- tip under holdCutFade / boltPlant leftover (v346) ---
check("tip under holdCutFade / boltPlant leftover seats no tip dump off cut", /Tip under holdCutFade \/ boltPlant leftover \(v346\)/.test(srcTxt) && /special-cancel cut→K tip used to/.test(srcTxt) && (/~26px/.test(srcTxt) || /hopped ~26px/.test(srcTxt)) && /leftover sheathe still owned the sheet/.test(srcTxt));
check("tip under holdCutFade / boltPlant leftover is draw-only", /Draw-only \(bladeTipX \/ bladeTipY\)/.test(srcTxt) && /const hfTip = holdCutFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /const hfTipY = holdCutFade\(f\)/.test(sliceFn(codeOnly, "bladeTipY")) && /1 - hfTip \* \(1 - sheatheK\)/.test(sliceFn(codeOnly, "bladeTipX")) && /1 - hfTipY \* \(1 - sheatheK\)/.test(sliceFn(codeOnly, "bladeTipY")) && !/hfTip/.test(sliceFn(codeOnly, "destRect")) && !/hfTip/.test(sliceFn(codeOnly, "bodyAABB")) && !/hfTip/.test(sliceFn(codeOnly, "poseBitmap")) && !/hfTip/.test(sliceFn(codeOnly, "landHit")));
check("tip under holdCutFade / boltPlant leftover seats recovery sheathe tip on idle", /recovery sheatheFade tip on idle-edge|recovery sheatheFade also seats/.test(srcTxt) && /f\.phase === \"recovery\"/.test(sliceFn(codeOnly, "bladeTipX")) && /f\.phase === \"recovery\"/.test(sliceFn(codeOnly, "bladeTipY")) && /sfTip > 0/.test(sliceFn(codeOnly, "bladeTipX")) && !/sfTip > 0 && f\.sheatheT > 0/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under holdCutFade / boltPlant leftover eases leftover-sheathe→slash", /Ease leftover-sheathe tip→slash with holdCutFade/.test(srcTxt) && /ex \+ \(sx - ex\) \* sk/.test(sliceFn(codeOnly, "bladeTipX")) && /ey \+ \(sy - ey\) \* sk/.test(sliceFn(codeOnly, "bladeTipY")) && /f\.linkSheathe/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under holdCutFade / boltPlant leftover castPlantXY follows tip", /holdingCutBolt\(f\)/.test(sliceFn(codeOnly, "castPlantXY")) && /return fallback/.test(sliceFn(codeOnly, "castPlantXY")) && /castPlantXY follows bladeTip/.test(srcTxt));
check("tip under holdCutFade / boltPlant leftover tip under boltPlantFade still 0", /holdingCutBolt/.test(sliceFn(codeOnly, "boltPlantFade")) && /Tip under boltPlantFade still 0 \(holdingCutBolt\)|holdingCutBolt still 0/.test(srcTxt));
check("tip under holdCutFade / boltPlant leftover does not regress tip under sheatheFade v345", /Tip under sheatheFade leftover \(v345\)/.test(srcTxt) && /const sfTip = sheatheFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")) && /sx \+ \(poseTip - sx\) \* \(1 - sfTip\)/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under holdCutFade / boltPlant leftover does not regress tipPlantK+tele v343", /Tip under tipPlantK\+tele leftover stack \(v343\)/.test(srcTxt) && /leftoverPlantTipK/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under holdCutFade / boltPlant leftover does not regress sheathe↔guardDrop v345", /Tip under sheathe↔guardDrop tip gate \(v345\)/.test(srcTxt) && /guardDropFade\(f\) > 0\.02 && f\.sheatheT > 0/.test(sliceFn(codeOnly, "bladeTipX")));
check("tip under holdCutFade / boltPlant leftover tipX / pad / frames locked", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt) && /tipX: 268/.test(srcTxt) && V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280);
check("tip under holdCutFade / boltPlant leftover is not a new combat verb", /No new combat verb/.test(srcTxt) && /active hitbox unchanged/.test(srcTxt) && !/KeyM/.test(codeOnly));
check("slash frames still 180/140/280 after tip under holdCutFade / boltPlant leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280);
check("golpe frames still 120/80/180 after tip under holdCutFade / boltPlant leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180);
check("K frames still 200/280 after tip under holdCutFade / boltPlant leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280);
check("riposte frames still 130/140/280 after tip under holdCutFade / boltPlant leftover", V.RIPOSTE_STARTUP===130 && V.RIPOSTE_ACTIVE===140 && V.RIPOSTE_RECOVERY===280);
check("cache v=348 after tip under holdCutFade / boltPlant leftover", /game.js\?v=350/.test(htmlTxt));

// LIVE: recovery tip tracks sheathe; cancel continuous; holdCutFade eases to slash; cast hard slash
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.facing=1; V.rival.facing=-1;
V.player.phase="recovery"; V.player.phaseT=200; V.player.cut="slash"; V.player.cutHit=true;
V.player.boltPhase=""; V.player.boltHoldCut=false; V.player.stamina=100;
V.player.guarding=false; V.player.stunT=0; V.player.linkBolt=true; V.player.sheatheT=0;
V.player.gait=0; V.openLeft=0; V.hitstopLeft=0;
freezeRivalAI();
const hcX0 = V.player.x;
const hcPre = {x:V.bladeTipX(V.player), y:V.bladeTipY(V.player)};
const hcSf = V.sheatheFade(V.player);
const hcR0 = V.destRect(V.player);
const hcSlashX = hcR0.dx + 991 * (hcR0.dw/1186);
const hcEdgeX = V.player.facing>0 ? hcR0.dx+hcR0.dw-8 : hcR0.dx+8;
const hcBlend = hcEdgeX + (hcSlashX - hcEdgeX) * hcSf;
check("tip under holdCutFade / boltPlant leftover live recovery tip tracks sheathe", hcSf>0.1 && hcSf<0.9 && Math.abs(hcPre.x-hcBlend)<8 && Math.abs(hcPre.x-hcSlashX)>8, `sf=${hcSf.toFixed(3)} tip=${hcPre.x.toFixed(2)} blend=${hcBlend.toFixed(2)} slash=${hcSlashX.toFixed(2)}`);
const hcOk = V.cancelIntoBolt(V.player);
const hcPost = {x:V.bladeTipX(V.player), y:V.bladeTipY(V.player)};
const hcHop = Math.hypot(hcPost.x-hcPre.x, hcPost.y-hcPre.y);
const hcHf = V.holdCutFade(V.player);
const hcPlant = V.castPlantXY(V.player);
check("tip under holdCutFade / boltPlant leftover live cancel continuous", hcOk && V.player.boltHoldCut && hcHf>0.8 && hcHop<8, `ok=${hcOk} hop=${hcHop.toFixed(2)} hf=${hcHf.toFixed(3)} pre=${hcPre.x.toFixed(2)} post=${hcPost.x.toFixed(2)}`);
check("tip under holdCutFade / boltPlant leftover live cast tracks tip at cancel", Math.abs(hcPlant.x-hcPost.x)<4 && Math.abs(hcPlant.x-hcSlashX)>6, `plant=${hcPlant.x.toFixed(2)} tip=${hcPost.x.toFixed(2)} slash=${hcSlashX.toFixed(2)}`);
let hcSawSlash=false, hcMaxHop=0, hcPrev=hcPost, hcEnd=null, hcAabb0=V.bodyAABB(V.player);
for (let i=0;i<20;i++){
  V.player.x=hcX0; V.player.gait=0; freezeRivalAI(); V.hitstopLeft=0; V.openLeft=0;
  V.update(STEP); V.player.x=hcX0;
  const tip={x:V.bladeTipX(V.player), y:V.bladeTipY(V.player)};
  const h=Math.hypot(tip.x-hcPrev.x, tip.y-hcPrev.y); if(h>hcMaxHop) hcMaxHop=h;
  const r=V.destRect(V.player); const slashX=r.dx+991*(r.dw/1186);
  if (V.holdCutFade(V.player)<0.02 && V.player.boltPhase==="startup" && Math.abs(tip.x-slashX)<8) hcSawSlash=true;
  if (V.holdCutFade(V.player)===0 && V.player.boltPhase==="startup" && !hcEnd) hcEnd={tipX:tip.x, slashX, plant:V.castPlantXY(V.player).x};
  hcPrev=tip;
  if (V.player.boltPhase==="recovery") break;
}
check("tip under holdCutFade / boltPlant leftover live eases to slash", hcSawSlash && hcEnd && Math.abs(hcEnd.tipX-hcEnd.slashX)<8, `saw=${hcSawSlash} end=${hcEnd?hcEnd.tipX.toFixed(2):"na"} slash=${hcEnd?hcEnd.slashX.toFixed(2):"na"} maxHop=${hcMaxHop.toFixed(2)}`);
check("tip under holdCutFade / boltPlant leftover live cast seats slash after fade", hcEnd && Math.abs(hcEnd.plant-hcEnd.slashX)<8 && Math.abs(hcEnd.tipX-hcEnd.slashX)<8, `plant=${hcEnd?hcEnd.plant.toFixed(2):"na"} tip=${hcEnd?hcEnd.tipX.toFixed(2):"na"} slash=${hcEnd?hcEnd.slashX.toFixed(2):"na"}`);
check("tip under holdCutFade / boltPlant leftover live no big tip hop", hcMaxHop<40, `maxHop=${hcMaxHop.toFixed(2)}`);
check("tip under holdCutFade / boltPlant leftover live AABB planted", Math.abs(V.bodyAABB(V.player).y-hcAabb0.y)<8, `dy=${(V.bodyAABB(V.player).y-hcAabb0.y).toFixed(2)}`);

// LIVE: idle K boltPlantFade tip path unchanged (v334)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900; V.player.facing=1;
V.player.phase="idle"; V.player.boltPhase=""; V.player.sheatheT=0; V.player.stamina=100;
V.openLeft=0; V.hitstopLeft=0; freezeRivalAI();
const ikDid = V.startBolt(V.player);
let ikBirth=null;
for (let i=0;i<20;i++){
  V.player.gait=0; freezeRivalAI(); V.hitstopLeft=0; V.update(STEP);
  if (V.player.boltPhase==="recovery" && !ikBirth){
    ikBirth={bf:V.boltPlantFade(V.player), tip:V.bladeTipX(V.player), plant:V.castPlantXY(V.player).x, hold:!!V.player.boltHoldCut};
    break;
  }
}
const ikR = V.destRect(V.player);
const ikKnife = ikR.dx + 268 * (ikR.dw/1186);
check("tip under holdCutFade / boltPlant leftover live idle K birth on knife", ikDid && ikBirth && !ikBirth.hold && ikBirth.bf>0.9 && Math.abs(ikBirth.tip-ikKnife)<12 && Math.abs(ikBirth.plant-ikKnife)<12, `did=${ikDid} birth=${JSON.stringify(ikBirth)} knife=${ikKnife.toFixed(2)}`);

// LIVE: idle sheathe tip still eases (v345 regression)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.player.x=400; V.rival.x=900;
V.player.phase="idle"; V.player.sheatheT=V.SHEATHE_MS; V.player.cut="slash";
V.player.guarding=false; V.player.boltPhase=""; V.player.facing=1;
freezeRivalAI();
const sh0 = V.bladeTipX(V.player);
const shR = V.destRect(V.player);
const shSlash = shR.dx + 991 * (shR.dw/1186);
check("tip under holdCutFade / boltPlant leftover live sheathe still starts slash", Math.abs(sh0-shSlash)<8 && V.sheatheFade(V.player)>0.9, `tip=${sh0.toFixed(2)} slash=${shSlash.toFixed(2)} sf=${V.sheatheFade(V.player).toFixed(3)}`);

check("cache v=348 after tip under holdCutFade / boltPlant leftover live", /game.js\?v=350/.test(htmlTxt));



// --- KO slashPose ox under pf leftover (v347) ---
check("KO slashPose ox under pf leftover eases mid-cut ox", /KO slashPose ox under pf leftover \(v347\)/.test(srcTxt) && /dump destRect ox\/rot the same tick/.test(srcTxt) && /beginFall set falling/.test(srcTxt) && /stun path/.test(srcTxt) && /gated hp>0/.test(srcTxt));
check("KO slashPose ox under pf leftover is destRect-only", /destRect-only \(slashPose\)|destRect-only/.test(srcTxt) && /parryFade\(f\)/.test(sliceFn(codeOnly, "slashPose")) && /f\.parryFadeT/.test(sliceFn(codeOnly, "slashPose")) && /AABB planted/.test(srcTxt) && !/parryFadeSheet/.test(sliceFn(codeOnly, "bodyAABB")) && !/parryFadeT/.test(sliceFn(codeOnly, "bodyAABB")));
check("KO slashPose ox under pf leftover mirrors interrupt fromOx", /fromOx \* pk/.test(sliceFn(codeOnly, "slashPose")) && /parryFadeSheet \|\| \"slash\"/.test(sliceFn(codeOnly, "slashPose")) && /facing \* fromOx \* pk/.test(sliceFn(codeOnly, "slashPose")));
check("KO slashPose ox under pf leftover idle KO still ox 0", /rot: 0, ox: 0, oy: -10 \* lift \* crumpleFade\(f\)/.test(codeOnly) && /Idle KO \(no pf\)|idle KO \(no pf\)|Idle KO \(no parryFadeT\)/.test(srcTxt));
check("KO slashPose ox under pf leftover keeps crumple oy", /fromOy \* pk \+ \(-10 \* lift \* crumpleFade\(f\)\)/.test(sliceFn(codeOnly, "slashPose")) && /crumpleFade\(f\)/.test(sliceFn(codeOnly, "slashPose")));
check("KO slashPose ox under pf leftover poseBitmap still hurt", /f\.falling \|\| f\.hp <= 0/.test(sliceFn(codeOnly, "poseBitmap")) && /f\.falling \|\| f\.hp <= 0/.test(sliceFn(codeOnly, "poseSheet")));
check("KO slashPose ox under pf leftover AABB planted on fall", /f\.falling/.test(sliceFn(codeOnly, "bodyAABB")) && /stunT > 0 && f\.hp > 0/.test(sliceFn(codeOnly, "bodyAABB")) && /f\.hp <= 0/.test(sliceFn(codeOnly, "bodyAABB")));
check("KO slashPose ox under pf leftover does not regress tip under holdCut v346", /Tip under holdCutFade \/ boltPlant leftover \(v346\)/.test(srcTxt) && /const hfTip = holdCutFade\(f\)/.test(sliceFn(codeOnly, "bladeTipX")));
check("KO slashPose ox under pf leftover tipX / plants / frames locked", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt) && V.FALL_MS===720 && V.GUARD_RAISE_MS===80 && V.SHEATHE_MS===140 && V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280);
check("KO slashPose ox under pf leftover is not a new combat verb", /No new combat verb/.test(srcTxt) && V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.FALL_MS===720 && V.SLASH_DMG===10);
check("slash frames still 180/140/280 after KO slashPose ox under pf leftover", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after KO slashPose ox under pf leftover", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after KO slashPose ox under pf leftover", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("riposte frames still 130/140/280 after KO slashPose ox under pf leftover", V.RIPOSTE_STARTUP===130 && V.RIPOSTE_ACTIVE===140 && V.RIPOSTE_RECOVERY===280);
check("no 6th button after KO slashPose ox under pf leftover", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=348 after KO slashPose ox under pf leftover", /game.js\?v=350/.test(htmlTxt));


// LIVE: mid-cut KO eases ox under pf (chip stun parity); idle KO still ox 0
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.mode = "play";
V.openLeft = 0; V.hitstopLeft = 0;
V.player.hp = 100; V.rival.hp = 8;
V.player.stamina = 100; V.rival.stamina = 100;
V.player.x = 400; V.rival.x = 700; // out of blade so resolveCuts cannot steal
V.player.facing = 1; V.rival.facing = -1;
V.player.phase = "idle"; V.player.phaseT = 0; V.player.boltPhase = "";
V.rival.guarding = false; V.rival.wantBlock = false; V.rival.closing = false;
V.rival.boltPhase = ""; V.rival.sheatheT = 0; V.rival.gait = 0;
V.rival.stunT = 0; V.rival.falling = false; V.rival.parryFadeT = 0; V.rival.parryFadeSheet = "";
V.rival.hp = 8;
V.startAttack(V.rival, "slash");
V.rival.phase = "active"; V.rival.phaseT = 20; V.rival.cut = "slash";
V.rival.telegraph = false; V.rival.cutHit = false;
V.openLeft = 0; V.hitstopLeft = 0;
const koOxWas = V.rival.phase === "active" && /slash/.test((V.poseBitmap(V.rival)&&V.poseBitmap(V.rival)._src)||"");
V.landHit(V.player, V.rival, 1);
const koOxHs = V.hitstopLeft > 0 && V.rival.hp <= 0;
const koOxPf = V.parryFade(V.rival);
const koOxPose = V.slashPose(V.rival);
const koOxAabb0 = V.bodyAABB(V.rival);
check("KO slashPose ox under pf leftover live freeze arms pf", koOxWas && koOxHs && koOxPf > 0.8 && V.rival.parryFadeSheet === "slash" && !V.rival.falling, `was=${koOxWas} hs=${koOxHs} pf=${koOxPf} pfs=${V.rival.parryFadeSheet} fall=${V.rival.falling} hp=${V.rival.hp}`);
check("KO slashPose ox under pf leftover live freeze eases ox", koOxHs && koOxPose && Math.abs(koOxPose.ox) > 20 && Math.abs(Math.abs(koOxPose.ox) - 72) < 1, `ox=${koOxPose ? koOxPose.ox.toFixed(2) : "na"} rot=${koOxPose ? koOxPose.rot.toFixed(3) : "na"} facing=${V.rival.facing}`);
check("KO slashPose ox under pf leftover live AABB planted at freeze", koOxHs && koOxAabb0 && Math.abs(V.bodyAABB(V.rival).y - koOxAabb0.y) < 0.05, `dy=${koOxAabb0 ? (V.bodyAABB(V.rival).y - koOxAabb0.y).toFixed(2) : "na"}`);
for (let i = 0; i < 25; i++) {
  if (V.hitstopLeft <= 0 && V.mode === "falling") break;
  V.update(STEP);
}
const koOxFall = V.mode === "falling" && V.rival.falling;
const koOxPoseFall = V.slashPose(V.rival);
const koOxPfFall = V.parryFade(V.rival);
check("KO slashPose ox under pf leftover live fall keeps ox under pf", koOxFall && koOxPfFall > 0.5 && Math.abs(koOxPoseFall.ox) > 10, `fall=${koOxFall} pf=${koOxPfFall.toFixed(3)} ox=${koOxPoseFall.ox.toFixed(2)} ft=${V.rival.fallT}`);
check("KO slashPose ox under pf leftover live AABB planted at fall", koOxFall && koOxAabb0 && Math.abs(V.bodyAABB(V.rival).y - koOxAabb0.y) < 0.05 && Math.abs(V.bodyAABB(V.rival).h - koOxAabb0.h) < 0.05, `dy=${koOxAabb0 ? (V.bodyAABB(V.rival).y - koOxAabb0.y).toFixed(2) : "na"}`);
for (let i = 0; i < 40; i++) {
  V.update(STEP);
  if (V.parryFade(V.rival) < 0.02 && V.rival.falling) break;
}
check("KO slashPose ox under pf leftover live ox clears after pf", V.rival.falling && V.parryFade(V.rival) < 0.02 && Math.abs(V.slashPose(V.rival).ox) < 0.05, `pf=${V.parryFade(V.rival).toFixed(3)} ox=${V.slashPose(V.rival).ox.toFixed(2)} fall=${V.rival.falling}`);

// LIVE: idle KO still ox 0 (no pf latch)
bootPlay(); wait(900);
freezeYouAI(); freezeRivalAI();
V.rival.hp = 10;
V.player.x = V.rival.x - 170;
V.keepApart();
V.attackEdge = true;
let idleKoHs = false, idleKoOx = null;
for (let i = 0; i < 80; i++) {
  freezeRivalAI();
  V.update(STEP);
  if (V.hitstopLeft > 0 && V.rival.hp <= 0) {
    idleKoHs = true;
    idleKoOx = V.slashPose(V.rival);
    break;
  }
}
check("KO slashPose ox under pf leftover live idle KO ox 0", idleKoHs && idleKoOx && Math.abs(idleKoOx.ox) < 0.05 && (V.rival.parryFadeT || 0) === 0, `hs=${idleKoHs} ox=${idleKoOx ? idleKoOx.ox.toFixed(2) : "na"} pft=${V.rival.parryFadeT} pfs=${V.rival.parryFadeSheet}`);
for (let i = 0; i < 25; i++) {
  if (V.hitstopLeft <= 0 && V.mode === "falling") break;
  V.update(STEP);
}
check("KO slashPose ox under pf leftover live idle fall ox 0", V.rival.falling && Math.abs(V.slashPose(V.rival).ox) < 0.05 && Math.abs(V.destRect(V.rival).rot) < 0.001, `fall=${V.rival.falling} ox=${V.slashPose(V.rival).ox.toFixed(2)} rot=${V.destRect(V.rival).rot.toFixed(3)}`);


check("cache v=348 after KO slashPose ox under pf leftover live", /game.js\?v=350/.test(htmlTxt));

// --- Knock grit leftover walk stamps ↔ punchCover (v348) ---
check("knock grit leftover walk stamps eases orphan mid-cover", /Knock grit leftover walk stamps ↔ punchCover \(v348\)/.test(srcTxt) && /ride the knock boot/.test(srcTxt) && /syncPlantDust/.test(srcTxt) && /fade on their own t\/life/.test(srcTxt) && /unmarked grit died/.test(srcTxt) && /mid-cover beside punch-marked/.test(srcTxt));
check("knock grit leftover walk stamps is draw-only cull", /Draw-only \(dropPlantUnderKnock\)/.test(srcTxt) && /function dropPlantUnderKnock/.test(srcTxt) && /dropPlantUnderKnock\(def\)/.test(sliceFn(codeOnly, "landHit")) && /dropPlantUnderKnock\(def\)/.test(sliceFn(codeOnly, "landBoltHit")) && !/dropPlantUnderKnock/.test(sliceFn(codeOnly, "destRect")) && !/dropPlantUnderKnock/.test(sliceFn(codeOnly, "bodyAABB")) && !/dropPlantUnderKnock/.test(sliceFn(codeOnly, "slashPose")) && !/dropPlantUnderKnock/.test(sliceFn(codeOnly, "plantDustK")));
check("knock grit leftover walk stamps culls unmarked only", /p\.speck \|\| p\.shove/.test(sliceFn(codeOnly, "dropPlantUnderKnock")) && /p\.punch/.test(sliceFn(codeOnly, "dropPlantUnderKnock")) && /p\.homeYou !== homeYou/.test(sliceFn(codeOnly, "dropPlantUnderKnock")) && /Specks still/.test(srcTxt) && /Shove trails stay/.test(srcTxt));
check("knock grit leftover walk stamps landThrow / tech too", /dropPlantUnderKnock\(atk\)/.test(sliceFn(codeOnly, "landThrow")) && /dropPlantUnderKnock\(def\)/.test(sliceFn(codeOnly, "landThrow")) && /dropPlantUnderKnock\(atk\)/.test(sliceFn(codeOnly, "landThrowTech")) && /dropPlantUnderKnock\(def\)/.test(sliceFn(codeOnly, "landThrowTech")));
check("knock grit leftover walk stamps keeps clash/pushblock culls", /dropPlantUnderClash\(\)/.test(sliceFn(codeOnly, "doClash")) && /dropBlockPlantUnderShove\(f\)/.test(sliceFn(codeOnly, "tryPushblock")) && /Clash dropPlantUnderClash \/ pushblock dropBlockPlantUnderShove/.test(srcTxt));
check("knock grit leftover walk stamps does not regress KO ox v347", /KO slashPose ox under pf leftover \(v347\)/.test(srcTxt) && /f\.falling \|\| \(f\.hp <= 0 && \(f\.parryFadeT \|\| 0\) > 0\)/.test(sliceFn(codeOnly, "slashPose")));
check("knock grit leftover walk stamps tipX / plants / frames locked", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt) && V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.KNOCK_MS===180 && V.KNOCK_PX===80 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.GUARD_RAISE_MS===80);
check("knock grit leftover walk stamps is not a new combat verb", /No new combat verb/.test(srcTxt) && V.SLASH_DMG===10 && V.HITSTOP_HIT===140 && V.HITSTOP_BLOCK===60 && V.CLASH_FX===1.7 && V.TECH_FX===1.5 && V.PUSHBLOCK_FX===1.85);
check("slash frames still 180/140/280 after knock grit leftover walk stamps", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120/80/180 after knock grit leftover walk stamps", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200/280 after knock grit leftover walk stamps", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("riposte frames still 130/140/280 after knock grit leftover walk stamps", V.RIPOSTE_STARTUP===130 && V.RIPOSTE_ACTIVE===140 && V.RIPOSTE_RECOVERY===280);
check("no 6th button after knock grit leftover walk stamps", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=348 after knock grit leftover walk stamps", /game\.js\?v=350/.test(htmlTxt));

// LIVE: leftover unmarked walk grit culled on knock; connect grit holds mid-cover
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.mode = "play"; V.openLeft = 0; V.hitstopLeft = 0;
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100; V.rival.stamina = 100;
V.player.x = 520; V.rival.x = 620; V.keepApart();
V.player.facing = 1; V.rival.facing = -1;
V.rival.guarding = false; V.rival.wantBlock = false; V.rival.closing = false;
V.rival.gait = 0; V.rival.stunT = 0; V.rival.falling = false;
{
  const r = V.destRect(V.rival);
  V.plantDust.push({
    x: r.pivX, y: 657, t: 40, life: 280, power: 0.8, facing: -1,
    shoveDir: 0, shove: false, vx: 0, homeYou: false, plantDX: 0, punch: false,
  });
}
const kgSeed = (V.plantDust||[]).filter(p=>!p.speck && p.homeYou===false && !p.punch).length;
V.player.phase = "active"; V.player.phaseT = 10; V.player.cut = "slash"; V.player.cutHit = false;
V.landHit(V.player, V.rival, 1);
const kgAfter = (V.plantDust||[]).filter(p=>!p.speck && p.homeYou===false);
const kgUnmarked = kgAfter.filter(p=>!p.punch);
const kgMarked = kgAfter.filter(p=>!!p.punch);
check("knock grit leftover walk stamps live culls unmarked on landHit", kgSeed>=1 && kgUnmarked.length===0 && kgMarked.length>=1 && kgMarked.every(p=>p.punch===true), `seed=${kgSeed} unmarked=${kgUnmarked.length} marked=${kgMarked.length}`);
check("knock grit leftover walk stamps live connect grit punch-marked", kgMarked.length>=1 && V.shake>0 && V.punchCover()>0 && V.plantDustK(kgMarked[0])>0.95, `marked=${kgMarked.length} shake=${V.shake} cover=${V.punchCover()} k=${kgMarked[0]?V.plantDustK(kgMarked[0]).toFixed(3):"na"}`);
let kgOrphan = false;
let kgSawCover = false;
for (let i = 0; i < 22; i++) {
  freezeRivalAI();
  V.rival.gait = 0; V.rival.closing = false; V.rival.wantBlock = false;
  V.rival.walkT = 0; V.rival.walkFadeHold = 0; V.rival.walkSettleT = 0;
  V.update(STEP);
  const cover = V.punchCover();
  const st = (V.plantDust||[]).filter(p=>!p.speck && !p.shove && p.homeYou===false);
  const um = st.filter(p=>!p.punch);
  const mk = st.filter(p=>!!p.punch);
  // Full cover only (PUNCH_PX): ease quarter can drop draw-K below 0.95 while cover still >8.
  if (cover >= V.PUNCH_PX - 0.01) {
    kgSawCover = true;
    if (um.length > 0) kgOrphan = true;
    if (mk.length && V.plantDustK(mk[0]) < 0.95) kgOrphan = true;
  }
}
check("knock grit leftover walk stamps live no unmarked mid-cover", kgSawCover && !kgOrphan, `sawCover=${kgSawCover} orphan=${kgOrphan}`);
bootPlay(); wait(50);
freezeYouAI(); freezeRivalAI();
V.openLeft = 0; V.hitstopLeft = 0;
V.plantDust.splice(0);
{
  const r = V.destRect(V.player);
  V.plantDust.push({
    x: r.pivX, y: 657, t: 0, life: 200, power: 0.8, facing: 1,
    shoveDir: 0, shove: false, vx: 0, homeYou: true, plantDX: 0, punch: false,
  });
}
const kgWalk = (V.plantDust||[]).find(p=>!p.speck && p.homeYou===true && !p.punch && p.life===200);
check("knock grit leftover walk stamps live walk grit stays linear", !!kgWalk && kgWalk.punch===false && kgWalk.t===0 && Math.abs(V.plantDustK(kgWalk)-1)<0.001 && V.punchCover()===0, `walk=${!!kgWalk} punch=${kgWalk&&kgWalk.punch} t=${kgWalk&&kgWalk.t} k=${kgWalk?V.plantDustK(kgWalk).toFixed(3):"na"} cover=${V.punchCover()}`);

check("cache v=348 after knock grit leftover walk stamps live", /game\.js\?v=350/.test(htmlTxt));

// --- Knock grit leftover atk walk stamps ↔ punchCover (v349) ---
check("knock grit leftover atk walk stamps eases orphan mid-cover", /Knock grit leftover atk walk stamps ↔ punchCover \(v349\)/.test(srcTxt) && /landHit \/ landBoltHit/.test(srcTxt) && /cull only the hurt fighter/.test(srcTxt) && /attacker's leftover walk\/idle/.test(srcTxt) && /unmarked atk grit died/.test(srcTxt) && /mid-cover beside punch-marked/.test(srcTxt) && /landThrow \/ landThrowTech/.test(srcTxt) && /already culled both/.test(srcTxt));
check("knock grit leftover atk walk stamps is draw-only cull", /Draw-only \(dropPlantUnderKnock\)/.test(srcTxt) && /dropPlantUnderKnock\(atk\)/.test(sliceFn(codeOnly, "landHit")) && /dropPlantUnderKnock\(def\)/.test(sliceFn(codeOnly, "landHit")) && /dropPlantUnderKnock\(atk\)/.test(sliceFn(codeOnly, "landBoltHit")) && /dropPlantUnderKnock\(def\)/.test(sliceFn(codeOnly, "landBoltHit")) && !/dropPlantUnderKnock/.test(sliceFn(codeOnly, "destRect")) && !/dropPlantUnderKnock/.test(sliceFn(codeOnly, "bodyAABB")) && !/dropPlantUnderKnock/.test(sliceFn(codeOnly, "slashPose")) && !/dropPlantUnderKnock/.test(sliceFn(codeOnly, "plantDustK")));
check("knock grit leftover atk walk stamps keeps throw\/tech both", /dropPlantUnderKnock\(atk\)/.test(sliceFn(codeOnly, "landThrow")) && /dropPlantUnderKnock\(def\)/.test(sliceFn(codeOnly, "landThrow")) && /dropPlantUnderKnock\(atk\)/.test(sliceFn(codeOnly, "landThrowTech")) && /dropPlantUnderKnock\(def\)/.test(sliceFn(codeOnly, "landThrowTech")));
check("knock grit leftover atk walk stamps keeps clash\/pushblock culls", /dropPlantUnderClash\(\)/.test(sliceFn(codeOnly, "doClash")) && /dropBlockPlantUnderShove\(f\)/.test(sliceFn(codeOnly, "tryPushblock")));
check("knock grit leftover atk walk stamps does not regress v348 def cull", /Knock grit leftover walk stamps ↔ punchCover \(v348\)/.test(srcTxt) && /dropPlantUnderKnock\(def\)/.test(sliceFn(codeOnly, "landHit")));
check("knock grit leftover atk walk stamps tipX \/ plants \/ frames locked", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt) && V.HITSTOP_HIT===140 && V.HITSTUN===350 && V.KNOCK_MS===180 && V.KNOCK_PX===80 && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.GUARD_RAISE_MS===80 && V.PUNCH_PX===12);
check("knock grit leftover atk walk stamps is not a new combat verb", /No new combat verb/.test(srcTxt) && V.SLASH_DMG===10 && V.HITSTOP_HIT===140 && V.HITSTOP_BLOCK===60 && V.CLASH_FX===1.7 && V.TECH_FX===1.5 && V.PUSHBLOCK_FX===1.85);
check("slash frames still 180\/140\/280 after knock grit leftover atk walk stamps", V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.LUNGE_PX===36);
check("golpe frames still 120\/80\/180 after knock grit leftover atk walk stamps", V.GOLPE_STARTUP===120 && V.GOLPE_ACTIVE===80 && V.GOLPE_RECOVERY===180 && V.GOLPE_LUNGE_PX===18);
check("K frames still 200\/280 after knock grit leftover atk walk stamps", V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.BOLT_SUPER_STOP===60);
check("riposte frames still 130\/140\/280 after knock grit leftover atk walk stamps", V.RIPOSTE_STARTUP===130 && V.RIPOSTE_ACTIVE===140 && V.RIPOSTE_RECOVERY===280);
check("no 6th button after knock grit leftover atk walk stamps", (htmlTxt.match(/data-tap=/g)||[]).length===3 && /data-tap="KeyL"/.test(htmlTxt) && /data-hold="KeyS"/.test(htmlTxt));
check("cache v=350 after knock grit leftover atk walk stamps", /game\.js\?v=350/.test(htmlTxt));

// LIVE: atk leftover unmarked walk grit culled on landHit; def connect grit holds
bootPlay(); wait(1700);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.mode = "play"; V.openLeft = 0; V.hitstopLeft = 0;
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100; V.rival.stamina = 100;
V.player.x = 520; V.rival.x = 620; V.keepApart();
V.player.facing = 1; V.rival.facing = -1;
V.rival.guarding = false; V.rival.wantBlock = false; V.rival.closing = false;
V.rival.gait = 0; V.rival.stunT = 0; V.rival.falling = false;
V.player.gait = 0; V.player.walkFadeHold = 0; V.player.walkSettleT = 0;
V.plantDust.splice(0);
{
  const y = V.destRect(V.player);
  const r = V.destRect(V.rival);
  V.plantDust.push({
    x: y.pivX, y: 657, t: 40, life: 280, power: 0.8, facing: 1,
    shoveDir: 0, shove: false, vx: 0, homeYou: true, plantDX: 0, punch: false,
  });
  V.plantDust.push({
    x: r.pivX, y: 657, t: 50, life: 280, power: 0.85, facing: -1,
    shoveDir: 0, shove: false, vx: 0, homeYou: false, plantDX: 0, punch: false,
  });
}
const kaAtkSeed = (V.plantDust||[]).filter(p=>!p.speck && !p.shove && p.homeYou===true && !p.punch).length;
const kaDefSeed = (V.plantDust||[]).filter(p=>!p.speck && !p.shove && p.homeYou===false && !p.punch).length;
V.player.phase = "active"; V.player.phaseT = 10; V.player.cut = "slash"; V.player.cutHit = false;
V.landHit(V.player, V.rival, 1);
const kaAtkAfter = (V.plantDust||[]).filter(p=>!p.speck && !p.shove && p.homeYou===true);
const kaDefAfter = (V.plantDust||[]).filter(p=>!p.speck && !p.shove && p.homeYou===false);
const kaAtkUm = kaAtkAfter.filter(p=>!p.punch);
const kaDefUm = kaDefAfter.filter(p=>!p.punch);
const kaDefMk = kaDefAfter.filter(p=>!!p.punch);
check("knock grit leftover atk walk stamps live culls atk unmarked on landHit", kaAtkSeed>=1 && kaAtkUm.length===0, `seed=${kaAtkSeed} unmarked=${kaAtkUm.length} after=${kaAtkAfter.length}`);
check("knock grit leftover atk walk stamps live still culls def unmarked", kaDefSeed>=1 && kaDefUm.length===0 && kaDefMk.length>=1 && kaDefMk.every(p=>p.punch===true), `defSeed=${kaDefSeed} defUm=${kaDefUm.length} marked=${kaDefMk.length}`);
check("knock grit leftover atk walk stamps live connect grit punch-marked", kaDefMk.length>=1 && V.shake>0 && V.punchCover()>0 && V.plantDustK(kaDefMk[0])>0.95, `marked=${kaDefMk.length} shake=${V.shake} cover=${V.punchCover()} k=${kaDefMk[0]?V.plantDustK(kaDefMk[0]).toFixed(3):"na"}`);
let kaOrphan = false;
let kaSawCover = false;
for (let i = 0; i < 22; i++) {
  freezeYouAI(); freezeRivalAI();
  V.rival.gait = 0; V.rival.closing = false; V.rival.wantBlock = false;
  V.rival.walkT = 0; V.rival.walkFadeHold = 0; V.rival.walkSettleT = 0;
  V.player.gait = 0; V.player.walkFadeHold = 0; V.player.walkSettleT = 0;
  V.update(STEP);
  const cover = V.punchCover();
  const atkSt = (V.plantDust||[]).filter(p=>!p.speck && !p.shove && p.homeYou===true);
  const defSt = (V.plantDust||[]).filter(p=>!p.speck && !p.shove && p.homeYou===false);
  const atkUm = atkSt.filter(p=>!p.punch);
  const defUm = defSt.filter(p=>!p.punch);
  const defMk = defSt.filter(p=>!!p.punch);
  if (cover >= V.PUNCH_PX - 0.01) {
    kaSawCover = true;
    if (atkUm.length > 0) kaOrphan = true;
    if (defUm.length > 0) kaOrphan = true;
    if (defMk.length && V.plantDustK(defMk[0]) < 0.95) kaOrphan = true;
  }
}
check("knock grit leftover atk walk stamps live no unmarked mid-cover", kaSawCover && !kaOrphan, `sawCover=${kaSawCover} orphan=${kaOrphan}`);
bootPlay(); wait(50);
freezeYouAI(); freezeRivalAI();
V.openLeft = 0; V.hitstopLeft = 0;
V.plantDust.splice(0);
{
  const r = V.destRect(V.player);
  V.plantDust.push({
    x: r.pivX, y: 657, t: 0, life: 200, power: 0.8, facing: 1,
    shoveDir: 0, shove: false, vx: 0, homeYou: true, plantDX: 0, punch: false,
  });
}
const kaWalk = (V.plantDust||[]).find(p=>!p.speck && p.homeYou===true && !p.punch && p.life===200);
check("knock grit leftover atk walk stamps live walk grit stays linear", !!kaWalk && kaWalk.punch===false && kaWalk.t===0 && Math.abs(V.plantDustK(kaWalk)-1)<0.001 && V.punchCover()===0, `walk=${!!kaWalk} punch=${kaWalk&&kaWalk.punch} t=${kaWalk&&kaWalk.t} k=${kaWalk?V.plantDustK(kaWalk).toFixed(3):"na"} cover=${V.punchCover()}`);

check("cache v=350 after knock grit leftover atk walk stamps live", /game\.js\?v=350/.test(htmlTxt));

// --- Cast brasa ↔ punchCover (v350) ---
check("cast brasa cover leftover seats mid-cover fade", /Cast brasa ↔ punchCover \(v350\)/.test(srcTxt) && /spent-super plant puff/.test(srcTxt) && /BOLT_CAST_FX_MS 220/.test(srcTxt) && /linear-drain mid-cover/.test(srcTxt) && /startBolt spend freeze/.test(srcTxt) && /BOLT_SUPER_STOP/.test(srcTxt) && /hit\|grab already hold\+clear/.test(srcTxt));
check("cast brasa cover leftover hold+clear seats", /Hold cast \(\+ hit \+ grab\) brasaFxT through live punch/.test(srcTxt) && /clear when cover/.test(srcTxt) && /brasaCoverHold/.test(sliceFn(codeOnly, "update")) && /brasaFxKind === "hit" \|\| brasaFxKind === "grab" \|\| brasaFxKind === "cast"/.test(sliceFn(codeOnly, "update")) && /brasaFxT = 0/.test(sliceFn(codeOnly, "update")));
check("cast brasa cover leftover cast spawn notes cover", /Hold\+clear through punchCover \(v350\)/.test(srcTxt) && /startBolt spend arms BOLT_SUPER_STOP/.test(srcTxt) && /Empty cast never arms shake/.test(srcTxt) && /brasaFxT = BOLT_CAST_FX_MS/.test(sliceFn(codeOnly, "spawnBrasaFx")));
check("cast brasa cover leftover empty\/feint stay linear", /Empty K never arms shake — stays linear/.test(srcTxt) && /Feint \/ reversal \/ wakeup never arm shake/.test(srcTxt) && !/brasaFxKind === "feint"/.test((sliceFn(codeOnly, "update").match(/const brasaCoverHold[\s\S]{0,350}/)||[""])[0]) && !/brasaFxKind === "reversal"/.test((sliceFn(codeOnly, "update").match(/const brasaCoverHold[\s\S]{0,350}/)||[""])[0]) && !/brasaFxKind === "wakeup"/.test((sliceFn(codeOnly, "update").match(/const brasaCoverHold[\s\S]{0,350}/)||[""])[0]));
check("cast brasa cover leftover block\/clash still linear", /Block \/ clash brasa kinds still linear/.test(srcTxt) && !/brasaFxKind === "block"/.test((sliceFn(codeOnly, "update").match(/const brasaCoverHold[\s\S]{0,350}/)||[""])[0]) && !/brasaFxKind === "clash"/.test((sliceFn(codeOnly, "update").match(/const brasaCoverHold[\s\S]{0,350}/)||[""])[0]));
check("cast brasa cover leftover is juice-only", /Juice only — grit culls v348/.test(srcTxt) && /BOLT_CAST_FX_MS/.test(srcTxt) && /220 \/ BOLT_SUPER_STOP 60/.test(srcTxt) && !/brasaCoverHold/.test(sliceFn(codeOnly, "destRect")) && !/brasaCoverHold/.test(sliceFn(codeOnly, "bodyAABB")) && !/brasaCoverHold/.test(sliceFn(codeOnly, "startBolt")) && !/brasaCoverHold/.test(sliceFn(codeOnly, "bumpShake")) && !/brasaCoverHold/.test(sliceFn(codeOnly, "spawnBrasaFx")) && !/brasaCoverHold/.test(sliceFn(codeOnly, "landHit")));
check("cast brasa cover leftover constants locked", V.BOLT_CAST_FX_MS===220 && V.BOLT_SUPER_STOP===60 && V.BOLT_SUPER_STOP===V.HITSTOP_BLOCK && V.BOLT_SUPER_FX===1.55 && V.BOLT_STARTUP===200 && V.HITSTOP_HIT===140 && V.GRAB_FX_MS===90 && V.BRASA_HIT_MS===140 && V.PUNCH_PX===12);
check("grit culls v348\/v349 / short flecks v333 locked after cast brasa cover", /Knock grit leftover walk stamps ↔ punchCover \(v348\)/.test(srcTxt) && /Knock grit leftover atk walk stamps ↔ punchCover \(v349\)/.test(srcTxt) && /Short brasa flecks ↔ punchCover \(v333\)/.test(srcTxt) && /brasaFxKind === "hit" \|\| brasaFxKind === "grab" \|\| brasaFxKind === "cast"/.test(sliceFn(codeOnly, "update")) && V.HITSTOP_HIT===140 && V.PUNCH_PX===12 && /k > 0\.25/.test(sliceFn(codeOnly, "plantDustK")) && /k > 0\.25/.test(sliceFn(codeOnly, "hurtFlashK")) && /k > 0\.25/.test(sliceFn(codeOnly, "punchCover")));
check("cast brasa cover leftover tipX \/ plants \/ frames locked", /tipX: 991/.test(srcTxt) && /tipX: 0/.test(srcTxt) && /tipX: 884/.test(srcTxt) && V.STARTUP===180 && V.ACTIVE===140 && V.RECOVERY===280 && V.BOLT_STARTUP===200 && V.BOLT_RECOVERY===280 && V.BOLT_SUPER_RECOVERY===380 && V.GUARD_RAISE_MS===80 && V.PUNCH_PX===12);
check("cast brasa cover leftover is not a new combat verb", /No new combat verb/.test(srcTxt) && V.SLASH_DMG===10 && V.BOLT_SUPER_DMG===28 && V.HITSTOP_HIT===140 && V.HITSTOP_BLOCK===60);
check("cache v=350 after cast brasa cover leftover", /game\.js\?v=350/.test(htmlTxt));

// LIVE: spent-super cast hold through punchCover — freeze peak; resume T held; mid-cover held; clear with cover
bootPlay(); wait(50);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.mode = "play"; V.openLeft = 0; V.hitstopLeft = 0;
V.player.hp = 100; V.rival.hp = 100;
V.player.stamina = 100; V.rival.stamina = 100;
V.player.meter = V.METER_MAX;
V.player.x = 520; V.rival.x = 720; V.keepApart();
V.player.facing = 1; V.rival.facing = -1;
V.rival.guarding = false; V.rival.wantBlock = false; V.rival.closing = false;
V.player.phase = "idle"; V.player.cut = ""; V.player.boltPhase = "";
const cbOk = V.startBolt(V.player, true);
let cbFreezeT=0, cbFreezeCover=0, cbFreezeKind="", cbFreezeScale=0, cbHit=false;
if (cbOk && V.brasaFxKind==="cast" && V.hitstopLeft>0) {
  cbHit=true; cbFreezeT=V.brasaFxT; cbFreezeCover=V.punchCover(); cbFreezeKind=V.brasaFxKind; cbFreezeScale=V.brasaFxScale;
}
check("cast brasa cover leftover live super freeze peak", cbHit && cbOk && cbFreezeKind==="cast" && cbFreezeT===V.BOLT_CAST_FX_MS && cbFreezeCover===V.PUNCH_PX && cbFreezeScale===V.BOLT_SUPER_FX && V.player.boltSuper===true && V.hitstopLeft===V.BOLT_SUPER_STOP, `hit=${cbHit} ok=${cbOk} kind=${cbFreezeKind} t=${cbFreezeT} cover=${cbFreezeCover} scale=${cbFreezeScale} hs=${V.hitstopLeft} super=${V.player.boltSuper}`);
for(let i=0;i<20;i++){ if(V.hitstopLeft<=0) break; V.update(STEP); }
for(let i=0;i<3;i++){ if(V.hitstopLeft<=0 && V.shake<V.BOLT_SUPER_STOP) break; V.update(STEP); }
check("cast brasa cover leftover live super resume T held", V.hitstopLeft<=0 && V.punchCover()===V.PUNCH_PX && V.shake>0 && V.brasaFxKind==="cast" && V.brasaFxT===V.BOLT_CAST_FX_MS, `hs=${V.hitstopLeft} cover=${V.punchCover()} kind=${V.brasaFxKind} t=${V.brasaFxT} shake=${V.shake}`);
let cbMidT=-1, cbMidCover=-1;
for(let i=0;i<20;i++){
  V.update(STEP);
  if(V.shake>0 && V.shakeDur>0 && V.shake/V.shakeDur>0.25 && V.punchCover()===V.PUNCH_PX && V.brasaFxKind==="cast" && V.brasaFxT===V.BOLT_CAST_FX_MS){
    cbMidT=V.brasaFxT; cbMidCover=V.punchCover();
    break;
  }
}
check("cast brasa cover leftover live super mid-cover T held", cbMidCover===V.PUNCH_PX && cbMidT===V.BOLT_CAST_FX_MS, `cover=${cbMidCover} t=${cbMidT}`);
for(let i=0;i<50;i++){ if(V.shake<=0) break; V.update(STEP); }
check("cast brasa cover leftover live super clears with cover", V.shake===0 && V.punchCover()===0 && V.brasaFxT===0, `shake=${V.shake} cover=${V.punchCover()} t=${V.brasaFxT} kind=${V.brasaFxKind}`);

// LIVE: empty cast still linear (no punch)
bootPlay(); wait(50);
freezeYouAI(); freezeRivalAI();
V.keys.clear();
V.openLeft=0; V.hitstopLeft=0;
V.player.meter = 0;
V.player.stamina = 100;
V.player.phase = "idle"; V.player.cut = ""; V.player.boltPhase = "";
const cbEmptyOk = V.startBolt(V.player, false);
const cbEmptyT0 = V.brasaFxT;
check("cast brasa cover leftover live empty arms linear", cbEmptyOk && V.brasaFxKind==="cast" && cbEmptyT0===V.BOLT_CAST_FX_MS && V.shake===0 && V.punchCover()===0 && V.player.boltSuper===false && (V.brasaFxScale||1)===1, `ok=${cbEmptyOk} kind=${V.brasaFxKind} t=${cbEmptyT0} shake=${V.shake} super=${V.player.boltSuper}`);
for(let i=0;i<3;i++) V.update(STEP);
check("cast brasa cover leftover live empty fades linear", V.brasaFxKind==="cast" && V.brasaFxT<cbEmptyT0-STEP*2 && V.brasaFxT>0 && V.shake===0, `t=${V.brasaFxT} t0=${cbEmptyT0}`);

// LIVE: cast no-punch synthetic still linear if armed without shakeDur
bootPlay();
V.openLeft=0; V.hitstopLeft=0;
freezeYouAI(); freezeRivalAI();
V.bumpShake(0,1,0);
V.spawnBrasaFx("cast", 640, 360, 1, V.player);
const cbNpT0=V.brasaFxT;
check("cast brasa cover leftover live cast no-punch arms", cbNpT0===V.BOLT_CAST_FX_MS && V.brasaFxKind==="cast" && V.shake===0 && V.punchCover()===0, `t=${cbNpT0} kind=${V.brasaFxKind}`);
for(let i=0;i<3;i++) V.update(STEP);
check("cast brasa cover leftover live cast no-punch fades linear", V.brasaFxT<cbNpT0-STEP*2 && V.brasaFxT>0 && V.shake===0, `t=${V.brasaFxT} t0=${cbNpT0}`);

check("cache v=350 after cast brasa cover leftover live", /game\.js\?v=350/.test(htmlTxt));


console.log("pass", pass.length, "fail", fail.length);
fail.forEach(f=>console.log("  FAIL", f));
process.exit(fail.length?1:0);

